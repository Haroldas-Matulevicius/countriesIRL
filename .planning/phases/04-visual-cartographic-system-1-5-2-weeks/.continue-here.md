# Continue here — Phase 4, paused after wave 2's first plan

**Paused 2026-08-06 at the owner's request** (usage headroom), immediately after `04-02` landed.
Nothing is mid-flight: no agent is running, the working tree is clean apart from one pre-existing
untracked file, and every landed plan has a committed SUMMARY.

## Position

**2 of 16 plans complete.** HEAD is on `master`; there is no phase branch (`branching_strategy: none`).

| Plan | Wave | State | Commits |
|---|---|---|---|
| `04-01` tracer | 1 | ✅ done | `42b2f0d` `fdda760` `873474f` `6d5b126` |
| `04-02` ramp model | 2 | ✅ done | `8452425` `e0053fd` `a58b978` `828743b` |
| `04-03` neutral units colourable | 2 | ⬅ **NEXT** | — |
| `04-04` … `04-16` | 3–13 | ○ pending | — |

Gate at pause: lint clean · **679/679 unit** (45 files) · `tsc -b` + build clean · 105 Playwright
(installed Chrome only) · `data:world:check` PASS.

## Next action

```
/gsd-execute-phase 4
```

Run `04-03` next. **Its owner gate is already decided — do not re-derive it, and do not stop on it:**

> **`policy-b-third-value`** — keep the twelve units non-core and add an explicit third
> `colorPolicy` value. `policy.coreStateCount` stays **195** and stays factually true;
> `coreDefinition` is untouched; a second count `selectableCount: 207` appears alongside it.
> Route (A) is measured **blocked outright**: `GIB` is a 10m supplement and `createCanonicalBytes`
> resolves core records only against the 50m `baseFeatures` index, and Antarctica is not a UN
> member state so promotion would falsify `coreDefinition`.
> **Creator-visible consequence accepted:** the twelve become clickable and paintable where they
> previously swallowed the click with no feedback. Every existing saved map keeps working; no
> creator data is lost.

`04-03`'s Task 2 precondition — network reach to `raw.githubusercontent.com` for
`npm run data:world:check` — was **verified reachable at pause (HTTP 200)**. Re-check it; it is a
live-network dependency, not a settled fact.

## Critical Anti-Patterns

| # | Anti-pattern | Severity | Note |
|---|---|---|---|
| 1 | Running `state.advance-plan`, `state.update-progress`, or `roadmap.update-plan-progress` | **blocking** | They infer status from file presence and have already zeroed this repo's progress counters, deleted its activity log, and marked an incomplete plan complete. `STATE.md` and `ROADMAP.md` are edited **by hand**, by the orchestrator only — executors must not touch either. |
| 2 | Treating the blanket authorization as a content review, or as covering the physical checks | **blocking** | It is a **blanket, in-advance, sight-unseen proceed-authorization**: it authorizes *proceeding*, it is **not** a content review, and it is **not** hash-bound. It does **not** convert `04-13`/`04-16`'s human-verify gates into passes. `04-16`'s eight physical cells are recorded **NOT PERFORMED** unless a human performs them. Writing PASS into a cell nobody executed is fabricating evidence. Full terms: [`04-AUTHORIZATION.md`](04-AUTHORIZATION.md). |
| 3 | Landing an assertion that cannot fail on its own subject | **blocking** | Both waves so far hit this. `04-01`'s plan-prescribed RED probe **could not go red** (the test imported the very constant it was meant to diverge from) and a second probe **reddened the wrong gate**. `04-02` found its TDD RED was a `Cannot find module` failure — the throws-at-import shape, which proves no behaviour assertion can fail. Every new gate is RED-proved by breaking **its own subject**, restored by scratchpad copy-back. If it cannot be made to go red, **say so plainly instead of claiming it passes.** |

## Carried-forward facts the next session needs

- **The luminance floor is `0.2164`, not the spec's `0.216`.** A surface sitting exactly on `0.216`
  measures 4.4941:1 against ink `#111827` and fails AA. Lives in `src/utils/contrast.ts` as
  `MIN_COMPOSITION_SURFACE_LUMINANCE`.
- **`src/utils/contrast.ts` is the single home** for `relativeLuminance`, `contrastRatio`, and
  `parseHexColor`. Import it. Never a second implementation.
- **The rail-height floor is 552px, not the 540px `04-UI-SPEC.md § 6.1` estimated** — measured in
  Chrome; the pinned HUD footer is what hangs below the fold, not the seven rows. **`OQ-2` is open
  and worse than assumed**; Phase 5's `05-05` Data HUD would add an eighth row (~600px).
- **`OQ-1` is answered** (`preset-set-a`) and **Map-style undo is `undo-b-reset-action`** — history
  stays colours-only, Live Invariant 2 untouched, `Reset Map Style` ghost action instead.
- **A dead luminance band exists at `(0.183333, 0.216351)`** where neither `#FFFFFF` nor `#111827`
  clears 4.5:1. Its upper edge is the same number as the surface floor. `04-08`, `04-10`, and
  `04-11` all pick composition colours that carry ink — recorded in `coding-rules/frontend.md`.
- **The 3:1 adjacent-step ramp gate was considered and NOT proposed.** The measurement does not
  support it: min 1.2944:1, max 2.5514:1, every ramp has a neighbour pair below 2:1.
- **`04-01` left one must_have unclaimed:** "the surface rect renders beneath the map skeleton" is
  not provable as written — the skeleton renders *instead of* `MapCanvas`, so the rect does not
  exist during loading.
- **A saved composition still reloads with default water.** Known Stub, creator-visible, owned by
  `04-14`'s V3 persistence work.

## Working-tree hazards

- `.planning/debug/kosovo-renders-white-uncolorable.md` is **untracked and stale** — it concludes
  the *opposite* of `D4-10` (*"which is correct and must not be changed"*). **`04-03` owns
  annotating or resolving it**, and lists it in `files_modified`. It has been deliberately left
  untracked through two waves; do not commit it separately.
- **Execution is sequential on the main working tree — no worktrees.** Chosen deliberately: 12 of
  13 waves are single-plan so isolation buys almost nothing, `04-03` needs the untracked file above
  (invisible inside a worktree), and this repo has already lost agent edits to worktree handling
  twice in one session. If you switch to worktrees, commit the kosovo file first.
- **Executors run on Opus, not the `executor_model: sonnet` in `.planning/config.json`** —
  `CLAUDE.md` § Model Routing puts the workhorse on Opus and overrides the config.
