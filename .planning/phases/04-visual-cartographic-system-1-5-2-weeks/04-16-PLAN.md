---
phase: 04-visual-cartographic-system-1-5-2-weeks
plan: 16
type: execute
wave: 13
depends_on: [04-15]
files_modified:
  - .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-16-REVIEW.md
  - .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-ACCEPTANCE.md
  - .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - CLAUDE.md
  - src/styles/uiContract.test.ts
autonomous: false
requirements: [D4-14, D4-15, D4-04, D4-05, D4-10]
estimate:
  tokens: 96000
  raw_tokens: 48000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "An **independent, non-author** review of the phase's aggregate diff has been performed and recorded, at the same bar as `03-11` / `03-12`. Executor self-reported checkpoints are not trusted for integration here — non-author review of an aggregate diff caught five real defects across three rounds on a single Phase 2 stack that the executor had already marked resolved."
    - "`package.json` and `package-lock.json` are byte-unchanged across the whole phase, verified by a **range diff against the phase-start SHA** — not by `git diff --quiet HEAD`, which is recorded in `CLAUDE.md` as a check that **passed silently on a committed change**, the exact threat it existed to catch."
    - "Browser scope is stated plainly: **installed Chrome 151.0.7922.75 only.** Microsoft Edge is **not installed on this machine**, the `msedge` project cannot launch, and **no Edge result may be produced or cited.** Firefox, Safari, and previous-version certification have never been run here."
    - "The five physical accessibility checks (A9 screen reader, A10 physical 200% zoom, A11 dark-theme review, A12 latin-ext diacritic export, A13 the D-5 confirmation above 1200px) and the three manual-only verifications are **performed by a human or recorded as NOT PERFORMED.** None may be inherited from Phase 3, where nine of twelve UAT cells were skipped — **skipped is not passed.**"
    - "Every open question that is still open at phase close is listed as open, with what would close it."
    - "`STATE.md` and `ROADMAP.md` are edited **by hand**. The gsd-sdk verbs `state.advance-plan`, `state.update-progress`, and `roadmap.update-plan-progress` are **never run** — they infer status from file presence and have already zeroed this repository's progress counters, deleted its activity log, and marked an incomplete plan complete."
  artifacts:
    - .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-16-REVIEW.md
    - .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-ACCEPTANCE.md
  key_links:
    - "the phase-start SHA -> `git diff <sha>..HEAD -- package.json package-lock.json` -> the zero-new-packages claim. A range diff, never a working-tree diff."
    - "each `04-NN-SUMMARY.md`'s recorded RED proofs -> the reviewer re-running them rather than copying the numbers."
---

<objective>
Close the phase honestly: an independent non-author review of the aggregate diff, a dependency gate
that can actually fail, the physical checks nobody can automate, and hand-edited status files.

Purpose: **executor self-reported checkpoints have proved unreliable in this project.** `STATE.md`
records that independent non-author review of an aggregate diff caught **five real defects across
three rounds on a single stack** that the executor had already marked resolved, and that Phase 3
caught **seven assertions that could not fail** before landing, plus two inside a plan's own verify
block. This plan is the same bar as `03-11` / `03-12`.

It is also where Phase 4 avoids repeating Phase 3's closing mistake. Phase 3 was closed by the owner
on a free exploration with the structured UAT **skipped**, leaving nine cells never performed. Those
cells cannot be inherited. Phase 4's own physical checks must be **performed here or recorded as NOT
PERFORMED** — never assumed, never substituted with an automated result.

Output: `04-16-REVIEW.md`, `04-ACCEPTANCE.md`, a RED-proved dependency gate, and hand-edited status files.
</objective>

<phase_requirement_substitution>
Phase 4 has **no requirement IDs**; `requirements` carries `04-CONTEXT.md` decision IDs. Full
rationale in `04-01-PLAN.md`. The review must confirm that **all eighteen decisions D4-01 … D4-18** are
accounted for — implemented, or explicitly deferred with a reason — and that this substitution is
recorded in the close-out so a later reader does not read it as dropped requirements.
</phase_requirement_substitution>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/STATE.md
@.planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-CONTEXT.md
@.planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-VALIDATION.md
@.planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: A dependency gate that can fail, and the browser-scope statement</name>
  <files>src/styles/uiContract.test.ts</files>
  <read_first>
    - .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-PATTERNS.md § No Analog Found
      (the `package.json`-unchanged gate has **no precedent**, and the closest attempt is explicitly
      recorded as a gate that passed silently)
    - CLAUDE.md § Guardrails (the recorded failure: *"a `git diff --quiet HEAD` evidence check that
      passes silently on a **committed** change — the exact threat it existed to catch"*)
    - .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-UI-SPEC.md § 14 (Registry Safety —
      zero new npm packages; `DropletIcon` vendored, not installed; font bytes vendored, not installed)
    - .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-RESEARCH.md § Package Legitimacy Audit
    - package.json (confirm the current dependency and devDependency sets)
  </read_first>
  <action>
Identify the **phase-start SHA** — the commit immediately before `04-01`'s first commit — and record it
verbatim in the SUMMARY and in `04-16-REVIEW.md`. Every dependency claim in this phase is relative to
that SHA.

Add a gate asserting `package.json` and `package-lock.json` are unchanged across the phase. **It must be
a range diff against the phase-start SHA**, i.e. `git diff <phase-start-sha>..HEAD -- package.json package-lock.json`
producing empty output. **Do not use `git diff --quiet HEAD`.** `CLAUDE.md` records that exact shape as
a gate that **passed silently on a committed change** — it compares the working tree against the last
commit, so a change that has already been committed is invisible to it. That is the precise failure this
gate exists to prevent, and `04-PATTERNS.md` flags it as having no safe precedent in this repo.

Land the gate where it runs in CI-equivalent form: a Vitest case in `src/styles/uiContract.test.ts` that
shells out to `git`, reading the phase-start SHA from a named constant with a comment recording what it
is and why. If shelling out from a `node`-environment Vitest case proves awkward, put it in a small
script the full-gate command chain runs and **say plainly in the SUMMARY which route was taken** — do
not leave it as an unrun claim.

**RED-prove it on its own subject, as a required step, and prove specifically the failure mode
`git diff --quiet HEAD` misses.** In a scratchpad-backed working copy: add a harmless devDependency to
`package.json`, **commit it**, run the gate, and confirm it goes **red** — a working-tree diff would pass
here, which is the whole point. Record the verbatim failure. Then reset that commit, restore by
**scratchpad copy-back**, and confirm with `git status` and `git log`. **Never `git checkout --` a file
with uncommitted work.**

Also assert and record the **browser scope**, as a stated fact rather than an inference: automated
browser evidence for Phase 4 comes from **installed Chrome 151.0.7922.75 only**. **Microsoft Edge is
not installed on this machine** — the `msedge` Playwright project cannot launch, so no Edge result may
be produced or cited. Firefox and Safari have never been run here. Phase 1/2 evidence recording "Edge
150" is **immutable — annotate it, never rewrite it** — and until that contradiction is explained, no
phase may cite it.
  </action>
  <verify>
    <automated>git diff --stat $(cat .planning/phases/04-visual-cartographic-system-1-5-2-weeks/.phase-start-sha 2>/dev/null || echo HEAD~1)..HEAD -- package.json package-lock.json && npx vitest run src/styles/uiContract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The phase-start SHA is recorded verbatim in the SUMMARY and in `04-16-REVIEW.md`.
    - The gate uses a **range** diff against that SHA —
      `grep -vE "^\s*(//|\*|/\*|#)" <gate source> | grep -c "diff --quiet HEAD"` returns 0.
      Comment lines are filtered first: `grep -c` counts comments, so a header explaining which shape
      was avoided would fail its own gate.
      <!-- planner-discipline-allow: diff --quiet HEAD -->
    - `npx vitest run src/styles/uiContract.test.ts` exits 0 with the dependency gate green.
    - The SUMMARY records the RED proof verbatim and states explicitly that the mutation was
      **committed** before the gate was run, and that the gate still went red.
    - `git log --oneline` after restoration shows no leftover mutation commit, and `git status` is clean.
    - The SUMMARY states the browser scope in the words above, with no Edge, Firefox, or Safari result
      claimed anywhere.
  </acceptance_criteria>
  <done>A dependency gate exists that fails on a committed change, has been seen to do so, and the browser scope is stated rather than inferred.</done>
</task>

<task type="auto">
  <name>Task 2: Independent non-author review of the aggregate diff</name>
  <files>.planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-16-REVIEW.md</files>
  <read_first>
    - .planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-12-REVIEW.md (the format and bar to match —
      it **re-ran every gate rather than copying numbers**)
    - every `.planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-NN-SUMMARY.md`
    - .planning/coding-rules/general.md § Live Invariants and § Immutable Safety Constraints
    - .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-CONTEXT.md § Implementation Decisions
      (all eighteen) and § Open Questions
    - .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-UI-SPEC.md §§ 12, 13
      (the fourteen `[ASSUMED]` rows and the seven carried contract disagreements)
  </read_first>
  <action>
**This review must be performed by an agent that did not author the code.** Executor self-reports are
not trusted for integration in this project, and the reason is recorded rather than theoretical.

Produce `04-16-REVIEW.md` covering the phase's **aggregate diff** from the phase-start SHA to HEAD:

1. **Re-run every gate. Do not copy a number from a SUMMARY.** `03-12`'s review re-ran everything and
   the honesty audit found zero overclaims; that is the bar. Record each command and its real output.
2. **Verify every RED proof was real.** For a sample of at least six across the phase — including
   `04-08`'s coastline proof, `04-10`'s band-inversion proof, `04-13`'s no-data-divergence proof,
   `04-14`'s G-2 proof, `04-15`'s blank-frame proof, and Task 1's committed-change proof — re-perform
   the mutation and confirm the assertion reddens **on its own subject**, then restore by scratchpad
   copy-back. Any proof that cannot be reproduced is a finding.
3. **Hunt the recorded failure shapes.** Search the phase's new assertions for: a tolerance that passes
   against its own probe; a row count written as `a.length * b.length`, green at zero rows; a probe
   that throws at import so its assertion never runs; a probe that reddens a **different** gate than the
   one being proven; a gate whose subject the browser neutralises anyway; and a working-tree diff used
   as an evidence check. All six have shipped in this repository.
4. **Check the Live Invariants.** Confirm 1, 2, 3, 4, 6, 7, and 9 are unregressed; confirm 5's text was
   **amended** (CD-5) and 8's was recorded **retired**, not deleted (CD-4).
5. **Check the Immutable Safety Constraints.** In particular: no approval was inferred or self-approved;
   nothing reads as though a historical snapshot shipped; D4-10 is recorded as a product-policy change on
   already-shipped hash-verified geometry and **never** as a bypassed approval; and no Edge, Firefox, or
   Safari result is claimed anywhere.
6. **Decision coverage.** Confirm each of D4-01 … D4-18 is implemented or explicitly deferred with a
   reason. Confirm no **Deferred Idea** from `04-CONTEXT.md` was implemented — per-country value labels,
   Malta-style inset boxes, revisiting `F-1`, and creator-adjustable ramp step count are all out of scope
   and must not appear.
7. **Contract disagreements and assumptions.** Confirm CD-1, CD-2, CD-3, CD-4, CD-5, CD-6, CD-7, CD-8,
   CD-10, and CD-11 each landed their annotation or amendment **in the same commit as the behaviour**.
   Confirm every `[ASSUMED]` row from `04-UI-SPEC.md § 12` that shipped is recorded as an assumption in a
   SUMMARY and **not** cited anywhere as an owner decision.
8. **Resolve the stale debug artifact.** Confirm `04-03` annotated
   `.planning/debug/kosovo-renders-white-uncolorable.md` as superseded and tracked it (CD-10). If it is
   still untracked or still reads as current guidance, that is a finding.
9. **Findings.** List every defect with severity and the file and line. **Do not fix them in this task** —
   a reviewer who fixes is no longer independent. Hand fixes back as follow-up work.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-16-REVIEW.md && npm run lint && npm test && npm run build && npm run test:e2e -- --project=chrome && npm run data:world:check</automated>
  </verify>
  <acceptance_criteria>
    - `04-16-REVIEW.md` exists and states which agent performed it and that it did not author the code.
    - Every gate's command and **real output** is recorded — not a number copied from a SUMMARY.
    - At least **six** RED proofs were re-performed, each named, with the observed failure recorded, and
      each restored by scratchpad copy-back with a clean `git status`.
    - All six recorded failure shapes were searched for, with the search method stated and the result
      recorded — including "none found" where that is the answer.
    - Live Invariants 1-9 are each individually addressed, with 5 amended and 8 retired.
    - All eighteen D4 decisions are individually accounted for.
    - No Deferred Idea appears in the shipped diff — verified by grep for value labels, inset boxes, and
      an adjustable ramp step count.
    - Every finding carries a severity, a file, and a line, and **none is fixed in this task**.
  </acceptance_criteria>
  <done>An agent that did not write the code has re-run every gate, re-performed six RED proofs, hunted the six recorded failure shapes, and listed its findings without fixing them.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" autonomous="false">
  <name>Task 3: OWNER GATE — the eight checks only a human can perform</name>
  <what-built>
The whole phase: ramps replacing the flat swatch grid, a redesigned Colors panel at 360px, a new
`Map style` tool with water / uncoloured fill / borders / bands, interior-border rendering with quiet
coastlines, gradient bands, title and subtitle and attribution text, a chrome-free legend in two forms
sitting below the title block, latin-ext font coverage, twelve previously-uncolourable units now
colourable, and V3 persistence.
  </what-built>
  <how-to-verify>
`npm run dev`, then work through the eight cells below. **Record each as PASS, FAIL, or NOT PERFORMED.
"Not performed" is a legitimate answer and is what must be written if a check is skipped — it is never
recorded as a pass.**

**⛔ Five physical accessibility checks (`04-UI-SPEC.md § 8`). None may cite a Phase 3 result — nine of
Phase 3's twelve UAT cells were never performed, including every one of these.**

1. **A9 — screen-reader pass** over the ramp strip, the `Map style` panel, and the text tools. Do the
   accessible names actually make sense read aloud? A ramp segment should read
   `Apply Blues shade 3 of 5`.
2. **A10 — physical 200% browser zoom** with a panel open at 360px. ⚠ A halved CSS viewport is the
   *equivalent* and must be labelled as such — **never** recorded as physical zoom.
3. **A11 — dark-theme visual review** of both new panels and the ramp strip. Automated contrast is A3
   and is a different claim.
4. **A12 — latin-ext diacritic export.** Set a title and a legend label containing `Košice`, `Łódź`, and
   `Magyarország`, export, **open the PNG, and look at the glyphs.** Confirm no glyph falls back
   mid-string. The automated half only proves the two font faces parse and that something changed — it
   is not this claim.
5. **A13 — the rail at and above the stated 540px floor.** Playwright covers 1280x540; the
   **D-5 confirmation at 1200px or wider** was never performed in Phase 3 and is not inherited.

**⛔ Three manual-only verifications (`04-VALIDATION.md`). They are manual because they cannot be
automated — not as licence to substitute an automated result.**

6. **The G-3 rework.** Open the Colors flyout at 360px, exercise ramp selection and manual painting, and
   compare against your original complaint: *"too squished, not organized well, hate the multi boxes
   within."* Is it answered?
7. **Cartographic resemblance.** Build the reference frame — ramp fills, quiet coasts, top band, title,
   bar legend — and compare side by side with your Eurostat image.
8. **Anything in the exported PNG that differs from what you saw on screen.** This is the class of defect
   the whole phase's export gating exists to catch, and a human eye catches shapes a pixel probe does not.
  </how-to-verify>
  <resume-signal>
Reply with eight results — `PASS`, `FAIL` with a description, or `NOT PERFORMED` — numbered 1 to 8.

⚠ **This gate cannot be delegated or auto-approved, and an automated result may never be substituted for
a physical claim.** A blanket, in-advance, sight-unseen authorization authorizes **proceeding**; it is
**not** a content review and it is **not** hash-bound (Immutable Safety Constraint 8). If that is what
is given, every unperformed cell is recorded as **NOT PERFORMED** in `04-ACCEPTANCE.md`, the phase closes
as **shipped at code level and physically unverified**, and `STATE.md` says so in those words — exactly
as Phase 3 was recorded. **Writing PASS into a cell nobody executed would be fabricating evidence.**
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Close the phase honestly — acceptance record, status files, and routing</name>
  <files>.planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-ACCEPTANCE.md, .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-VALIDATION.md, .planning/STATE.md, .planning/ROADMAP.md, CLAUDE.md</files>
  <read_first>
    - .planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-UAT.md (the acceptance-record format, and how
      skipped cells were recorded as skipped rather than passed)
    - .planning/STATE.md (the whole file — the Current Position, Pending Todos, and Blockers sections
      this must update **by hand**)
    - .planning/ROADMAP.md § Phase 4 and § Progress (the canonical status and counts)
    - CLAUDE.md § Orientation, § Stack & Architecture, § Guardrails, § Update Process (the
      two-most-recent-entries rule)
    - .planning/coding-rules/general.md § Planning-file safety
  </read_first>
  <action>
Write `04-ACCEPTANCE.md` recording Task 3's eight results verbatim, with the date, who performed each,
and **which kind of authorization** was given — a blanket, in-advance, sight-unseen proceed-authorization
is **not** a content review and is **not** hash-bound, and the record must say which one it was.
**Any cell not performed is recorded as NOT PERFORMED.** Skipped is not passed.

Update `04-VALIDATION.md`'s frontmatter: set `status: validated` and `nyquist_compliant` **only if**
Task 2's review confirmed every row of the per-decision map is covered by a landed, RED-proved gate. If
any row is not, leave the flags false and list the gaps. A plan marking its own phase compliant is the
self-report this project has learned not to trust — this is set **after** the independent review, on its
evidence.

Update `.planning/STATE.md` and `.planning/ROADMAP.md` **by hand**. ⚠ **Never run the gsd-sdk verbs
`state.advance-plan`, `state.update-progress`, or `roadmap.update-plan-progress`** — they infer status
from file presence and have already zeroed this repository's progress counters, deleted its activity log,
and marked an incomplete plan complete. Use scoped `Edit` calls, never a whole-file `Write` on either.

In `STATE.md`: the new Current Position with the real gate numbers; the browser scope stated plainly;
every open item carried forward with what would close it — **OQ-1** (if the water preset list was answered
by a blanket authorization rather than a choice), **OQ-2 / D-5** (the rail below 540px, still open, and
Phase 5's eighth row), **OQ-3** (whether `G-1` is resolved), **OQ-4** (proportional versus classed,
surfacing at Phase 5 planning), **OQ-5** (the mixed-map legend default), every `[ASSUMED]` row from
`04-UI-SPEC.md § 12` that shipped, and every finding from `04-16-REVIEW.md` that was not fixed. Carry
forward, unchanged, the two open **Phase 2** owner gates (`02-25`, `02-28`), the historical-snapshot
deferral for missing rights-cleared source material, the immutable Edge-record contradiction, and the
storage validator that still admits all five snapshot ids.

In `ROADMAP.md`: Phase 4's plan list and status, and the § Progress counts. Confirm the `05-02` amendment
from `04-03` and the `04-05` and `04-08` amendments from `04-09` and `04-13` are all present and dated.

Update `CLAUDE.md` per its own § Update Process — propose the exact edit, bump the "Last updated" date,
and keep only the **two most recent** entries, merging the two oldest in the same edit if a third would
appear. What changed and belongs in the routing table: the composition layer stack and the new
`Map style` tool; `src/utils/ramps.ts`, `contrast.ts`, `bands.ts`, `compositionText.ts`, and
`constants/mapStyle.ts` in the non-obvious paths; the export clone now honouring composition border
weights rather than imposing a fixed one; the world asset's **207 colourable units alongside 195 core
states**; the legend's deleted chrome and two forms; V3 persistence; the latin-ext font's second asset
row; and the new selector-ceiling number. **Do not describe any physical check as performed unless
Task 3 recorded it as performed.**
  </action>
  <verify>
    <automated>test -f .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-ACCEPTANCE.md && npm run lint && npm test && npm run build && npm run data:world:check</automated>
  </verify>
  <acceptance_criteria>
    - `04-ACCEPTANCE.md` exists with **eight** numbered cells, each PASS, FAIL, or NOT PERFORMED, dated,
      and naming the authorization type in the words of Immutable Safety Constraint 8.
    - `grep -c "NOT PERFORMED" .planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-ACCEPTANCE.md`
      matches the number of cells the owner did not perform — no cell is silently upgraded.
    - `04-VALIDATION.md` frontmatter reflects the review's evidence, and any gap is listed.
    - `STATE.md` and `ROADMAP.md` were edited with scoped `Edit` calls —
      `git diff --stat .planning/STATE.md .planning/ROADMAP.md` shows targeted hunks, not a full-file rewrite.
    - The session transcript contains **no** invocation of `state.advance-plan`, `state.update-progress`,
      or `roadmap.update-plan-progress`.
    - `STATE.md` lists all five open questions with what would close each, plus every unfixed review finding.
    - The two Phase 2 owner gates, the historical deferral language, and the Edge-record contradiction are
      carried forward **unchanged**.
    - `CLAUDE.md` carries at most two "Last updated" entries and describes no physical check as performed
      unless `04-ACCEPTANCE.md` says it was.
    - `grep -rn "Edge" CLAUDE.md .planning/STATE.md | grep -vi "not installed\|never\|immutable\|annotate"`
      returns nothing that reads as an Edge pass.
  </acceptance_criteria>
  <done>The phase is closed with its physical checks recorded as performed or not performed, its open questions listed, its status files hand-edited, and no claim anywhere that outruns its evidence.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
| Kind | Symbol / path |
|---|---|
| Doc | `04-16-REVIEW.md` — independent non-author aggregate-diff review with re-run gates, six re-performed RED proofs, and findings |
| Doc | `04-ACCEPTANCE.md` — eight physical/manual cells, each PASS / FAIL / NOT PERFORMED, with the authorization type named |
| Gate | A `package.json` / `package-lock.json` **range diff** against the phase-start SHA, RED-proved against a **committed** change |
| Record | The phase-start SHA, recorded verbatim |
| Frontmatter | `04-VALIDATION.md` `status` and `nyquist_compliant`, set on the review's evidence |
| Doc | `STATE.md`, `ROADMAP.md`, and `CLAUDE.md` updated by hand |

**New npm packages: ZERO — and this plan is where that is proven rather than asserted.**
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the phase's aggregate diff -> the reviewer | The reviewer must not be the author; a self-report is not evidence. |
| the owner's physical observations -> the acceptance record | The one input no automation can produce. |
| npm registry -> the repository | Verified as untouched, rather than assumed untouched. |

**Calibration, stated plainly:** this plan ships **no product code**. It is a review, a gate, and a set
of records in a browser-only, localhost-only app. There are **no applicable product-runtime threats for
this surface.** The threats here are to the **integrity of the phase's evidence and of its status
files**, which is what the register records — and in this repository those are not hypothetical: the
status files have been corrupted before, and gates that could not fail have shipped before.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16-01 | Repudiation | An unperformed physical check recorded as a pass | high | mitigate | Task 3's resume-signal permits `NOT PERFORMED` as a legitimate answer and Task 4's acceptance criteria count those cells. **Writing PASS into a cell nobody executed would be fabricating evidence**, and the record names the authorization type so a blanket proceed-authorization can never read as a content review. |
| T-04-16-02 | Repudiation | A self-reported review passing defects through | high | mitigate | The review must be performed by an agent that did not author the code, must re-run every gate rather than copy numbers, and must re-perform at least six RED proofs. Its findings are **not fixed in the same task** — a reviewer who fixes is no longer independent. |
| T-04-16-03 | Tampering | Supply chain — a package added mid-phase and committed | high | mitigate | A **range diff against the phase-start SHA**, RED-proved by committing a devDependency and watching the gate go red. `git diff --quiet HEAD` is explicitly forbidden because it is recorded as having passed silently on exactly this. |
| T-04-16-04 | Tampering | The gsd-sdk status verbs corrupting `STATE.md` / `ROADMAP.md` | high | mitigate | Named and forbidden in Task 4's action, and the acceptance criteria require the transcript to contain no invocation of the three verbs and require scoped `Edit` hunks rather than a full-file rewrite. |
| T-04-16-05 | Spoofing | Citing a browser result that cannot exist | high | mitigate | Browser scope is stated as a fact in Task 1 and grep-checked in Task 4. **Edge is not installed**, so no Edge result may be produced or cited; the Phase 1/2 "Edge 150" record is immutable and must be annotated, never rewritten, and no phase may cite it until the contradiction is explained. |
| T-04-16-06 | Repudiation | An open question quietly closing at phase end | medium | mitigate | Task 4 requires all five open questions to be listed with what would close each, plus every shipped `[ASSUMED]` row and every unfixed review finding. |
| T-04-16-SC | Tampering | npm / lockfile | high | mitigate | **This plan is the phase's supply-chain gate.** Zero installs across the phase, proven by range diff. `DropletIcon` and the two font files are **vendored, not installed**, each with an in-repo provenance record and a locally computed SHA-256. `04-RESEARCH.md § Package Legitimacy Audit` records zero package-manager install tasks, so no `[ASSUMED]` or `[SUS]` package exists and no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `npm run lint && npm test && npm run build && npm run test:e2e -- --project=chrome && npm run data:world:check` all green, with **real output** recorded by the reviewer rather than copied.
- **Installed Chrome 151.0.7922.75 only.** Microsoft Edge is **not installed on this machine**; the
  `msedge` project cannot launch and **no Edge result may be produced or cited.** Firefox, Safari, and
  previous-version certification have never been run here.
- Six RED proofs re-performed by the reviewer, each on its own subject.
- The dependency gate RED-proved against a **committed** change.
- The eight physical/manual cells recorded as PASS, FAIL, or **NOT PERFORMED** — never assumed.
- `STATE.md` and `ROADMAP.md` hand-edited; the three destructive gsd-sdk verbs never run.
</verification>

<success_criteria>
- An agent that did not write the code re-ran every gate and listed its findings without fixing them.
- The zero-new-packages claim is proven by a gate that has been seen to fail on a committed change.
- Every physical check is recorded as performed or not performed, and none is inherited from Phase 3.
- Every open question, shipped assumption, and unfixed finding is carried forward in `STATE.md` with
  what would close it.
- Nothing in the close-out claims more than its evidence supports.
</success_criteria>

<output>
Create `.planning/phases/04-visual-cartographic-system-1-5-2-weeks/04-16-SUMMARY.md` when done.
</output>
