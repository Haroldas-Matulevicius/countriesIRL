---
phase: 3
slug: clean-ui-overhaul
status: approved
shadcn_initialized: false
preset: none
created: 2026-08-06
approved: 2026-08-06
checker_flags_resolved: 7
upstream_design_system: /Users/matul/claudeprojects/themely/Design.md (sibling repo, read-only)
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for the Clean UI Overhaul. Generated by gsd-ui-researcher,
> verified by gsd-ui-checker. Consumed by `gsd-planner` (task authoring), `gsd-executor`
> (visual source of truth), and `gsd-ui-auditor` (retroactive audit).

## How to read this file

- **`03-CONTEXT.md` D-01…D-35 are owner-locked.** This file does not revisit them. Where a row
  below carries a `D-nn` tag, the decision is quoted from CONTEXT and the row only adds the
  per-surface detail CONTEXT delegated to Claude's Discretion.
- **Rows tagged `[SPEC]`** are decisions this document makes under
  `03-CONTEXT.md` § Claude's Discretion: *"exact per-surface application of the Themely recipes
  to CountriesIRL-specific chrome that Themely has no analog for — colour swatch grid, legend
  editor rows, the map's own `--map-*` tokens, saved-map row anatomy"*, plus icon selection.
- **There are no `[EXCEPTION]` rows.** The draft carried one — a 3.02:1 white-on-accent label in
  dark mode — and the owner resolved it during checker review rather than accepting it, by making
  the Export fill mode-invariant. Every text-on-surface pair in this contract meets WCAG AA in
  both modes. If a future revision must accept a standard it cannot meet, tag the row
  `[EXCEPTION]`, give it a measured value, and name the change that would clear it.
- `03-01` produces CountriesIRL's own `Design.md` (D-02). **This file is that document's source
  brief**, not its replacement: `Design.md` vendors the token table verbatim and carries the
  Tailwind→CSS translation notes; this file carries the contract the gates assert.

## Provenance

| Source | Read this session | Used for |
|---|---|---|
| `03-CONTEXT.md` | full | D-01…D-35, all locked |
| `03-RESEARCH.md` | §Standard Stack, Patterns 1/3/4/5/7/8, §Current-State Inventory, §Pitfalls P-2…P-5, OQ-2/3/4/5 | token inventory, retired-name lists, gate shapes |
| `.planning/ROADMAP.md` §Phase 3 | full | plans `03-01`…`03-10` and their gates |
| `.planning/REQUIREMENTS.md` | full | NFR5/6/7/11, F4, F5 |
| `.planning/coding-rules/{general,frontend,export}.md` | full / §Styling+§Visual Tokens+§Map Chrome+§Global Action Strip+§Creator-Safe Copy / §Size Contract+§Clone Contract | live invariants, export firewall |
| `themely/Design.md` | full | palette, type scale, radii, shadows, motion, component recipes |
| `themely/src/app/globals.css` | token blocks (`:root` 173-258, `.dark` 319-357) | **authoritative** where it disagrees with Design.md — no disagreement found in the token families this phase adopts |
| `themely/src/components/app-sidebar.tsx` | `PrimaryNavRow` 584-630, `DisabledNavRow` 637-667 | rail row recipe |
| `themely/src/lib/motion/tokens.ts` | full | motion TS mirror shape |
| `themely/src/components/ui/search.tsx` | full | vendored animated-icon shape + strokeWidth marker |
| CountriesIRL source | `theme.css`, `snapshots.ts`, `periods.ts`, `CompositionBar.tsx`, `Tooltip.tsx`, `ToastRegion.tsx`, `MapNavigation.tsx`, `export.ts:255-292`, `MapCanvas.css:85-120` | current-state facts asserted below |

---

## Design System

| Property | Value |
|----------|-------|
| Tool | **none** — hand-authored CSS custom properties in `src/styles/theme.css` |
| Preset | not applicable |
| Component library | none (no Radix, no Base UI, no shadcn) |
| Styling | plain CSS files imported by `main.tsx`; **no Tailwind, no CSS-in-JS** (`coding-rules/frontend.md` §Styling) |
| Icon library | **vendored** lucide-animated components (D-28), authored in-repo under `src/components/icons/` |
| Font | Inter Variable, **self-hosted woff2**, vendored bytes (D-09; no Google Fonts `@import`) |
| Animation runtime | `motion` **pinned exact `12.40.0`** (D-27; byte-matches the repo the icons are vendored from) |

### shadcn gate — resolved by upstream, not asked

`components.json` is absent and the stack is React + Vite, which normally triggers the shadcn
initialization gate. **The gate is answered `none` by locked upstream decisions and was not put
to the user**, because initializing shadcn would contradict four of them at once:

1. **D-01/D-02/D-03** adopt the Themely token system *vendored into this repo* as `--themely-*`
   CSS custom properties. shadcn ships its own semantic tokens (`bg-card`, `border-border`);
   Themely's own `Design.md:66` forbids reaching for them — *"their palette doesn't match
   Themely's."*
2. **shadcn requires Tailwind.** Every Themely recipe must be *translated* to CSS, not copied
   (`03-RESEARCH.md` §P-5); adding Tailwind is a build-system change nobody authorized.
3. **Roadmap Amendment 3** already counts `motion` + vendored icons as the phase's dependency
   budget, entering a phase specced as "chrome, layout, and tokens only". A third surface would
   be undocumented divergence.
4. `coding-rules/frontend.md` §Styling binds plain CSS files.

**Consequence for the registry safety gate:** the shadcn registry vetting protocol is not
applicable. It is replaced by the **vendoring provenance gate** in § Vendoring Safety below,
which is the same protocol applied to the real supply-chain surface this phase has.

---

## Design Tokens

### Namespace rule (D-03)

Colour tokens use Themely's `--themely-*` names **verbatim**; type roles use `--text-*`; motion
uses `--motion-*`. Families Themely does not namespace (spacing, focus, radii, and the
CountriesIRL-only `--map-*` / `--tooltip-*` export set) keep their existing local names. A host's
`globals.css` can therefore become the token source and our declarations act as fallbacks.

### Colour — light (`:root`)

Verbatim from `themely/src/app/globals.css:178-199`. **No value may be adjusted.**

| Token | Value | Role in CountriesIRL |
|---|---|---|
| `--themely-platinum` | `#ffffff` | Editor wall — the full-bleed shell background behind the rail and panel |
| `--themely-porcelain` | `#f8fafd` | Card / row surface: rail hover, panel body, saved-map rows, empty states, icon chips |
| `--themely-powder` | `#e5edf5` | Active surface: the open tool's rail row, the selected colour tile, the selected legend position |
| `--themely-apple-blue` | `#0071e3` | Sole saturated accent — see § Accent budget |
| `--themely-apple-blue-hover` | `#005db8` | Accent hover only |
| `--themely-midnight-ink` | `#061b31` | Primary text, panel titles, primary icon fill |
| `--themely-slate-blue` | `#50617a` | Secondary text, descriptions, helper labels |
| `--themely-nav-ink` | `#0d0d0d` | Rail row text/icons — **constant across inactive/hover/active** (D-29) |
| `--themely-ghost-gray` | `#64748d` | Tertiary meta: saved-map timestamps, entry counts, placeholders |
| `--themely-stone-gray` | `#d8d6df` | Hairline borders and dividers — always consumed at 60% for card borders |
| `--themely-red` | `#ff5252` | Destructive only: `Delete Saved Map`, error banners. Never decorative |
| `--themely-on-accent` | `#ffffff` | Text/icons on an Apple Blue or Themely Red **fill**. Identical in both modes |
| `--themely-media-backdrop` | `#000000` | Fixed black — scrims. Identical in both modes |
| `--themely-on-media` | `#ffffff` | Text/icons on dark media. Identical in both modes |

### Colour — dark (`.dark`, D-08)

Verbatim from `themely/src/app/globals.css:323-341`. The flip is a **`.dark` class on the editor
mount root**, never `document.documentElement`, and **no `prefers-color-scheme` query exists
anywhere in the dark path** (D-30).

| Token | Dark value |
|---|---|
| `--themely-platinum` | `#000000` |
| `--themely-porcelain` | `#16181c` |
| `--themely-powder` | `#1d1f23` |
| `--themely-apple-blue` | `#2997ff` |
| `--themely-apple-blue-hover` | `#1a7fd4` |
| `--themely-midnight-ink` | `#e7e9ea` |
| `--themely-slate-blue` | `#8b9099` |
| `--themely-nav-ink` | `#ffffff` |
| `--themely-ghost-gray` | `#71767b` |
| `--themely-stone-gray` | `#2f3336` |
| `--themely-red` | `#ff6b6b` |
| `--themely-on-accent` / `-media-backdrop` / `-on-media` | **unchanged** — `#ffffff` / `#000000` / `#ffffff` |

`.dark` additionally carries `color-scheme: dark` so native controls and scrollbars follow.
`export.ts:269` and `:285` set `style.colorScheme = 'light'` on the export frame and the cloned
SVG — that is deliberate export theme-independence and **must not be removed**.

### The mode-invariant (fixed) token set — the export firewall

**Live Invariant 9 outranks Themely's "tokens flip themselves" rule for this family.** These
tokens are declared **exactly once, in the unconditioned `:root`**, and appear in **no** `.dark`
block, no media query, and no `@supports` block. Redefining one makes the exported PNG follow the
viewer's theme — a defect no rendering test catches (`coding-rules/frontend.md` §Visual Tokens;
`03-RESEARCH.md` §P-2).

| Token | Value | Phase 3 disposition | Why fixed |
|---|---|---|---|
| `--map-surface` | `#ffffff` | unchanged | paints the exported square |
| `--map-fill-default` | `#ffffff` | unchanged | uncoloured country fill in the PNG |
| `--map-border-default` | `#000000` | unchanged | in the PNG; mirrors `DEFAULT_BORDER_COLOR` |
| `--map-border-hover` | `#000000` | unchanged | state is carried by stroke-**width**, never colour |
| `--map-border-selected` | `#000000` | unchanged | as above |
| `--map-border-focus` | `#0071e3` | **value changes** `#0f766e` → `#0071e3` **[SPEC]** | see note below |
| `--map-fixed-text` | `#111827` | **unchanged — stays fixed** **[SPEC]** | resolves the Discretion question: any re-tone changes exported pixels for every existing saved composition, and D-25 already spends the phase's one deliberate export-pixel change on the legend typeface. Phase 4 owns the map's visual language |
| `--map-skeleton-fill` / `-stroke` | `#e5e7eb` / `#d1d5db` | unchanged | loading state inside the square |
| `--swatch-border` | `#9ca3af` | unchanged | **mirrors the literal `LegendOverlay.tsx:313` hard-codes (`stroke="#9CA3AF"`)**. Do not "tokenize" the legend — see § Legend colour exemption |
| `--tooltip-surface` | `#061b31` | **re-toned** (D-22) | see § Tooltip |
| `--tooltip-text` | `#ffffff` | **re-toned** (D-22) | |
| `--tooltip-border` | `rgba(255, 255, 255, 0.14)` | **re-toned** **[SPEC]** | |
| `--tooltip-shadow` | `0 4px 12px -2px rgba(6, 27, 49, 0.10)` | re-toned to the popover tier (D-06) | |
| `--map-frame-edge` | `rgba(6, 27, 49, 0.55)` | **new** **[SPEC]** | D-32 export-frame hairline |
| `--map-frame-scrim` | `rgba(6, 27, 49, 0.06)` | **new** **[SPEC]** | D-32 out-of-frame dim |
| `--accent-fill` | `#0071e3` | **new** (owner-decided) | the `Export PNG` fill. Fixed so white-on-accent stays at 4.70:1 in both modes — see § The Export fill is mode-invariant |
| `--accent-fill-hover` | `#005db8` | **new** (owner-decided) | hover pair for the above |

The last two are chrome, not export, and are listed here because they share the *mechanism*
(declared once in `:root`, never in `.dark`) for a different reason: accessibility rather than
export independence. Assertion 4 covers the export family; assertion 26 covers these two.

**`--map-border-focus` re-tone [SPEC].** `#0f766e` is the retired teal. Leaving it on the map's
focus stroke would keep a second saturated hue alive in the product, which D-05 forbids
(*"no second saturated accent may be introduced"*). It moves to Apple Blue's **light** value and
stays fixed in both modes. This is safe for the export because `sanitizeExportClone` strips
`focused` classes and `data-focused` before capture (`coding-rules/export.md` §Strip semantics),
so the focus stroke never reaches a PNG. It stays dashed at 3px — the one map border state
carried by colour rather than weight, because it must read against an already-selected country.

**Deleted from the fixed set [SPEC].** `--mixed-color-light` / `--mixed-color-dark` /
`--active-check-border` / `--active-check-surface` / `--active-check-text` are **chrome**, not
export, and their fixed values go invisible under `.dark` (a `#111827` check chip on Powder
`#1d1f23`). They are **deleted, not aliased** (D-06's pattern) and their two surfaces are
re-expressed on flipping `--themely-*` tokens — see § Colour swatch grid.
`--map-shadow` is **deleted**: a full-bleed canvas has nothing to elevate (D-06 reserves shadow
for floating chrome).

### Accent budget — Apple Blue is one thing per surface (D-05)

| Surface | Its **one** Apple Blue element | Everything else on that surface |
|---|---|---|
| Tool rail (56px) | **`Export PNG`** filled button in the footer (D-13), filled from the mode-invariant `--accent-fill` | Active tool row = Powder background + Nav Ink. Theme toggle = **neutral** (D-30). Undo/Redo = neutral |
| Tool panel (280px) | the panel's **single primary action**, where it has one (`Apply Color`, `Save Map`) | Selected/active states use Powder + Midnight Ink. A panel with no primary action carries **no** accent |
| Canvas region | **none** (D-21 — the accent belongs to Export) | Floating controls, export frame, and legend chrome are neutral with Stone Gray hairlines |
| Tooltip | **none** (D-22) | dark ink chip |
| Toast region | **none** | error = Themely Red; status/warning = neutral ink on Porcelain |
| Onboarding banner | its single CTA | dismiss is a ghost button |

**Global exemption:** `:focus-visible` rings are Apple Blue on every surface and are **exempt
from the per-surface budget** — they are transient, keyboard-only, and required by NFR11. Recorded
here so the exemption cannot silently widen to hover or active states.

**Second semantic colour:** `--themely-red` only, and only for destructive actions and error
states. No third status hue exists — info is Apple Blue or no colour at all.

### The Export fill is mode-invariant — and why (owner-decided 2026-08-06)

**`Export PNG`'s fill is `--accent-fill: #0071e3` in BOTH modes**, consumed from its own
mode-invariant token rather than from the flipping `--themely-apple-blue`.

| Pair | Ratio | Verdict |
|---|---|---|
| `#ffffff` on `#0071e3` (the shipped fill, both modes) | **4.70:1** | ✅ AA for normal text |
| `#ffffff` on `#2997ff` (what the flipping token would give in dark) | **3.02:1** | ❌ below AA's 4.5:1 |
| `#ffffff` on `#1a7fd4` (`--themely-apple-blue-hover` dark) | **4.18:1** | ❌ still short |

**Why this is not a change to any locked decision.** D-13 holds — Export is still a filled Apple
Blue primary and still the rail's one accent surface. D-08 is untouched — `--themely-apple-blue`
still flips to `#2997ff` for every other consumer (focus rings, panel primary actions, the
onboarding CTA, accent-tinted chips). This is **one surface consuming a fixed value**, which is
the same mode-invariant pattern this contract already uses for `--tooltip-surface`,
`--tooltip-text`, `--map-border-focus`, and the `--map-*` family.

**Do not "simplify" this back onto the flipping token.** Pointing `--accent-fill` at
`--themely-apple-blue` looks tidier and silently reintroduces a 3.02:1 white-on-blue label in
dark mode — a failure no rendering test catches. `--accent-fill` is declared **exactly once, in
the unconditioned `:root`**, appears in no `.dark` block, and joins the mode-invariant set in
§ The mode-invariant (fixed) token set. Its hover pair is `--accent-fill-hover: #005db8`, also
fixed. Guarded by assertion 26.

There is **no contrast exception in this contract.** The matrix asserts every text-on-surface pair
at AA in both modes, and it still asserts its own row count so it cannot silently resolve to
nothing.

### Typography (D-09, D-10)

Inter Variable, self-hosted woff2 vendored into `src/assets/`. Fallback stack is Themely's
declared substitute: `system-ui, -apple-system, "Segoe UI", sans-serif`. `tabular-nums` on for
any numeric column. The four retired tokens `--font-label` / `--font-body` / `--font-heading` /
`--font-display` and their companions `--weight-regular` / `--weight-semibold` are **deleted, not
aliased** (D-10) — the role tokens bundle weight.

Each role bundles size + line-height + weight + tracking, as a CSS class the way Tailwind's
`text-<role>` utility does upstream:

| Role | Size | Line height | Weight | Tracking | Phase 3 consumers |
|---|---|---|---|---|---|
| `--text-display` | 40px | 1.10 | 700 | -0.025em | **none** — declared for D-09 parity; see exemption |
| `--text-h1` | 30px | 1.20 | 600 | -0.02em | `FatalErrorState` heading |
| `--text-h2` | 24px | 1.25 | 600 | -0.015em | Save form heading inside the Saved Maps panel |
| `--text-h3` | 18px | 1.40 | 600 | -0.01em | tool panel title; HUD composition name; empty-state heading |
| `--text-subheading` | 16px | 1.40 | 500 | 0 | onboarding banner heading; fatal-error lead line. **Never inside the 280px tool panel** — see § Legend editor rows |
| `--text-body` | 15px | 1.55 | 400 | 0 | prose: empty-state body, confirmation prompts, onboarding |
| `--text-body-sm` | 14px | 1.50 | 400 | 0 | **the workhorse** — rail rows, buttons, inputs, list rows |
| `--text-caption` | 12px | 1.40 | 400 | 0 | meta, helper text, field errors, swatch labels |
| `--text-eyebrow` | 11px | 1.30 | 500 | +0.08em | inert pills, uppercase section eyebrows |
| `--text-stat` | 30px | 1.00 | 600 | -0.02em | **none** — declared for D-09 parity; see exemption |

**Chrome subset (normative for restraint).** The editor's own chrome uses exactly four roles:
`--text-h3` (titles), `--text-body-sm` (rows, buttons, inputs), `--text-caption` (meta), and
`--text-eyebrow` (pills). Weights in chrome: 400, 500, 600. The remaining roles serve dialogs,
prose, and error states.

**Near-size adjacency rule.** `--text-body-sm` (14), `--text-body` (15), and `--text-subheading`
(16) sit within 2px of each other — inherited from Themely's scale, where a wider layout keeps
them apart. Inside the **280px tool panel** at most **two** of the three may appear, and a
hierarchy step within the panel is carried by **weight**, not by a 1–2px size difference. In
practice the panel uses `--text-body-sm` (400 for rows, 500 for sub-headings) and `--text-body`
(prose only, in empty states and confirmations); `--text-subheading` does not appear there at all.

**Consumer exemption, closed set.** `coding-rules/frontend.md` records that *"a declared token
needs a consumer, or its contract assertion is theatre."* `--text-display` and `--text-stat` have
no consumer in this app (there is no marketing hero and no stat card), but D-09 vendors the whole
scale. They are declared **with an explicit, commented exemption naming the reason**, and the
exemption list in the contract test is a **closed set of exactly these two** — adding a third
must fail.

**Legend typography [D-25 — changes exported pixels].** The legend renders inside the
export-bearing composition. Its labels adopt Inter at the sizes `LegendOverlay.tsx:34-38` already
declares (`small: 24`, `medium: 32`, `large: 40` user units) at weight **600**, matching the
existing `fontWeight="600"`. Two consequences the planner must carry, both already flagged in
CONTEXT and RESEARCH:

- The character-per-line heuristic is part of D-25's blast radius. `LegendOverlay.tsx:39-43`
  (`LEGEND_CHARACTERS_PER_LINE`) and `src/utils/legend.ts:69-73` (`LABEL_CHARACTERS_PER_LINE`)
  hold the **same** `{ small: 24, medium: 18, large: 14 }` table under two names. Collapse to one
  exported constant and re-derive it from Inter's worst-case advance width (OQ-5, P-4). Legend
  overflow clipping the PNG is a defect this project has already shipped once.
- The font must actually reach the export clone (D-34/D-34a). Until OQ-1's Playwright spike
  confirms an inline base64 `@font-face` renders inside SVG-as-image in installed Chrome, legend
  typography is **not locked**. That spike is blocking and precedes any legend restyle.

**Legend colour exemption.** `LegendOverlay.tsx:65-74` hard-codes `THEME_COLORS`
(`background: '#FFFFFF'`, `text: '#111827'`, `border: '#CBD5E1'`) and `:313` hard-codes
`stroke="#9CA3AF"`. These are **deliberate export-fixed values, not an oversight.** The new
"no hex literal in `.tsx`" assertion must give `LegendOverlay.tsx` an explicit, commented
exemption naming this reason. Note the name collision: the legend's `light`/`dark` is a
*creator-chosen legend theme*, not the app's colour scheme (P-3).

### Spacing scale

The existing 8-point tokens survive unchanged (`theme.css:23-30`). Themely has no custom spacing
tokens (it uses Tailwind's default scale), so this is the translation.

| Token | Value | Usage |
|---|---|---|
| `--space-xs` | 4px | icon gaps, inline padding, swatch gutters |
| `--space-sm` | 8px | compact element spacing, grid gaps inside the panel |
| `--space-md` | 16px | default element spacing, panel horizontal padding |
| `--space-lg` | 24px | section padding, floating-control inset from the canvas edge |
| `--space-xl` | 32px | gap between panel sections |
| `--space-2xl` | 48px | **minimum interactive control height** (carried forward) |
| `--space-3xl` | 64px | HUD header block height |
| `--target-compact` | 44px | touch target under the narrow breakpoint (D-20) |

**Exceptions: none.** Every spacing value in this contract is a multiple of 4: rail 56, panel 280,
rail row 36, rail icon slot 32, icon glyph 20, control min-height 48, touch target 44. Radii are a
separate scale and are not spacing.

### Radii (D-07)

| Token | Value | Themely equivalent | Applies to |
|---|---|---|---|
| `--radius-control` | 8px | `rounded-md` | buttons, inputs, rail rows' inner chips, tooltip |
| `--radius-card` | 14px | `rounded-xl` | cards, panel sections, saved-map rows, inline banners |
| `--radius-modal` | 18px | `rounded-2xl` | the one remaining large surface (fatal error state) |
| `--radius-pill` | 9999px | `rounded-full` | pills and badges |

`--radius-large: 16px` is **retired** (superseded by `--radius-card: 14px`). The rail row itself
uses **10px** (Themely's `rounded-lg`), declared as `--radius-row: 10px`.

### Elevation (D-06)

Flat with hairlines. **`--shadow-inspector`, `--shadow-navigation`, and the entire `--glass-*`
family (64 references) are deleted, not aliased**, so stale references fail loudly at the contract
test. `backdrop-filter` is **forbidden outright** — a blanket ban, simpler than the old
approved-surface allowlist and it cannot rot.

| Token | Value | Only for |
|---|---|---|
| `--hairline` | `0 0 0 1px color-mix(in srgb, var(--themely-stone-gray) 60%, transparent)` | cards, inputs, list rows, panel sections |
| `--popover-shadow` | `0 4px 12px -2px rgba(6, 27, 49, 0.10)` | floating map controls, the narrow-width bottom sheet, toasts |
| `--dialog-shadow` | `0 10px 40px -10px rgba(6, 27, 49, 0.20)` | fatal error surface |
| `--toast-shadow` | *retired* → consumes `--popover-shadow` | |
| `--modal-shadow` | *retired* → consumes `--dialog-shadow` | |

`.dark` swaps `--popover-shadow` to `0 4px 12px -2px rgba(0, 0, 0, 0.45)`, matching
`themely/globals.css:357`. **No shadow token may be referenced by anything the export clone
carries** — a hairline is still a `box-shadow`.

The export-unsafe-CSS guard (`phase2CssContract.test.ts:947-994`) carries forward at **high
priority**, and its stated reason must be rewritten when it moves: the Phase 2 wording —
*"html2canvas rasterises it differently than the browser paints it"* — **goes false the moment
`03-01` lands**, because D-34 removes html2canvas entirely. The true post-D-34 reason is
stronger, not weaker:

> Under D-34 the clone is serialised with `XMLSerializer` into a `data:image/svg+xml` URL and
> rasterised as an `<img>`. That image is an **isolated document**: it sees none of the host
> page's stylesheets. A `box-shadow`, `filter`, `backdrop-filter`, `mask`, or `clip-path` applied
> through an external CSS rule does not render "differently" — it renders **not at all**, and the
> only signal is a PNG that quietly lost an effect the editor shows on screen. Anything an
> exported surface needs must be an inline attribute or an inline style inside the serialised
> subtree. `sanitizeExportClone` hard-sets stroke and stroke-width inline for exactly this reason;
> it does **not** neutralise an inherited effect.

D-06 makes hairline `box-shadow` pervasive across chrome, so the guard is more load-bearing after
this phase than before it. `EXPORT_CONTENT_PATTERN` must stay bound back to `MapCanvas.tsx`
source — `.map-unit-path` was once omitted from that pattern for a whole phase.

### Motion (D-26)

CSS vars are the runtime source of truth; `src/lib/motion/tokens.ts` is a TS mirror pinned by a
lockstep test (pure constants — fits the `node` Vitest environment).

| Token | Value | TS mirror | Role |
|---|---|---|---|
| `--motion-ease-out` | `cubic-bezier(0.22, 1, 0.36, 1)` | `EASE_OUT` | entrance/settle; **absorbs the existing `--easing-camera`, byte-identical** |
| `--motion-ease-snappy` | `cubic-bezier(0.2, 0.8, 0.2, 1)` | `EASE_SNAPPY` | hover/press micro-feedback; **absorbs `--easing-control: ease-out` as a deliberate retime** |
| `--motion-ease-in` | `cubic-bezier(0.4, 0, 1, 1)` | `EASE_IN` | exit curve (bottom sheet down) |
| `--motion-duration-fast` | `150ms` | `DURATION_FAST` | **absorbs `--motion-fast`, byte-identical** |
| `--motion-duration-base` | `240ms` | `DURATION_BASE` | **absorbs `--motion-camera`, byte-identical**; panel open/close |
| `--motion-duration-slow` | `360ms` | `DURATION_SLOW` | theme crossfade |
| `--motion-scene` | `160ms` | `SCENE_CROSSFADE_DURATION_MS` | **kept local, documented** — Themely's "do-not-snap" idiom. Retiming the scene crossfade to 150ms is a visible change |

`CAMERA_MOTION_DURATION_MS` derives from `DURATION_BASE` rather than restating `240`.
`@media (prefers-reduced-motion: reduce)` zeroes `--motion-duration-fast`, `-base`, `-slow`, and
`--motion-scene`, and **every motion token must have a live consumer** — a CSS `var()` or a named
read in `utils/motion.ts`. That consumer assertion is not optional: three motion tokens were once
declared, reduced-motion-gated, and read by nothing, so the gate proved nothing.

`prefers-reduced-motion` remains legitimate everywhere. **D-30 forbids `prefers-color-scheme`
only.** `prefers-reduced-transparency`, `prefers-contrast`, and `forced-colors` also survive.

### Preference blocks under a class-based dark mode

The Phase 2 rule *"a preference media query must define BOTH schemes"* survives, restated in class
terms: any `prefers-contrast` / `forced-colors` override of a `--themely-*` token must be declared
for **both** `:root` and `.dark` inside the same at-rule, because the preference block is authored
after the palette at equal specificity and a literal written for one mode silently wins in the
other. This is the defect that once painted a light bar under light text at **1.0:1** for the user
who asked for *more* contrast.

```css
@media (prefers-contrast: more) {
  :root  { --border-width: 2px; --focus-width: 3px; --themely-stone-gray: #50617a; … }
  .dark  { --themely-stone-gray: #8b9099; … }
}
```

---

## Layout Contract

### The shell (D-11, D-16, D-19, D-32)

One mountable editor component behind an explicit props boundary. The **editor mount root** is
the outermost element, is full-bleed and opaque, carries `.dark` when dark mode is on, and is the
only element the host must place. `html` / `body` are reduced to layout-only rules with **no
themed background** — everything the editor paints lives inside its own subtree (resolves OQ-3,
and is a transition-readiness sub-gate).

```
.map-editor                       ← mount root; grid; block-size: 100dvh; [data-panel-open]
│                                   background: var(--themely-platinum)
├── .tool-rail          56px      ← always present; data-editor-only="true"
│   ├── .tool-rail__header        ← D-12 HUD identity block (pinned, never scrolls)
│   ├── .tool-rail__tools         ← the 4 tool rows + Undo/Redo pair (scrolls if it must)
│   └── .tool-rail__footer        ← D-13 Export (accent) · D-30 theme toggle (neutral)
├── .tool-panel         0 | 280px ← at most ONE open (D-17); reserves layout space (D-19)
│   └── .tool-panel__body         ← the one scroll container
└── .map-workspace      1fr       ← full-bleed canvas region
    ├── .map-workspace__canvas    ← container-type: size; overflow: hidden
    │   ├── div.map-export-source
    │   │   └── svg.map-canvas    ← THE EXPORT SOURCE — viewBox "0 0 1080 1080"
    │   │       ├── g[data-layer="camera"]
    │   │       └── g[data-layer="legend"]   ← legendSlot, INSIDE the svg (D-24)
    │   ├── .map-frame            ← D-32 export frame; data-editor-only; aria-hidden
    │   └── navigationSlot        ← D-21 floating cluster; sibling of the export source
    ├── .period-hud               ← D-14 inert period surface + the D-15 live region
    └── ToastRegion               ← unchanged allowlist boundary
```

```css
.map-editor {
  display: grid;
  grid-template-columns: var(--rail-width) var(--panel-width) 1fr;
  block-size: 100dvh;
}
:root { --rail-width: 56px; --panel-width: 0px; }
.map-editor[data-panel-open='true'] { --panel-width: 280px; }
```

- **`[data-panel-open]` is exactly `'true' | 'false'`** — never absent, never a third value, so a
  contract assertion can enumerate it.
- The panel track animates on the registered `--panel-width` custom property (`@property`) over
  `--motion-duration-base` with `--motion-ease-out` — **not** on `grid-template-columns`, which
  fires many layout passes per frame across a 248 × 3-path SVG.
- **No `ResizeObserver` is required and none may be added.** `MapCanvas.tsx:839-840` fixes the
  `viewBox` and `useCameraController.ts:310-313` pins d3-zoom's `extent` to `[[0,0],[1080,1080]]`
  rather than the element rect, so a rail/panel reflow cannot disturb the projection, the camera
  lease, or the export (D-32, verified).
- **`overflow-x: hidden` stays on `body` and nowhere else.** On a non-viewport element it computes
  `overflow-y: auto`, silently killing sticky positioning inside it.
- **`touch-action: none` stays on `svg.map-canvas` and nowhere else** — the ownership set is
  asserted, not just the value. D-20's bottom sheet makes ancestors newly relevant.

### The canvas region and the export frame (D-32) **[SPEC]**

The map **surface** is edge-to-edge. The `viewBox` stays `0 0 1080 1080` and
`preserveAspectRatio="xMidYMid meet"` is unchanged, so the composition square lands centred at
side `min(width, height)` of the canvas region, and geometry beyond it paints into the gutters —
that is the full-bleed feel. The export clone re-asserts the square (`export.ts:277-284`,
`overflow: hidden`), so gutter geometry is clipped and the PNG is untouched.

```css
.map-workspace__canvas {
  container-type: size;          /* makes min(w,h) expressible in pure CSS */
  overflow: hidden;
  background: var(--map-surface);
}
.map-frame {
  --frame-side: min(100cqw, 100cqh);
  position: absolute;
  inset: 0;
  margin: auto;
  inline-size: var(--frame-side);
  block-size: var(--frame-side);
  aspect-ratio: 1;
  pointer-events: none;
  border: 1px solid var(--map-frame-edge);
  box-shadow: 0 0 0 100vmax var(--map-frame-scrim);   /* dims everything outside */
}
```

| Property | Contract |
|---|---|
| Element | `<div class="map-frame" data-editor-only="true" aria-hidden="true">`, rendered by `MapWorkspace` as a **structural child of the canvas region and a sibling of `div.map-export-source`** — never a descendant of `svg.map-canvas`. Placement decides export membership; `data-editor-only` is the second line of defence, not the first |
| Not a slot | `MapWorkspace`'s typed `legendSlot` / `navigationSlot` are preserved **verbatim** (D-24). The frame is structural chrome, not composable content, so it does not become a third slot |
| Geometry | must land **exactly** where `preserveAspectRatio="xMidYMid meet"` puts the viewBox square. Both are "centred `min(w,h)` square of the same box", which is what makes the equality true rather than approximate |
| Appearance | 1px `--map-frame-edge` hairline; everything outside dimmed by `--map-frame-scrim`. No accent, no radius, no label |
| `aspect-ratio: 1` | **moves here** from `.map-workspace__square`. The Phase 2 assertion survives, relocated — and the relocation is the visible form of the D-32 decision |
| Rename | `.map-workspace__square` → `.map-workspace__canvas`. A class named `__square` that is no longer square is exactly the stale name this repo's rules reject. `background: var(--map-surface)` and `overflow: hidden` carry over; `aspect-ratio: 1`, the border, `--radius-large`, and `--map-shadow` do not |

### Floating map controls (D-21) **[SPEC]**

Bottom-right, Google-Maps idiom, neutral surfaces with Stone Gray hairlines, **no accent**. One
bordered cluster holding four 44×44 icon buttons — not four floating pills (a compact control
cluster is one surface).

| Control | Accessible name | Note |
|---|---|---|
| Zoom in | `Zoom In` | unchanged |
| Zoom out | `Zoom Out` | unchanged; **absent at the whole-world fit** — its absence is asserted |
| Pan | `Move Map` | **retained** — see the note below |
| Reset view | `Reset View` | **new home.** `CompositionBar` dissolves (D-11); `Reset View` is camera-only and must exist **exactly once** in the composed DOM. `Reset All Colors` is content reset and lives in the Colors panel — the two never sit together |

> **Note for the owner.** D-21 names three controls. `Move Map` and its pan popover are retained
> as a fourth because they are the **only keyboard pan affordance** in the app; dropping them
> would regress NFR11 and the keyboard-navigation acceptance cells. Flagged here rather than
> decided silently — if the owner wants a three-control cluster, keyboard panning needs a
> replacement first.

**Placement rule — the cluster occupies the letterbox gutter, never the export frame.** The
top-left collision between screen-positioned chrome and canvas-positioned legend content is a
defect this project has already shipped; the fix was to move the chrome off the square, and the
same discipline applies here:

```css
.map-navigation {
  --frame-side: min(100cqw, 100cqh);
  position: absolute;
  inset-inline-end: max(var(--space-lg), calc((100cqw - var(--frame-side)) / 2 + var(--space-sm)));
  inset-block-end:  max(var(--space-lg), calc((100cqh - var(--frame-side)) / 2 + var(--space-sm)));
}
```

- Pure CSS container math; no measurement, no observer.
- Because `--frame-side` is `min(w, h)`, at least one gutter is non-zero at any non-square canvas
  region, and the cluster is pushed clear on that axis.
- **Bounded, documented exception:** when the canvas region is within ~96px of square neither
  gutter fits and the cluster rests over the frame's bottom-right corner. Accepted because
  (a) it is editor-only chrome that never enters the PNG, (b) the wrapper is `pointer-events:
  none` with `auto` restored only on the four button rects, and (c) the legend's default preset
  is top-left. The e2e gate asserts **non-intersection** with the frame rect at every spec'd
  viewport (assert non-intersection, not placement) — a regression that re-anchors the cluster
  into the frame at a normal viewport goes RED.

### Narrow width (D-20)

Below the existing 1200px breakpoint the grid collapses:

| Element | Narrow behaviour |
|---|---|
| Rail | becomes a **bottom bar**, icons thumb-reachable, `--target-compact` (44px) targets, `data-editor-only="true"` |
| Panel | a tapped tool raises a **bottom sheet** over the map, entering on `--motion-duration-base` / `--motion-ease-out` and exiting on `--motion-ease-in`. This is the one surface that overlays the canvas |
| Floating cluster | sits directly above the bottom bar, still outside the frame |
| HUD header / footer | fold into the bottom bar: composition name truncates to one line; Export stays pinned and visible |

360px containment and the 200 %-equivalent check still apply (the halved CSS viewport is
**labelled as the equivalent**, never as physical zoom). `prefers-reduced-motion` and
`prefers-reduced-transparency` behaviour is re-verified in the new chrome —
`prefers-reduced-transparency` is asserted **statically**, because Playwright cannot emulate it
and emulation a browser does not support is not evidence.

---

## Component Contracts

### 1. Rail row — the signature component (D-16, D-29)

Translated from `themely/src/components/app-sidebar.tsx:610-627` (`PrimaryNavRow`). **Translated,
not copied** — CountriesIRL has no Tailwind, so a copied `className` string produces an unstyled
element that looks wired up (P-5).

| Themely utility | CSS translation |
|---|---|
| `flex h-9 w-full items-center whitespace-nowrap` | `display: flex; block-size: 36px; inline-size: 100%; align-items: center; white-space: nowrap` |
| `rounded-lg` | `border-radius: var(--radius-row)` (10px) |
| `text-body-sm` | the `--text-body-sm` role bundle (14/1.5/400) |
| `text-themely-nav-ink` | `color: var(--themely-nav-ink)` |
| `hover:bg-themely-porcelain` | `background: var(--themely-porcelain)` on `:hover` |
| `active && 'bg-themely-powder'` | `background: var(--themely-powder)` on `[aria-expanded="true"]` |
| icon slot `h-8 w-8`, glyph `size={20}` | `block-size: 32px; inline-size: 32px`, glyph 20px **via the `size` prop, never className sizing** |

Binding behaviour:

- **Hover paint is INSTANT.** No `transition` on the row background. Themely states an ease here
  is *"a regression, not polish"* (D-29). Only the icon glyph animates.
- **Colour is constant across inactive / hover / active** (`--themely-nav-ink`). No accent bar, no
  blue active icon, no weight bump. **Only the row background carries state.**
- The glyph animation is **triggered from ROW hover** via the imperative handle, not icon hover:
  `onMouseEnter → if (!reducedMotion) iconRef.current?.startAnimation()`,
  `onMouseLeave → iconRef.current?.stopAnimation()`. `startAnimation()` is gated by
  reduced-motion; `stopAnimation()` is **unconditional**.
- The row is a `<button type="button">` with `aria-expanded` and `aria-controls` pointing at the
  panel. Rows are plain tab stops.
- **The rail must not introduce a second roving-tabindex writer.** There is exactly one, restored
  in commit `074173e`. A second writer is the regression class that commit fixed.
- **No positional CSS selector may style a rail row.** `:nth-child` / `:first-child` /
  `:last-child` bind a role to an ordinal, and order is copy. Key on a role class and a stable
  `data-tool` attribute. A rail of near-identical icon rows is the ideal place to reintroduce this
  bug, and the ban is enforced by the contract test, not by review.

**Tool inventory and icon selection [SPEC]** (D-28's vendoring rules apply to every glyph):

| `data-tool` | Rail label / accessible name | Panel contents | Icon (lucide source glyph) |
|---|---|---|---|
| `colors` | `Colors` | `SelectionPanel` + `ColorPicker` presets + custom hex + `Reset All Colors` | `palette` |
| `countries` | `Countries` | `CountryList` + `LocateCountry` | `list` |
| `legend` | `Legend` | `LegendEditor` (entries, theme, text size, border, position) | `layers` |
| `saved` | `Saved Maps` | save form + saved-map list | `folder` |

Pinned, non-tool rail rows (same row recipe, no `aria-expanded`):
`Undo Color Change` (`undo-2`), `Redo Color Change` (`redo-2`) — labels unchanged, because the
e2e locators and the toast allowlist are keyed to them.

Every rail row shows a **tooltip on hover and focus** carrying its label, because the rail is
icon-only at 56px. That tooltip is chrome (`data-editor-only="true"`) and uses the § Tooltip
recipe.

### 2. HUD header — composition identity (D-12) **[SPEC]**

Pinned at the top of the rail, above the tools, **always visible, never scrolls away**. Mirrors
Themely's workspace-switcher slot.

| Slot | Content | Type role | Colour |
|---|---|---|---|
| Glyph | 32×32 `--radius-row` chip, `--themely-powder` background, Apple-Blue-free monogram of the composition name (or a `map` glyph when unnamed) | `--text-h3` | `--themely-midnight-ink` |
| Name | the composition name, one line, `text-overflow: ellipsis` | `--text-h3` | `--themely-midnight-ink` |
| State | `Saved` / `Unsaved changes` pill, `--radius-pill`, `--text-eyebrow` uppercase | `--text-eyebrow` | Porcelain background + Slate Blue text (neutral pill) |

- The header is **56px wide when the panel is closed**: name and pill are `opacity: 0` and
  removed from the accessible name only by truncation, never by `display: none`. When the panel
  is open the header spans rail + panel (336px) and shows the full block.
- **The composition name remains identity owned by the composition root**, set only on a
  committed save or load, and cleared by `Reset All Colors` and by deleting the saved map it
  names. That invariant is unchanged by the layout move.
- No Apple Blue anywhere in this block — the rail's one accent is Export.

### 3. HUD footer — Export and theme (D-13, D-30) **[SPEC]**

Pinned at the bottom of the rail, **always visible regardless of panel or scroll state**.

| Control | Treatment |
|---|---|
| `Export PNG` | **the rail's one Apple Blue surface.** Filled **`--accent-fill`** (mode-invariant `#0071e3` — **not** `--themely-apple-blue`; see § The Export fill is mode-invariant), label `--themely-on-accent` at `--text-body-sm` weight 500, `--radius-control`, hover `--accent-fill-hover`, disabled 40 % opacity with no pointer events. Icon-only at 56px (glyph `download`, 20px) with its label in the hover/focus tooltip; full `[icon] Export PNG` when the panel is open. Busy label swaps exactly: `Export PNG` ⇄ `Exporting PNG…`, with native `disabled` + `aria-busy` — never `aria-disabled` on a still-clickable button |
| Theme toggle | **neutral** icon control (D-30). Ghost treatment: transparent background, `--themely-nav-ink` glyph, hover `--themely-porcelain`. Glyph `sun` in light, `moon` in dark. Accessible name states the destination: `Switch to dark theme` / `Switch to light theme`, with `aria-pressed` reflecting the current mode |

- `Export PNG` is the only filled action in the composed DOM — **exactly one**
  `controls__action--primary` may exist. Style it on its role class, never on its container or
  position.
- `Controls` stays **one component with a declared `variant`** — a `rail` variant is **added**,
  never a copy. Exactly one instance is mounted at a time, which is what keeps "one filled action"
  and "one `Reset All Colors`" true by construction rather than by review.
- Theme persists through the **storage-adapter interface**, never raw `localStorage`, respecting
  the bounded V2 record contract. Absent key ⇒ **light**. **No `prefers-color-scheme` read
  anywhere**, not even to seed a first-run default.
- A presentational control that starts an async owner action keeps its own **synchronous
  activation lock** (`useRef` cleared in `finally`); the parent's `isExporting` is only true one
  render later.

### 4. Period surface and the rehomed live region (D-14, D-15) **[SPEC]**

`CompositionBar` dissolves. Its two responsibilities land in one new `.period-hud` surface in the
canvas region — **not** in the rail, because the region is an `aria-describedby` target as well as
a live region and it must sit next to the control it describes.

**What the surface displays — the exact contract:**

- It renders the options returned by `useSnapshotCatalog` (i.e. `resolvePeriodOptions(...)` over
  the approved manifest at `public/data/snapshots/index.json`), which currently resolve to
  **exactly one entry: `Modern — current borders`**.
- **It must NOT render `SNAPSHOT_CATALOG` directly.** That constant is a **five-entry label
  registry** (`src/constants/snapshots.ts:10-19`), not an approval list. Rendering it would make
  four deferred snapshots nameable in the UI — a direct violation of Immutable Safety Constraint 3
  (*"a BLOCKED packet is never counted as a delivered snapshot"*) and Live Invariant 6.
- With one resolved option the surface renders as a **visibly inert read-only pill**, not a
  disabled `<select>`: label `Map period` (`--text-eyebrow` uppercase, `--themely-slate-blue`) above
  the single resolved label (`--text-body-sm`, `--themely-midnight-ink`), on a `--themely-porcelain`
  pill with a `--hairline` border and `--radius-pill`. No dropdown affordance, no chevron, no
  "coming soon", no count of hidden periods. Onboarding and status copy never advertise a deferred
  feature.
- **The surface is presentation only.** Catalog semantics, approval semantics, and
  `resolvePeriodOptions` are untouched. If a second approved entry ever reaches the manifest, the
  surface returns to an interactive `<select>` with no copy change — that path must remain
  reachable in code, not deleted.

**The live region keeps its identity:**

- `COMPOSITION_PERIOD_STATUS_ID` stays **byte-identical** (`'composition-bar-period-status'`), the
  element keeps `role="status" aria-live="polite"`, and the period control keeps
  `aria-describedby` pointing at it. It is not merged into `ToastRegion` (an allowlist for
  *transient* messages, which would either force every period string into
  `APPROVED_STATIC_MESSAGES` or degrade it to a severity fallback) and not merged into
  `.selection-live-region` (one region with two writers interleaves and truncates announcements).
- `ToastRegion` remains the allowlist boundary for every creator-facing message, unchanged.
- **Gate:** assert the exact live-region markup string (mirroring `App.test.tsx:274-276`) **and**
  that the id in `aria-describedby` resolves to an element that exists. The second assertion is
  the one that goes RED if the region is dropped during the rail migration.

### 5. Tool panel shell (D-17, D-19) **[SPEC]**

| Property | Value |
|---|---|
| Width | 280px, reserves layout space, never overlays the map |
| Surface | `--themely-platinum` body with a `--hairline` right edge; sections are `--themely-porcelain` cards at `--radius-card` |
| Padding | `--space-md` (16px) horizontal, `--space-md` top, `--space-xl` (32px) between sections |
| Title | the tool's label at `--text-h3`, `--themely-midnight-ink`, with the panel's close control (`Close <Tool>`) as a neutral ghost icon button at the trailing edge |
| Scroll | `.tool-panel__body` is **the one scroll container**; the title row does not scroll |
| Open/close | one at a time (D-17). Clicking the open tool's row closes it. `Escape` closes the panel and returns focus to the row that opened it |
| First run | **closed** (D-18) — full-bleed world map plus a quiet icon strip. Thereafter the rail restores the last-open tool through the **storage-adapter interface**, never raw `localStorage`, respecting the bounded V2 record contract. Absent key ⇒ closed |
| Nesting | **no borders on top of borders.** A `--hairline` card never contains another `--hairline` card — stack by background shift (Platinum → Porcelain) instead |
| Stacking | **stack labelled controls; reserve rows for icon-only or single-word controls.** A row of full-phrase controls has no width at which it fits a 280px column |

Focus movement into the opened panel and keyboard reachability are covered by the existing
single-roving-tabindex invariant. The focus-order test asserts the **spec'd** order, including
controls whose disabled state removes them; a test that documents a deviation is a comment, not a
test, and every focus-order claim needs a RED probe against the arrangement it replaced.

### 6. Colour swatch grid **[SPEC]** — no Themely analog

Lives in the `colors` panel. The nearest Themely recipe is the icon-chip-inside-a-card pattern;
everything below is the translation to a colour picker.

**Grid.**

```css
.color-picker__preset-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(76px, 1fr));
  gap: var(--space-sm);
}
```

The column count is **derived from a minimum track wide enough for the longest label**, never
fixed. A fixed count plus `width: max-content` inside `overflow: hidden` is what once clipped
`Magenta` at the tile edge with nothing failing. Overflow stays visible so the next one is caught
by eye. At 280px minus 2 × 16px padding this resolves to 3 columns.

**Tile.**

| Part | Contract |
|---|---|
| Element | `<button type="button" data-color-name="…" aria-label="Apply <Name>">` — role-classed, never styled by position |
| Box | `min-block-size: 48px`; `--radius-control`; `--themely-platinum` background; `--hairline` border; `--space-sm` padding; vertical stack |
| Swatch | 24×24, `border-radius: 8px`, `background: <preset colour>`, `border: 1px solid var(--swatch-border)` — the border is what keeps a **white** preset visible on a Platinum tile, and it is the same fixed value the exported legend swatch uses |
| Label | preset name at `--text-caption`, `--themely-midnight-ink`, never truncated |
| Hover | `background: var(--themely-porcelain)`. **Instant, no transition** — the rail rule applies to every hoverable row/tile surface in the editor |
| Selected | `background: var(--themely-powder)`; label goes `--themely-midnight-ink`; a `check` glyph (16px, `--themely-midnight-ink`) sits at the tile's trailing-top corner **on the tile background, not on the swatch**, so it stays legible over any creator colour. `aria-pressed="true"` |
| Focus | the global Apple Blue `:focus-visible` ring |

This replaces `--active-check-border` / `-surface` / `-text`, which are **deleted**: their fixed
`#111827`-on-white values went invisible under `.dark`.

**Mixed-selection indicator.** When the selection holds more than one colour, the "current colour"
preview renders as a 24×24 chip split diagonally between `--themely-porcelain` and
`--themely-slate-blue` with the `--swatch-border` hairline, plus the visible text `Mixed`. This is
chrome and **flips with the theme** — it represents a state, not a colour. Replaces
`--mixed-color-light` / `--mixed-color-dark`, which are **deleted**.

**Uncoloured is not filled.** `canonicalizeColorMap` deletes any entry equal to `DEFAULT_COLOR`, so
white and uncoloured are one state. Unset rows get an **outline**, coloured rows get a **fill** —
painting a filled chip on all 195 rows made an untouched map read as fully coloured. The accessible
name stays `Current color #FFFFFF`, because the country really is white on the map and every row
locator is keyed on that string.

**Custom hex form.** Input at `--text-body-sm`, `--radius-control`, `--hairline` border, focus ring
Apple Blue. Its error message is `--text-caption` in `--themely-red`, wired by
`aria-describedby` + `aria-invalid`. The form's submit is the panel's **one** primary action
(`Apply Color`, filled Apple Blue) — and it is the only Apple Blue element in the `colors` panel.

### 7. Legend editor rows **[SPEC]** — no Themely analog

Lives in the `legend` panel. The legend itself stays a **canvas overlay inside the export-bearing
composition** (D-24) — only the *editor* moves. Nothing here reads `legend.position` raw; every
read goes through `resolveLegendPosition` / `resolveLegendRender`.

**Entry row** (`.legend-editor__entry`), one per active colour:

```
┌──────────────────────────────────────────────────────┐
│ [swatch 20] [label input ................] [12/24]   │  ← row 1
│                                   [↑] [↓] [⋮⋮ drag]  │  ← row 2 (actions)
└──────────────────────────────────────────────────────┘
```

| Part | Contract |
|---|---|
| Row | `--themely-porcelain` card, `--radius-card`, `--hairline`, `--space-sm` padding, `--space-sm` gap. **No inner hairline** — the label input sits directly on the card with a `--hairline` bottom edge only |
| Swatch | 20×20, `border-radius: 6px`, `border: 1px solid var(--swatch-border)`, `aria-label="Color <hex>"` — unchanged semantics |
| Label input | `--text-body-sm`, transparent background, full row width, placeholder `--themely-ghost-gray` |
| Counter | `--text-caption`, `--themely-ghost-gray`, `tabular-nums`, `aria-live="off"` (unchanged — it must not announce on every keystroke). Turns `--themely-red` at the limit |
| Actions | icon-only ghost buttons at 44×44 (`--target-compact`), `--themely-nav-ink` glyphs. **Stacked onto their own row**, because a full-phrase control row has no width at which it fits a 280px column |
| Reorder | keyboard reorder via the two arrow buttons is the primary path; drag is an enhancement. The drag handle keeps `aria-label="Drag <label> to reorder"` |
| Invalid | the existing `data-legend-validation="invalid"` hook stays; an invalid row gets a `--themely-red` left edge (2px inset, **not** a positional selector — keyed on the data attribute) and its message at `--text-caption` in `--themely-red` |

**Style controls** (theme, text size, border, position) are four `<fieldset>`s, each a Porcelain
card whose `<legend>` is **`--text-body-sm` at weight 500** — deliberately *not* `--text-subheading`.
16px and 14px are only 2px apart, which reads as an accident rather than a hierarchy when both
land in the same 280px column; the weight bump carries the hierarchy instead. Applies to every
`<fieldset>` legend and sub-section heading **inside the tool panel**. Their options are **pills**: neutral (Porcelain + Slate
Blue) when unselected, **Powder + Midnight Ink** when selected. **No accent** — the `legend` panel
has no primary action, so per D-05 it carries no Apple Blue at all.

**Position picker** is a 3×3 grid of 44×44 cells with the four presets and `Custom`; the selected
cell is Powder. Its announcements are the existing approved strings
(`Legend moved to Top left.` etc.) — unchanged, already allowlisted.

**Empty state** (no coloured countries yet): the compact Themely empty-state recipe —
`--radius-card`, **dashed** `--themely-stone-gray` at 70 %, `--themely-porcelain` at 40 %,
centred, `--space-lg` padding, a 40×40 round `--themely-apple-blue` at 10 % chip holding a
16px `layers` glyph. Copy unchanged (see § Copywriting).

### 8. Saved-map row anatomy **[SPEC]** — no Themely analog

**Structural change the planner must cost:** D-19 names saved-map rows as *panel* content, so
`SaveLoad`'s modal dialog dissolves into the `saved` tool panel. The modal machinery
(`role="dialog"`, `aria-modal`, the overlay, the imperative `inert`) retires **with the dialog**.
What survives, verbatim, is the nested-confirmation contract — it was never about the modal:

- a confirmation renders as a **sibling** of the surface it interrupts, not a descendant;
- it carries its own **`tabIndex={-1}`** (as a sibling it has no focusable ancestor, so a
  mouse-down on its body text drops focus to `document.body` and every other guarantee voids at
  once);
- `Escape` dismisses the **innermost** open confirmation, branching over every open layer in
  order — a handler that knows about one of two sibling confirmations discards the other's prompt;
- focus returns to the control that opened it, from an **effect**, keyed by a **stable row key**
  (index keys break as soon as a row is deleted).

The `02-22` action-order semantics must be **explicitly preserved or explicitly superseded in the
plan — recorded, not silent** (the roadmap `03-06` gate already says this).

**Row.**

```
┌────────────────────────────────────────────────────────┐
│ [chip]  Baltic Tour 2026                               │
│         12 countries · Saved 6 Aug 2026                │
│                          [Load This Map] [Delete ⌫]    │
└────────────────────────────────────────────────────────┘
```

| Part | Contract |
|---|---|
| Row | `--themely-porcelain` card, `--radius-card`, `--hairline`, `px 16 / py 12`, `--space-sm` gap. Hover `--themely-powder`, **instant** |
| Chip | 32×32 `--radius-control`, `--themely-platinum` background, `--hairline` ring, `map` glyph in `--themely-slate-blue`. Chips step **up** from the Porcelain card |
| Name | `--text-body-sm`, `--themely-midnight-ink`, one line, ellipsis |
| Meta | `--text-caption`, `--themely-ghost-gray`, `tabular-nums`. Consumes the `SavedMapSummary` projection; the period short label resolves through `SNAPSHOT_CATALOG` and never through manifest text — **but see the correction below, which this restyle must not paper over** |
| `Load This Map` | ghost button: transparent, `--themely-midnight-ink`, `--hairline` border, hover `--themely-porcelain`. Accessible name `Load This Map: <name>` (unchanged) |
| `Delete Saved Map` | **destructive**: `--themely-red` text on transparent, hover `--themely-red` at 10 % background. Accessible name `Delete Saved Map: <name>` (unchanged). **Never one-shot** — always paired with the inline confirmation |
| Confirm state | the row's actions swap to the confirmation pair (`Delete Map: <name>` filled `--themely-red` with `--themely-on-accent` text, `Keep Map: <name>` ghost) plus the prompt at `--text-caption` |
| Empty state | the compact empty-state recipe, copy unchanged |
| Save form | name input + `Save Map` — the `saved` panel's **one** Apple Blue element |

**Correction — a saved-map row *can* name a deferred snapshot today, and this is pre-existing
Phase 2 behavior, not something Phase 3 introduces.** The inference *"the catalog is the only
label source, **therefore** a stored record can never name a deferred period"* does not follow,
and it is written into the source as a comment at `SaveLoad.tsx:127-131`. Verified this session:

- `storage.ts:61-63` builds `SNAPSHOT_IDS` from **all five** `SNAPSHOT_CATALOG` entries, and the
  record validator at `:483-484` admits any id in that set — so a hand-crafted `localStorage`
  record carrying `"snapshotId": "1914"` validates.
- `getPeriodShortLabel` (`SaveLoad.tsx:132-137`) then looks that id up in the same five-entry
  registry and returns `1914`.

The label is still catalog-derived (Live Invariant 6 is intact — manifest text never reaches the
UI), and reaching it requires hand-editing browser storage, so this is a **weak, local**
exposure, not a path by which a deferred snapshot becomes *reachable*. Phase 3 must not restate
the false inference in the restyled row, and **must not silently fix it either** — a behavior
change to the storage validator is out of a chrome phase's scope.

**Recommendation for the planner (stronger than narrowing the wording):** add an **approved-id
filter** to the short-label resolver — resolve through the ids the approved manifest actually
yields (the same source `resolvePeriodOptions` uses), returning `null` otherwise. That makes the
original claim *true* rather than merely narrower, is a few lines, and is RED-provable by
planting a `1914` record and asserting the row shows no period label. Record it as a deliberate
decision either way; do not leave the false comment standing.

### 9. Tooltip — dark ink chip (D-22)

The map tooltip is an HTML overlay (`div.map-tooltip`), never inside `svg.map-canvas`, so it
cannot reach the export clone. It is nonetheless declared with **fixed, mode-invariant** tokens:

| Property | Value | Why |
|---|---|---|
| Background | `--tooltip-surface: #061b31` | Midnight Ink's **light** value, literal. Consuming the flipping token would produce a light chip with white text in dark mode |
| Text | `--tooltip-text: #ffffff` | matches `--themely-on-accent`'s both-modes contract |
| Border | `--tooltip-border: rgba(255, 255, 255, 0.14)` | gives the chip an edge against a near-black dark-mode wall |
| Radius | 8px (`--radius-control`) | D-22 |
| Shadow | `--tooltip-shadow` = the popover tier | D-06 |
| Padding | `--space-sm` / `--space-md`; `--text-caption` for the two detail lines, `--text-body-sm` weight 600 for the country name |

Chosen over a light Porcelain card because creators colour countries white, and a near-white
tooltip over a near-white fill loses its edge. Contrast: white on `#061b31` measures **17.9:1**.

**Cursor and copy discipline carries forward verbatim (D-23).** A colourable unit gets
`cursor: pointer`; a non-colourable unit (`colorOwnerId === null` — Kosovo, Western Sahara,
Antarctica; 12 units) gets `cursor: default`, keeps `NEUTRAL_UNIT_COLOR`'s solid light-grey fill in
**both** colour resolvers, and its tooltip states the honest reason — `Not colorable in this map` —
rather than announcing a colour. Roadmap `03-07` requires an assertion that goes RED if this
regresses to a colour readout. The neutral fill is a solid colour, never a CSS `filter`.

### 10. Toast region and onboarding

`ToastRegion` is unchanged as a **boundary, not a renderer**: everything it receives is untrusted
until it matches an exact approved string or a bounded dynamic pattern; anything else degrades to
the severity fallback. Only its surface is restyled — `--themely-porcelain` card, `--radius-card`,
`--hairline`, `--popover-shadow`, text `--text-body-sm`. Error variant: `--themely-red` at 10 %
background, `--themely-red` at 20 % border, `--themely-red` text (the Themely inline-banner
recipe). **No accent.**

The onboarding banner becomes an inline info card on the canvas region: Porcelain, `--radius-card`,
`--hairline`, a single CTA (its one Apple Blue element) and a ghost `Dismiss Help`. Its accent must
be keyed on a **role class**, never `button:first-child` — that exact selector once meant reordering
would have moved the accent onto `Dismiss Help` with nothing failing.

---

## Copywriting Contract

**This phase introduces no new toast, status, or live-region message.** Every string added below
is a static control label, heading, or pill — none reaches `onStatusMessage`. The `ToastRegion`
allowlist (`APPROVED_STATIC_MESSAGES`, the three dynamic patterns, and
`APPROVED_PERIOD_ANNOUNCEMENTS`) is therefore **unchanged**, and its positive-test count must not
change. That is itself a gate: if the allowlist grows, a message was introduced without a test.

### Primary CTA and existing copy — reused verbatim

| Element | Copy | Source |
|---|---|---|
| Primary CTA | **`Export PNG`** (busy: `Exporting PNG…`) | existing `Controls.tsx:5`; labels swap exactly |
| Undo / Redo | `Undo Color Change` / `Redo Color Change` | existing |
| Camera reset | `Reset View` | existing `PERIOD_COPY.resetView` |
| Content reset | `Reset All Colors` | existing |
| Period label | `Map period` | existing `PERIOD_COPY.periodLabel` |
| Period value | `Modern — current borders` | resolved from the approved manifest, never from `SNAPSHOT_CATALOG` |
| Legend empty heading | `Your legend will appear here` | existing `LegendEditor.tsx:334` |
| Legend empty body | `Color at least one country to create the first legend entry.` | existing `LegendEditor.tsx:335` |
| Saved-maps empty heading | `No saved maps yet` | existing `SaveLoad.tsx:703` |
| Saved-maps empty body | `SAVED_EMPTY_BODY` (unchanged) | existing |
| Export failure | `The PNG could not be created. Your map is unchanged. Try Export PNG again.` | existing; **retry offered** |
| Invalid composition | `The map layout could not be captured. Your map is unchanged. Move the legend, then try Export PNG again.` | existing; **no retry** — a synchronous refusal refuses identically forever |
| Legend blocked | `getLegendBlockingMessage` output | existing; **no retry** |
| Storage blocked | `This browser blocked local saves. You can keep editing and export a PNG, but maps cannot be saved here.` | existing |
| Quota exceeded | `Browser storage is full. Delete an older saved map, then save this map again.` | existing |
| Fatal world-load | `We couldn't load the world map` / `PERIOD_COPY.fatalBody` / `Reload Map` | existing — the **one** sanctioned reload prompt, because there is no composition to destroy yet |

**Two copy rules bind every string in this phase.** No message may say *"Refresh the page"* — the
composition lives only in browser memory, so a refresh destroys every unsaved colour, camera,
period, and legend. And no copy may advertise a deferred feature: no "coming soon", no unbound
keyboard shortcut, no control that does not exist.

### New strings introduced by this phase

| Element | Copy | Notes |
|---|---|---|
| Rail tool | `Colors` | rail tooltip + accessible name + panel title |
| Rail tool | `Countries` | |
| Rail tool | `Legend` | |
| Rail tool | `Saved Maps` | |
| Panel close | `Close Colors` / `Close Countries` / `Close Legend` / `Close Saved Maps` | one per tool; verb + the tool's own noun |
| Theme toggle | `Switch to dark theme` / `Switch to light theme` | names the **destination**, with `aria-pressed` on the current mode |
| HUD state pill | `Saved` / `Unsaved changes` | two states only; no third |
| Custom colour submit | `Apply Color` | the `colors` panel's one primary action |
| Save submit | `Save Map` | the `saved` panel's one primary action |

### Destructive actions

| Action | Confirmation |
|---|---|
| `Delete Saved Map: <name>` | **never one-shot.** The row's actions swap in place to `Delete Map: <name>` (filled `--themely-red`, `--themely-on-accent` text) + `Keep Map: <name>` (ghost). `Escape` cancels the confirmation only, and focus returns to that row's `Delete Saved Map` |
| `Reset All Colors` | undoable, so no modal confirmation. The existing toast names the escape hatch: `All colors reset. Use Undo Color Change to restore them.` It also clears the composition name, because the identity would otherwise outlive the thing it names |
| Loading a saved map over unsaved work | the existing dirty-load confirmation; `Keep Editing` refocuses that row's `Load This Map` |

---

## UI Considerations

Applicable state considerations resolved: **13 covered, 3 backstop, 0 unresolved.**

| Category | Element(s) | Status | Resolution / Reason |
|---|---|---|---|
| empty | legend editor entry list | ✅ covered | Zero coloured countries renders the compact dashed empty-state card with the existing heading + body copy (§ Copywriting); the entry list is absent, not a zero-row table |
| empty | saved-maps list | ✅ covered | Zero saved records renders the compact empty-state card with the existing heading + body copy |
| empty | tool panel, first run | ✅ covered | D-18: the panel is **closed** on first run — full-bleed map plus a quiet icon strip. Not an empty panel; the absent key resolves to closed |
| empty | composition identity | ✅ covered | Unnamed composition shows the `map` glyph chip and no name row; `Unsaved changes` pill only after the first edit |
| loading | map canvas | ✅ covered | Skeleton paints from the fixed `--map-skeleton-fill` / `-stroke` inside the canvas region; `PERIOD_COPY.worldLoading` announces through the rehomed live region |
| error | world asset fails to load | ✅ covered | `FatalErrorState` at `--text-h1` on a `--radius-modal` surface with `--dialog-shadow`; existing copy; the one sanctioned reload prompt |
| error | export refusal | ✅ covered | Branch exhaustively on the reason union; a retry is offered **only** where a retry can succeed (§ Copywriting) |
| error | storage unavailable / quota | ✅ covered | Existing per-reason copy; the editor stays usable and Export stays enabled |
| error | invalid custom hex | ✅ covered | `--text-caption` in `--themely-red`, wired by `aria-invalid` + `aria-describedby` |
| populated | country list at 195 rows, colour grid at 10 presets | ✅ covered | Panel body is the single scroll container; derived grid tracks; unset rows outlined, coloured rows filled |
| partial | saved-map list with unreadable records | ✅ covered | Existing message: `Some saved maps could not be read and were left out of the list. Your current map is unchanged.` |
| zero-one-many | period options | ✅ covered | 1 approved option ⇒ inert read-only pill (D-14); ≥2 ⇒ the interactive `<select>` path, which stays reachable in code. **Never rendered from `SNAPSHOT_CATALOG`** |
| zero-one-many | legend entries | ✅ covered | 0 ⇒ empty card; 1..N ⇒ rows; **zero legends is not a missing legend** and must still export a white square |
| long-text | composition name at 56px and at 336px | 🧪 backstop | One line with `text-overflow: ellipsis` plus the full name in the hover/focus tooltip. Held-out visual UI-state check at both rail widths and at 360px |
| overflow | legend labels versus the legend box | 🧪 backstop | D-25 changes advance widths, so the characters-per-line heuristic must be re-derived and the **rendered** result gated: drive a maximum-length label at `large` and assert the exported legend stays inside the safe inset. Legend overflow clipping the PNG has shipped here once |
| overflow | tool panel body at 360px and the 200 %-equivalent | 🧪 backstop | Stacked labelled controls, derived grid tracks, one scroll container; re-verified in the new chrome by `responsive.spec.ts`, with the halved viewport labelled as the **equivalent** of 200 % zoom, never as physical zoom |

---

## Vendoring Safety

shadcn is not initialized, so the shadcn registry protocol is **not applicable**. The same
protocol is applied to this phase's real supply-chain surface: a pinned runtime dependency and
source files copied from a sibling repository.

| Source | Artifacts | Safety gate |
|---|---|---|
| npm — `motion` | pinned exact **`12.40.0`** (byte-matching the repo the icons are vendored from; never a caret range) | **SUS (`too-new`) — reason is the publish cadence, not the package.** 17.5M weekly downloads, matching source repo `github.com/motiondivision/motion`, `postinstall: null`. Provenance is the sibling repo's own declared dependency, not a web search. **A `checkpoint:human-verify` is required before the install**, per the SUS protocol. **Do not take `motion@latest`** — `13.0.0` shipped 2026-08-05 and its React peer range is unchecked |
| npm — `@fontsource-variable/inter` | **not taken.** The woff2 is hand-vendored into `src/assets/` | Avoided: the repo already has a first-class pattern for a bundled, hash-verified, same-origin asset, and these bytes end up inside every exported PNG, so they must be reviewable and pinnable |
| `themely/src/lib/motion/tokens.ts` | the six motion constants | **read in full this session (2026-08-06) — no flags.** Pure constants; no `fetch`, no `process.env`, no dynamic execution |
| `themely/src/components/app-sidebar.tsx` | `PrimaryNavRow` / `DisabledNavRow` recipe (translated, not copied) | **read lines 560-667 this session (2026-08-06) — no flags** |
| `themely/src/components/ui/search.tsx` | the vendored animated-icon **shape** (one exemplar) | **read in full this session (2026-08-06) — no flags.** Confirms the contract: `forwardRef` + a structurally identical `*IconHandle` (`{ startAnimation, stopAnimation }` via `useImperativeHandle`), size via the **`size` prop**, and the `strokeWidth` 2→1.5 patch carrying its marker comment |
| `themely/src/components/ui/{message-square-more,sparkles,square-pen}.tsx` | remaining upstream animated icons, if any are taken | **PENDING — not reviewed this session; no claim is made about them.** Closing this row is a **plan task**, not a note: see § Two planner requirements below |
| New map-specific icons (`palette`, `list`, `layers`, `folder`, `download`, `undo-2`, `redo-2`, `sun`, `moon`, `map`, `check`, `plus`, `minus`, `crosshair`) | authored in-repo from lucide glyph paths in the exemplar's shape | **authored, not fetched.** No registry install, no network at build or run time. The `strokeWidth` 2→1.5 patch and its marker comment must be re-applied on any re-vendor |

**Runtime network rule, unchanged:** no runtime third-party request exists or may be added. The
font is same-origin bundled bytes; the export path embeds it inline as base64, which is not a
fetch.

### Two planner requirements — these are tasks, not prose

Both were carried as narrative above; neither survives as a gate unless it is a plan task with an
owner. `/gsd:plan-phase 3` must author both in `03-01`.

**R-V1 — the `motion` install is an owner gate.** `03-01` carries an explicit
`checkpoint:human-verify` with **`autonomous: false`**, placed **before** the install command, not
after it. It states the SUS verdict and its reasoning verbatim:

> `motion` is flagged **SUS** with the single reason `too-new`. The signal derives from the most
> recent publish date, not the package's age: 17.5M weekly downloads, source repo
> `github.com/motiondivision/motion`, `postinstall: null`. The strongest legitimacy signal
> available is that it is the sibling repo's own declared dependency and the vendored icon files
> import from it directly. **Do not take `motion@latest` — `13.0.0` shipped 2026-08-05** and its
> React peer range was not checked. Pin exact **`12.40.0`**, never a caret range, so a fresh
> `npm install` cannot silently pull `13.x`.

An automated result may not be substituted for this checkpoint, and a blanket, in-advance,
sight-unseen approval authorizes *proceeding* — it is not a content review and it is not
hash-bound. Record which one is actually held.

**R-V2 — every vendored icon file carries dated review evidence.** `03-01` carries a task that,
for each file taken from the sibling repo or authored from lucide glyph paths, reads the file in
full and records a provenance line in the same evidence shape the three reviewed rows above
already use: `read in full — {no flags | flags listed} — {YYYY-MM-DD}`. Scanned for: `fetch(`,
`XMLHttpRequest`, `navigator.sendBeacon`, `process.env`, `eval(`, `Function(`, dynamic imports
from external URLs, and obfuscated identifiers. **A file with no provenance line may not be
vendored** — enforced by assertion 28, so the evidence set and the file set cannot drift apart.
This closes the three `PENDING` rows above with evidence rather than with assumption.

---

## Contract Assertions This Spec Owes

Each must be broken once and observed **RED** before landing — *a gate must be able to fail on the
bug it covers*, and this repo has already shipped four tests that could not. Use the scratchpad
copy-and-restore procedure for every RED probe; **never `git checkout --` a file with uncommitted
work.**

| # | Assertion | RED probe | Plan |
|---|---|---|---|
| 1 | No rule in any stylesheet carries `prefers-color-scheme` in its at-rule conditions | re-add `@media (prefers-color-scheme: dark) { :root { … } }` | `03-03` |
| 2 | No retired token name appears in any stylesheet (`--accent*`, `--text-primary/secondary/muted`, `--border-strong`, `--surface-*`, `--glass-*`, `--shadow-inspector`, `--shadow-navigation`, `--modal-shadow`, `--toast-shadow`, `--font-label/body/heading/display`, `--weight-*`, `--radius-large`, `--map-shadow`, `--mixed-color-*`, `--active-check-*`) | re-add `--accent: #0f766e` | `03-03` |
| 3 | Every `--themely-*` in `:root` has a counterpart in `.dark` and vice versa, **except** the fixed trio (`--themely-on-accent`, `-media-backdrop`, `-on-media`) which must be **identical** in both | drop one token from `.dark` | `03-03` |
| 4 | **No export token is declared outside the unconditioned `:root`** — extended to `.dark` (Live Invariant 9) | add `--map-surface` to `.dark` | `03-03` |
| 5 | Every export token is declared **exactly once** and has a live consumer | delete a consumer | `03-03` |
| 6 | Every `--motion-*` token has a consumer and is zeroed under `prefers-reduced-motion` | remove a consumer | `03-03` |
| 7 | Motion CSS ↔ TS lockstep (`node` env, pure constants) | change one value in one layer | `03-01` |
| 8 | No hex/rgba literal in any component `.tsx`, with a **closed** exemption list containing exactly `LegendOverlay.tsx` and its stated reason | add a literal to another component | `03-03` |
| 9 | Type-role consumer exemption is a **closed set of exactly** `--text-display` and `--text-stat` | add a third | `03-01` |
| 10 | `[data-panel-open]` is exactly `'true' \| 'false'`; the panel track resolves to `0px` closed and `280px` open | set a third value | `03-02` |
| 11 | The `.map-frame` client rect equals the on-screen projection of the SVG's viewBox corners `(0,0)`/`(1080,1080)` via `getScreenCTM()` | inset the frame by 1px | `03-02` |
| 12 | The floating cluster's rect does **not intersect** `.map-frame` at every spec'd viewport, at **every** legend preset | re-anchor the cluster to the frame's corner | `03-07` |
| 13 | **Scoped to `.period-hud`:** the period surface renders exactly the manifest-derived options, and none of the four historical labels appears **within that surface** | render `SNAPSHOT_CATALOG` directly in the period surface | `03-06` |
| 14 | The rehomed live region's exact markup string is present **and** the `aria-describedby` id resolves to an existing element | delete the region | `03-06` |
| 15 | Exactly one `Reset View`, exactly one `Reset All Colors`, exactly one filled primary action in the **composed** DOM | render a second | `03-05` / `03-06` |
| 16 | No positional selector (`:nth-child`, `:first-child`, `:last-child`) styles any `button`, `input`, `select`, `a`, or `summary` | style a rail row by index | `03-02` |
| 17 | `backdrop-filter` appears nowhere; no `filter` / `box-shadow` / `text-shadow` / `mix-blend-mode` / `mask` / `clip-path` on exported content, with `EXPORT_CONTENT_PATTERN` still bound back to `MapCanvas.tsx` source | add a hairline `box-shadow` to `.map-canvas` | `03-03` |
| 18 | `touch-action: none` on `svg.map-canvas` and nowhere else — assert the **ownership set** | add it to the bottom sheet | `03-08` |
| 19 | The contrast matrix resolves through the real cascade for every (mode × preference) combination, **asserts its own row count**, and every text-on-surface pair meets AA in **both** modes — **no exceptions are enumerated** | let the matrix resolve to nothing | `03-01` / `03-03` |
| 20 | Stylesheets are **globbed**, and the globbed file count equals the count imported by `main.tsx` | add a `.css` file that nothing imports | `03-09` |
| 21 | The distinct-selector inventory is **at most** the recorded count, so growth fails | add a dead rule | `03-09` |
| 22 | Every vendored icon exports a `forwardRef` component **and** a structurally identical `*IconHandle`, sizes via the `size` prop, and carries the `strokeWidth` 2→1.5 marker comment | drop the handle from one icon | `03-01` |
| 23 | The `ToastRegion` allowlist and its positive-test count are **unchanged** by this phase | add a status message without a test | `03-06` |
| 24 | The export PNG is identical across **`.dark` class toggling**, forced colors, and DPR — the rewritten form of `responsive.spec.ts:1025,1048` | make the export theme-sensitive on purpose (D-35) | `03-08` |
| 25 | The exported legend renders in **Inter** — measured on **rasterised pixels**, never on markup. See § Assertion 25 below | export the same composition twice, once with `injectExportFontFace` disabled, and assert the two legend regions **differ** | export plan (Amendment 4) |
| 26 | `--accent-fill` / `--accent-fill-hover` are declared **exactly once in the unconditioned `:root`** and appear in no `.dark` block; the `Export PNG` fill resolves to `#0071e3` in **both** modes | point `--accent-fill` at `var(--themely-apple-blue)` — the dark row of the contrast matrix drops to 3.02:1 and fails | `03-03` |
| 27 | The composed DOM contains **exactly one** roving-tabindex writer | add a second writer in the rail | `03-05` |
| 28 | Every vendored icon file has a recorded, dated "read in full — no flags" provenance line, and the recorded set equals the set of files under `src/components/icons/` | vendor a file without its provenance line | `03-01` |

Assertions 4, 17, 24, and 25 are the export firewall. Assertion 24 is **not optional**: once the
theme flip moves to a class, `page.emulateMedia({ colorScheme })` changes nothing, both exports
become trivially identical, and Live Invariant 9 loses its only browser-level guard — this repo's
fifth "gate that cannot fail" (D-35).

Assertion 25's Plan column is deliberately unnumbered: **there is no export-pipeline plan in
`ROADMAP.md` § Phase 3 yet.** It closes when **Roadmap Amendment 4** lands as a real `ROADMAP.md`
edit during `/gsd:plan-phase 3` and the new plan gets its number. Until then this row has no
owner, which is exactly the state Amendment 4 exists to fix.

### Assertion 25 — what is measured, and why the obvious version cannot fail

**The trap.** The cheap implementation asserts that `font-family: Inter` appears in the cloned
SVG's serialised markup. That declaration is present **whether or not the font ever resolves** —
`LegendOverlay.tsx:167` already names Inter today, and the legend already exports in a system
fallback because no `@font-face` reaches the isolated SVG-as-image document. Such a test is green
right now, before any work is done, and would stay green if `injectExportFontFace` were deleted.
It is this repo's "gate that cannot fail" class, and it would be guarding a **change to exported
pixels**.

**What is measured instead — pixel inequality against a font-less control.** Chosen because it
measures the rasterised output rather than an intention, needs no font-metric table, and cannot be
satisfied by a declaration:

1. Build one composition with a fixed legend label (a string with distinctive Inter advance
   widths, e.g. `Wig 111 fjord`).
2. Export it twice in the same browser context: once through the normal path, once with the
   embedded `@font-face` deliberately suppressed via a test-only seam on
   `injectExportFontFace`.
3. Crop both PNGs to the legend region — which `resolveLegendRender` already yields in canvas
   units, so the crop is derived, not hard-coded — and assert the two crops **differ** beyond a
   noise threshold.
4. Assert a **discrimination control** in the same test: both crops must be non-blank and must
   differ from a third, deliberately blank crop. Without this, three empty regions satisfy the
   inequality perfectly — the exact defect shape of the pixel probe this repo already shipped,
   which asserted only cross-context *equality*.

**RED probe:** delete the `@font-face` injection from the real (non-suppressed) path. The two
crops become byte-identical and the assertion fails.

**Alternative, if pixel cropping proves flaky:** measure a known label's rendered advance width in
the clone against Inter's published metric for that string at that size, asserting a match within
±1 %, with the same font-less control run asserting a *mismatch*. State whichever is chosen in the
export plan — but the control run is not optional in either form.

**This assertion is gated on OQ-1.** If the spike shows an inline base64 `@font-face` does not
resolve inside SVG-as-image in installed Chrome, assertion 25 has nothing to assert and D-25 needs
the owner descope, not a weaker test.

---

## What This Spec Does Not Change

- **The PNG is exactly 1080×1080.** Always. Non-negotiable.
- **Placement in `MapWorkspace`'s typed `legendSlot` / `navigationSlot` decides export
  membership.** Those two slots are preserved verbatim (D-24). The legend stays **inside**
  `svg.map-canvas`; every overlay stays **outside** it. `data-editor-only="true"` is the second
  line of defence, not the first — and the rail, panel, floating cluster, export frame, tooltip,
  period surface, and toast region all carry it.
- **`SNAPSHOT_CATALOG` semantics, the approval chain, and `resolvePeriodOptions`.** D-14 changes
  presentation only. Nothing here makes a deferred snapshot nameable or reachable, and
  "deferred" is never rendered as "done".
- **One `MapCanvasHandle` and one `svg.map-canvas`**, guarded by `data-camera-owner-sentinel`; the
  camera lease and its outermost-`finally` release order.
- **Undo/redo history is colours only**, never selection — which is the only reason undo cannot
  resurrect a selection the active scene does not contain.
- **The single roving-tabindex writer** restored in `074173e`.
- **`ToastRegion` is the allowlist boundary** for every creator-facing message.
- **Browser certification is Chrome-only for this phase** and must say so plainly: Edge is not
  installed on this machine, so `03-10`'s evidence records **"Edge not certified — not
  installed"** (D-33). Phase 3 must not cite or repeat Phase 2's Edge record, which is immutable
  evidence to be annotated, never rewritten. Firefox and Safari remain unverified and must never
  be reported as passed.
- **`02-28` binds `fe5f946`.** The acceptance matrix describes the pre-Phase-3 build and is
  performed against the preserved tag, never against a restyled HEAD. Tagging it is the first task
  of `03-01` (D-31).

---

## Open Items for the Planner

1. **OQ-1 is blocking and survives D-34 in amended form.** A throwaway Playwright spike must
   confirm that an inline base64 `@font-face` actually renders inside SVG-as-image in installed
   Chrome **before** legend typography is locked. Prove it RED by removing the embedded font. If
   the spike fails, the honest descope — chrome gets Inter, the exported legend keeps its system
   fallback — is an **owner decision**, not an executor's.
2. **`Move Map` retention** (§ Floating map controls) — flagged for owner awareness because D-21
   names three controls and this spec keeps four on accessibility grounds.
3. **`SaveLoad`'s dialog dissolving into the `saved` panel** is a real structural cost inside a
   phase specced as chrome-only, and it lands on nested-confirmation and focus-trap code. The
   `02-22` action-order semantics must be preserved or explicitly superseded **in writing**.
4. **The saved-map period short label** (§ Saved-map row anatomy) resolves through the five-entry
   registry, so a hand-crafted storage record can name `1914`. **Pre-existing Phase 2 behavior,
   not a Phase 3 regression.** Decide explicitly: adopt the recommended approved-id filter on the
   resolver, or leave the behavior and delete the false comment at `SaveLoad.tsx:127-131`. Either
   way it is recorded, not left standing as an untrue claim.
5. **Two vendoring tasks** (§ Two planner requirements): R-V1's `autonomous: false`
   `checkpoint:human-verify` before the `motion@12.40.0` install, and R-V2's per-file provenance
   evidence for every vendored icon.
6. **Roadmap Amendments 1–4** must land as explicit `ROADMAP.md` edits in the same commit series
   — including Amendment 4, which adds the export-pipeline plan the § Plans list does not yet
   have and which **owns assertion 25**. `ROADMAP.md` § Out of scope must no longer read as
   though `src/utils/export.ts` internals are untouched, and must drop "dark mode".
7. **`ROADMAP.md` `03-02`'s "left HUD column (collapsible sections, one scroll container)"** is
   superseded by the icon rail + flyout (Amendment 2). This spec's § Layout Contract is the
   replacement text.

**Resolved during checker review (2026-08-06), no longer open:** the dark-mode white-on-accent
contrast failure. The owner chose a mode-invariant `--accent-fill: #0071e3` for the Export button
(4.70:1 in both modes) rather than an accepted exception — see § The Export fill is
mode-invariant. D-08 and D-13 are both untouched, and there is now **no contrast exception in
this contract**.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: **PASS**
- [x] Dimension 2 Visuals: **PASS**
- [x] Dimension 3 Color: **PASS**
- [x] Dimension 4 Typography: **PASS**
- [x] Dimension 5 Spacing: **PASS**
- [x] Dimension 6 Registry Safety: **PASS**

**Approval: APPROVED 2026-08-06.** Verified by `gsd-ui-checker` with 7 non-blocking flags; all 7
resolved in this revision.

| Flag | Resolution |
|---|---|
| 1 — three type sizes within 2px | **Accepted as inherited from Themely's scale, mitigated where it bites.** Tool-panel `<fieldset>` legends and sub-headings drop to `--text-body-sm` weight 500, so 16px and 14px never co-occur in the 280px column. New § Near-size adjacency rule caps the panel at two of the three adjacent roles and carries hierarchy by weight |
| 2 — `motion` gate was prose | **R-V1** — an `autonomous: false` `checkpoint:human-verify` before the install, with the `too-new` reasoning and the `13.0.0` warning carried verbatim |
| 3 — three unvetted vendoring rows | **R-V2** — a plan task producing the same dated `read in full — no flags` evidence shape, enforced by new assertion 28 |
| 4 — assertion 25 measured nothing rendered | **Bound to rasterised pixels.** New § Assertion 25 specifies a legend-region pixel-diff against a font-suppressed control run, plus a blank-crop discrimination control so three empty regions cannot satisfy it. Concrete RED probe stated. Gated on OQ-1 |
| 5 — the §8 `SNAPSHOT_CATALOG` claim did not follow | **Corrected against source.** Assertion 13 narrowed to `.period-hud`, where it is true and load-bearing; §8 restates the real behavior, marks it **pre-existing Phase 2**, and recommends the approved-id filter that would make the original claim true. Now open item 4 |
| 6 — html2canvas rationale goes false at `03-01` | **Reason rewritten for post-D-34.** The guard is retained and strengthened: in an SVG-as-image isolated document an externally-styled effect renders **not at all**, which is a harder failure than a rasterizer mismatch |
| 7 — no roving-tabindex assertion | **New assertion 27** — exactly one writer in the composed DOM, RED probe "add a second writer in the rail" |

**Owner decision recorded in the same pass:** the `[EXCEPTION]` this file carried at draft is
**removed, not accepted** — Export's fill is mode-invariant `#0071e3` (4.70:1 in both modes),
guarded by new assertion 26.

---

*Phase: 3 — Clean UI Overhaul*
*Upstream design system: `/Users/matul/claudeprojects/themely/Design.md` + `src/app/globals.css` (read-only, sibling repo)*
*Downstream: `03-01` vendors this into CountriesIRL's own `Design.md`; `uiContract.test.ts` asserts § Contract Assertions.*



