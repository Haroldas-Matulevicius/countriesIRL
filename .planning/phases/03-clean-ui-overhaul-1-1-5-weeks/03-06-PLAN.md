---
phase: 03-clean-ui-overhaul-1-1-5-weeks
plan: 06
type: execute
wave: 6
depends_on:
  - 03-05
files_modified:
  - src/components/editor/ToolRail.tsx
  - src/components/editor/ToolRailRow.tsx
  - src/components/editor/ToolPanel.tsx
  - src/components/editor/HudHeader.tsx
  - src/components/editor/HudFooter.tsx
  - src/components/editor/ThemeToggle.tsx
  - src/components/editor/MapEditor.tsx
  - src/components/Controls.tsx
  - src/components/Controls.test.tsx
  - src/components/ColorPicker.tsx
  - src/components/SelectionPanel.tsx
  - src/utils/storage.ts
  - src/utils/storage.test.ts
  - src/constants/config.ts
  - src/styles/editor.css
  - src/styles/uiContract.test.ts
  - tests/e2e/rail.spec.ts
autonomous: true
requirements:
  - D-05
  - D-12
  - D-13
  - D-16
  - D-17
  - D-18
  - D-29
  - D-30
  - A-15
  - A-27

estimate:
  tokens: 190000
  raw_tokens: 95000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "D-16 / D-17: a 56px always-present icon rail opens exactly ONE tool panel at a time — the VS Code / Figma model, not an accordion — and clicking the open tool's row closes it."
    - "D-18: first run opens with the panel CLOSED; thereafter the rail restores the last-open tool through the StorageAdapter interface, never raw localStorage, respecting the bounded V2 record contract, with an absent key resolving to closed."
    - "D-29: row background paint is INSTANT with no transition; only the icon glyph animates, triggered from ROW hover through the imperative handle; startAnimation() is reduced-motion-gated and stopAnimation() is unconditional; nav text and icon colour are CONSTANT across inactive / hover / active."
    - "D-12: composition identity and saved/dirty state live in a pinned HUD header at the top of the rail that never scrolls away, and the composition name remains identity owned by the composition root, set only on a committed save or load."
    - "D-13 / D-05: Export PNG is a pinned filled button in the HUD footer consuming the mode-invariant --accent-fill, and it is the rail's ONE Apple Blue surface — the theme toggle and Undo/Redo are neutral."
    - "D-30: a neutral theme toggle in the rail footer writes .dark to the editor mount root, persists through the storage adapter, defaults to light when the key is absent, and reads no OS colour-scheme preference anywhere — not even to seed a first-run default."
    - "Assertion 27: the composed DOM contains EXACTLY ONE roving-tabindex writer — the rail must not introduce a second, which is the regression class commit 074173e fixed."
    - "Assertion 15 (rail half): exactly one Reset View, exactly one Reset All Colors, and exactly one filled primary action exist in the composed DOM."
    - "Controls stays ONE component with a declared variant — a rail variant is ADDED, never a copy — which is what keeps 'one filled action' true by construction rather than by review."
  artifacts:
    - src/components/editor/ToolRail.tsx
    - src/components/editor/ToolRailRow.tsx
    - src/components/editor/ToolPanel.tsx
    - src/components/editor/HudHeader.tsx
    - src/components/editor/HudFooter.tsx
    - src/components/editor/ThemeToggle.tsx
    - tests/e2e/rail.spec.ts
  key_links:
    - "ToolRailRow owns the icon ref and the row-hover handlers; the imperative handle contract from 03-02's vendored icons is what makes D-29's row-triggered glyph animation possible at all."
    - "The theme toggle writes .dark to MapEditor's mount root — the same seam 03-05 gated against document.documentElement, and the seam a future host controls."
    - "The last-open-tool key and the theme key both cross the StorageAdapter; 03-05's one-localStorage-site gate is what keeps them from becoming raw writes."
  backstops:
    - statement: "Focus moves into the opened panel and every rail row is keyboard reachable, covered by the existing single-roving-tabindex invariant; the focus-order test asserts the SPEC'D order including controls whose disabled state removes them."
      verification: backstop
    - statement: "The composition name truncates to one line with ellipsis at both the 56px closed rail and the 336px open state, with the full name in the hover/focus tooltip."
      verification: backstop
---

<objective>
Build the rail. A 56px icon strip with a pinned identity header and a pinned Export/theme footer,
opening exactly one 280px tool panel at a time, plus the colours and palette tools migrated into
it — with the theme toggle that closes D-08's open sub-question.

Purpose: this is the signature surface of the whole phase and the one with the most ways to
quietly regress. Three of them have already happened in this repo: a second roving-tabindex
writer, an accent bound to `button:first-child`, and a control duplicated because a component was
copied instead of given a variant.

Output: the rail, the panel shell, the HUD header and footer, a working `.dark` toggle persisted
through the storage adapter, and `tests/e2e/rail.spec.ts` proving focus order and singleton
actions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/storage.md
@Design.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-UI-SPEC.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-05-SUMMARY.md

Read narrowly:
- `03-RESEARCH.md` § Pattern 3, § Pattern 6, § Pattern 7, § P-5, § P-7
- `03-RESEARCH.md` § Code Examples → "the Themely nav-row recipe to translate" (lines ~1502-1536)

Sibling repo, read-only:
- `/Users/matul/claudeprojects/themely/src/components/app-sidebar.tsx` — `PrimaryNavRow` 584-630,
  `DisabledNavRow` 637-667
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| `src/components/editor/ToolRail.tsx` | component | the 56px strip; `data-editor-only="true"` |
| `src/components/editor/ToolRailRow.tsx` | component | the signature row; owns `iconRef` + row-hover handlers |
| `src/components/editor/ToolPanel.tsx` | component | the single 280px flyout; `.tool-panel__body` is the one scroll container |
| `src/components/editor/HudHeader.tsx` | component | D-12 identity block: glyph chip, name, `Saved`/`Unsaved changes` pill |
| `src/components/editor/HudFooter.tsx` | component | D-13 `Export PNG` + D-30 theme toggle |
| `src/components/editor/ThemeToggle.tsx` | component | neutral `sun`/`moon` control, `aria-pressed`, names the destination |
| `Controls` `rail` variant | variant | ADDED to the existing declared-variant set, never a copy |
| `LAST_OPEN_TOOL_KEY`, `THEME_MODE_KEY` | constants | in `src/constants/config.ts`, following the `ONBOARDING_DISMISSED_KEY` precedent |
| `getLastOpenTool` / `setLastOpenTool` / `getThemeMode` / `setThemeMode` | adapter methods | added to `StorageAdapter` |
| `tests/e2e/rail.spec.ts` | e2e | focus order, one-panel-at-a-time, singleton actions, theme toggle round-trip |
| new copy strings | strings | `Colors`, `Countries`, `Legend`, `Saved Maps`, `Close <Tool>`, `Switch to dark theme` / `Switch to light theme`, `Saved` / `Unsaved changes`, `Apply Color` |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: The rail row, end to end — icon strip opens one panel, glyph animates from row hover (D-16, D-17, D-29)</name>
  <files>src/components/editor/ToolRail.tsx, src/components/editor/ToolRailRow.tsx, src/components/editor/ToolPanel.tsx, src/components/editor/MapEditor.tsx, src/styles/editor.css</files>
  <read_first>
    - `/Users/matul/claudeprojects/themely/src/components/app-sidebar.tsx` lines 560-667 — `PrimaryNavRow` and `DisabledNavRow`, the recipe to TRANSLATE
    - `03-UI-SPEC.md` § Component Contracts 1 (the utility→CSS translation table and the binding behaviour list) and § 5 (tool panel shell)
    - `src/components/icons/` — the vendored icons and their `*IconHandle` contract from 03-02
    - `src/lib/motion/tokens.ts` — the motion constants
    - `src/utils/motion.ts` — the existing reduced-motion helper
    - `src/hooks/useInspectorUiState.ts` — the state the inspector column used; the rail's open-tool state replaces it
    - `03-RESEARCH.md` § P-5 (Tailwind recipes copied instead of translated), § P-7 (two roving-tabindex writers)
  </read_first>
  <action>
Wire ONE tool end to end before building four: rail row → `aria-expanded` → panel track opens →
panel renders that tool's content → `Escape` closes and returns focus to the row that opened it.
Then expand to the full inventory. Production code, not a prototype.

**A. Translate the Themely recipe; do not copy it.** CountriesIRL has no Tailwind, so a copied
`className` string produces an unstyled element that *looks* wired up. Use the utility→CSS
translation table in `03-UI-SPEC.md` § Component Contracts 1: `block-size: 36px`,
`inline-size: 100%`, `align-items: center`, `white-space: nowrap`,
`border-radius: var(--radius-row)` (10px), the `--text-body-sm` role bundle,
`color: var(--themely-nav-ink)`, hover `--themely-porcelain`, active
(`[aria-expanded="true"]`) `--themely-powder`, a 32×32 icon slot with a 20px glyph **sized via the
`size` prop, never className sizing**.

**B. The binding behaviours, each of which is a decision and not a default:**
- **Hover paint is INSTANT.** No `transition` on the row background. Themely states an ease here
  is *"a regression, not polish"*. Only the glyph animates.
- **Colour is CONSTANT across inactive / hover / active** (`--themely-nav-ink`). No accent bar, no
  blue active icon, no weight bump. **Only the row background carries state.**
- The glyph animation is triggered from **ROW** hover through the imperative handle, not icon
  hover: `onMouseEnter → if (!reducedMotion) iconRef.current?.startAnimation()`,
  `onMouseLeave → iconRef.current?.stopAnimation()`. `startAnimation()` is reduced-motion-gated;
  `stopAnimation()` is **unconditional** — a glyph left mid-animation when the preference flips is
  a stuck animation, not a respected preference.
- The row is a `<button type="button">` with `aria-expanded` and `aria-controls` pointing at the
  panel. **Rows are plain tab stops.**
- **No positional CSS selector may style a rail row.** Key on a role class and a stable
  `data-tool` attribute. A rail of near-identical icon rows is the ideal place to reintroduce that
  bug; assertion 16 (landed in 03-03) enforces it.

**C. Do NOT introduce a second roving-tabindex writer.** There is exactly one, restored in commit
`074173e`, and a second is the regression class that commit fixed. The rail's rows are plain tab
stops or they reuse the existing writer. Assertion 27 (Task 4) gates this.

**D. Tool inventory** (`data-tool` → label → panel contents → glyph):
`colors` → `Colors` → `SelectionPanel` + `ColorPicker` presets + custom hex + `Reset All Colors` →
`palette`; `countries` → `Countries` → `CountryList` + `LocateCountry` → `list`;
`legend` → `Legend` → `LegendEditor` → `layers`; `saved` → `Saved Maps` → save form + saved-map
list → `folder`. The `legend`, `countries`, and `saved` panel *contents* are migrated in **03-07**;
this task builds the rail rows and the panel shell for all four and wires `colors` fully.

Pinned non-tool rows (same recipe, **no** `aria-expanded`): `Undo Color Change` (`undo-2`) and
`Redo Color Change` (`redo-2`). **Labels unchanged** — the e2e locators and the toast allowlist are
keyed to them.

Every rail row shows a **tooltip on hover and focus** carrying its label, because the rail is
icon-only at 56px. That tooltip is chrome (`data-editor-only="true"`) and uses the § Tooltip
recipe.

**E. The panel shell (D-17, D-19).** 280px, reserves layout space, never overlays the map.
`--themely-platinum` body with a `--hairline` right edge; sections are `--themely-porcelain` cards
at `--radius-card`. Title at `--text-h3` with a neutral ghost close button labelled
`Close <Tool>`. `.tool-panel__body` is **the one scroll container**; the title row does not scroll.
**One at a time** — opening a tool closes the previous; clicking the open tool's row closes it;
`Escape` closes the panel and returns focus to the row that opened it.

**No borders on top of borders:** a `--hairline` card never contains another `--hairline` card —
stack by background shift (Platinum → Porcelain). **Stack labelled controls**; reserve rows for
icon-only or single-word controls — a row of full-phrase controls has no width at which it fits a
280px column.

**F. `data-editor-only="true"`** on the rail, the panel, and the row tooltips. They are chrome and
must never reach the export clone.
  </action>
  <verify>
    <automated>npm run lint && npm test && npm run build</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/styles/editor.css','utf8').split('\n').filter(l=>!/^\s*(\/\*|\*)/.test(l)).join('\n');const m=s.match(/\.tool-rail__row[^{]*\{[^}]*\}/g)||[];for(const r of m){if(/transition/.test(r)&&/background/.test(r)){console.error('rail row background is transitioned; D-29 requires instant paint');process.exit(1)}};console.log('INSTANT_HOVER_OK')"</automated>
    <automated>npx playwright test tests/e2e/export.spec.ts --project=chrome --reporter=line</automated>
  </verify>
  <done>
    Clicking a rail icon opens that tool's panel and closes any previous one; the panel reserves
    280px and the canvas reflows; `Escape` closes it and focus lands back on the opening row;
    hovering a row animates only the glyph, instantly paints the row background, and never changes
    the text or icon colour; the export clone contains no rail or panel markup.
  </done>
  <acceptance_criteria>
    - No `transition` applies to a rail row's background in any stylesheet.
    - Rail row text/icon colour resolves to `--themely-nav-ink` in all three states — no accent,
      no weight change.
    - `startAnimation()` is reduced-motion-gated and `stopAnimation()` is called unconditionally.
    - Icon glyphs are sized via the `size` prop; no className-based sizing appears.
    - Exactly one panel can be open; opening a second closes the first.
    - `Escape` returns focus to the row that opened the panel.
    - The rail, panel, and row tooltips all carry `data-editor-only="true"`.
    - `tests/e2e/export.spec.ts` still passes in Chrome.
  </acceptance_criteria>
  <reversibility rating="costly">D-16. The rail-and-panel DOM is what the rewritten CSS contract test, the responsive spec, and the focus-order e2e all assert against.</reversibility>
</task>

<task type="auto">
  <name>Task 2: HUD header and footer — identity, Export, and the theme toggle (D-12, D-13, D-30, D-05)</name>
  <files>src/components/editor/HudHeader.tsx, src/components/editor/HudFooter.tsx, src/components/editor/ThemeToggle.tsx, src/components/Controls.tsx, src/components/Controls.test.tsx, src/styles/editor.css</files>
  <read_first>
    - `src/components/Controls.tsx` (full) — the declared `variant` (`app-bar` | `strip`) and the `controls__action--primary` role class; `:5` carries the `Export PNG` label
    - `src/components/Controls.test.tsx` (full)
    - `03-UI-SPEC.md` § Component Contracts 2 and 3, § Accent budget, § The Export fill is mode-invariant
    - `src/hooks/useCompositionExportTransaction.ts` — the export lock and `isExporting`
    - `03-RESEARCH.md` § Pattern 7 (`.dark` on the mount root)
  </read_first>
  <action>
**A. HUD header (D-12).** Pinned at the top of the rail, above the tools, **always visible, never
scrolls away**. Three slots: a 32×32 `--radius-row` chip on `--themely-powder` holding an
Apple-Blue-free monogram of the composition name (or a `map` glyph when unnamed); the name at
`--text-h3` in `--themely-midnight-ink`, one line with `text-overflow: ellipsis`; and a
`Saved` / `Unsaved changes` pill at `--text-eyebrow` uppercase on Porcelain with Slate Blue text.
Two pill states only — no third.

At 56px (panel closed) the name and pill are `opacity: 0` and removed from the accessible name
**only by truncation, never by `display: none`**. Open (336px) shows the full block.

**The composition name remains identity owned by the composition root**, set only on a committed
save or load, and cleared by `Reset All Colors` and by deleting the saved map it names. That
invariant is unchanged by the layout move. **No Apple Blue anywhere in this block.**

**B. HUD footer (D-13).** Pinned at the bottom, **always visible regardless of panel or scroll
state**.

`Export PNG` is **the rail's one Apple Blue surface**: filled from the mode-invariant
`--accent-fill` — **not** `--themely-apple-blue`, because the flipping token gives white-on-blue
at 3.02:1 in dark mode. Label `--themely-on-accent` at `--text-body-sm` weight 500,
`--radius-control`, hover `--accent-fill-hover`, disabled at 40% opacity with no pointer events.
Icon-only at 56px (`download` glyph, 20px) with its label in the hover/focus tooltip; full
`[icon] Export PNG` when the panel is open. Busy label swaps **exactly**:
`Export PNG` ⇄ `Exporting PNG…`, with native `disabled` + `aria-busy` — **never `aria-disabled` on
a still-clickable button**.

A presentational control that starts an async owner action keeps its own **synchronous activation
lock** (`useRef` cleared in `finally`); the parent's `isExporting` is only true one render later.

**C. `Controls` gains a `rail` variant — it is ADDED, never copied.** `Controls` stays one
component with a declared `variant`; exactly one instance is mounted at a time, which is what
keeps "one filled action" and "one `Reset All Colors`" true **by construction rather than by
review**. Extend `Controls.test.tsx` to assert the variant set and that no second `Controls`
implementation exists.

**D. The theme toggle (D-30).** A **neutral** icon control in the footer beside Export — per D-05
Export keeps the rail's single Apple Blue fill, so the toggle never carries the accent. Ghost
treatment: transparent background, `--themely-nav-ink` glyph, hover `--themely-porcelain`. Glyph
`sun` in light, `moon` in dark. The accessible name states the **destination** —
`Switch to dark theme` / `Switch to light theme` — with `aria-pressed` reflecting the current
mode.

It writes `.dark` to the **editor mount root**, never `document.documentElement` (03-05's gate
enforces this). **No OS colour-scheme read anywhere** — not even to seed a first-run default. The
standalone app defaults to **light**. This keeps the host story clean: a future host controls
`.dark` and there is no OS listener to fight it.

**E. The accent budget is a countable property, not a guideline.** The rail's one Apple Blue
element is `Export PNG`. Active tool row = Powder + Nav Ink. Theme toggle = neutral. Undo/Redo =
neutral. `:focus-visible` rings are Apple Blue on every surface and are **exempt** from the
per-surface budget — transient, keyboard-only, required by NFR11 — and that exemption may not
widen to hover or active states.

**F. Style the accent on a role class, never on a container or a position.** The exact selector
`button:first-child` once meant reordering would have moved the accent onto a `Dismiss` button
with nothing failing.
  </action>
  <verify>
    <automated>npx vitest run src/components/Controls.test.tsx</automated>
    <automated>node -e "const {execSync}=require('child_process');const out=execSync(\"grep -rn 'prefers-color-scheme' src --include='*.ts' --include='*.tsx' || true\").toString().trim();if(out){console.error('OS colour-scheme read in TS/TSX:\n'+out);process.exit(1)};console.log('NO_OS_SCHEME_READ')"</automated>
    <automated>grep -rn "documentElement" src/ && { echo "FAIL: documentElement reference"; exit 1; } || echo MOUNT_ROOT_ONLY</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    The rail header shows the composition glyph, name, and a two-state pill and never scrolls
    away; the footer keeps `Export PNG` and the theme toggle pinned and visible in every panel and
    scroll state; Export is the only filled accent surface in the rail and resolves to `#0071e3`
    in both modes; the toggle flips `.dark` on the mount root with no OS preference read anywhere.
  </done>
  <acceptance_criteria>
    - `Controls` still exports one component with a declared `variant`; a `rail` variant was added
      and no second implementation exists.
    - `Export PNG` consumes `--accent-fill`, not `--themely-apple-blue`.
    - The busy state uses native `disabled` + `aria-busy`; `aria-disabled` on a clickable button
      appears nowhere.
    - The theme toggle's accessible name names the destination and `aria-pressed` reflects the
      current mode.
    - No `prefers-color-scheme` read exists in any `.ts` or `.tsx` under `src/`.
    - No `documentElement` reference exists under `src/`.
    - The header name is hidden by truncation/opacity at 56px, never by `display: none`.
    - No accent is bound to a positional selector.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Persist the last-open tool and the theme through the StorageAdapter (D-18, D-30)</name>
  <files>src/utils/storage.ts, src/utils/storage.test.ts, src/constants/config.ts, src/components/editor/MapEditor.tsx</files>
  <read_first>
    - `src/utils/storage.ts` lines 40-180 — the `StorageAdapter` interface (`:71-88`), the bounded V2 limits (`:45-47`), `getDefaultStorage` (`:140-146`), and `hasSafeJsonBudget` (`:176+`)
    - `src/constants/config.ts` — `ONBOARDING_DISMISSED_KEY` at `:5`, the precedent to follow
    - `src/utils/storage.test.ts` — the existing adapter test shape
    - `.planning/coding-rules/storage.md`
    - `03-RESEARCH.md` § Pattern 9(b), § Runtime State Inventory (the new-keys row)
  </read_first>
  <action>
Two small persisted values, both crossing the same seam, both absent-tolerant.

**A. Follow the `ONBOARDING_DISMISSED_KEY` precedent, do not widen the composition record.**
`src/constants/config.ts:5` already establishes the pattern for a small non-composition key
surfaced through dedicated adapter methods. Add `LAST_OPEN_TOOL_KEY` and `THEME_MODE_KEY` beside
it, and extend `StorageAdapter` with `getLastOpenTool` / `setLastOpenTool` and `getThemeMode` /
`setThemeMode`, each returning the existing `StorageResult` shape.

**B. Both go through the adapter, never raw `localStorage`.** 03-05's gate asserts exactly one
production `localStorage` site under `src/` and it is `storage.ts`. A raw write in `ToolRail.tsx`
or `ThemeToggle.tsx` fails that gate — which is the point.

**C. Respect the bounded V2 record contract.** The length / depth / node budgets are checked
**before** `JSON.parse`. These two values are tiny, but they must be validated on read like
everything else: an unrecognised tool id resolves to **closed**, an unrecognised theme value
resolves to **light**. Do not trust stored strings.

**D. Absent-tolerant defaults are already decided.** A returning creator has neither key. D-18
says first run opens with the panel **closed** — full-bleed world map plus a quiet icon strip —
and only *thereafter* does the rail restore the last-open tool. D-30 says absent theme ⇒
**light**. Neither default may be derived from an OS preference.

**E. Storage failure must not break the editor.** The existing per-reason copy applies: when
storage is blocked, the editor stays usable and Export stays enabled. A failed read of either new
key falls back to its default silently — it is a preference, not data, and it must not produce a
creator-facing message. `ToastRegion` remains the allowlist boundary and **this phase introduces
no new toast, status, or live-region message**; its positive-test count must not change.

**F. Extend `storage.test.ts`** with round-trip, absent-key, malformed-value, and
storage-blocked cases for both keys. *RED probe (mandatory task step):* make the absent-tool case
default to an open panel instead of closed, run `npx vitest run src/utils/storage.test.ts`,
OBSERVE RED, capture output, restore by copying the scratchpad file back, re-run green. Never
`git checkout --` a file with uncommitted work.
  </action>
  <verify>
    <automated>npx vitest run src/utils/storage.test.ts</automated>
    <automated>node -e "const {execSync}=require('child_process');const out=execSync(\"grep -rln 'localStorage' src --include='*.ts' --include='*.tsx' | grep -v '\\\\.test\\\\.' || true\").toString().trim();const f=out?out.split('\n'):[];if(f.length!==1||f[0]!=='src/utils/storage.ts'){console.error('localStorage sites:\n'+out);process.exit(1)};console.log('ONE_STORAGE_SITE_OK')"</automated>
    <automated>npx vitest run src/components/ToastRegion.test.tsx</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    Both new keys round-trip through `StorageAdapter`; an absent last-open-tool key yields a
    closed panel and an absent theme key yields light; malformed values fall back to the same
    defaults; a blocked storage backend leaves the editor fully usable with Export enabled and
    produces no new creator-facing message; the one-`localStorage`-site gate still passes.
  </done>
  <acceptance_criteria>
    - `StorageAdapter` gains exactly four methods and the composition record shape is unchanged.
    - Absent-key behaviour is asserted for both keys with the correct defaults (closed, light).
    - Malformed stored values resolve to the defaults rather than being trusted.
    - The `localStorage` file list under non-test `src/` still has exactly one entry.
    - `ToastRegion`'s positive-test count is unchanged by this plan.
    - The SUMMARY carries the captured RED output from the absent-tool default probe.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Colours panel migration, plus assertions 15 and 27 with their RED proofs</name>
  <files>src/components/ColorPicker.tsx, src/components/SelectionPanel.tsx, src/styles/uiContract.test.ts, tests/e2e/rail.spec.ts</files>
  <read_first>
    - `src/components/ColorPicker.tsx` and `src/components/SelectionPanel.tsx` (full)
    - `src/components/ResetColorsAction.tsx`
    - `03-UI-SPEC.md` § Component Contracts 6 (colour swatch grid) in full
    - `tests/e2e/support/appHarness.ts` (full) — import these fixtures; never re-declare helpers
    - `tests/e2e/responsive.spec.ts` — read the existing focus-order idioms only; **03-09 owns this file**, do not edit it here
    - `src/utils/colors.ts` — `canonicalizeColorMap` and `DEFAULT_COLOR`
  </read_first>
  <action>
**A. Migrate the colours tools into the `colors` panel.** `SelectionPanel`, the `ColorPicker`
presets, the custom hex form, and `Reset All Colors`. Restyle per `03-UI-SPEC.md` § 6:

- **Grid tracks are DERIVED, never fixed:**
  `grid-template-columns: repeat(auto-fit, minmax(76px, 1fr)); gap: var(--space-sm)`. A fixed
  count plus `width: max-content` inside `overflow: hidden` is what once clipped a preset name at
  the tile edge with nothing failing. Overflow stays **visible** so the next one is caught by eye.
- **Tile:** `<button type="button" data-color-name="…" aria-label="Apply <Name>">` — role-classed,
  never styled by position. `min-block-size: 48px`, `--radius-control`, Platinum background,
  `--hairline`, vertical stack. Swatch 24×24 at 8px radius with a
  `1px solid var(--swatch-border)` edge — that border is what keeps a **white** preset visible on
  a Platinum tile, and it is the same fixed value the exported legend swatch uses.
- **Hover:** `--themely-porcelain`, **instant, no transition** — the rail rule applies to every
  hoverable row or tile in the editor.
- **Selected:** `--themely-powder` background, `aria-pressed="true"`, and a 16px `check` glyph at
  the tile's trailing-top corner **on the tile background, not on the swatch**, so it stays
  legible over any creator colour. This replaces `--active-check-border` / `-surface` / `-text`,
  deleted in 03-04 because their fixed values went invisible under `.dark`.
- **Mixed selection:** a 24×24 chip split diagonally between `--themely-porcelain` and
  `--themely-slate-blue` with the `--swatch-border` hairline, plus the visible text `Mixed`. This
  is chrome and **flips with the theme** — it represents a state, not a colour. Replaces the
  deleted `--mixed-color-*` pair.
- **Uncoloured is not filled.** `canonicalizeColorMap` deletes any entry equal to `DEFAULT_COLOR`,
  so white and uncoloured are one state. Unset rows get an **outline**, coloured rows a **fill** —
  painting a filled chip on all 195 rows once made an untouched map read as fully coloured. The
  accessible name stays `Current color #FFFFFF`, because the country really is white and every row
  locator is keyed on that string.
- **Custom hex form:** input at `--text-body-sm`, `--radius-control`, `--hairline`, Apple Blue
  focus ring; error at `--text-caption` in `--themely-red`, wired by `aria-invalid` +
  `aria-describedby`. Its submit is the panel's **one** primary action (`Apply Color`) and the
  only Apple Blue element in the `colors` panel.

**B. Assertion 27 — exactly one roving-tabindex writer in the composed DOM.** Add it to
`uiContract.test.ts` (or, if it needs a live DOM, to `tests/e2e/rail.spec.ts` — state which and
why). *RED probe (mandatory task step):* add a second writer in the rail, OBSERVE RED, capture,
restore by copying from the scratchpad.

**C. Assertion 15 (rail half) — exactly one `Reset View`, exactly one `Reset All Colors`, exactly
one filled primary action in the composed DOM.** `Reset View` is camera-only and lives in the
floating cluster (03-08); `Reset All Colors` is content reset and lives in the Colors panel — **the
two never sit together**. 03-07 completes this assertion once the remaining panels are migrated.
*RED probe:* render a second `Reset All Colors`, OBSERVE RED, capture, restore.

**D. `tests/e2e/rail.spec.ts`.** Import fixtures from `tests/e2e/support/appHarness.ts` — do not
re-declare camera helpers; two existing specs already carry that debt as a recorded pending todo.
Assert:
- the **spec'd** focus order through header → rail rows → footer → panel, **including controls
  whose disabled state removes them**. A test that documents a deviation is a comment, not a test;
  every focus-order claim needs a RED probe against the arrangement it replaced.
- one panel open at a time; clicking the open row closes it; `Escape` closes and returns focus;
- the colour workflow end to end: select a country, apply a preset, see the fill, undo, redo;
- the theme toggle round-trip: toggle, confirm `.dark` lands on the **mount root** (not
  `documentElement`), reload, confirm the choice persisted, and confirm the map fill did **not**
  change with the theme.

Chrome only. Edge is not installed (D-33) — no `msedge` run, no Edge result.
  </action>
  <verify>
    <automated>npx vitest run src/styles/uiContract.test.ts src/components/ColorPicker.test.tsx</automated>
    <automated>npx playwright test tests/e2e/rail.spec.ts --project=chrome --reporter=line</automated>
    <automated>grep -q "support/appHarness" tests/e2e/rail.spec.ts && echo HARNESS_IMPORTED</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    The colours tools live in the `colors` panel with derived grid tracks, instant hover, a
    tile-background check on the selected tile, an outline-vs-fill distinction for unset rows, and
    exactly one filled `Apply Color` action; assertion 27 and the rail half of assertion 15 are
    green and RED-proven; `rail.spec.ts` passes in Chrome.
  </done>
  <acceptance_criteria>
    - The preset grid uses `repeat(auto-fit, minmax(76px, 1fr))` — no fixed column count.
    - No preset label is truncated at 280px; overflow is visible.
    - The selected-tile check glyph sits on the tile background, not on the swatch.
    - Unset rows render as outlines and coloured rows as fills; the `Current color #FFFFFF`
      accessible name is unchanged.
    - Exactly one roving-tabindex writer exists in the composed DOM (assertion 27, RED-proven).
    - Exactly one `Reset All Colors` and exactly one filled primary action exist (assertion 15
      rail half, RED-proven).
    - `rail.spec.ts` imports from `appHarness.ts` and declares no camera helper of its own.
    - `grep -c msedge tests/e2e/rail.spec.ts` returns 0.
    - The SUMMARY carries two captured RED outputs (second writer, second `Reset All Colors`).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored preference → editor state | Two new persisted keys read on mount; stored strings are untrusted |
| creator input → the custom hex field | The one input surface this plan touches |
| rail/panel chrome → the exported composition | Everything in this plan is `data-editor-only` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-21 | Tampering | a raw `localStorage` write for the new preference keys | high | mitigate | Both keys cross `StorageAdapter`; 03-05's one-site gate re-run in this plan's verify; the `ONBOARDING_DISMISSED_KEY` precedent followed rather than widening the composition record |
| T-03-22 | Denial of service | a poisoned or oversized stored preference | medium | mitigate | Bounded V2 budgets checked before `JSON.parse`; unrecognised tool id ⇒ closed, unrecognised theme ⇒ light; failures are silent fallbacks, not creator-facing messages |
| T-03-23 | Information disclosure | an unbounded string reaching a creator-facing surface | medium | accept | This phase introduces **no** new toast, status, or live-region message; `ToastRegion`'s allowlist and its positive-test count are asserted unchanged, so the risk does not materialise here |
| T-03-24 | Repudiation | a focus-order test that documents a deviation instead of asserting the spec | high | mitigate | The focus-order test asserts the SPEC'D order including disabled-removed controls, with a RED probe against the arrangement it replaced |
| T-03-25 | Tampering | a second roving-tabindex writer | high | mitigate | Assertion 27 with an executed RED probe; rail rows are plain tab stops and do not add a writer |
| T-03-26 | Input validation | the custom hex field | low | mitigate | Unchanged `normalizeColor` path; `aria-invalid` + `aria-describedby` wiring; no new input routed around the existing validators |
</threat_model>

<verification>
```bash
npm run lint
npm test
npm run build
npx playwright test tests/e2e/rail.spec.ts tests/e2e/export.spec.ts tests/e2e/shell.spec.ts --project=chrome
npx vitest run src/utils/storage.test.ts src/components/Controls.test.tsx src/styles/uiContract.test.ts
grep -rn "prefers-color-scheme" src/ ; echo "expect: no output"
grep -rn "documentElement" src/ ; echo "expect: no output"
```

By hand: `npm run dev`, hard reload, then check both modes via the footer toggle (there is no OS
listener to use), confirm the panel restores the last-open tool on reload and opens **closed** on
a cleared storage, and confirm the map fill does not change when the theme flips.

Chrome only. Edge is not installed (D-33).
</verification>

<success_criteria>
- [ ] One tool panel open at a time; clicking the open row closes it; `Escape` closes and restores focus (D-16, D-17)
- [ ] Row hover paints instantly with constant text/icon colour; only the glyph animates, from row hover, reduced-motion-gated on start and unconditional on stop (D-29)
- [ ] The HUD header pins identity and a two-state pill and never scrolls away; the name is hidden by truncation, never `display: none` (D-12)
- [ ] `Export PNG` is pinned, filled from `--accent-fill`, and is the rail's only accent surface (D-13, D-05)
- [ ] The neutral theme toggle writes `.dark` to the mount root, persists through the adapter, defaults to light, and reads no OS preference anywhere (D-30)
- [ ] Last-open tool persists through the adapter with an absent key resolving to closed (D-18)
- [ ] `Controls` gained a `rail` variant and was not copied
- [ ] Exactly one roving-tabindex writer (assertion 27, RED-proven)
- [ ] Exactly one `Reset All Colors` and one filled primary action (assertion 15 rail half, RED-proven)
- [ ] `tests/e2e/rail.spec.ts` green in Chrome, importing shared fixtures
- [ ] `ToastRegion`'s allowlist and positive-test count are unchanged
</success_criteria>

<output>
Create `.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-06-SUMMARY.md` when done, carrying a
`RED probes (4, with output)` section and a `New copy strings` list confirming none of them reaches
`onStatusMessage`.

**Do not run `state.advance-plan`, `state.update-progress`, or `roadmap.update-plan-progress`.**
Edit `.planning/STATE.md` and `.planning/ROADMAP.md` by hand.
</output>
