---
phase: 03-clean-ui-overhaul-1-1-5-weeks
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/styles/editor.css
  - src/styles/MapCanvas.css
  - src/styles/uiContract.test.ts
  - src/main.tsx
  - src/components/MapWorkspace.tsx
  - src/components/MapWorkspace.test.tsx
  - tests/e2e/shell.spec.ts
autonomous: true
requirements:
  - D-11
  - D-16
  - D-19
  - D-32
  - A-10
  - A-11
  - A-16

estimate:
  tokens: 150000
  raw_tokens: 75000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "D-11 / D-32: the map surface is edge to edge with no top chrome, and a visible centred 1:1 export frame marks exactly what lands in the PNG, so WYSIWYG is preserved without touching the 1080x1080 contract."
    - "D-16 / D-19: a 56px always-present icon rail and a panel track that resolves to 0px closed and 280px open sit in one CSS grid on the editor mount root; the panel RESERVES layout space and the canvas reflows — it never overlays the map."
    - "Assertion 10: `[data-panel-open]` is exactly 'true' | 'false' — never absent, never a third value — and the panel track resolves to 0px closed and 280px open."
    - "Assertion 11: the `.map-frame` client rect equals the on-screen projection of the SVG viewBox corners (0,0) and (1080,1080) via `getScreenCTM()`, so the frame marks the real export square rather than approximating it."
    - "Assertion 16: no positional selector (:nth-child, :first-child, :last-child) styles any button, input, select, a, or summary — a rail of near-identical icon rows is the ideal place to reintroduce that bug."
    - "D-32 verified consequence: no `ResizeObserver` is added anywhere, because the viewBox is fixed and d3-zoom's extent is pinned to [[0,0],[1080,1080]] — a rail/panel reflow cannot disturb the projection, the camera lease, or the export."
    - "`src/styles/uiContract.test.ts` exists as the successor to `phase2CssContract.test.ts`, with the stylesheet parser and helpers ported VERBATIM (infrastructure, not policy) so the successor is not a weaker reimplementation."
  artifacts:
    - src/styles/editor.css
    - src/styles/uiContract.test.ts
    - tests/e2e/shell.spec.ts
  key_links:
    - "`.map-frame` geometry ↔ `preserveAspectRatio=\"xMidYMid meet\"` — both are 'the centred min(w,h) square of the same box', which is what makes assertion 11 an equality rather than an approximation."
    - "`[data-panel-open]` on `.map-editor` is the single source of the panel track width; 03-06's rail rows write it and nothing else may."
    - "The ported parser in `uiContract.test.ts` is what 03-04, 03-08, 03-09, and 03-10 all add assertions to — a weak parser here weakens six later gates."
  backstops:
    - statement: "Transitioning the registered `--panel-width` custom property is smoother than transitioning `grid-template-columns` across a 248x3-path SVG; worst case the panel snaps instantly, which D-29's spirit arguably prefers."
      verification: backstop
    - statement: "The tool panel body remains contained and usable at 360px and at the 200%-equivalent halved viewport; re-verified in the new chrome by 03-09, with the halved viewport labelled as the equivalent, never as physical zoom."
      verification: backstop
---

<objective>
Land the shell: one CSS grid on the editor mount root carrying a 56px icon rail, a panel track
that reserves 0px or 280px, and a full-bleed canvas region with a centred 1:1 export frame. Create
`uiContract.test.ts` as the successor to `phase2CssContract.test.ts` by porting its parser
verbatim, and land assertions 10, 11, and 16 with RED proofs.

Purpose: this is the structural decision every later plan builds on, and D-32 makes it a
WYSIWYG-critical one. The frame is not decoration — it is the creator's only signal of what lands
in the PNG once the canvas stops being a square.

Output: `src/styles/editor.css`, `src/styles/uiContract.test.ts`, `tests/e2e/shell.spec.ts`, and
a `MapWorkspace` that renders the frame as structural chrome without becoming a third slot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/export.md
@Design.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-UI-SPEC.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-02-SUMMARY.md

Read narrowly:
- `03-RESEARCH.md` § Pattern 3, § Pattern 8, § Current-State Inventory (`Controls.css` split table),
  § OQ-2
- `03-CONTEXT.md` D-11, D-16, D-19, D-32
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| `src/styles/editor.css` | stylesheet | `.map-editor` grid, `.tool-rail`, `.tool-panel`, `.map-workspace`, `--rail-width`, `--panel-width` |
| `src/styles/uiContract.test.ts` | test | successor to `phase2CssContract.test.ts`; parser + helpers ported verbatim; owns assertions 10, 16 here and gains 1-9, 12, 17-21, 26 later |
| `tests/e2e/shell.spec.ts` | e2e | assertion 11 (frame ↔ viewBox equality) and the measured 0px/280px panel track |
| `.map-frame` element | DOM | `data-editor-only="true" aria-hidden="true"`, structural sibling of `div.map-export-source` |
| `.map-workspace__canvas` | CSS class | renamed from `.map-workspace__square`; `aspect-ratio: 1` moves to `.map-frame` |
| new CSS custom properties | tokens | `--rail-width`, `--panel-width`, `--map-frame-edge`, `--map-frame-scrim` |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end shell slice — full-bleed canvas, one rail track, one panel track, one export frame</name>
  <files>src/styles/editor.css, src/main.tsx, src/components/MapWorkspace.tsx, src/styles/MapCanvas.css</files>
  <read_first>
    - `src/components/MapWorkspace.tsx` (full) — the typed `legendSlot` / `navigationSlot` contract that must survive verbatim
    - `src/components/MapCanvas.tsx` lines 830-860 — the fixed `viewBox` and `preserveAspectRatio`
    - `src/hooks/useCameraController.ts` lines 295-340 — the pinned d3-zoom `extent`
    - `src/styles/MapCanvas.css` lines 85-120 — the current `.map-workspace__square` rules
    - `src/styles/App.css` — the `.app`, `.app-bar`, `.workspace*` rules the grid replaces
    - `src/main.tsx` — the explicit stylesheet import list
    - `03-UI-SPEC.md` § Layout Contract in full (lines ~417-570)
  </read_first>
  <action>
Wire ONE path through every layer this phase touches: mount root → grid → rail track → panel
track → canvas region → export frame → the untouched export source. One entry point, no other
call sites, real error handling on the single path. This is production code, not a prototype —
03-05 and 03-06 expand from it, they do not replace it.

**A. `src/styles/editor.css`.** Author the shell exactly as `03-UI-SPEC.md` § The shell specifies:

`.map-editor` is the mount root — `display: grid`, `grid-template-columns: var(--rail-width)
var(--panel-width) 1fr`, `block-size: 100dvh`, `background: var(--themely-platinum)`, and it
carries the `[data-panel-open]` attribute. `--rail-width: 56px` and `--panel-width: 0px` on
`:root`; `.map-editor[data-panel-open='true']` sets `--panel-width: 280px`.

Register `--panel-width` with `@property` (syntax `<length>`, `inherits: true`,
`initial-value: 0px`) and transition **the custom property**, over `--motion-duration-base` with
`--motion-ease-out`. Do **not** transition `grid-template-columns` — it fires many layout passes
per frame across a 248 × 3-path SVG. If the registered-property transition does not work in
installed Chrome, accept an instant snap and record that; do not fall back to transitioning the
grid template.

`html` / `body` drop to **layout-only rules with no themed background** — everything the editor
paints lives inside its own subtree (this resolves OQ-3 and is a transition-readiness sub-gate,
because a host must be able to place one opaque element and get the whole editor). Keep
`overflow-x: hidden` on `body` **and nowhere else**: on a non-viewport element it computes
`overflow-y: auto` and silently kills sticky positioning inside it.

**B. The canvas region and the export frame (D-32).** Rename `.map-workspace__square` to
`.map-workspace__canvas` — a class named `__square` that is no longer square is exactly the stale
name this repo's rules reject. Carry over `background: var(--map-surface)` and
`overflow: hidden`; add `container-type: size` so `min(w,h)` is expressible in pure CSS. Do NOT
carry over `aspect-ratio: 1`, the border, `--radius-large`, or `--map-shadow`.

`aspect-ratio: 1` **moves to `.map-frame`** — the Phase 2 assertion survives, relocated, and the
relocation is the visible form of the D-32 decision. Author `.map-frame` with the
`--frame-side: min(100cqw, 100cqh)` / `inset: 0` / `margin: auto` / `pointer-events: none`
recipe from the UI-SPEC, a 1px `--map-frame-edge` hairline, and
`box-shadow: 0 0 0 100vmax var(--map-frame-scrim)` to dim everything outside. No accent, no
radius, no label. Declare `--map-frame-edge: rgba(6, 27, 49, 0.55)` and
`--map-frame-scrim: rgba(6, 27, 49, 0.06)` in the **unconditioned `:root`** and in no `.dark`
block — they are new members of the mode-invariant set.

**C. `MapWorkspace` renders the frame.** Add `<div class="map-frame" data-editor-only="true"
aria-hidden="true">` as a **structural child of the canvas region and a sibling of
`div.map-export-source`** — never a descendant of `svg.map-canvas`. It is **not** a slot: the
typed `legendSlot` / `navigationSlot` props are preserved **verbatim** (D-24), and the frame is
structural chrome, not composable content. Placement decides export membership;
`data-editor-only` is the second line of defence, not the first.

**D. One real panel-open path.** Drive `[data-panel-open]` from real state so the track actually
animates end to end. The attribute is exactly `'true' | 'false'` — never absent, never a third
value — so assertion 10 can enumerate it. For this tracer, one keyboard-reachable trigger is
enough; 03-06 replaces the trigger with the rail rows. That swap is a trigger change, not an
architectural change, which is why the stub is acceptable here. Do not add a second writer of
this attribute.

**E. Do not add a `ResizeObserver`, and do not let anyone else.** `MapCanvas.tsx:839-840` fixes
the `viewBox` and `useCameraController.ts:310-313` pins d3-zoom's `extent` to
`[[0,0],[1080,1080]]` rather than the element rect, so a rail/panel reflow cannot disturb the
projection, the camera lease, or the export. This is verified, not assumed. If something appears
to need one, that is a signal the geometry was re-derived somewhere it should not have been.

**F. Import the new stylesheet in `main.tsx`.** `main.tsx` imports stylesheets explicitly; add
`editor.css` there. Note for 03-10: the successor contract test globs the styles directory, so
the globbed count and the `main.tsx` import count must stay equal.
  </action>
  <verify>
    <automated>npm run lint && npm test && npm run build</automated>
    <automated>grep -rn "ResizeObserver" src/ && { echo "FAIL: ResizeObserver introduced"; exit 1; } || echo NO_RESIZE_OBSERVER</automated>
    <automated>grep -c "map-workspace__square" src/ -r || echo NO_STALE_SQUARE_CLASS</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/styles/editor.css','utf8');if(/transition[^;]*grid-template-columns/.test(s)){console.error('grid-template-columns is transitioned');process.exit(1)};if(!/@property\s+--panel-width/.test(s)){console.error('--panel-width not registered with @property');process.exit(1)};console.log('PANEL_TRACK_OK')"</automated>
  </verify>
  <done>
    `npm run dev` shows a full-bleed map surface with no top chrome, a 56px rail column, a panel
    column that opens to 280px and reflows the canvas without overlaying it, and a centred square
    frame with everything outside it dimmed. Export still produces a 1080×1080 PNG. No
    `ResizeObserver` exists anywhere under `src/`.
  </done>
  <acceptance_criteria>
    - `.map-editor` uses a three-track grid and `block-size: 100dvh`.
    - `[data-panel-open]` is present on `.map-editor` at all times with value `true` or `false`.
    - `.map-frame` is a sibling of `div.map-export-source`, carries `data-editor-only="true"` and
      `aria-hidden="true"`, and is NOT a descendant of `svg.map-canvas`.
    - `MapWorkspace`'s `legendSlot` and `navigationSlot` prop types are byte-unchanged, and the
      frame is not added as a third slot.
    - `aspect-ratio: 1` appears on `.map-frame` and no longer on the canvas-region class.
    - `--map-frame-edge` and `--map-frame-scrim` are declared once in `:root` and in no `.dark`
      block.
    - `grep -rn "ResizeObserver" src/` returns nothing.
    - `grep -rn "map-workspace__square" src/` returns nothing.
    - `npm run lint && npm test && npm run build` clean.
  </acceptance_criteria>
  <reversibility rating="costly">D-16/D-19. The rail-and-panel DOM is what the rewritten CSS contract test, the responsive spec, and the focus-order e2e all assert against; reverting means re-authoring all three.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Create `uiContract.test.ts` — port the parser verbatim, land assertions 10 and 16</name>
  <files>src/styles/uiContract.test.ts</files>
  <read_first>
    - `src/styles/phase2CssContract.test.ts` (full — this is a port, so the whole file matters; note lines 202-213 hard-coding four filenames, 215/221/233 the `prefers-color-scheme` conditions, 947-994 the export-unsafe guard, 1016-1025 the square assertions)
    - `03-RESEARCH.md` § Pattern 8 (lines ~937-1011) — the keep-verbatim / carry-forward / retire triage
    - `03-UI-SPEC.md` § Contract Assertions (the full 28-row table)
  </read_first>
  <action>
Create `src/styles/uiContract.test.ts` as the successor to `phase2CssContract.test.ts`.

**Port the parser and helpers VERBATIM.** `03-RESEARCH.md` § Pattern 8 triages the existing file
into three groups; the first is *"Keep verbatim — infrastructure, not policy."* Copy that
infrastructure across unchanged. A hand-rewritten parser in the successor is how a contract test
quietly gets weaker — and this file is what 03-04, 03-08, 03-09, and 03-10 all add to.

**Do not delete `phase2CssContract.test.ts` in this plan.** Both run side by side until 03-04
lands the new tokens and retires the old file. Deleting it here would leave the Phase 2 token
rules unguarded during the wave the tokens are being replaced in.

**Change the file globbing now, not later.** `phase2CssContract.test.ts:202-213` hard-codes four
stylesheet filenames. The successor **globs** `src/styles/**/*.css` instead, because 03-10 splits
`Controls.css` into `src/styles/controls/*.css` and a hard-coded list means a new file silently
escapes every assertion. Assertion 20 (globbed count equals `main.tsx` import count) lands in
03-10; build the globbing seam here so 03-10 only adds the count comparison.

**Assertion 10 — the panel track.** Assert `[data-panel-open]` resolves to exactly the two values
`'true'` and `'false'` in the stylesheet's selector set (no third value is styled), and that the
panel track resolves to `0px` when closed and `280px` when open. *RED probe:* add a
`[data-panel-open='partial']` rule and watch it fail.

**Assertion 16 — no positional selector on an interactive element.** Assert that no rule whose
selector contains `:nth-child`, `:first-child`, or `:last-child` also targets `button`, `input`,
`select`, `a`, or `summary`. These bind a role to an ordinal, and order is copy. Key on a role
class and a stable `data-tool` attribute instead. *RED probe:* add a rule styling a rail row by
index.

**Carry forward the export-unsafe-CSS guard at high priority** (`phase2CssContract.test.ts:947-994`).
Port the guard itself now and keep `EXPORT_CONTENT_PATTERN` **bound back to `MapCanvas.tsx`
source** — `.map-unit-path` was once omitted from that pattern for a whole phase. Its *stated
reason* is rewritten in 03-02's `Design.md` and must be rewritten here in the same words: the
Phase 2 wording about html2canvas rasterising differently **goes false when D-34 lands**; the true
post-D-34 reason is that an externally-styled effect inside an SVG-as-image isolated document
renders **not at all**.

**RED PROBES (mandatory, as task steps).** For each of assertions 10 and 16: copy the subject file
to the scratchpad, break the subject, run `npx vitest run src/styles/uiContract.test.ts`, OBSERVE
RED, capture the output, restore by copying back, re-run green. Never `git checkout --` a file
with uncommitted work.

**Comment discipline.** Do not write the literal strings these assertions negative-grep for into
any comment in the files under test. A header comment that names the forbidden pattern makes the
gate self-invalidating. Where a count is taken over a stylesheet, filter comment lines first
(`grep -v '^\s*/\*' | grep -v '^\s*\*'`) rather than counting raw.
  </action>
  <verify>
    <automated>npx vitest run src/styles/uiContract.test.ts</automated>
    <automated>npx vitest run src/styles/phase2CssContract.test.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/styles/uiContract.test.ts','utf8');if(!/glob|readdirSync/.test(s)){console.error('uiContract.test.ts does not glob the styles directory');process.exit(1)};if(/'App\.css'[\s\S]{0,120}'Controls\.css'[\s\S]{0,120}'MapCanvas\.css'/.test(s)){console.error('hard-coded stylesheet filename list survived the port');process.exit(1)};console.log('GLOB_SEAM_OK')"</automated>
    <automated>npm test</automated>
  </verify>
  <done>
    `src/styles/uiContract.test.ts` exists with the parser ported verbatim, globs the styles
    directory rather than hard-coding filenames, enforces assertions 10 and 16, and carries the
    export-unsafe-CSS guard with its post-D-34 reason. `phase2CssContract.test.ts` still passes
    alongside it.
  </done>
  <acceptance_criteria>
    - Both contract tests pass; neither is deleted in this plan.
    - `uiContract.test.ts` discovers stylesheets by globbing, and no hard-coded four-filename list
      survives.
    - `EXPORT_CONTENT_PATTERN` is present and bound back to `MapCanvas.tsx` source.
    - The export-unsafe guard's stated reason does NOT claim html2canvas rasterises differently.
    - The SUMMARY carries the captured RED output for assertion 10 AND assertion 16.
    - No comment in any file under test contains a literal that an assertion negative-greps for.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Assertion 11 — the frame rect equals the viewBox projection, measured in a real browser</name>
  <files>tests/e2e/shell.spec.ts</files>
  <read_first>
    - `tests/e2e/support/appHarness.ts` (full) — import these fixtures; never re-declare camera or browser helpers
    - `tests/e2e/phase2-composition.spec.ts` — the existing measurement idioms to match
    - `03-UI-SPEC.md` § The canvas region and the export frame — the geometry contract table
    - `src/components/MapCanvas.tsx` lines 830-860
  </read_first>
  <action>
Write `tests/e2e/shell.spec.ts`. Import the shared fixtures from `tests/e2e/support/appHarness.ts`
— do not re-declare helpers; two existing specs already carry that debt and it is a recorded
pending todo.

**Assertion 11.** In the page, take `svg.map-canvas`, call `getScreenCTM()`, and project the
viewBox corners `(0,0)` and `(1080,1080)` through it to screen coordinates. Take
`.map-frame`'s `getBoundingClientRect()`. Assert the two agree within a sub-pixel tolerance
(≤ 1px on each edge; state the tolerance in the test and say why).

This is an equality rather than an approximation because both are literally "the centred
`min(w, h)` square of the same box": `preserveAspectRatio="xMidYMid meet"` centres the viewBox
square, and `--frame-side: min(100cqw, 100cqh)` with `inset: 0; margin: auto` centres the frame.
Assert it at more than one viewport shape — at least one wide, one tall, and one near-square — so
a rule that only happens to line up at the default size fails.

**Also measure the panel track (assertion 10's runtime half).** With `[data-panel-open='false']`
assert the computed rail column is 56px and the panel column is 0px; with `'true'` assert the
panel column is 280px and the canvas column shrank by exactly that amount. Assert the panel does
**not** overlay the map — the canvas region's left edge moves right by 280px rather than staying
put (D-19).

**And assert the export is undisturbed by the reflow.** Open the panel, close it, open it again,
then export and assert the PNG is exactly 1080×1080. That is the cheap end-to-end confirmation of
D-32's verified claim that no `ResizeObserver` is needed.

**RED PROBE (mandatory, as a task step).** Copy `src/styles/MapCanvas.css` (or wherever
`.map-frame` is authored) to the scratchpad. Inset the frame by 1px — e.g. change `inset: 0` to
`inset: 1px`. Run `npx playwright test tests/e2e/shell.spec.ts --project=chrome`, OBSERVE RED,
capture the output, restore by copying the scratchpad file back, re-run green. Never
`git checkout --` a file with uncommitted work.

Chrome only. Edge is not installed on this machine (D-33); do not add an `msedge` run and do not
report an Edge result.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/shell.spec.ts --project=chrome --reporter=line</automated>
    <automated>grep -q "from './support/appHarness'" tests/e2e/shell.spec.ts && echo HARNESS_IMPORTED</automated>
  </verify>
  <done>
    `tests/e2e/shell.spec.ts` passes in Chrome, asserting frame↔viewBox equality at three viewport
    shapes, the measured 56/0/280 track widths in both panel states, non-overlay reflow, and a
    1080×1080 export after a panel open/close cycle. The 1px-inset RED probe was executed and
    captured.
  </done>
  <acceptance_criteria>
    - The spec imports from `tests/e2e/support/appHarness.ts` and declares no camera helper of
      its own.
    - Frame↔viewBox equality is asserted at a wide, a tall, and a near-square viewport.
    - The closed-state panel column measures 0px and the open-state 280px, measured, not assumed.
    - A post-reflow export is asserted to be exactly 1080×1080.
    - `grep -c msedge tests/e2e/shell.spec.ts` returns 0.
    - The SUMMARY carries the captured RED output from the 1px inset probe.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Record the D-32 / OQ-2 resolution and the narrow-width contract in writing</name>
  <files>src/components/MapWorkspace.test.tsx, Design.md</files>
  <read_first>
    - `src/components/MapWorkspace.test.tsx` (full) — the existing slot-contract assertions to extend, not replace
    - `03-UI-SPEC.md` § Narrow width (D-20) — spec only here; 03-09 implements and gates it
    - `03-RESEARCH.md` § OQ-2
  </read_first>
  <action>
Two recording jobs, both of which exist so a later reader cannot mistake a decision for an
accident.

**A. Extend `MapWorkspace.test.tsx`** (`node` env, markup-string assertions — this repo's Vitest
has no DOM, and `App.test.tsx:274-276` is the precedent for asserting on rendered markup strings).
Add assertions that:
- the `legendSlot` and `navigationSlot` props are still typed and still render into their
  documented positions — **preserved verbatim** (D-24);
- `.map-frame` renders as a **sibling** of `div.map-export-source`, not a descendant of
  `svg.map-canvas` — placement decides export membership;
- `.map-frame` carries `data-editor-only="true"` and `aria-hidden="true"`;
- the frame is not exposed as a slot prop.

*RED probe:* move the frame inside `div.map-export-source` and watch the sibling assertion fail.
Copy to the scratchpad first; restore by copying back.

**B. Record OQ-2's resolution in `Design.md`** under the layout section. State the decision in the
form D-32 states it: the map **surface** is full-bleed and pans/zooms in the Google-Maps idiom,
but a visible square export frame sits centred on it marking exactly what lands in the PNG. The
SVG `viewBox` stays `0 0 1080 1080`, `preserveAspectRatio="xMidYMid meet"` is unchanged, geometry
beyond the square paints into the gutters, and the export clone re-asserts the square so gutter
geometry is clipped and the PNG is untouched.

Record the two verified facts that make this safe, with their file:line citations, so nobody
re-derives them: `useCameraController.ts:310-313` pins d3-zoom's `extent` to `[[0,0],[1080,1080]]`
rather than the element rect, and `MapCanvas.tsx:839-840` fixes the `viewBox`. **Therefore no
`ResizeObserver` is required and none may be added.**

Also record the **narrow-width contract** (D-20) as specification, not implementation: below the
existing 1200px breakpoint the rail becomes a bottom bar with `--target-compact` (44px) targets,
a tapped tool raises a bottom sheet over the map (the one surface that overlays the canvas),
entering on `--motion-duration-base` / `--motion-ease-out` and exiting on `--motion-ease-in`, the
floating cluster sits above the bottom bar and still outside the frame, and the HUD header/footer
fold into the bottom bar with Export still pinned and visible. 03-09 implements and gates this;
recording it here is what stops 03-09 inventing it.

Add the `Last updated` line per `CLAUDE.md` § Update Process and keep only the two most recent
entries — merge the two oldest into one line in the same edit if a third would appear.
  </action>
  <verify>
    <automated>npx vitest run src/components/MapWorkspace.test.tsx</automated>
    <automated>node -e "const s=require('fs').readFileSync('Design.md','utf8');const need=['useCameraController.ts:310-313','MapCanvas.tsx:839-840','ResizeObserver','--target-compact'];const miss=need.filter(t=>!s.includes(t));if(miss.length){console.error('Design.md missing OQ-2/D-20 record:',miss);process.exit(1)};console.log('RECORDED_OK')"</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    `MapWorkspace.test.tsx` asserts the slot contract is preserved verbatim and the frame is a
    sibling carrying `data-editor-only`; `Design.md` records the D-32/OQ-2 resolution with its
    two verified citations and the narrow-width contract for 03-09 to implement against.
  </done>
  <acceptance_criteria>
    - `npx vitest run src/components/MapWorkspace.test.tsx` passes with the new assertions.
    - The slot-prop types in `MapWorkspace.tsx` are byte-unchanged from before this plan.
    - `Design.md` cites `useCameraController.ts:310-313` and `MapCanvas.tsx:839-840` verbatim and
      states that no `ResizeObserver` may be added.
    - `Design.md` carries the D-20 narrow-width contract as specification for 03-09.
    - The SUMMARY carries the captured RED output from the frame-sibling probe.
    - `Design.md` has at most two `Last updated` entries.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor mount root → host document | The shell must paint entirely inside its own subtree so a future host can place one element |
| chrome DOM → the export-bearing composition | `.map-frame`, the rail, and the panel must never cross into `svg.map-canvas` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | `.map-frame` placement | high | mitigate | Rendered as a structural sibling of `div.map-export-source`, asserted in `MapWorkspace.test.tsx`; `data-editor-only="true"` as the second line of defence; a descendant placement fails the sibling assertion |
| T-03-08 | Tampering | shell CSS reaching exported content | medium | mitigate | The export-unsafe-CSS guard is ported into `uiContract.test.ts` at high priority with `EXPORT_CONTENT_PATTERN` bound back to `MapCanvas.tsx`; `backdrop-filter` banned outright |
| T-03-09 | Denial of service | transitioning `grid-template-columns` across a 248x3-path SVG | low | mitigate | `--panel-width` registered with `@property` and transitioned instead; a `grid-template-columns` transition fails Task 1's automated check |
| T-03-10 | Repudiation | a layout gate that cannot fail | high | mitigate | Assertions 10, 11, and 16 each carry a mandatory executed RED probe with captured output; assertion 11 measures projected geometry rather than asserting a class exists |
</threat_model>

<verification>
```bash
npm run lint
npm test
npm run build
npx playwright test tests/e2e/shell.spec.ts --project=chrome
npx vitest run src/styles/uiContract.test.ts src/styles/phase2CssContract.test.ts
grep -rn "ResizeObserver" src/ ; echo "expect: no output"
```

Then by hand: `npm run dev`, **hard reload** (the dev server serves unhashed stylesheets and a warm
cache will show the old chrome), and confirm the full-bleed surface, the centred dimmed-outside
frame, and the panel reflow.

Chrome only. Edge is not installed (D-33) and no Edge result may be reported.
</verification>

<success_criteria>
- [ ] `.map-editor` grid renders a 56px rail track, a 0/280px panel track, and a `1fr` canvas region at `100dvh`
- [ ] `[data-panel-open]` is always present and always `'true'` or `'false'` (assertion 10, RED-proven)
- [ ] `.map-frame` client rect equals the projected viewBox square within 1px at wide, tall, and near-square viewports (assertion 11, RED-proven by a 1px inset)
- [ ] No positional selector styles an interactive element (assertion 16, RED-proven)
- [ ] `legendSlot` / `navigationSlot` prop types are byte-unchanged and the frame is not a third slot
- [ ] `aspect-ratio: 1` moved from the canvas region to `.map-frame`; `.map-workspace__square` no longer exists
- [ ] No `ResizeObserver` anywhere under `src/`
- [ ] `uiContract.test.ts` globs stylesheets and ported the parser verbatim; `phase2CssContract.test.ts` still passes
- [ ] Export after a panel open/close cycle is exactly 1080×1080
- [ ] `Design.md` records the OQ-2 resolution with its two verified citations and the D-20 narrow-width contract
</success_criteria>

<output>
Create `.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-03-SUMMARY.md` when done, carrying a
`RED probes (4, with output)` section: assertion 10, assertion 16, assertion 11 (1px inset), and
the frame-sibling probe.

**Do not run `state.advance-plan`, `state.update-progress`, or `roadmap.update-plan-progress`.**
Edit `.planning/STATE.md` and `.planning/ROADMAP.md` by hand.
</output>
