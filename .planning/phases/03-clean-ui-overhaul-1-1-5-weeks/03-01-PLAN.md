---
phase: 03-clean-ui-overhaul-1-1-5-weeks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/assets/inter-latin-variable.woff2
  - src/assets/README.md
  - tests/e2e/spike-export-font.spec.ts
autonomous: false
requirements:
  - D-09
  - D-27
  - D-31
  - OQ-1
  - R-V1
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 48000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "D-31: `git tag --list` names an annotated tag on fe5f946060707c48c3d9591d368b5f3f8f90dd4d, created before any other Phase 3 work, so the owner can check out the exact build 02-28-ACCEPTANCE-MATRIX.md describes."
    - "OQ-1 is answered from a real browser, not from documentation: a throwaway Playwright spike proves (or disproves) that an inline base64 @font-face renders inside an SVG-as-image in installed Chrome, and the answer is recorded in writing before any legend typography is locked (D-25 precondition, owned by 03-11)."
    - "D-27: `motion` is installed at the exact version 12.40.0 with no caret range, and the install was preceded by an owner-performed checkpoint carrying the SUS `too-new` verdict verbatim (R-V1)."
    - "D-09: Inter Variable woff2 bytes exist same-origin in the repo with a recorded byte size and SHA-256, and no Google Fonts import or any http(s) @import exists in any stylesheet."
  artifacts:
    - src/assets/inter-latin-variable.woff2
    - src/assets/README.md
    - tests/e2e/spike-export-font.spec.ts
  key_links:
    - "The OQ-1 spike verdict gates 03-11's font-embedding seam and therefore D-25 — a negative verdict escalates to the owner, it never downgrades to a weaker test."
    - "The tag is what makes `git diff acceptance-02-28..HEAD` a meaningful review range for 03-12, and what keeps 02-28's subject reachable."
    - "The vendored font bytes are read by 03-02's @font-face and by 03-11's inline export embedding; the recorded SHA-256 is the only thing pinning what ends up inside every exported PNG."
  backstops:
    - statement: "The vendored Inter woff2 is a latin subset in the 100-300KB range, so base64-inlining it into the JS bundle for 03-11's export path is a proportionate cost rather than a load regression."
      verification: backstop
---

<objective>
Do the three things that are irreversible, safety-critical, or blocking — and nothing else.
Preserve the `02-28` acceptance evidence with a git tag, answer the one blocking open question
(OQ-1) from a real browser, and take the phase's supply-chain surfaces through their owner gates.

Purpose: `fe5f946` loses its subject if it ever becomes unreachable (D-31). D-25's entire
mechanism rests on a browser behaviour nobody in this project has observed (OQ-1). And `motion` is
a runtime dependency entering a phase specced as tokens-only, flagged SUS, requiring a human
before the install (R-V1).

This plan deliberately stops short of authoring anything. `03-02` writes the design contract, the
motion mirror, and the icons — all of which are downstream of the D-01 commitment, whose gate is
`03-02` Task 1 because that is where the irreversible authoring actually happens.

Output: the annotated tag, a written OQ-1 verdict, the pinned `motion` dependency, and the
vendored Inter bytes with their hash recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/export.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-CONTEXT.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-UI-SPEC.md

Read narrowly, never whole (each is 50-140KB):
- `.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-RESEARCH.md` § Standard Stack,
  § Package Legitimacy Audit, § Pattern 1, § Pattern 2, § Environment Availability,
  § Open Questions (OQ-1, OQ-4)
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| git tag `acceptance-02-28` | git object | D-31 evidence preservation; annotated, not lightweight |
| `tests/e2e/spike-export-font.spec.ts` | throwaway test | OQ-1 verdict; re-run as 03-11's gate, then deleted or promoted |
| `src/assets/inter-latin-variable.woff2` | binary asset | the ONE font file; same-origin, byte-recorded |
| `src/assets/README.md` | doc | font provenance: source, subset, byte size, SHA-256, base64-inflated size, licence |
| `motion@12.40.0` in `package.json` | dependency | exact pin, no caret |

**Tag created, never a file edit:** `02-28-ACCEPTANCE-MATRIX.md` is NOT modified by this plan.
Its SHA binding is unchanged and the `02-28` owner gate stays OPEN.
</artifacts_this_phase_produces>

<source_coverage_audit>
## Multi-source coverage audit (phase-wide, recorded once here)

**Phase 3 is TWELVE plans**, `03-01`…`03-12`. `03-01` and `03-02` are the two halves of what the
ROADMAP § Phase 3 numbered list calls plan 1; every subsequent roadmap item shifts by one, so
roadmap item *n* is plan `03-{n+1}` for *n* ≥ 2. The ROADMAP § Phase 3 plan-file ledger carries the
authoritative mapping.

**GOAL** (ROADMAP § Phase 3 Goal) — replace slate/teal tokens + app-bar/right-inspector with a
super-clean minimal studio: full-bleed pannable canvas + one left tool HUD, Themely design system.
→ COVERED by 03-02 (tokens spec) · 03-03 (layout) · 03-04 (token values) · 03-05 (workspace)
· 03-06 (HUD shell) · 03-07 (tool migration) · 03-08 (map chrome) · 03-10 (CSS sweep).

**REQ** — ROADMAP declares **no** requirement IDs on Phase 3. Per the orchestrator's instruction
no REQ-IDs are invented; plans bind to D-NN decision ids and UI-SPEC assertion numbers instead.
The REQUIREMENTS.md items the UI-SPEC cites (NFR5/6/7/11, F4, F5) are *carried forward unchanged*
by this phase, not newly implemented: NFR11 keyboard/focus (03-06, 03-09), NFR5/6/7 responsive and
preference behaviour (03-09), F4 legend (03-07, 03-11), F5 export (03-11).

**RESEARCH** — every feature/constraint in `03-RESEARCH.md`:

| Item | Plan |
|---|---|
| Pattern 1 — Inter reaching the export clone | 03-01 (bytes, OQ-1) → 03-11 (seam) |
| Pattern 2 — a D-25 gate that can go RED | 03-11 (assertion 25) |
| Pattern 3 — icon rail + reserved-space flyout | 03-03, 03-06 |
| Pattern 4 — rehoming the `role="status"` live region | 03-07 (assertion 14) |
| Pattern 5 — motion tokens + lockstep | 03-02 (assertion 7) |
| Pattern 6 — vendored animated icons out of Tailwind | 03-02 (assertion 22) |
| Pattern 7 — `.dark` on the mount root | 03-04 (CSS), 03-06 (toggle), 03-05 (mount root) |
| Pattern 8 — `phase2CssContract.test.ts` → `uiContract.test.ts` | 03-03 (create), 03-04 (tokens), 03-10 (inventory) |
| Pattern 9(a) — one mountable editor, props boundary | 03-05 |
| Pattern 9(b) — one `localStorage` site | 03-05 (gate), 03-06/03-07 (new keys) |
| Pattern 9(c) — `/data/` config home + predicate exemption | 03-05 |
| Pattern 9(d) — namespaced tokens | 03-04 |
| Pattern 9(e) — no `documentElement.classList` | 03-05 |
| Current-State Inventory — retired tokens | 03-04 |
| `Controls.css` surface split | 03-10 |
| Snapshot catalog precise state | 03-07 (assertion 13) |
| Runtime State Inventory — new storage keys, stale `.artifacts/` | 03-06, 03-07, 03-11 |
| P-1 … P-9 pitfalls | P-1→03-09, P-2→03-04, P-3→03-04/03-11, P-4→03-11, P-5→03-03/03-06, P-6→03-01, P-7→03-06, P-8→03-11, P-9→03-01/03-12 |
| OQ-1 | 03-01 (spike) → 03-11 (gate) |
| OQ-2 | resolved by D-32 → 03-03 |
| OQ-3 | resolved → 03-05 (mount root) |
| OQ-4 | resolved → 03-01 (pin 12.40.0) |
| OQ-5 | 03-11 (collapse the duplicated per-line tables) |
| Security Domain — ASVS V5/V6/V12/V14 | 03-01 (supply chain), 03-07 (input), 03-11 (filename) |

**CONTEXT** — D-01…D-35 plus D-34a, every id assigned:

| D | Plan | D | Plan | D | Plan |
|---|---|---|---|---|---|
| D-01 | 03-02 | D-13 | 03-06 | D-25 | 03-11 |
| D-02 | 03-02 | D-14 | 03-07 | D-26 | 03-02 |
| D-03 | 03-02, 03-04 | D-15 | 03-07 | D-27 | 03-01 |
| D-04 | 03-04 | D-16 | 03-03, 03-06 | D-28 | 03-02 |
| D-05 | 03-04, 03-06, 03-08 | D-17 | 03-06 | D-29 | 03-06 |
| D-06 | 03-04 | D-18 | 03-06 | D-30 | 03-06 |
| D-07 | 03-04 | D-19 | 03-03 | D-31 | 03-01 |
| D-08 | 03-04, 03-09 | D-20 | 03-09 | D-32 | 03-03 |
| D-09 | 03-01, 03-02, 03-04 | D-21 | 03-08 | D-33 | 03-12 |
| D-10 | 03-04 | D-22 | 03-08 | D-34 | 03-11 |
| D-11 | 03-03, 03-05, 03-07 | D-23 | 03-08 | D-34a | 03-11 |
| D-12 | 03-06 | D-24 | 03-05 | D-35 | 03-09 |

**Exclusions (not gaps):** the four CONTEXT § Deferred Ideas (warm paper cast → Phase 4; scale bar
→ rejected; embedding in Themely → needs new authorization; the dark-toggle deferral, since
resolved by D-30 and now IN scope).

**Result: 0 unplanned items.** No source item is MISSING, so no phase split is proposed.
</source_coverage_audit>

<tasks>

<task type="tracer">
  <name>Task 1: Tag `fe5f946` before any Phase 3 commit lands (D-31)</name>
  <files>(no file changes — a git object only)</files>
  <read_first>
    - `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-28-ACCEPTANCE-MATRIX.md` — read the header/SHA-binding lines ONLY, to confirm which commit it binds. **Do not edit this file.**
    - `.planning/coding-rules/general.md` § Git safety, § Immutable Safety Constraints (constraint 8)
    - `03-CONTEXT.md` D-31
  </read_first>
  <action>
Create an **annotated** git tag named `acceptance-02-28` on commit
`fe5f946060707c48c3d9591d368b5f3f8f90dd4d`, per D-31. This is the first Phase 3 action and must
land before any other Phase 3 commit.

Run `git cat-file -t fe5f946060707c48c3d9591d368b5f3f8f90dd4d` first and confirm it prints
`commit`. If it does not, STOP and report — the acceptance matrix has lost its subject and that
is an owner escalation, not something to work around.

Create the tag with `git tag -a acceptance-02-28 fe5f946060707c48c3d9591d368b5f3f8f90dd4d -m
"..."`. The message states, in plain words: this is the pre-Phase-3 build that
`02-28-ACCEPTANCE-MATRIX.md` describes; the matrix is performed against this commit and never
against a restyled HEAD; the `02-28` gate is OPEN and this tag does not close it.

**Do not modify `02-28-ACCEPTANCE-MATRIX.md`.** Do not fill, annotate, or pre-approve any cell.
Do not push the tag (this repo has no remote workflow in scope). Do not create a lightweight tag
— an annotated tag carries the message that explains what it protects.

This task is the phase tracer for evidence preservation: it is the thinnest action that touches
the whole chain (Phase 2 evidence → git → Phase 3 execution) and it must be provably done before
anything else moves. It is also what makes `git diff acceptance-02-28..HEAD` — the review range
`03-12` depends on — well defined.
  </action>
  <verify>
    <automated>git tag --list | grep -qx 'acceptance-02-28' && test "$(git rev-list -n 1 acceptance-02-28)" = "fe5f946060707c48c3d9591d368b5f3f8f90dd4d" && test "$(git cat-file -t acceptance-02-28)" = "tag" && echo TAG_OK</automated>
    <automated>git diff --quiet HEAD -- .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-28-ACCEPTANCE-MATRIX.md && echo MATRIX_UNTOUCHED</automated>
  </verify>
  <done>
    An annotated tag `acceptance-02-28` points at `fe5f946060707c48c3d9591d368b5f3f8f90dd4d`;
    `git cat-file -t` reports `tag` (annotated), not `commit` (lightweight);
    `02-28-ACCEPTANCE-MATRIX.md` is byte-unchanged and the gate is still recorded as OPEN.
  </done>
  <acceptance_criteria>
    - `git tag --list` contains exactly the line `acceptance-02-28`.
    - `git rev-list -n 1 acceptance-02-28` prints `fe5f946060707c48c3d9591d368b5f3f8f90dd4d`.
    - `git cat-file -t acceptance-02-28` prints `tag`.
    - `git status --porcelain` shows no modification to any file under
      `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/`.
    - The SUMMARY records that `02-28` remains OPEN and that this tag is a safeguard on evidence,
      not a resolution of the gate.
  </acceptance_criteria>
  <reversibility rating="reversible">A tag can be deleted and recreated; nothing downstream depends on its name beyond documentation and the 03-12 review range.</reversibility>
</task>

<task type="tracer">
  <name>Task 2: OQ-1 spike — does an inline base64 @font-face render inside SVG-as-image in installed Chrome?</name>
  <files>tests/e2e/spike-export-font.spec.ts</files>
  <read_first>
    - `03-RESEARCH.md` § Pattern 1 (lines ~531-655) — the exact html2canvas serialisation shape being reproduced
    - `03-RESEARCH.md` § Open Questions → OQ-1 (lines ~1655-1673)
    - `03-UI-SPEC.md` § Assertion 25 (lines ~1107-1145)
    - `src/utils/export.ts` — read `createExportFrame` and `sanitizeExportClone` only; do NOT edit this file in this plan
    - `tests/e2e/support/appHarness.ts` — import shared fixtures; never re-declare helpers
    - `playwright.config.ts` — confirm the `chrome` project name
  </read_first>
  <action>
Write a **throwaway** Playwright spec that answers OQ-1 from installed Chrome. This is the phase
tracer for the export path: the thinnest end-to-end proof that the mechanism `03-11` depends on
actually works in the one browser this project certifies.

It runs **before** the D-01 commitment gate in `03-02` deliberately — it gathers information and
commits to nothing, so blocking it behind an owner response would only delay the answer that
shapes the decision.

Reproduce the html2canvas serialisation shape exactly, because the shape is the question. Inside
the page, build a minimal SVG string containing a `<style>` element whose text is a `@font-face`
rule whose `src` is a `url(data:font/woff2;base64,...)` value, plus a `<text>` element that names
that font family at a large size. Serialise it with `XMLSerializer`, wrap it as
`"data:image/svg+xml," + encodeURIComponent(serialised)` — `encodeURIComponent`, **not** base64,
because that is what html2canvas does at `html2canvas.js:4562` and the escaping is part of what
might break. Load it into an `Image`, `drawImage` it onto a canvas, and read the pixels.

Run the same build twice in the same browser context: once with the `@font-face` rule present,
once with it removed. Compare the rasterised text region. Assert the two differ beyond a noise
threshold. Add a **discrimination control** in the same spec: a third render of an empty region,
asserted to be blank, and both text renders asserted non-blank — so two empty rasters cannot
satisfy the inequality. This is the same defect shape as the pixel probe this repo already
shipped, which only asserted cross-context equality and was satisfied by three blank canvases.

Use any woff2 with visibly distinctive advance widths and record which file was used; if Task 4
has not run yet, a system-available woff2 is acceptable for the spike. Use a label string whose
advance widths differ sharply between the embedded face and the system fallback.

Run it: `npx playwright test tests/e2e/spike-export-font.spec.ts --project=chrome`.

**Record the verdict in writing in the plan SUMMARY**, under a section headed exactly
`OQ-1 verdict`, whose **first line states POSITIVE or NEGATIVE and nothing else**. Downstream
tooling in `03-11` reads that line. Follow it with the Chrome version, the raw pixel-difference
numbers, and the date.

**Gate — stop and decide.** If the spike comes back NEGATIVE (the two renders do not differ, i.e.
Chrome ignored the data-URI font), do NOT proceed to weaken the test, do NOT invent a markup-level
substitute, and do NOT continue into legend typography. Halt and escalate to the owner with the
two recorded fallbacks from `03-RESEARCH.md` § Alternatives Considered: (a) convert legend labels
to `<path>` outlines at export time, or (b) descope D-25 to chrome-only Inter. Per
`03-UI-SPEC.md` § Open Items item 1, that descope is an **owner decision, not an executor's**.

Edge is not installed on this machine (D-33). Do not add an `msedge` run and do not report an
Edge result. Safari is the documented exception for this technique and is out of certification
scope — record that fact, do not solve it, and never describe the technique as cross-browser.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/spike-export-font.spec.ts --project=chrome --reporter=line</automated>
  </verify>
  <done>
    The spec runs green in Chrome and its assertions are: (1) font-present and font-absent text
    rasters DIFFER; (2) both text rasters are NON-BLANK; (3) a deliberately blank control raster
    IS blank and differs from both. The SUMMARY carries an `OQ-1 verdict` section whose first line
    is POSITIVE or NEGATIVE, followed by the numbers, the Chrome version, and the date. If the
    verdict is negative the plan is HALTED and escalated instead.
  </done>
  <acceptance_criteria>
    - `tests/e2e/spike-export-font.spec.ts` exists and passes under `--project=chrome`.
    - The spec contains all three assertions (difference, non-blankness of both text renders,
      blank control) — a spec asserting only difference is REJECTED.
    - The spec serialises via `encodeURIComponent` of `XMLSerializer` output, not base64.
    - `grep -c 'msedge' tests/e2e/spike-export-font.spec.ts` returns 0.
    - The SUMMARY has a section headed exactly `OQ-1 verdict` whose **first line** is the single
      word POSITIVE or NEGATIVE.
    - The word "cross-browser" does not describe this technique anywhere in the SUMMARY.
  </acceptance_criteria>
  <precondition>Google Chrome is installed and launchable by Playwright's `chrome` channel; `npx playwright test --project=chrome --list` succeeds.</precondition>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: R-V1 — owner gate before the `motion` install (SUS `too-new`)</name>
  <what-built>
Nothing yet. This checkpoint sits **before** the install command, not after it, per
`03-UI-SPEC.md` § Two planner requirements (R-V1).

The verdict and its reasoning, carried verbatim from the UI-SPEC:

> `motion` is flagged **SUS** with the single reason `too-new`. The signal derives from the most
> recent publish date, not the package's age: 17.5M weekly downloads, source repo
> `github.com/motiondivision/motion`, `postinstall: null`. The strongest legitimacy signal
> available is that it is the sibling repo's own declared dependency and the vendored icon files
> import from it directly. **Do not take `motion@latest` — `13.0.0` shipped 2026-08-05** and its
> React peer range was not checked. Pin exact **`12.40.0`**, never a caret range, so a fresh
> `npm install` cannot silently pull `13.x`.

Two facts the owner should have in hand:
- `motion@12` declares `"react": "^18.0.0 || ^19.0.0"`. This repo is on `react 18.3.1`. Verified.
- `03-RESEARCH.md` § Standard Stack recommends `12.43.0`; the **approved `03-UI-SPEC.md`
  supersedes it with `12.40.0`**, byte-matching the repo the icons are vendored from, which
  removes research assumption A4 entirely. `12.40.0` is the version to install.
  </what-built>
  <how-to-verify>
1. Open `https://www.npmjs.com/package/motion` in a browser. Confirm the package page, the weekly
   download count, and that the linked repository is `github.com/motiondivision/motion`.
2. Confirm version `12.40.0` exists in the version list and note its publish date.
3. Run `npm view motion@12.40.0 scripts.postinstall` and confirm it prints nothing / `undefined`.
4. Run `npm view motion@12.40.0 peerDependencies` and confirm React 18 is in range.
5. Confirm the sibling repo's declaration first-hand:
   `grep '"motion"' /Users/matul/claudeprojects/themely/package.json` and
   `node -p "require('/Users/matul/claudeprojects/themely/node_modules/motion/package.json').version"`.
6. Decide: approve the exact-pinned `12.40.0` install, or refuse it.

**This is also the natural moment to halt the phase if D-01 is in any doubt.** `motion` enters the
build because the Themely design system was adopted (D-27/D-28, Roadmap Amendment 3). The **D-01
one-way commitment gate itself is `03-02` Task 1**, placed there because that is where the
irreversible authoring happens — the token namespace, the type scale, the icon recipe, the motion
contract. Installing a pinned package and vendoring font bytes are `costly`, not one-way: both undo
with `npm uninstall` and a file deletion. So refusing here costs nothing and stops everything.

**An automated result may not be substituted for this checkpoint.** If the approval on file is a
blanket, in-advance, sight-unseen authorization, that authorizes *proceeding* — it is **not** a
content review and it is **not** hash-bound. Record in the SUMMARY which one is actually held
(Immutable Safety Constraint 8).
  </how-to-verify>
  <resume-signal>Type `approved: motion@12.40.0 exact` to authorize the install, or describe the refusal. A generic "approved" is not sufficient — the message must name the exact version.</resume-signal>
  <acceptance_criteria>
    - The plan does not proceed to Task 4 until the owner has responded.
    - The SUMMARY records: the owner's verbatim response, the date, and an explicit statement of
      whether the authorization held is a content review or a blanket proceed-authorization.
    - `workflow.auto_advance` does not apply — this checkpoint is never auto-approvable.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Install `motion@12.40.0` exact and vendor the Inter Variable woff2 (D-27, D-09)</name>
  <files>package.json, package-lock.json, src/assets/inter-latin-variable.woff2, src/assets/README.md</files>
  <read_first>
    - `package.json` — the current dependency block and the exact-pin style already used (`html2canvas` is `"1.4.1"`, unprefixed; match that style)
    - `03-RESEARCH.md` § Standard Stack and § Package Legitimacy Audit
    - `03-UI-SPEC.md` § Vendoring Safety (the table rows for `motion` and `@fontsource-variable/inter`)
    - `public/data/world-manifest.json` and the `data:world:check` script in `package.json` — the existing pattern for a bundled, hash-recorded, same-origin asset
  </read_first>
  <action>
**A. Install `motion` at exactly `12.40.0`.** Run `npm install --save-exact motion@12.40.0`.
Confirm `package.json` records `"motion": "12.40.0"` with **no** caret, tilde, or range —
matching the existing `html2canvas` pin style. Do NOT take `motion@latest`.

**B. Vendor the Inter Variable woff2 by hand into `src/assets/inter-latin-variable.woff2`.**
Do **not** install `@fontsource-variable/inter` — `03-UI-SPEC.md` § Vendoring Safety records this
as *not taken*, and the reason is load-bearing: these bytes end up inside every exported PNG
(`03-11`), so they must be reviewable and pinnable, and the repo already has a first-class pattern
for a bundled, hash-verified, same-origin asset.

Obtain a **latin-subset** Inter Variable woff2 covering weights 100-900. Record where the bytes
came from. If no source is reachable offline, STOP and report rather than substituting a
different typeface — Inter is named by D-09, by `LegendOverlay.tsx:167`, and by Themely's
`Design.md`.

**C. Record the bytes.** Write `src/assets/README.md` carrying, for the font file: source and
version, the subset taken, the exact byte size from `wc -c`, the SHA-256 from
`shasum -a 256`, and the SIL Open Font Licence attribution. This is the same discipline
`world-manifest.json` applies to the world asset; the reason is identical and stated in
`03-RESEARCH.md` § Security Domain (ASVS V6).

**D. Measure before `03-11` commits to inlining.** Record the base64-inflated size
(`≈ byte_size × 4 / 3`) in the same README. Research assumption A3 estimated 100-300KB raw; if
the real file is materially larger, say so plainly in the SUMMARY so `03-11`'s `?inline` decision
is made on a measured number rather than an estimate.

**E. Forbidden, and gated in `03-04`.** Do not add a Google Fonts `@import`, any
`@import url(http`, or any other runtime third-party request. Themely's own `globals.css:1` uses a
Google Fonts import; C-1 forbids it here. The font must be same-origin bundled bytes.

**F. Do not author the `@font-face` rule here.** `03-02` writes the design contract and `03-04`
lands the stylesheet declaration. This task delivers reviewable, hash-recorded bytes and nothing
else.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const v=p.dependencies.motion;if(v!=='12.40.0'){console.error('BAD motion pin:',v);process.exit(1)};console.log('PIN_OK',v)"</automated>
    <automated>test -f src/assets/inter-latin-variable.woff2 && node -e "const s=require('fs').statSync('src/assets/inter-latin-variable.woff2');if(s.size<20000){console.error('font too small, likely a placeholder:',s.size);process.exit(1)};console.log('FONT_BYTES',s.size)"</automated>
    <automated>grep -rEl "@import[[:space:]]+url\(['\"]?https?:" src/ index.html || echo NO_REMOTE_IMPORT</automated>
    <automated>grep -q "$(shasum -a 256 src/assets/inter-latin-variable.woff2 | cut -d' ' -f1)" src/assets/README.md && echo HASH_RECORDED</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    `motion` is a dependency at exactly `12.40.0` with no range prefix; a latin-subset Inter
    Variable woff2 exists at `src/assets/inter-latin-variable.woff2`; `src/assets/README.md`
    records its source, subset, byte size, SHA-256, base64-inflated size, and licence; no
    stylesheet or `index.html` carries an http(s) `@import`; `npm run lint && npm test && npm run
    build` is clean.
  </done>
  <acceptance_criteria>
    - `node -p "require('./package.json').dependencies.motion"` prints exactly `12.40.0`.
    - `grep -c '"motion": "\^' package.json` returns 0.
    - `@fontsource-variable/inter` does NOT appear in `package.json`.
    - The SHA-256 printed by `shasum -a 256 src/assets/inter-latin-variable.woff2` appears
      verbatim in `src/assets/README.md`.
    - Searching `src/` and `index.html` for an `@import` of an http or https URL returns no file.
    - The SUMMARY records the measured raw and base64 sizes as numbers.
  </acceptance_criteria>
  <reversibility rating="costly">D-27 adds a runtime dependency to a phase specced as tokens-only; removing `motion` later means re-authoring every animated surface in CSS, because D-28's vendored icons ARE `motion` components.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → `node_modules` | A newly added runtime dependency (`motion`) crosses into the build |
| vendored font bytes → every exported PNG | Font bytes become part of creator output via `03-11`'s inline embedding |
| the wider web → the running app | Currently ZERO runtime third-party requests exist; this phase must not open one |
| Phase 2 evidence → Phase 3 execution | `fe5f946` must stay reachable and unmodified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `npm install motion` | high | mitigate | SUS `too-new` verdict; exact pin `12.40.0` with no caret (Task 4 verify gate); `postinstall: null` verified; blocking non-auto-approvable owner checkpoint before the install (Task 3, R-V1) |
| T-03-03 | Information disclosure | a Google Fonts / CDN `@import` smuggled in with the design system (P-6) | high | mitigate | Hand-vendored same-origin woff2; `@fontsource-variable/inter` deliberately not taken; grep gate for `@import url(http` here, promoted to a standing contract assertion in `03-04` |
| T-03-04 | Denial of service (self-inflicted) | base64 font bytes inflating the JS bundle | low | mitigate | Latin subset only; raw and base64-inflated sizes measured and recorded in `src/assets/README.md` before `03-11` commits to `?inline` |
| T-03-05 | Repudiation | claiming a browser result that was never run | high | mitigate | Edge is not installed (D-33); the OQ-1 spike is Chrome-only and says so; acceptance criteria forbid an `msedge` reference in the spike and forbid describing the technique as cross-browser |
| T-03-06 | Tampering | Phase 2 acceptance evidence losing its subject | high | mitigate | D-31 annotated tag on `fe5f946` created first, verified to be an annotated (not lightweight) tag pointing at the exact SHA; `02-28-ACCEPTANCE-MATRIX.md` verified byte-unchanged |
| T-03-SC | Tampering | npm installs generally | high | mitigate | Package Legitimacy Audit present in `03-RESEARCH.md`; no `[SLOP]` packages; both `[SUS]` packages gated — `motion` by Task 4's blocking human checkpoint, `@fontsource-variable/inter` avoided entirely |
</threat_model>

<verification>
```bash
npm run lint
npm test
npm run build
npx playwright test tests/e2e/spike-export-font.spec.ts --project=chrome
git tag --list
```

Then confirm by hand:
- `git rev-list -n 1 acceptance-02-28` = `fe5f946060707c48c3d9591d368b5f3f8f90dd4d`
- `git status --porcelain` shows nothing under
  `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/`
- The SUMMARY carries: the owner's R-V1 response verbatim, and the `OQ-1 verdict` section whose
  first line is POSITIVE or NEGATIVE.

**Edge is not run and must not be reported.** Chrome only, per D-33.
</verification>

<success_criteria>
- [ ] `git tag --list` shows an annotated `acceptance-02-28` on `fe5f946060707c48c3d9591d368b5f3f8f90dd4d`, and `02-28-ACCEPTANCE-MATRIX.md` is byte-unchanged with its gate still OPEN
- [ ] The `OQ-1 verdict` section exists with POSITIVE or NEGATIVE on its first line, plus measured numbers and browser version; a NEGATIVE verdict halted the plan and escalated to the owner rather than weakening any assertion
- [ ] `motion` is pinned at exactly `12.40.0` and the owner's R-V1 response is recorded verbatim, with an explicit statement of whether it is a content review or a blanket proceed-authorization
- [ ] Inter Variable woff2 is vendored same-origin with source, subset, byte size, SHA-256, base64-inflated size, and licence recorded; no http(s) `@import` exists anywhere
- [ ] `npm run lint && npm test && npm run build` clean
</success_criteria>

<output>
Create `.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-01-SUMMARY.md` when done.

It must carry, as named sections: `R-V1 owner response (verbatim)`,
`OQ-1 verdict` (**first line = POSITIVE or NEGATIVE, nothing else**), and
`Font bytes (raw / base64 / SHA-256)`.

**Do not run `state.advance-plan`, `state.update-progress`, or `roadmap.update-plan-progress`.**
Those gsd-sdk verbs have already corrupted this repo's tracking files. Edit `.planning/STATE.md`
and `.planning/ROADMAP.md` by hand.
</output>
