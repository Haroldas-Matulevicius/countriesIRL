---
phase: 03-clean-ui-overhaul-1-1-5-weeks
plan: 07
type: execute
wave: 7
depends_on:
  - 03-06
files_modified:
  - src/components/editor/PeriodHud.tsx
  - src/components/CompositionBar.tsx
  - src/components/SaveLoad.tsx
  - src/components/SaveLoad.test.tsx
  - src/components/LegendEditor.tsx
  - src/components/LegendEditor.test.tsx
  - src/components/LegendDisclosure.tsx
  - src/components/CountryList.tsx
  - src/components/LocateCountry.tsx
  - src/components/editor/ToolPanel.tsx
  - src/App.tsx
  - src/App.test.tsx
  - src/styles/uiContract.test.ts
  - tests/e2e/history.spec.ts
  - tests/e2e/persistence.spec.ts
autonomous: true
requirements:
  - D-11
  - D-14
  - D-15
  - A-13
  - A-14
  - A-15
  - A-23
  - OPEN-ITEM-3
  - OPEN-ITEM-4

estimate:
  tokens: 195000
  raw_tokens: 98000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "D-14 / assertion 13: the period surface renders the options returned by resolvePeriodOptions over the approved manifest — currently exactly one, Modern — current borders — and NEVER renders SNAPSHOT_CATALOG directly, so none of the four deferred historical labels appears within that surface."
    - "D-14: with one resolved option the surface is a visibly inert read-only pill, not a disabled select; no dropdown affordance, no chevron, no coming-soon copy, no count of hidden periods. The interactive select path stays reachable in code for the day a second approved entry arrives."
    - "D-15 / assertion 14: the rehomed live region keeps COMPOSITION_PERIOD_STATUS_ID byte-identical, keeps role=status aria-live=polite, and the period control's aria-describedby still resolves to an element that EXISTS."
    - "D-11 completes: CompositionBar is gone and every responsibility it held has an owner — identity in the HUD header (03-06), the period surface and live region here."
    - "Assertion 23: the ToastRegion allowlist and its positive-test count are UNCHANGED by this phase — if the allowlist grew, a message was introduced without a test."
    - "Assertion 15 completes: exactly one Reset View, exactly one Reset All Colors, and exactly one filled primary action exist in the composed DOM across all four panels."
    - "The 02-22 action-order semantics are explicitly preserved or explicitly superseded IN WRITING, and the nested-confirmation contract survives the dialog's retirement verbatim: sibling not descendant, its own tabIndex -1, Escape dismisses the innermost, focus returns from an effect keyed by a stable row key."
    - "OPEN ITEM 4 is decided and recorded: the approved-id filter is adopted on the saved-map short-label RESOLVER (presentation), and the storage validator at storage.ts:61-63 / :483-484 is NOT changed — a behaviour change there is out of a chrome phase's scope."
    - "Immutable Safety Constraint 3 holds: a BLOCKED packet is never counted as a delivered snapshot, deferred never reads as done, and nothing here makes a deferred snapshot nameable or reachable."
  artifacts:
    - src/components/editor/PeriodHud.tsx
  key_links:
    - "COMPOSITION_PERIOD_STATUS_ID is both a live-region id AND an aria-describedby target — merging it into ToastRegion or the selection region orphans the description, which no visual check catches."
    - "The period surface reads useSnapshotCatalog's resolved options; reading SNAPSHOT_CATALOG directly would make four deferred snapshots nameable in the UI."
    - "SaveLoad's modal machinery retires WITH the dialog, but the nested-confirmation contract was never about the modal and must survive it verbatim."
  backstops:
    - statement: "The saved-maps list and the legend entry list each render the compact empty-state card with their existing heading and body copy at zero records; the entry list is absent, not a zero-row table."
      verification: backstop
    - statement: "Zero legends is not a missing legend — a composition with no legend entries must still export a white square rather than refusing."
      verification: backstop
---

<objective>
Finish the migration: the legend editor, the country list, the saved-maps surface, and the period
control all leave the retired containers. `CompositionBar` dissolves and its `role="status"` live
region gets a defined new home. Then land the four assertions that make the move provable.

Purpose: this is the plan where a dissolving container can silently take an accessibility
guarantee with it. The period status region is not only a live region — it is the period control's
`aria-describedby` target, and dropping it degrades the control's accessible description with no
visual signal at all. Two of this repo's known defects (a deleted inspector landmark, a legend
overflow clipping the PNG) came from exactly this kind of move.

Output: `PeriodHud`, the migrated panels, a retired `CompositionBar`, and assertions 13, 14, 15,
and 23.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/data.md
@.planning/coding-rules/storage.md
@Design.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-UI-SPEC.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-06-SUMMARY.md

Read narrowly:
- `03-RESEARCH.md` § Pattern 4 (rehoming the live region), § Snapshot catalog — the precise state
  (lines ~1247-1281), § P-3
- `03-CONTEXT.md` D-14, D-15
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| `src/components/editor/PeriodHud.tsx` | component | the `.period-hud` surface: inert period pill + the rehomed `role="status"` region; `data-editor-only="true"` |
| **retired** `src/components/CompositionBar.tsx` | removal | dissolves per D-11; both responsibilities rehomed first |
| `getPeriodShortLabel` approved-id filter | behaviour | resolves through the ids the approved manifest yields, returning `null` otherwise (OPEN ITEM 4 decision) |
| assertions 13, 14, 15 (completion), 23 | test code | in `uiContract.test.ts`, `App.test.tsx`, and the e2e specs |
| new copy strings | strings | `Close Countries`, `Close Legend`, `Close Saved Maps` (the `Close <Tool>` set begun in 03-06) — none reaches `onStatusMessage` |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: The period surface and the rehomed live region, end to end (D-14, D-15, assertions 13 + 14)</name>
  <files>src/components/editor/PeriodHud.tsx, src/components/CompositionBar.tsx, src/App.tsx, src/App.test.tsx, tests/e2e/history.spec.ts</files>
  <read_first>
    - `src/components/CompositionBar.tsx` (full) — `COMPOSITION_PERIOD_STATUS_ID` at `:8`, the `aria-describedby` wiring at `:59`, and the live region at `:82-88`
    - `src/hooks/useSnapshotCatalog.ts` lines 1-80 — `resolvePeriodOptions` and the Modern-only fallback comment at `:46-49`
    - `src/constants/snapshots.ts` lines 1-25 — the FIVE-entry `SNAPSHOT_CATALOG` label registry
    - `src/utils/periods.ts` — `PERIOD_COPY`, including `periodLabel` and `worldLoading`
    - `src/App.tsx` lines 1040-1063 — the existing selection live region and the `.selection-live-region` precedent
    - `src/App.test.tsx` lines 265-290 — the exact live-region markup assertion idiom (`:274-276`)
    - `03-UI-SPEC.md` § Component Contracts 4 in full
    - `.planning/coding-rules/data.md` — the approval chain
  </read_first>
  <action>
One path, end to end: approved manifest → `useSnapshotCatalog` → resolved options → the inert
pill → its `aria-describedby` → the live region that still exists and still announces.

**A. Create `src/components/editor/PeriodHud.tsx`** as the `.period-hud` surface **in the canvas
region, not in the rail** — the region is an `aria-describedby` target as well as a live region,
and it must sit next to the control it describes. Carry `data-editor-only="true"`.

**B. What it renders — the exact contract.** It renders the options returned by
`useSnapshotCatalog` (i.e. `resolvePeriodOptions(...)` over the approved manifest at
`public/data/snapshots/index.json`), which currently resolve to **exactly one entry:
`Modern — current borders`**.

**It must NOT render `SNAPSHOT_CATALOG` directly.** That constant
(`src/constants/snapshots.ts:10-19`) is a **five-entry label registry**, not an approval list.
Rendering it would make four deferred snapshots nameable in the UI — a direct violation of
Immutable Safety Constraint 3 (*a BLOCKED packet is never counted as a delivered snapshot*) and
Live Invariant 6.

With one resolved option the surface is a **visibly inert read-only pill**, not a disabled
`<select>`: the label `Map period` at `--text-eyebrow` uppercase in `--themely-slate-blue` above
the single resolved label at `--text-body-sm` in `--themely-midnight-ink`, on a
`--themely-porcelain` pill with a `--hairline` border and `--radius-pill`. **No dropdown
affordance, no chevron, no "coming soon", no count of hidden periods.** Onboarding and status copy
never advertise a deferred feature.

**The surface is presentation only.** Catalog semantics, approval semantics, and
`resolvePeriodOptions` are untouched. If a second approved entry ever reaches the manifest the
surface returns to an interactive `<select>` **with no copy change** — that path must remain
reachable in code, **not deleted**.

**C. The live region keeps its identity (D-15).** `COMPOSITION_PERIOD_STATUS_ID` stays
**byte-identical** (`'composition-bar-period-status'` — do not rename it to match the new
component; the id is referenced by an assertion and by `aria-describedby`). The element keeps
`role="status" aria-live="polite"`, and the period control keeps `aria-describedby` pointing at it.

Do **not** merge it into `ToastRegion` — that is an allowlist for *transient* messages, and
routing a *persistent description* through it would either force every period string into
`APPROVED_STATIC_MESSAGES` or degrade it to a severity fallback. Do **not** merge it into
`.selection-live-region` — one region with two writers interleaves and truncates announcements.

**D. Retire `CompositionBar.tsx`** only after both of its responsibilities have owners: identity
went to the HUD header in 03-06, the period surface and live region land here. Delete the file;
do not leave it rendered-but-hidden.

**E. Assertion 14 — two halves, and the second one is the load-bearing half.** Mirror
`App.test.tsx:274-276` to assert the **exact live-region markup string**, AND assert that the id
in `aria-describedby` **resolves to an element that exists**. The second assertion is the one that
goes RED if the region is dropped during the rail migration; the first alone would pass against a
region that nothing points at.

*RED probe (mandatory task step):* delete the region, run `npx vitest run src/App.test.tsx`,
OBSERVE RED on the resolve assertion, capture output, restore by copying from the scratchpad.
**Never `git checkout --` a file with uncommitted work.**

**F. Assertion 13 — scoped to `.period-hud`.** Assert the period surface renders exactly the
manifest-derived options AND that none of the four historical labels appears **within that
surface**. Scope it to `.period-hud`: `SNAPSHOT_CATALOG` legitimately holds five labels elsewhere
in the module graph, so an unscoped assertion would be asserting something false.

*RED probe (mandatory task step):* render `SNAPSHOT_CATALOG` directly in the period surface,
OBSERVE RED, capture, restore by copying back.

**G. Do not promote geometry and do not widen reachability.** No file under `public/data/` is
touched. `SNAPSHOT_CATALOG` keeps its five entries with unchanged ids and labels. The approved
manifest keeps exactly one entry. Nothing in this task may make a deferred snapshot nameable or
reachable, and "deferred" may never read as "done".
  </action>
  <verify>
    <automated>npx vitest run src/App.test.tsx</automated>
    <automated>npx vitest run src/styles/uiContract.test.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/editor/PeriodHud.tsx','utf8');if(/SNAPSHOT_CATALOG/.test(s)){console.error('PeriodHud reads SNAPSHOT_CATALOG directly');process.exit(1)};if(!s.includes('composition-bar-period-status')&&!/COMPOSITION_PERIOD_STATUS_ID/.test(s)){console.error('live region id not carried into PeriodHud');process.exit(1)};console.log('PERIOD_HUD_OK')"</automated>
    <automated>test ! -f src/components/CompositionBar.tsx && echo COMPOSITION_BAR_RETIRED</automated>
    <automated>git diff --quiet HEAD -- public/data/ && echo PUBLIC_DATA_UNTOUCHED</automated>
    <automated>npx playwright test tests/e2e/history.spec.ts --project=chrome --reporter=line</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    `.period-hud` renders one inert `Map period` pill reading `Modern — current borders`, sourced
    from the resolved manifest options and never from the constant; the live region kept its
    byte-identical id, role, and politeness and the control's `aria-describedby` still resolves;
    `CompositionBar.tsx` is deleted; `public/data/` is untouched.
  </done>
  <acceptance_criteria>
    - `PeriodHud.tsx` contains no reference to `SNAPSHOT_CATALOG`.
    - `COMPOSITION_PERIOD_STATUS_ID` is byte-identical to its Phase 2 value.
    - Assertion 14 asserts BOTH the exact markup string AND that `aria-describedby` resolves.
    - Assertion 13 is scoped to `.period-hud` and none of the four historical labels appears
      within it.
    - The surface renders as a pill with no `<select>`, no chevron, and no deferred-feature copy;
      the interactive `<select>` path still exists in the source.
    - `src/components/CompositionBar.tsx` no longer exists and nothing references it.
    - `git diff HEAD -- public/data/` is empty and `SNAPSHOT_CATALOG` still has five unchanged
      entries.
    - The SUMMARY carries two captured RED outputs (region deleted, constant rendered).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Saved Maps — the dialog dissolves into the panel, the confirmation contract survives (OPEN ITEM 3)</name>
  <files>src/components/SaveLoad.tsx, src/components/SaveLoad.test.tsx, tests/e2e/persistence.spec.ts</files>
  <read_first>
    - `src/components/SaveLoad.tsx` (full — the modal machinery, the nested confirmations, `getPeriodShortLabel` at `:132-137`, and the comment at `:127-131`)
    - `src/components/SaveLoad.test.tsx` (full)
    - `03-UI-SPEC.md` § Component Contracts 8 in full, including the Correction and the Recommendation
    - `.planning/coding-rules/storage.md` — the confirmation semantics
    - `tests/e2e/persistence.spec.ts` and `tests/e2e/support/appHarness.ts`
    - the `02-22` plan or summary under `.planning/phases/02-*/` — the action-order semantics referenced below
  </read_first>
  <action>
This is the structural cost the UI-SPEC flagged: D-19 names saved-map rows as *panel* content, so
`SaveLoad`'s modal dialog dissolves into the `saved` tool panel, and it lands on
nested-confirmation and focus-trap code.

**A. The modal machinery retires WITH the dialog.** `role="dialog"`, `aria-modal`, the overlay, and
the imperative `inert` all go.

**B. What survives, verbatim — it was never about the modal.** Carry the nested-confirmation
contract across unchanged:
- a confirmation renders as a **sibling** of the surface it interrupts, not a descendant;
- it carries its own **`tabIndex={-1}`** — as a sibling it has no focusable ancestor, so a
  mouse-down on its body text drops focus to `document.body` and every other guarantee voids at
  once;
- `Escape` dismisses the **innermost** open confirmation, branching over every open layer in
  order — a handler that knows about one of two sibling confirmations discards the other's prompt;
- focus returns to the control that opened it, **from an effect**, keyed by a **stable row key**
  (index keys break as soon as a row is deleted).

**C. `02-22` action-order semantics: preserve or supersede, IN WRITING.** The roadmap's `03-07`
gate already requires this and it must be **recorded, not silent**. Read the `02-22` artifact,
state in the SUMMARY which semantics are at stake, and state explicitly for each: preserved, or
superseded and why. A silent change here is the failure mode the gate exists to catch.

**D. Row anatomy** per `03-UI-SPEC.md` § 8: Porcelain card at `--radius-card` with `--hairline`,
`px 16 / py 12`, hover `--themely-powder` **instant**; a 32×32 chip stepping **up** to Platinum
with a `map` glyph in Slate Blue; name at `--text-body-sm` one line with ellipsis; meta at
`--text-caption` in `--themely-ghost-gray` with `tabular-nums`, consuming the `SavedMapSummary`
projection.

`Load This Map` is a ghost button; its accessible name stays `Load This Map: <name>`.
`Delete Saved Map` is **destructive**: `--themely-red` text on transparent, hover red at 10%;
accessible name stays `Delete Saved Map: <name>`; **never one-shot** — the row's actions swap in
place to `Delete Map: <name>` (filled red, on-accent text) + `Keep Map: <name>` (ghost) plus the
prompt at `--text-caption`. `Escape` cancels the confirmation only, and focus returns to that
row's `Delete Saved Map`.

The save form's `Save Map` is the `saved` panel's **one** Apple Blue element. Empty state uses the
compact recipe with **copy unchanged** (`No saved maps yet` + `SAVED_EMPTY_BODY`).

**E. OPEN ITEM 4 — DECIDED. Adopt the approved-id filter on the resolver; do NOT touch the storage
validator.**

The fact, verified in the UI-SPEC: `storage.ts:61-63` builds `SNAPSHOT_IDS` from **all five**
catalog entries and the record validator at `:483-484` admits any id in that set, so a hand-crafted
record carrying `"snapshotId": "1914"` validates; `getPeriodShortLabel` (`SaveLoad.tsx:132-137`)
then resolves it to `1914`. This is **pre-existing Phase 2 behaviour, not a Phase 3 regression**,
and reaching it requires hand-editing browser storage — a weak, local exposure, not a path by
which a deferred snapshot becomes reachable.

**The decision, and its reasoning, to be recorded in the SUMMARY and in
`.planning/coding-rules/data.md`:** add the approved-id filter to `getPeriodShortLabel` so it
resolves through the ids the **approved manifest** actually yields (the same source
`resolvePeriodOptions` uses), returning `null` otherwise and rendering no period label. This is a
**presentation-layer** change of a few lines, squarely inside a chrome phase's scope, and it makes
the original claim *true* rather than merely narrower.

**Do NOT change `storage.ts:61-63` or the validator at `:483-484`.** A behaviour change to the
storage validator is out of a chrome phase's scope and would alter what stored records are
admitted — that is a data-layer decision for a later phase, not a restyle.

**Delete the false comment at `SaveLoad.tsx:127-131` in the same edit.** The inference *"the
catalog is the only label source, therefore a stored record can never name a deferred period"*
does not follow, and leaving it standing beside a now-filtered resolver would be a second untrue
claim.

*RED probe (mandatory task step):* plant a `localStorage` record carrying
`"snapshotId": "1914"` in the e2e context, assert the saved-map row shows **no** period label,
then remove the filter and confirm the assertion goes RED. Capture both outputs. Restore by
copying from the scratchpad.
  </action>
  <verify>
    <automated>npx vitest run src/components/SaveLoad.test.tsx src/utils/storage.test.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/SaveLoad.tsx','utf8');if(/role=\"dialog\"|aria-modal/.test(s)){console.error('modal machinery survives the dialog retirement');process.exit(1)};console.log('MODAL_RETIRED')"</automated>
    <automated>git diff HEAD -- src/utils/storage.ts | grep -E '^[+-].*(SNAPSHOT_IDS|483|484)' && { echo "FAIL: storage validator changed"; exit 1; } || echo VALIDATOR_UNTOUCHED</automated>
    <automated>npx playwright test tests/e2e/persistence.spec.ts --project=chrome --reporter=line</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    Saved maps live in the `saved` panel with no dialog; the nested-confirmation contract is
    intact (sibling, own `tabIndex`, innermost-`Escape`, effect-based focus return keyed by a
    stable row key); `getPeriodShortLabel` filters through the approved manifest ids and returns
    nothing for a deferred id; the false comment is deleted; the storage validator is unchanged;
    the `02-22` semantics disposition is written down.
  </done>
  <acceptance_criteria>
    - No `role="dialog"` or `aria-modal` remains in `SaveLoad.tsx`.
    - Deleting a saved map is never one-shot; the confirmation is a sibling with its own
      `tabIndex={-1}` and `Escape` dismisses the innermost layer.
    - Focus return is effect-based and keyed by a stable row key, not an index.
    - A planted `1914` record renders **no** period label on its row, and removing the filter
      makes that assertion RED.
    - `src/utils/storage.ts`'s `SNAPSHOT_IDS` construction and record validator are unchanged.
    - The comment at the former `SaveLoad.tsx:127-131` is deleted.
    - The SUMMARY records the `02-22` disposition per semantic (preserved / superseded + why) and
      the OPEN ITEM 4 decision with its reasoning.
    - `.planning/coding-rules/data.md` records the resolver filter, with at most two
      `Last updated` entries.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Legend editor and Countries panels migrate (D-11 completion)</name>
  <files>src/components/LegendEditor.tsx, src/components/LegendEditor.test.tsx, src/components/LegendDisclosure.tsx, src/components/CountryList.tsx, src/components/LocateCountry.tsx, src/components/editor/ToolPanel.tsx</files>
  <read_first>
    - `src/components/LegendEditor.tsx` (full) — including the empty-state copy at `:334-335` and the `data-legend-validation` hook
    - `src/components/CountryList.tsx` and `src/components/LocateCountry.tsx` (full)
    - `src/utils/legend.ts` — `resolveLegendPosition` / `resolveLegendRender`; **nothing reads `legend.position` raw**
    - `03-UI-SPEC.md` § Component Contracts 7 in full, § Copywriting Contract
    - `.planning/coding-rules/frontend.md` § Styling
  </read_first>
  <action>
Move the legend editor into the `legend` panel and the country list + locate into the `countries`
panel. **The legend itself stays a canvas overlay inside the export-bearing composition (D-24) —
only the *editor* moves.**

**A. Nothing reads `legend.position` raw.** Every read goes through `resolveLegendPosition` /
`resolveLegendRender` (Live Invariant 3). One bypass clips legend content out of the exported PNG.

**B. Entry rows** per `03-UI-SPEC.md` § 7: a Porcelain card at `--radius-card` with `--hairline`;
**no inner hairline** — the label input sits directly on the card with a bottom edge only. A 20×20
swatch at 6px radius with the `--swatch-border` hairline and the unchanged
`aria-label="Color <hex>"`. Label input at `--text-body-sm` on a transparent background. Counter at
`--text-caption` in ghost gray with `tabular-nums` and **`aria-live="off"` (unchanged — it must not
announce on every keystroke)**, turning `--themely-red` at the limit. Actions are icon-only ghost
buttons at 44×44 **stacked onto their own row**, because a full-phrase control row has no width at
which it fits a 280px column.

Keyboard reorder via the two arrow buttons is the **primary** path; drag is an enhancement. The
drag handle keeps `aria-label="Drag <label> to reorder"`. The existing
`data-legend-validation="invalid"` hook stays; an invalid row gets a red left edge **keyed on the
data attribute, not a positional selector**.

**C. Style controls** (theme, text size, border, position) are four `<fieldset>`s, each a Porcelain
card whose `<legend>` is **`--text-body-sm` at weight 500** — deliberately *not*
`--text-subheading`. 16px and 14px are only 2px apart and read as an accident rather than a
hierarchy in the same 280px column; the weight bump carries the hierarchy. This applies to every
`<fieldset>` legend and sub-section heading inside the tool panel. Options are **pills**: neutral
Porcelain + Slate Blue unselected, **Powder + Midnight Ink** selected. **No accent** — the `legend`
panel has no primary action, so per D-05 it carries no Apple Blue at all.

The position picker is a 3×3 grid of 44×44 cells with the four presets plus `Custom`; the selected
cell is Powder. Its announcements are the **existing approved strings** (`Legend moved to Top
left.` etc.) — unchanged and already allowlisted.

**D. Empty states, copy unchanged.** Legend: `Your legend will appear here` +
`Color at least one country to create the first legend entry.` — the compact dashed empty-state
recipe (dashed Stone Gray at 70%, Porcelain at 40%, `--space-lg` padding, a 40×40 round
Apple-Blue-at-10% chip holding a 16px `layers` glyph). At zero coloured countries the entry list is
**absent, not a zero-row table**.

**Zero legends is not a missing legend** — a composition with no legend entries must still export
a white square, not refuse.

**E. Countries panel.** `CountryList` keeps the unfiltered modern 195-core catalog; out-of-scene
rows are **disabled, never removed** (Live Invariant 5). At 195 rows the panel body is the single
scroll container. `LocateCountry` moves with it.

**F. Copy discipline.** No message may say *"Refresh the page"* — the composition lives only in
browser memory and a refresh destroys every unsaved colour, camera, period, and legend. No copy
may advertise a deferred feature: no "coming soon", no unbound keyboard shortcut, no control that
does not exist.

**G. Do not touch line-wrap heuristics.** `LEGEND_CHARACTERS_PER_LINE` and
`LABEL_CHARACTERS_PER_LINE` are D-25's blast radius and belong to **03-11**. Changing them here,
before the legend typeface changes, would re-derive them against the wrong font.
  </action>
  <verify>
    <automated>npx vitest run src/components/LegendEditor.test.tsx src/components/CountryList.test.tsx src/components/LocateCountry.test.tsx src/utils/legend.test.ts</automated>
    <automated>node -e "const {execSync}=require('child_process');const out=execSync(\"grep -rn 'legend\\\\.position' src --include='*.tsx' --include='*.ts' | grep -v 'utils/legend' | grep -v '\\\\.test\\\\.' || true\").toString().trim();if(out){console.error('raw legend.position read:\n'+out);process.exit(1)};console.log('LEGEND_POSITION_RESOLVED_ONLY')"</automated>
    <automated>git diff --quiet HEAD -- src/utils/legend.ts && echo LEGEND_UTIL_UNTOUCHED</automated>
    <automated>npx playwright test tests/e2e/legend.spec.ts tests/e2e/locate.spec.ts --project=chrome --reporter=line</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    The legend editor and the countries tools render inside their panels at 280px with stacked
    labelled controls, weight-carried hierarchy, no accent in the legend panel, unchanged copy and
    announcements, and no raw `legend.position` read anywhere; the legend overlay itself is
    unchanged and still inside the composition.
  </done>
  <acceptance_criteria>
    - No file outside `src/utils/legend.ts` reads `legend.position` raw.
    - `src/utils/legend.ts` is byte-unchanged by this plan (its per-line table belongs to 03-11).
    - `<fieldset>` legends inside the tool panel are `--text-body-sm` at weight 500;
      `--text-subheading` does not appear inside the panel.
    - The `legend` panel contains no Apple Blue element.
    - Legend and saved-maps empty-state copy is character-identical to the existing strings.
    - Out-of-scene country rows are disabled, not removed.
    - The counter keeps `aria-live="off"`.
    - Invalid rows are keyed on `data-legend-validation`, not a positional selector.
    - `tests/e2e/legend.spec.ts` and `tests/e2e/locate.spec.ts` pass in Chrome.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Assertions 15 (completion) and 23 — singleton actions and an unchanged allowlist</name>
  <files>src/styles/uiContract.test.ts, src/App.test.tsx</files>
  <read_first>
    - `src/components/ToastRegion.tsx` — `APPROVED_STATIC_MESSAGES` (`:44+`), the three dynamic patterns, `APPROVED_PERIOD_ANNOUNCEMENTS`, and the live-region wiring at `:202`
    - `src/components/ToastRegion.test.tsx` (full) — count its positive tests before and after
    - `03-UI-SPEC.md` § Copywriting Contract — the "introduces no new message" claim this assertion enforces
  </read_first>
  <action>
**A. Assertion 15 — completion.** With all four panels now migrated, assert on the **composed**
DOM: exactly one `Reset View`, exactly one `Reset All Colors`, and exactly one filled primary
action. `Reset View` is camera-only and lives in the floating cluster (03-08 places it);
`Reset All Colors` is content reset and lives in the Colors panel — **the two never sit together**.
Style the filled action on its **role class** (`controls__action--primary`), never on its container
or its position.

*RED probe (mandatory task step):* render a second filled primary action, OBSERVE RED, capture,
restore by copying from the scratchpad.

**B. Assertion 23 — the `ToastRegion` allowlist and its positive-test count are UNCHANGED by this
phase.** Record the counts as literals in the assertion: the number of entries in
`APPROVED_STATIC_MESSAGES`, the three dynamic patterns, the `APPROVED_PERIOD_ANNOUNCEMENTS`
entries, and the number of positive tests in `ToastRegion.test.tsx`. **If the allowlist grows, a
message was introduced without a test** — that is the whole point of the assertion, and it is why
the counts are hard numbers rather than a shape check.

`03-UI-SPEC.md` § Copywriting Contract records that **this phase introduces no new toast, status,
or live-region message**. Every string added by 03-06 and 03-07 is a static control label,
heading, or pill; none reaches `onStatusMessage`. Verify that claim rather than assuming it: list
every new string in the SUMMARY and state, for each, that it is a label and not a message.

*RED probe (mandatory task step):* add a status message without a test, OBSERVE RED, capture,
restore by copying from the scratchpad.

**C. Confirm the D-11 dissolution is complete and lossless.** Cross-check 03-05's
`Relocated controls → receiving plan` table against the composed DOM: every control that existed
before the app bar and inspector column dissolved is reachable now. A missing control here is the
deleted-inspector-landmark defect repeating, and it was found by independent review rather than by
a test the first time.

**D. `ToastRegion` remains a boundary, not a renderer.** Everything it receives is untrusted until
it matches an exact approved string or a bounded dynamic pattern; anything else degrades to the
severity fallback. Only its **surface** is restyled — Porcelain card, `--radius-card`,
`--hairline`, `--popover-shadow`, `--text-body-sm`; error variant is red at 10% background, red at
20% border, red text. **No accent.**
  </action>
  <verify>
    <automated>npx vitest run src/styles/uiContract.test.ts src/App.test.tsx src/components/ToastRegion.test.tsx</automated>
    <automated>npx playwright test tests/e2e/rail.spec.ts tests/e2e/persistence.spec.ts tests/e2e/history.spec.ts --project=chrome --reporter=line</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    The composed DOM holds exactly one `Reset View`, one `Reset All Colors`, and one filled
    primary action; the `ToastRegion` allowlist and its positive-test count are asserted as
    literal numbers and are unchanged; every control relocated by 03-05 is reachable; both RED
    probes were executed and captured.
  </done>
  <acceptance_criteria>
    - Assertion 15 asserts exactly one of each of the three singletons on the composed DOM.
    - Assertion 23 encodes hard counts for the allowlist entries and the positive-test count, and
      those counts equal their pre-phase values.
    - Every new Phase 3 string is listed in the SUMMARY and identified as a label, not a message.
    - The SUMMARY confirms every control in 03-05's relocation table is reachable in the composed
      DOM.
    - `ToastRegion`'s error variant carries no accent.
    - The SUMMARY carries two captured RED outputs (second filled action, untested status
      message).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved manifest → the period UI | The only source that may name a period; the five-entry catalog is a label registry, not an approval list |
| stored composition record → the saved-map row | A hand-crafted record can carry a deferred snapshot id |
| any code path → `ToastRegion` | Everything it receives is untrusted until it matches the allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-27 | Spoofing | a deferred snapshot made nameable in the period surface | critical | mitigate | `PeriodHud` renders `resolvePeriodOptions` output only; assertion 13 scoped to `.period-hud` asserts none of the four historical labels appears there; a source scan forbids `SNAPSHOT_CATALOG` in that component; RED-proven by rendering the constant |
| T-03-28 | Spoofing | a hand-crafted storage record naming `1914` on a saved-map row | medium | mitigate | Approved-id filter adopted on `getPeriodShortLabel` (presentation layer); RED-proven by planting a `1914` record; the storage validator is deliberately NOT changed and that boundary is recorded |
| T-03-29 | Repudiation | losing the period control's accessible description when the bar dissolves | high | mitigate | Assertion 14's second half asserts `aria-describedby` resolves to an existing element; RED-proven by deleting the region |
| T-03-30 | Information disclosure | an unbounded string reaching a creator-facing surface | medium | mitigate | `ToastRegion` stays a boundary; assertion 23 pins the allowlist and its positive-test count as hard numbers; RED-proven by adding an untested status message |
| T-03-31 | Denial of service | a control lost between two plans during the dissolution | medium | mitigate | Task 4 cross-checks 03-05's relocation table against the composed DOM; assertion 15 pins the three singletons |
| T-03-32 | Tampering | legend content clipped out of the PNG by a raw `legend.position` read | high | mitigate | Grep gate asserting every read goes through `resolveLegendPosition` / `resolveLegendRender`; `src/utils/legend.ts` asserted byte-unchanged so the per-line table stays with 03-11 |
</threat_model>

<verification>
```bash
npm run lint
npm test
npm run build
npx playwright test tests/e2e/history.spec.ts tests/e2e/persistence.spec.ts tests/e2e/legend.spec.ts tests/e2e/locate.spec.ts tests/e2e/rail.spec.ts --project=chrome
npx vitest run src/App.test.tsx src/styles/uiContract.test.ts src/components/ToastRegion.test.tsx
git diff --stat HEAD -- public/data/ ; echo "expect: no output"
```

By hand: `npm run dev`, hard reload, and confirm the period pill reads `Modern — current borders`
with no dropdown affordance and no mention of any other period anywhere in the UI.

Chrome only. Edge is not installed (D-33).
</verification>

<success_criteria>
- [ ] The period surface renders resolved manifest options only, as an inert pill, with no deferred label anywhere in it (D-14, assertion 13, RED-proven)
- [ ] The rehomed live region keeps its byte-identical id, role, and politeness, and `aria-describedby` still resolves (D-15, assertion 14, RED-proven on the resolve half)
- [ ] `CompositionBar.tsx` is deleted and both of its responsibilities have owners (D-11)
- [ ] Saved maps live in the panel with no dialog, and the nested-confirmation contract survives verbatim
- [ ] The `02-22` action-order semantics are recorded as preserved or superseded, per semantic, in writing
- [ ] OPEN ITEM 4 decided: approved-id filter on the resolver, storage validator untouched, false comment deleted, RED-proven with a planted `1914` record
- [ ] Legend and countries panels fit 280px with stacked labelled controls and weight-carried hierarchy; the legend panel carries no accent
- [ ] No raw `legend.position` read; `src/utils/legend.ts` byte-unchanged
- [ ] Exactly one `Reset View`, one `Reset All Colors`, one filled primary action (assertion 15, RED-proven)
- [ ] `ToastRegion`'s allowlist and positive-test count are pinned as hard numbers and unchanged (assertion 23, RED-proven)
- [ ] `public/data/` untouched; `SNAPSHOT_CATALOG` still five unchanged entries
</success_criteria>

<output>
Create `.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-07-SUMMARY.md` when done, carrying:
`02-22 action-order disposition (per semantic)`, `OPEN ITEM 4 decision + reasoning`,
`New copy strings — each identified as a label, not a message`, `Relocated controls reachability
cross-check`, and `RED probes (7, with output)`.

**Do not run `state.advance-plan`, `state.update-progress`, or `roadmap.update-plan-progress`.**
Edit `.planning/STATE.md` and `.planning/ROADMAP.md` by hand.
</output>
