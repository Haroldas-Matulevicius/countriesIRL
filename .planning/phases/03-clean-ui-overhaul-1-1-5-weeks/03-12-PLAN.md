---
phase: 03-clean-ui-overhaul-1-1-5-weeks
plan: 12
type: execute
wave: 12
depends_on:
  - 03-11
files_modified:
  - .planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-12-REVIEW.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - D-33
  - REVIEW-INDEPENDENCE

estimate:
  tokens: 155000
  raw_tokens: 78000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The reviewer is NOT the executor of 03-01 through 03-11. Independent non-author review of the AGGREGATE diff is mandatory: executor self-reported checkpoints previously missed five real defects across three rounds on a single stack that the executor had already marked resolved."
    - "D-33: the phase gate runs Chrome ONLY, and its evidence states 'Edge not certified — not installed' rather than omitting it or inferring a pass. Microsoft Edge is not installed on this machine, so the msedge Playwright project cannot launch."
    - "Phase 3 does not cite or repeat STATE.md's 'Edge 150 — 71/71' record. That is immutable Phase 2 evidence to be annotated, never rewritten, and resolving the contradiction is filed against Phase 2."
    - "All 28 UI-SPEC contract assertions exist in the tree, each is bound to an owning plan, and each has a RECORDED, CAPTURED RED proof — an assertion with no RED proof is treated as a finding, not as a pass."
    - "Every trackable decision D-01 through D-35 is implemented and evidenced; nothing was silently dropped, descoped, or reduced to a 'v1'."
    - "The full gate is green: npm run lint && npm test && npm run build, plus the full Playwright suite on Chrome."
    - "02-28 remains OPEN, binds fe5f946, and was not modified by any Phase 3 plan; the acceptance-02-28 tag points at the exact commit."
    - "STATE.md and ROADMAP.md are updated BY HAND — the gsd-sdk verbs state.advance-plan, state.update-progress, and roadmap.update-plan-progress are never run, because they have already zeroed this repo's progress counters, deleted its activity log, and marked an incomplete plan complete."
  artifacts:
    - .planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-12-REVIEW.md
  key_links:
    - "The review reads the AGGREGATE diff across 03-01..03-11, not per-plan diffs — the defects this step exists to catch live in the interaction between plans, not inside one."
    - "The assertion-to-RED-proof ledger is the phase's honesty check: an assertion that exists but was never observed failing is exactly the class this repo has shipped four times."
    - "Chrome-only evidence must be stated affirmatively; an omitted browser reads as a pass to the next reader."
  backstops:
    - statement: "No visual or behavioural regression survives the phase that a Chrome e2e run would catch; anything Chrome cannot certify (Firefox, Safari, Edge, previous versions) is named as unverified rather than left silent."
      verification: backstop
---

<objective>
Independently review the aggregate Phase 3 diff, run the full gate on Chrome, and record the
evidence honestly — including what was not certified.

Purpose: executor self-reported checkpoints have proven unreliable here. Independent non-author
review of an aggregate diff caught five real defects across three rounds on a single stack that
the executor had already marked resolved: a stuck export gate, a phantom cross-scene selection, a
deleted inspector landmark, and a legend overflow clipping the PNG. This phase touched the token
system, the composition root, the layout, and `src/utils/export.ts` — the review is not optional
and it is not a formality.

Output: `03-12-REVIEW.md` with a findings ledger, an assertion-to-RED-proof table, a decision
coverage table, and a browser-scope statement that says plainly what was not run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/export.md
@.planning/coding-rules/storage.md
@.planning/coding-rules/data.md
@Design.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-CONTEXT.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-UI-SPEC.md

All eleven plan summaries:
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-01-SUMMARY.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-02-SUMMARY.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-03-SUMMARY.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-04-SUMMARY.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-05-SUMMARY.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-06-SUMMARY.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-07-SUMMARY.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-08-SUMMARY.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-09-SUMMARY.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-10-SUMMARY.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-11-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| `03-12-REVIEW.md` | evidence | findings ledger, assertion↔RED-proof table, D-01…D-35 coverage table, gate evidence, browser-scope statement |
| hand-edited `.planning/STATE.md` | tracking | position, decisions, blockers, pending todos — edited by hand only |
| hand-edited `.planning/ROADMAP.md` | tracking | § Progress counts and the Phase 3 plan checkboxes — edited by hand only |
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm reviewer independence before the review begins</name>
  <decision>Confirm that the agent or person performing this review did NOT execute any of 03-01 through 03-11.</decision>
  <context>
The ROADMAP states the requirement plainly: *"Reviewer is not the executor of `03-01`…`03-11`."*
`CLAUDE.md` § Guardrails states it again: *"Independent review is not optional. Executor
self-reported checkpoints proved unreliable: non-author review of an aggregate diff caught five
real defects that the executor had already marked resolved."*

This checkpoint exists because the failure mode is silent. A reviewer who wrote the code reads
their own intent into the diff and confirms what they meant rather than what they wrote. Every one
of the five previously-missed defects had already been marked resolved by its author.

Confirm one of:
- a **fresh agent session** with no execution history in this phase, or
- a **different person**, or
- (if neither is available) the owner explicitly accepts a **self-review** and the review record
  says so in its own header, prominently, so no later reader mistakes it for independent review.

The third option is not equivalent to the first two and must never be recorded as if it were.
  </context>
  <options>
    <option id="independent">
      <name>Independent — a fresh session or a different person performs the review</name>
      <pros>Satisfies the ROADMAP requirement and the guardrail; this is the configuration that caught five defects.</pros>
      <cons>Requires a context reset or a second party.</cons>
    </option>
    <option id="self-review-accepted">
      <name>Self-review, explicitly accepted by the owner</name>
      <pros>Unblocks the phase when no independent reviewer is available.</pros>
      <cons>The configuration that has already missed five real defects here. Must be labelled as such in the review header.</cons>
    </option>
  </options>
  <resume-signal>Select: independent, or self-review-accepted. Name the option.</resume-signal>
  <acceptance_criteria>
    - The selection is recorded in `03-12-REVIEW.md`'s header, with the date.
    - If `self-review-accepted`, the review header states prominently that this is NOT an
      independent review and names the owner who accepted it.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Review the aggregate Phase 3 diff</name>
  <files>.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-12-REVIEW.md</files>
  <read_first>
    - the aggregate diff: `git diff acceptance-02-28..HEAD --stat` then the full diff per area
    - all ten plan summaries (loaded in `<context>` above)
    - `.planning/coding-rules/general.md` § Live Invariants and § Immutable Safety Constraints
    - `03-UI-SPEC.md` § What This Spec Does Not Change
  </read_first>
  <action>
Read the **aggregate** diff, not the per-plan diffs. `git diff acceptance-02-28..HEAD` is the exact
range — the tag created in 03-01 marks the pre-restyle build, which is what makes this range
meaningful. The defects this step exists to catch live **between** plans, not inside one.

Review against these, in this order:

**A. The nine Live Invariants** (`coding-rules/general.md`). For each, state PASS or a finding
with a file:line citation. Pay particular attention to:
- **#3** nothing reads `legend.position` raw on a render or export path;
- **#4** exactly one `MapCanvasHandle` and one `svg.map-canvas` across the 1200px transition, and
  the camera lease released from the outermost `finally` on every path;
- **#6** the period selector is catalog-driven and deferred snapshots stay structurally
  unreachable;
- **#7** export strips semantics, never geometry; a legend outside the canonical SVG is a hard
  refusal, never a silently legend-less PNG;
- **#9** `--map-*` are export tokens declared exactly once in `:root` — **now extended to `.dark`**.

**B. The ten Immutable Safety Constraints.** In particular: nothing reads as though a historical
snapshot shipped; `public/data/` is untouched; no browser outside the Playwright configuration is
reported as passed; a blanket approval is recorded as a proceed-authorization and not as a content
review.

**C. `03-UI-SPEC.md` § What This Spec Does Not Change.** Verify each item held: exactly 1080×1080;
placement decides export membership with the two typed slots preserved verbatim; `SNAPSHOT_CATALOG`
semantics and `resolvePeriodOptions` untouched; one `MapCanvasHandle` and one `svg.map-canvas`;
undo/redo is colours only; one roving-tabindex writer; `ToastRegion` is the allowlist boundary.

**D. Scope-reduction audit.** Grep the added lines of the diff, and the summaries, for `v1`,
`for now`, `placeholder`, `static for now`, `hardcoded for now`, `future enhancement`,
`basic version`, `wired later`, `skip for now`. Every hit is a finding until shown otherwise.
Nothing in this phase was authorized to be delivered as a reduced version of a locked decision.

**Exclude the HTML attribute form of `placeholder` before matching.** `placeholder=`,
`'placeholder'`, and `"placeholder"` are legitimate — `03-06` adds a custom hex input and `03-07`
adds legend label fields, both of which carry real `placeholder` attributes. The audit is looking
for the word used as an *excuse*, in prose and comments, not for a form-field attribute. Strip the
attribute form first, then match.

**E. Cross-plan interaction hazards** — the specific places an aggregate review earns its keep:
- a control relocated by 03-05 and never re-homed by 03-06/03-07 (the deleted-landmark class);
- a token deleted in 03-04 whose consumer was migrated in one file and missed in another;
- a rule moved in 03-10 that was order-dependent and now applies differently;
- an assertion added in an early plan that a later plan quietly narrowed or skipped;
- a test file that re-declares helpers instead of importing `tests/e2e/support/appHarness.ts`.

**F. Write findings, not impressions.** Each finding gets: a severity, a file:line, what is wrong,
and what would make it right. A finding is not resolved by an executor saying it is resolved —
that is precisely the pattern that failed here five times. Re-verify each fix in the tree.

**G. Do not fix code in this task.** Record findings. If a finding is fixed, the fix is a separate,
attributed change and the review record names who made it and re-states the re-verification.
  </action>
  <verify>
    <automated>git diff --stat acceptance-02-28..HEAD | tail -5</automated>
    <automated>node -e "const {execSync}=require('child_process');const added=execSync('git diff acceptance-02-28..HEAD -- src tests').toString().split('\n').filter(l=>l.startsWith('+')&&!l.startsWith('+++')).map(l=>l.replace(/placeholder\s*=/gi,'').replace(/'placeholder'|\x22placeholder\x22/gi,'')).join('\n');const bad=['for now','placeholder','future enhancement','wired later','skip for now','basic version'];const hits=bad.filter(t=>new RegExp(t,'i').test(added));if(hits.length){console.error('scope-reduction language in the diff:',hits);process.exit(1)};console.log('NO_SCOPE_REDUCTION_LANGUAGE')"</automated>
    <automated>test -f .planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-12-REVIEW.md && echo REVIEW_STARTED</automated>
  </verify>
  <done>
    `03-12-REVIEW.md` carries a findings ledger covering the nine Live Invariants, the ten
    Immutable Safety Constraints, the UI-SPEC's does-not-change list, a scope-reduction audit, and
    the five named cross-plan interaction hazards — each with PASS or a cited finding.
  </done>
  <acceptance_criteria>
    - The review cites `git diff acceptance-02-28..HEAD` as its range.
    - Each of the nine Live Invariants has an explicit PASS or a cited finding.
    - Each of the ten Immutable Safety Constraints has an explicit PASS or a cited finding.
    - The scope-reduction audit is recorded with its search terms and its results.
    - Every finding carries severity, file:line, the defect, and the remedy.
    - No finding is marked resolved on an executor's say-so; each fix is re-verified in the tree
      and the re-verification is recorded.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: The assertion ledger and the decision coverage table</name>
  <files>.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-12-REVIEW.md</files>
  <read_first>
    - `03-UI-SPEC.md` § Contract Assertions — the 28-row table with its owning plans
    - `03-CONTEXT.md` § Implementation Decisions — D-01 through D-35
    - all ten plan summaries' `RED probes` sections
    - `src/styles/uiContract.test.ts`, `src/lib/motion/tokens.test.ts`, `src/components/icons/iconContract.test.ts`, `src/config/editorConfig.test.ts`, and the e2e specs
  </read_first>
  <action>
Two ledgers, both of which are the honesty check on the whole phase.

**A. The assertion ledger — 28 rows, one per UI-SPEC contract assertion.** For each, record:

| # | Assertion | Owning plan | Where it lives (file) | RED proof captured? | Verdict |
|---|---|---|---|---|---|

**An assertion that exists but has no captured RED proof is a FINDING, not a pass.** This repo has
shipped four tests that could not fail — a self-comparing performance gate, a fixture asserting
wiring it re-implemented, a pixel probe satisfied by three blank canvases, and the theme gate D-35
names. Each of them read as proof. The RED proof is the only thing distinguishing an assertion
from a comment.

Give extra scrutiny to the four that make up the **export firewall** — assertions 4, 17, 24, and
25 — and to the two the planner flagged as most likely to become gates that cannot fail:

- **Assertion 25.** Confirm it measures **rasterised pixels**, that its crop bounds derive from
  `resolveLegendRender` rather than being hard-coded, that it has a **content floor** and a
  **blank-crop discrimination control**, and that 03-11 captured its RED output when the
  `@font-face` injection was deleted. A markup-level `font-family: Inter` check is **green today,
  before any work** and is not acceptable evidence.
- **Assertion 24.** Confirm no `emulateMedia({ colorScheme })` survives in
  `tests/e2e/responsive.spec.ts`, and that the theme axis toggles `.dark` on the editor mount
  root. Then confirm **two** RED proofs exist, not one: `03-09`'s original (captured against the
  html2canvas path) **and** `03-11` Task 7 probe 9's re-proof against the replaced rasterisation
  path. Assertion 24 is Live Invariant 9's **only browser-level guard**, and `03-11`'s own
  governing rule is that a test which passed against html2canvas and still passes has not been
  shown to test the new code. **A ledger row citing only `03-09`'s proof is a FINDING.**

  Note when reading the two records: the probe adds `--map-surface` to `.dark`, which also fails
  assertion 4 by design. Both summaries should say so; a record showing only one failure means the
  probe was narrowed.

**Match the guards to what the review must actually contain.** Two checks in this task's verify
block are deliberately narrow rather than bare greps, because bare greps would fail on this
review's own required output:
- the MISSING check matches a **table row** (`^|…| MISSING |`), not the word anywhere — Task 3's
  own verdict vocabulary is `IMPLEMENTED | DEFERRED | MISSING` and the prose says "no MISSING
  rows";
- the `colorScheme` check matches the **emulation call shape**, not the bare identifier — `03-11`
  requires `style.colorScheme = 'light'` to survive on the export frame and the cloned SVG, so a
  spec legitimately referencing `colorScheme` must not trip it.

**B. The decision coverage table — D-01 through D-35 plus D-34a.** For each: the implementing
plan, the evidence (file, test, or summary section), and a verdict of IMPLEMENTED, DEFERRED (with
the owner decision that deferred it), or **MISSING**. A MISSING row is a phase-blocking finding.

Where a decision was answered by an owner checkpoint (D-01, D-25, D-27's R-V1, D-34), record the
owner's response verbatim and state whether the authorization held is a **content review** or a
**blanket, sight-unseen proceed-authorization**. Immutable Safety Constraint 8 requires recording
which one you actually have; they are not interchangeable.

**C. Verify the two vendoring requirements closed with evidence.** R-V1: the `motion` checkpoint
was performed by a human before the install, with the `too-new` verdict carried, and `motion` is
pinned at exactly `12.40.0`. R-V2: every file under `src/components/icons/` has a dated
`read in full` provenance line, the recorded set equals the file set, and the three previously
PENDING upstream files carry an explicit dated disposition.

**D. Verify the two recorded planner decisions landed.** OPEN ITEM 3 — the `02-22` action-order
semantics were recorded per semantic as preserved or superseded. OPEN ITEM 4 — the approved-id
filter on the saved-map short-label resolver was adopted, the storage validator was **not**
changed, and the false comment was deleted.

**E. File OPEN ITEM 4's residual, because a deliberate partial fix still leaves a fact
outstanding.** The `03-07` decision was to filter the **presentation layer** and deliberately not
touch `storage.ts:61-63` (`SNAPSHOT_IDS` built from all five catalog entries) or the record
validator at `:483-484` — a behaviour change there is out of a chrome phase's scope. That
reasoning is sound, but it leaves a true statement nobody has written down: **the storage
validator still admits a hand-crafted record carrying any of the five snapshot ids, including the
four deferred ones; only the label rendering is filtered.**

Record that in `.planning/STATE.md` § Pending Todos in Task 4, filed the same way the Edge
contradiction is filed — as an outstanding fact against the layer that owns it, not as Phase 3
work and not as a defect. Word it so a later reader cannot mistake the presentation filter for a
validator fix. Reaching the behaviour requires hand-editing browser storage, so it is a weak,
local exposure, not a path by which a deferred snapshot becomes reachable — say that too, so the
todo is neither overstated nor silently dropped.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-12-REVIEW.md','utf8');const missingD=[];for(let i=1;i<=35;i++){const id='D-'+String(i).padStart(2,'0');if(!s.includes(id))missingD.push(id)};if(missingD.length){console.error('decisions absent from the coverage table:',missingD);process.exit(1)};console.log('ALL_35_DECISIONS_PRESENT')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-12-REVIEW.md','utf8');const rows=s.split('\n').filter(l=>/^\|.*\|\s*MISSING\s*\|/.test(l));if(rows.length){console.error('coverage table contains MISSING row(s) — phase-blocking:\n'+rows.join('\n'));process.exit(1)};console.log('NO_MISSING_ROWS')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('tests/e2e/responsive.spec.ts','utf8');if(/emulateMedia\(\s*\{[^}]*colorScheme/.test(s)){console.error('colorScheme emulation survives in responsive.spec.ts');process.exit(1)};console.log('ASSERTION_24_REARMED')"</automated>
    <automated>node -e "const p=require('./package.json');if(p.dependencies.motion!=='12.40.0'){console.error('motion pin drifted:',p.dependencies.motion);process.exit(1)};console.log('MOTION_PIN_OK')"</automated>
  </verify>
  <done>
    `03-12-REVIEW.md` carries a 28-row assertion ledger with a captured-RED-proof column and a
    36-row decision coverage table (D-01…D-35 plus D-34a) with no MISSING rows; R-V1 and R-V2 are
    closed with evidence; the two recorded planner decisions are verified landed.
  </done>
  <acceptance_criteria>
    - All 28 assertions appear with an owning plan, a file location, and a RED-proof verdict.
    - Any assertion without a captured RED proof is recorded as a FINDING.
    - All 36 decision ids appear with an implementing plan, evidence, and a verdict; no MISSING
      rows remain.
    - Owner-checkpoint responses are recorded verbatim, each labelled content review or blanket
      proceed-authorization.
    - Assertion 25 is confirmed to measure pixels with derived crops, a content floor, and a blank
      control.
    - Assertion 24 is confirmed to toggle `.dark` with no surviving `colorScheme` emulation, and
      **two** RED proofs are on record — `03-09`'s and `03-11` Task 7 probe 9's re-proof against
      the replaced rasterisation path. A row citing only `03-09` is recorded as a FINDING.
    - `motion` is pinned at exactly `12.40.0`.
    - The OPEN ITEM 4 residual is written for Task 4 to file: the storage validator still admits
      all five snapshot ids and only the presentation layer is filtered.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: The full gate on Chrome, honest browser scope, and hand-edited tracking files</name>
  <files>.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-12-REVIEW.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/STATE.md` (full) — the Current Position, Decisions, Pending Todos, and Blockers sections to edit by hand
    - `.planning/ROADMAP.md` § Phase 3 and § Progress — the canonical counts
    - `.planning/coding-rules/general.md` § Planning-file safety
    - `playwright.config.ts` — the `chrome` and `msedge` project declarations
  </read_first>
  <action>
**A. Run the full gate.** In order:

```
npm run lint
npm test
npm run build
npx playwright test --project=chrome
```

Record the actual numbers: lint result, unit pass/total and file count, build result, and e2e
pass/total. Numbers, not adjectives.

**B. Browser scope — state it plainly, affirmatively (D-33).** Microsoft Edge is **not installed**
on this machine (`/Applications` holds no `Microsoft*.app`; `~/Library/Caches/ms-playwright` holds
only `ffmpeg-1011`), so `playwright.config.ts`'s `msedge` project — which uses
`channel: 'msedge'`, i.e. system Edge — **cannot launch**. The gate therefore runs Chrome only.

The evidence must state **"Edge not certified — not installed"**. Not omitted. Not inferred. An
omitted browser reads as a pass to the next reader, and Immutable Safety Constraint 9 forbids
reporting an unrun browser as passed.

**Do not cite `STATE.md`'s "Edge 150 — 71/71" record.** That is **immutable Phase 2 evidence** at
`fe5f946` — annotate, never rewrite. Resolving how an Edge result was recorded on a machine with
no Edge is **out of Phase 3 scope** and is filed as a pending todo against Phase 2's evidence. Phase
3 must not cite it and must not repeat it.

**Firefox, Safari, and previous-version certification have never been run here** and must never be
reported as passed. Record them as unverified. Note additionally that Safari is the *documented
exception* for 03-11's data-URI-font-in-SVG-image technique — recorded, not solved, and never
described as cross-browser.

**C. Record browser versions honestly.** Chrome's version is recorded **by hand** and must not be
presented as machine evidence — the Phase 2 evidence JSON captured only Node, npm, platform, and
arch, and that limitation still applies.

**D. Confirm the Phase 2 evidence is intact.** `git rev-list -n 1 acceptance-02-28` equals
`fe5f946060707c48c3d9591d368b5f3f8f90dd4d`; nothing under
`.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/` changed; `02-28` is still
OPEN and `02-25` is still OPEN. Phase 3 closed neither and was never authorized to.

**E. Update `.planning/STATE.md` and `.planning/ROADMAP.md` BY HAND.**

**Never run the gsd-sdk verbs `state.advance-plan`, `state.update-progress`, or
`roadmap.update-plan-progress`.** They infer status from file presence and have already zeroed
this repo's progress counters, deleted its activity log, and marked an incomplete plan complete.
Edit both files with a text editor.

In `STATE.md`: update `stopped_at`, `last_activity`, `progress`, the Current Position section, the
Decisions section (the Phase 3 decisions that became durable engineering contracts belong in
`coding-rules/`, not restated here), and Pending Todos.

**Two items must be filed in § Pending Todos, both as outstanding facts rather than as defects:**
1. The Phase 2 Edge-record contradiction, filed **against Phase 2** and phrased as an annotation
   task, never as a rewrite.
2. The OPEN ITEM 4 residual from Task 3(E): the storage validator (`storage.ts:61-63`, `:483-484`)
   still admits a hand-crafted record carrying any of the five snapshot ids; **only the
   presentation layer is filtered**, by the approved-id filter `03-07` added to
   `getPeriodShortLabel`. Note that reaching it requires hand-editing browser storage, so it is a
   weak, local exposure and not a path by which a deferred snapshot becomes reachable. Word it so
   the presentation filter cannot be mistaken for a validator fix.

`progress` counts Phase 3 as **twelve** plans, not eleven — `03-01` and `03-02` are the two halves
of the ROADMAP § Phase 3 numbered list's first item.

In `ROADMAP.md`: tick the Phase 3 plan checkboxes that are genuinely complete and update
§ Progress, which is **canonical** for status and counts. Do not tick a plan whose findings are
open.

**F. Do not claim more than was verified.** `02-28` stays OPEN. Deployment does not exist and is
not claimed. No historical snapshot shipped. If any finding from Task 2 or Task 3 is unresolved,
the phase is **not** complete and the review says so — a green gate with open findings is a green
gate with open findings, not a completed phase.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <automated>npx playwright test --project=chrome --reporter=line</automated>
    <automated>test "$(git rev-list -n 1 acceptance-02-28)" = "fe5f946060707c48c3d9591d368b5f3f8f90dd4d" && git diff --quiet HEAD -- .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/ && echo PHASE2_EVIDENCE_INTACT</automated>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-12-REVIEW.md','utf8');if(!/Edge not certified — not installed/.test(s)){console.error('review does not state the Edge scope verbatim');process.exit(1)};if(/Edge 150/.test(s)){console.error('review cites STATE.md Edge record — forbidden');process.exit(1)};console.log('BROWSER_SCOPE_HONEST')"</automated>
    <automated>npm run data:world:check</automated>
  </verify>
  <done>
    The full gate ran on Chrome with recorded numbers; the review states "Edge not certified — not
    installed" and cites no Edge pass; Firefox, Safari, and previous versions are recorded as
    unverified; the `acceptance-02-28` tag still points at `fe5f946` and Phase 2's evidence
    directory is unchanged with both owner gates still OPEN; `STATE.md` and `ROADMAP.md` were
    edited by hand.
  </done>
  <acceptance_criteria>
    - `npm run lint && npm test && npm run build` all pass, with pass/total numbers recorded.
    - The full Playwright suite passes on the `chrome` project, with pass/total recorded.
    - The review states **"Edge not certified — not installed"** verbatim and contains no Edge
      pass claim and no citation of `STATE.md`'s Edge record.
    - Firefox, Safari, and previous-version certification are recorded as unverified.
    - Chrome's version is labelled as recorded by hand, not as machine evidence.
    - `git rev-list -n 1 acceptance-02-28` equals the bound SHA and Phase 2's directory is
      unchanged; `02-25` and `02-28` are both still OPEN.
    - `STATE.md` and `ROADMAP.md` were edited by hand; no gsd-sdk state or roadmap mutation verb
      was run.
    - No plan is ticked in `ROADMAP.md` while any of its findings is open.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor claim → recorded evidence | The boundary this whole plan exists to police |
| unrun browser → a reported result | An omitted browser reads as a pass |
| automated tooling → the tracking files | Three gsd-sdk verbs have already corrupted them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-57 | Repudiation | a self-review recorded as an independent review | critical | mitigate | Task 1's blocking checkpoint records which configuration was used; a self-review must say so prominently in the review header |
| T-03-58 | Repudiation | claiming an Edge pass or omitting the browser | critical | mitigate | The review must state "Edge not certified — not installed" verbatim; a check forbids citing `STATE.md`'s Edge record; Firefox/Safari/previous versions recorded as unverified |
| T-03-59 | Repudiation | an assertion with no RED proof counted as a pass | critical | mitigate | The 28-row ledger has a captured-RED-proof column; a missing proof is a FINDING, not a pass |
| T-03-60 | Tampering | the tracking files corrupted by an automated status verb | high | mitigate | `STATE.md` and `ROADMAP.md` edited by hand only; the three forbidden verbs named explicitly in the action and the acceptance criteria |
| T-03-61 | Repudiation | Phase 2 evidence altered or its gates closed by Phase 3 | critical | mitigate | The `acceptance-02-28` tag SHA and the Phase 2 directory are both asserted unchanged; `02-25` and `02-28` confirmed still OPEN |
| T-03-62 | Repudiation | a locked decision silently reduced to a partial delivery | high | mitigate | The 36-row decision coverage table admits no MISSING rows; the scope-reduction language audit runs over the aggregate diff |
</threat_model>

<verification>
```bash
npm run lint
npm test
npm run build
npx playwright test --project=chrome
npm run data:world:check
git rev-list -n 1 acceptance-02-28
git diff --stat acceptance-02-28..HEAD
git diff --stat HEAD -- .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/ ; echo "expect: no output"
```

**Chrome only.** Microsoft Edge is not installed on this machine, so the `msedge` Playwright
project cannot launch. Record **"Edge not certified — not installed"**. Do not cite `STATE.md`'s
Edge record — that is immutable Phase 2 evidence to be annotated, never rewritten, and resolving
the contradiction is Phase 2 work. Firefox, Safari, and previous-version certification have never
been run here and must never be reported as passed.
</verification>

<success_criteria>
- [ ] Reviewer independence is confirmed and recorded; a self-review, if used, is labelled as such in the review header
- [ ] The aggregate `acceptance-02-28..HEAD` diff was reviewed against the nine Live Invariants, the ten Immutable Safety Constraints, and the UI-SPEC's does-not-change list, each with PASS or a cited finding
- [ ] The scope-reduction language audit ran and its results are recorded
- [ ] All five named cross-plan interaction hazards were checked
- [ ] The 28-row assertion ledger is complete, with a captured-RED-proof verdict per row; assertions 24 and 25 verified in detail, and assertion 24 carries **two** proofs (`03-09`'s and `03-11` probe 9's re-proof against the replaced export path)
- [ ] The OPEN ITEM 4 residual is filed in `STATE.md` § Pending Todos — validator unchanged, presentation filtered
- [ ] The 36-row decision coverage table (D-01…D-35 + D-34a) has no MISSING rows
- [ ] R-V1 and R-V2 are closed with evidence; `motion` is pinned at exactly `12.40.0`
- [ ] OPEN ITEM 3 and OPEN ITEM 4 decisions are verified landed
- [ ] `npm run lint && npm test && npm run build` green with recorded numbers, and the full Playwright suite green on Chrome
- [ ] The evidence states "Edge not certified — not installed", cites no Edge pass, and does not repeat `STATE.md`'s Edge record (D-33)
- [ ] `acceptance-02-28` still points at `fe5f946`; Phase 2's evidence is unchanged; `02-25` and `02-28` are both still OPEN
- [ ] `STATE.md` and `ROADMAP.md` were edited by hand; no forbidden gsd-sdk verb was run
</success_criteria>

<output>
Create `.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-12-REVIEW.md` and
`.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-12-SUMMARY.md`.

`03-12-REVIEW.md` must carry, as named sections: `Reviewer independence`, `Findings ledger`,
`Live Invariants (9)`, `Immutable Safety Constraints (10)`, `Scope-reduction audit`,
`Cross-plan interaction hazards`, `Assertion ledger (28)`, `Decision coverage (D-01…D-35, D-34a)`,
`Vendoring requirements (R-V1, R-V2)`, `Gate evidence (numbers)`, and
`Browser scope — Chrome only; Edge not certified — not installed`.

**Do not run `state.advance-plan`, `state.update-progress`, or `roadmap.update-plan-progress`.**
Edit `.planning/STATE.md` and `.planning/ROADMAP.md` by hand.
</output>
