---
phase: 03-clean-ui-overhaul-1-1-5-weeks
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - src/components/editor/MapEditor.tsx
  - src/components/editor/MapEditor.test.tsx
  - src/components/MapWorkspace.tsx
  - src/components/MapWorkspace.test.tsx
  - src/config/editorConfig.ts
  - src/config/editorConfig.test.ts
  - src/hooks/useGeoData.ts
  - src/constants/snapshots.ts
  - src/App.tsx
  - src/main.tsx
  - src/styles/App.css
  - .planning/coding-rules/data.md
autonomous: true
requirements:
  - D-11
  - D-24
  - TR-a
  - TR-b
  - TR-c
  - TR-e

estimate:
  tokens: 170000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Transition-readiness (a): the canvas plus HUD assemble into ONE mountable editor component behind an explicit props boundary that never assumes it owns document or window chrome beyond its own mount point."
    - "D-11: the top app bar is gone as a container — the canvas is full-bleed edge to edge — and the tools it held are relocated, not deleted."
    - "D-24: MapWorkspace's typed legendSlot / navigationSlot contract is preserved VERBATIM and export membership is still decided by placement, so nothing moved into or out of the exported composition."
    - "Transition-readiness (c): the data asset base path is a parameter living in exactly one config home; the two historicalValidation safety predicates are EXEMPTED rather than parameterised, because parameterising them would quietly widen the approval chain."
    - "Transition-readiness (b): exactly one production file under src/ touches localStorage, and that file is src/utils/storage.ts — everything else goes through the StorageAdapter interface."
    - "Transition-readiness (e): no file under src/ writes a class to document.documentElement; the .dark class is written to the editor mount root only, so a future host controls it with no OS listener to fight."
    - "Live Invariant 4 is untouched: exactly one MapCanvasHandle and one svg.map-canvas survive the restructure, guarded by data-camera-owner-sentinel, with the camera lease released from the outermost finally on every path."
  artifacts:
    - src/components/editor/MapEditor.tsx
    - src/components/editor/MapEditor.test.tsx
    - src/config/editorConfig.ts
    - src/config/editorConfig.test.ts
  key_links:
    - "MapEditor is the ONE component a host mounts; every later plan's surface hangs off it, and the props boundary is what makes the future Themely transition a mount rather than a rewrite."
    - "editorConfig.ts is the single home for the /data/ base path; useGeoData.ts and snapshots.ts read it, and historicalValidation.ts's two predicates deliberately do NOT."
    - "The mount root carries .dark (03-06 writes it) — if it were on document.documentElement a host could not override it."
  backstops:
    - statement: "Retiring the inspector column as a container does not orphan any tool: every control it held has a named destination in 03-06 or 03-07, and the composed DOM keeps exactly one of each singleton action."
      verification: backstop
---

<objective>
Assemble the editor into one mountable component behind an explicit props boundary, dissolve the
app bar and the inspector column as *containers*, and turn the four transition-readiness points
into RED-provable gates.

Purpose: the ROADMAP makes transition-readiness *"binding for every Phase 3 plan"*, and this is
the plan where it stops being prose. Three of the four points are already largely discharged in
the codebase — the `StorageAdapter` exists with exactly one `localStorage` site, and the
hard-coded `/data/` literals number three. What is missing is the gate that keeps them true.

Output: `src/components/editor/MapEditor.tsx` with an exported props interface,
`src/config/editorConfig.ts` as the single asset-path home, and four grep-shaped gates each proven
RED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/export.md
@.planning/coding-rules/storage.md
@.planning/coding-rules/data.md
@Design.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-UI-SPEC.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-04-SUMMARY.md

Read narrowly:
- `03-RESEARCH.md` § Pattern 9 in full (lines ~1012-1121) — the four gates with their complete
  file:line inventories and RED probes
- `03-RESEARCH.md` § Recommended Project Structure
- `03-CONTEXT.md` § Integration Points
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| `src/components/editor/MapEditor.tsx` | component | the ONE component a host mounts; exports exactly one component and its props interface |
| `src/components/editor/MapEditor.test.tsx` | test | boundary gate — text scan + `renderToStaticMarkup` single-root assertion |
| `src/config/editorConfig.ts` | module | `DATA_BASE_PATH` and the derived asset URLs; the single `/data/` home |
| `src/config/editorConfig.test.ts` | test | the `/data/` grep gate with its two named predicate exemptions |
| storage-site gate | test code | added to `editorConfig.test.ts` or a sibling — exactly one `localStorage` file under `src/` |
| documentElement gate | test code | no `documentElement.classList` under `src/` |
| **retired as containers** | removal | the top app bar (`.app-bar`) and the right inspector column; their tools relocate in 03-06/03-07 |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: One mountable editor behind an explicit props boundary (transition-readiness a, D-11)</name>
  <files>src/components/editor/MapEditor.tsx, src/components/editor/MapEditor.test.tsx, src/App.tsx, src/main.tsx, src/styles/App.css</files>
  <read_first>
    - `src/App.tsx` (full — it is the composition root and owns durable state; this task moves the *shell*, not the state ownership)
    - `src/App.test.tsx` lines 200-300 — the markup-string assertion idiom (`:274-276`) this repo already uses for component-shaped code in the `node` environment
    - `src/main.tsx` (full)
    - `src/styles/App.css` (336 lines) — the `.app`, `.app-bar`, `.workspace*` rules
    - `src/components/AppHeader.tsx`, `src/components/CompositionBar.tsx` — what the app bar currently holds; note that `CompositionBar`'s live region is rehomed in **03-07**, not here
    - `src/styles/editor.css` — the `.map-editor` grid landed in 03-03
    - `03-RESEARCH.md` § Pattern 9(a)
  </read_first>
  <action>
Wire one path end to end: `main.tsx` mounts exactly one `MapEditor`, `MapEditor` renders the
03-03 shell (`.map-editor` grid → rail track → panel track → canvas region → export frame →
`MapWorkspace` → `svg.map-canvas`), and the export still produces a 1080×1080 PNG. Production
code, not a prototype — 03-06 and 03-07 fill the rail and panel, they do not replace this.

**A. Create `src/components/editor/MapEditor.tsx`.** It exports **exactly one** component and its
**exported** props interface. The props boundary is the point: everything the editor needs from a
host arrives as a prop, and the editor never reaches outside its own mount point for chrome.

The props boundary must at minimum carry: the storage adapter (or a factory for it), the data
asset base path (from Task 2's config home), and the initial theme mode. Do **not** add auth or
entitlement awareness — that belongs to the future host and the ROADMAP puts it explicitly out of
scope.

**B. The mount root is the editor root.** `.map-editor` is full-bleed, opaque, `100dvh`, and is
the **only element the host must place**. It is where `.dark` is written (03-06 writes it) —
**never `document.documentElement`**, or a host cannot override it. `html` / `body` were reduced
to layout-only rules with no themed background in 03-03; confirm that still holds after this
restructure.

**C. Dissolve the app bar as a container (D-11).** Remove `.app-bar` and the top chrome. **The
tools it held are relocated, not deleted** — `Export PNG`, `Undo`/`Redo`, and the composition
identity all have named destinations in 03-06, and the period surface plus its live region have a
named destination in 03-07. Do NOT delete a control here whose new home has not been built yet;
if a control would otherwise be orphaned between plans, keep it rendered in a temporary position
and record which plan takes it. Losing a control between two plans is how the deleted-inspector-
landmark defect happened before.

Retire the right inspector column as a *container* the same way: its tools move, they do not die.

**D. Live Invariant 4 must survive the restructure.** Exactly one `MapCanvasHandle` and one
`svg.map-canvas` across the 1200px responsive transition, guarded by
`data-camera-owner-sentinel`. Save reads the camera live and non-locking; export takes an
idempotent `CameraFreezeLease` released from the **outermost `finally` on every path**. Do not
duplicate the canvas for a narrow layout and do not introduce a second handle owner.

**E. `MapEditor.test.tsx` — the boundary gate** (`node` env, no DOM):
- a **text scan** of `MapEditor.tsx` asserting it exports exactly one component, that its props
  interface is exported, and that the file contains **no** `document.` or `window.` reference
  outside a narrowly allowed, explicitly listed set;
- a **structural** assertion: `renderToStaticMarkup(<MapEditor …/>)` produces markup with exactly
  one root element. `App.test.tsx:274-276` is the precedent for asserting on markup strings here.

*RED probe (mandatory task step):* copy `MapEditor.tsx` to the scratchpad, add
`document.body.classList.add('x')` at module scope, run
`npx vitest run src/components/editor/MapEditor.test.tsx`, OBSERVE RED, capture the output,
restore by copying the scratchpad file back, re-run green. **Never `git checkout --` a file with
uncommitted work.**
  </action>
  <verify>
    <automated>npx vitest run src/components/editor/MapEditor.test.tsx</automated>
    <automated>grep -rn "documentElement.classList" src/ && { echo "FAIL: documentElement class write"; exit 1; } || echo NO_DOCUMENT_ELEMENT_WRITE</automated>
    <automated>npm run lint && npm test && npm run build</automated>
    <automated>npx playwright test tests/e2e/export.spec.ts tests/e2e/shell.spec.ts --project=chrome --reporter=line</automated>
  </verify>
  <done>
    `main.tsx` mounts one `MapEditor`; `MapEditor` exports one component and an exported props
    interface, touches no `document`/`window` chrome outside its allowed list, and renders a
    single root; the app bar and inspector column are gone as containers with every tool's
    destination named; export is still 1080×1080 and the camera sentinel still guards exactly one
    handle.
  </done>
  <acceptance_criteria>
    - `src/components/editor/MapEditor.tsx` exports exactly one component and one exported props
      interface.
    - The text gate's allowed `document`/`window` list is explicit and short; anything outside it
      fails.
    - `renderToStaticMarkup` produces exactly one root element.
    - `grep -rn "documentElement.classList" src/` returns nothing.
    - No control that existed before this plan is unreachable after it; the SUMMARY lists each
      relocated control and the plan that takes it.
    - `tests/e2e/export.spec.ts` and `tests/e2e/shell.spec.ts` pass in Chrome.
    - The SUMMARY carries the captured RED output from the module-scope `document.body` probe.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: One `/data/` config home, with the two safety predicates deliberately exempted (transition-readiness c)</name>
  <files>src/config/editorConfig.ts, src/config/editorConfig.test.ts, src/hooks/useGeoData.ts, src/constants/snapshots.ts, .planning/coding-rules/data.md</files>
  <read_first>
    - `src/hooks/useGeoData.ts` lines 1-40 — `WORLD_MANIFEST_URL` and `WORLD_DATA_URL`
    - `src/constants/snapshots.ts` (full) — `SNAPSHOT_MANIFEST_URL` at `:7` and the five-entry `SNAPSHOT_CATALOG` at `:10-19`
    - `src/utils/historicalValidation.ts` lines 1090-1100 and 1185-1195 — the two predicates that must NOT be parameterised
    - `03-RESEARCH.md` § Pattern 9(c) — the complete literal inventory, including the test/fixture and build-script lists that are deliberately out of scope
    - `.planning/coding-rules/data.md` — where the exemption reason is recorded
  </read_first>
  <action>
**A. Create `src/config/editorConfig.ts`** as the single home for the data asset base path. Export
the base path and derive the three asset URLs from it. Re-point `useGeoData.ts:11-12` and
`snapshots.ts:7` at the derived values. The base path arrives through `MapEditor`'s props boundary
with the current value as its default, so the standalone app is unchanged and a host can override
it.

**B. Do NOT parameterise the two `historicalValidation.ts` literals — this is a trap and it is
recorded as one.** `historicalValidation.ts:1098` (`!input.assetPath.startsWith('/data/')`) and
`:1190` (`entry.assetPath.startsWith('/data/snapshots/')`) are **safety predicates on
manifest-declared asset paths**, not fetch URLs. Parameterising them alongside the fetch paths
would let a host-configured base path widen what counts as an acceptable asset path — a loosening
of the approval chain dressed up as a refactor. Leave both as literals and **exempt them
explicitly in the gate, by file and line, with the reason inline**.

If the base path ever becomes genuinely configurable, the predicate must validate against the
*configured* base, not against a wildcard. Record that condition in
`.planning/coding-rules/data.md` in this same commit (documentation-as-you-build: the rule lands
with the code, not in a later catch-up plan). Bump its `Last updated` line and keep at most two
entries.

**C. Write `src/config/editorConfig.test.ts` — the grep gate.** It greps `src/**/*.{ts,tsx}`
excluding `*.test.*` for the `/data/` literal and asserts the only matches are
`src/config/editorConfig.ts` plus the two named `historicalValidation.ts` predicate lines. The
exemption is a **closed, line-cited set** — a third match anywhere fails.

Do **not** chase the test/fixture literals (`worldDataAsset.test.ts`,
`historicalValidation.test.ts`, `snapshotScene.test.ts`, `periods.test.ts`,
`MapWorkspace.test.tsx`, the e2e specs and `historicalFixture.ts`) or the build scripts
(`prepareWorldData.mjs`, `prepareGeoData.mjs`, `verifyPhase2ExactCommit.mjs`) — they are out of
scope for this gate and `03-RESEARCH.md` lists them precisely so nobody wastes a pass on them.

*RED probe (mandatory task step):* copy `useGeoData.ts` to the scratchpad, re-add the literal
`/data/world-modern.geojson` to it, run `npx vitest run src/config/editorConfig.test.ts`, OBSERVE
RED, capture the output, restore by copying back, re-run green.

**D. `SNAPSHOT_CATALOG` is untouched.** Moving `SNAPSHOT_MANIFEST_URL` behind the config home does
not change catalog or approval semantics. `SNAPSHOT_CATALOG` remains a **five-entry label
registry** and reachability remains decided by the approved manifest at
`public/data/snapshots/index.json`, which holds exactly one entry. Do not promote geometry, do not
make a deferred snapshot nameable or reachable, and do not widen what the manifest validator
admits. The period surface's rendering rule (render `resolvePeriodOptions`, never the constant) is
03-07's gate; this task must simply not break it.
  </action>
  <verify>
    <automated>npx vitest run src/config/editorConfig.test.ts</automated>
    <automated>node -e "const {execSync}=require('child_process');const out=execSync(\"grep -rn \\\"'/data/\\\" src --include='*.ts' --include='*.tsx' | grep -v '\\\\.test\\\\.' || true\").toString().trim();const lines=out?out.split('\n'):[];const ok=lines.every(l=>l.startsWith('src/config/editorConfig.ts')||l.startsWith('src/utils/historicalValidation.ts'));if(!ok){console.error('unexpected /data/ literal:\n'+out);process.exit(1)};console.log('DATA_PATH_HOME_OK',lines.length)"</automated>
    <automated>npx vitest run src/utils/historicalValidation.test.ts src/utils/periods.test.ts src/utils/snapshotScene.test.ts</automated>
    <automated>npm run data:world:check</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    `src/config/editorConfig.ts` is the only production fetch-path home for `/data/`; the two
    `historicalValidation.ts` predicates remain literals and are exempted by file and line with an
    inline reason; `data.md` records the condition under which the predicate would have to change;
    the world-asset hash check still passes and no catalog or approval semantics moved.
  </done>
  <acceptance_criteria>
    - The `/data/` grep over non-test `src/` returns matches only in `editorConfig.ts` and the two
      cited `historicalValidation.ts` lines.
    - The gate's exemption list is closed and cites lines, not just a filename.
    - `src/constants/snapshots.ts` still exports a five-entry `SNAPSHOT_CATALOG` with unchanged
      ids and labels.
    - `npm run data:world:check` passes.
    - `.planning/coding-rules/data.md` is updated in the same commit with at most two
      `Last updated` entries.
    - The SUMMARY carries the captured RED output from re-adding the world-data literal.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: The storage-site gate and the slot-contract regression guard (transition-readiness b, D-24)</name>
  <files>src/config/editorConfig.test.ts, src/components/MapWorkspace.tsx, src/components/MapWorkspace.test.tsx</files>
  <read_first>
    - `src/utils/storage.ts` lines 40-150 — the `StorageAdapter` interface (`:71-88`), the bounded V2 limits (`:45-47`), and `getDefaultStorage` (`:140-146`) with the single `localStorage` reference at `:142`
    - `src/constants/config.ts` lines 1-10 — `ONBOARDING_DISMISSED_KEY`, the precedent for a small non-composition key
    - `src/components/MapWorkspace.tsx` (full) — the typed slots
    - `.planning/coding-rules/storage.md`
    - `03-RESEARCH.md` § Pattern 9(b) — the complete `localStorage` inventory
  </read_first>
  <action>
**A. The storage-site gate.** Add an assertion that `window.localStorage` and bare `localStorage.`
appear in **exactly one** file under `src/`, and that file is `src/utils/storage.ts`. Test
injection sites (`App.test.tsx:212`, `ErrorBoundary.test.tsx:59`) and the browser-context
`page.evaluate` sites under `tests/e2e/` are out of scope — they are test setup, not app code, and
`03-RESEARCH.md` enumerates them so nobody widens the gate to chase them.

*RED probe (mandatory task step):* copy a component file to the scratchpad, add a raw
`localStorage.setItem(...)` to it, run the gate, OBSERVE RED, capture output, restore by copying
back, re-run green.

**Record the shape 03-06 and 03-07 must follow**, in `.planning/coding-rules/storage.md` if it is
not already there: the two new keys this phase introduces — D-18's last-open tool and D-30's
theme — go through the `StorageAdapter`, follow the `ONBOARDING_DISMISSED_KEY` precedent of a
small **separate** key rather than widening the composition record, and respect the bounded V2
contract (`MAX_STORAGE_SERIALIZED_LENGTH`, `MAX_STORAGE_JSON_DEPTH`, `MAX_STORAGE_JSON_NODES`,
all checked **before** `JSON.parse`). Both keys must be **absent-tolerant**: a returning creator
has neither, and the defaults are already decided — panel **closed** (D-18) and **light** (D-30).

**B. The slot-contract regression guard (D-24).** Extend `MapWorkspace.test.tsx` so the
restructure cannot quietly move something into or out of the exported composition:
- `legendSlot` renders **inside** `svg.map-canvas` under `g[data-layer="legend"]`;
- `navigationSlot` renders **outside** `svg.map-canvas`, as a sibling of
  `div.map-export-source`;
- the camera layer precedes the legend layer in child order (the shape
  `isPreservedComposition` depends on);
- `.map-frame` is still a sibling, still `data-editor-only="true"`, still not a slot.

**Placement decides export membership. `data-editor-only` is the second line of defence, not the
first.** Moving tools into the rail must not move anything into or out of the exported
composition — with the single deliberate exception of D-25's legend typography, which is 03-11's
work and not this plan's.

*RED probe (mandatory task step):* move the `navigationSlot` render inside `svg.map-canvas`,
observe the guard go RED, capture, restore by copying back.

**C. Confirm the export refusals still fire.** Run the export e2e slice and confirm the three
refusal reasons are intact: disconnected source, multi-SVG, and legend-outside-the-canvas
(`invalid-composition`, never a silently legend-less PNG). This plan must not change
`src/utils/export.ts` — 03-11 owns that file. If a change here appears to require an `export.ts`
edit, stop and record it: it means the restructure moved something across the composition
boundary.
  </action>
  <verify>
    <automated>npx vitest run src/config/editorConfig.test.ts src/components/MapWorkspace.test.tsx src/utils/storage.test.ts</automated>
    <automated>node -e "const {execSync}=require('child_process');const out=execSync(\"grep -rln 'localStorage' src --include='*.ts' --include='*.tsx' | grep -v '\\\\.test\\\\.' || true\").toString().trim();const files=out?out.split('\n'):[];if(files.length!==1||files[0]!=='src/utils/storage.ts'){console.error('localStorage sites under src/ (expected exactly src/utils/storage.ts):\n'+out);process.exit(1)};console.log('ONE_STORAGE_SITE_OK')"</automated>
    <automated>git diff --quiet HEAD -- src/utils/export.ts && echo EXPORT_TS_UNTOUCHED</automated>
    <automated>npx playwright test tests/e2e/export.spec.ts --project=chrome --reporter=line</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    Exactly one production file under `src/` touches `localStorage` and it is
    `src/utils/storage.ts`; `MapWorkspace.test.tsx` guards slot placement, layer order, and the
    frame's sibling status; `src/utils/export.ts` is byte-unchanged by this plan; the export e2e
    slice with its three refusal reasons is green.
  </done>
  <acceptance_criteria>
    - The `localStorage` file list under non-test `src/` has exactly one entry:
      `src/utils/storage.ts`.
    - `MapWorkspace.test.tsx` asserts legend-inside-svg, navigation-outside-svg, camera-before-
      legend order, and the frame's sibling + `data-editor-only` status.
    - `git diff HEAD -- src/utils/export.ts` is empty.
    - `tests/e2e/export.spec.ts` passes in Chrome with all three refusal reasons exercised.
    - `.planning/coding-rules/storage.md` records the two new-key requirements (adapter, separate
      key, bounded V2, absent-tolerant defaults) with at most two `Last updated` entries.
    - The SUMMARY carries two captured RED outputs (raw `localStorage`, slot moved inside the SVG).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host application → editor mount root | The props boundary; everything the editor needs crosses here and nothing else |
| manifest-declared asset path → the approval chain | `historicalValidation`'s two predicates guard what counts as an acceptable asset path |
| chrome DOM → exported composition | Placement in `MapWorkspace`'s typed slots is the decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-16 | Elevation of privilege | parameterising `historicalValidation`'s `/data/` predicates | critical | mitigate | Both left as literals and exempted by file AND line with an inline reason; the condition under which they would have to change is recorded in `coding-rules/data.md`; the gate's exemption set is closed so a third match fails |
| T-03-17 | Tampering | a raw `localStorage` write bypassing the bounded V2 contract | high | mitigate | Grep gate asserting exactly one production `localStorage` file; new D-18/D-30 keys follow the `ONBOARDING_DISMISSED_KEY` precedent through the adapter; RED-proven by adding a raw write |
| T-03-18 | Tampering | a control moved into or out of the exported composition during the restructure | high | mitigate | `MapWorkspace.test.tsx` guards slot placement and layer order; `export.ts` asserted byte-unchanged; the export e2e slice exercises all three refusal reasons |
| T-03-19 | Tampering | the editor writing a theme class to `document.documentElement` | medium | mitigate | Grep gate for `documentElement.classList` under `src/`; the class is written to the mount root only, so a host can override it |
| T-03-20 | Denial of service | losing a control between two plans (the deleted-inspector-landmark class) | medium | mitigate | Task 1 forbids deleting a control whose new home is not built; the SUMMARY lists each relocated control and its receiving plan |
</threat_model>

<verification>
```bash
npm run lint
npm test
npm run build
npx vitest run src/components/editor/ src/config/
npx playwright test tests/e2e/export.spec.ts tests/e2e/shell.spec.ts tests/e2e/transactions.spec.ts --project=chrome
npm run data:world:check
grep -rn "documentElement.classList" src/ ; echo "expect: no output"
git diff --stat HEAD -- src/utils/export.ts ; echo "expect: no output"
```

Chrome only. Edge is not installed (D-33).
</verification>

<success_criteria>
- [ ] One `MapEditor` component with an exported props interface, no stray `document`/`window` chrome, one root element (transition-readiness a, RED-proven)
- [ ] The app bar and inspector column are gone as containers, and every control they held has a named destination — none orphaned (D-11)
- [ ] `legendSlot` / `navigationSlot` are preserved verbatim; nothing moved into or out of the exported composition (D-24, RED-proven)
- [ ] `src/config/editorConfig.ts` is the single production `/data/` home; the two `historicalValidation` predicates are exempted by line with the reason recorded in `data.md` (transition-readiness c, RED-proven)
- [ ] Exactly one production `localStorage` site under `src/` (transition-readiness b, RED-proven)
- [ ] No `documentElement.classList` under `src/` (transition-readiness e)
- [ ] `src/utils/export.ts` is byte-unchanged by this plan
- [ ] Live Invariant 4 holds: one `MapCanvasHandle`, one `svg.map-canvas`, sentinel intact, lease released from the outermost `finally`
- [ ] `SNAPSHOT_CATALOG` and the approval chain are untouched; `npm run data:world:check` passes
</success_criteria>

<output>
Create `.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-05-SUMMARY.md` when done, carrying a
`Relocated controls → receiving plan` table and a `RED probes (4, with output)` section.

**Do not run `state.advance-plan`, `state.update-progress`, or `roadmap.update-plan-progress`.**
Edit `.planning/STATE.md` and `.planning/ROADMAP.md` by hand.
</output>
