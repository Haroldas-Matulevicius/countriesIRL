---
phase: 03-clean-ui-overhaul-1-1-5-weeks
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - src/styles/theme.css
  - src/styles/App.css
  - src/styles/Controls.css
  - src/styles/MapCanvas.css
  - src/styles/editor.css
  - src/styles/uiContract.test.ts
  - src/styles/themeTokens.test.ts
  - src/styles/phase2CssContract.test.ts
autonomous: true
requirements:
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - A-01
  - A-02
  - A-03
  - A-04
  - A-05
  - A-06
  - A-08
  - A-09
  - A-17
  - A-19
  - A-26

estimate:
  tokens: 180000
  raw_tokens: 90000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "D-04 / D-03: the Themely cool palette is declared verbatim under the --themely-* namespace, no value adjusted, and every custom property in theme.css matches an explicit namespace allowlist so an un-namespaced --accent cannot reappear."
    - "D-08 / assertion 1: the dark palette flips from a `.dark` class and NO stylesheet rule anywhere carries an OS colour-scheme preference in its at-rule conditions — not even to seed a first-run default (D-30)."
    - "D-06 / D-10 / assertion 2: every retired token is DELETED, not aliased, so a stale reference fails loudly at the contract test instead of silently resolving."
    - "Assertion 3: every --themely-* token in :root has a counterpart in .dark and vice versa, EXCEPT the fixed trio (--themely-on-accent, -media-backdrop, -on-media) which must be identical in both."
    - "Live Invariant 9 / assertion 4: no export token is declared outside the unconditioned :root — extended to .dark — or the exported PNG starts following the viewer's theme, a defect no rendering test catches."
    - "Assertion 26: --accent-fill and --accent-fill-hover are declared exactly once in the unconditioned :root and appear in no .dark block, so the Export PNG label stays at 4.70:1 white-on-accent in BOTH modes rather than dropping to 3.02:1 in dark."
    - "Assertion 19: the contrast matrix resolves through the real cascade for every (mode x preference) combination, asserts its own row count, and every text-on-surface pair meets AA in both modes with NO enumerated exceptions."
    - "Assertion 5 / 6: every export token and every motion token is declared exactly once and has a live consumer — a declared token with no consumer makes its assertion theatre."
    - "Assertion 8: no hex or rgba literal appears in any component .tsx, with a CLOSED exemption list containing exactly LegendOverlay.tsx and its stated export-fixed reason."
    - "Assertion 9: the type-role consumer exemption is a closed set of exactly --text-display and --text-stat; adding a third fails."
    - "Assertion 17: backdrop-filter appears nowhere, and no filter / box-shadow / text-shadow / mix-blend-mode / mask / clip-path reaches exported content, with EXPORT_CONTENT_PATTERN still bound back to MapCanvas.tsx source."
    - "D-07 / D-09: radii follow Themely (8/14/18/9999 plus --radius-row 10px) and the ten type roles bundle size, line-height, weight, and tracking, with tabular-nums on any numeric column."
  artifacts:
    - src/styles/theme.css
    - src/styles/themeTokens.test.ts
  key_links:
    - "theme.css :root and .dark are the ONLY token source; every restyled surface in 03-06 through 03-10 resolves through them, so a wrong value here propagates everywhere silently."
    - "The mode-invariant export set (--map-*, --tooltip-*, --swatch-border) is the export firewall; one of them redefined inside .dark makes the PNG follow the viewer's theme."
    - "Deleting --shadow-inspector / --shadow-navigation / --glass-* is what makes their 64 stale references fail LOUDLY in 03-10's sweep instead of resolving to an alias."
  backstops:
    - statement: "Under prefers-contrast: more and forced-colors, every --themely-* override is declared for BOTH :root and .dark inside the same at-rule, so a literal written for one mode cannot silently win in the other — the defect that once painted a light bar under light text at 1.0:1."
      verification: backstop
---

<objective>
Replace the token system. Land the Themely cool palette verbatim in `:root` and the "Lights Out"
palette in `.dark`, delete every retired token rather than aliasing it, move the dark flip off the
OS preference and onto a class, and land the ten contract assertions that make all of the above
provable.

Purpose: this is the plan where "delete, don't alias" earns its keep. An alias would let 64 stale
`--glass-*` references and every `--accent` consumer keep working while looking migrated. Deleting
them makes each one fail at the contract test, which is the only way a 349-line token file gets
honestly replaced.

Output: a rewritten `src/styles/theme.css`, ten new assertions in `src/styles/uiContract.test.ts`,
a rewritten `themeTokens.test.ts`, and the retirement of `phase2CssContract.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/export.md
@Design.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-UI-SPEC.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-03-SUMMARY.md

Read narrowly:
- `03-RESEARCH.md` § Current-State Inventory (lines ~1122-1246) — the complete retired-token
  declaration/consumer tables and the complete OS-colour-preference inventory
- `03-RESEARCH.md` § Pattern 7, § Pattern 8, § P-2, § P-3
- `/Users/matul/claudeprojects/themely/src/app/globals.css` — `:root` 173-258, `.dark` 319-357
  (authoritative upstream)
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| rewritten `src/styles/theme.css` | stylesheet | `--themely-*` light `:root` + `.dark`; the mode-invariant export set; `--text-*` role bundles; `--radius-*`; `--hairline` / `--popover-shadow` / `--dialog-shadow`; `--motion-*` |
| `--accent-fill`, `--accent-fill-hover` | tokens | NEW, mode-invariant, `#0071e3` / `#005db8` |
| `--radius-row` | token | NEW, 10px, the rail row radius |
| rewritten `src/styles/themeTokens.test.ts` | test | exact palette values, light and dark |
| assertions 1-9, 17, 19, 26 | test code | added to `src/styles/uiContract.test.ts` |
| **deleted** `src/styles/phase2CssContract.test.ts` | removal | superseded; its infrastructure was ported in 03-03 |
| **deleted** tokens | removal | `--accent*`, `--surface-accent-tint`, `--text-primary/secondary/muted`, `--border-strong`, `--surface-*`, `--glass-*` (64 refs), `--shadow-inspector`, `--shadow-navigation`, `--modal-shadow`, `--toast-shadow`, `--font-label/body/heading/display`, `--weight-regular/semibold`, `--radius-large`, `--map-shadow`, `--mixed-color-light/dark`, `--active-check-border/surface/text` |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Land the palette — Themely cool in `:root`, Lights Out in `.dark`, class-driven</name>
  <files>src/styles/theme.css, src/styles/themeTokens.test.ts</files>
  <read_first>
    - `src/styles/theme.css` (full, 349 lines — this is a rewrite, so all of it matters)
    - `src/styles/themeTokens.test.ts` (full)
    - `Design.md` § the token tables written by 03-02
    - `03-UI-SPEC.md` § Design Tokens (lines ~90-415) in full
    - `03-RESEARCH.md` § Current-State Inventory → the OS-colour-preference inventory table
      (`theme.css:111`, `:144`, `:196`, plus comments at `:13`, `:18`, `:159`)
  </read_first>
  <action>
Rewrite the token layer top to bottom. One path, end to end: a value in `Design.md` → a
declaration in `theme.css` → a passing exact-value assertion in `themeTokens.test.ts` → a visibly
correct surface in `npm run dev` under both modes.

**A. Light `:root`.** Declare the thirteen `--themely-*` colour tokens with the exact values from
`Design.md` / `03-UI-SPEC.md` § Colour — light. **No value may be adjusted.**

**B. Dark `.dark`.** Declare the eleven flipping tokens with the "Lights Out" values. The fixed
trio (`--themely-on-accent`, `--themely-media-backdrop`, `--themely-on-media`) must be
**identical** in both — `#ffffff`, `#000000`, `#ffffff`. Add `color-scheme: dark` to `.dark` so
native controls and scrollbars follow.

**C. The flip is a class, and the OS preference leaves the dark path entirely (D-08, D-30).**
Delete the existing dark palette block at `theme.css:111` and the nested dark-glass block at
`:144`. Rewrite `theme.css:196` as `.dark` inside `@media (prefers-contrast: more)` rather than
as a compound OS-preference condition. Rewrite the explanatory comments at `:13`, `:18`, `:159`.

<!-- planner-discipline-allow: prefers-color-scheme -->
**Comment discipline, and it is load-bearing here.** Assertion 1 negative-greps stylesheets for
the OS colour-scheme at-rule condition. Do **not** write that literal into any comment in
`theme.css` explaining its absence — a header comment naming the forbidden pattern makes the gate
self-invalidating, and this repo has already shipped three gates that could not fail. Describe the
rule in `Design.md` instead, where nothing greps. Where the assertion counts occurrences, filter
comment lines before counting rather than counting raw.

`prefers-reduced-motion`, `prefers-reduced-transparency`, `prefers-contrast`, and `forced-colors`
remain **legitimate and must not be swept** — D-30 names the OS colour-scheme query only.

**D. Preference blocks under a class-based dark mode.** The Phase 2 rule *"a preference media
query must define BOTH schemes"* survives, restated in class terms: any `prefers-contrast` or
`forced-colors` override of a `--themely-*` token must be declared for **both** `:root` and
`.dark` inside the same at-rule. The preference block is authored after the palette at equal
specificity, so a literal written for one mode silently wins in the other — this is the defect
that once painted a light bar under light text at **1.0:1** for the user who asked for *more*
contrast.

**E. The mode-invariant (fixed) export set — the export firewall.** Declare `--map-surface`,
`--map-fill-default`, `--map-border-default`, `--map-border-hover`, `--map-border-selected`,
`--map-border-focus`, `--map-fixed-text`, `--map-skeleton-fill`, `--map-skeleton-stroke`,
`--swatch-border`, `--tooltip-surface`, `--tooltip-text`, `--tooltip-border`, `--tooltip-shadow`,
`--map-frame-edge`, `--map-frame-scrim` **exactly once, in the unconditioned `:root`**, and in no
`.dark` block, no media query, and no `@supports` block. Live Invariant 9 outranks Themely's
"tokens flip themselves" rule for this family.

Two deliberate changes inside the fixed set, both already decided:
- `--map-border-focus` re-tones from the retired teal to Apple Blue's **light** value `#0071e3`
  and stays fixed in both modes. Leaving the teal would keep a second saturated hue alive, which
  D-05 forbids. This is safe for export because `sanitizeExportClone` strips `focused` classes and
  `data-focused` before capture, so the focus stroke never reaches a PNG. It stays dashed at 3px.
- `--map-fixed-text` **stays `#111827`, unchanged**. This resolves the Claude's-Discretion
  question: re-toning it changes exported pixels for every existing saved composition, and D-25
  already spends the phase's one deliberate export-pixel change on the legend typeface. Phase 4
  owns the map's visual language.

The tooltip four re-tone per D-22 (`#061b31` surface, `#ffffff` text,
`rgba(255,255,255,0.14)` border, popover-tier shadow) but stay **fixed**, because consuming the
flipping token would produce a light chip with white text in dark mode.

**F. `--accent-fill` / `--accent-fill-hover` — new, and mode-invariant for a DIFFERENT reason.**
`#0071e3` and `#005db8`, declared exactly once in the unconditioned `:root`, in no `.dark` block.
This is accessibility, not export independence: white on `#0071e3` is **4.70:1** (AA), while the
flipping `--themely-apple-blue` would give white on `#2997ff` = **3.02:1** in dark, and its hover
pair `#1a7fd4` = **4.18:1** — both below AA. **Do not "simplify" `--accent-fill` back onto the
flipping token.** It looks tidier and silently reintroduces a failing label that no rendering test
catches. D-13 still holds — Export is still a filled Apple Blue primary and still the rail's one
accent surface. D-08 is untouched — `--themely-apple-blue` still flips for every other consumer.

**G. Delete, never alias.** Remove every retired token *declaration* named in
`03-RESEARCH.md` § Current-State Inventory, plus `--mixed-color-light` / `--mixed-color-dark` /
`--active-check-border` / `--active-check-surface` / `--active-check-text` (chrome, not export —
their fixed values go invisible under `.dark`) and `--map-shadow` (a full-bleed canvas has nothing
to elevate). Do not leave a compatibility alias for any of them. Their consumers are migrated in
Task 2.

**H. Typography, radii, elevation, motion.** Land the ten `--text-*` role bundles (size,
line-height, weight, tracking each), the same-origin `@font-face` for the vendored Inter with the
declared fallback stack `system-ui, -apple-system, "Segoe UI", sans-serif`, `tabular-nums` for
numeric columns, the four radii plus `--radius-row: 10px`, `--hairline` / `--popover-shadow` /
`--dialog-shadow` (with `.dark` swapping `--popover-shadow` to
`0 4px 12px -2px rgba(0, 0, 0, 0.45)`), and the six `--motion-*` tokens reconciled with 03-02's TS
mirror. `@media (prefers-reduced-motion: reduce)` zeroes `--motion-duration-fast`, `-base`,
`-slow`, and `--motion-scene`.

**I. Rewrite `themeTokens.test.ts`** to assert the exact new values for light and dark, including
the fixed-trio identity and the `--accent-fill` pair. *RED probe:* re-add the retired teal
`--accent: #0f766e` to `:root` and watch it fail.
  </action>
  <verify>
    <automated>npx vitest run src/styles/themeTokens.test.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/styles/theme.css','utf8').split('\n').filter(l=>!/^\s*(\/\*|\*)/.test(l)).join('\n');if(/prefers-color-scheme/.test(s)){console.error('OS colour-scheme query survives in theme.css');process.exit(1)};console.log('CLASS_FLIP_OK')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/styles/theme.css','utf8');const dark=(s.match(/\.dark\s*\{[\s\S]*?\}/g)||[]).join('\n');for(const t of ['--map-surface','--map-fill-default','--map-border-default','--map-border-focus','--map-fixed-text','--swatch-border','--tooltip-surface','--tooltip-text','--map-frame-edge','--map-frame-scrim','--accent-fill','--accent-fill-hover']){if(dark.includes(t)){console.error('mode-invariant token redeclared in .dark:',t);process.exit(1)}};console.log('EXPORT_FIREWALL_OK')"</automated>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>
    `theme.css` declares the Themely cool palette in `:root` and Lights Out in `.dark`, driven by
    a class with no OS colour-scheme query anywhere; the sixteen mode-invariant export tokens plus
    the two accent-fill tokens are declared once in `:root` and in no `.dark` block; every retired
    token declaration is deleted with no alias; `themeTokens.test.ts` asserts the exact values and
    RED-fails when the retired teal is re-added.
  </done>
  <acceptance_criteria>
    - After stripping comment lines, `theme.css` contains zero occurrences of the OS colour-scheme
      at-rule condition.
    - `prefers-reduced-motion`, `prefers-reduced-transparency`, `prefers-contrast`, and
      `forced-colors` are all still present.
    - No token in the mode-invariant list appears inside any `.dark` block.
    - `--map-border-focus` resolves to `#0071e3`; `--map-fixed-text` is still `#111827`.
    - `--accent-fill` is `#0071e3` and `--accent-fill-hover` is `#005db8`, each declared exactly
      once.
    - No comment in `theme.css` contains a literal that assertion 1 or 2 negative-greps for.
    - The SUMMARY carries the captured RED output from re-adding the retired teal.
  </acceptance_criteria>
  <reversibility rating="costly">D-08. Undoing the class-based dark mode means re-deriving a second palette and re-running every contrast check, and the media-query→class switch changes observable behaviour for users who relied on an OS-level dark preference.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Migrate every retired-token consumer, then land assertions 1-6 and 26</name>
  <files>src/styles/App.css, src/styles/Controls.css, src/styles/MapCanvas.css, src/styles/editor.css, src/styles/uiContract.test.ts</files>
  <read_first>
    - `03-RESEARCH.md` § Current-State Inventory → the per-token declaration+consumer tables (every file:line is enumerated there; use them as the worklist rather than re-grepping blind)
    - `src/styles/App.css` (336 lines), `src/styles/MapCanvas.css`, and the `Controls.css` regions the inventory names
    - `Design.md` § the accent budget and § elevation
  </read_first>
  <action>
Migrate consumers, then gate them.

**A. Work the enumerated consumer lists.** `03-RESEARCH.md` § Current-State Inventory lists every
declaration and every consumer for `--accent*`, `--text-primary/secondary/muted`,
`--border-strong`, `--shadow-inspector`, `--shadow-navigation`, the 64-reference `--glass-*`
family, and `--font-label/body/heading/display` + `--weight-*`. Use those tables as the worklist.
Map each consumer onto its replacement per `Design.md`:

- `--accent` / `--accent-hover` → `--themely-apple-blue` / `--themely-apple-blue-hover`, **except**
  the `Export PNG` fill which consumes `--accent-fill` / `--accent-fill-hover`;
  `--accent-contrast` → `--themely-on-accent`.
- `--text-primary` → `--themely-midnight-ink`; `--text-secondary` → `--themely-slate-blue`;
  `--text-muted` → `--themely-ghost-gray`.
- `--border-strong` → the hairline model; there is no Themely equivalent, and D-06 is
  hairline-only.
- `--shadow-inspector` → deleted with its container; `--shadow-navigation` → `--popover-shadow`;
  `--modal-shadow` → `--dialog-shadow`; `--toast-shadow` → `--popover-shadow`.
- The six `--glass-*` consumers (`App.css:56,62,226,239`, `MapCanvas.css:214,221`) → flat surfaces
  with hairlines. **`backdrop-filter` is forbidden outright** — a blanket ban, simpler than the old
  approved-surface allowlist and it cannot rot.
- The `--font-*` consumers → the `--text-*` role bundles.

**B. Assertion 1 — no OS colour-scheme condition in any stylesheet.** Parse at-rule conditions
across the globbed stylesheet set, not just `theme.css`. *RED probe:* re-add a dark palette block
guarded by the OS colour-scheme query.

**C. Assertion 2 — no retired token name in any stylesheet.** Assert against the full retired
list from `03-UI-SPEC.md` assertion 2. *RED probe:* re-add the retired teal declaration.

**D. Assertion 3 — `:root` ↔ `.dark` parity.** Every `--themely-*` in `:root` has a counterpart in
`.dark` and vice versa, **except** the fixed trio which must be **identical** in both. *RED probe:*
drop one token from `.dark`.

**E. Assertion 4 — Live Invariant 9, extended to `.dark`.** No export token is declared outside
the unconditioned `:root`. *RED probe:* add `--map-surface` to `.dark`.

**F. Assertion 5 — every export token declared exactly once AND has a live consumer.** *RED probe:*
delete a consumer.

**G. Assertion 6 — every `--motion-*` token has a consumer and is zeroed under reduced motion.**
This is not optional: three motion tokens were once declared, reduced-motion-gated, and read by
nothing, so the gate proved nothing. *RED probe:* remove a consumer.

**H. Assertion 26 — the accent-fill pair.** Declared **exactly once in the unconditioned `:root`**,
in no `.dark` block, and the `Export PNG` fill resolves to `#0071e3` in **both** modes.
*RED probe:* point `--accent-fill` at `var(--themely-apple-blue)` — the dark row of the contrast
matrix drops to 3.02:1 and Task 3's matrix fails.

**RED PROBES: all seven are mandatory task steps**, each executed, observed, and captured. For
each: copy the subject file to the scratchpad, break it, run
`npx vitest run src/styles/uiContract.test.ts`, OBSERVE RED, capture output, restore by copying
back, re-run green. **Never `git checkout --` a file with uncommitted work** — two agents lost
edits that way in one session.

**Comment discipline.** None of these assertions' negative-grep literals may appear in a comment
in any file under test. Where a count is taken, strip CSS comment lines first.
  </action>
  <verify>
    <automated>npx vitest run src/styles/uiContract.test.ts</automated>
    <automated>node -e "const fs=require('fs');const glob=(d)=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?glob(d+'/'+e.name):(e.name.endsWith('.css')?[d+'/'+e.name]:[]));const files=glob('src/styles');const retired=['--accent','--accent-hover','--accent-contrast','--surface-accent-tint','--text-primary','--text-secondary','--text-muted','--border-strong','--glass-','--shadow-inspector','--shadow-navigation','--modal-shadow','--toast-shadow','--font-label','--font-body','--font-heading','--font-display','--weight-regular','--weight-semibold','--radius-large','--map-shadow','--mixed-color-','--active-check-'];let bad=[];for(const f of files){const t=fs.readFileSync(f,'utf8').split('\n').filter(l=>!/^\s*(\/\*|\*)/.test(l)).join('\n');for(const r of retired){if(t.includes(r))bad.push(f+' :: '+r)}};if(bad.length){console.error('retired tokens survive:',bad);process.exit(1)};console.log('NO_RETIRED_TOKENS')"</automated>
    <automated>node -e "const fs=require('fs');const glob=(d)=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?glob(d+'/'+e.name):(e.name.endsWith('.css')?[d+'/'+e.name]:[]));for(const f of glob('src/styles')){const t=fs.readFileSync(f,'utf8').split('\n').filter(l=>!/^\s*(\/\*|\*)/.test(l)).join('\n');if(/backdrop-filter/.test(t)){console.error('backdrop-filter survives in',f);process.exit(1)}};console.log('NO_BACKDROP_FILTER')"</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    Every enumerated retired-token consumer resolves to its replacement; assertions 1-6 and 26 are
    in `uiContract.test.ts` and green; `backdrop-filter` appears in no stylesheet; all seven RED
    probes were executed and captured.
  </done>
  <acceptance_criteria>
    - The retired-token scan over all globbed stylesheets (comment lines stripped) returns nothing.
    - `backdrop-filter` appears in no stylesheet.
    - Assertions 1, 2, 3, 4, 5, 6, and 26 all exist in `uiContract.test.ts` and pass.
    - The SUMMARY carries seven captured RED outputs, one per assertion, each naming the file
      broken and the restore method (scratchpad copy-back).
    - `npm run lint && npm test && npm run build` clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: The contrast matrix and the two closed exemption sets (assertions 19, 8, 9)</name>
  <files>src/styles/uiContract.test.ts</files>
  <read_first>
    - `src/styles/phase2CssContract.test.ts` — the existing contrast-matrix code and its cascade-resolution helper, ported in 03-03
    - `03-UI-SPEC.md` § The Export fill is mode-invariant (the three-ratio table), § Typography (the closed consumer exemption), § Legend colour exemption
    - `src/components/LegendOverlay.tsx` lines 30-80 and line 313 — `THEME_COLORS` and the hard-coded swatch stroke
  </read_first>
  <action>
Three assertions, all of which are about a set being **closed** rather than merely present.

**A. Assertion 19 — the contrast matrix.** It resolves through the **real cascade** for every
(mode × preference) combination — light and dark, each crossed with default,
`prefers-contrast: more`, and `forced-colors` — and asserts that every text-on-surface pair meets
AA in **both** modes.

Two properties are non-negotiable:
- **It asserts its own row count.** A matrix that iterates whatever it finds can resolve to
  nothing and still pass. Assert the expected number of rows explicitly.
- **No exceptions are enumerated.** `03-UI-SPEC.md` records that the draft `[EXCEPTION]` was
  **removed, not accepted** — the owner chose a mode-invariant `--accent-fill` instead. There is
  now no contrast exception in this contract, and adding one back is a change to the contract, not
  a test fix.

*RED probe:* let the matrix resolve to nothing (e.g. point it at a selector that does not exist)
and confirm the row-count assertion fails rather than the matrix passing vacuously. Run a second
probe: point `--accent-fill` at the flipping `--themely-apple-blue` and confirm the dark row drops
to 3.02:1 and fails.

**B. Assertion 8 — no hex/rgba literal in any component `.tsx`, closed exemption list.** The
exemption list contains **exactly** `LegendOverlay.tsx` and its stated reason. That file
hard-codes `THEME_COLORS` (`background: '#FFFFFF'`, `text: '#111827'`, `border: '#CBD5E1'`) and
`stroke="#9CA3AF"` at `:313` — these are **deliberate export-fixed values, not an oversight**, and
`--swatch-border: #9ca3af` mirrors the last one. Note the name collision recorded in P-3: the
legend's `light`/`dark` is a *creator-chosen legend theme*, not the app's colour scheme. Write the
reason into the exemption in the test, not only into a comment in the component.
*RED probe:* add a hex literal to a different component and watch it fail.

**C. Assertion 9 — the type-role consumer exemption is a closed set of exactly two.**
`--text-display` and `--text-stat` have no consumer in this app (no marketing hero, no stat card),
but D-09 vendors the whole scale, so they are declared **with an explicit commented exemption
naming the reason**. The exemption list in the contract test is a closed set of exactly these two.
*RED probe:* add a third and watch it fail.

**RED PROBES: all four are mandatory task steps** (matrix-resolves-to-nothing, accent-fill
flipped, third hex literal, third type-role exemption). Copy to scratchpad, break, observe RED,
capture, restore by copying back, re-run green. Never `git checkout --` a file with uncommitted
work.
  </action>
  <verify>
    <automated>npx vitest run src/styles/uiContract.test.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/styles/uiContract.test.ts','utf8');if(!/rows?\.length\)?\s*(\)\s*)?\.toBe|toHaveLength/.test(s)){console.error('contrast matrix has no explicit row-count assertion');process.exit(1)};console.log('ROW_COUNT_ASSERTED')"</automated>
    <automated>npm test</automated>
  </verify>
  <done>
    The contrast matrix resolves through the real cascade for all six (mode × preference)
    combinations, asserts its own row count, enumerates no exceptions, and every pair meets AA in
    both modes. The hex-literal exemption is closed at exactly `LegendOverlay.tsx` with its reason
    recorded, and the type-role exemption is closed at exactly two.
  </done>
  <acceptance_criteria>
    - The contrast matrix contains an explicit row-count assertion.
    - No contrast exception is enumerated anywhere in `uiContract.test.ts`.
    - The hex-literal exemption list contains exactly one filename.
    - The type-role exemption list contains exactly two token names.
    - The SUMMARY carries four captured RED outputs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Assertion 17 and the retirement of `phase2CssContract.test.ts`</name>
  <files>src/styles/uiContract.test.ts, src/styles/phase2CssContract.test.ts</files>
  <read_first>
    - `src/styles/phase2CssContract.test.ts` lines 940-1030 — the export-unsafe guard and the square assertions, to confirm nothing is dropped in the retirement
    - `.planning/coding-rules/export.md` § Strip semantics
    - `03-UI-SPEC.md` § Elevation — the rewritten post-D-34 reason
  </read_first>
  <action>
**A. Assertion 17 — the export-unsafe-CSS guard, at high priority.** `backdrop-filter` appears
**nowhere**; no `filter`, `box-shadow`, `text-shadow`, `mix-blend-mode`, `mask`, or `clip-path`
applies to exported content; and `EXPORT_CONTENT_PATTERN` is still **bound back to `MapCanvas.tsx`
source** — `.map-unit-path` was once omitted from that pattern for a whole phase, which is the
failure mode this binding exists to prevent.

D-06 makes hairline `box-shadow` pervasive across chrome, so this guard is **more** load-bearing
after this phase than before it — a hairline is still a `box-shadow`.

Write the reason correctly for the post-D-34 world (03-11 removes html2canvas): under D-34 the
clone is serialised with `XMLSerializer` into a `data:image/svg+xml` URL and rasterised as an
`<img>`. That image is an **isolated document**: it sees none of the host page's stylesheets. An
effect applied through an external CSS rule does not render "differently" — it renders **not at
all**, and the only signal is a PNG that quietly lost an effect the editor shows on screen.
`sanitizeExportClone` hard-sets stroke and stroke-width inline for exactly this reason; it does
**not** neutralise an inherited effect.

*RED probe:* add a hairline `box-shadow` to `.map-canvas` and watch it fail.

**B. Retire `phase2CssContract.test.ts`.** Before deleting it, produce a written mapping: for each
assertion in the old file, name either the assertion number in `uiContract.test.ts` that carries
it forward, or the reason it is retired as Phase 2 policy that D-01…D-31 replaces. Put that
mapping in the plan SUMMARY. `03-RESEARCH.md` § Pattern 8 already triages the file into
keep-verbatim / carry-forward / retire — use it as the starting point and reconcile against the
actual file.

Then delete `src/styles/phase2CssContract.test.ts`. Do not leave it skipped, do not leave it
importing from the successor — a skipped contract test is a gate that cannot fail wearing a
different hat.

Confirm the total assertion count did not silently shrink: the SUMMARY states how many assertions
the old file carried, how many the new file carries, and accounts for the delta line by line.

**C. Update the coding rules in the same commit.** `CLAUDE.md` § Update Process binds this:
update `.planning/coding-rules/frontend.md` § Styling / § Visual Tokens with the new token
namespace, the class-based dark flip, the extended Live Invariant 9 wording (now covering `.dark`),
the `backdrop-filter` outright ban, and the successor test's name. Bump its `Last updated` line and
keep only the **two** most recent entries — merge the two oldest into one line in the same edit if
a third would appear. Rule 3 is real and `frontend.md` had accumulated seven entries before.
  </action>
  <verify>
    <automated>test ! -f src/styles/phase2CssContract.test.ts && echo OLD_CONTRACT_RETIRED</automated>
    <automated>npx vitest run src/styles/uiContract.test.ts</automated>
    <automated>grep -rn "phase2CssContract" src/ tests/ && { echo "FAIL: stale reference to the retired contract test"; exit 1; } || echo NO_STALE_REFS</automated>
    <automated>node -e "const s=require('fs').readFileSync('.planning/coding-rules/frontend.md','utf8');const n=(s.match(/^\*Last updated:/gm)||[]).length;if(n>2){console.error('frontend.md has',n,'Last updated entries; max 2');process.exit(1)};console.log('LAST_UPDATED_OK',n)"</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    Assertion 17 is in place with its post-D-34 reason and `EXPORT_CONTENT_PATTERN` bound back to
    source; `phase2CssContract.test.ts` is deleted with a written per-assertion carry-forward
    mapping and an accounted assertion-count delta; `frontend.md` is updated in the same commit
    with at most two `Last updated` entries.
  </done>
  <acceptance_criteria>
    - `src/styles/phase2CssContract.test.ts` no longer exists and nothing references it.
    - The SUMMARY contains a per-assertion carry-forward table and an explicit
      old-count / new-count / delta accounting.
    - Assertion 17's stated reason does NOT claim html2canvas rasterises differently.
    - `EXPORT_CONTENT_PATTERN` is bound back to `MapCanvas.tsx` source.
    - `.planning/coding-rules/frontend.md` is updated in the same commit and has at most two
      `Last updated` entries.
    - The SUMMARY carries the captured RED output from the `.map-canvas` box-shadow probe.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer theme → exported PNG | The one boundary Live Invariant 9 exists to hold; a token redefined in `.dark` crosses it silently |
| stylesheet cascade → accessible contrast | Preference blocks are authored after the palette at equal specificity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Tampering | an export token redefined inside `.dark` | critical | mitigate | Assertion 4 extends Live Invariant 9 to `.dark`; a standalone node check scans every `.dark` block for the mode-invariant list; RED-proven by adding `--map-surface` to `.dark` |
| T-03-12 | Information disclosure | white-on-accent at 3.02:1 in dark mode | high | mitigate | `--accent-fill` mode-invariant, declared once in `:root`, guarded by assertion 26 plus the contrast matrix's dark row; RED-proven by pointing it at the flipping token |
| T-03-13 | Repudiation | a contrast matrix that resolves to nothing and passes | high | mitigate | Assertion 19 asserts its own row count and enumerates no exceptions; RED-proven by making it resolve to nothing |
| T-03-14 | Tampering | a retired token silently aliased instead of deleted | medium | mitigate | Delete-never-alias is enforced by assertion 2 over the globbed stylesheet set with comment lines stripped |
| T-03-15 | Denial of service | an inherited CSS effect silently dropped from the PNG | high | mitigate | Assertion 17 with `EXPORT_CONTENT_PATTERN` bound back to `MapCanvas.tsx`; `backdrop-filter` banned outright; RED-proven by adding a hairline `box-shadow` to `.map-canvas` |
</threat_model>

<verification>
```bash
npm run lint
npm test
npm run build
npx vitest run src/styles/uiContract.test.ts src/styles/themeTokens.test.ts
npm run test:e2e -- --project=chrome
```

Then by hand: `npm run dev`, **hard reload** (the dev server serves unhashed stylesheets; a warm
cache shows the old palette and has misled a judgement here before), and check both modes by
toggling the `.dark` class on the editor mount root in devtools — there is no OS listener to use.

Chrome only. Edge is not installed (D-33).
</verification>

<success_criteria>
- [ ] Themely cool in `:root` and Lights Out in `.dark`, verbatim, no value adjusted (D-04)
- [ ] The dark flip is class-driven and no stylesheet carries an OS colour-scheme at-rule condition (D-08/D-30, assertion 1)
- [ ] Every retired token is deleted with no alias, across all globbed stylesheets (D-06/D-10, assertion 2)
- [ ] `:root` ↔ `.dark` parity holds with the fixed trio identical in both (assertion 3)
- [ ] No export token appears in any `.dark` block (Live Invariant 9, assertion 4)
- [ ] Every export token and every motion token is declared once and has a live consumer (assertions 5, 6)
- [ ] `--accent-fill` / `--accent-fill-hover` are mode-invariant and the Export label is 4.70:1 in both modes (assertion 26)
- [ ] The contrast matrix asserts its own row count, enumerates no exceptions, and meets AA in both modes (assertion 19)
- [ ] The hex-literal exemption is closed at exactly `LegendOverlay.tsx`; the type-role exemption is closed at exactly two (assertions 8, 9)
- [ ] `backdrop-filter` appears nowhere; assertion 17 carries the post-D-34 reason and `EXPORT_CONTENT_PATTERN` is bound back to source
- [ ] `phase2CssContract.test.ts` is deleted with a written carry-forward mapping and an accounted count delta
- [ ] Thirteen RED probes executed and captured across the four tasks
</success_criteria>

<output>
Create `.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-04-SUMMARY.md` when done, carrying:
`RED probes (13, with output)`, `phase2CssContract.test.ts carry-forward mapping`, and
`Assertion count: old / new / delta`.

**Do not run `state.advance-plan`, `state.update-progress`, or `roadmap.update-plan-progress`.**
Edit `.planning/STATE.md` and `.planning/ROADMAP.md` by hand.
</output>
