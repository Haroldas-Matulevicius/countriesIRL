---
phase: 03-clean-ui-overhaul-1-1-5-weeks
plan: 09
type: execute
wave: 9
depends_on:
  - 03-08
files_modified:
  - src/styles/editor.css
  - src/hooks/useResponsiveLayout.ts
  - src/components/editor/ToolPanel.tsx
  - src/components/editor/ToolRail.tsx
  - src/styles/uiContract.test.ts
  - tests/e2e/responsive.spec.ts
autonomous: true
requirements:
  - D-08
  - D-20
  - D-35
  - A-18
  - A-24

estimate:
  tokens: 165000
  raw_tokens: 82000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-35 / assertion 24: the export-independence gate is RE-ARMED against the class-based dark mode — it toggles the .dark class on the editor mount root instead of emulating an OS colour scheme, and it is RED-proven by making the export theme-sensitive on purpose."
    - "The rewritten assertion 24 covers all three axes it always covered: theme, forced colors, and device pixel ratio — the exported PNG is byte-identical across every combination, which is Live Invariant 9's only browser-level guard."
    - "D-20: below the existing 1200px breakpoint the rail becomes a bottom bar with 44px touch targets and a tapped tool raises a bottom sheet over the map — the ONE surface allowed to overlay the canvas."
    - "Assertion 18: touch-action: none is on svg.map-canvas AND NOWHERE ELSE — the OWNERSHIP SET is asserted, not just the value, because D-20's bottom sheet makes ancestors newly relevant."
    - "360px containment and the 200%-equivalent check still hold in the new chrome, with the halved CSS viewport labelled as the EQUIVALENT of 200% zoom, never as physical zoom."
    - "prefers-reduced-motion behaviour is re-verified in the new chrome; prefers-reduced-transparency is asserted STATICALLY because Playwright cannot emulate it and emulation a browser does not support is not evidence."
    - "The floating cluster sits above the bottom bar and still outside the frame at narrow widths; the HUD header and footer fold into the bottom bar with Export still pinned and visible."
  artifacts:
    - rewritten tests/e2e/responsive.spec.ts preference blocks
  key_links:
    - "responsive.spec.ts:1025,1048 is the exact site D-35 names; once .dark drives the palette, page.emulateMedia({ colorScheme }) is a NO-OP and both exports become trivially identical — the gate passes while proving nothing."
    - "Assertion 18's ownership set matters because the bottom sheet introduces new ancestors of svg.map-canvas; touch-action on one of them would silently break map panning."
    - "Live Invariant 9's CSS-level guard is assertion 4 (03-04); assertion 24 is its ONLY browser-level guard, which is why losing it is a phase-level risk rather than a test-suite detail."
  backstops:
    - statement: "Every interactive control in the bottom bar and bottom sheet meets the 44px --target-compact touch target, and the tool panel body remains one scroll container at 360px."
      verification: backstop
---

<objective>
Re-arm the gate the dark-mode switch would otherwise have silently killed, and re-verify the
narrow-width and reduced-preference behaviour in the new chrome.

Purpose: `tests/e2e/responsive.spec.ts:1025,1048` flips theme with `page.emulateMedia({
colorScheme })`. Once `.dark` drives the palette that emulation changes **nothing**, both exports
become trivially identical, and Live Invariant 9 loses its only browser-level guard. That would be
this repo's fifth "gate that cannot fail" — and this one guards a change to exported pixels.

Output: a rewritten `responsive.spec.ts` whose theme axis toggles the class and is RED-proven by a
deliberately theme-sensitive export, plus the narrow-width layout and assertion 18's ownership set.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/export.md
@Design.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-UI-SPEC.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-08-SUMMARY.md

Read narrowly:
- `03-RESEARCH.md` § P-1 (lines ~1329-1354) — the complete description of the disarming
- `03-RESEARCH.md` § Current-State Inventory → the OS-colour-preference inventory, which cites
  `responsive.spec.ts:718`, `:759`, `:1025`, `:1048`, `:1052` by line
- `03-CONTEXT.md` D-20, D-35
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| rewritten theme axis in `responsive.spec.ts` | e2e | class toggling on the mount root replaces OS-scheme emulation at `:718`, `:759`, `:1025`, `:1048`, `:1052` |
| bottom-bar rail variant | CSS + component | below 1200px; `--target-compact` (44px) targets; `data-editor-only="true"` |
| bottom-sheet panel variant | CSS + component | the one surface allowed to overlay the canvas; enters on `--motion-ease-out`, exits on `--motion-ease-in` |
| assertion 18 | test code | `touch-action: none` ownership set |
| assertion 24 | e2e | export identical across `.dark` toggle × forced colors × DPR |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Re-arm assertion 24 — export independence across a CLASS-toggled theme (D-35)</name>
  <files>tests/e2e/responsive.spec.ts</files>
  <read_first>
    - `tests/e2e/responsive.spec.ts` lines 700-780 and 1000-1060 — the preference blocks at `:718`, `:759` and the export-independence contexts at `:1025`, `:1048`, `:1052`
    - `03-RESEARCH.md` § P-1 in full
    - `.planning/coding-rules/export.md` § Clone contract and the discrimination-control guidance (a cross-export equality needs a known-different state exported in the same run)
    - `src/components/editor/ThemeToggle.tsx` and `src/components/editor/MapEditor.tsx` — where `.dark` is written
    - `tests/e2e/support/appHarness.ts` (full)
  </read_first>
  <action>
This is the load-bearing task of the plan. Do it first, and do it before the narrow-width work, so
a failure here cannot be mistaken for a layout problem.

**A. Understand exactly what breaks.** `responsive.spec.ts:1015-1052` asserts *"the PNG is
identical across theme, forced colors, and device pixel ratio."* Its theme axis flips with
`page.emulateMedia({ colorScheme })`. After 03-04 moved the palette onto a `.dark` class, that
emulation changes **nothing** in the page — so both exports are trivially identical and the
assertion passes while proving nothing. The test still reports green. Live Invariant 9 is then
guarded only at the CSS level (assertion 4) and has **no browser-level guard at all**.

**B. Rewrite the theme axis to toggle the class.** Replace the OS-scheme emulation at `:718`,
`:759`, `:1025`, `:1048`, and `:1052` with an action that adds and removes `.dark` on the **editor
mount root** — the same element `ThemeToggle` writes to, and **not** `document.documentElement`.
Prefer driving it through the real theme toggle control where the test's shape allows, so the gate
exercises the shipped path rather than a test-only backdoor; where it must be direct, set the class
on the mount root explicitly.

Keep the other two axes exactly as they are: **forced colors** and **device pixel ratio** are still
real emulations and must remain.

**C. RED-PROVE IT BY MAKING THE EXPORT THEME-SENSITIVE ON PURPOSE.** This is the whole point of
D-35 and it is a mandatory task step, not a suggestion:
1. Copy `src/styles/theme.css` to the session scratchpad.
2. Add a `.dark` override for one export token — `--map-surface` is the clearest — so the exported
   square genuinely changes with the theme.
3. Run `npx playwright test tests/e2e/responsive.spec.ts --project=chrome -g "preference"` and
   **OBSERVE THE ASSERTION FAIL.** Capture the failure output verbatim.
4. Restore `theme.css` by **copying the scratchpad file back**. **Never `git checkout --` a file
   with uncommitted work** — two agents lost edits that way in one session.
5. Re-run and observe green.

If the assertion does **not** fail in step 3, the rewrite did not take and the gate is still dead.
Stop, report, and fix the rewrite before proceeding — do not proceed on a green that was never
tested against the bug it covers.

**Expect a SECOND failure in step 3, and do not read it as a broken probe.** Adding
`--map-surface` to `.dark` also violates **assertion 4** (`03-04`'s export-firewall check: no
export token may be declared outside the unconditioned `:root`). Both gates going red at once is
**correct and by design** — the probe deliberately commits the exact defect both of them exist to
catch, one at the CSS level and one at the browser level. Record both failures. Restore
`theme.css` by copying the scratchpad file back and confirm both go green again.

**This proof will be re-taken in `03-11`.** `03-11` replaces the entire rasterisation path
(D-34), and its own governing rule is that a test which passed against html2canvas and still
passes has not been shown to test the new code. Assertion 24 is Live Invariant 9's only
browser-level guard, so `03-11` Task 7 probe 9 re-runs exactly this probe against the new path.
Capturing a clean, quotable RED output here is what makes that re-run a comparison rather than a
fresh start.

**D. Also confirm the emulation is genuinely gone.** Assert in the spec, or check separately, that
no `emulateMedia({ colorScheme ... })` call remains in `responsive.spec.ts`. A leftover call is
harmless but misleading, and a later reader would take it as evidence the theme axis is covered.

**E. Keep the byte-identity claim honest.** The assertion compares exported PNG bytes (or a
disjoint-region pixel signature, matching whatever the existing test does). Do not weaken it to a
size check or a dimension check while rewriting the axis — that would be trading one gate that
cannot fail for another.

Chrome only. Edge is not installed (D-33); do not add an `msedge` run and do not report an Edge
result.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/responsive.spec.ts --project=chrome --reporter=line</automated>
    <automated>node -e "const s=require('fs').readFileSync('tests/e2e/responsive.spec.ts','utf8');if(/emulateMedia\(\s*\{[^}]*colorScheme/.test(s)){console.error('colorScheme emulation still present in responsive.spec.ts');process.exit(1)};if(!/classList\.(add|toggle|remove)\(\s*['\"]dark['\"]/.test(s)&&!/Switch to (dark|light) theme/.test(s)){console.error('no .dark class toggle or theme-control drive found');process.exit(1)};console.log('THEME_AXIS_REARMED')"</automated>
    <automated>grep -c "documentElement" tests/e2e/responsive.spec.ts || echo NO_DOCUMENT_ELEMENT_IN_SPEC</automated>
  </verify>
  <done>
    `responsive.spec.ts`'s export-independence test toggles `.dark` on the editor mount root,
    keeps the forced-colors and DPR axes as real emulations, still compares exported bytes, and
    **was observed RED** against a deliberately theme-sensitive `--map-surface`. No
    `colorScheme` emulation remains in the file.
  </done>
  <acceptance_criteria>
    - `emulateMedia({ colorScheme ... })` appears nowhere in `tests/e2e/responsive.spec.ts`.
    - The theme axis toggles `.dark` on the editor mount root, not on `document.documentElement`.
    - The forced-colors and DPR axes are unchanged and still real emulations.
    - The comparison is still byte/pixel identity, not a size or dimension check.
    - **The SUMMARY carries the verbatim RED failure output from step 3**, plus a statement that
      the restore was done by copying from the scratchpad.
    - The SUMMARY notes that assertion 4 failed simultaneously and that this is expected, not a
      broken probe.
    - The SUMMARY states that this proof is re-taken by `03-11` Task 7 probe 9 against the
      replaced rasterisation path.
    - `grep -c msedge tests/e2e/responsive.spec.ts` returns 0.
  </acceptance_criteria>
  <reversibility rating="reversible">A test rewrite; the risk is not in reverting it but in landing it un-RED-proven.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Narrow width — bottom bar and bottom sheet (D-20), plus assertion 18's ownership set</name>
  <files>src/styles/editor.css, src/hooks/useResponsiveLayout.ts, src/components/editor/ToolPanel.tsx, src/components/editor/ToolRail.tsx, src/styles/uiContract.test.ts</files>
  <read_first>
    - `src/hooks/useResponsiveLayout.ts` (full) — the existing 1200px breakpoint; do not invent a second one
    - `src/styles/editor.css` — the grid landed in 03-03 and the rail/panel rules from 03-06
    - `03-UI-SPEC.md` § Narrow width (D-20) and § The shell (the `touch-action` ownership rule)
    - `Design.md` § the D-20 narrow-width contract recorded by 03-03 Task 4
    - `src/lib/motion/tokens.ts` — the entry/exit curves
  </read_first>
  <action>
Implement the narrow-width contract 03-03 recorded, against the **existing** 1200px breakpoint —
`useResponsiveLayout.ts` already owns it and a second breakpoint is drift.

| Element | Narrow behaviour |
|---|---|
| Rail | becomes a **bottom bar**, icons thumb-reachable, `--target-compact` (44px) targets, `data-editor-only="true"` |
| Panel | a tapped tool raises a **bottom sheet** over the map — **the one surface that overlays the canvas** — entering on `--motion-duration-base` / `--motion-ease-out`, exiting on `--motion-ease-in` |
| Floating cluster | sits directly above the bottom bar, **still outside the frame** |
| HUD header / footer | fold into the bottom bar; the composition name truncates to one line; **Export stays pinned and visible** |

**Assertion 18 — assert the OWNERSHIP SET, not just the value.** `touch-action: none` belongs on
`svg.map-canvas` **and nowhere else**. This matters now specifically because D-20's bottom sheet
introduces new ancestors of the canvas, and `touch-action` on one of them silently breaks map
panning on touch. Assert the full set of selectors carrying the property equals exactly
`{ svg.map-canvas }`.

*RED probe (mandatory task step):* add `touch-action: none` to the bottom sheet, run
`npx vitest run src/styles/uiContract.test.ts`, OBSERVE RED, capture output, restore by copying
from the scratchpad.

**`overflow-x: hidden` stays on `body` and nowhere else.** On a non-viewport element it computes
`overflow-y: auto`, silently killing sticky positioning inside it — and the bottom bar and pinned
Export both depend on sticky/fixed behaviour holding.

**Exactly one `MapCanvasHandle` and one `svg.map-canvas` across the 1200px transition** (Live
Invariant 4), guarded by `data-camera-owner-sentinel`. Do **not** render a second canvas for the
narrow layout. The camera lease and its outermost-`finally` release order are unchanged.

**No second roving-tabindex writer** in the bottom bar (assertion 27, landed in 03-06).

**No positional selectors** on the bottom bar's rows (assertion 16, landed in 03-03) — the bar is
the same near-identical-row hazard as the rail.
  </action>
  <verify>
    <automated>npx vitest run src/styles/uiContract.test.ts</automated>
    <automated>npx playwright test tests/e2e/responsive.spec.ts --project=chrome --reporter=line</automated>
    <automated>node -e "const fs=require('fs');const glob=(d)=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?glob(d+'/'+e.name):(e.name.endsWith('.css')?[d+'/'+e.name]:[]));let hits=[];for(const f of glob('src/styles')){const t=fs.readFileSync(f,'utf8').split('\n');t.forEach((l,i)=>{if(/touch-action\s*:\s*none/.test(l)&&!/^\s*(\/\*|\*)/.test(l))hits.push(f+':'+(i+1))})};if(hits.length!==1){console.error('touch-action: none ownership set is not exactly one site:',hits);process.exit(1)};console.log('TOUCH_ACTION_OWNERSHIP_OK',hits[0])"</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    Below 1200px the rail is a bottom bar with 44px targets and a tapped tool raises a bottom
    sheet over the map; the cluster sits above the bar and outside the frame; Export stays pinned
    and visible; `touch-action: none` has exactly one owner; one canvas and one handle survive the
    transition.
  </done>
  <acceptance_criteria>
    - The narrow layout uses the existing 1200px breakpoint; no second breakpoint is introduced.
    - Every bottom-bar and bottom-sheet control meets the 44px `--target-compact` target.
    - `touch-action: none` appears on exactly one selector, and it is `svg.map-canvas`
      (assertion 18, RED-proven).
    - `overflow-x: hidden` appears only on `body`.
    - Exactly one `svg.map-canvas` and one `MapCanvasHandle` exist across the transition.
    - `Export PNG` is visible and reachable in the narrow layout.
    - The SUMMARY carries the captured RED output from the bottom-sheet `touch-action` probe.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 360px containment, the 200%-equivalent, and reduced-preference behaviour in the new chrome</name>
  <files>tests/e2e/responsive.spec.ts, src/styles/uiContract.test.ts</files>
  <read_first>
    - `tests/e2e/responsive.spec.ts` — the existing 360px and halved-viewport blocks, and the reduced-motion / reduced-transparency blocks
    - `03-UI-SPEC.md` § Narrow width, § Motion → "Preference blocks under a class-based dark mode"
    - `.planning/coding-rules/general.md` § Browser Compatibility
    - `Design.md` § motion
  </read_first>
  <action>
Re-verify the preference and containment behaviour **in the new chrome** — the old assertions were
written against the app bar and inspector column and several will be asserting against DOM that no
longer exists.

**A. 360px containment.** No horizontal overflow, no clipped label, the tool panel body remains
**one scroll container**, and every control stays reachable. `03-UI-SPEC.md` records the stacking
rule this depends on: stack labelled controls; reserve rows for icon-only or single-word controls.

**B. The 200%-equivalent.** The halved CSS viewport is **labelled as the equivalent** of 200%
zoom, **never as physical zoom**. This wording is a recorded Phase 2 decision and it is not a
formality — reporting an emulated result as a physical one is exactly the class of claim
Immutable Safety Constraint 8 forbids. Keep the label in the test name and in the SUMMARY.

**C. `prefers-reduced-motion`.** Re-verify in the new chrome: the panel/sheet transition, the theme
crossfade, the scene crossfade, and the icon glyph animations all respect it.
`--motion-duration-fast`, `-base`, `-slow`, and `--motion-scene` are zeroed under the preference
(assertion 6, landed in 03-04), `startAnimation()` is gated and `stopAnimation()` is unconditional
(D-29, landed in 03-06). Assert the *observable* result, not the presence of the media query.

**D. `prefers-reduced-transparency` is asserted STATICALLY.** Playwright cannot emulate it, and
**emulation a browser does not support is not evidence**. Assert it in the CSS contract test —
that the preference block exists and defines the surfaces it must — and leave the physical check
to the owner acceptance matrix, exactly as Phase 2 did. Do not add a Playwright emulation for it
and do not report one.

**E. Preference blocks must define BOTH modes.** Any `prefers-contrast` or `forced-colors`
override of a `--themely-*` token is declared for **both** `:root` and `.dark` inside the same
at-rule. The preference block is authored after the palette at equal specificity, so a literal
written for one mode silently wins in the other — the defect that once painted a light bar under
light text at **1.0:1** for the user who asked for *more* contrast. 03-04 landed the rule; this
task verifies it survives in the narrow chrome too.

**F. Every rewritten assertion needs a RED probe against the arrangement it replaced.** For the
360px containment and the reduced-motion blocks, break the subject once (e.g. give the panel body a
fixed width wider than 360px; remove the reduced-motion zeroing), observe RED, capture, restore by
copying from the scratchpad. A rewritten test that has never failed is a rewritten test nobody has
checked.

**G. Record the browser scope plainly.** The suite runs **Chrome only**. Edge is not installed on
this machine, so the `msedge` Playwright project cannot launch (D-33). The SUMMARY must say
**"Edge not certified — not installed"** rather than omitting it or inferring a pass. Do **not**
cite `STATE.md`'s Edge record — that is immutable Phase 2 evidence to be annotated, never
rewritten, and resolving the contradiction is filed against Phase 2, not Phase 3.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/responsive.spec.ts --project=chrome --reporter=line</automated>
    <automated>npx vitest run src/styles/uiContract.test.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('tests/e2e/responsive.spec.ts','utf8');if(/emulateMedia\(\s*\{[^}]*reducedTransparency/.test(s)){console.error('attempted emulation of prefers-reduced-transparency');process.exit(1)};console.log('NO_UNSUPPORTED_EMULATION')"</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    360px containment and the 200%-equivalent hold in the new chrome with the equivalence labelled
    as such; reduced-motion behaviour is verified by observable result;
    `prefers-reduced-transparency` is asserted statically with no emulation attempt; preference
    blocks define both modes; every rewritten assertion has an executed RED probe.
  </done>
  <acceptance_criteria>
    - No horizontal overflow and no clipped label at 360px; the panel body is one scroll container.
    - The halved viewport is labelled as the **equivalent** of 200% zoom in both the test name and
      the SUMMARY.
    - Reduced-motion assertions check observable behaviour, not the presence of a media query.
    - No Playwright emulation of `prefers-reduced-transparency` exists.
    - Every `prefers-contrast` / `forced-colors` token override defines both `:root` and `.dark`.
    - The SUMMARY states **"Edge not certified — not installed"** and does not cite `STATE.md`'s
      Edge record.
    - The SUMMARY carries at least two captured RED outputs from this task.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer theme / OS preference → exported PNG | Live Invariant 9's boundary; assertion 24 is its only browser-level guard |
| touch input → the map camera | `touch-action` ownership decides whether panning works on touch |
| emulated preference → a reported result | An emulation a browser does not support is not evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-38 | Repudiation | assertion 24 silently disarmed by the class-based flip | critical | mitigate | Theme axis rewritten to toggle `.dark` on the mount root; a source check forbids any surviving `colorScheme` emulation; RED-proven by adding a `.dark` override for `--map-surface` and observing the export diverge |
| T-03-39 | Tampering | an export token following the viewer's theme | critical | mitigate | Assertion 4 at the CSS level (03-04) plus assertion 24 at the browser level; both must pass |
| T-03-40 | Denial of service | `touch-action: none` on a new bottom-sheet ancestor breaking map panning | high | mitigate | Assertion 18 asserts the full OWNERSHIP SET equals exactly `svg.map-canvas`; RED-proven by adding it to the sheet |
| T-03-41 | Repudiation | reporting an emulated preference as a physical check | high | mitigate | `prefers-reduced-transparency` asserted statically with the emulation explicitly forbidden; the halved viewport labelled as the 200% **equivalent** in the test name and the SUMMARY |
| T-03-42 | Repudiation | claiming an Edge pass | high | mitigate | Chrome-only run; the SUMMARY states "Edge not certified — not installed"; `STATE.md`'s Edge record is not cited |
| T-03-43 | Denial of service | a second `svg.map-canvas` rendered for the narrow layout | high | mitigate | Live Invariant 4 re-asserted across the 1200px transition via `data-camera-owner-sentinel` |
</threat_model>

<verification>
```bash
npm run lint
npm test
npm run build
npx playwright test tests/e2e/responsive.spec.ts --project=chrome
npx playwright test --project=chrome     # full suite, Chrome only
npx vitest run src/styles/uiContract.test.ts
grep -n "colorScheme" tests/e2e/responsive.spec.ts ; echo "expect: no output"
```

Chrome only. Edge is not installed on this machine, so the `msedge` project cannot launch. Record
**"Edge not certified — not installed"** (D-33). Do not cite `STATE.md`'s Edge record — it is
immutable Phase 2 evidence and resolving the contradiction is Phase 2 work.
</verification>

<success_criteria>
- [ ] Assertion 24 toggles `.dark` on the mount root, keeps the forced-colors and DPR axes as real emulations, still compares exported bytes, and was **observed RED** against a deliberately theme-sensitive `--map-surface` (D-35)
- [ ] No `colorScheme` emulation remains anywhere in `responsive.spec.ts`
- [ ] Below 1200px the rail is a bottom bar and a tapped tool raises a bottom sheet — the one surface allowed to overlay the canvas (D-20)
- [ ] `touch-action: none` has exactly one owner, `svg.map-canvas` (assertion 18, RED-proven)
- [ ] `overflow-x: hidden` is on `body` only; one canvas and one handle survive the 1200px transition
- [ ] 360px containment holds; the halved viewport is labelled as the 200% **equivalent**
- [ ] Reduced-motion is verified by observable behaviour; `prefers-reduced-transparency` is asserted statically with no emulation attempt
- [ ] Preference blocks define both `:root` and `.dark` inside the same at-rule
- [ ] The SUMMARY states "Edge not certified — not installed" and cites no Edge pass
</success_criteria>

<output>
Create `.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-09-SUMMARY.md` when done, carrying:
`Assertion 24 RED proof (verbatim output)`, `RED probes (4+, with output)`, and a
`Browser scope: Chrome only — Edge not certified, not installed` statement.

**Do not run `state.advance-plan`, `state.update-progress`, or `roadmap.update-plan-progress`.**
Edit `.planning/STATE.md` and `.planning/ROADMAP.md` by hand.
</output>
