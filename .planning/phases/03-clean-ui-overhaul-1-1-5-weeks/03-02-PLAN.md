---
phase: 03-clean-ui-overhaul-1-1-5-weeks
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - Design.md
  - CLAUDE.md
  - src/lib/motion/tokens.ts
  - src/lib/motion/tokens.test.ts
  - src/utils/motion.ts
  - src/styles/theme.css
  - src/components/icons/index.ts
  - src/components/icons/PROVENANCE.md
  - src/components/icons/iconContract.test.ts
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-09
  - D-26
  - D-28
  - A-07
  - A-09
  - A-19
  - A-22
  - A-28
  - R-V2

estimate:
  tokens: 100000
  raw_tokens: 50000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "D-01: the one-way commitment to the Themely design system is confirmed by the owner BEFORE any token, type-scale, icon, or motion contract is authored — the gate sits in the same plan as the door it guards."
    - "D-26 / assertion 7: the six motion values exist in exactly two places — CSS custom properties and a TS mirror — pinned by a lockstep test that fails when either layer changes alone, and the test asserts its own row count so a dropped token cannot silently shrink the comparison."
    - "D-28 / assertion 22: every vendored icon exports a forwardRef component AND a structurally identical *IconHandle with { startAnimation, stopAnimation }, sizes via the `size` prop, and carries the strokeWidth 2->1.5 marker comment."
    - "R-V2 / assertion 28: every file under src/components/icons/ has a dated `read in full — {no flags | flags listed} — YYYY-MM-DD` provenance line, and the recorded set equals the file set TWO-WAY, so evidence and files cannot drift apart."
    - "D-01 / D-02 / D-03: CountriesIRL owns a Design.md derived from Themely's, naming Themely as upstream, vendoring the token table verbatim under the --themely-* namespace, with no cross-repo test dependency."
    - "D-09: the ten type roles are specified with size, line-height, weight, and tracking bundled per role, with a CLOSED consumer exemption of exactly --text-display and --text-stat."
    - "The export-unsafe-CSS reason is written for the post-D-34 world: in an SVG-as-image isolated document an externally-styled effect renders NOT AT ALL, which is a harder failure than a rasterizer mismatch."
  artifacts:
    - Design.md
    - src/lib/motion/tokens.ts
    - src/lib/motion/tokens.test.ts
    - src/components/icons/PROVENANCE.md
    - src/components/icons/iconContract.test.ts
  key_links:
    - "Design.md is the normative source 03-03 and 03-04 implement against; a token value wrong here propagates silently into theme.css."
    - "src/lib/motion/tokens.ts is read by src/utils/motion.ts and by the panel/sheet transitions in 03-03/03-09; the lockstep test is the only thing keeping CSS and TS from drifting."
    - "src/components/icons/PROVENANCE.md is read by iconContract.test.ts — the evidence file IS the gate's input, not documentation beside it."
  backstops:
    - statement: "The three CountriesIRL-only surfaces Themely has no analog for (colour swatch grid, legend editor rows, saved-map row anatomy) are specified in Design.md following the nearest Themely recipe and marked [FOR REVIEW], so what was decided at Claude's discretion is visible rather than buried."
      verification: backstop
---

<objective>
Author the design contract every later plan implements against: CountriesIRL's own `Design.md`,
the motion token mirror with its lockstep gate, and the vendored animated icon set with per-file
dated provenance.

Purpose: `03-01` delivered the pinned dependency and the font bytes — both cheaply reversible.
This plan is where the commitment becomes irreversible, so it opens with the D-01 one-way gate and
then turns that commitment into a written, testable contract. Everything from `03-03` onward reads
`Design.md` as normative — a token value wrong here propagates into `theme.css` and then into every
surface without anything failing.

Output: `Design.md`, `src/lib/motion/tokens.ts` + its lockstep test, and
`src/components/icons/` with `PROVENANCE.md` and `iconContract.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/export.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-CONTEXT.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-UI-SPEC.md
@.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-01-SUMMARY.md

Read narrowly:
- `03-RESEARCH.md` § Pattern 5, § Pattern 6, § P-5, § P-6
- `03-RESEARCH.md` § Code Examples → "the vendored animated-icon shape" (lines ~1537-1574)

Sibling repo, read-only, absolute paths (outside this repository — never write there):
- `/Users/matul/claudeprojects/themely/Design.md`
- `/Users/matul/claudeprojects/themely/src/app/globals.css` (`:root` 173-258, `.dark` 319-357)
- `/Users/matul/claudeprojects/themely/src/lib/motion/tokens.ts`
- `/Users/matul/claudeprojects/themely/src/lib/motion/__tests__/tokens.test.ts`
- `/Users/matul/claudeprojects/themely/src/components/ui/search.tsx`
- `/Users/matul/claudeprojects/themely/src/components/ui/{message-square-more,sparkles,square-pen}.tsx`
- `/Users/matul/claudeprojects/themely/.planning/coding-rules/frontend.md` § Vendored lucide-animated Icons
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Artifact | Kind | Notes |
|---|---|---|
| `Design.md` (repo root) | doc | CountriesIRL's own design contract, derived from Themely's |
| `src/lib/motion/tokens.ts` | module | `EASE_OUT`, `EASE_SNAPPY`, `EASE_IN`, `DURATION_FAST`, `DURATION_BASE`, `DURATION_SLOW`, `SCENE_CROSSFADE_DURATION_MS`, `CAMERA_MOTION_DURATION_MS` |
| `src/lib/motion/tokens.test.ts` | test | assertion 7 — CSS ↔ TS lockstep, `node` env |
| `src/components/icons/*.tsx` | modules | vendored animated icons (inventory in Task 3) |
| `src/components/icons/index.ts` | module | barrel export of every icon component + its `*IconHandle` type |
| `src/components/icons/PROVENANCE.md` | evidence | R-V2 per-file dated review lines |
| `src/components/icons/iconContract.test.ts` | test | assertions 22 + 28 |
| six `--motion-*` declarations | tokens | added to `theme.css`'s existing `:root` additively; `03-04` reconciles the retired names |
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: One-way commitment gate — adopting the Themely design system (D-01)</name>
  <decision>Confirm the one-way commitment to the Themely design system before `Design.md`, the motion contract, and the icon recipe are authored.</decision>
  <context>
D-01 is already the owner's explicit direction and this checkpoint does NOT reopen it. It exists
because D-01 is rated **one-way** in `03-CONTEXT.md`: every token name, the type scale, the icon
recipe, and the motion contract flow from it. Reversing after `03-04` lands means re-authoring
`theme.css`, `Design.md`, `uiContract.test.ts`, and every restyled surface — plus re-running every
contrast check.

**The gate sits here rather than in `03-01` because this is where the irreversible work is.**
`03-01` created a git tag, ran a throwaway spike, and installed a pinned package plus some font
bytes — all `costly` at worst, all undone with `npm uninstall` and a file deletion. Tasks 2, 3, and
4 of *this* plan author the contract that cannot be cheaply unwound. This is the last cheap moment.

What is being committed to, concretely:
- Token names use Themely's `--themely-*` namespace verbatim (D-03), which is also what discharges
  the ROADMAP's binding transition-readiness requirement.
- The palette is Themely cool, verbatim, no value adjusted (D-04).
- Apple Blue `#0071e3` is the sole saturated accent (D-05); the retired teal `#0f766e` family and
  the `--glass-*` family are DELETED, not aliased (D-06).
- A vendored icon surface enters a phase specced as "chrome, layout, and tokens only"
  (D-28 — Roadmap Amendment 3).
- CountriesIRL gets its own `Design.md`; there is **no cross-repo test dependency** (D-02). Drift
  from upstream Themely is accepted and handled by hand.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed as decided (D-01 stands)</name>
      <pros>Matches the owner's explicit direction; discharges transition-readiness by construction; the sibling repo is available and was read during research.</pros>
      <cons>One-way: the reversal cost lands on every surface Phase 3 touches.</cons>
    </option>
    <option id="hold">
      <name>Hold — the owner wants to re-examine before anything is authored</name>
      <pros>Near-zero sunk cost; only a git tag, a throwaway spike, a pinned package, and some font bytes exist so far, and the last two are trivially removable.</pros>
      <cons>Blocks the entire phase; 03-03 through 03-12 all implement against `Design.md`.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, or hold</resume-signal>
  <acceptance_criteria>
    - The owner's selection is recorded verbatim in the plan SUMMARY, with the date.
    - If `hold` is selected, Tasks 2, 3, and 4 do not run and the phase stops.
    - No file in `files_modified` is touched before the owner responds.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Motion tokens — TS mirror plus the CSS↔TS lockstep test (D-26, assertion 7)</name>
  <files>src/lib/motion/tokens.ts, src/lib/motion/tokens.test.ts, src/utils/motion.ts, src/styles/theme.css</files>
  <behavior>
    - Test 1: each of the six token names appears in `src/lib/motion/tokens.ts` AND is declared as a CSS custom property in `src/styles/theme.css`, and the two values are equal after normalisation (`cubic-bezier(a, b, c, d)` ↔ `[a,b,c,d]`; `150ms` ↔ `0.15`).
    - Test 2: the test asserts its own row count — the number of compared pairs equals the number of TS motion constants exported, so a token dropped from either layer fails rather than silently shrinking the comparison set.
    - Test 3: changing one value in one layer alone fails the test (the RED probe, executed below).
    - Test 4: `SCENE_CROSSFADE_DURATION_MS` is present and documented as deliberately local at 160ms — it is NOT retimed to 150ms.
    - Test 5: `CAMERA_MOTION_DURATION_MS` is DERIVED from `DURATION_BASE` rather than restating `240`.
  </behavior>
  <read_first>
    - `/Users/matul/claudeprojects/themely/src/lib/motion/tokens.ts` (full — the shape to mirror)
    - `/Users/matul/claudeprojects/themely/src/lib/motion/__tests__/tokens.test.ts` (full — the lockstep test shape)
    - `src/utils/motion.ts` and `src/utils/motion.test.ts` — the existing motion helpers, and where `CAMERA_MOTION_DURATION_MS` currently lives
    - `src/styles/theme.css` — the existing `--easing-camera`, `--easing-control`, `--motion-fast`, `--motion-camera` declarations and the `prefers-reduced-motion` block
    - `03-UI-SPEC.md` § Motion (D-26) — the token table with the "absorbs" column
    - `03-RESEARCH.md` § Pattern 5
  </read_first>
  <action>
Create `src/lib/motion/tokens.ts` as the TS mirror of the `--motion-*` CSS custom properties, in
Themely's shape: named exported constants, easings as 4-tuples `as const`, durations in seconds.

The seven values, per `03-UI-SPEC.md` § Motion:
`EASE_OUT` = `[0.22, 1, 0.36, 1]`; `EASE_SNAPPY` = `[0.2, 0.8, 0.2, 1]`;
`EASE_IN` = `[0.4, 0, 1, 1]`; `DURATION_FAST` = `0.15`; `DURATION_BASE` = `0.24`;
`DURATION_SLOW` = `0.36`; `SCENE_CROSSFADE_DURATION_MS` = `160`.

Derive `CAMERA_MOTION_DURATION_MS` from `DURATION_BASE` (`DURATION_BASE * 1000`) rather than
restating `240`, and re-point `src/utils/motion.ts` at the new module rather than keeping a second
copy of the number.

Three reconciliations to carry out deliberately, each with a one-line comment naming what it
absorbs so a later reader does not read a rename as a retime:
- `--easing-camera` and `--motion-fast` are already **byte-identical** to Themely's `EASE_OUT`
  and `DURATION_FAST`. Absorb them; do not declare a second token beside them.
- `--motion-camera` is byte-identical to `DURATION_BASE`. Absorb.
- `--easing-control: ease-out` maps onto `--motion-ease-snappy`, and this is **not** byte-equal.
  It is a deliberate retime of control micro-feedback (research assumption A8). Comment it as
  deliberate.
- `--motion-scene: 160ms` stays **local at 160ms** and is documented, not retimed to 150ms —
  Themely's "do-not-snap" idiom. Retiming the scene crossfade is a visible change nobody asked for.

Write `src/lib/motion/tokens.test.ts` as the lockstep gate. It runs in the `node` Vitest
environment (pure constants plus a file read — no DOM), which is why this shape fits this repo.
It reads `src/styles/theme.css` as text, extracts the `--motion-*` declarations, and compares each
against its TS constant after normalising units. **It asserts its own row count**: the number of
pairs compared must equal the number of exported motion constants, so a token dropped from either
side fails loudly instead of shrinking the comparison silently. Three motion tokens were once
declared, reduced-motion-gated, and read by nothing, so a gate that only iterates what it finds
proves nothing.

Also assert that **every** `--motion-*` token has a live consumer — a `var()` in a stylesheet or a
named read in `src/lib/motion/tokens.ts` / `src/utils/motion.ts` (this is assertion 6's TS half;
its CSS half lands in `03-04`).

**The CSS side does not fully exist yet.** `03-04` lands the full token rewrite. In this plan, add
the six `--motion-*` declarations to `theme.css`'s existing `:root` **additively, removing
nothing**, so the lockstep test is green here and `03-04` only has to reconcile the retired names.
If that ordering conflicts with `03-04`'s retired-token gate, the executor records the conflict
rather than deleting an assertion.

**RED PROBE (mandatory, and it is a task step, not advice).** After the test is green:
1. Copy `src/lib/motion/tokens.ts` to the session scratchpad. **Never `git checkout --` a file
   with uncommitted work** — two agents lost edits that way in one session.
2. Change `DURATION_BASE` from `0.24` to `0.25` in the TS layer only.
3. Run `npx vitest run src/lib/motion/tokens.test.ts` and OBSERVE IT FAIL. Capture the failure
   output.
4. Restore by copying the scratchpad file back.
5. Re-run and observe green.
6. Record the RED output in the SUMMARY.

A gate must be able to fail on the bug it covers. This repo has already shipped tests that could
not.
  </action>
  <verify>
    <automated>npx vitest run src/lib/motion/tokens.test.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/motion/tokens.ts','utf8');if(/CAMERA_MOTION_DURATION_MS\s*=\s*240/.test(s)){console.error('CAMERA_MOTION_DURATION_MS restates 240 instead of deriving from DURATION_BASE');process.exit(1)};console.log('DERIVED_OK')"</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    `src/lib/motion/tokens.ts` exports the seven constants plus a derived
    `CAMERA_MOTION_DURATION_MS`; `src/lib/motion/tokens.test.ts` passes, asserts its own row
    count, and asserts every `--motion-*` token has a consumer; the RED probe was executed and
    its failure output is recorded; `npm test` is green.
  </done>
  <acceptance_criteria>
    - `npx vitest run src/lib/motion/tokens.test.ts` passes.
    - The test file contains an explicit row-count assertion (the compared-pair count equals the
      exported-constant count).
    - `CAMERA_MOTION_DURATION_MS` is computed from `DURATION_BASE`, not written as a literal 240.
    - `SCENE_CROSSFADE_DURATION_MS` equals 160 and carries a comment saying it is deliberately
      local.
    - No existing `theme.css` declaration was removed by this task.
    - The SUMMARY contains the captured RED output from step 3 and confirms the restore was done
      by copying from the scratchpad, not by `git checkout --`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Vendor the animated icons with per-file dated provenance (D-28, R-V2, assertions 22 + 28)</name>
  <files>src/components/icons/*.tsx, src/components/icons/index.ts, src/components/icons/PROVENANCE.md, src/components/icons/iconContract.test.ts</files>
  <read_first>
    - `/Users/matul/claudeprojects/themely/src/components/ui/search.tsx` (full — the exemplar shape)
    - `/Users/matul/claudeprojects/themely/src/components/ui/message-square-more.tsx` (full — currently UNVETTED, see below)
    - `/Users/matul/claudeprojects/themely/src/components/ui/sparkles.tsx` (full — currently UNVETTED)
    - `/Users/matul/claudeprojects/themely/src/components/ui/square-pen.tsx` (full — currently UNVETTED)
    - `/Users/matul/claudeprojects/themely/.planning/coding-rules/frontend.md` § Vendored lucide-animated Icons — the full vendoring contract
    - `03-UI-SPEC.md` § Vendoring Safety and § Two planner requirements (R-V2)
    - `03-UI-SPEC.md` § Component Contracts 1 — the tool inventory naming each glyph
    - `03-RESEARCH.md` § Pattern 6 (translating out of Tailwind) and § P-5
  </read_first>
  <action>
Author the icon set under `src/components/icons/`. Every file carries the vendoring contract
intact: a `forwardRef` component **plus** a structurally identical `*IconHandle` exposing
`{ startAnimation, stopAnimation }` via `useImperativeHandle`; sizing via the **`size` prop**,
never className sizing; and the **`strokeWidth` 2→1.5 local patch with its marker comment**
re-applied.

**Glyph inventory** (from `03-UI-SPEC.md` § Vendoring Safety and § Component Contracts 1):
`palette`, `list`, `layers`, `folder`, `download`, `undo-2`, `redo-2`, `sun`, `moon`, `map`,
`check`, `plus`, `minus`, `crosshair`.

These are **authored in-repo from lucide glyph paths in the exemplar's shape** — no registry
install, no network at build or run time. `search.tsx` is the shape exemplar; read it, then write
CountriesIRL's own files. **Translate, never copy the Tailwind `className` strings** — this repo
has no Tailwind, and a copied `className` produces an unstyled element that *looks* wired up
(P-5). Sizing and colour come from the `size` prop and CSS custom properties.

**R-V2 — the provenance evidence, which is a task output, not a note.** Write
`src/components/icons/PROVENANCE.md`. For **every** file under `src/components/icons/`, record one
line in the exact evidence shape the three already-reviewed UI-SPEC rows use:
`{filename} — read in full — {no flags | flags listed} — {YYYY-MM-DD}`.

Scan each file for: `fetch(`, `XMLHttpRequest`, `navigator.sendBeacon`, `process.env`, `eval(`,
`Function(`, dynamic imports from external URLs, and obfuscated identifiers. Record what was
scanned for, not just the verdict.

**Close the three PENDING rows with evidence, not assumption.** `03-UI-SPEC.md` § Vendoring Safety
records `message-square-more.tsx`, `sparkles.tsx`, and `square-pen.tsx` as
*"PENDING — not reviewed this session; no claim is made about them."* Read each in full now. If a
glyph is taken from any of them, its provenance line records that read. If none is taken, record
that explicitly — `not vendored; read in full on {date}; {flags}` — so the PENDING rows close
either way. **A file with no provenance line may not be vendored.**

Write `src/components/icons/iconContract.test.ts` (`node` env, text-based — this repo's Vitest has
no DOM, so component behaviour is proven in Playwright, not here):

- **Assertion 22:** for every `*.tsx` under `src/components/icons/`, assert the file exports a
  `forwardRef` component, exports a `*IconHandle` type whose members are exactly
  `startAnimation` and `stopAnimation`, uses `useImperativeHandle`, accepts a `size` prop, and
  contains the `strokeWidth` marker comment with `strokeWidth={1.5}` (or the literal `1.5`).
- **Assertion 28:** assert every file under `src/components/icons/` has a provenance line in
  `PROVENANCE.md` matching the dated evidence shape, **and** that the recorded set EQUALS the
  file set — not a subset in either direction. A file with no line fails; a line with no file
  fails. That two-way equality is what stops the evidence set and the file set drifting apart.
- Assert **no className-based sizing**: no `h-\d`/`w-\d`/`size-\d` Tailwind-shaped token appears
  in any icon file (P-5).

**RED PROBES (mandatory, both of them, as task steps):**

*Probe A — assertion 22.* Copy one icon file to the scratchpad. Delete its `*IconHandle` export.
Run `npx vitest run src/components/icons/iconContract.test.ts`, OBSERVE RED, capture the output,
restore by copying the scratchpad file back, re-run green.

*Probe B — assertion 28.* Copy `PROVENANCE.md` to the scratchpad. Add a new icon file with no
provenance line (or delete one line). Run the test, OBSERVE RED, capture the output, restore both
by copying back, re-run green.

Never `git checkout --` a file with uncommitted work. Copy to the scratchpad and restore by
copying back.
  </action>
  <verify>
    <automated>npx vitest run src/components/icons/iconContract.test.ts</automated>
    <automated>node -e "const fs=require('fs');const files=fs.readdirSync('src/components/icons').filter(f=>f.endsWith('.tsx'));const prov=fs.readFileSync('src/components/icons/PROVENANCE.md','utf8');const missing=files.filter(f=>!prov.includes(f));if(missing.length){console.error('no provenance line for:',missing);process.exit(1)};console.log('PROVENANCE_OK',files.length)"</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    Every glyph in the inventory exists under `src/components/icons/` in the vendoring shape;
    `PROVENANCE.md` carries a dated `read in full` line for every file including an explicit
    disposition for the three previously-PENDING upstream files; `iconContract.test.ts` enforces
    assertions 22 and 28 with two-way set equality; both RED probes were executed and captured.
  </done>
  <acceptance_criteria>
    - `npx vitest run src/components/icons/iconContract.test.ts` passes.
    - The provenance-line count equals the `src/components/icons/*.tsx` file count, verified
      two-way by the test itself.
    - `PROVENANCE.md` names `message-square-more.tsx`, `sparkles.tsx`, and `square-pen.tsx`
      explicitly with a dated disposition — the three PENDING rows are closed with evidence.
    - No file under `src/components/icons/` contains a Tailwind-shaped sizing token.
    - Every icon file contains the strokeWidth marker comment and renders at 1.5.
    - The SUMMARY carries the captured RED output for BOTH probe A and probe B.
    - `npm run lint && npm test` green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Author CountriesIRL's own `Design.md` (D-01, D-02, D-03, D-09)</name>
  <files>Design.md, CLAUDE.md</files>
  <read_first>
    - `/Users/matul/claudeprojects/themely/Design.md` (full — §Tokens Colors, §Tokens Colors Dark Mode, §Tokens Typography, §Tokens Spacing & Shapes, §Tokens Motion, §Components, §Do's and Don'ts, §Surfaces, §Elevation, §Layout)
    - `/Users/matul/claudeprojects/themely/src/app/globals.css` — `:root` 173-258 and `.dark` 319-357. **Authoritative where it disagrees with Themely's Design.md.**
    - `03-UI-SPEC.md` § Design Tokens in full (lines ~90-415) — the vendored table, already reconciled
    - `03-UI-SPEC.md` § Component Contracts 6, 7, 8 (the three surfaces with no Themely analog)
    - `CLAUDE.md` § Documentation Routing (the `Design.md` row) and § Update Process
  </read_first>
  <action>
Create `Design.md` at the repo root. `CLAUDE.md` already anticipates it
(*"Does not exist yet. Create it when Phase 3 visual polish starts."*) — update that row in the
same commit so the routing table stops describing it as absent.

Structure and content:

**1. Upstream attribution (D-02).** Name `/Users/matul/claudeprojects/themely/Design.md` and
`themely/src/app/globals.css` as upstream, with the read date. State plainly that there is **no
cross-repo test dependency**: CountriesIRL is localhost-only with bundled assets, so the repo
stays self-contained and drift from upstream is accepted and handled by hand.

**2. The token table, vendored verbatim (D-03, D-04).** Colour light `:root` and dark `.dark`,
copied from `03-UI-SPEC.md` § Design Tokens — which has already reconciled Themely's Design.md
against `globals.css`. **No value may be adjusted.** Include the `--themely-*` namespace rule and
say what it discharges: the ROADMAP's binding transition-readiness requirement that new tokens
coexist with a host app's stylesheet, so a host's `globals.css` can *become* the token source and
our declarations act as fallbacks.

**3. The mode-invariant (fixed) token set — the export firewall.** Reproduce
`03-UI-SPEC.md`'s table and its reason: Live Invariant 9 outranks Themely's "tokens flip
themselves" rule for this family. Include `--map-border-focus`'s re-tone to Apple Blue's light
value, `--map-fixed-text` staying fixed, the two new `--map-frame-*` tokens, and
`--accent-fill` / `--accent-fill-hover` with the contrast table (4.70:1 vs 3.02:1 vs 4.18:1) and
the standing instruction not to "simplify" `--accent-fill` back onto the flipping token.

**4. Typography (D-09, D-10).** The ten-role scale with size / line-height / weight / tracking,
the Inter self-hosted rule and fallback stack, `tabular-nums` for numeric columns, the **chrome
subset** (four roles only), the **near-size adjacency rule** for the 280px panel, and the
**closed** consumer exemption of exactly `--text-display` and `--text-stat` with the reason
commented. Record that the four retired `--font-*` tokens and `--weight-regular` /
`--weight-semibold` are deleted, not aliased.

**5. Spacing, radii, elevation, motion.** The 8-point scale, the four radii plus
`--radius-row: 10px`, the flat-with-hairlines model (`--hairline`, `--popover-shadow`,
`--dialog-shadow`) with `--shadow-inspector` / `--shadow-navigation` / the whole `--glass-*`
family **deleted, not aliased**, the outright ban on `backdrop-filter`, and the motion token set
from Task 2.

**6. The accent budget (D-05).** Apple Blue is one thing per surface. Reproduce the per-surface
table. Record the global `:focus-visible` exemption explicitly so it cannot silently widen to
hover or active states, and record that `--themely-red` is the only second semantic colour.

**7. CountriesIRL-only anatomy — the Claude's-Discretion surfaces, surfaced here for review.**
Themely has no analog for these, so `03-CONTEXT.md` § Claude's Discretion directs that they be
specified here following the nearest Themely recipe and surfaced for review. Carry
`03-UI-SPEC.md` § Component Contracts 6/7/8 into `Design.md`: the colour swatch grid (derived grid
tracks, tile anatomy, the deleted `--active-check-*` and `--mixed-color-*` replacements), the
legend editor rows, the saved-map row anatomy, plus the map's own `--map-*` surfaces, the HUD/rail
anatomy, the legend chrome, and the tooltip. Mark this section **[FOR REVIEW]** so the owner can
see what was decided at discretion rather than dictated.

**8. The export-unsafe-CSS reason, rewritten for post-D-34.** The Phase 2 wording
(*"html2canvas rasterises it differently than the browser paints it"*) **goes false the moment
D-34 lands**. Carry the corrected reason from `03-UI-SPEC.md` § Elevation: in an SVG-as-image
isolated document an externally-styled effect renders **not at all**, which is a harder failure
than a rasterizer mismatch, and the only signal is a PNG that quietly lost an effect the editor
shows on screen.

**9. Do's and Don'ts**, translated: hover paint is instant on rows and tiles; nav text/icon colour
is constant across inactive/hover/active; no per-component dark overrides — tokens flip
themselves; `--themely-on-accent` stays `#ffffff` in both modes; no positional CSS selector styles
an interactive element; recipes are **translated** from Tailwind, never copied.

Add the `Last updated` line per `CLAUDE.md` § Update Process, and keep only the two most recent
entries (rule 3 is real and has been violated before).

**Do not** re-derive or "improve" any Themely value. Where this file and `03-UI-SPEC.md` disagree,
the approved UI-SPEC wins and the disagreement is reported, not silently resolved.
  </action>
  <verify>
    <automated>test -f Design.md && node -e "const s=require('fs').readFileSync('Design.md','utf8');const need=['--themely-platinum','--themely-apple-blue','--accent-fill','--map-border-focus','--text-body-sm','--radius-row','--motion-ease-out','--hairline','themely/Design.md'];const miss=need.filter(t=>!s.includes(t));if(miss.length){console.error('Design.md missing:',miss);process.exit(1)};console.log('DESIGN_OK')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('Design.md','utf8');for(const [tok,val] of [['--themely-platinum','#ffffff'],['--themely-porcelain','#f8fafd'],['--themely-powder','#e5edf5'],['--themely-apple-blue','#0071e3'],['--themely-midnight-ink','#061b31'],['--themely-nav-ink','#0d0d0d'],['--themely-stone-gray','#d8d6df'],['--themely-red','#ff5252']]){if(!s.includes(val)){console.error('missing verbatim value for',tok,val);process.exit(1)}};console.log('PALETTE_VERBATIM_OK')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('CLAUDE.md','utf8');if(/Design\.md\`[^|]*\|[^|]*\*\*Does not exist yet\*\*/.test(s)){console.error('CLAUDE.md still says Design.md does not exist');process.exit(1)};console.log('ROUTING_UPDATED')"</automated>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <done>
    `Design.md` exists at the repo root carrying all nine sections; every light and dark palette
    value matches Themely verbatim; the mode-invariant set, the accent budget with its
    `:focus-visible` exemption, the closed type-role exemption, and the post-D-34 export-unsafe
    reason are all present; the CountriesIRL-only anatomy section is marked `[FOR REVIEW]`;
    `CLAUDE.md`'s routing row no longer says the file does not exist.
  </done>
  <acceptance_criteria>
    - Every light-mode and dark-mode colour value in `Design.md` matches `03-UI-SPEC.md`
      § Design Tokens character for character.
    - `Design.md` states there is no cross-repo test dependency on Themely (D-02).
    - `Design.md` contains the `--accent-fill` contrast table with all three ratios and the
      instruction not to point it at the flipping token.
    - The type-role consumer exemption in `Design.md` names exactly `--text-display` and
      `--text-stat` and states that the list is closed.
    - The export-unsafe-CSS reason in `Design.md` does NOT claim html2canvas rasterises
      differently; it states the isolated-document reason.
    - The CountriesIRL-only anatomy section carries a `[FOR REVIEW]` marker.
    - `CLAUDE.md`'s `Design.md` row is updated in the same commit.
    - `Design.md` has at most two `Last updated` entries.
  </acceptance_criteria>
  <reversibility rating="one-way">D-01. Every token name, the type scale, the icon recipe, and the motion contract flow from this document; reverting means re-authoring `theme.css`, `Design.md`, the contract test, and every restyled surface. The owner confirmed this commitment at Task 1 of this plan, immediately before this work.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sibling repo (`themely/`) → `src/components/icons/` | Source files copied across a repository boundary with no shared CI |
| upstream Design.md → CountriesIRL's contract | A mis-transcribed token value propagates silently into every surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering | vendored icon files from the sibling repo | medium | mitigate | R-V2 per-file dated `read in full` provenance scanning for `fetch(`, `XMLHttpRequest`, `sendBeacon`, `process.env`, `eval(`, `Function(`, external dynamic imports, obfuscated identifiers; two-way set equality enforced by assertion 28 and RED-proven |
| T-03-63 | Tampering | a Tailwind `className` copied instead of translated, producing an element that looks wired up | medium | mitigate | Assertion 22 plus a scan forbidding Tailwind-shaped sizing tokens in any icon file (P-5) |
| T-03-64 | Repudiation | a motion token declared, reduced-motion-gated, and read by nothing | medium | mitigate | The lockstep test asserts its own row count and asserts every `--motion-*` token has a live consumer; RED-proven by changing one layer alone |
| T-03-65 | Tampering | a mis-transcribed palette value entering the design contract | high | mitigate | Verbatim value checks against `03-UI-SPEC.md` in the verify block; the UI-SPEC wins over `Design.md` on any disagreement and the disagreement is reported, not silently resolved |
</threat_model>

<verification>
```bash
npm run lint
npm test
npm run build
npx vitest run src/lib/motion/tokens.test.ts src/components/icons/iconContract.test.ts
```

Then confirm by hand that `Design.md` § the CountriesIRL-only anatomy section is marked
`[FOR REVIEW]` and that the SUMMARY lists its discretion items for the owner to skim.

Chrome only where a browser is involved. Edge is not installed (D-33).
</verification>

<success_criteria>
- [ ] `src/lib/motion/tokens.ts` + `tokens.test.ts` enforce the CSS↔TS lockstep with a self-asserting row count (assertion 7, RED-proven)
- [ ] `CAMERA_MOTION_DURATION_MS` derives from `DURATION_BASE`; `--motion-scene` stays local at 160ms; the three absorbed tokens carry their deliberate-reconciliation comments
- [ ] Every vendored icon satisfies the forwardRef + `*IconHandle` + `size`-prop + strokeWidth-marker contract (assertion 22, RED-proven)
- [ ] Evidence and files are two-way equal under `src/components/icons/`, and the three previously PENDING upstream files carry a dated disposition (assertion 28 / R-V2, RED-proven)
- [ ] `Design.md` exists with all nine sections and matches `03-UI-SPEC.md` values character for character; `CLAUDE.md`'s routing row is updated in the same commit
- [ ] Three RED probes executed and captured; every restore was done by copying from the scratchpad, never `git checkout --`
- [ ] `npm run lint && npm test && npm run build` clean
</success_criteria>

<output>
Create `.planning/phases/03-clean-ui-overhaul-1-1-5-weeks/03-02-SUMMARY.md` when done.

It must carry, as named sections: `RED probes (3, with output)` and
`Design.md discretion items [FOR REVIEW]`.

**Do not run `state.advance-plan`, `state.update-progress`, or `roadmap.update-plan-progress`.**
Edit `.planning/STATE.md` and `.planning/ROADMAP.md` by hand.
</output>
