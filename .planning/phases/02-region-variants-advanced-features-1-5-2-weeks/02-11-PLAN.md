---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "11"
type: execute
wave: 3
depends_on: ["02-03", "02-10"]
files_modified:
  - src/components/LegendDisclosure.tsx
  - src/components/LegendEditor.tsx
  - src/components/LegendOverlay.tsx
  - src/components/LegendEditor.test.tsx
  - tests/e2e/legend.spec.ts
  - tests/e2e/fixtures/legend.html
autonomous: true
requirements: [F4.1, F4.2, F4.3, F4.4, F4.5, F5.2, NFR11]
must_haves:
  truths:
    - "LegendOverlay renders only a React-owned SVG `<g>` payload for the canonical MapCanvas legend slot; it never creates a sibling/second SVG and remains outside the camera transform (D-10)."
    - "Creators can edit/order/style/place the complete legend with drag and equivalent controls (D-24, D-25, D-26, D-27)."
    - "Real Chrome proves drag propagation, keyboard reorder/nudge, focus retention, and export-blocking semantics."
  artifacts:
    - path: src/components/LegendOverlay.tsx
      provides: "Export-safe SVG legend"
    - path: src/components/LegendEditor.tsx
      provides: "Accessible semantic controls"
    - path: tests/e2e/legend.spec.ts
      provides: "Focused Chrome legend slice"
  key_links:
    - from: src/components/LegendEditor.tsx
      to: src/providers/CompositionStateProvider.tsx
      via: "typed legend commands"
      pattern: "legend"
---

<objective>
Build the editable export-safe legend and prove browser-only interactions in the owning plan.

Purpose: Turn every effective non-white color, including active historical colors, into a complete accessible composition layer.
Output: Disclosure, editor, SVG overlay, tests, and focused Chrome evidence.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/export.md
@src/utils/legend.ts
@src/providers/CompositionStateProvider.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement legend disclosure and editor semantics</name>
  <files>src/components/LegendDisclosure.tsx, src/components/LegendEditor.tsx, src/components/LegendEditor.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md sections 12–13; src/utils/legend.ts; src/hooks/useCompositionState.ts</read_first>
  <behavior>- Collapsed exact summaries and empty state.
- 32-char labels, commit/restore behavior, Move Up/Down and Alt+Arrow semantics.
- Exact theme/size/opacity/border/corner sets and custom nudge controls.
- >30/non-fitting labels block export and no entry is omitted.</behavior>
  <action>Implement validated drafts and provider-owned semantic commands. Limit Node tests to helpers/static semantics; real focus/reorder/drag claims belong to Task 3.</action>
  <verify><automated>npm test -- src/components/LegendEditor.test.tsx src/utils/legend.test.ts && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>Exact copy/native semantics/style sets/validation pass.</acceptance_criteria>
  <done>The semantic legend editor is green.</done>
  <commit>feat(2-legend): add accessible legend editing</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the export-safe SVG legend and pointer movement</name>
  <files>src/components/LegendOverlay.tsx, src/components/LegendEditor.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; .planning/coding-rules/export.md; 02-UI-SPEC.md sections 12–14; src/utils/legend.ts</read_first>
  <behavior>- LegendOverlay returns one top-level `<g data-layer="legend">` intended for MapCanvas's typed slot; it renders no `<svg>`, HTML wrapper, portal target, or sibling overlay.
- Visible styling uses only SVG rect/text/swatch primitives.
- Editor hit targets/handles/focus are data-editor-only.
- Pointer movement clamps and stops camera propagation.
- Canonical 1080 geometry matches the pure model.</behavior>
  <action>Render deterministic SVG geometry as a group-only component, with `aria-hidden` visual text, editor-only hit area, clamped pointer drag, and no prohibited effects/foreignObject/CSS transforms. The group is the exact live/export representation that Plan 02-18 inserts after the D3 camera group inside MapCanvas; do not add a standalone/sibling SVG fallback.</action>
  <verify><automated>npm test -- src/components/LegendEditor.test.tsx src/utils/legend.test.ts && npm run build</automated></verify>
  <acceptance_criteria>Tests prove a group-only root, deterministic primitives, clamping/editor-only markup, and absence of any `<svg>`, HTML wrapper, or sibling-overlay fallback.</acceptance_criteria>
  <done>Live and exported legend share one deterministic representation.</done>
  <commit>feat(2-legend): render the in-canvas SVG legend</commit>
</task>

<task type="auto">
  <name>Task 3: Prove legend focus, drag, reorder, and blocking in Chrome</name>
  <files>tests/e2e/legend.spec.ts, tests/e2e/fixtures/legend.html</files>
  <read_first>.planning/coding-rules/general.md; 02-REVIEWS.md browser-allocation concern; playwright.config.ts; src/components/LegendEditor.tsx; src/components/LegendOverlay.tsx</read_first>
  <action>Create a Vite-served legend fixture and Chrome tests for label commit/Escape, Move/Alt+Arrow focus retention, pointer reorder/position without camera propagation, corner/nudge equivalence, exact style controls, historical-color-derived entries, >30/non-fitting export block, and editor-only handles.</action>
  <verify><automated>npm run test:e2e -- --project=chrome --grep "legend"</automated></verify>
  <acceptance_criteria>All real-browser legend interaction cases pass with no skipped tests.</acceptance_criteria>
  <done>Legend interaction acceptance is not deferred to the final aggregate.</done>
  <commit>test(2-legend): verify browser legend editing</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| stored/user legend text/style → DOM/SVG | Persisted content is untrusted |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-24 | Elevation of Privilege | labels | mitigate | Text nodes only and 32-char cap. |
| T-02-25 | Tampering | styles | mitigate | Enum/range commands only. |
| T-02-26 | Denial of Service | layout | mitigate | 30-entry cap and deterministic wrap. |
</threat_model>
<verification>Legend Node tests, Chrome slice, lint, TypeScript, and build pass.</verification>
<success_criteria>The legend is complete, accessible, export-safe, and backed by real-browser interaction evidence.</success_criteria>
<execution_notes>Do not change branches. Commit each task atomically.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-11-SUMMARY.md` when done.</output>
