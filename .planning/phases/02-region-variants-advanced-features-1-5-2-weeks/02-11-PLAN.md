---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "11"
type: execute
wave: 3
depends_on: ["02-03", "02-10"]
files_modified:
  - src/components/LegendDisclosure.tsx
  - src/components/LegendEditor.tsx
  - src/components/LegendOverlay.tsx
  - src/components/LegendEditor.test.tsx
autonomous: true
requirements: [F4.1, F4.2, F4.3, F4.4, F4.5, F5.2, NFR11]
must_haves:
  truths:
    - "The visible legend is an SVG overlay outside the camera transform and occupies no map viewport space (D-10)."
    - "Creators can edit labels/order/style and use corners, drag, nudge buttons, or keyboard movement (D-24–D-27)."
    - "Every active color is represented or export is visibly blocked; editor handles never enter export (D-08, D-27)."
  artifacts:
    - path: src/components/LegendOverlay.tsx
      provides: "Export-safe SVG legend and editor-only move target"
    - path: src/components/LegendEditor.tsx
      provides: "Accessible semantic legend controls"
    - path: src/components/LegendDisclosure.tsx
      provides: "Collapsed progressive-disclosure entry"
  key_links:
    - from: src/components/LegendEditor.tsx
      to: src/providers/CompositionStateProvider.tsx
      via: "typed legend commands"
      pattern: "legend"
---

<objective>
Build the approved editable in-canvas legend using export-safe SVG primitives and fully equivalent accessible controls.

Purpose: Turn map colors into a polished, editable composition layer without creating a freeform design editor.
Output: Disclosure, semantic editor, SVG overlay, and component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/export.md
@src/utils/legend.ts
@src/providers/CompositionStateProvider.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify legend disclosure and editor semantics</name>
  <files>src/components/LegendEditor.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md sections 12–13 and copywriting; 02-PATTERNS.md P11/P14; src/utils/legend.test.ts; src/components/ColorPicker.test.tsx</read_first>
  <behavior>- Disclosure is collapsed by default with exact empty/populated summaries.
- Entry rows expose swatch, 32-char label draft/count, Move Up/Down, and Alt+Arrow semantics with focus retention.
- Empty labels and non-fitting labels retain prior canvas value and visibly block export.
- Exactly Light/Dark/Soft, Small/Medium/Large, 70–100 opacity step 5, None/Hairline/Strong, and four corners are offered.
- Custom position exposes four nudge buttons and keyboard nudge semantics.
- >30 active colors shows exact blocking error.</behavior>
  <action>Write static semantic and pure helper tests. Assert exact copy, native controls, aria-expanded/controls/keyshortcuts, disabled empty state, and no HTML/CSS effect options.</action>
  <verify><automated>npm test -- src/components/LegendEditor.test.tsx src/utils/legend.test.ts</automated></verify>
  <acceptance_criteria>Tests fully cover UI-SPEC legend states before implementation; no snapshot test or subjective visual assertion is used.</acceptance_criteria>
  <done>The legend interaction contract is executable.</done>
  <commit>test(2-legend): specify legend editor semantics</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement disclosure and semantic legend editor</name>
  <files>src/components/LegendDisclosure.tsx, src/components/LegendEditor.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; src/components/LegendEditor.test.tsx; src/utils/legend.ts; src/hooks/useCompositionState.ts</read_first>
  <action>Implement local validated label drafts that commit on blur/Enter and restore on Escape, deterministic reorder commands with live announcements/focus retention, exact radio/range controls, corner and custom nudge actions, and collapsed disclosure behavior. Keep labels as React text/input values, never HTML. Keep panel state presentation-local while semantic legend state remains provider-owned.</action>
  <verify><automated>npm test -- src/components/LegendEditor.test.tsx && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>All tests pass; every drag/reorder/move operation has a visible click/tap alternative; exact style options and copy match UI-SPEC.</acceptance_criteria>
  <done>Creators can fully edit the legend without relying on drag or color alone.</done>
  <commit>feat(2-legend): add accessible legend editor</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render and drag the export-safe SVG legend</name>
  <files>src/components/LegendOverlay.tsx, src/components/LegendEditor.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; .planning/coding-rules/export.md; 02-UI-SPEC.md sections 12–14; src/utils/legend.ts; src/types/composition.ts</read_first>
  <action>Render a React-owned SVG `<g data-layer="legend">` using only `rect`, `text`, and swatch primitives in canonical 1080 units. Place it after the D3 camera group, apply measured/clamped top-left coordinates, render deterministic columns/two-line text, and stop pointer propagation during whole-hit-area drag. Mark hit target, handles, and focus outlines `data-editor-only`; set editor legend text `aria-hidden` because the semantic panel owns accessibility. Never use filter, blur, shadow, foreignObject, external font, or CSS transform.</action>
  <verify><automated>npm test -- src/components/LegendEditor.test.tsx src/utils/legend.test.ts && npm run build</automated></verify>
  <acceptance_criteria>Markup contains the legend layer outside camera ownership, export-safe primitives only, exact canonical geometry, and editor-only drag affordances; camera callbacks are not invoked by legend drag.</acceptance_criteria>
  <done>The live and exported legend share one deterministic SVG representation.</done>
  <commit>feat(2-legend): render the in-canvas SVG legend</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| stored/user legend text/style → DOM/SVG | Persisted content is untrusted |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-24 | Elevation of Privilege | legend labels | mitigate | React text nodes/SVG text only; max 32 chars; no HTML sinks. |
| T-02-25 | Tampering | legend style | mitigate | Enum/range commands only; no raw CSS strings or unsupported effects. |
| T-02-26 | Denial of Service | legend size | mitigate | 30-entry cap and deterministic wrapping/export block. |
</threat_model>

<verification>
Focused legend tests, lint, strict TypeScript, build, and full unit suite pass.
</verification>

<success_criteria>
The legend is editable, accessible, export-safe, camera-independent, bounded, and never silently drops active colors.
</success_criteria>

<execution_notes>Do not change branches. Commit test, editor, and overlay tasks separately. Preserve D3/React ownership boundaries.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-11-SUMMARY.md` when done.</output>
