---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "30"
type: execute
wave: 12
depends_on: ["02-18", "02-21", "02-29"]
files_modified:
  - src/hooks/useCompositionExportTransaction.ts
  - src/hooks/useCompositionExportTransaction.test.tsx
autonomous: true
requirements: [F5.1, F5.2, F5.3, F5.5, NFR4, NFR11]
must_haves:
  truths:
    - "Export resolves the currently bound sole MapCanvasHandle, acquires its CameraFreezeLease, and captures its connected one-SVG export source containing the live in-SVG legend."
    - "Lease release occurs in the outermost finally on success, legend block, clone/canvas/blob/URL/anchor/callback failure, and thrown status/focus code; repeated release is harmless."
    - "After every path, camera input and later save/remount use the frozen semantic camera rather than stale state."
  artifacts:
    - path: src/hooks/useCompositionExportTransaction.ts
      provides: "Exact live-camera export transaction and outermost lease cleanup"
    - path: src/hooks/useCompositionExportTransaction.test.tsx
      provides: "Success/failure/release/save/remount ordering tests"
  key_links:
    - from: src/hooks/useCompositionExportTransaction.ts
      to: src/hooks/useCameraController.ts
      via: "current-handle accessor, CameraFreezeLease, and connected export-source access"
      pattern: "getMapCanvasHandle|freezeAndSnapshot|getExportSource|release"
---

<objective>
Extract the exact live-camera export transaction into a focused tested hook.

Purpose: Guarantee semantic settlement and exact-once-effective camera release across every path while App remains a composition root.
Output: Export orchestration hook and exhaustive lifecycle tests.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/export.md
@src/hooks/useCameraController.ts
@src/utils/export.ts
@src/components/MapCanvas.tsx
@src/components/LegendOverlay.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement and prove the complete export lease transaction</name>
  <files>src/hooks/useCompositionExportTransaction.ts, src/hooks/useCompositionExportTransaction.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/export.md; 02-REVIEWS.md live-camera lifecycle; Plan 02-21 export utility contract; camera/scene/legend/provider interfaces</read_first>
  <behavior>- Synchronous activation lock, resolve the currently rebound sole MapCanvasHandle, then lease acquisition from that handle.
- Frozen camera is synchronously committed to composition before clone/capture and is the state seen by immediate save/responsive remount.
- Selected scene finalizes and outgoing becomes inaccessible/removed; legend validates/measures before capture; `getExportSource()` must return one connected div containing exactly one canonical SVG with the exact live legend after the camera group and all visual wrapped geography.
- Legend-block and every injected throw/failure release lease from one outermost finally and clear busy/activation lock.
- Success/failure is followed by working drag/wheel/Locate/Reset and live-camera save.
- Double release does not double-unlock or throw.</behavior>
  <action>Implement the hook with injected `getMapCanvasHandle(): MapCanvasHandle | null` and one outer `try/finally` that encloses all post-lease work and caller callbacks. Resolve the handle at activation rather than caching it, acquire the lease and export source from that same visible handle, and reject missing/disconnected/multiple-SVG/no-legend/sibling-legend sources before capture while still releasing the lease. The export utility remains responsible for clone/canvas/download resources; this hook owns camera lease, semantic settlement, selected-scene/legend preparation, busy lock, creator-safe outcome, and focus-on-legend-error. Test capture, invalid legend, source missing/disconnected/multiple SVG, missing or sibling legend, changed live legend, html2canvas, blob, URL, anchor click, handoff, status callback, stale pre-remount handle, and remount callback failures. Assert release ordering and subsequent save uses frozen camera.</action>
  <verify><automated>npm test -- src/hooks/useCompositionExportTransaction.test.tsx src/utils/export.test.ts src/hooks/useCompositionSaveTransaction.test.tsx && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>All success/failure/remount/save-during-animation/post-release input cases pass; only the rebound visible handle is used, the exact live legend and all visual wrapped geography reach the clone, and no branch exits with an active lease/busy lock.</acceptance_criteria>
  <done>The export transaction is complete, idempotently releasable, and App-ready.</done>
  <commit>feat(2-transactions): add exact export orchestration</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| live camera/DOM callbacks → export lifecycle | Throws can strand input or stale semantic state |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-78 | Denial of Service | camera lock | mitigate | One outermost finally and idempotent lease release. |
| T-02-79 | Tampering | frozen semantic state | mitigate | Commit exact lease camera before capture/save/remount. |
</threat_model>
<verification>Export/save transaction tests, export utility tests, lint, TypeScript pass.</verification>
<success_criteria>Every export path releases camera and preserves the exact frozen composition state.</success_criteria>
<execution_notes>Do not change branches. App wiring is Plan 23.</execution_notes>
<output>Create `02-30-SUMMARY.md` when done.</output>
