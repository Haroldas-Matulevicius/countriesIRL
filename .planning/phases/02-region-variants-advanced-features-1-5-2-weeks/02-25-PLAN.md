---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "25"
type: execute
wave: 11
depends_on: ["02-24"]
files_modified:
  - .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CODING-RULES-PROPOSAL.patch
autonomous: false
requirements: [F2.1, F2.2, F4.2, F5.2, F6.1, NFR11]
must_haves:
  truths:
    - "Durable coding-rule changes are captured as exact reviewable bytes before engine documents are modified, as required by CLAUDE.md (D-28, D-29)."
    - "The user approves the complete proposal artifact itself, not a prose summary of stale azimuthal, complete-per-period, cloud-sync, and map-only export corrections."
  artifacts:
    - path: .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CODING-RULES-PROPOSAL.patch
      provides: "Canonical unified diff for all five proposed engine-document corrections"
  key_links:
    - from: .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CODING-RULES-PROPOSAL.patch
      to: .planning/coding-rules/frontend.md
      via: "exact applicable unified-diff hunk"
      pattern: "diff --git a/.planning/coding-rules/frontend.md b/.planning/coding-rules/frontend.md"
    - from: .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CODING-RULES-PROPOSAL.patch
      to: .planning/CODING_RULES.md
      via: "exact applicable unified-diff hunk"
      pattern: "diff --git a/.planning/CODING_RULES.md b/.planning/CODING_RULES.md"
---

<objective>
Create and obtain repository-required human approval for the exact durable coding-rule patch before applying it.

Purpose: Keep engine documentation trustworthy and comply with CLAUDE.md's manual-maintenance policy without modifying coding-rule sources during review.
Output: A committed, byte-exact unified diff plus explicit approval or requested corrections for that exact artifact.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@CLAUDE.md
@.planning/CODING_RULES.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/data.md
@.planning/coding-rules/export.md
@.planning/coding-rules/storage.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Materialize the exact coding-rule proposal without touching engine sources</name>
  <files>.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CODING-RULES-PROPOSAL.patch</files>
  <read_first>CLAUDE.md Update Process; current CODING_RULES.md and all four subsystem rules; 02-RESEARCH.md Deprecated/outdated; 02-PATTERNS.md Planner Warnings; completed Plan 02-24 summary and implemented camera/data/export/storage summaries</read_first>
  <action>Create `02-CODING-RULES-PROPOSAL.patch` as the complete canonical unified diff for only `.planning/CODING_RULES.md`, `frontend.md`, `data.md`, `export.md`, and `storage.md`, without modifying those five files in the current worktree. Copy the current five files into `.git/02-rules-proposal-before/` and a separate `.git/02-rules-proposal-after/` tree while preserving repository-relative paths, edit only the after copies, and generate the artifact in the exact target-path order by appending `diff --git a/{path} b/{path}` plus `diff -u --label a/{path} --label b/{path}` output for each before/after pair; accept only diff exit 1 for an intended change and fail on any other exit. This algorithm works whether the source documents are tracked or untracked. Keep the before copies through automated verification, then remove both disposable trees. The patch must encode the exact wrapped fixed-Mercator/D3 ownership rules, world manifest and reviewed partial-overlay/modern-fallback/provenance rules including pole coordinates, current camera/scene/legend export transaction plus safe named filenames and unchanged connected-anchor cleanup, V2 complete local compositions with bounded validation and in-memory V1 migration, index routing updates, and each file's exact 2026-07-24 two-entry footer hygiene. Remove only contradictory future guidance and preserve unrelated Phase 1 contracts. Do not edit, stage, or commit any coding-rule source file in this task.</action>
  <verify><automated>trap 'rm -rf .git/02-rules-proposal-before .git/02-rules-proposal-after' EXIT; for path in .planning/CODING_RULES.md .planning/coding-rules/frontend.md .planning/coding-rules/data.md .planning/coding-rules/export.md .planning/coding-rules/storage.md; do cmp -s ".git/02-rules-proposal-before/$path" "$path" || exit 1; done && git apply --check --whitespace=error-all .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CODING-RULES-PROPOSAL.patch && npm run lint && npm test && npm run build</automated></verify>
  <acceptance_criteria>The artifact contains exact hunks for all five and only those five source files, applies cleanly to the current tree, has no whitespace errors, and byte comparison with the isolated before copies proves the current coding-rule sources remain unchanged while the product gate is green.</acceptance_criteria>
  <done>A concrete exact patch is committed for human review before any engine-document source modification.</done>
  <commit>docs(2-rules): propose exact Phase 2 rule corrections</commit>
</task>
<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Approve the exact proposal artifact</name>
  <files>none</files>
  <read_first>CLAUDE.md Update Process; .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CODING-RULES-PROPOSAL.patch; Task 1 commit and SHA-256</read_first>
  <action>Display the entire raw contents of `02-CODING-RULES-PROPOSAL.patch` to the user without truncation, paraphrase, summary substitution, or hidden hunks, together with its SHA-256. Ask the user to approve those exact displayed bytes. Plan 26 remains blocked if the user requests any wording change; revise and recommit the proposal artifact, redisplay the complete revised artifact and new SHA-256, and obtain approval before continuing.</action>
  <decision>Whether the exact displayed unified diff may be applied byte-for-byte to the five coding-rule source files.</decision>
  <context>CLAUDE.md requires exact proposed edits to be reviewed before manually maintained engine documents are changed. Approval of a summary is insufficient; the complete patch and its hash are the review object.</context>
  <options>
    <option id="approve"><name>Approve the displayed patch bytes</name><pros>Authorizes Plan 26 to apply exactly the displayed artifact and nothing else.</pros><cons>Any later wording change requires a new proposal artifact and approval.</cons></option>
    <option id="revise"><name>Request exact revisions</name><pros>Changes the concrete artifact before any engine source is touched.</pros><cons>Blocks Plan 26 until the revised full artifact is displayed and approved.</cons></option>
  </options>
  <verify><automated>git apply --check --whitespace=error-all .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CODING-RULES-PROPOSAL.patch && sha256sum .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CODING-RULES-PROPOSAL.patch</automated></verify>
  <acceptance_criteria>The user has seen the complete exact artifact and explicitly approves its recorded SHA-256, or supplies exact requested revisions that trigger regeneration and redisplay; no coding-rule source has changed.</acceptance_criteria>
  <done>The exact proposal bytes—not a summary—are human-reviewed before modification.</done>
  <resume-signal>Select `approve` and quote the displayed SHA-256, or select `revise` with exact corrections.</resume-signal>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| implementation patterns → proposal artifact | Incorrect or incomplete exact text could cause future regressions |
| proposal artifact → human approval | A summarized review could conceal unintended hunks |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-60 | Tampering | coding-rule proposal | mitigate | Generate one canonical applicable patch, keep source files unchanged, display every byte and bind approval to SHA-256. |
</threat_model>
<verification>The canonical patch applies cleanly, source rules remain unchanged, the full artifact and hash are displayed, and the human decision is recorded.</verification>
<success_criteria>Plan 26 has explicit authorization for one exact committed patch and no coding-rule source was modified during proposal review.</success_criteria>
<execution_notes>Do not change branches. Task 1 may create and commit only the proposal artifact. Task 2 modifies no files and cannot accept approval of a summary.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-25-SUMMARY.md` when done, recording the approved proposal path, commit, and SHA-256.</output>
