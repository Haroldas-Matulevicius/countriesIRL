---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "33"
type: execute
wave: 5
depends_on: ["02-31", "02-32"]
files_modified:
  - sources/historical/1492.source-approval.json
  - sources/historical/1700.source-approval.json
  - sources/historical/1815.source-approval.json
  - sources/historical/1914.source-approval.json
autonomous: false
requirements: [F2.1, F2.3, F2.4, NFR8]
must_haves:
  truths:
    - "A named human rights/source reviewer who is explicitly neither executor nor implementer approves every snapshot's six-region local evidence bundle before geometry generation."
    - "Approval exists as one canonical machine-validated source-approval JSON per snapshot; SUMMARY prose alone never unlocks Plans 13–17, and any changed bound byte invalidates approval."
  artifacts:
    - path: .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-31-SUMMARY.md
      provides: "1492/1700 readiness hashes/status"
    - path: sources/historical/1492.source-approval.json
      provides: "Durable named/non-executor/six-region source approval bound to exact manifest/archive/member/input/mode hashes"
    - path: sources/historical/1914.source-approval.json
      provides: "Durable 1914 source approval under the same exact contract"
  key_links:
    - from: sources/historical/{id}.source-approval.json
      to: geometry plans 02-13 through 02-16
      via: "mandatory CLI validation of current durable approval bytes"
      pattern: "sourceApprovalSha256|validate-source-approval"
---

<objective>
Obtain non-automated approval of the exact source/license/tracing readiness bundles before curation.

Purpose: Prevent engineering from self-certifying rights or treating unavailable source material as delivery.
Output: Explicit per-snapshot review matrix followed by four canonical durable source-approval JSON artifacts, or blocking corrections.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-31-SUMMARY.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-32-SUMMARY.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-VALIDATION.md
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve exact historical source and license readiness</name>
  <files>none</files>
  <read_first>All four source manifests/evidence archive listings/input hashes; Plan 31/32 summaries; 02-VALIDATION.md source gate</read_first>
  <action>Review each snapshot separately and each of its six regional records. Confirm source item identity, exact rights/license/attribution, source-manifest SHA-256, canonical evidence-archive SHA-256, sorted member-path/member-SHA-256 inventory and inventory SHA-256, input/tracing-geometry SHA-256, uncertainty, and exact vector-extraction/manual-trace mode. For manual tracing, confirm separate evidence/procedure/operator/control-point hashes and operator identity/date; do not require or accept a fabricated regeneration command. Record reviewer full identity, role, review date, explicit confirmation that the reviewer is neither executor nor implementer, and PASS/FAIL plus observations for each snapshot and region. Any missing/uncertain rights, hash mismatch, executor/implementer reviewer, or merged region decision is FAIL/BLOCKED.</action>
  <what-built>Four offline source/license/tracing readiness bundles.</what-built>
  <how-to-verify>Inspect manifests and archive inventories/hashes; compare rights statements to source item pages; verify all six regional records and input hashes. Approve only snapshots whose every region is PASS.</how-to-verify>
  <verify><automated>for id in 1492 1700 1815 1914; do node scripts/prepareHistoricalSnapshot.mjs --snapshot "$id" --sources "sources/historical/$id.sources.json" --validate-sources || exit 1; done</automated></verify>
  <acceptance_criteria>Named reviewer/role/date/non-executor/non-implementer status and 24 regional source decisions are returned against the exact manifest/archive/member/input/mode hashes; all must PASS before Task 2. BLOCKED is never interpreted as delivered.</acceptance_criteria>
  <done>Only rights/source-approved snapshots may proceed to geometry generation.</done>
  <resume-signal>Provide reviewer full name, role, date, explicit non-executor/non-implementer confirmation, and PASS/FAIL with observations and exact bound hashes for each snapshot and six region IDs.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Materialize and validate four durable source-approval artifacts</name>
  <files>sources/historical/1492.source-approval.json, sources/historical/1700.source-approval.json, sources/historical/1815.source-approval.json, sources/historical/1914.source-approval.json</files>
  <read_first>Task 1 complete structured reviewer payload; all four current source manifests, canonical evidence ZIPs/member inventories, and input geometries; scripts/prepareHistoricalSnapshot.mjs; historical validation tests</read_first>
  <action>Transcribe the Task 1 payload exactly into one canonical source-approval JSON per snapshot. Each artifact must contain reviewer identity, role, review date, explicit `isExecutor:false` and `isImplementer:false`, six separate regional source/license decisions and observations, source-manifest SHA-256, canonical evidence-archive SHA-256, canonical member-inventory SHA-256 plus every normalized member path/hash, input/tracing-geometry SHA-256, declared `vector-extraction` or `manual-trace` mode, and mode-specific extraction-specification hash or manual evidence/procedure/operator/control-point hashes. Do not infer, summarize, merge, or repair reviewer decisions. Reject executor/implementer/Claude/Codex identity, any non-PASS region, any mismatch to current bytes, or any noncanonical archive/member inventory. Serialize canonical sorted JSON; these files, not SUMMARY prose, are the only downstream source approval.</action>
  <verify><automated>for id in 1492 1700 1815 1914; do node scripts/prepareHistoricalSnapshot.mjs --snapshot "$id" --sources "sources/historical/$id.sources.json" --input "sources/historical/$id.input.geojson" --source-approval "sources/historical/$id.source-approval.json" --validate-source-approval || exit 1; done && npm test -- src/utils/historicalValidation.test.ts src/utils/historicalPreparationCli.test.ts</automated></verify>
  <acceptance_criteria>All four canonical artifacts validate current reviewer/non-executor status, 24 decisions, manifest/archive/member/input/mode hashes, and manual-trace hashes where applicable; changing any bound byte causes nonzero validation.</acceptance_criteria>
  <done>Plans 13–16 can proceed only through the four current durable source approvals.</done>
  <commit>docs(2-history): seal historical source approvals</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| automated source bundle → rights claim | Tooling cannot establish legal/factual sufficiency alone |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-71 | Spoofing | rights approval | mitigate | Named role/date, explicit non-executor/non-implementer status, per-region decisions, and canonical durable JSON. |
| T-02-81 | Tampering | source approval bytes | mitigate | Current manifest/archive/member/input/mode hash comparison and downstream source-approval SHA chaining. |
</threat_model>
<verification>All source-readiness checks remain green, every mandatory human cell is PASS, and all four durable source-approval validation commands exit 0.</verification>
<success_criteria>Geometry generation starts only from current machine-validated durable source-approval JSON, never SUMMARY prose.</success_criteria>
<execution_notes>No product/source file changes. Unavailable or failed evidence blocks; it does not reduce scope.</execution_notes>
<output>Create `02-33-SUMMARY.md` with reviewer matrix plus exact source-approval paths and SHA-256 values; the JSON artifacts remain authoritative.</output>
