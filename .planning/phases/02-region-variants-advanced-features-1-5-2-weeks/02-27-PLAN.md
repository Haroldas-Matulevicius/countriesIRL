---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "27"
type: execute
wave: 13
depends_on: ["02-23", "02-24", "02-26"]
files_modified:
  - tests/e2e/phase2-composition.spec.ts
autonomous: true
requirements: [F1.1, F1.2, F1.3, F1.4, F1.5, F1.6, F2.1, F2.2, F2.3, F2.4, F2.5, F3.1, F3.2, F3.3, F3.4, F3.5, F4.1, F4.2, F4.3, F4.4, F4.5, F5.1, F5.2, F5.3, F5.5, F6.1, F6.2, F7.1, F7.2, F7.3, NFR1, NFR2, NFR3, NFR4, NFR5, NFR6, NFR7, NFR8, NFR9, NFR10, NFR11]
must_haves:
  truths:
    - "Chrome and Edge automation proves the unified camera, selection/history, reviewed snapshots, legend, persistence, responsive DOM, and exact PNG workflows together (D-01–D-29)."
    - "Warm historical switching completes under 500ms and failures remain truthful; timing is not fabricated from cold acquisition or skipped samples (NFR3)."
    - "Exported PNGs are exact 1080x1080 opaque viewport/legend compositions with no editor state and valid cleanup (D-07–D-11)."
  artifacts:
    - path: tests/e2e/phase2-composition.spec.ts
      provides: "Full installed Chrome/Edge Phase 2 browser regression suite"
  key_links:
    - from: tests/e2e/phase2-composition.spec.ts
      to: playwright.config.ts
      via: "installed chrome/msedge projects"
      pattern: "chrome|msedge"
---

<objective>
Complete the automated installed-browser acceptance suite and run the authoritative product/data/build gate.

Purpose: Prove the full Phase 2 implementation as an integrated local browser product before manual physical-touch and visual approval.
Output: Complete Chrome/Edge E2E coverage and green source-scoped gate.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-VALIDATION.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@CLAUDE.md
@.planning/coding-rules/general.md
@tests/e2e/phase2-composition.spec.ts
@playwright.config.ts
@package.json
</context>
<tasks>
<task type="auto">
  <name>Task 1: Complete Chrome and Edge world-composition E2E coverage</name>
  <files>tests/e2e/phase2-composition.spec.ts</files>
  <read_first>.planning/coding-rules/general.md; 02-VALIDATION.md requirement map; 02-UI-SPEC.md Implementation Acceptance Checklist; current E2E file; all Plan 02-23/24 summaries</read_first>
  <action>Add real non-skipped tests for: one world/no region selector; whole-world initial view; drag/wheel pointer anchor, wrap, pole clamp, drag-not-select; zoom/pan controls; Locate and Reset View independence; one logical accessible country and decorative copies; 195 core interaction; all five periods/fallback/tooltips/color continuity/crossfade/reduced motion; warm cached switch under 500ms; legend derivation/edit/order/styles/corner/custom/nudge/overflow block; V1/V2 save/load/dirty/delete/focus/responsive remount; 360px/200% containment; offline-after-load; export normal/Pacific/during Locate/during crossfade with exact PNG dimensions/opacity/content/sanitization/filename; clean console/runtime/product network. Use installed channels only and deterministic waits on states/events, not arbitrary sleeps.</action>
  <verify><automated>npm run test:e2e</automated></verify>
  <acceptance_criteria>Every test passes independently in configured Chrome and Edge; zero skipped/todo cases; downloaded files are inspected and removed by test cleanup; no remote product request occurs.</acceptance_criteria>
  <done>The complete automated browser contract is green.</done>
  <commit>test(2-e2e): verify the world composition workflow</commit>
</task>
<task type="auto">
  <name>Task 2: Run the immutable verification-only Phase 2 gate</name>
  <files>none</files>
  <read_first>.planning/coding-rules/general.md; 02-VALIDATION.md Sampling Rate and Sign-Off; package.json; all deterministic preparation scripts; current git status; summaries for Plans 02-23 through 02-26</read_first>
  <action>Verification only: run source-scoped lint, full unit tests, strict app/node TypeScript, deterministic world and all four snapshot checks, production build, and full Chrome/Edge E2E against the exact tree produced before this task. Do not edit product code, tests, assets, scripts, configuration, or coding rules in this task. On any failure, stop immediately and require an explicit gap-closure plan that records the failing command/assertion, owning files and tests, prerequisite/dependent plans, and focused/full rerun commands. The gap plan—not this final gate—owns any correction. If a future correction changes a durable pattern or any coding-rule contract, rerun Plans 02-25 and 02-26 before restarting this immutable final gate. Never weaken assertions, counts, hashes, NFR3, accessibility, exact PNG rules, or delete evidence.</action>
  <verify><automated>npm run lint && npm test && npm exec tsc -- -b --pretty false && npm run data:world:check && for id in 1492 1700 1815 1914; do node scripts/prepareHistoricalSnapshot.mjs --snapshot "$id" --sources "sources/historical/$id.sources.json" --input "sources/historical/$id.input.geojson" --output "public/data/snapshots/$id.geojson" --review-output "data/historical-reviewed/$id.review.json" --review-html "data/historical-reviewed/$id.review.html" --check || exit 1; done && npm run build && npm run test:e2e</automated></verify>
  <acceptance_criteria>Every command exits 0 without any file mutation or inline correction; source tests remain source-scoped; exact counts/hashes/approvals and browser scope remain truthful. A failure produces no PASS and can proceed only through a named gap-closure plan.</acceptance_criteria>
  <done>Phase 2 is automation-complete on an immutable green tree and ready for human-only verification.</done>
  <commit>none — immutable verification-only gate</commit>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser downloads/console/network → acceptance | Evidence may be stale or incomplete |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-62 | Repudiation | browser evidence | mitigate | Run both installed channels from current build with deterministic assertions. |
| T-02-63 | Tampering | gate remediation | mitigate | Final gate is read/execute-only; any failure stops for an explicit owning-file/test/dependency gap-closure plan. |
| T-02-64 | Denial of Service | browser artifacts | mitigate | Isolated output/download cleanup and bounded deterministic waits. |
</threat_model>
<verification>The exact full command chain exits zero from the current implementation.</verification>
<success_criteria>All automated Phase 2 requirements and locked decisions are proven in source, asset, build, Chrome, and Edge gates.</success_criteria>
<execution_notes>Do not change branches. Preserve all evidence. Task 1 may commit only its E2E coverage; Task 2 is immutable verification-only and makes no commit or correction.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-27-SUMMARY.md` when done.</output>
