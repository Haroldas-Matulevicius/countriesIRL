---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "27"
type: execute
wave: 17
depends_on: ["02-23", "02-24", "02-26", "02-36"]
files_modified:
  - tests/e2e/final-integration.spec.ts
  - scripts/verifyPhase2ExactCommit.mjs
  - .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-27-EXACT-COMMIT.json
autonomous: true
requirements: [F1.1, F1.2, F1.3, F1.4, F1.5, F1.6, F2.1, F2.2, F2.3, F2.4, F2.5, F3.1, F3.2, F3.3, F3.4, F3.5, F4.1, F4.2, F4.3, F4.4, F4.5, F5.1, F5.2, F5.3, F5.5, F6.1, F6.2, F7.1, F7.2, F7.3, NFR1, NFR2, NFR3, NFR4, NFR5, NFR6, NFR7, NFR8, NFR9, NFR10, NFR11]
must_haves:
  truths:
    - "Focused Chrome slices already passed in owning plans; final gate aggregates every domain in installed Chrome and Edge."
    - "The authoritative gate runs after the final E2E commit in a detached clean worktree at exact recorded HEAD with fresh `npm ci`."
    - "No dirty/untracked file outside that worktree can influence lint/test/type/data/build/browser results."
    - "Evidence records exact verified SHA, commands, browser versions, approval/asset hashes, and cleanup; Plan 28 binds to the same SHA."
  artifacts:
    - path: scripts/verifyPhase2ExactCommit.mjs
      provides: "Safe detached-worktree exact-commit gate"
    - path: .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-27-EXACT-COMMIT.json
      provides: "Machine-readable verified SHA/evidence"
    - path: tests/e2e/final-integration.spec.ts
      provides: "Cross-domain Chrome/Edge integration cases"
  key_links:
    - from: 02-27-EXACT-COMMIT.json
      to: Plan 02-28
      via: "verifiedSha acceptance binding"
      pattern: "verifiedSha"
---

<objective>
Complete cross-domain E2E coverage, then prove the exact final implementation commit from a fresh detached clean worktree.

Purpose: Replace the dirty-worktree gate with the accepted Phase 1 exact-commit evidence pattern.
Output: Final integration spec, reusable safe gate script, and exact verified-SHA evidence.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-VALIDATION.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@CLAUDE.md
@playwright.config.ts
@package.json
@tests/e2e
</context>

<tasks>
<task type="auto">
  <name>Task 1: Complete cross-domain E2E and exact-commit gate tooling</name>
  <files>tests/e2e/final-integration.spec.ts, scripts/verifyPhase2ExactCommit.mjs</files>
  <read_first>.planning/coding-rules/general.md; 02-VALIDATION.md; 02-REVIEWS.md final-gate/browser concerns; all focused E2E specs; package/playwright config; Phase 1 exact-commit summary pattern</read_first>
  <action>Add only missing cross-domain integration cases; do not duplicate focused specs. Assert one world/no region selector, 195 logical/248 visible baseline, historical entity click/keyboard/history/persistence/legend, full transaction lifecycle, responsive/offline/accessibility/clean console/network, and downloaded PNG content. Create a Node ESM gate script that accepts an explicit SHA/evidence path, refuses dirty SHA ambiguity, creates a unique temp directory outside the repository, runs `git worktree add --detach` at that SHA, executes `npm ci`, lint, full source tests, strict TypeScript, world check, all four approval-aware offline historical checks, build, and full `npm run test:e2e` (both installed projects), captures versions/results/hashes, then removes/prunes the worktree in outermost finally without broad clean. It must never copy files from the current workspace into the gate worktree.</action>
  <verify><automated>npm run test:e2e -- --project=chrome && node scripts/verifyPhase2ExactCommit.mjs --help</automated></verify>
  <acceptance_criteria>Chrome aggregate is green, no active 57-path assertion exists, script exposes exact SHA/evidence arguments and safe cleanup, and no skipped/todo cases remain.</acceptance_criteria>
  <done>Final E2E/tooling is committed; its resulting HEAD is the candidate exact SHA.</done>
  <commit>test(2-e2e): complete Phase 2 exact-commit validation</commit>
</task>

<task type="auto">
  <name>Task 2: Verify the final E2E commit in a fresh detached worktree</name>
  <files>.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-27-EXACT-COMMIT.json</files>
  <read_first>Task 1 commit/HEAD; exact gate script; package lock; all historical approvals; current git status</read_first>
  <action>Record `verifiedSha=$(git rev-parse HEAD)` immediately after Task 1 commit. Run the exact gate script with that SHA and the evidence JSON output path. The script must create the detached clean worktree, run fresh `npm ci` and the entire lint/test/type/world/history/build/Chrome/Edge chain inside it, record SHA/Node/npm/browser versions/commands/exit status/world hash/four public overlay hashes/four approval hashes/NFR3 sample evidence references/PNG artifact hashes, and safely remove/prune the worktree. After success, inspect only the generated JSON schema/hash references; do not rerun against or copy from the dirty workspace. Commit the evidence JSON as documentation after the code SHA has been verified. Any failure creates no PASS/evidence commit and requires a named gap plan.</action>
  <verify><automated>node scripts/verifyPhase2ExactCommit.mjs --sha "$(git rev-parse HEAD)" --evidence .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-27-EXACT-COMMIT.json</automated></verify>
  <acceptance_criteria>JSON says PASS for exact Task 1 SHA, includes fresh npm ci and all gates/browsers/hashes, records successful worktree cleanup, and cannot include files absent from that SHA.</acceptance_criteria>
  <done>Phase 2 automated evidence is bound to one exact clean commit.</done>
  <commit>docs(2-gate): record exact Phase 2 verified commit</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| current workspace → acceptance gate | Dirty/untracked files must not influence evidence |
| clean worktree output → evidence JSON | Results/hashes must bind exact SHA |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-62 | Repudiation | browser evidence | mitigate | Both installed channels and machine-readable results. |
| T-02-63 | Tampering | exact gate | mitigate | Detached SHA, npm ci, no workspace copy, full cleanup. |
| T-02-64 | Denial of Service | temp worktree/artifacts | mitigate | Unique temp root and outermost-finally removal/prune. |
</threat_model>
<verification>Exact detached-worktree gate exits 0 and evidence JSON records PASS for the candidate SHA.</verification>
<success_criteria>All automated Phase 2 claims are reproducible from the exact committed revision alone.</success_criteria>
<execution_notes>Do not change branches. Task 2 is verification-only except the evidence JSON/docs commit; product corrections require gap closure.</execution_notes>
<output>Create `02-27-SUMMARY.md` recording exact verified SHA and evidence path/hash.</output>
