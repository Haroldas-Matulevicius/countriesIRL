---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/map.ts
  - src/types/composition.ts
  - src/types/ui.ts
  - src/constants/camera.ts
  - src/constants/snapshots.ts
autonomous: true
requirements: [F2.1, F2.2, F2.4, F3.1, F3.2, F3.3, F3.4, F3.5, F4.1, F4.2, F4.3, F4.4, F4.5, F6.1, F6.2, F7.1, F7.2, F7.3, NFR9, NFR11]
must_haves:
  truths:
    - "The application models one semantic wrapped-world camera, never Europe/World/North America modes (D-01–D-06)."
    - "Camera, historical snapshot, legend, and visible scene settings form a complete composition separate from color history (D-18, D-19, D-29)."
    - "Historical identities and fallback modes are explicit rather than inferred from names (D-20–D-22)."
    - "Legend state supports one entry per active non-white color and only bounded style enums/numbers (D-24–D-27)."
  artifacts:
    - path: src/types/composition.ts
      provides: "Camera, scene, snapshot, legend, composition, and saved-record contracts"
    - path: src/constants/camera.ts
      provides: "Named camera limits and initial world fit"
    - path: src/constants/snapshots.ts
      provides: "Manifest and review-status constants"
  key_links:
    - from: src/types/composition.ts
      to: src/types/map.ts
      via: "stable CountryId and ColorMap references"
      pattern: "CountryId|ColorMap"
---

<objective>
Define the interface-first semantic contracts every Phase 2 subsystem implements.

Purpose: Prevent camera, history, legend, persistence, data, and export plans from inventing incompatible shapes.
Output: Strict readonly composition contracts and named camera/snapshot constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@CLAUDE.md
@.planning/CODING_RULES.md
@.planning/coding-rules/general.md
@.planning/coding-rules/data.md
@src/types/map.ts
@src/types/ui.ts
</context>

<interfaces>
Existing contracts to preserve: `CountryId = string`, sparse uppercase `ColorMap`, `MapState` color history/selection, discriminated `GeoDataState`, `StorageResult`, and `ExportResult`. New composition contracts must reference these rather than duplicate them.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Define scene and complete-composition contracts</name>
  <files>src/types/map.ts, src/types/composition.ts, src/types/ui.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-CONTEXT.md D-01–D-29; 02-RESEARCH.md Runtime State Inventory and Architecture Patterns; 02-PATTERNS.md P1; src/types/map.ts; src/types/ui.ts</read_first>
  <action>Create readonly types for `CameraState`, `CameraTransformSnapshot`, `SceneFeature`, `BoundaryMode`, `SnapshotManifestEntry`, `SnapshotLoadState`, `LegendEntryState`, `LegendState`, `CompositionState`, `SavedCompositionV2`, and coordinated load/freeze results. Extend `GeoFeature` only with fields required at trusted scene boundaries; keep source geometry identity, logical `entityId`, and `colorOwnerId` distinct per D-12–D-14 and D-22. Extend warning/error unions for legacy migration, unsupported version, unavailable snapshot, invalid composition, and export validation. Do not place camera/snapshot/legend in `MapState` or `MapAction` per D-18.</action>
  <verify><automated>npm exec tsc -- -p tsconfig.app.json --noEmit && npm run lint</automated></verify>
  <acceptance_criteria>No `any`, mutable collection, raw CSS string, raw geometry persistence, region-mode enum, or nullable partial composition exists; all expected failures are discriminated.</acceptance_criteria>
  <done>Downstream plans can implement against one strict semantic composition contract.</done>
  <commit>feat(2-contracts): define world composition interfaces</commit>
</task>

<task type="auto">
  <name>Task 2: Lock camera and snapshot constants</name>
  <files>src/constants/camera.ts, src/constants/snapshots.ts</files>
  <read_first>.planning/coding-rules/general.md; 02-UI-SPEC.md sections 4.4, 9, 10, 12, and 13; 02-RESEARCH.md Patterns 1–4 and Historical Snapshot Strategy; src/constants/config.ts</read_first>
  <action>Define `WORLD_SIZE=1080`, `MIN_ZOOM=1`, `MAX_ZOOM=24`, Mercator latitude `85.05112878`, click distance `4`, zoom factor `1.5`, pan fraction `0.125`, Locate padding `0.12`, Locate min zoom `2`, camera motion `240`, scene crossfade `160`, and initial camera `{zoom:1, centerLongitude:0, centerLatitude:0}`. Define modern ID, manifest URL, allowed review statuses, candidate IDs/labels `1492`, `1700`, `1815`, `1914`, and cache bounds. Use constants as typed readonly values, not duplicated literals. Record that max zoom is locked for implementation but remains subject to final browser acceptance.</action>
  <verify><automated>npm exec tsc -- -p tsconfig.app.json --noEmit && npm run lint</automated></verify>
  <acceptance_criteria>Constants exactly match the approved UI contract; no azimuthal projection, region selector, animation timeline, or external runtime URL is introduced.</acceptance_criteria>
  <done>Camera and snapshot implementations share one named invariant set.</done>
  <commit>feat(2-contracts): add camera and snapshot constants</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| unknown JSON/localStorage → typed contracts | Persisted and bundled values are attacker-editable |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-03 | Tampering | composition dictionaries | mitigate | Typed safe-key contracts; validators later reject `__proto__`, `constructor`, and `prototype`. |
| T-02-04 | Denial of Service | camera/legend values | mitigate | Named finite bounds, enum styles, label/entry caps, and no persisted geometry. |
</threat_model>

<verification>
Strict application TypeScript and lint pass; source search finds no separate region-mode contract and no composition fields inside color history.
</verification>

<success_criteria>
Every later plan imports the same camera, scene, historical, legend, storage, and export types without redefining them.
</success_criteria>

<execution_notes>Do not change branches. Read `general.md` first. Commit each task separately and preserve all unrelated evidence.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-02-SUMMARY.md` when done.</output>
