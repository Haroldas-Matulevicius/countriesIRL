---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/map.ts
  - src/types/composition.ts
  - src/types/ui.ts
  - src/constants/camera.ts
  - src/constants/snapshots.ts
autonomous: true
requirements: [F2.1, F2.2, F2.4, F3.1, F3.2, F3.3, F3.4, F3.5, F4.1, F4.2, F4.3, F4.4, F4.5, F6.1, F6.2, F7.1, F7.2, F7.3, NFR9, NFR11]
must_haves:
  truths:
    - "The application models one semantic wrapped-world camera, never region modes (D-01–D-06)."
    - "Exactly one shared `MapCanvasHandle` bridges the visible canvas to root composition; it owns live camera reads/freezes/view actions/focus and connected export-source access without constructing a second controller (D-06–D-09, D-16–D-19)."
    - "The 195-core browser/Locate catalog remains modern, while approved historical entities with distinct stable IDs are selectable/colorable only in the active effective scene; dependency/disputed/neutral units remain non-selectable (D-12–D-16, D-22)."
    - "Camera, snapshot, legend, and visible settings form a complete composition separate from color history (D-18, D-19, D-29)."
  artifacts:
    - path: src/types/composition.ts
      provides: "Single MapCanvasHandle, camera lease/read contracts, scene interaction policy, legend, composition, persistence, and snapshot types"
    - path: src/constants/camera.ts
      provides: "Named camera limits and initial world fit"
    - path: src/constants/snapshots.ts
      provides: "Manifest and review-status constants"
  key_links:
    - from: src/types/composition.ts
      to: src/types/map.ts
      via: "stable CountryId/ColorMap and effective-scene entity references"
      pattern: "CountryId|ColorMap|CameraFreezeLease|MapCanvasHandle"
---

<objective>
Define the interface-first semantic contracts every Phase 2 subsystem implements, including the complete live-camera transaction and historical-entity policy.

Purpose: Prevent camera, history, scene interaction, persistence, and export plans from inventing incompatible lifecycle or identity rules.
Output: Strict readonly contracts and named camera/snapshot constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@CLAUDE.md
@.planning/CODING_RULES.md
@.planning/coding-rules/general.md
@.planning/coding-rules/data.md
@src/types/map.ts
@src/types/ui.ts
</context>

<interfaces>
Preserve `CountryId`, sparse uppercase `ColorMap`, color history/selection, discriminated data/storage/export outcomes. Add `CameraFreezeLease { camera: CameraState; release(): void }` and exactly one exported `MapCanvasHandle` with `readCurrentCamera`, `freezeAndSnapshot`, `resetView`, `locate`, `restore`, logical-country `focusCountry`, and `getExportSource(): HTMLDivElement | null`. This handle is the only root-facing camera/canvas bridge; `useCameraController` remains MapCanvas-internal. Preserve explicit effective-scene selectability without duplicating the 195-core modern catalog.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Define the single canvas handle, effective-scene, and complete-composition contracts</name>
  <files>src/types/map.ts, src/types/composition.ts, src/types/ui.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-CONTEXT.md D-01–D-29; 02-REVIEWS.md camera and historical-entity concerns; 02-RESEARCH.md Runtime State Inventory and Pattern 5; src/types/map.ts; src/types/ui.ts</read_first>
  <action>Create readonly types for `CameraState`, `CameraFreezeLease`, and exactly one root-facing `MapCanvasHandle` exposing `readCurrentCamera()`, `freezeAndSnapshot()`, `resetView()`, `locate()`, `restore()`, `focusCountry()`, and connected `getExportSource()` access. Do not export the internal hook/controller or define parallel camera-controller interfaces. Also define `SceneFeature`, `BoundaryMode`, stable scene/entity/color-owner identity, snapshot/source-approval/factual-approval records, legend/composition/saved-record/load outcomes. Lock the reviewer-recommended interaction policy: the browser/Locate catalog contains exactly the modern 195 core states; curator-approved historical entities with distinct stable IDs set `isSelectable:true` while active and participate in map click, keyboard selection, color history, persistence, and legend derivation; clear dependencies inherit but are not independently selectable; disputed/indeterminate/neutral units remain non-selectable. Period reconciliation retains selection only for IDs in the incoming effective scene. Define `CameraFreezeLease.release()` as idempotent and require outermost-finally release by transaction owners. Define the export source as the existing connected `HTMLDivElement` containing the one canonical map SVG, preserving the Phase 1 export utility boundary. Keep camera/snapshot/legend out of `MapState`/`MapAction` per D-18.</action>
  <verify><automated>npm exec tsc -- -p tsconfig.app.json --noEmit && npm run lint</automated></verify>
  <acceptance_criteria>No `any`, mutable collection, region-mode enum, nullable partial composition, raw geometry persistence, second modern catalog, second camera-controller contract, or alternate export-source ref exists; the one MapCanvasHandle and historical selectability are unambiguous.</acceptance_criteria>
  <done>Downstream plans share one complete transaction and historical-interaction contract.</done>
  <commit>feat(2-contracts): define world composition transactions</commit>
</task>

<task type="auto">
  <name>Task 2: Lock camera and snapshot constants</name>
  <files>src/constants/camera.ts, src/constants/snapshots.ts</files>
  <read_first>.planning/coding-rules/general.md; 02-UI-SPEC.md sections 4.4, 9, 10, 12, and 13; 02-RESEARCH.md Patterns 1–4 and Historical Snapshot Strategy; src/constants/config.ts</read_first>
  <action>Define `WORLD_SIZE=1080`, `MIN_ZOOM=1`, `MAX_ZOOM=24`, Mercator latitude `85.05112878`, click distance `4`, zoom factor `1.5`, pan fraction `0.125`, Locate padding `0.12`, Locate min zoom `2`, camera motion `240`, scene crossfade `160`, and initial camera `{zoom:1, centerLongitude:0, centerLatitude:0}`. Define Modern plus candidate IDs/labels 1492/1700/1815/1914, manifest URL, cache bounds, allowed review statuses, and exact six region IDs. Do not duplicate labels or create a continuous slider.</action>
  <verify><automated>npm exec tsc -- -p tsconfig.app.json --noEmit && npm run lint</automated></verify>
  <acceptance_criteria>Constants exactly match UI-SPEC; no azimuthal projection, region selector, timeline, or external runtime URL exists.</acceptance_criteria>
  <done>Camera, snapshot, and evidence implementations share one named invariant set.</done>
  <commit>feat(2-contracts): add camera and snapshot constants</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| unknown JSON/localStorage → typed contracts | Persisted/bundled values are attacker-editable |
| live D3 camera → save/export transaction | Visible frame can differ from committed semantic state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-03 | Tampering | composition dictionaries | mitigate | Safe-key contracts and bounded validators. |
| T-02-04 | Denial of Service | camera lease | mitigate | Idempotent release and explicit outermost-finally ownership. |
| T-02-05 | Spoofing | historical selectability | mitigate | Curator-authored stable IDs and explicit `isSelectable`; no name inference. |
</threat_model>

<verification>
Strict TypeScript/lint pass; searches find no region-mode contract, camera history fields, or ambiguous historical selection rule.
</verification>

<success_criteria>
Every later plan imports the same MapCanvasHandle, camera lease/read, effective-scene identity, persistence, legend, and export contracts.
</success_criteria>

<execution_notes>Do not change branches. Read general.md first. Commit each task separately.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-02-SUMMARY.md` when done.</output>
