---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "26"
type: execute
wave: 12
depends_on: ["02-25"]
files_modified:
  - .planning/CODING_RULES.md
  - .planning/coding-rules/frontend.md
  - .planning/coding-rules/data.md
  - .planning/coding-rules/export.md
  - .planning/coding-rules/storage.md
autonomous: true
requirements: [F2.1, F2.2, F4.2, F5.2, F6.1, F6.2, NFR11]
must_haves:
  truths:
    - "The five engine documents differ from their pre-Plan-26 state by exactly the user-approved proposal bytes and no hand-edited additions (D-28, D-29)."
    - "Approved engine documents describe the actual wrapped camera, partial historical fallback, complete composition storage, and exact legend-inclusive export (D-01, D-07–D-11, D-19–D-29)."
    - "No unreviewed rule change, cloud/backend guidance, azimuthal recommendation, or complete-period-file assumption remains authoritative (D-28)."
  artifacts:
    - path: .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CODING-RULES-PROPOSAL.patch
      provides: "Approved canonical source diff and SHA-256 recorded by Plan 25"
    - path: .planning/coding-rules/frontend.md
      provides: "Wrapped camera and one-logical-copy rules"
    - path: .planning/coding-rules/data.md
      provides: "World manifest and historical provenance/fallback rules"
    - path: .planning/coding-rules/export.md
      provides: "Exact viewport+legend export transaction"
    - path: .planning/coding-rules/storage.md
      provides: "V2 local composition and V1 migration rules"
    - path: .planning/CODING_RULES.md
      provides: "Updated routing descriptions for all corrected subsystem rules"
  key_links:
    - from: .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CODING-RULES-PROPOSAL.patch
      to: .planning/CODING_RULES.md
      via: "git apply followed by byte-for-byte canonical diff comparison"
      pattern: "diff --git a/.planning/CODING_RULES.md b/.planning/CODING_RULES.md"
    - from: .planning/CODING_RULES.md
      to: .planning/coding-rules/storage.md
      via: "updated routing description"
      pattern: "complete composition"
---

<objective>
Apply the exact coding-rule patch approved in Plan 25 and prove the resulting source diff is byte-identical to it.

Purpose: Prevent future executors from reintroducing stale projection, history, cloud, storage, or export patterns while enforcing CLAUDE.md's exact-review boundary.
Output: Five synchronized engine-document updates produced solely by the approved patch, with footer hygiene and green subsystem gates.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@CLAUDE.md
@.planning/CODING_RULES.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/data.md
@.planning/coding-rules/export.md
@.planning/coding-rules/storage.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CODING-RULES-PROPOSAL.patch
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-25-SUMMARY.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Apply and prove the approved coding-rule patch exactly</name>
  <files>.planning/CODING_RULES.md, .planning/coding-rules/frontend.md, .planning/coding-rules/data.md, .planning/coding-rules/export.md, .planning/coding-rules/storage.md</files>
  <read_first>CLAUDE.md Update Process; Plan 02-25 summary approval including proposal path and SHA-256; the complete approved proposal artifact; current five target files; implemented camera/data/history/export/storage summaries</read_first>
  <action>First verify that `02-25-SUMMARY.md` records explicit user approval for the current proposal artifact SHA-256 and that the artifact still hashes to that value. Run `git apply --check --whitespace=error-all` and then apply that patch once with `git apply --whitespace=error-all`; do not manually edit any target file before or after application. Before application, copy the five current files into `.git/02-rules-apply-before/` while preserving repository-relative paths. After applying, regenerate the canonical source diff in the same exact path order and algorithm used by Plan 25: append `diff --git a/{path} b/{path}` plus `diff -u --label a/{path} --label b/{path}` output comparing each before copy with its current source, accepting only diff exit 1 for an intended change. Require a byte-for-byte `cmp` match between that actual patch and the approved proposal artifact. This comparison is mandatory whether the source documents began tracked or untracked. Any mismatch or failed hunk stops execution and returns to Plan 25 for a revised exact proposal and approval; never repair it inline. After equality is proven, run the focused camera/world/history/export/storage tests, lint, strict TypeScript, and production build. Preserve every unrelated Phase 1 rule and the exact footer text encoded by the approved patch.</action>
  <verify><automated>trap 'rm -rf .git/02-rules-apply-before .git/02-rules-actual.patch' EXIT; : > .git/02-rules-actual.patch; for path in .planning/CODING_RULES.md .planning/coding-rules/frontend.md .planning/coding-rules/data.md .planning/coding-rules/export.md .planning/coding-rules/storage.md; do printf 'diff --git a/%s b/%s\n' "$path" "$path" >> .git/02-rules-actual.patch; diff -u --label "a/$path" --label "b/$path" ".git/02-rules-apply-before/$path" "$path" >> .git/02-rules-actual.patch; code=$?; test "$code" -eq 1 || exit "$code"; done; cmp -s .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CODING-RULES-PROPOSAL.patch .git/02-rules-actual.patch && npm test -- src/utils/camera.test.ts src/utils/worldDataAsset.test.ts src/utils/historicalValidation.test.ts src/utils/export.test.ts src/utils/storage.test.ts && npm run lint && npm exec tsc -- -b --pretty false && npm run build</automated></verify>
  <acceptance_criteria>The approved artifact hash matches Plan 25's recorded approval; all five files are changed only by `git apply`; the regenerated canonical diff is byte-identical to the approved patch; focused tests, lint, strict TypeScript, and build exit 0; no sixth file or unapproved hunk is present.</acceptance_criteria>
  <done>The durable coding rules exactly match the approved proposal and the shipped Phase 2 implementation.</done>
  <commit>docs(2-rules): apply approved Phase 2 rule corrections</commit>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| approved proposal → engine docs | Scope drift could alter future implementation guidance |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-61 | Tampering | coding rules | mitigate | Bind execution to the approved artifact hash, apply it mechanically, and byte-compare the resulting canonical source diff. |
</threat_model>
<verification>The proposal hash, `git apply --check`, canonical byte comparison, focused subsystem tests, lint, strict TypeScript, build, and target-file scope all pass.</verification>
<success_criteria>Future work is routed to accurate Phase 2 camera/data/export/storage rules, with every source byte traceable to the exact human-approved patch.</success_criteria>
<execution_notes>Do not change branches or CLAUDE.md. Do not hand-edit target files. A failed apply, hash, or byte comparison returns to Plan 25 and cannot be corrected inline.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-26-SUMMARY.md` when done, recording the approved patch hash and successful byte comparison.</output>
