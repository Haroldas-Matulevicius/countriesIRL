---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "12"
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-10"]
files_modified:
  - scripts/prepareHistoricalSnapshot.mjs
  - src/utils/historicalValidation.ts
  - src/utils/historicalValidation.test.ts
  - src/hooks/useSnapshotData.ts
  - src/hooks/useSnapshotData.test.tsx
  - src/utils/historicalPreparationCli.test.ts
  - src/utils/fixtures/historicalSnapshot.ts
  - public/data/snapshots/index.json
autonomous: true
requirements: [F2.1, F2.2, F2.4, F2.5, NFR3, NFR8, NFR9]
must_haves:
  truths:
    - "Historical time is a curated manifest, never a continuous precision-implying slider (D-20)."
    - "Source/license readiness becomes one durable machine-validated source-approval JSON per snapshot before geometry generation; summary prose alone is never approval."
    - "Deterministic vector extraction and manual tracing are distinct evidence modes; manual traces are hash/procedure verified, never falsely regenerated."
    - "Source approval and factual approval are separate durable artifacts: source approval binds reviewer identity/role/date/non-executor status, six regional source/license decisions, manifest/archive/member/input/mode/manual-trace hashes; factual approval later binds that exact source-approval SHA plus candidate hashes."
  artifacts:
    - path: scripts/prepareHistoricalSnapshot.mjs
      provides: "Offline source-readiness, source-approval, vector/manual generation, factual-approval, and exact check modes"
    - path: src/utils/historicalValidation.ts
      provides: "Runtime/build validation of manifests, canonical evidence inventories, durable source approvals, assets, and factual approvals"
    - path: src/hooks/useSnapshotData.ts
      provides: "Cached reviewed snapshot loader retaining prior scene"
  key_links:
    - from: scripts/prepareHistoricalSnapshot.mjs
      to: data/historical-reviewed
      via: "offline hash-bound checks without live-network dependency"
      pattern: "validate-sources|validate-source-approval|source-approval|approval"
---

<objective>
Build the historical engine and honest evidence gates before any source acquisition or promotion.

Purpose: Keep Phase 2 history fully scoped while preventing missing rights, manual traces, self-approval, or changed bytes from becoming delivery claims.
Output: Validation/CLI/cache infrastructure and Modern-only production seed.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-VALIDATION.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/data.md
@scripts/prepareWorldData.mjs
@src/utils/scene.ts
@src/types/composition.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validate readiness, durable source approvals, assets, and factual approvals</name>
  <files>src/utils/historicalValidation.test.ts, src/utils/historicalValidation.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-RESEARCH.md Historical Strategy/Security; 02-REVIEWS.md historical evidence concerns; src/types/composition.ts; src/utils/geojson.ts</read_first>
  <behavior>- Each snapshot has six separate regional records: poland, lithuania, hungary, balkans, iberia, scandinavia.
- Source readiness records exact local evidence paths/hashes, rights disposition, attribution, retrieval date, uncertainties, canonical evidence-archive SHA-256, and a sorted unique member-path/member-SHA-256 inventory with its own canonical inventory SHA-256.
- Durable source approval requires reviewer identity, reviewer role, review date, explicit `isExecutor:false` and `isImplementer:false`, six independent source/license decisions, source-manifest SHA-256, evidence-archive SHA-256, canonical member-inventory SHA-256 plus every member path/hash, input/tracing-geometry SHA-256, and the declared `vector-extraction` or `manual-trace` mode.
- Manual-trace source approval additionally binds evidence, procedure, operator-record, and control-point SHA-256 values; vector extraction binds the exact extraction specification/command hash.
- Durable factual approval requires named qualified reviewer identity/role/date, six independent factual decisions/uncertainties, the exact current source-approval JSON SHA-256, and hashes for source manifest, input/tracing geometry, output overlay, review JSON, review HTML.
- Executor/implementer/Claude/Codex self-approval is rejected and any changed manifest/archive/member/input/source-approval/output/review byte invalidates the applicable approval.
- Only fully approved entries are production-selectable.</behavior>
  <action>Implement bounded unknown-input validators for readiness manifests, canonical ZIP evidence inventories, durable source approvals, assets, and factual approvals. Use discriminated vector/manual modes. Source approval must compare every recorded field/hash to current manifest, archive bytes, sorted unique normalized archive members, input/tracing geometry, and mode-specific evidence; factual approval must compare its `sourceApprovalSha256` to the current source-approval JSON before checking the five candidate/review hashes. Require exact reviewer identity/role/date and explicit non-executor/non-implementer booleans; prohibit executor/implementer/Claude/Codex identities. Enforce all six separate regional decisions and fail build/generation/promotion hard on any changed byte.</action>
  <verify><automated>npm test -- src/utils/historicalValidation.test.ts src/utils/scene.test.ts</automated></verify>
  <acceptance_criteria>Tests cover missing rights, noncanonical/changed archive member, changed approval JSON, wrong source-approval SHA, blocked source, vector/manual mode mismatch, manual trace missing evidence/procedure/operator/control-point hash, stale input/output/review bytes, self-approval, missing region, merged Poland/Lithuania, and valid source+factual bundles.</acceptance_criteria>
  <done>Unsupported or stale historical claims cannot be marked reviewed.</done>
  <commit>feat(2-history): validate historical readiness and approvals</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the offline historical CLI for vector and manual-trace modes</name>
  <files>scripts/prepareHistoricalSnapshot.mjs, src/utils/historicalPreparationCli.test.ts, src/utils/fixtures/historicalSnapshot.ts, public/data/snapshots/index.json</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-REVIEWS.md source/manual-tracing concerns; scripts/prepareWorldData.mjs; package.json</read_first>
  <behavior>- `--validate-sources` verifies all local evidence, rights, canonical archive bytes/member inventory, mode records, and six regions offline.
- `--validate-source-approval --source-approval <path>` verifies the durable reviewer/non-executor matrix and all current source/manifest/archive/member/input/mode-specific hashes.
- Vector mode records/replays deterministic retrieval/extraction and can regenerate canonical input/output in memory; manual-trace mode verifies evidence/procedure/operator/control-point/input hashes without pretending to regenerate the trace.
- Candidate generation and `--check` require `--source-approval <path>` and fail before output work if it is absent/stale/self-approved. Factual `--approval <path>` additionally binds the current source-approval SHA.
- `--check` is offline/non-mutating and fails on source/source-approval/input/output/review/factual-approval drift.
- Missing rights-approved material exits nonzero and leaves the catalog Modern-only.</behavior>
  <action>Implement exact modes and fixtures, including bounded canonical ZIP central-directory/member validation with normalized sorted paths, duplicate/path-traversal/encryption/unsupported-compression rejection, fixed canonical metadata, and uncompressed member SHA-256 comparison. Generation still requires distinct `--input`, `--output`, `--review-output`, `--review-html` and now mandatory `--source-approval`; `--approval` is required only for factual promotion checks. Provide `--validate-source-approval` for Plan 33. Do not download during checks, reuse output as input, treat source SUMMARY prose as approval, or require a fabricated deterministic command for manual traces. Seed production `index.json` with Modern only.</action>
  <verify><automated>npm test -- src/utils/historicalPreparationCli.test.ts && node scripts/prepareHistoricalSnapshot.mjs --help && npm run lint</automated></verify>
  <acceptance_criteria>Vector regeneration and manual-trace verification both pass valid fixtures; changed archive/member/source-approval/input bytes, missing rights, wrong mode, and self-approval fail before candidate generation; checks are offline and non-mutating.</acceptance_criteria>
  <done>Every later snapshot plan has an honest executable source/evidence contract.</done>
  <commit>feat(2-history): add offline historical preparation modes</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement reviewed snapshot caching and failure retention</name>
  <files>src/hooks/useSnapshotData.ts, src/hooks/useSnapshotData.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; .planning/coding-rules/data.md; src/hooks/useGeoData.ts; src/utils/historicalValidation.ts; src/constants/snapshots.ts</read_first>
  <behavior>- Same-origin approved assets cache by ID; warm repeat does not refetch.
- Superseded/unmounted loads abort and stale results cannot commit.
- Previous completed scene remains during load/failure; retry replaces only after validation.
- Draft/source-reviewed/blocked/stale-approval IDs are rejected before asset fetch.
- Coverage/fallback metadata survives cache and retry.</behavior>
  <action>Implement the abortable cache/loader and expose measurement marks for warm readiness. Do not make third-party runtime requests or silently switch periods.</action>
  <verify><automated>npm test -- src/hooks/useSnapshotData.test.tsx src/utils/historicalValidation.test.ts src/utils/scene.test.ts && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>Caching, abort, stale suppression, prior-scene retention, retry, and approval gating pass.</acceptance_criteria>
  <done>Only current exact approved bytes can become selectable snapshots.</done>
  <commit>feat(2-history): load approved snapshot data</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| historical sources/manual traces → build artifacts | Rights, procedure, bytes, IDs may be false or stale |
| approval identity → production status | Executor could self-approve |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-27 | Tampering | evidence/assets/approvals | mitigate | Canonical archive-member inventory, exact offline hashes, distinct vector/manual modes, and source-approval SHA chaining. |
| T-02-28 | Spoofing | reviewer status | mitigate | Named role/date, six decisions, self-approval rejection. |
| T-02-29 | Denial of Service | geometry | mitigate | Bounded validation and cache. |
</threat_model>
<verification>Validation/CLI/cache tests, lint, TypeScript, and build pass while Modern remains the only catalog entry.</verification>
<success_criteria>Historical engineering is executable, but F2/NFR8 remains blocked until all source/license and factual evidence plans pass.</success_criteria>
<execution_notes>Do not change branches. Do not claim NFR8 from automation. Commit each task atomically.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-12-SUMMARY.md` when done.</output>
