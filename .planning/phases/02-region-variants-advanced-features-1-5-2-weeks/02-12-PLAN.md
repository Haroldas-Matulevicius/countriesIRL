---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "12"
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-10"]
files_modified:
  - scripts/prepareHistoricalSnapshot.mjs
  - src/utils/historicalValidation.ts
  - src/utils/historicalValidation.test.ts
  - src/hooks/useSnapshotData.ts
  - public/data/snapshots/index.json
autonomous: true
requirements: [F2.1, F2.2, F2.4, F2.5, NFR3, NFR8, NFR9]
must_haves:
  truths:
    - "Historical time is a curated manifest of named verified snapshots, never a continuous precision-implying slider (D-20)."
    - "Partial historical coverage is explicitly declared and composes over modern fallback (D-21)."
    - "Only historian-reviewed entries can be selected in production; draft/source-reviewed assets remain non-production (D-20, D-21)."
  artifacts:
    - path: scripts/prepareHistoricalSnapshot.mjs
      provides: "Deterministic source/provenance/identity/topology pipeline"
    - path: src/utils/historicalValidation.ts
      provides: "Runtime manifest/asset validation"
    - path: src/hooks/useSnapshotData.ts
      provides: "Abortable cached loader retaining previous completed scene on failure"
  key_links:
    - from: src/hooks/useSnapshotData.ts
      to: public/data/snapshots/index.json
      via: "same-origin validated catalog load"
      pattern: "snapshots/index"
---

<objective>
Build the historical snapshot engine and evidence gate independently of the provenance-heavy curated assets.

Purpose: Make source/license/review status, identity, fallback, caching, and failure behavior enforceable before any historical period is advertised.
Output: Deterministic preparation CLI, validators/tests, loader, and Modern-only reviewed catalog seed.

Risk: Four historically accurate snapshots across six regions cannot be truthfully claimed from engineering alone. Plans 13–17 remain required evidence work and can block final Phase 2 completion without approved sources/review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-VALIDATION.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/data.md
@scripts/prepareWorldData.mjs
@src/utils/scene.ts
@src/types/composition.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define historical manifest and asset validation</name>
  <files>src/utils/historicalValidation.test.ts, src/utils/historicalValidation.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-RESEARCH.md Historical Snapshot Strategy and Security Domain; 02-PATTERNS.md P6/P14; src/types/composition.ts; src/utils/geojson.ts</read_first>
  <behavior>- Manifest requires id, exact label/asOf/path/hash, non-empty coverage, source URL/license/access/attribution, review status, and fallback label.
- Only historian-reviewed entries are production-selectable.
- Assets reject hash mismatch, duplicate/colliding IDs, unsafe keys, unsupported/non-finite geometry, missing coverage/provenance, and undeclared source licenses.
- Valid neighboring features survive malformed records at runtime; build check fails hard.
- Modern entry is always available and fully reviewed.</behavior>
  <action>Write malformed/valid table tests, then implement unknown-input validators with bounded arrays/strings/features and typed runtime outcomes. Never equate source-reviewed with historian-reviewed or infer identity continuity.</action>
  <verify><automated>npm test -- src/utils/historicalValidation.test.ts src/utils/scene.test.ts</automated></verify>
  <acceptance_criteria>Every provenance/review/identity/hash/coverage failure has a deterministic test and technical reason; creator copy is not embedded in the validator.</acceptance_criteria>
  <done>Unreviewed or malformed history cannot enter the production selector.</done>
  <commit>feat(2-history): validate historical manifests and assets</commit>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic historical preparation</name>
  <files>scripts/prepareHistoricalSnapshot.mjs, public/data/snapshots/index.json</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-PATTERNS.md P13; 02-RESEARCH.md Source Assessment and Recommended Snapshot Catalog; scripts/prepareWorldData.mjs; package.json</read_first>
  <action>Implement a build-only CLI accepting `--snapshot`, `--sources`, `--input`, `--output`, `--review-output`, and `--check`. Verify every source SHA-256 and license record, require explicit logical entity IDs/coverage regions, run named deterministic mapshaper cleanup, reject overlaps/slivers/non-finite paths/collisions, emit canonical compact GeoJSON plus review metadata, and generate a self-contained local review HTML with source links and six region panels. Seed `index.json` with only the Modern reviewed entry and candidate metadata excluded from production options. Do not download or bundle CShapes/Euratlas/restricted vectors without explicit rights.</action>
  <verify><automated>node scripts/prepareHistoricalSnapshot.mjs --help && npm run lint</automated></verify>
  <acceptance_criteria>CLI has deterministic check mode and fails when provenance, review status, source hash, identity, coverage, or topology evidence is incomplete; production index exposes Modern only at this point by rule, not scope reduction.</acceptance_criteria>
  <done>The curation pipeline can turn reviewed evidence into reproducible candidate assets.</done>
  <commit>feat(2-history): add deterministic snapshot preparation</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Load and cache reviewed snapshots without replacing a good scene on failure</name>
  <files>src/hooks/useSnapshotData.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; .planning/coding-rules/data.md; 02-PATTERNS.md P4; src/hooks/useGeoData.ts; src/utils/historicalValidation.ts; src/constants/snapshots.ts</read_first>
  <action>Implement an abortable same-origin catalog/asset loader with validated in-memory cache by snapshot ID. Keep the previous completed scene visible during load, expose loading/error/retry state, reject non-historian-reviewed IDs in production, and return validated overlay plus persistent coverage/fallback metadata. Measure warm switch readiness for NFR3 without hiding failures.</action>
  <verify><automated>npm test -- src/utils/historicalValidation.test.ts src/utils/scene.test.ts && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>Abort cleanup and prior-scene retention are testable through injected fetch/cache helpers; no runtime third-party request or silent fallback to an unrequested period occurs.</acceptance_criteria>
  <done>Reviewed historical assets can be switched safely and cached locally.</done>
  <commit>feat(2-history): load validated snapshot data</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| historical sources → build artifacts | Source data, rights, and IDs are untrusted |
| bundled snapshot JSON → browser | Assets may be malformed/substituted |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-27 | Tampering | snapshot sources/assets | mitigate | Exact source/asset hashes, provenance records, deterministic check mode. |
| T-02-28 | Spoofing | historical review status | mitigate | Production selector requires explicit historian-reviewed status backed by approval summaries. |
| T-02-29 | Denial of Service | historical geometry | mitigate | Bounded features/strings, topology/path checks, cached validated assets. |
</threat_model>

<verification>
Historical validation tests, scene tests, CLI help/check behavior, lint, strict TypeScript, and build pass.
</verification>

<success_criteria>
The application can only advertise reviewed historical assets and can truthfully retain modern fallback and prior-scene behavior.
</success_criteria>

<execution_notes>Do not change branches. Do not claim NFR8 from automated geometry checks. Commit each task atomically and preserve all evidence.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-12-SUMMARY.md` when done.</output>
