---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "12"
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-10"]
files_modified:
  - scripts/prepareHistoricalSnapshot.mjs
  - src/utils/historicalValidation.ts
  - src/utils/historicalValidation.test.ts
  - src/hooks/useSnapshotData.ts
  - src/hooks/useSnapshotData.test.tsx
  - src/utils/historicalPreparationCli.test.ts
  - src/utils/fixtures/historicalSnapshot.ts
  - public/data/snapshots/index.json
autonomous: true
requirements: [F2.1, F2.2, F2.4, F2.5, NFR3, NFR8, NFR9]
must_haves:
  truths:
    - "Historical time is a curated manifest of named verified snapshots, never a continuous precision-implying slider (D-20)."
    - "Partial historical coverage is explicitly declared and composes over modern fallback (D-21)."
    - "Only historian-reviewed entries can be selected in production; draft/source-reviewed assets remain non-production (D-20, D-21)."
  artifacts:
    - path: scripts/prepareHistoricalSnapshot.mjs
      provides: "Deterministic source/provenance/identity/topology pipeline"
    - path: src/utils/historicalValidation.ts
      provides: "Runtime manifest/asset validation"
    - path: src/hooks/useSnapshotData.ts
      provides: "Abortable cached loader retaining previous completed scene on failure"
  key_links:
    - from: src/hooks/useSnapshotData.ts
      to: public/data/snapshots/index.json
      via: "same-origin validated catalog load"
      pattern: "snapshots/index"
---

<objective>
Build the historical snapshot engine and evidence gate independently of the provenance-heavy curated assets.

Purpose: Make source/license/review status, identity, fallback, caching, and failure behavior enforceable before any historical period is advertised.
Output: Deterministic preparation CLI, validators/tests, loader, and Modern-only reviewed catalog seed.

Risk: Four historically accurate snapshots across six regions cannot be truthfully claimed from engineering alone. Plans 13–17 remain required evidence work and can block final Phase 2 completion without approved sources/review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-VALIDATION.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/data.md
@scripts/prepareWorldData.mjs
@src/utils/scene.ts
@src/types/composition.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and prove historical manifest, six-region, and asset validation</name>
  <files>src/utils/historicalValidation.test.ts, src/utils/historicalValidation.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-RESEARCH.md Historical Snapshot Strategy, resolved questions, and Security Domain; 02-PATTERNS.md P6/P14; src/types/composition.ts; src/utils/geojson.ts</read_first>
  <behavior>- Manifest requires id, exact label/asOf/path/hash, source records, review status, fallback label, and six separate coverage/review records with IDs `poland`, `lithuania`, `hungary`, `balkans`, `iberia`, and `scandinavia`.
- Shared Poland–Lithuania polity geometry is allowed only when Poland and Lithuania retain separate source, uncertainty, and reviewer evidence records.
- Only historian-reviewed entries are production-selectable.
- Assets reject hash mismatch, duplicate/colliding IDs, unsafe keys, unsupported/non-finite geometry, missing coverage/provenance, and undeclared source licenses.
- Valid neighboring features survive malformed records at runtime; build validation fails hard.
- Modern is always available and fully reviewed.</behavior>
  <action>Write malformed/valid table tests first in this task, then implement unknown-input validators with bounded arrays/strings/features and typed runtime outcomes. Never equate source-reviewed with historian-reviewed, infer identity continuity, merge Poland and Lithuania evidence, or embed creator copy in the validator. Finish green.</action>
  <verify><automated>npm test -- src/utils/historicalValidation.test.ts src/utils/scene.test.ts</automated></verify>
  <acceptance_criteria>Every provenance/review/identity/hash/license/coverage failure has a deterministic test; all six exact region IDs and separate Poland/Lithuania evidence are enforced.</acceptance_criteria>
  <done>Unreviewed, malformed, or incompletely evidenced history cannot enter the production selector.</done>
  <commit>feat(2-history): validate historical manifests and six-region assets</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement one exact historical preparation CLI with executable fixtures</name>
  <files>scripts/prepareHistoricalSnapshot.mjs, src/utils/historicalPreparationCli.test.ts, src/utils/fixtures/historicalSnapshot.ts, public/data/snapshots/index.json</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-PATTERNS.md P13/P14; 02-RESEARCH.md Source Assessment and resolved historical gate; scripts/prepareWorldData.mjs; package.json</read_first>
  <behavior>- Source-only validation uses exactly `--snapshot {id} --sources {manifest} --validate-sources` and verifies every manifest-listed source/tracing input artifact, SHA-256, right, six-region record, and deterministic retrieval/extraction command.
- Generation requires separate `--input {source-or-tracing.geojson}` and `--output {candidate.geojson}` plus `--review-output {review.json}` and `--review-html {review.html}`; input and output may not be the same path.
- `--check` accepts the same sources/input/output/review paths, regenerates canonical bytes in memory, and fails on output/review drift.
- Valid fixtures prove source validation and generation; tampered source bytes fail; committed-byte tampering fails `--check`.
- Missing provenance, command, review, identity, coverage, or topology evidence exits nonzero.</behavior>
  <action>Create executable fixtures and tests first, then implement the exact build-only CLI contract. Source manifests must name actual local source-geometry or tracing-input artifact paths and exact deterministic retrieval/extraction commands; the CLI validates those artifacts and command records before generation. Verify source SHA-256/license, require explicit logical entity IDs and the six coverage records, run named deterministic mapshaper cleanup, reject overlaps/slivers/non-finite paths/collisions, emit canonical compact GeoJSON plus review JSON and self-contained review HTML, and support non-mutating `--check`. Seed `index.json` with only the Modern reviewed entry and keep candidates unlisted. Do not add implicit output paths, reuse output as input, download restricted vectors, or expose draft/source-reviewed entries.</action>
  <verify><automated>npm test -- src/utils/historicalPreparationCli.test.ts && node scripts/prepareHistoricalSnapshot.mjs --help && npm run lint</automated></verify>
  <acceptance_criteria>The fixture suite exits green only when source validation, generation, tamper failure, and `--check` drift behavior all match the exact contract; Modern remains the sole production option.</acceptance_criteria>
  <done>Every snapshot plan can use one deterministic, test-proven source/input/output/check contract.</done>
  <commit>feat(2-history): add the deterministic snapshot preparation CLI</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement and prove reviewed snapshot caching and failure retention</name>
  <files>src/hooks/useSnapshotData.ts, src/hooks/useSnapshotData.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; .planning/coding-rules/data.md; 02-PATTERNS.md P4/P14; src/hooks/useGeoData.ts; src/utils/historicalValidation.ts; src/constants/snapshots.ts</read_first>
  <behavior>- The manifest and each approved asset are fetched same-origin and validated before use.
- Validated results cache by snapshot ID and a warm repeat selection performs no duplicate asset fetch.
- Unmount or superseding selection aborts in-flight work and prevents stale state commits.
- The prior completed scene remains visible while loading and after failure.
- Retry starts a new request and can replace the prior scene only after successful validation.
- Draft/source-reviewed/unknown IDs are rejected in production before asset fetch; historian-reviewed IDs are allowed.
- Coverage/fallback metadata survives cache and retry paths.</behavior>
  <action>Write dedicated hook/helper tests first, then implement the abortable same-origin catalog/asset loader with a validated in-memory cache by snapshot ID, prior-scene retention, typed loading/error/retry state, and production review-status gate. Measure warm switch readiness for NFR3 without hiding failures. Do not make runtime third-party requests or silently switch to an unrequested period.</action>
  <verify><automated>npm test -- src/hooks/useSnapshotData.test.tsx src/utils/historicalValidation.test.ts src/utils/scene.test.ts && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>Dedicated tests prove caching, abort cleanup, prior-scene retention, retry, stale-result suppression, and review-status gating; every final command passes.</acceptance_criteria>
  <done>Reviewed historical assets switch safely, cache locally, and never replace a good scene on failure.</done>
  <commit>feat(2-history): load and cache validated snapshot data</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| historical sources → build artifacts | Source data, rights, and IDs are untrusted |
| bundled snapshot JSON → browser | Assets may be malformed/substituted |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-27 | Tampering | snapshot sources/assets | mitigate | Exact source/asset hashes, provenance records, deterministic check mode. |
| T-02-28 | Spoofing | historical review status | mitigate | Production selector requires explicit historian-reviewed status backed by approval summaries. |
| T-02-29 | Denial of Service | historical geometry | mitigate | Bounded features/strings, topology/path checks, cached validated assets. |
</threat_model>

<verification>
Historical validation tests, scene tests, CLI help/check behavior, lint, strict TypeScript, and build pass.
</verification>

<success_criteria>
The application can only advertise reviewed historical assets and can truthfully retain modern fallback and prior-scene behavior.
</success_criteria>

<execution_notes>Do not change branches. Do not claim NFR8 from automated geometry checks. Commit each task atomically and preserve all evidence.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-12-SUMMARY.md` when done.</output>
