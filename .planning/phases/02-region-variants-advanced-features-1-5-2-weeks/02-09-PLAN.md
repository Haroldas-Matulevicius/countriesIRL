---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "09"
type: execute
wave: 5
depends_on: ["02-07"]
files_modified:
  - src/components/CountryList.tsx
  - src/components/CountryList.test.tsx
  - src/components/LocateCountry.tsx
  - src/components/LocateCountry.test.tsx
  - tests/e2e/locate.spec.ts
  - tests/e2e/fixtures/locate.html
autonomous: true
requirements: [F1.2, F1.4, F3.1, F3.2, F3.5, NFR5, NFR9, NFR11]
must_haves:
  truths:
    - "Country selection remains a coloring/bulk operation and Locate is separate (D-16)."
    - "Country browser and Locate contain exactly the modern 195-core catalog, including true-size islands; distinct historical entities are map/keyboard selectable only while active and never enter this catalog (D-12, D-15, D-22)."
    - "Locate never selects, colors, or creates history and its combobox behavior is proven in Chrome."
  artifacts:
    - path: src/components/LocateCountry.tsx
      provides: "Committed-target accessible modern-country Locate combobox"
    - path: src/components/CountryList.tsx
      provides: "Modern-core search and Select Visible"
    - path: tests/e2e/locate.spec.ts
      provides: "Focused Chrome Locate slice"
  key_links:
    - from: src/components/LocateCountry.tsx
      to: src/hooks/useCameraController.ts
      via: "parent onLocate callback"
      pattern: "onLocate"
---

<objective>
Upgrade the modern country browser and add a separate accessible Locate workflow without conflating the historical effective scene.

Purpose: Keep the 195-core browsing model predictable while approved historical entities remain directly interactive on the active map.
Output: Country filtering, committed Locate, and focused Chrome evidence.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@src/components/CountryList.tsx
@src/providers/MapStateProvider.tsx
@src/hooks/useGeoData.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add modern-core search and Select Visible</name>
  <files>src/components/CountryList.test.tsx, src/components/CountryList.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md section 11; src/components/CountryList.tsx; src/providers/MapStateProvider.tsx</read_first>
  <behavior>- Search filters case-insensitive modern display names without selection changes.
- Select Visible replaces selection with filtered valid modern core IDs.
- Exact no-results/clear copy works.
- Rows retain checkboxes/swatches and never move camera.
- Historical-only IDs are not added to the modern browser.</behavior>
  <action>Implement memoized filtering and stable handlers against the reviewed 195-core catalog. Preserve provider selection ownership and effective-white swatches. Historical entities remain active-scene map interactions, not browser rows.</action>
  <verify><automated>npm test -- src/components/CountryList.test.tsx src/hooks/useMapState.test.ts</automated></verify>
  <acceptance_criteria>Filtered-only selection, exact 195 catalog, and historical-ID exclusion pass.</acceptance_criteria>
  <done>The modern country browser supports world-scale bulk workflows without redefining historical interaction.</done>
  <commit>feat(2-countries): add world country filtering</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the explicit committed-target Locate combobox</name>
  <files>src/components/LocateCountry.test.tsx, src/components/LocateCountry.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md Locate contract; src/types/map.ts; src/utils/camera.ts</read_first>
  <behavior>- Draft opens a listbox of exactly 195 modern core states.
- Down/Up, Enter, Escape, committed-target invalidation, no-match copy, and Clear Locate Search follow the contract.
- Activation calls onLocate once and no selection/color callback.
- Historical-only IDs are rejected as Locate targets.</behavior>
  <action>Implement the ARIA combobox with bounded draft, explicit committed `CountryId`, exact copy, and typed `onLocate`. Keep small states identical; no markers/insets per D-15.</action>
  <verify><automated>npm test -- src/components/LocateCountry.test.tsx && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>Semantic/no-match/modern-only cases pass and Locate remains natively disabled until commit.</acceptance_criteria>
  <done>Every modern core state can be located without changing coloring intent.</done>
  <commit>feat(2-locate): add accessible country locate flow</commit>
</task>

<task type="auto">
  <name>Task 3: Prove Locate combobox and camera independence in Chrome</name>
  <files>tests/e2e/locate.spec.ts, tests/e2e/fixtures/locate.html</files>
  <read_first>.planning/coding-rules/general.md; 02-REVIEWS.md browser-allocation concern; playwright.config.ts; src/components/LocateCountry.tsx; src/components/CountryList.tsx</read_first>
  <action>Create a Vite-served Locate fixture and focused Chrome Playwright tests for listbox keyboard operation, commit/edit invalidation, exact no-match clearing/focus return, small-island Locate, retained action focus, and zero selection/color/history effects. Assert exactly 195 options and no historical-only ID.</action>
  <verify><automated>npm run test:e2e -- --project=chrome --grep "Locate Country|country search"</automated></verify>
  <acceptance_criteria>Real browser focus/combobox/camera independence passes with no skipped cases.</acceptance_criteria>
  <done>Locate behavior is proven in its owning plan rather than deferred to the final aggregate.</done>
  <commit>test(2-locate): verify browser locate flow</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| search text/data labels → rendered suggestions | User and bundled text enter UI |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-19 | Elevation of Privilege | search text | mitigate | Text nodes only and bounded draft. |
| T-02-20 | Denial of Service | filtering | mitigate | Fixed 195-state catalog and memoization. |
</threat_model>

<verification>Focused component/state tests and Chrome Locate slice pass.</verification>
<success_criteria>Creators can filter/select modern countries and independently Locate any modern core state while active historical entities remain map-only interactions.</success_criteria>
<execution_notes>Do not change branches. Commit each slice atomically.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-09-SUMMARY.md` when done.</output>
