---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "09"
type: execute
wave: 5
depends_on: ["02-07"]
files_modified:
  - src/components/CountryList.tsx
  - src/components/CountryList.test.tsx
  - src/components/LocateCountry.tsx
  - src/components/LocateCountry.test.tsx
autonomous: true
requirements: [F1.2, F1.4, F3.1, F3.2, F3.5, NFR5, NFR9, NFR11]
must_haves:
  truths:
    - "Country selection remains a coloring/bulk operation and Locate is a separate camera action (D-16)."
    - "All 195 core states, including true-size islands, are searchable and locatable without markers or insets (D-12, D-15)."
    - "Locate never selects, colors, or creates a color-history entry (D-16, D-18)."
  artifacts:
    - path: src/components/LocateCountry.tsx
      provides: "Committed-target accessible country combobox and Locate action"
    - path: src/components/CountryList.tsx
      provides: "Search, Select Visible, and stable checkbox selection"
  key_links:
    - from: src/components/LocateCountry.tsx
      to: src/hooks/useCameraController.ts
      via: "parent onLocate callback"
      pattern: "onLocate"
---

<objective>
Upgrade the country browser for the 195-state world and add a distinct accessible Locate workflow.

Purpose: Keep coloring selection predictable while making every small or off-screen state reachable.
Output: Tested country filtering/bulk selection and committed-target Locate combobox.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@src/components/CountryList.tsx
@src/providers/MapStateProvider.tsx
@src/hooks/useGeoData.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add country search and Select Visible</name>
  <files>src/components/CountryList.test.tsx, src/components/CountryList.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md section 11; 02-PATTERNS.md P11; src/components/CountryList.tsx; src/providers/MapStateProvider.tsx</read_first>
  <behavior>- Search filters case-insensitive display names without changing selection.
- Select Visible replaces selection with filtered valid core IDs.
- Clear Country Search and exact no-results copy work.
- Rows retain native checkboxes, full names, current swatches, and no camera action.
- Search and selection remain stable with 195 entries.</behavior>
  <action>Write tests, then implement memoized filtering and stable handlers. Rename the existing select-all behavior to exact `Select Visible`; preserve stable ID keys, provider selection ownership, and effective-white swatches.</action>
  <verify><automated>npm test -- src/components/CountryList.test.tsx src/hooks/useMapState.test.ts</automated></verify>
  <acceptance_criteria>Tests prove filtered-only selection and exact copy; no display name becomes a state key and no row invokes Locate.</acceptance_criteria>
  <done>The world country list remains fast and supports bulk creator workflows.</done>
  <commit>feat(2-countries): add world country filtering</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the explicit committed-target Locate combobox</name>
  <files>src/components/LocateCountry.test.tsx, src/components/LocateCountry.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md Locate contract and copywriting table; 02-PATTERNS.md P11; src/types/map.ts; src/utils/camera.ts</read_first>
  <behavior>- Editable draft opens an accessible listbox of 195 core states.
- Down/Up changes active suggestion, Enter commits, Escape closes.
- Editing after commit invalidates the target and disables Locate Country.
- Exact no-match heading/body/Clear Locate Search are scoped to Locate.
- Activation calls onLocate once, retains button focus, and does not call selection/color callbacks.</behavior>
  <action>Implement a native/ARIA combobox with local draft and committed `CountryId`, safe text rendering, bounded suggestions, exact labels/copy, and a typed `onLocate(countryId)` callback. Keep small states identical to all others; do not add markers/insets per D-15.</action>
  <verify><automated>npm test -- src/components/LocateCountry.test.tsx && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>All semantic and no-match tests pass; Locate remains natively disabled until explicit commit and has no dependency on map selection state.</acceptance_criteria>
  <done>Every core country can be located without changing coloring intent.</done>
  <commit>feat(2-locate): add accessible country locate flow</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| search text/data labels → rendered suggestions | User text and bundled names enter UI |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-19 | Elevation of Privilege | search/no-result text | mitigate | React text nodes only; no innerHTML; bounded draft length. |
| T-02-20 | Denial of Service | filtering | mitigate | Fixed 195-state list, memoized normalized search, no remote query. |
</threat_model>

<verification>
Focused component/state tests, lint, strict TypeScript, and full unit suite pass.
</verification>

<success_criteria>
Creators can filter/select world countries and independently center any core state, with exact accessible no-result behavior.
</success_criteria>

<execution_notes>Do not change branches. Commit each tested behavior slice atomically and preserve the provider-owned selection model.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-09-SUMMARY.md` when done.</output>
