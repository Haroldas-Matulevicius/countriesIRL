---
context: phase
phase: 02-region-variants-advanced-features-1-5-2-weeks
status: executing_descoped
last_updated: 2026-07-26T12:00:00Z
---

# Phase 2 — Resume Here

**This file is the session-resumption artifact and nothing more.** It carries the current
position, the next action, and hazards specific to *this machine and this working tree*. It
deliberately does **not** restate the invariants, the safety constraints, or the plan counts —
those had drifted apart across three files. Each now has one home:

| What you're looking for | Canonical home |
|---|---|
| Live invariants · immutable safety constraints | [`coding-rules/general.md`](../../coding-rules/general.md) |
| Plan status, counts, per-group ledger, verified gates | [`ROADMAP.md`](../../ROADMAP.md) § Progress |
| Live position, open decisions, blockers, pending todos | [`STATE.md`](../../STATE.md) |
| Why historical was descoped | [`02-DESCOPE-DECISION.md`](02-DESCOPE-DECISION.md) |
| Per-plan execution narrative | [`02-ACTIVITY-LOG.md`](02-ACTIVITY-LOG.md) + the `02-NN-SUMMARY.md` files |
| Phase-local deferrals | [`deferred-items.md`](deferred-items.md) |

> `.planning/HANDOFF.json` was deleted on 2026-07-26. It was a one-shot handoff artifact, already
> consumed, and had become a stale third copy of the above. Do not recreate it.

## Where things stand

- **Branch:** `gsd/phase-01-pattern-map`, primary checkout
  `C:\Users\matul\ClaudeProjects\CountriesIRL`.
- **All Phase 2 engineering is complete.** The verified SHA is
  **`fe5f946060707c48c3d9591d368b5f3f8f90dd4d`** — gate PASS from a fresh detached clean worktree
  with a fresh `npm ci`. Numbers and what the gate does *not* cover:
  [`ROADMAP.md`](../../ROADMAP.md) § Verified gates.
- **Two owner gates are OPEN:** `02-25` and `02-28`. See [`STATE.md`](../../STATE.md)
  § The two open owner gates for exactly what is and is not authorized on each.
- **Historical geometry does not ship.** All four packets fail closed for missing archival
  material. The engine ships and needs no rework when data arrives.

## Next action

**Hand [`02-28-ACCEPTANCE-MATRIX.md`](02-28-ACCEPTANCE-MATRIX.md) to the owner.** It is already
bound to `fe5f946` and the 60 automatable cells are pre-filled with cited evidence. Every physical
cell is `PENDING` and stays that way until a human performs the check.

**Do not:**

- dispatch any historical plan — the material does not exist, and approval cannot create it;
- treat the owner's blanket approval as satisfying `02-28`, or describe `02-25`'s approval as
  hash-bound;
- cite D-63 to justify a Phase 2 timing decision;
- run `state.advance-plan`, `state.update-progress`, or `roadmap.update-plan-progress` — they
  corrupt this repo's tracking files. Edit `STATE.md` and `ROADMAP.md` by hand.

## Working-tree hazards on this machine

- **24 git worktrees exist under `.claude/worktrees/`**, roughly 20 from earlier sessions. All are
  gitignored, none are prunable, and they are left in place. Clean up with `git worktree remove`
  only when confirmed unneeded. Two stale temp worktrees predating the `02-27` gate run were also
  left alone.
- **`.claude/worktrees/agent-a7d36e7ddf6a00457/node_modules` is empty.** A directory junction was
  created into it from a temporary worktree, and `git worktree remove --force` followed the
  junction and emptied the shared target. Run `npm ci` there before reusing it. The primary
  checkout is unaffected. **Never junction or symlink `node_modules` between worktrees** —
  [`coding-rules/general.md`](../../coding-rules/general.md) § Git safety.
- **Never `git checkout --` a file with uncommitted work.** Two agents lost edits that way in a
  single session. Copy to a scratchpad outside the repo and restore by copying back.

## Shared test fixtures

Browser fixtures live in `tests/e2e/support/`. Import `appHarness.ts`; do not re-declare the
camera helpers. `responsive.spec.ts` already does this; `persistence.spec.ts` and
`phase2-composition.spec.ts` still hold local copies (tracked in [`STATE.md`](../../STATE.md)
§ Pending Todos).
