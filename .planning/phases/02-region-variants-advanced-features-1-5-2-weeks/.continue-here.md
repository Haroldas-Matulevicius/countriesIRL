---
context: phase
phase: 02-region-variants-advanced-features-1-5-2-weeks
status: executing_descoped
last_updated: 2026-07-25T23:55:00Z
---

# Phase 2 Execution Handoff

Canonical fresh-session handoff. Machine-readable companion: `.planning/HANDOFF.json`.

> **This file supersedes the 2026-07-25T02:08 version entirely.** That version described a
> phase blocked behind a historical approval chain with two unintegrated correction
> worktrees. Both worktrees are now integrated, and the historical chain is formally
> **deferred** rather than blocked. Do not act on the old wording.

## What changed this session

1. **Phase 2 was descoped.** Historical snapshots 1492/1700/1815/1914 are deferred to a
   data-acquisition phase. See [`02-DESCOPE-DECISION.md`](02-DESCOPE-DECISION.md).
2. **Both correction worktrees were reviewed and integrated.** The Wave 5 production-wiring
   stack went through **three** independent non-author reviews; the historical
   source-readiness infrastructure went through verification of its own claims.
3. **`02-17` and `02-18` completed.**
4. **The planning docs were restructured** to the three-layer model.

## Current state

- **Branch:** `gsd/phase-01-pattern-map`
- **Plans:** 23 complete · 8 deferred · 3 engineering remaining · 2 owner gates
- **Gates on primary:** 37 files / 492 tests · lint · `tsc -b` · world check 248/195 ·
  build · Chrome E2E 65/65 · Edge E2E 65/65 (both re-run at `02-24`)
- **Historical promotion:** zero. Catalog is Modern-only and hash-verified.

## Why historical is deferred, not blocked

The four packets fail closed for **missing archival material**, not missing approval. No
human sign-off can unblock them:

| Snapshot | Missing |
|---|---|
| 1492 | Semkowicz-Romer scans + catalog, CNIG 15094 product archive, manual-trace operator records and control points, six-region geometry |
| 1700 | Karlowitz frontier demarcation, approved six-region production geometry |
| 1815, 1914 | Same class of gaps, from `02-32` |

All four additionally require independent rights, factual, and topology review.

**The engine ships.** Validation, scene composition, modern fallback, crossfade, identity
rejection, legend, export integration, and the approval-aware promotion path are all
implemented and tested. The Chrome E2E round-trips a complete historical scene using an
in-memory fixture. When rights-cleared geometry arrives, it drops into
`public/data/snapshots/` via the existing chain with **no rework of shipped code**.

## Exact plan ledger

### Complete — 22
`02-01`–`02-12`, `02-17` (rescoped), `02-18` (rescoped), `02-19`, `02-20`, `02-21`, `02-22`,
`02-23`, `02-24`, `02-29`, `02-30`, `02-32`

`02-32` is complete only as hash-bound **BLOCKED** readiness evidence. Zero production
snapshots. `deliveryCounted=false`.

### Deferred with the historical chain — 8
`02-31`, `02-33`, `02-13`, `02-14`, `02-15`, `02-16`, `02-34`, `02-35`

Original dependency order is preserved and still valid:
`02-31` → `02-33` → `02-13`–`02-16` → `02-34` → `02-35` → `02-17` (deferred tasks).

### Engineering remaining — 4
`02-24`, `02-26`, `02-27` (partial), `02-36`

`02-23` completed 2026-07-25 **without touching `src/App.tsx` or `src/main.tsx`**. The
composition root already satisfied every clause the plan names (provider bootstrap, delegated
transactions, one callback-ref-bound handle with no camera controller in `App`, legend through
`MapWorkspace`'s typed `legendSlot`, one keyed responsive workspace), so the plan delivered the
missing *guards* — `src/App.test.tsx` (handle-accessor identity, legend containment, one DOM,
landmark counts, no controller import) and `tests/e2e/transactions.spec.ts`. Shared browser
fixtures now live in `tests/e2e/support/`; `02-24`/`02-27` should import `appHarness.ts` instead
of re-declaring the camera helpers.

**Read this before assuming these are large.** Their *behavior* is already implemented and
covered by 469 unit tests and 53 Chrome E2E cases. What is missing is mostly the
plans' named file shapes:

| Plan | What actually remains |
|---|---|
| `02-26`, `02-36` | Apply the `02-25` doc patches |
| `02-27` | `tests/e2e/final-integration.spec.ts` — the gate script is **done and validated** |

### Owner gates — 2
- **`02-25`** — doc patches. The owner gave blanket pre-approval **sight-unseen**; the
  patches do not exist yet. Record that honestly in the artifact rather than claiming
  hash-bound approval.
- **`02-28`** — acceptance matrix. **Cannot be delegated or approved in the abstract.** Its
  own resume-signal rejects generic approval and forbids automation substitution for
  physical claims. A human must perform the touch, screen-reader, and visual checks.

## What `02-24` changed, and what it deliberately did not

`02-24` landed the exact UI-SPEC token contract, the responsive layout corrections, and the two
missing suites (`src/styles/phase2CssContract.test.ts`, `tests/e2e/responsive.spec.ts`). Three
things a later plan must know:

1. **`--map-*` are export tokens.** They are declared exactly once in `:root` and the contract
   test walks nested at-rules to prove no conditional block redefines one. Invariant 7 is now
   proven by a real export in three browser contexts (light/DPR 1, dark/DPR 3,
   forced-colors/DPR 2) whose downloaded PNGs are pixel-identical.
2. **`overflow-x: hidden` must stay on `body` only.** It was removed from `.app` because on a
   non-viewport element it computes `overflow-y: auto`, which silently disables the sticky app
   bar. Putting it back breaks the bar with nothing failing.
3. **Two placement gaps remain, and they are `App.tsx` work, not CSS.** `MapNavigation` renders
   inside `workspace__actions` rather than as an overlay at the top-left of the square
   (UI-SPEC 10), and `GlobalActions` is not in the app bar (UI-SPEC 8). The compact focus order
   therefore reads action strip → navigation → composition bar → map. `responsive.spec.ts`
   asserts the order that actually exists and names the deviation; do not "fix" the test.

## Live invariants — do not regress

1. **Selection/color can never reach a country absent from the active scene.** Eleven routes
   were enumerated and gated. `commitScene` reconciles selection in the same write as the
   snapshot switch; `ColorPicker.selectableCountryIds` is required and intersected. Undo/redo
   is safe only because history stores **colors only, never selection** — if you ever put
   selection into a history snapshot, you reopen this.
2. **Nothing reads `legend.position` raw on a render or export path.** Everything goes
   through `resolveLegendPosition`/`resolveLegendRender` in `src/utils/legend.ts`. One bypass
   reintroduces legend content being clipped out of the exported PNG.
3. **One `MapCanvasHandle`, one `svg.map-canvas`** across the 1200px transition. Guarded by
   `data-camera-owner-sentinel`.
4. **CountryList and Locate keep the unfiltered modern 195-core catalog.** Out-of-scene rows
   are *disabled*, never removed.
5. **The period selector is catalog-driven.** Deferred snapshots must stay structurally
   unreachable, and a manifest label can never override an approved catalog label.
6. **Export strips semantics, never geometry.** Every visible wrapped date-line path stays in
   the clone; roles, titles, ids, tab stops, all `aria-*`, editor state, and the outgoing
   crossfade layer are removed. Border normalization targets `path.scene-path` — a
   `path.country-path`-only rule silently leaves the 2px selection border on decorative
   wrapped repeats. A legend rendered outside the canonical SVG is a hard
   `invalid-composition` failure, never a silently legend-less PNG.
7. **Legend opacity is a single 0-100 scale.** A stored 0-1 fraction is repaired to percent
   and reported, not silently clamped.

## Immutable safety constraints — unchanged by the descope

1. Historical geometry, rights, and factual approvals are never inferred or fabricated.
2. No unapproved historical geometry reaches `public/data` or the catalog.
3. A BLOCKED packet is not a delivered snapshot and is never counted as one.
4. Executor self-approval is forbidden for source/license and factual review.
5. The six region IDs are never silently merged.
6. Phase 2 is browser-only and localhost-only.

## Process lesson worth keeping

**Executor self-reported checkpoints were not reliable for integration.** Across three
review rounds on one stack, independent non-author review of the aggregate diff caught five
real defects that the executor's own checkpoint had marked resolved — a permanently stuck
export gate, phantom cross-scene selection, a silently deleted accessibility landmark, a
legend overflow clipping the PNG, and a tautological performance gate. Keep the independent
review step for every stack.

## Open decision for the owner

**NFR3 timing threshold.** D-63 retired timing gates for **Phase 1 only** and does not carry
into Phase 2. Phase 2 asserts no threshold today and records warm period-switch samples plus
their median as advisory annotations in `tests/e2e/history.spec.ts`. Set a threshold from
those real numbers, or explicitly extend D-63 to Phase 2.

## First actions next session

1. Start in the primary checkout: `C:\Users\matul\ClaudeProjects\CountriesIRL`.
2. Read this file and `.planning/HANDOFF.json`.
3. Continue with `02-26`/`02-36` (the `02-25` doc patches).
4. Generate the `02-25` patches, apply via `02-26`/`02-36`.
5. Add `tests/e2e/final-integration.spec.ts`, then re-run the gate:
   `node scripts/verifyPhase2ExactCommit.mjs --sha <final> --evidence <path>`
6. Hand `02-28` to the owner with the automated cells pre-filled.

**Do not** dispatch any historical plan, or treat the owner's blanket approval as satisfying
`02-28`.

## Environment note (self-inflicted, worth knowing)

`.claude/worktrees/agent-a7d36e7ddf6a00457/node_modules` is **empty**. During this session a
directory junction was created from a temporary worktree's `node_modules` to that one;
`git worktree remove --force` followed the junction and emptied the shared target. That
worktree is already merged and disposable, but if you reuse it, run `npm ci` first.

**Primary is unaffected** — 330 packages, 404/404 tests across repeated runs, Chrome and
Edge 34/34.

**Lesson:** do not junction `node_modules` between git worktrees. Run `npm ci` in each, or
`git worktree remove` will delete the shared target.
