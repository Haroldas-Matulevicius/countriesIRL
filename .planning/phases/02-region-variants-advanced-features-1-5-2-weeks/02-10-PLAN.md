---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "10"
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/utils/scene.ts
  - src/utils/scene.test.ts
  - src/utils/legend.ts
  - src/utils/legend.test.ts
autonomous: true
requirements: [F2.2, F2.4, F4.1, F4.2, F4.3, F4.4, F4.5, NFR9]
must_haves:
  truths:
    - "Effective scene colors honor parent inheritance, neutral units, modern fallback, and explicit historical identity continuity (D-13, D-21, D-22)."
    - "Legend entries are derived from unique effective non-white colors, restore dormant metadata, and use uppercase hex defaults (D-24, D-25)."
    - "Legend layout never silently omits colors and validates the approved 30-entry/export bounds (D-26, D-27)."
  artifacts:
    - path: src/utils/scene.ts
      provides: "Modern/historical scene composition and effective colors"
    - path: src/utils/legend.ts
      provides: "Legend reconciliation, layout, clamp, and validation"
  key_links:
    - from: src/utils/legend.ts
      to: src/utils/scene.ts
      via: "effective visible color list"
      pattern: "effective"
---

<objective>
Implement the pure scene and legend algorithms before rendering or persistence.

Purpose: Ensure visible geography, history identity, parent colors, legend entries, overflow, and export validation share deterministic rules.
Output: Exhaustively tested scene composition and legend reconciliation/layout utilities.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/data.md
@src/utils/colors.ts
@src/types/composition.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose effective modern and historical scene state</name>
  <files>src/utils/scene.test.ts, src/utils/scene.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-RESEARCH.md Pattern 5 and Historical Composition Model; 02-PATTERNS.md scene assignment; src/utils/colors.ts; src/types/map.ts; src/types/composition.ts</read_first>
  <behavior>- Core entities use their own colors; clear dependencies inherit parent colors; neutral/disputed units remain neutral.
- Historical overlays cover declared regions while modern features remain fallback elsewhere.
- Matching curated entity IDs retain colors; new/different historical IDs are white.
- Boundary mode/provenance/period labels are explicit and immutable.
- Source IDs never become color keys without curated mapping.</behavior>
  <action>Write tests first, then implement pure immutable scene assembly and effective color derivation. Do not mutate `ColorMap`, infer predecessor relationships, or project modern colors backward per D-22.</action>
  <verify><automated>npm test -- src/utils/scene.test.ts src/utils/colors.test.ts</automated></verify>
  <acceptance_criteria>All inheritance, neutral, fallback, identity, and default-white cases pass; output contains no source-name inference.</acceptance_criteria>
  <done>Rendering and legend consumers receive one truthful effective scene.</done>
  <commit>feat(2-scene): compose effective world scenes</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile and validate deterministic legends</name>
  <files>src/utils/legend.test.ts, src/utils/legend.ts</files>
  <read_first>.planning/coding-rules/general.md; 02-UI-SPEC.md sections 12 and 13; 02-RESEARCH.md Legend Model; 02-PATTERNS.md legend assignment; src/types/composition.ts; src/utils/scene.ts</read_first>
  <behavior>- Unique uppercase non-white effective colors create one entry in first-use order with hex label.
- Unused entries become dormant and restore prior label/order when active again.
- 1–8/9–16/17–30 entries use 1/2/3 columns; >30 blocks export.
- Labels are 1–32 characters and deterministic wrapping can block export.
- themes/sizes/opacity/borders/positions accept only declared enums/ranges.
- corner/custom positions clamp the full measured bounds inside 32 units.</behavior>
  <action>Write table tests, then implement reconciliation, reordering, layout geometry, measurement-independent bounds calculations, nudge/corner clamp, and validation results. Use color keys only after rejecting reserved keys. Preserve dormant metadata in state.</action>
  <verify><automated>npm test -- src/utils/legend.test.ts && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>Tests cover every capacity band, style enum, bound, invalid label, reserved key, opacity/text-size range, and dormant lifecycle.</acceptance_criteria>
  <done>Legend UI/export can rely on one complete deterministic model.</done>
  <commit>feat(2-legend): implement legend reconciliation and layout</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| color maps/labels/styles → scene and legend | Values may originate from storage or user input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-21 | Tampering | legend dictionaries/styles | mitigate | Reserved-key rejection, enum-only styles, clamped finite values. |
| T-02-22 | Denial of Service | legend layout | mitigate | 30-entry cap, 32-character labels, deterministic bounded layout. |
| T-02-23 | Spoofing | historical identity | mitigate | Explicit curator-owned entity IDs; no heuristic predecessor transfer. |
</threat_model>

<verification>
Focused scene/legend tests, lint, strict TypeScript, and full unit suite pass.
</verification>

<success_criteria>
Visible map colors and legend contents agree for core, inherited, neutral, historical, and fallback geography.
</success_criteria>

<execution_notes>Do not change branches. Commit each algorithm with its tests atomically.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-10-SUMMARY.md` when done.</output>
