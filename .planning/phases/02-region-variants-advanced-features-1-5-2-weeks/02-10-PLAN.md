---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "10"
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/utils/scene.ts
  - src/utils/scene.test.ts
  - src/utils/legend.ts
  - src/utils/legend.test.ts
autonomous: true
requirements: [F1.2, F1.3, F1.5, F2.2, F2.4, F4.1, F4.2, F4.3, F4.4, F4.5, NFR9]
must_haves:
  truths:
    - "Effective scenes honor parent inheritance, neutral units, modern fallback, and explicit historical identity continuity (D-13, D-21, D-22)."
    - "Approved distinct historical entities are selectable/colorable/history-capable while active; modern browser/Locate stays separate; dependencies/disputed/neutral units remain non-selectable."
    - "Period reconciliation retains selection only for identities present in the incoming effective scene."
    - "Legend entries derive from effective non-white colors, including active historical entities, and never silently omit colors (D-24–D-27)."
  artifacts:
    - path: src/utils/scene.ts
      provides: "Effective scene, selectable identity set, selection reconciliation, and effective colors"
    - path: src/utils/legend.ts
      provides: "Legend reconciliation/layout/validation"
  key_links:
    - from: src/utils/legend.ts
      to: src/utils/scene.ts
      via: "effective visible color list"
      pattern: "effective"
---

<objective>
Implement the pure effective-scene and legend algorithms, including the locked historical interaction policy.

Purpose: Ensure rendering, selection/history, persistence, and legend use one deterministic identity/color model.
Output: Tested scene composition, selectable-ID reconciliation, and legend utilities.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/data.md
@src/utils/colors.ts
@src/types/composition.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Compose effective scenes and reconcile active interaction identities</name>
  <files>src/utils/scene.test.ts, src/utils/scene.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-RESEARCH.md Pattern 5/Historical Composition; 02-REVIEWS.md historical-entity concern; src/utils/colors.ts; src/types/map.ts; src/types/composition.ts</read_first>
  <behavior>- Core entities use own colors; dependencies inherit; neutral/disputed units stay neutral/non-selectable.
- Historical overlays replace declared coverage while modern features remain fallback elsewhere.
- Continuing IDs retain color; distinct approved historical IDs start white and are selectable while active.
- Selection reconciliation keeps only IDs selectable in the incoming scene.
- Historical IDs remain stable color/history/persistence keys; source IDs/names never become keys heuristically.</behavior>
  <action>Write tests first, then implement immutable effective-scene assembly, effective colors, `getSelectableEntityIds`, and `reconcileSelectionForScene`. Lock the reviewer-recommended policy without re-asking the user. Do not mutate ColorMap, project modern colors backward, include neutral units, or merge modern browser/Locate membership into effective-scene selectability.</action>
  <verify><automated>npm test -- src/utils/scene.test.ts src/utils/colors.test.ts</automated></verify>
  <acceptance_criteria>Continuing/new historical, dependency, neutral, fallback, and outgoing-selection removal cases pass.</acceptance_criteria>
  <done>All consumers receive one truthful scene and interaction identity set.</done>
  <commit>feat(2-scene): compose interactive effective scenes</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile and validate deterministic legends</name>
  <files>src/utils/legend.test.ts, src/utils/legend.ts</files>
  <read_first>.planning/coding-rules/general.md; 02-UI-SPEC.md sections 12–13; 02-RESEARCH.md Legend Model; src/types/composition.ts; src/utils/scene.ts</read_first>
  <behavior>- Unique uppercase non-white effective colors create one entry, including colors owned by active historical IDs.
- Undo/redo or period removal hides entries while dormant metadata restores labels/order when colors return.
- 1–8/9–16/17–30 use 1/2/3 columns; >30 blocks export.
- Labels/style/positions use exact bounds/enums and reserved keys fail closed.</behavior>
  <action>Implement reconciliation, reordering, deterministic layout, nudge/corner clamp, and validation against effective-scene colors. Add tests showing a newly colored historical entity creates a legend entry, undo removes it, redo restores it, and a period switch hides/restores dormant metadata by stable ID/color.</action>
  <verify><automated>npm test -- src/utils/legend.test.ts && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>Historical legend/history lifecycle and every capacity/style/bounds case pass.</acceptance_criteria>
  <done>Visible map colors and legend content agree for modern, inherited, historical, and fallback geography.</done>
  <commit>feat(2-legend): implement effective-scene legends</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| scene manifests/colors/labels → interaction and legend | Curated and stored values are untrusted |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-21 | Tampering | legend dictionaries/styles | mitigate | Safe keys, enums, finite bounds. |
| T-02-22 | Denial of Service | legend layout | mitigate | 30-entry/32-character limits. |
| T-02-23 | Spoofing | historical identity/selectability | mitigate | Explicit stable IDs and curator-owned `isSelectable`; no inference. |
</threat_model>
<verification>Focused scene/legend tests, lint, strict TypeScript, and full suite pass.</verification>
<success_criteria>Selection, color history, persistence, and legend can all use the same effective-scene policy.</success_criteria>
<execution_notes>Do not change branches. Commit each algorithm with tests atomically.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-10-SUMMARY.md` when done.</output>
