---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "20"
type: execute
wave: 7
depends_on: ["02-18", "02-19"]
files_modified:
  - src/components/SaveLoad.tsx
  - src/components/SaveLoad.test.tsx
autonomous: true
requirements: [F6.1, F6.2, F4.3, F4.4, F4.5, NFR5, NFR7, NFR11]
must_haves:
  truths:
    - "Save/Load lists and restores complete compositions through one coordinated callback (D-19)."
    - "Legacy maps explain Modern/whole-world/default-legend migration and are not rewritten until explicit save (D-19)."
    - "Dirty loads and deletes use the exact accessible confirmation/focus behavior while the working composition stays recoverable (D-19)."
  artifacts:
    - path: src/components/SaveLoad.tsx
      provides: "Versioned complete-composition dialog/sheet"
    - path: src/components/SaveLoad.test.tsx
      provides: "Migration, confirmation, metadata, close/focus contract"
  key_links:
    - from: src/components/SaveLoad.tsx
      to: src/hooks/useLocalStorage.ts
      via: "typed complete composition operations"
      pattern: "useLocalStorage"
---

<objective>
Upgrade the saved-map surface to the approved complete-composition workflow and recovery copy.

Purpose: Let creators safely preserve/load colors, view, period, and legend without losing unsaved work or Phase 1 saves.
Output: Tested accessible dialog/sheet with coordinated load and confirmations.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/storage.md
@src/components/SaveLoad.tsx
@src/components/SaveLoad.test.tsx
@src/hooks/useLocalStorage.ts
@src/types/composition.ts
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement and prove complete-composition Save/Load states, confirmations, and focus</name>
  <files>src/components/SaveLoad.tsx, src/components/SaveLoad.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; .planning/coding-rules/storage.md; 02-UI-SPEC.md section 15 and copywriting; 02-PATTERNS.md P10/P14; src/components/SaveLoad.tsx; src/components/SaveLoad.test.tsx; src/hooks/useLocalStorage.ts; src/types/composition.ts</read_first>
  <behavior>- Rows show name, DD MMM YYYY, period, legend count, and whole/custom view metadata.
- Legacy rows show exact migration metadata and warning on load.
- Existing name shows inline replace warning/CTA.
- Delete is two-step inline with next/previous/name focus.
- Dirty load opens exact compact confirmation; clean load does not.
- Close Saved Maps header, Escape, desktop/compact scrim, mobile no-scrim, focus trap, responsive-equivalent opener restoration, and successful-load map focus all work.
- Successful load emits one complete validated composition and never partially applies state from effects.</behavior>
  <action>Write/extend helper and SSR tests first in this task for exact copy, metadata, dialogs, close paths, focus restoration, and safe React text rendering. Then accept current complete composition, dirty flag, known snapshot metadata, and one `onLoadComposition` callback; preserve lazy modal-open refresh and typed adapter use. Implement exact row metadata, legacy warning, replace warning, inline delete confirmation, dirty-load dialog, all close/focus paths, and creator-safe errors. Validate the complete record/snapshot before invoking parent load; never use raw localStorage/JSON, HTML sinks, alert/confirm, or component effects to apply partial colors/camera/legend. End on the final passing command.</action>
  <verify><automated>npm test -- src/components/SaveLoad.test.tsx src/utils/storage.test.ts && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>Every UI-SPEC persistence and focus state passes; successful load emits one complete value and map-focus request; failures leave working composition and name draft intact; stored text uses text nodes.</acceptance_criteria>
  <done>Creators can safely manage complete local compositions and legacy saves.</done>
  <commit>feat(2-save-load): implement tested complete composition dialogs</commit>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| persisted names/metadata → modal DOM | Stored text is untrusted |
| load confirmation → App | One action replaces current composition |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-46 | Elevation of Privilege | stored text | mitigate | React text nodes only and adapter length/type validation. |
| T-02-47 | Repudiation | destructive/load actions | mitigate | Explicit inline confirmations and status announcements. |
| T-02-48 | Tampering | partial load | mitigate | Validate/resolve first; emit one complete coordinated value only. |
</threat_model>
<verification>SaveLoad/storage tests, lint, strict TypeScript, build, and full unit suite pass.</verification>
<success_criteria>Complete and legacy maps can be listed, saved, replaced, loaded, and deleted with exact accessible recovery behavior.</success_criteria>
<execution_notes>Do not change branches. Finish the TDD task green and commit the tested Save/Load slice atomically. Preserve modal ownership and one storage adapter.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-20-SUMMARY.md` when done.</output>
