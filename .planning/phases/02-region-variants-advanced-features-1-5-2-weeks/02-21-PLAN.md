---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "21"
type: execute
wave: 11
depends_on: ["02-07", "02-11", "02-18"]
files_modified:
  - src/utils/export.ts
  - src/utils/export.test.ts
  - tests/e2e/export.spec.ts
  - tests/e2e/fixtures/export.html
autonomous: true
requirements: [F5.1, F5.2, F5.3, F5.5, NFR4]
must_haves:
  truths:
    - "Export utility preserves all visible wrapped geometry required for Pacific/date-line composition while stripping only duplicate accessibility/editor semantics (D-07, D-08)."
    - "Selected finished scene, fallback geometry, colors, background, and legend remain; outgoing/editor state is removed (D-08, D-10)."
    - "Output remains exact 1080x1080 opaque/DPR-independent with safe filename/download cleanup (D-11)."
  artifacts:
    - path: src/utils/export.ts
      provides: "Pure prepared-DOM clone/capture/download utility"
    - path: tests/e2e/export.spec.ts
      provides: "Focused Chrome clone/download/Pacific slice"
  key_links:
    - from: src/utils/export.ts
      to: src/components/MapCanvas.tsx
      via: "prepared canonical SVG clone"
      pattern: "data-layer|data-editor-only"
---

<objective>
Extend the export chokepoint for exact wrapped-world/legend content without embedding camera orchestration in the utility.

Purpose: Preserve visible Pacific geometry and Phase 1 download guarantees; Plan 30 owns lease/finally orchestration.
Output: Tested clone/sanitize/capture/filename/download utility plus focused Chrome export evidence.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/export.md
@src/utils/export.ts
@src/components/MapCanvas.tsx
@src/components/LegendOverlay.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement exact prepared-composition export sanitization</name>
  <files>src/utils/export.ts, src/utils/export.test.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/export.md; 02-UI-SPEC.md section 14; 02-REVIEWS.md wrapped-copy concern; existing export utility/tests</read_first>
  <behavior>- Camera transform and every visible wrapped geometry path remain in clone.
- Decorative copies lose roles/titles/tabindex/IDs/accessibility-only helpers but visual path geometry is not deleted.
- Outgoing scene/editor-only/selection/focus/hover/tooltip/navigation/status is removed.
- Selected scene/fallback/fills/borders/background/legend remain.
- 1080x1080/opacity/DPR/filename/anchor handoff/resource cleanup pass on all utility failures.</behavior>
  <action>Keep this utility focused on a synchronously prepared source. Extend sanitize logic to strip semantics from visible repeat geometry rather than removing the geometry. Remove outgoing group and editor-only nodes. Preserve fixed 540@2x capture, exact dimension validation, allowTaint false, connected anchor, bounded handoff, nested resource cleanup, and safe named filename. Do not acquire/release camera lease here.</action>
  <verify><automated>npm test -- src/utils/export.test.ts && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit && npm run build</automated></verify>
  <acceptance_criteria>Centered/Pacific clone cases and all resource failure branches pass; no visible wrapped path is stripped solely because it is decorative for accessibility.</acceptance_criteria>
  <done>Prepared compositions export exactly and safely.</done>
  <commit>feat(2-export): preserve wrapped composition export</commit>
</task>

<task type="auto">
  <name>Task 2: Prove Pacific clone/download behavior in Chrome</name>
  <files>tests/e2e/export.spec.ts, tests/e2e/fixtures/export.html</files>
  <read_first>.planning/coding-rules/general.md; 02-REVIEWS.md browser allocation/wrapped geometry; playwright.config.ts; export utility; camera/legend components</read_first>
  <action>Create an export fixture with a Pacific/date-line prepared SVG, selected historical/fallback scene, custom legend, visible repeat paths and duplicate accessibility semantics. In Chrome, download and inspect exact 1080x1080 opaque output, filename, seam-free visible wrapped geometry, selected scene/legend retention, and absence of editor/accessibility duplicate semantics. Cover click/canvas/blob failures and renewed fixture interaction; Plan 30 adds camera lease integration tests.</action>
  <verify><automated>npm run test:e2e -- --project=chrome --grep "export|Pacific"</automated></verify>
  <acceptance_criteria>Real download/content/failure cases pass and files are cleaned beneath ignored artifact root.</acceptance_criteria>
  <done>Core export parity is proven early in Chrome.</done>
  <commit>test(2-export): verify Pacific PNG export</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| live prepared DOM/map name → PNG/download | DOM/text/resources are untrusted |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-49 | Tampering | clone/resources | mitigate | Same-origin, explicit sanitize, exact dimensions. |
| T-02-50 | Tampering | filename | mitigate | Whitelist/cap/fixed suffix. |
| T-02-51 | Denial of Service | resources | mitigate | Bounded handoff/nested cleanup. |
</threat_model>
<verification>Utility tests and Chrome export slice pass.</verification>
<success_criteria>All visible wrapped composition content exports exactly without duplicate editor/accessibility semantics.</success_criteria>
<execution_notes>Do not change branches. Camera lifecycle is Plan 30.</execution_notes>
<output>Create `02-21-SUMMARY.md` when done.</output>
