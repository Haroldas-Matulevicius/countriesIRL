---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "21"
type: execute
wave: 7
depends_on: ["02-07", "02-11", "02-18"]
files_modified:
  - src/utils/export.ts
  - src/utils/export.test.ts
autonomous: true
requirements: [F5.1, F5.2, F5.3, F5.5, NFR4]
must_haves:
  truths:
    - "PNG captures the exact last painted camera frame, including date-line-spanning copies, and freezes motion synchronously (D-07, D-09)."
    - "Export keeps geography/colors/history state/legend/style but removes every editor indicator and outgoing crossfade (D-08, D-10)."
    - "Output remains exact 1080x1080 opaque, DPR-independent, safely downloaded, and fully cleaned (D-11)."
  artifacts:
    - path: src/utils/export.ts
      provides: "Sanitized exact-current-composition PNG and safe filename"
    - path: src/utils/export.test.ts
      provides: "Freeze/sanitize/parity/cleanup tests"
  key_links:
    - from: src/utils/export.ts
      to: src/components/MapCanvas.tsx
      via: "cloned current SVG transform and finalized scene"
      pattern: "data-layer"
---

<objective>
Extend the verified export chokepoint to capture the exact unified camera, reviewed scene, and legend composition.

Purpose: Preserve the Phase 1 exact PNG guarantee while adding the full visible Phase 2 output.
Output: Tested freeze-first export sanitization, filename rules, parity, and cleanup.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/export.md
@src/utils/export.ts
@src/utils/export.test.ts
@src/components/MapCanvas.tsx
@src/components/LegendOverlay.tsx
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify exact viewport, legend, sanitization, and filename behavior</name>
  <files>src/utils/export.test.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/export.md; 02-UI-SPEC.md section 14; 02-PATTERNS.md P8/P14; src/utils/export.test.ts; src/utils/export.ts</read_first>
  <behavior>- Freeze/finalize/measure callbacks run synchronously before first await/clone.
- Camera transform, visible wrapped copies, selected scene, fallback geometry, fills, background, and legend persist in clone.
- data-editor-only, outgoing scenes, selection/focus/hover, tooltips, navigation, status, accessible decorative artifacts are removed.
- centered and Pacific views preserve exact transform/legend.
- output is 1080x1080 opaque at multiple DPRs.
- unnamed and sanitized named filename/date rules pass; unsupported chars/whitespace/repeated underscores/60-char cap are enforced.
- connected-anchor handoff and exact-once nested cleanup remain unchanged on every failure/success.</behavior>
  <action>Extend the existing fake DOM/canvas/URL lifecycle suite. Add same-origin/no-taint assertions and export-blocked legend validation. Do not replace tested cleanup with a new download mechanism.</action>
  <verify><automated>npm test -- src/utils/export.test.ts</automated></verify>
  <acceptance_criteria>All D-07–D-11 cases are represented, including export during Locate and crossfade, before implementation.</acceptance_criteria>
  <done>The exact Phase 2 export transaction is test-defined.</done>
  <commit>test(2-export): specify composition export parity</commit>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement freeze-first exact composition export</name>
  <files>src/utils/export.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/export.md; src/utils/export.test.ts; src/utils/export.ts; src/types/composition.ts</read_first>
  <action>Extend `exportMapPng` to receive optional map name plus synchronous preparation callbacks supplied by App: camera freeze/snapshot, selected-scene finalization, and legend measurement/validation. Acquire no async work before these callbacks. Clone the current SVG transform/legend, remove `[data-editor-only]`, outgoing scene groups, editor state/classes/attributes, duplicate accessibility-only artifacts, and unsupported effects while preserving current fills/borders/history/fallback/background. Keep fixed 540 frame at scale 2, exact dimension rejection, allowTaint false/same-origin assets, Blob URL, connected anchor, 100ms handoff, and nested finally cleanup. Implement conservative safe filename token and fixed `.png` suffix.</action>
  <verify><automated>npm test -- src/utils/export.test.ts && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit && npm run build</automated></verify>
  <acceptance_criteria>All tests pass; no remote resource, CSS filter, foreignObject, raw filename, stale React camera state, or cleanup regression exists.</acceptance_criteria>
  <done>Export downloads the exact clean visible composition at 1080x1080.</done>
  <commit>feat(2-export): export the exact world composition</commit>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| live DOM/user map name → PNG/download | DOM may contain editor/untrusted text and filenames |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-49 | Tampering | export clone/resources | mitigate | Same-origin assets, allowTaint false, explicit sanitization, exact dimensions. |
| T-02-50 | Tampering | filename | mitigate | Whitelist tokenization, 60-char cap, fixed date and `.png`. |
| T-02-51 | Denial of Service | resource lifecycle | mitigate | Activation lock, bounded handoff, nested finally cleanup. |
</threat_model>
<verification>Focused export tests, lint, strict TypeScript, build, and full unit suite pass.</verification>
<success_criteria>Centered, regional, and date-line views export exactly with legend/history scene and no editor chrome.</success_criteria>
<execution_notes>Do not change branches. Preserve the connected-anchor/finally lifecycle exactly; commit RED and GREEN separately.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-21-SUMMARY.md` when done.</output>
