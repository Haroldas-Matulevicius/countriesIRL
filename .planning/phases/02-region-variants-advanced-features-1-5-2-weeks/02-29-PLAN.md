---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "29"
type: execute
wave: 5
depends_on: ["02-03", "02-07", "02-12", "02-19"]
files_modified:
  - src/hooks/useCompositionLoadTransaction.ts
  - src/hooks/useCompositionLoadTransaction.test.tsx
  - src/hooks/useCompositionSaveTransaction.ts
  - src/hooks/useCompositionSaveTransaction.test.tsx
autonomous: true
requirements: [F1.5, F2.2, F4.3, F4.4, F4.5, F6.1, F6.2, NFR11]
must_haves:
  truths:
    - "Save and load receive the currently bound sole MapCanvasHandle from App; save calls its non-locking live read and load calls its restore/focus operations, including after responsive remount (D-19)."
    - "Load validates/resolves first, then applies color/history reset, composition, camera, focus, status, and baseline as one cancellable intent."
    - "App will consume focused transactions rather than embedding orchestration details."
  artifacts:
    - path: src/hooks/useCompositionSaveTransaction.ts
      provides: "Live-camera complete save assembler"
    - path: src/hooks/useCompositionLoadTransaction.ts
      provides: "Atomic cancellable load orchestration"
  key_links:
    - from: src/hooks/useCompositionSaveTransaction.ts
      to: src/hooks/useCameraController.ts
      via: "injected current-handle accessor; no hook/controller construction"
      pattern: "getMapCanvasHandle|readCurrentCamera|restore"
---

<objective>
Extract complete load and live-camera save transactions from App into focused tested hooks.

Purpose: Prevent stale-camera saves, partial loads, and an oversized composition root.
Output: Independent save/load orchestration hooks with injected boundaries.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/storage.md
@src/hooks/useCameraController.ts
@src/hooks/useLocalStorage.ts
@src/providers/MapStateProvider.tsx
@src/providers/CompositionStateProvider.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement atomic composition load transaction</name>
  <files>src/hooks/useCompositionLoadTransaction.ts, src/hooks/useCompositionLoadTransaction.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-RESEARCH.md complete load pattern; 02-REVIEWS.md App decomposition; storage/snapshot/provider contracts</read_first>
  <behavior>- Validate complete record before mutation.
- Resolve approved snapshot and cancel superseded/unmounted loads. Resolve the currently bound MapCanvasHandle only at the restore/focus step so responsive remounts cannot leave a stale handle.
- On success batch color load/history reset, composition load, call `restore` and `focusCountry`/map focus through the currently bound sole handle, dirty baseline, and one status.
- Failure leaves visible composition unchanged.
- Historical color IDs persist only when valid stable IDs; selection reconciles to loaded effective scene.</behavior>
  <action>Implement an injected hook/utility with explicit loading result, cancellation token, and `getMapCanvasHandle(): MapCanvasHandle | null` accessor. Never import or invoke `useCameraController` here and never cache a handle across a responsive remount. No component effects independently apply partial fields. Keep creator copy/status mapping outside low-level validation but emit one typed outcome.</action>
  <verify><automated>npm test -- src/hooks/useCompositionLoadTransaction.test.tsx src/utils/storage.test.ts src/hooks/useSnapshotData.test.tsx</automated></verify>
  <acceptance_criteria>Ordering, cancellation, failure atomicity, history reset, selection reconciliation, missing-handle behavior, restore/focus on the currently rebound visible handle, and baseline cases pass.</acceptance_criteria>
  <done>Complete loads are one deterministic intent.</done>
  <commit>feat(2-transactions): add atomic composition load</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement live-camera save transaction</name>
  <files>src/hooks/useCompositionSaveTransaction.ts, src/hooks/useCompositionSaveTransaction.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/storage.md; 02-REVIEWS.md live-camera save concern; camera/composition/storage contracts</read_first>
  <behavior>- Every save resolves the currently bound sole MapCanvasHandle and calls its non-locking `readCurrentCamera` synchronously at activation.
- Save during animated Locate, active wheel sequence, or drag uses the visible camera snapshot.
- Save does not lock/interrupt camera input.
- Assembled snapshot includes colors, current camera, selected approved period, legend/dormant metadata/style/position, background/settings, and excludes editor/transient state.
- Successful save marks the exact assembled baseline; failure does not.</behavior>
  <action>Implement a focused injected save hook that accepts `getMapCanvasHandle`, resolves the currently mounted/rebound handle at activation, reads its live camera, assembles immutable `CompositionSnapshot`, delegates to the sole storage facade, and marks saved only after success. Do not import/create a controller or retain a stale handle reference. Add tests where committed React camera is intentionally stale relative to live camera, including Locate/wheel cases.</action>
  <verify><automated>npm test -- src/hooks/useCompositionSaveTransaction.test.tsx src/utils/storage.test.ts && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>All stale/live camera, missing handle, responsive rebind, success/failure, exact snapshot, and no-lock cases pass; only the visible handle receives calls.</acceptance_criteria>
  <done>Saves always represent the live visible composition.</done>
  <commit>feat(2-transactions): save the live composition</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| live camera/providers → storage/load mutations | Ordering/staleness can produce false compositions |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-76 | Tampering | load transaction | mitigate | Validate/resolve then one batched commit. |
| T-02-77 | Tampering | save camera | mitigate | Synchronous non-locking live read at activation. |
</threat_model>
<verification>Focused load/save transaction tests, storage/snapshot tests, lint, TypeScript pass.</verification>
<success_criteria>App can delegate complete load/save without stale camera or partial state.</success_criteria>
<execution_notes>Do not change branches. Keep hooks injected/testable and App-free.</execution_notes>
<output>Create `02-29-SUMMARY.md` when done.</output>
