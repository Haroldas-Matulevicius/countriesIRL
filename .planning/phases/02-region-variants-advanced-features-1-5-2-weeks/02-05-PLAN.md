---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "05"
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - src/utils/geojson.ts
  - src/utils/geojson.test.ts
  - src/hooks/useGeoData.ts
  - src/utils/worldDataAsset.test.ts
autonomous: true
requirements: [F7.1, F7.2, F7.3, NFR1, NFR9]
must_haves:
  truths:
    - "The browser loads one validated world asset and core lookup from same-origin bundled files (D-01, D-12, D-28)."
    - "Malformed world units are skipped with typed warnings while valid geography remains usable (D-13–D-15)."
  artifacts:
    - path: src/hooks/useGeoData.ts
      provides: "Abortable world asset/manifest loader and lookup"
    - path: src/utils/worldDataAsset.test.ts
      provides: "Exact source/count/policy/path asset gate"
  key_links:
    - from: src/hooks/useGeoData.ts
      to: /data/world-modern.geojson
      via: "same-origin fetch"
      pattern: "world-modern"
---

<objective>
Move the runtime data boundary from fixed Europe to the validated world asset and reviewed manifest.

Purpose: Give camera, country browser, selection, persistence, and scene logic one stable 195-state lookup while preserving partial-failure recovery.
Output: World-aware validator/loader and deterministic asset tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/data.md
@src/utils/geojson.ts
@src/utils/geojson.test.ts
@src/hooks/useGeoData.ts
@public/data/world-manifest.json
@public/data/world-modern.geojson
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend validation for world scene metadata and poles</name>
  <files>src/utils/geojson.test.ts, src/utils/geojson.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-PATTERNS.md P6/P14; src/utils/geojson.ts; src/utils/geojson.test.ts; src/types/map.ts</read_first>
  <behavior>- Latitude exactly -90 and 90 is accepted; values outside are rejected.
- Scene IDs, entity IDs, color-owner IDs, selectability, boundary mode, and provenance are validated from unknown input.
- Reserved object keys, duplicate logical IDs, unsupported geometry, and non-finite coordinates are skipped with warnings.
- Valid neighboring features survive malformed records.</behavior>
  <action>Write tests first, then update normalization to rebuild trusted values from unknown JSON. Preserve the Phase 1 feature ID/name/Polygon/MultiPolygon contract and typed warning recovery. Do not use assertions to bypass manifest joins.</action>
  <verify><automated>npm test -- src/utils/geojson.test.ts</automated></verify>
  <acceptance_criteria>Boundary and malicious-key cases pass; no `any`, fallback display-name ID, crash, or whole-collection discard occurs when valid neighbors remain.</acceptance_criteria>
  <done>The runtime geometry boundary safely accepts world and scene metadata.</done>
  <commit>feat(2-data): validate world scene features</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Load and verify the canonical world asset</name>
  <files>src/hooks/useGeoData.ts, src/utils/worldDataAsset.test.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-PATTERNS.md P4/P13/P14; src/hooks/useGeoData.ts; public/data/world-manifest.json; public/data/world-modern.geojson; scripts/prepareWorldData.mjs</read_first>
  <behavior>- Loader fetches manifest and world asset once, aborts both on unmount, and exposes 195 selectable core states plus all visible units.
- Asset test verifies exact hashes/counts/supplements/parent policy and finite path output.
- Failed manifest or asset leaves a typed fatal state; malformed units become warnings when usable data remains.</behavior>
  <action>Generalize `useGeoData` to same-origin world URLs, validate both payloads as unknown, build O(1) entity/core lookups, and expose reviewed country metadata for search/Locate. Add the exact asset gate and keep the original map-load instrumentation advisory. No runtime third-party request or Europe fallback mode is allowed per D-01 and D-28.</action>
  <verify><automated>npm test -- src/utils/geojson.test.ts src/utils/worldDataAsset.test.ts && node scripts/prepareWorldData.mjs --check</automated></verify>
  <acceptance_criteria>Tests prove 195 selectable states, 248 visible units, six supplements, finite paths, valid parents, abort cleanup, and same-origin URLs.</acceptance_criteria>
  <done>All map consumers can use one validated world data state.</done>
  <commit>feat(2-data): load the unified world dataset</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| bundled JSON → runtime objects | Static files remain untrusted input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-10 | Tampering | manifest/GeoJSON | mitigate | Unknown-input rebuilding, reserved-key rejection, exact asset tests. |
| T-02-11 | Denial of Service | malformed geometry | mitigate | Bounded counts, finite coordinates, supported geometry, safe path generation. |
</threat_model>

<verification>
Focused data tests, deterministic asset check, lint, strict TypeScript, and full unit suite pass.
</verification>

<success_criteria>
The app exposes one stable world/core data state and remains usable when only individual units are malformed.
</success_criteria>

<execution_notes>Do not change branches. Use RED then GREEN commits where practical; preserve all evidence.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-05-SUMMARY.md` when done.</output>
