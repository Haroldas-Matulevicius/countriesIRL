---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "06"
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/utils/camera.ts
  - src/utils/camera.test.ts
  - src/utils/mapProjection.ts
  - src/utils/mapProjection.test.ts
autonomous: true
requirements: [F3.1, F3.2, F3.3, F3.4, F3.5, F7.1, F7.2, F7.3, NFR11]
must_haves:
  truths:
    - "A new camera is whole-world centered and can frame Europe, North America, or any region without a mode selector (D-01, D-02)."
    - "Horizontal transforms wrap continuously with at most one full world at minimum zoom; vertical transforms cannot reveal pole-empty space (D-04, D-05)."
    - "Locate chooses the nearest antimeridian-safe copy and camera persistence is semantic, finite, and canonical (D-15–D-17, D-29)."
  artifacts:
    - path: src/utils/camera.ts
      provides: "Pure wrapped camera, semantic conversion, Locate and pan/zoom math"
    - path: src/utils/camera.test.ts
      provides: "Camera invariant gate"
  key_links:
    - from: src/utils/camera.ts
      to: src/constants/camera.ts
      via: "named camera bounds"
      pattern: "MIN_ZOOM|WORLD_SIZE"
---

<objective>
Implement and prove the pure wrapped-world camera mathematics before any D3 gesture wiring.

Purpose: Make date-line continuity, pole clamping, Locate, persistence, and export deterministic and independently testable.
Output: Fixed Mercator projection plus pure camera transforms and exhaustive invariant tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@src/utils/mapProjection.ts
@src/utils/mapProjection.test.ts
@src/constants/camera.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify wrapped camera and Locate invariants</name>
  <files>src/utils/camera.test.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-RESEARCH.md Patterns 1–4 and Pitfalls 1–2; 02-UI-SPEC.md section 10; src/constants/camera.ts; src/utils/mapProjection.test.ts</read_first>
  <behavior>- `k` clamps to 1–24 and minimum never displays more than one complete world.
- Horizontal translations normalize modulo transformed world width.
- Vertical translation clamps to `[1080-1080k, 0]`.
- Semantic camera round-trips canonical longitude and clamped latitude within numerical tolerance.
- center-anchored zoom preserves the anchor; pan steps move 12.5% of current viewport.
- antimeridian bounds and nearest wrapped copy locate Fiji/Russia/date-line geometries correctly.
- invalid/non-finite persisted values repair to safe initial bounds.</behavior>
  <action>Write table/property-style tests using D3 `zoomIdentity`, `geoBounds`, and representative Polygon/MultiPolygon fixtures. Include programmatic Reset/Locate targets because `zoom.transform` does not apply extents automatically.</action>
  <verify><automated>npm test -- src/utils/camera.test.ts</automated></verify>
  <acceptance_criteria>Tests fail for missing camera utility and cover every D-02–D-05/D-16–D-17 invariant without DOM access.</acceptance_criteria>
  <done>The camera behavior is specified independently of UI and D3 events.</done>
  <commit>test(2-camera): specify wrapped camera invariants</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fixed Mercator and pure camera utilities</name>
  <files>src/utils/camera.ts, src/utils/mapProjection.ts, src/utils/mapProjection.test.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; src/utils/camera.test.ts; src/utils/mapProjection.ts; src/utils/mapProjection.test.ts; src/constants/camera.ts; src/types/composition.ts</read_first>
  <action>Replace fixed-Europe fitting with one canonical square Mercator projection and safe path generation. Implement finite clamp/wrap, `cameraToTransform`, `transformToCamera`, center zoom, step pan, nearest copy, and antimeridian-safe Locate target helpers. Persist only `{zoom, centerLongitude, centerLatitude}`; never raw unbounded x/y. Keep geometry generation separate from camera frames and retain finite path containment.</action>
  <verify><automated>npm test -- src/utils/camera.test.ts src/utils/mapProjection.test.ts && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>All camera/projection tests pass; projection returns finite world paths; no `fitExtent` to Europe, azimuthal projection, per-frame reprojection, or DOM access remains.</acceptance_criteria>
  <done>The mathematically constrained camera is ready for the controller and canvas.</done>
  <commit>feat(2-camera): implement wrapped world camera math</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| persisted/programmatic numbers → camera math | Values may be non-finite or extreme |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-12 | Denial of Service | camera transforms | mitigate | Finite checks, clamped zoom/latitude/y, normalized longitude/x. |
| T-02-13 | Tampering | Locate geometry | mitigate | Use validated geometry plus spherical D3 bounds/centroid, never raw arithmetic means. |
</threat_model>

<verification>
Focused camera/projection tests, lint, strict TypeScript, and full unit suite pass in under the Nyquist feedback target.
</verification>

<success_criteria>
All user and programmatic camera paths share one proven constraint and semantic serialization model.
</success_criteria>

<execution_notes>Do not change branches. Commit RED and GREEN separately. Preserve evidence and do not weaken constants to make tests pass.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-06-SUMMARY.md` when done.</output>
