---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - eslint.config.js
  - playwright.config.ts
  - .gitignore
  - tests/e2e/phase2-composition.spec.ts
autonomous: true
requirements: [F7.1, F7.2, F7.3, NFR11]
must_haves:
  truths:
    - "Phase 2 validation runs only against product/config/test inputs while preserved planning evidence remains untouched (D-28)."
    - "Installed-browser automation starts from the accepted Phase 1 browser behavior and is explicitly replaced by a world baseline at the world cutover."
    - "Every Playwright report, trace, screenshot, video, test result, and download is isolated beneath one ignored artifact root before any browser test runs."
  artifacts:
    - path: package.json
      provides: "Exact-pinned mapshaper and Playwright commands"
    - path: playwright.config.ts
      provides: "Installed Chrome/Edge projects and isolated artifact root"
    - path: .gitignore
      provides: "Exact browser-artifact ignore boundary"
    - path: tests/e2e/phase2-composition.spec.ts
      provides: "Passing pre-cutover Phase 1 browser baseline"
  key_links:
    - from: playwright.config.ts
      to: .gitignore
      via: "all output paths live below the ignored root"
      pattern: "artifacts/playwright"
---

<objective>
Establish the exact-pinned Phase 2 build/browser validation boundary and artifact hygiene before feature work.

Purpose: Preserve accepted Phase 1 evidence, prevent browser artifacts from contaminating exact-commit gates, and give every later browser slice a deterministic installed-Chrome route.
Output: Exact dependencies, source-scoped lint, installed-browser Playwright, ignored artifact isolation, and a passing historical Phase 1 baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-VALIDATION.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@CLAUDE.md
@.planning/CODING_RULES.md
@.planning/coding-rules/general.md
@package.json
@eslint.config.js
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install audited exact build and browser dependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>CLAUDE.md; .planning/coding-rules/general.md; 02-RESEARCH.md Package Legitimacy Audit and Standard Stack; package.json; package-lock.json</read_first>
  <action>Run `npm install --save-dev --save-exact mapshaper@0.7.48 @playwright/test@1.61.1`. Preserve every existing exact pin and do not download Playwright-managed browsers. Add only `"test:e2e": "playwright test"`; Plan 02-04 still introduces `data:world:check` atomically with its implementation. Do not add region modes, deployment, backend, auth, cloud, or environment-secret commands per D-01 and D-28.</action>
  <verify><automated>npm ls mapshaper@0.7.48 @playwright/test@1.61.1 --depth=0</automated></verify>
  <acceptance_criteria>Both packages are exact in package.json/lockfile, existing runtime versions are unchanged, and npm reports no missing/extraneous direct dependency.</acceptance_criteria>
  <done>The approved tools are reproducibly installed without browser downloads or premature data commands.</done>
  <commit>chore(2-tooling): add exact Phase 2 validation tools</commit>
</task>

<task type="auto">
  <name>Task 2: Source-scope lint and isolate every Playwright artifact</name>
  <files>eslint.config.js, playwright.config.ts, .gitignore</files>
  <read_first>.planning/coding-rules/general.md; 02-RESEARCH.md Pitfall 11 and Validation Architecture; 02-PATTERNS.md P14 and Playwright assignment; eslint.config.js; .gitignore; package.json</read_first>
  <action>Keep product/script/config files linted while ignoring `.planning/**` and `.claude/**`. Configure Playwright with installed `chrome` and `msedge` channels, deterministic local Vite webServer, and all output beneath `.artifacts/playwright/`: test results/traces/screenshots/videos, HTML report, and per-test downloads. Add exact `.gitignore` rules for that dedicated root; cleanup remains a second layer, not the primary isolation mechanism. Verify each configured output path with `git check-ignore`. Keep localhost-only behavior per D-28.</action>
  <verify><automated>npm run lint && npm exec playwright -- --version && git check-ignore .artifacts/playwright/test-results/probe.txt .artifacts/playwright/report/index.html .artifacts/playwright/downloads/probe.png</automated></verify>
  <acceptance_criteria>Lint passes without moving/deleting evidence; Chrome/Edge installed channels are exact; every configured output path is ignored; no report/output path escapes `.artifacts/playwright/`.</acceptance_criteria>
  <done>Browser runs cannot pollute the tracked/untracked product evidence boundary.</done>
  <commit>chore(2-validation): isolate browser test artifacts</commit>
</task>

<task type="auto">
  <name>Task 3: Preserve the accepted Phase 1 browser baseline before cutover</name>
  <files>tests/e2e/phase2-composition.spec.ts</files>
  <read_first>.planning/coding-rules/general.md; 02-VALIDATION.md; 02-PATTERNS.md P14; Phase 1 final summaries; playwright.config.ts; src/App.tsx; src/components/MapCanvas.tsx</read_first>
  <action>Create a passing browser baseline that opens the current app, asserts one active workspace, one connected map listbox, the accepted 57 unique non-empty labeled Phase 1 paths, 360px containment, and clean page/console errors. Record in the test description and Plan summary that this assertion is historical pre-cutover evidence: Plan 02-07 must delete/replace the runtime 57-path assertion when the world asset becomes active, while git history and this task summary preserve the Phase 1 result. Do not leave skipped/todo placeholders.</action>
  <verify><automated>npm run test:e2e -- --project=chrome --grep "Phase 1 baseline"</automated></verify>
  <acceptance_criteria>The baseline passes in installed Chrome, writes only ignored artifacts, and explicitly names Plan 02-07 as the world-baseline replacement owner.</acceptance_criteria>
  <done>The accepted Phase 1 runtime behavior is preserved historically without becoming an impossible Phase 2 final assertion.</done>
  <commit>test(2-e2e): preserve the accepted browser baseline</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| package registry → local toolchain | Build/test packages execute locally |
| browser page/downloads → runner artifacts | Browser output is untrusted evidence input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Tampering | npm installs | mitigate | Audit-approved exact versions and lockfile; no browser download path. |
| T-02-02 | Denial of Service | ESLint/artifact discovery | mitigate | Source-scope lint and one ignored Playwright artifact root. |
| T-02-SC | Tampering | npm supply chain | mitigate | Reuse the verified Package Legitimacy Audit and exact pins. |
</threat_model>

<verification>
`npm run lint`, `npm test`, `npm run build`, the Chrome baseline grep, and all `git check-ignore` probes pass.
</verification>

<success_criteria>
Phase 2 has reproducible installed-browser tooling, preserved pre-cutover evidence, and browser outputs that cannot influence tracked-tree or exact-commit conclusions.
</success_criteria>

<execution_notes>Do not create/switch branches. Preserve current `.planning/**` and `.claude/**` evidence. Commit each task separately.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-01-SUMMARY.md` when done.</output>
