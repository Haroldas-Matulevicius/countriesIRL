---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - eslint.config.js
  - playwright.config.ts
  - tests/e2e/phase2-composition.spec.ts
autonomous: true
requirements: [F7.1, F7.2, F7.3, NFR11]
must_haves:
  truths:
    - "Phase 2 validation runs against product files without deleting or scanning preserved planning evidence (D-28)."
    - "Installed-browser automation can exercise the one unified world composer rather than region modes (D-01)."
  artifacts:
    - path: package.json
      provides: "Exact-pinned mapshaper and Playwright commands"
    - path: playwright.config.ts
      provides: "Installed Chrome and Edge projects"
    - path: tests/e2e/phase2-composition.spec.ts
      provides: "Runnable Phase 2 browser smoke boundary"
  key_links:
    - from: package.json
      to: playwright.config.ts
      via: "test:e2e script"
      pattern: "test:e2e"
---

<objective>
Establish the exact-pinned Phase 2 build and browser-validation boundary before feature work.

Purpose: Keep every later task measurable while preserving the Phase 1 evidence tree and local-only runtime.
Output: Source-scoped lint, approved exact dependencies, installed-browser Playwright configuration, and a passing baseline smoke.

Risk: The locked scope exceeds the roadmap's 1.5–2 week estimate once factual historical curation is included; this plan only establishes engineering feedback and does not compress historical evidence work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-VALIDATION.md
@CLAUDE.md
@.planning/CODING_RULES.md
@.planning/coding-rules/general.md
@package.json
@eslint.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install audited exact build and browser dependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>CLAUDE.md; .planning/coding-rules/general.md; 02-RESEARCH.md sections Package Legitimacy Audit, Standard Stack, and Wave 0 Gaps; 02-PATTERNS.md P14 plus package.json assignment; package.json; package-lock.json</read_first>
  <action>Run `npm install --save-dev --save-exact mapshaper@0.7.48 @playwright/test@1.61.1`. Preserve every existing exact pin and do not download Playwright-managed browsers. Set the exact script values `"test:e2e": "playwright test"` and `"data:world:check": "node scripts/prepareWorldData.mjs --check"`; do not add deployment, server, auth, environment-secret, or region-mode commands per D-01 and D-28. Follow the P14 package/script analog rather than generator defaults.</action>
  <verify><automated>npm ls mapshaper@0.7.48 @playwright/test@1.61.1 --depth=0</automated></verify>
  <acceptance_criteria>Both packages are exact in package.json and lockfile; existing React/D3/html2canvas versions are unchanged; npm reports no missing or extraneous direct dependency.</acceptance_criteria>
  <done>The approved tools are reproducibly installed without browser downloads or runtime dependencies.</done>
  <commit>chore(2-tooling): add exact Phase 2 validation tools</commit>
</task>

<task type="auto">
  <name>Task 2: Source-scope lint and configure installed browsers</name>
  <files>eslint.config.js, playwright.config.ts</files>
  <read_first>.planning/coding-rules/general.md; 02-RESEARCH.md Pitfall 11 and Validation Architecture; 02-PATTERNS.md P14 plus package/eslint assignments and No Safe Analog guidance for playwright.config.ts; eslint.config.js; package.json</read_first>
  <action>Extend the existing flat ESLint analog per P14: add `.planning/**` to root ignores while preserving `.claude/**`, keep product/script/config paths linted, and add explicit Node globals for scripts/config plus Playwright test/config files. For the no-safe-analog Playwright boundary, create `playwright.config.ts` with deterministic Vite webServer, projects using `channel: 'chrome'` and `channel: 'msedge'`, isolated output, no remote URL, and no browser-install command. Keep localhost-only behavior per D-28.</action>
  <verify><automated>npm run lint && npm exec playwright -- --version</automated></verify>
  <acceptance_criteria>Lint passes without deleting or moving untracked evidence; config names exactly Chrome and Edge installed channels; no Vercel, cloud, or remote-browser configuration exists.</acceptance_criteria>
  <done>Product lint and local installed-browser automation are deterministic.</done>
  <commit>chore(2-validation): scope lint and configure browser tests</commit>
</task>

<task type="auto">
  <name>Task 3: Create a passing Phase 1 baseline browser smoke</name>
  <files>tests/e2e/phase2-composition.spec.ts</files>
  <read_first>.planning/coding-rules/general.md; 02-VALIDATION.md; 02-PATTERNS.md P14 and No Safe Analog guidance for the first Playwright suite; 01-15-SUMMARY.md; playwright.config.ts; src/App.tsx; src/components/MapCanvas.tsx</read_first>
  <action>Create a source-controlled smoke that opens the current app, asserts one active workspace, one connected map listbox, 57 unique non-empty labeled Phase 1 paths, no page-level horizontal overflow at 360px, and clean page/console errors. Name future describe blocks for camera, legend, history, persistence, export, and accessibility, but do not add skipped or placeholder tests; later plans append real tests.</action>
  <verify><automated>npm run test:e2e -- --grep "Phase 1 baseline"</automated></verify>
  <acceptance_criteria>The smoke passes in both configured projects and produces no browser download, deployment, or planning-tree mutation.</acceptance_criteria>
  <done>The first browser gate preserves the accepted baseline before world changes begin.</done>
  <commit>test(2-e2e): preserve the accepted browser baseline</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| package registry → local toolchain | Build/test packages execute locally |
| browser page → test runner | Browser output and downloads are untrusted evidence inputs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Tampering | npm installs | mitigate | Use only audit-approved exact versions and lockfile; no postinstall/browser download path. |
| T-02-02 | Denial of Service | ESLint discovery | mitigate | Ignore `.planning/**` and `.claude/**`; never delete evidence to make lint pass. |
| T-02-SC | Tampering | npm supply chain | mitigate | Reuse the verified Package Legitimacy Audit and exact pins; fail if metadata/version differs. |
</threat_model>

<verification>
`npm run lint`, `npm test`, `npm run build`, and the baseline Playwright grep all pass without changing product behavior.
</verification>

<success_criteria>
Phase 2 has fast unit/build feedback plus installed Chrome/Edge automation, and preserved evidence no longer contaminates lint.
</success_criteria>

<execution_notes>Do not create, switch, merge, or rebase branches. Preserve all current untracked `.planning/**` and `.claude/**` evidence. Commit each task separately with the specified atomic message.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-01-SUMMARY.md` when done.</output>
