---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "23"
type: execute
wave: 9
depends_on: ["02-22"]
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/main.tsx
autonomous: true
requirements: [F1.1, F1.2, F1.3, F1.4, F1.5, F1.6, F2.1, F2.2, F2.4, F3.1, F3.2, F3.3, F3.4, F3.5, F4.1, F4.2, F4.3, F4.4, F4.5, F5.1, F5.2, F5.3, F5.5, F6.1, F6.2, F7.1, F7.2, F7.3, NFR5, NFR6, NFR7, NFR9, NFR10, NFR11]
must_haves:
  truths:
    - "One active responsive workspace coordinates world data, color state, composition state, camera, history, legend, persistence, and export above presentation branches (D-01, D-18, D-19, D-29)."
    - "Export is one freeze/finalize/measure/capture transaction and complete load is one validate/resolve/apply intent (D-07–D-11, D-19)."
    - "Locate, Reset View, period, and legend edits remain outside color history and preserve Phase 1 color/selection behavior (D-16–D-18)."
    - "The product remains browser-only, same-origin, and localhost-only with no mode selector, server, auth, cloud, or deployment work (D-01, D-28)."
  artifacts:
    - path: src/App.tsx
      provides: "Root cross-store orchestration and one active responsive composition"
    - path: src/main.tsx
      provides: "MapStateProvider plus CompositionStateProvider bootstrap"
    - path: src/App.test.tsx
      provides: "Orchestration and single-DOM contract tests"
  key_links:
    - from: src/App.tsx
      to: src/utils/export.ts
      via: "synchronous preparation callbacks then exportMapPng"
      pattern: "freeze|finalize|exportMapPng"
    - from: src/App.tsx
      to: src/providers/CompositionStateProvider.tsx
      via: "complete load and dirty baseline"
      pattern: "loadComposition"
---

<objective>
Compose all Phase 2 capabilities into one map-first responsive application and atomic cross-store workflows.

Purpose: Make the full locked contract usable without competing stores, partial load/export states, or duplicate responsive DOM.
Output: Provider bootstrap, root orchestration, and integration tests.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/data.md
@.planning/coding-rules/export.md
@.planning/coding-rules/storage.md
@src/App.tsx
@src/App.test.tsx
@src/main.tsx
@src/providers/MapStateProvider.tsx
@src/providers/CompositionStateProvider.tsx
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify one-workspace and atomic orchestration behavior</name>
  <files>src/App.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md responsive/focus/export/save sections; 02-PATTERNS.md P9/P14; src/App.test.tsx; src/App.tsx; all new component/provider public interfaces</read_first>
  <behavior>- Exactly one desktop or compact workspace is mounted at 1200px branch; all durable state lives above it.
- New composition opens whole-world Modern with empty legend and preserved Phase 1 color history.
- Locate/Reset/period/legend calls never call color undo/redo or selection mutations.
- complete load resolves snapshot then batches color load/history reset, composition load, camera restore, focus map, status, and dirty baseline.
- export lock synchronously freezes camera/finalizes scene/validates legend before any await and blocks on legend error.
- responsive remount preserves colors/camera/period/legend and SaveLoad opener recovery.
- no runtime third-party URL, mode selector, service worker, auth, cloud, or deployment path exists.</behavior>
  <action>Extend integration/SSR helper tests and exported orchestration helpers. Use injected callbacks for freeze/load ordering rather than requiring browser DOM for every assertion.</action>
  <verify><automated>npm test -- src/App.test.tsx src/components/Controls.test.tsx src/components/SaveLoad.test.tsx</automated></verify>
  <acceptance_criteria>Tests cover ordering and negative cross-store assertions before App implementation; no test relies on arbitrary timing sleeps.</acceptance_criteria>
  <done>The root integration contract is executable.</done>
  <commit>test(2-app): specify unified composition orchestration</commit>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bootstrap providers and compose the complete responsive workflow</name>
  <files>src/main.tsx, src/App.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; .planning/coding-rules/data.md; .planning/coding-rules/export.md; .planning/coding-rules/storage.md; src/App.test.tsx; src/main.tsx; src/App.tsx; all new hooks/components</read_first>
  <action>Wrap App with MapStateProvider and CompositionStateProvider. In App keep world/snapshot/persistence/toast/export lock/camera controller refs and durable state above the one matchMedia branch. Assemble exact desktop and compact/mobile order from UI-SPEC; wire CompositionBar, MapWorkspace, MapNavigation, selection/color, world CountryList/Locate, Legend disclosure/editor, global actions, SaveLoad, onboarding, and stable live regions. Coordinate complete load only after snapshot resolution and apply colors/composition/camera as one intent. Coordinate export synchronously before await and focus first legend error when blocked. Preserve Phase 1 no-op color/history, focus, storage recovery, and local-only behavior.</action>
  <verify><automated>npm test -- src/App.test.tsx src/components/MapWorkspace.test.tsx src/components/SaveLoad.test.tsx src/components/Controls.test.tsx && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit && npm run build</automated></verify>
  <acceptance_criteria>All integration tests pass; source has one export source/ref, one responsive workspace branch, one storage writer, separate map/composition providers, and no region selector/server/cloud/deployment code.</acceptance_criteria>
  <done>The complete unified world composer is functionally wired.</done>
  <commit>feat(2-app): compose the unified world editor</commit>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| multiple validated subsystems → root transactions | Partial ordering can create inconsistent/exportable state |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-54 | Tampering | complete load | mitigate | Validate/resolve first, then one coordinated batched intent. |
| T-02-55 | Tampering | export transaction | mitigate | Synchronous lock/freeze/finalize/validate before await. |
| T-02-56 | Denial of Service | duplicate state/DOM | mitigate | One active workspace and one owner per state category. |
</threat_model>
<verification>All source tests, lint, strict TypeScript, data checks, and production build pass.</verification>
<success_criteria>A creator can use every Phase 2 capability in one coherent local browser workflow without regressions to Phase 1 state ownership.</success_criteria>
<execution_notes>Do not change branches. Use the GSD executor/workhorse model. Commit tests before implementation and preserve all evidence.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-23-SUMMARY.md` when done.</output>
