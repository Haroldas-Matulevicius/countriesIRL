---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "23"
type: execute
wave: 13
depends_on: ["02-22", "02-29", "02-30"]
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/main.tsx
  - tests/e2e/transactions.spec.ts
autonomous: true
requirements: [F1.1, F1.2, F1.3, F1.4, F1.5, F1.6, F2.1, F2.2, F2.4, F3.1, F3.2, F3.3, F3.4, F3.5, F4.1, F4.2, F4.3, F4.4, F4.5, F5.1, F5.2, F5.3, F5.5, F6.1, F6.2, F7.1, F7.2, F7.3, NFR5, NFR6, NFR7, NFR9, NFR10, NFR11]
must_haves:
  truths:
    - "App is a composition root delegating load/save/export ordering to focused hooks, not a monolithic transaction implementation."
    - "One active responsive workspace keeps state above the branch (D-01, D-18, D-19, D-29)."
    - "App stores exactly one MapCanvasHandle, unbinds/rebinds it through responsive remounts, and delegates every camera/save/load/export operation to the currently visible handle without constructing a second controller."
    - "Provider legend state is rendered through MapWorkspace's typed legend slot as LegendOverlay inside the one canonical SVG, not as a sibling overlay; historical interaction and modern-195 browsing remain truthful."
  artifacts:
    - path: src/App.tsx
      provides: "Composition root and one responsive DOM"
    - path: src/main.tsx
      provides: "Provider bootstrap"
    - path: tests/e2e/transactions.spec.ts
      provides: "Integrated Chrome save/export/remount transaction evidence"
  key_links:
    - from: src/App.tsx
      to: src/components/MapWorkspace.tsx
      via: "one callback-ref-bound MapCanvasHandle plus provider-backed legend slot"
      pattern: "MapCanvasHandle|legendSlot|LegendOverlay"
    - from: src/App.tsx
      to: src/hooks/useCompositionExportTransaction.ts
      via: "current-handle accessor delegated to export/save/load hooks"
      pattern: "useCompositionExportTransaction|getMapCanvasHandle"
---

<objective>
Compose all Phase 2 subsystems while keeping App small and transaction ordering delegated.

Purpose: Deliver one coherent editor without stale camera, competing stores, partial transactions, or duplicate responsive DOM.
Output: Provider bootstrap, composition root, integration tests, and Chrome transaction evidence.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@src/App.tsx
@src/main.tsx
@src/hooks/useCompositionLoadTransaction.ts
@src/hooks/useCompositionSaveTransaction.ts
@src/hooks/useCompositionExportTransaction.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Compose providers, UI, and delegated transactions</name>
  <files>src/App.tsx, src/App.test.tsx, src/main.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md responsive/focus/export/save sections; 02-REVIEWS.md App decomposition; current App/main; all public hook/component interfaces</read_first>
  <behavior>- One desktop/compact workspace at 1200px; durable state above branch.
- App owns one callback ref/current accessor for `MapCanvasHandle`, clears it on unmount, rebinds the replacement visible canvas after the 1200px responsive remount, and never imports/constructs `useCameraController`.
- App derives narrow callbacks from the current handle for MapNavigation zoom/pan, Locate, Reset View, load restore/focus, live save, and export; children never receive controller ownership.
- App calls focused load/save/export hooks and contains no duplicate transaction algorithm.
- One Reset View in CompositionBar; MapNavigation exactly three actions.
- Effective-scene map selection accepts approved historical IDs; CountryList/Locate stays modern 195.
- Export/save locks/status/focus and responsive remount preserve exact camera/composition; tests prove calls move the visible SVG transform before and after remount.
- App creates `LegendOverlay` from provider legend/effective-scene state and passes it through MapWorkspace's typed `legendSlot`; the composed DOM has one SVG, camera group then legend group, and no sibling legend.
- No region selector/server/cloud/deployment path.</behavior>
  <action>Wrap providers, compose exact UI, and pass narrow callbacks/results. Create one stable callback-ref/current-accessor for `MapCanvasHandle`; bind only the mounted visible MapCanvas through MapWorkspace, clear stale handles on unmount, and pass narrow callbacks into MapNavigation, Locate, CompositionBar Reset View, load/save, and export transactions. Build `LegendOverlay` from provider/effective-scene legend state and pass it through MapWorkspace's typed legend slot so it renders inside the canonical SVG. Keep App as wiring and creator-safe feedback only; do not inline validation, snapshot resolution, live-camera assembly, lease lifecycle, or export resource cleanup. Add integration tests with mocked hooks proving delegation and one DOM.</action>
  <verify><automated>npm test -- src/App.test.tsx src/hooks/useCompositionLoadTransaction.test.tsx src/hooks/useCompositionSaveTransaction.test.tsx src/hooks/useCompositionExportTransaction.test.tsx src/components/MapWorkspace.test.tsx src/components/SaveLoad.test.tsx && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit && npm run build</automated></verify>
  <acceptance_criteria>App stays composition-focused; exactly one controller-backed handle is bound, all callbacks affect the visible SVG, responsive remount rebinds before later save/load/export, one canonical SVG contains camera then exact provider legend with no sibling fallback, and all prior delegation/reset/catalog/history cases pass.</acceptance_criteria>
  <done>The complete world editor is composition-green without monolithic App logic.</done>
  <commit>feat(2-app): compose the unified world editor</commit>
</task>

<task type="auto">
  <name>Task 2: Prove integrated live-camera transactions and remount recovery in Chrome</name>
  <files>tests/e2e/transactions.spec.ts</files>
  <read_first>.planning/coding-rules/general.md; 02-REVIEWS.md camera transaction/App concerns; current app; focused transaction/browser specs</read_first>
  <action>Add installed-Chrome integration tests against the real app proving one controller instance/one bound handle, callbacks mutating the visible SVG transform, handle unbind/rebind across responsive remount, save during Locate/wheel then load, export success and each exposed failure class followed by renewed camera input, export during Locate/crossfade, frozen camera preserved through immediate responsive remount, one canonical SVG with camera-independent exact legend and no sibling overlay, approved historical entity color→undo→redo→save/load→legend, and one active workspace/focus continuity. Reuse deterministic app hooks/test controls, not sleeps.</action>
  <verify><automated>npm run test:e2e -- --project=chrome --grep "transaction|responsive remount|historical color history"</automated></verify>
  <acceptance_criteria>All integrated transaction/remount/history cases pass with camera input working after every export path.</acceptance_criteria>
  <done>The cross-subsystem transaction contract is proven in real Chrome.</done>
  <commit>test(2-app): verify composition transactions</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| subsystem outcomes → root UI | Incorrect wiring can create partial/stale state |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-54 | Tampering | root transactions | mitigate | Delegate to tested hooks and mock-order integration tests. |
| T-02-55 | Denial of Service | duplicate state/DOM | mitigate | One owner and one responsive workspace. |
</threat_model>
<verification>Integration/unit/build and focused Chrome transaction tests pass.</verification>
<success_criteria>A creator can use every capability through one coherent root with complete live-camera transactions.</success_criteria>
<execution_notes>Do not change branches. Keep App composition-focused.</execution_notes>
<output>Create `02-23-SUMMARY.md` when done.</output>
