---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "28"
type: execute
wave: 18
depends_on: ["02-27"]
files_modified: []
autonomous: false
requirements: [F2.1, F2.2, F2.3, F2.4, F2.5, F3.2, F3.4, F4.2, F4.3, F4.4, F4.5, F5.2, F7.1, F7.2, F7.3, NFR3, NFR5, NFR7, NFR8, NFR11]
must_haves:
  truths:
    - "Human acceptance runs against a detached preview of the exact Plan 27 verified SHA, not the current workspace."
    - "A fixed matrix records device/OS/browser/screen reader/viewport/preferences/PASS-FAIL-UNAVAILABLE/PNG and historical hashes/observations."
    - "Every mandatory cell must be PASS; physical touch UNAVAILABLE is not passed and blocks completion."
    - "No blocked historical snapshot or unavailable evidence is claimed delivered."
  artifacts:
    - path: .planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-27-EXACT-COMMIT.json
      provides: "Exact verified SHA and automated evidence"
    - path: data/historical-reviewed/1492.approval.json
      provides: "Durable exact historical approval"
  key_links:
    - from: 02-27-EXACT-COMMIT.json
      to: human acceptance matrix
      via: "same verifiedSha"
      pattern: "verifiedSha"
---

<objective>
Complete human-only physical, visual, accessibility, historical, and exact-export acceptance against the exact verified SHA.

Purpose: Close evidence automation cannot prove without generic approval or wrong-build claims.
Output: Fixed acceptance matrix with explicit PASS/FAIL/UNAVAILABLE and exact artifact hashes.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-VALIDATION.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-27-EXACT-COMMIT.json
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-27-SUMMARY.md
@CLAUDE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Start a reproducible preview from the exact verified SHA</name>
  <files>none</files>
  <read_first>02-27 exact evidence/summary; UI-SPEC checklist; four approval files; current git status</read_first>
  <action>Read `verifiedSha` from the exact evidence JSON and verify the commit exists and all recorded public overlay/approval hashes match blobs at that SHA. Create a unique detached acceptance worktree outside the repository at exactly that SHA, run `npm ci` and `npm run build`, then start `npm run preview -- --host 127.0.0.1 --port 4173 --strictPort`. Record absolute worktree path, preview URL, process ID, exact SHA, installed Chrome/Edge versions, device/OS, and available screen-reader route before checkpoint. Do not use current workspace files. Keep the process/worktree until Task 2 resumes, then terminate the recorded PID and remove/prune the exact worktree in outermost cleanup regardless of approval/failure.</action>
  <verify><automated>node -e "const fs=require('node:fs');const e=JSON.parse(fs.readFileSync('.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-27-EXACT-COMMIT.json','utf8'));if(e.status!=='PASS'||!/^[0-9a-f]{40}$/.test(e.verifiedSha))process.exit(1);"</automated></verify>
  <acceptance_criteria>Preview is served only from exact verified SHA on 127.0.0.1:4173 with recorded PID/worktree and deterministic cleanup instructions.</acceptance_criteria>
  <done>The correct tested build is ready for human checks.</done>
  <commit>none — verification setup only</commit>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Complete the fixed Phase 2 acceptance matrix</name>
  <read_first>Exact preview details; UI-SPEC sections 7–24; VALIDATION Manual-Only Verifications; exact evidence JSON; four approval records</read_first>
  <action>Perform the matrix against the exact preview only. The final summary must contain: verified SHA; device model/OS; browser/version per route; screen reader/version; every viewport 1440/1024/768/360; 200% zoom; light/dark; reduced motion/transparency; increased contrast; forced colors; physical one-finger pan/pinch; keyboard/focus/screen-reader flows; every historical atlas; normal/Pacific/during-Locate/during-crossfade exports; PASS/FAIL/UNAVAILABLE; observations; PNG filename/dimensions/SHA-256; and source/input/output/review/approval hashes for each snapshot. Mandatory physical touch, browser, screen-reader, viewport/preference, historical, and export cells must be PASS. UNAVAILABLE physical touch or screen reader blocks approval. After response, executor performs the recorded PID/worktree cleanup before summary.</action>
  <what-built>The exact clean-commit unified world composer with durable approved history, camera transactions, legend, complete saves, and exact PNG export.</what-built>
  <how-to-verify>Use the fixed matrix: physical pan/pinch/date-line/pole/no accidental selection and alternatives; visual hierarchy/tokens/copy/one DOM/360/200%/preferences; keyboard and named screen reader through color/Locate/camera/period/legend/save/load/export; Chrome and Edge Pacific historical exports during movement/crossfade; reopen all four atlases and compare exact hashes; confirm no console/runtime/product-network error or crash.</how-to-verify>
  <verify><automated>node -e "const fs=require('node:fs');const e=JSON.parse(fs.readFileSync('.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-27-EXACT-COMMIT.json','utf8'));if(e.status!=='PASS')process.exit(1);"</automated></verify>
  <acceptance_criteria>Every mandatory matrix cell explicitly PASS on named routes; PNG and historical hashes recorded; no generic approval, inferred availability, or blocked snapshot claim.</acceptance_criteria>
  <done>Phase 2 has exact-SHA human acceptance for every mandatory non-automatable outcome.</done>
  <resume-signal>Return the completed matrix with PASS/FAIL/UNAVAILABLE and observations; generic `approved Phase 2` is insufficient.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| exact SHA preview → human observations | Wrong build or vague matrix could overstate acceptance |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-65 | Repudiation | human acceptance | mitigate | Same verified SHA and fixed named matrix. |
| T-02-66 | Spoofing | physical/historical claims | mitigate | No emulation/automation substitution; exact hashes. |
</threat_model>
<verification>Exact evidence remains PASS and completed matrix has no missing/UNAVAILABLE mandatory cell.</verification>
<success_criteria>The full locked Phase 2 contract is accepted truthfully against one exact revision.</success_criteria>
<execution_notes>No product changes or deployment. Always clean preview PID/worktree after checkpoint resumes.</execution_notes>
<output>Create `02-28-SUMMARY.md` containing the full fixed matrix and exact SHA.</output>
