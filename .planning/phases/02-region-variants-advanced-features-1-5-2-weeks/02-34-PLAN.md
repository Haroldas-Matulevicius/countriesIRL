---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "34"
type: execute
wave: 7
depends_on: ["02-13", "02-14", "02-15", "02-16"]
files_modified: []
autonomous: false
requirements: [F2.1, F2.2, F2.3, F2.4, F2.5, NFR8, NFR9]
must_haves:
  truths:
    - "A named qualified factual reviewer evaluates all six regions for each exact candidate only after the current source-approval bundle validates, and supplies its SHA plus candidate/review hashes."
    - "Executor self-approval is forbidden; partial approval does not make a snapshot promotion-eligible."
  artifacts:
    - path: data/historical-reviewed/1492.approval-request.json
      provides: "Exact candidate hash bundle for review"
    - path: data/historical-reviewed/1914.review.html
      provides: "Self-contained factual review atlas"
  key_links:
    - from: approval request artifacts
      to: Plan 35 durable approvals
      via: "structured human response bound to exact hashes"
      pattern: "sourceApprovalSha256|sourceManifestSha256|reviewHtmlSha256"
---

<objective>
Obtain qualified factual approval for the exact four candidate bundles after curation.

Purpose: Bind NFR8 and six-region decisions to named human review rather than a generic resume signal.
Output: Structured reviewer payload for durable sealing in Plan 35.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-VALIDATION.md
@data/historical-reviewed/1492.approval-request.json
@data/historical-reviewed/1700.approval-request.json
@data/historical-reviewed/1815.approval-request.json
@data/historical-reviewed/1914.approval-request.json
@sources/historical/1492.source-approval.json
@sources/historical/1700.source-approval.json
@sources/historical/1815.source-approval.json
@sources/historical/1914.source-approval.json
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Review all exact historical candidates and provide structured approval</name>
  <files>none</files>
  <read_first>All four approval-request JSON files; source manifests/input geometries/output overlays/review JSON/review HTML; 02-VALIDATION.md NFR8 instructions</read_first>
  <action>A reviewer who is not the executor/implementer reviews every snapshot and each exact region ID separately. Before factual review, require the current durable source-approval JSON to pass machine validation for each snapshot. Supply reviewer full name, role/qualification, date, per-region PASS/FAIL, uncertainties/observations, and confirm the exact `sourceApprovalSha256` plus five candidate/review hashes copied from each approval request: source manifest, input/tracing geometry, output overlay, review JSON, review HTML. Any source-approval mismatch/change, source/input/output/review byte mismatch, missing region, merged Poland/Lithuania decision, or failed region blocks that snapshot.</action>
  <what-built>Four exact candidate overlays and self-contained six-region atlases with hash request bundles.</what-built>
  <how_to_verify>Open each review HTML, compare all six panels to the committed evidence/source manifest, verify labels/identity/fallback/topology at Instagram scale, and cross-check every request hash with the current file bytes.</how_to_verify>
  <verify><automated>for id in 1492 1700 1815 1914; do node scripts/prepareHistoricalSnapshot.mjs --snapshot "$id" --sources "sources/historical/$id.sources.json" --input "sources/historical/$id.input.geojson" --source-approval "sources/historical/$id.source-approval.json" --output "data/historical-reviewed/$id.geojson" --review-output "data/historical-reviewed/$id.review.json" --review-html "data/historical-reviewed/$id.review.html" --check || exit 1; done</automated></verify>
  <acceptance_criteria>Reviewer identity/role/date, 24 separate regional decisions, uncertainties, current source-approval SHA, and five candidate/review hashes per snapshot are supplied; all mandatory cells PASS and reviewer is not executor/implementer.</acceptance_criteria>
  <done>Complete structured factual review is available for durable sealing.</done>
  <resume-signal>Provide the exact structured reviewer payload; do not use a generic `approved` signal.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| candidate bytes → factual approval | Reviewer could approve stale/different bytes |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-72 | Spoofing | reviewer identity | mitigate | Named role/date and explicit non-executor requirement. |
| T-02-73 | Tampering | reviewed bytes | mitigate | Current source-approval SHA plus five exact candidate/review hashes per snapshot. |
</threat_model>
<verification>All candidate checks remain green and every structured human cell is PASS.</verification>
<success_criteria>Factual approval is exact, qualified, six-region, and byte-bound.</success_criteria>
<execution_notes>No files are modified; failed/unavailable review blocks promotion.</execution_notes>
<output>Create `02-34-SUMMARY.md` preserving the full structured reviewer payload.</output>
