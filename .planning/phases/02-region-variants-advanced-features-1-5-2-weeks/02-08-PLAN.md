---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "08"
type: execute
wave: 5
depends_on: ["02-07"]
files_modified:
  - src/components/MapNavigation.tsx
  - src/components/MapNavigation.test.tsx
  - tests/e2e/navigation.spec.ts
  - tests/e2e/fixtures/navigation.html
autonomous: true
requirements: [F3.2, F3.3, F3.4, F3.5, NFR11]
must_haves:
  truths:
    - "Every essential pan/zoom action has a visible single-pointer and keyboard alternative (D-06)."
    - "MapNavigation contains exactly Zoom In, Zoom Out, and Move Map; CompositionBar remains the sole Reset View owner (D-17)."
    - "Browser focus, outside-click, and repeated activation behavior are proven in real Chrome rather than inferred from Node SSR."
  artifacts:
    - path: src/components/MapNavigation.tsx
      provides: "Exact navigation cluster and pan popover"
    - path: tests/e2e/navigation.spec.ts
      provides: "Focused Chrome navigation/focus slice"
  key_links:
    - from: src/components/MapNavigation.tsx
      to: src/hooks/useCameraController.ts
      via: "bounded semantic callbacks"
      pattern: "onZoomIn|onZoomOut|onPan"
---

<objective>
Provide the exact accessible camera-control alternatives and prove their browser behavior.

Purpose: Satisfy direct-manipulation alternatives without keyboard-command collisions or duplicate Reset View ownership.
Output: Tested navigation component plus focused Chrome coverage.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@src/components/Controls.tsx
@src/hooks/useCameraController.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the exact three-action navigation cluster</name>
  <files>src/components/MapNavigation.tsx, src/components/MapNavigation.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md sections 10 and 20; 02-PATTERNS.md MapNavigation assignment; src/components/Controls.test.tsx; src/hooks/useCameraController.ts</read_first>
  <behavior>- Exactly three 44x44 buttons are Zoom In, Zoom Out, Move Map.
- Move Map exposes four 44x44 pan buttons.
- Zoom buttons are natively disabled at limits.
- No Reset View action/callback is rendered.
- All navigation nodes are editor-only.</behavior>
  <action>Implement project-owned 20x20 inline SVG icons, exact native labels/states, controlled popover, center-anchored factor-1.5 zoom, and 12.5% pan callbacks. Node tests cover static semantics/helper state only; do not claim real focus/outside-click behavior until Task 2.</action>
  <verify><automated>npm test -- src/components/MapNavigation.test.tsx && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>Exact actions/dimensions/native states and absence of Reset View pass.</acceptance_criteria>
  <done>The semantic navigation surface is implementation-green.</done>
  <commit>feat(2-navigation): add the exact map controls</commit>
</task>

<task type="auto">
  <name>Task 2: Prove navigation focus and dismissal in installed Chrome</name>
  <files>tests/e2e/navigation.spec.ts, tests/e2e/fixtures/navigation.html</files>
  <read_first>.planning/coding-rules/general.md; 02-REVIEWS.md browser-allocation concern; playwright.config.ts; src/components/MapNavigation.tsx; src/hooks/useCameraController.ts</read_first>
  <action>Create a Vite-served navigation fixture and focused Chrome Playwright cases for opening the pan popover, Escape close with focus restored to Move Map, outside-click close, min/max disabled states, repeated pan/zoom callbacks, editor-only marking, and absence of Reset View. Use state/event waits, not sleeps.</action>
  <verify><automated>npm run test:e2e -- --project=chrome --grep "camera controls|Move Map"</automated></verify>
  <acceptance_criteria>All real-browser focus/dismissal/disabled cases pass with no skipped tests; outputs stay under the ignored artifact root.</acceptance_criteria>
  <done>The navigation interaction claim is backed by Chrome evidence in its owning plan.</done>
  <commit>test(2-navigation): verify browser camera controls</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| repeated controls → camera callbacks | Rapid input must remain bounded |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-17 | Denial of Service | navigation buttons | mitigate | Bounded controller operations and truthful disabled states. |
| T-02-18 | Spoofing | icon-only controls | mitigate | Exact labels/tooltips/focus and real-browser verification. |
</threat_model>

<verification>Focused Node semantics and installed-Chrome navigation tests pass.</verification>
<success_criteria>All direct camera gestures have usable semantic alternatives and exactly one Reset View remains elsewhere.</success_criteria>
<execution_notes>Do not change branches. Commit each task atomically.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-08-SUMMARY.md` when done.</output>
