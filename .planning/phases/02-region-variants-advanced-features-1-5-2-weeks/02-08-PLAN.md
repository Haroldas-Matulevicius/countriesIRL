---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "08"
type: execute
wave: 5
depends_on: ["02-07"]
files_modified:
  - src/components/MapNavigation.tsx
  - src/components/MapNavigation.test.tsx
autonomous: true
requirements: [F3.2, F3.3, F3.4, F3.5, NFR11]
must_haves:
  truths:
    - "Every essential pan/zoom action has a visible single-pointer and keyboard alternative (D-06)."
    - "Reset View returns only the camera to whole-world fit and is a truthful no-op when already there (D-17, D-18)."
  artifacts:
    - path: src/components/MapNavigation.tsx
      provides: "Zoom, Move Map popover, pan buttons, and Reset View callbacks"
  key_links:
    - from: src/components/MapNavigation.tsx
      to: src/hooks/useCameraController.ts
      via: "semantic callbacks supplied by MapWorkspace/App"
      pattern: "onZoomIn|onPan|onResetView"
---

<objective>
Provide the approved semantic camera controls that make drag and pinch functionality accessible without keyboard-command collisions.

Purpose: Satisfy the flexible-centering/zoom intent with one camera and explicit alternatives instead of regional preset modes.
Output: Tested map navigation cluster and popover behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@src/components/Controls.tsx
@src/hooks/useCameraController.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify navigation semantics and focus behavior</name>
  <files>src/components/MapNavigation.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md sections 10 and 20; 02-PATTERNS.md MapNavigation assignment; src/components/Controls.test.tsx</read_first>
  <behavior>- Exactly three 44x44 cluster buttons are labeled Zoom In, Zoom Out, Move Map.
- Move Map exposes four 44x44 pan buttons and closes on Escape/outside click.
- Escape restores focus to Move Map.
- Zoom buttons are natively disabled at limits.
- Reset View has visible text, is disabled at the initial view, and announces no no-op.
- All overlay nodes are editor-only.</behavior>
  <action>Use static semantic tests plus pure exported focus/dismiss helpers; do not introduce icon libraries, emoji, or Arrow-key camera shortcuts on country paths.</action>
  <verify><automated>npm test -- src/components/MapNavigation.test.tsx</automated></verify>
  <acceptance_criteria>Exact labels, native semantics, dimensions/classes, aria-expanded/controls, and focus restoration are asserted before implementation.</acceptance_criteria>
  <done>The accessible navigation contract is test-defined.</done>
  <commit>test(2-navigation): specify map navigation controls</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement map navigation and Reset View controls</name>
  <files>src/components/MapNavigation.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; src/components/MapNavigation.test.tsx; src/hooks/useCameraController.ts; src/constants/camera.ts</read_first>
  <action>Implement project-owned 20x20 inline SVG icons inside exact 44x44 native buttons, a controlled pan popover, Escape/outside dismissal, focus return, min/max disabled states, center-anchored factor-1.5 zoom callbacks, 12.5% pan callbacks, and visible `Reset View`. Mark cluster/popover editor-only. Keep status messaging parent-owned and do not mutate camera, selection, or history in the component itself.</action>
  <verify><automated>npm test -- src/components/MapNavigation.test.tsx && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>All tests pass; no third-party icons or mode selector exists; controls expose typed event-only props and visible alternatives required by D-06.</acceptance_criteria>
  <done>Creators can pan, zoom, and reset through semantic controls.</done>
  <commit>feat(2-navigation): add accessible camera controls</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| repeated button activation → camera callbacks | User can generate rapid events |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-17 | Denial of Service | navigation buttons | mitigate | Bounded controller operations and truthful disabled states. |
| T-02-18 | Spoofing | icon-only controls | mitigate | Exact aria-labels, tooltips, and visible focus; no ambiguous glyph-only semantics. |
</threat_model>

<verification>
Focused tests, lint, strict TypeScript, and full unit suite pass.
</verification>

<success_criteria>
All direct map gestures have equivalent visible controls, and Reset View remains separate from color reset/history.
</success_criteria>

<execution_notes>Do not change branches. Use only project-owned inline SVG and atomic task commits.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-08-SUMMARY.md` when done.</output>
