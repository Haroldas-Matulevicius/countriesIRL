---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "08"
type: execute
wave: 5
depends_on: ["02-07"]
files_modified:
  - src/components/MapNavigation.tsx
  - src/components/MapNavigation.test.tsx
autonomous: true
requirements: [F3.2, F3.3, F3.4, F3.5, NFR11]
must_haves:
  truths:
    - "Every essential pan/zoom action has a visible single-pointer and keyboard alternative (D-06)."
    - "The visible MapNavigation cluster contains exactly Zoom In, Zoom Out, and Move Map; it never duplicates the CompositionBar Reset View action (D-06, D-17)."
  artifacts:
    - path: src/components/MapNavigation.tsx
      provides: "Exactly Zoom In, Zoom Out, Move Map, and the four-direction pan popover"
  key_links:
    - from: src/components/MapNavigation.tsx
      to: src/hooks/useCameraController.ts
      via: "semantic callbacks supplied by MapWorkspace/App"
      pattern: "onZoomIn|onZoomOut|onPan"
---

<objective>
Provide the approved semantic camera controls that make drag and pinch functionality accessible without keyboard-command collisions.

Purpose: Satisfy the flexible-centering/zoom intent with one camera and explicit alternatives instead of regional preset modes.
Output: Tested map navigation cluster and popover behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@src/components/Controls.tsx
@src/hooks/useCameraController.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and prove the exact three-action map navigation cluster</name>
  <files>src/components/MapNavigation.tsx, src/components/MapNavigation.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md sections 10 and 20; 02-PATTERNS.md MapNavigation assignment and P14; src/components/Controls.test.tsx; src/hooks/useCameraController.ts; src/constants/camera.ts</read_first>
  <behavior>- Exactly three 44x44 cluster buttons are labeled Zoom In, Zoom Out, and Move Map.
- Move Map exposes four 44x44 pan buttons and closes on Escape/outside click.
- Escape restores focus to Move Map.
- Zoom buttons are natively disabled at limits.
- No Reset View button, link, menu item, or duplicate callback is rendered by MapNavigation.
- All cluster/popover nodes are editor-only and country-path Arrow keys remain reserved for roving focus.</behavior>
  <action>Write semantic/focus tests first in this task, then implement project-owned 20x20 inline SVG icons inside exact 44x44 native buttons, a controlled pan popover, Escape/outside dismissal, focus return, min/max disabled states, center-anchored factor-1.5 zoom callbacks, and 12.5% pan callbacks. Mark cluster/popover editor-only. Per the binding UI-SPEC and D-17, CompositionBar is the sole visible Reset View owner; MapNavigation must not render or accept a visible Reset View action, although the camera controller may retain its imperative reset method for CompositionBar/App. Keep status messaging parent-owned. End with the passing verification command and no RED-only commit.</action>
  <verify><automated>npm test -- src/components/MapNavigation.test.tsx && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>Exact labels, native semantics, dimensions/classes, aria-expanded/controls, focus restoration, and absence of Reset View are asserted and pass; no third-party icon, region mode, or country Arrow-key camera shortcut exists.</acceptance_criteria>
  <done>Creators have accessible pan/zoom alternatives while Reset View remains uniquely owned by CompositionBar.</done>
  <commit>feat(2-navigation): add the exact map navigation cluster</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| repeated button activation → camera callbacks | User can generate rapid events |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-17 | Denial of Service | navigation buttons | mitigate | Bounded controller operations and truthful disabled states. |
| T-02-18 | Spoofing | icon-only controls | mitigate | Exact aria-labels, tooltips, and visible focus; no ambiguous glyph-only semantics. |
</threat_model>

<verification>
Focused tests, lint, strict TypeScript, and full unit suite pass.
</verification>

<success_criteria>
All direct pan/zoom gestures have equivalent visible controls, and the composed UI can contain exactly one Reset View action in CompositionBar.
</success_criteria>

<execution_notes>Do not change branches. Use only project-owned inline SVG and atomic task commits.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-08-SUMMARY.md` when done.</output>
