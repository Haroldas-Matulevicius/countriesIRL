---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "04"
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - scripts/prepareWorldData.mjs
  - public/data/world-modern.geojson
  - public/data/world-manifest.json
  - public/data/README.md
  - .gitattributes
autonomous: true
requirements: [F7.1, F7.2, F7.3, NFR9]
must_haves:
  truths:
    - "One bundled modern world contains exactly 195 selectable core states (D-01, D-12)."
    - "Visible non-core units use reviewed parent inheritance or neutral policy, never runtime name inference (D-13, D-14)."
    - "Small island states retain true geometry and remain available through search/Locate (D-15)."
  artifacts:
    - path: scripts/prepareWorldData.mjs
      provides: "Hash-pinned deterministic hybrid Natural Earth pipeline"
    - path: public/data/world-manifest.json
      provides: "195 core IDs, source joins, parent/neutral policy, hashes, license"
    - path: public/data/world-modern.geojson
      provides: "Canonical 248-unit runtime asset"
  key_links:
    - from: scripts/prepareWorldData.mjs
      to: public/data/world-modern.geojson
      via: "canonical generation and --check"
      pattern: "--check"
---

<objective>
Generate the deterministic modern-world data platform that replaces the fixed Europe asset at runtime.

Purpose: Satisfy the world/North America/Europe expansion intent through one authoritative canvas without political or identity inference in the browser.
Output: Reviewed manifest, canonical hybrid asset, provenance, and checkout-stable bytes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/data.md
@scripts/prepareGeoData.mjs
@public/data/README.md
@.gitattributes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the reviewed canonical world manifest</name>
  <files>public/data/world-manifest.json</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-RESEARCH.md Modern World Data Recommendation; 02-CONTEXT.md D-12–D-15; public/data/README.md</read_first>
  <action>Create a canonical reviewed manifest containing Natural Earth 5.1.1 50m source URL/hash `3e458fc036ad0a66411f2c1e6cac49c5d7bfb81cb1123bc513b22511a2b7fdeb`, the exact 195-state list, source-ID exception joins, six 10m supplements `ESB, WSB, UMI, CSI, CLP, GIB`, parent color-owner mappings, and explicit neutral/non-selectable records. Use the research discretion outcome that ambiguous Cook Islands/Niue and disputed/indeterminate units remain visible, neutral, and non-selectable; do not create a runtime POV toggle per D-14.</action>
  <verify><automated>node -e "const m=require('./public/data/world-manifest.json'); const ids=new Set(m.coreStates.map(x=>x.id)); if(ids.size!==195||m.supplements.length!==6) process.exit(1)"</automated></verify>
  <acceptance_criteria>Exactly 195 unique core IDs exist; every parent points to a core ID or null; all source URLs, versions, hashes, license/POV policy, and reviewed exceptions are explicit.</acceptance_criteria>
  <done>Political identity and inheritance policy is data-driven and reviewable.</done>
  <commit>feat(2-world): define canonical world manifest</commit>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic hybrid world generation</name>
  <files>scripts/prepareWorldData.mjs, public/data/world-modern.geojson, .gitattributes</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; 02-PATTERNS.md P13; scripts/prepareGeoData.mjs; public/data/world-manifest.json; package.json</read_first>
  <action>Implement build-only ESM generation using exact local/URL sources, SHA-256 verification, mapshaper only for named deterministic topology operations, strict Polygon/MultiPolygon validation, minimal runtime properties, stable sorting, compact JSON plus LF, and non-mutating `--check`. Assert 248 units, 195 selectable core states, six supplements, no duplicate logical/source IDs, finite coordinates, no unused source properties, and parent/neutral policy match. Add LF rules for world/snapshot JSON/GeoJSON. Never hand-edit generated output.</action>
  <verify><automated>node scripts/prepareWorldData.mjs && node scripts/prepareWorldData.mjs --check</automated></verify>
  <acceptance_criteria>Generation succeeds from pinned sources; a tampered source hash or committed byte causes nonzero exit; output is canonical and contains no runtime third-party URL requirement.</acceptance_criteria>
  <done>The committed world asset is reproducible, bounded, and offline-ready.</done>
  <commit>feat(2-world): generate deterministic world asset</commit>
</task>

<task type="auto">
  <name>Task 3: Record world provenance and runtime policy</name>
  <files>public/data/README.md</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; public/data/world-manifest.json; scripts/prepareWorldData.mjs; public/data/README.md</read_first>
  <action>Append exact source/version/hash/license, 50m+10m composition, 195/248 counts, default Natural Earth POV, parent/neutral policy, generation/check commands, and offline same-origin boundary. Preserve Phase 1 provenance. Do not claim disputed units are core states or that historical snapshots are complete.</action>
  <verify><automated>node scripts/prepareWorldData.mjs --check && npm run lint</automated></verify>
  <acceptance_criteria>README contains both Phase 1 and Phase 2 provenance without contradicting D-01 or advertising unreviewed history.</acceptance_criteria>
  <done>Dataset origin and policy can be independently audited.</done>
  <commit>docs(2-data): document world asset provenance</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| upstream Natural Earth bytes → build script | Remote/local source bytes are untrusted |
| generated JSON → browser | Bundled assets can be malformed or substituted |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-07 | Tampering | source assets | mitigate | Exact SHA-256, source-version URLs, canonical byte checks, reviewed manifest. |
| T-02-08 | Denial of Service | geometry | mitigate | Exact counts, supported geometry only, finite coordinates, minimal properties, deterministic topology operations. |
| T-02-09 | Information Disclosure | source metadata | accept | Provenance is intentionally public and contains no secrets. |
</threat_model>

<verification>
Generation and `--check` pass twice; manifest counts and policy references are exact; no runtime source fetch is added.
</verification>

<success_criteria>
A deterministic 248-unit world asset exposes exactly 195 selectable states and reviewed inheritance/neutral behavior.
</success_criteria>

<execution_notes>Do not change branches or hand-edit generated files. Preserve all existing evidence and commit each task atomically.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-04-SUMMARY.md` when done.</output>
