---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "24"
type: execute
wave: 10
depends_on: ["02-23"]
files_modified:
  - src/styles/theme.css
  - src/styles/App.css
  - src/styles/MapCanvas.css
  - src/styles/Controls.css
  - src/styles/phase2CssContract.test.ts
autonomous: true
requirements: [F3.4, F4.2, F4.4, F4.5, F5.2, NFR5, NFR7, NFR11]
must_haves:
  truths:
    - "The white export-safe map square remains dominant while restrained liquid-glass chrome appears only on approved surfaces (D-08, D-10)."
    - "One active DOM follows exact desktop/tablet/mobile hierarchy, contains at 360px/200% zoom, and preserves camera/composition state (D-06, D-19)."
    - "Reduced motion removes camera/crossfade motion; reduced transparency/high contrast/forced colors receive opaque semantic fallbacks (D-06, D-23)."
    - "Legend composition uses only export-safe SVG presentation and remains inside the 32-unit inset (D-26, D-27)."
  artifacts:
    - path: src/styles/theme.css
      provides: "Exact approved tokens, themes, motion and preference fallbacks"
    - path: src/styles/App.css
      provides: "Responsive one-DOM workspace layout"
    - path: src/styles/MapCanvas.css
      provides: "World square, navigation, path, tooltip, legend editor/export separation"
    - path: src/styles/Controls.css
      provides: "Unified inspector, controls, modal/sheet, toast presentation"
  key_links:
    - from: src/styles/App.css
      to: src/App.tsx
      via: "desktop/compact class contracts"
      pattern: "workspace--desktop|workspace--compact"
---

<objective>
Apply the binding Phase 2 visual contract, responsive hierarchy, accessibility preferences, and export isolation.

Purpose: Make the unified composer intentionally map-first and polished without generic dashboard chrome or export-unsafe effects.
Output: Four updated component-scoped stylesheets.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/export.md
@src/styles/theme.css
@src/styles/App.css
@src/styles/MapCanvas.css
@src/styles/Controls.css
@src/App.tsx
</context>
<tasks>
<task type="auto">
  <name>Task 1: Implement exact tokens, light/dark chrome, and preference fallbacks</name>
  <files>src/styles/theme.css, src/styles/phase2CssContract.test.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; .planning/coding-rules/export.md; 02-UI-SPEC.md sections 2–6 and 20; src/styles/theme.css</read_first>
  <action>Create a source-scoped Vitest CSS/UI contract test, then replace tokens with exact spacing 4/8/16/24/32/48/64, font sizes 14/16/20/28, weights 400/600, radii 8/16, motion 150/160/240, exact teal/light/dark/map colors, and approved shadows. The test must assert every exact token value and allowed set, reject extra spacing/font/weight/radius/motion values and all gradients, and prove glass declarations occur only for the app bar, inspector shell, and map-navigation cluster under `@supports`, with opaque/mobile/reduced-transparency/more-contrast/forced-colors fallbacks. Keep creator map fills fixed outside dark overrides.</action>
  <verify><automated>npm test -- src/styles/phase2CssContract.test.ts && npm run lint && npm run build</automated></verify>
  <acceptance_criteria>The executable contract test proves exact token values/allowed sets, no gradient or unapproved glass selector, complete opaque preference fallbacks, and fixed map/export tokens outside dark overrides.</acceptance_criteria>
  <done>The approved visual system is encoded in one token source.</done>
  <commit>feat(2-styles): define the Phase 2 visual tokens</commit>
</task>
<task type="auto">
  <name>Task 2: Implement responsive map-first layout and unified inspector</name>
  <files>src/styles/App.css, src/styles/Controls.css, src/styles/phase2CssContract.test.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md sections 7–12 and 15–18; src/App.tsx; src/styles/App.css; src/styles/Controls.css</read_first>
  <action>Extend the CSS/UI contract test, then implement sticky app bar, centered max-1440 desktop grid `minmax(0,1fr) 376px` with 24px gap, one inspector shell with divided sections, the exact 1200 React branch-compatible contract and 900/768 CSS sublayouts, mobile two-column actions with full-width Export, full-height Save/Load sheet, exact 48px standard and 44px icon controls, country/Locate/legend states, safe areas, 360px containment, and 200% zoom usability. The test must reject CSS `order`, duplicate workspace trees hidden by display/visibility/offscreen rules, nested glass cards, fixed widths exceeding `calc(100vw - 32px)`, and any page-level horizontal-scroll workaround.</action>
  <verify><automated>npm test -- src/styles/phase2CssContract.test.ts src/App.test.tsx src/components/Controls.test.tsx src/components/SaveLoad.test.tsx && npm run build</automated></verify>
  <acceptance_criteria>The executable contract test confirms exact 1200/900/768 rules, 360px containment, target sizes, desktop grid/sheet behavior, and absence of CSS order or duplicate-tree hiding.</acceptance_criteria>
  <done>The workflow hierarchy is correct at desktop, tablet, and 360px mobile.</done>
  <commit>feat(2-styles): compose the responsive world workspace</commit>
</task>
<task type="auto">
  <name>Task 3: Style world paths, camera controls, tooltip, legend, and export isolation</name>
  <files>src/styles/MapCanvas.css, src/styles/Controls.css, src/styles/phase2CssContract.test.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; .planning/coding-rules/export.md; 02-UI-SPEC.md sections 6, 10, 12–14, 19–20; src/components/MapCanvas.tsx; src/components/LegendOverlay.tsx; src/components/MapNavigation.tsx; src/styles/MapCanvas.css</read_first>
  <action>Extend the CSS/UI contract test, then style the square white scene, `touch-action:none` only on the interactive square, non-scaling 1px boundaries, 2px selected, 3px teal dashed focused logical path, no duplicate-copy focus, one top-left approved glass navigation cluster, popover, measured tooltip, period fallback status, SVG legend editor-only hit target, validation states, and complete `[data-editor-only]` export exclusion. The test must reject gradients everywhere and reject `filter`, `backdrop-filter`, blur, `box-shadow`, text-shadow, unsupported CSS transforms, or foreignObject-dependent styling on the exportable map/legend selectors; only the three approved editor-chrome glass selectors may use backdrop-filter.</action>
  <verify><automated>npm test -- src/styles/phase2CssContract.test.ts src/components/MapWorkspace.test.tsx src/components/LegendEditor.test.tsx && npm run build</automated></verify>
  <acceptance_criteria>The executable contract test proves export-map/legend selectors contain no prohibited effects, gradients, or filters; glass remains restricted; exact focus/target, legend inset/overflow, and square-only touch-action behavior pass.</acceptance_criteria>
  <done>The live editor is polished while exported composition remains deterministic.</done>
  <commit>feat(2-styles): style world navigation and legend</commit>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| persisted style state → CSS/SVG presentation | Values must not become arbitrary CSS |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-57 | Tampering | legend/style classes | mitigate | Components emit only validated enum classes/numeric SVG attributes. |
| T-02-58 | Denial of Service | large blurred surfaces | mitigate | Opaque baseline, glass on three small chrome surfaces only, no map filters. |
| T-02-59 | Spoofing | color-only states | mitigate | Border/check/text/focus/ARIA cues accompany all state colors. |
</threat_model>
<verification>Source tests, static CSS contract probes, lint, strict TypeScript, and production build pass.</verification>
<success_criteria>The UI matches the approved map-first design contract at all breakpoints and preference modes without export drift.</success_criteria>
<execution_notes>Do not change branches. Treat UI-SPEC tokens/copy/layout as binding, not inspiration. Commit each styling slice separately.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-24-SUMMARY.md` when done.</output>
