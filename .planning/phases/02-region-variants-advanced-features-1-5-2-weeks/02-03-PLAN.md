---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "03"
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/providers/CompositionStateProvider.tsx
  - src/hooks/useCompositionState.ts
  - src/hooks/useCompositionState.test.tsx
autonomous: true
requirements: [F4.3, F4.4, F4.5, F6.1, F6.2, NFR11]
must_haves:
  truths:
    - "Committed camera, snapshot, legend, background, and dirty baseline have one React owner above responsive branches (D-19, D-29)."
    - "Pan/zoom and legend edits never enter the 50-action color Undo/Redo history (D-18)."
    - "Reset View can change camera only, while complete load applies composition as one intent (D-17, D-19)."
  artifacts:
    - path: src/providers/CompositionStateProvider.tsx
      provides: "Composition reducer/context and semantic commands"
    - path: src/hooks/useCompositionState.ts
      provides: "Guarded context accessor"
  key_links:
    - from: src/hooks/useCompositionState.ts
      to: src/providers/CompositionStateProvider.tsx
      via: "CompositionStateContext"
      pattern: "CompositionStateContext"
---

<objective>
Create the single React owner for durable Phase 2 composition state while leaving color history untouched.

Purpose: Make save/load, responsive remount, export, and future scene layers coordinate through semantic commands rather than competing local state.
Output: Tested provider and guarded hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@src/providers/MapStateProvider.tsx
@src/hooks/useMapState.ts
@src/types/composition.ts
@src/constants/camera.ts
</context>

<interfaces>
Copy the memoized semantic-command shape from `MapStateProvider`, but own `CompositionState` only. `MapStateProvider.loadState(colors)` remains the only color-history reset command; App will coordinate it with `loadComposition` later.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and prove composition ownership and dirty-baseline behavior</name>
  <files>src/providers/CompositionStateProvider.tsx, src/hooks/useCompositionState.ts, src/hooks/useCompositionState.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-PATTERNS.md P2/P3/P9/P14; src/providers/MapStateProvider.tsx; src/hooks/useMapState.ts; src/types/composition.ts; src/constants/camera.ts</read_first>
  <behavior>- Initial state is whole-world Modern with default legend/background.
- Camera, snapshot, and legend edits set dirty state without creating history.
- `loadComposition` atomically replaces every composition field and establishes a clean baseline.
- `markSaved` updates the baseline without changing visible state.
- Hook use outside provider throws the exact guarded-context error.
- Semantic no-ops preserve state identity and the provider never imports or mutates `MapState.history`.</behavior>
  <action>Write the provider/hook behavior tests first in the task, then implement `createInitialCompositionState`, a pure reducer, `CompositionStateContext`, `CompositionStateProvider`, and memoized commands for committed camera, snapshot, legend entry/style/order/position, background, `loadComposition`, and `markSaved`. Canonicalize and compare semantic values before state changes. Do not store live D3 transforms, geometry, focus, selection, dialogs, crossfade intermediates, or undo stacks per D-18, D-19, and D-29. Make `useCompositionState` a thin guard only. The task is complete only with the final green command; do not commit a broken RED-only tree.</action>
  <verify><automated>npm test -- src/hooks/useCompositionState.test.tsx && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>All public commands and guard behavior pass; one no-op returns existing state identity; stable semantic callbacks are exported; no raw dispatch or color-history ownership is exposed.</acceptance_criteria>
  <done>One tested composition owner is ready for camera, legend, snapshot, persistence, and App integration.</done>
  <commit>feat(2-composition): add tested composition state ownership</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| UI/load commands → composition provider | Inputs may originate from drafts or untrusted storage |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-05 | Tampering | provider commands | mitigate | Accept only typed semantic values; storage validation occurs before coordinated load. |
| T-02-06 | Denial of Service | repeated updates | mitigate | Suppress semantic no-ops and keep high-frequency live transforms outside React state. |
</threat_model>

<verification>
Focused tests, full lint, and strict TypeScript pass; source scan confirms no history array or localStorage access in the provider.
</verification>

<success_criteria>
Responsive remounts and later coordinated operations can preserve one complete composition without contaminating color Undo/Redo.
</success_criteria>

<execution_notes>Do not change branches. Finish the TDD task green and commit the tested provider atomically. Preserve all user evidence.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-03-SUMMARY.md` when done.</output>
