---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "19"
type: execute
wave: 6
depends_on: ["02-03", "02-10", "02-17"]
files_modified:
  - src/utils/storage.ts
  - src/utils/storage.test.ts
  - src/hooks/useLocalStorage.ts
autonomous: true
requirements: [F6.1, F6.2, F4.3, F4.4, F4.5, NFR11]
must_haves:
  truths:
    - "Saved maps round-trip colors, semantic camera, period, legend metadata/style/position, and visible settings (D-19)."
    - "Phase 1 color-only records migrate in memory to Modern/whole-world/default legend and are rewritten only on explicit save (D-19)."
    - "Max-10, newest-first, replace-by-name, local-only, quota/unavailable, and partial-valid recovery remain intact (D-19, D-28)."
  artifacts:
    - path: src/utils/storage.ts
      provides: "Single V1/V2 parser/writer and bounded validation"
    - path: src/hooks/useLocalStorage.ts
      provides: "Reactive complete-composition persistence facade"
  key_links:
    - from: src/hooks/useLocalStorage.ts
      to: src/utils/storage.ts
      via: "one injected adapter"
      pattern: "createStorageAdapter"
---

<objective>
Upgrade the single localStorage authority to complete versioned composition snapshots with safe legacy migration.

Purpose: Preserve Phase 1 recovery guarantees while making camera, period, and legend durable.
Output: Tested V2 parser/writer, V1 in-memory migration, and updated hook API.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/storage.md
@src/utils/storage.ts
@src/utils/storage.test.ts
@src/hooks/useLocalStorage.ts
@src/types/composition.ts
@public/data/snapshots/index.json
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify bounded V2 validation and V1 migration</name>
  <files>src/utils/storage.test.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/storage.md; 02-RESEARCH.md Persistence and Migration/Security Domain; 02-PATTERNS.md P7/P14; src/utils/storage.test.ts; src/types/composition.ts</read_first>
  <behavior>- V2 round-trips every durable composition field but no selection/focus/dialog/transient scene.
- V1 `{name,colors,timestamp}` loads as Modern, initial camera, generated default legend, white background, with legacy warning and no write.
- Explicit subsequent save writes V2.
- Unknown versions are rejected while valid neighbors remain.
- camera numbers, snapshot IDs, colors, names, labels, entry count/order, positions, opacity/size, and enums are bounded/repaired with typed warnings.
- reserved keys, oversized/deep data, quota/unavailable, max-10/newest-first/replacement behavior are covered.</behavior>
  <action>Extend the existing FakeStorage suite with malicious nested keys, 30-entry legend bounds, oversized records, unsupported versions, mixed valid/corrupt arrays, no-write-on-list/load assertions, and coordinated V2 values.</action>
  <verify><automated>npm test -- src/utils/storage.test.ts</automated></verify>
  <acceptance_criteria>Tests cover every untrusted localStorage threat and migration outcome before implementation; no test expects silent whole-store discard.</acceptance_criteria>
  <done>The complete persistence contract is executable.</done>
  <commit>test(2-storage): specify composition persistence</commit>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement the single V1/V2 storage adapter and hook</name>
  <files>src/utils/storage.ts, src/hooks/useLocalStorage.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/storage.md; src/utils/storage.test.ts; src/utils/storage.ts; src/hooks/useLocalStorage.ts; src/types/composition.ts; src/utils/legend.ts; src/utils/camera.ts</read_first>
  <action>Upgrade the existing adapter in place; keep key `countriesirl_maps`, one writer, injected Storage/clock, max 10, newest-first, exact trimmed replacement, partial recovery, and onboarding independence. Parse JSON as unknown; reject prototype-pollution keys; bound records/strings/entries; validate canonical colors, camera, known reviewed snapshot IDs, legend enums/numbers/positions, and white background. Migrate V1 only in memory and emit `legacy-migrated`; write V2 only on explicit save/replace. Update hook signatures to complete compositions without moving parsing into React.</action>
  <verify><automated>npm test -- src/utils/storage.test.ts && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>All legacy/current/corrupt/quota/unavailable tests pass; list/load perform no write; geometry/path strings are never persisted; one storage adapter remains.</acceptance_criteria>
  <done>Complete compositions persist locally with safe Phase 1 migration.</done>
  <commit>feat(2-storage): persist complete compositions</commit>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| localStorage JSON → application state | User/extension-modifiable persistent input |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-43 | Elevation of Privilege | stored names/labels | mitigate | Length/type validation and later text-node rendering only. |
| T-02-44 | Tampering | nested dictionaries/styles | mitigate | Reserved-key rejection, enum/range validation, canonical colors. |
| T-02-45 | Denial of Service | oversized/deep JSON | mitigate | Max 10, bounded entries/strings, reject/repair before expensive processing. |
</threat_model>
<verification>Focused storage tests, lint, strict TypeScript, full unit suite, and build pass.</verification>
<success_criteria>Legacy and V2 saves recover safely, and complete composition state round-trips without cloud/backend work.</success_criteria>
<execution_notes>Do not change branches. Preserve one storage writer and commit tests before implementation.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-19-SUMMARY.md` when done.</output>
