---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "19"
type: execute
wave: 4
depends_on: ["02-03", "02-06", "02-10", "02-12"]
files_modified:
  - src/utils/storage.ts
  - src/utils/storage.test.ts
  - src/hooks/useLocalStorage.ts
autonomous: true
requirements: [F6.1, F6.2, F4.3, F4.4, F4.5, NFR11]
must_haves:
  truths:
    - "V2 round-trips colors including approved historical IDs, semantic camera, period, legend, and visible settings (D-19, D-22)."
    - "V1 migrates in memory and writes only on explicit save (D-19)."
    - "Raw serialized length is rejected before JSON.parse, then iterative depth/node budgeting occurs before detailed validation."
    - "The adapter accepts a complete live-camera snapshot assembled by transaction hooks; it never reads stale camera state itself."
  artifacts:
    - path: src/utils/storage.ts
      provides: "Single bounded V1/V2 parser/writer"
    - path: src/hooks/useLocalStorage.ts
      provides: "Complete-composition persistence facade"
  key_links:
    - from: src/hooks/useLocalStorage.ts
      to: src/utils/storage.ts
      via: "one injected adapter"
      pattern: "createStorageAdapter"
---

<objective>
Upgrade the sole localStorage authority with pre-parse resource bounds and full modern/historical composition persistence.

Purpose: Preserve Phase 1 recovery while preventing oversized/deep attacker-edited storage and stale-camera saves.
Output: Tested bounded V2 parser/writer, V1 migration, and hook API.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/storage.md
@src/utils/storage.ts
@src/utils/storage.test.ts
@src/hooks/useLocalStorage.ts
@src/types/composition.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the bounded V1/V2 composition storage authority</name>
  <files>src/utils/storage.ts, src/utils/storage.test.ts, src/hooks/useLocalStorage.ts</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/storage.md; 02-RESEARCH.md Persistence/Security; 02-REVIEWS.md storage/live-camera concerns; src/utils/storage.ts; src/utils/storage.test.ts; src/types/composition.ts; src/utils/legend.ts; src/utils/camera.ts</read_first>
  <behavior>- Raw value exceeding a named serialized-byte/UTF-16-length limit is rejected before JSON.parse.
- Parsed unknown value passes an iterative non-recursive depth/node budget before record validation; deep/wide cycles are impossible in JSON but malicious depth/width is bounded.
- V2 persists all durable fields and approved stable historical color IDs, excluding editor/transient state.
- V1 loads Modern/initial camera/default legend with warning/no write; explicit save writes V2.
- Unknown versions/corrupt neighbors/unsafe keys are handled with partial recovery.
- Max-10/newest-first/replace/quota/unavailable/no-write-on-list/load remain.
- Adapter writes the exact `CompositionSnapshot` supplied by save transaction and performs no camera read.</behavior>
  <action>Write FakeStorage tests first, including raw oversized JSON that must not invoke the injected parser, deeply nested and excessively wide objects rejected by iterative budget, boundary-valid payloads, malicious keys, approved historical IDs, V1/V2, quota/unavailable, mixed records, and no-write reads. Upgrade the existing adapter only; name limits, avoid recursion, canonicalize camera through shared math, validate snapshot IDs/legend enums, and keep one `countriesirl_maps` writer.</action>
  <verify><automated>npm test -- src/utils/storage.test.ts && npm run lint && npm exec tsc -- -p tsconfig.app.json --noEmit</automated></verify>
  <acceptance_criteria>Oversized pre-parse/deep-node tests prove ordering, all migration/recovery/security cases pass, and no storage path reads camera state or filters valid approved historical IDs to the modern 195 only.</acceptance_criteria>
  <done>Complete compositions persist locally within explicit CPU/memory and identity bounds.</done>
  <commit>feat(2-storage): bound and persist compositions</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| localStorage string → parser/application state | User/extension-modifiable input |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-43 | Elevation of Privilege | stored text | mitigate | Type/length validation and text rendering downstream. |
| T-02-44 | Tampering | nested dictionaries | mitigate | Safe keys and enum/range validation. |
| T-02-45 | Denial of Service | huge/deep JSON | mitigate | Raw limit before parse plus iterative depth/node budget. |
</threat_model>
<verification>Focused storage tests, lint, strict TypeScript, full suite, build pass.</verification>
<success_criteria>Legacy and complete saves recover safely without stale-camera or unbounded-input behavior.</success_criteria>
<execution_notes>Do not change branches. Preserve one storage writer.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-19-SUMMARY.md` when done.</output>
