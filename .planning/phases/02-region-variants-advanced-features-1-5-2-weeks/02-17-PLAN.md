---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "17"
type: execute
wave: 5
depends_on: ["02-13", "02-14", "02-15", "02-16"]
files_modified:
  - public/data/snapshots/index.json
  - public/data/snapshots/1492.geojson
  - public/data/snapshots/1700.geojson
  - public/data/snapshots/1815.geojson
  - public/data/snapshots/1914.geojson
autonomous: true
requirements: [F2.1, F2.2, F2.3, F2.4, F2.5, NFR3, NFR8, NFR9]
must_haves:
  truths:
    - "Production exposes Modern plus exactly four fully approved curated snapshots, never unapproved candidates (D-20)."
    - "Each promoted snapshot asserts the exact six coverage IDs `poland`, `lithuania`, `hungary`, `balkans`, `iberia`, and `scandinavia`, with separate evidence/review records and worldwide modern fallback (D-21, F2.3)."
    - "Promotion preserves explicit entity identities and default-white behavior for new/different entities (D-22)."
  artifacts:
    - path: public/data/snapshots/index.json
      provides: "Production reviewed snapshot catalog"
    - path: public/data/snapshots/1492.geojson
      provides: "Approved 1492 overlay"
    - path: public/data/snapshots/1914.geojson
      provides: "Approved 1914 overlay"
  key_links:
    - from: public/data/snapshots/index.json
      to: public/data/snapshots/1492.geojson
      via: "assetPath and SHA-256"
      pattern: "1492.geojson"
---

<objective>
Promote only the four factually approved historical assets into the bundled production catalog.

Purpose: Convert completed evidence checkpoints into a deterministic product selector without weakening provenance or review status.
Output: Five-entry catalog (Modern plus four snapshots) and canonical public overlay assets.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/data.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-13-SUMMARY.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-14-SUMMARY.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-15-SUMMARY.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-16-SUMMARY.md
@scripts/prepareHistoricalSnapshot.mjs
</context>
<tasks>
<task type="auto">
  <name>Task 1: Publish and byte-verify approved overlays while they remain unlisted</name>
  <files>public/data/snapshots/1492.geojson, public/data/snapshots/1700.geojson, public/data/snapshots/1815.geojson, public/data/snapshots/1914.geojson</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; all four snapshot SUMMARY files; all four source manifests/input artifacts/review JSON files; current public/data/snapshots/index.json; scripts/prepareHistoricalSnapshot.mjs</read_first>
  <action>Verify each prerequisite summary records the exact approval signal `approved {snapshot}` and every approved source/input/review hash still matches. If any approval or byte is absent, stop without modifying public assets or catalog. Otherwise copy only the exact approved candidate bytes into `public/data/snapshots/` while leaving `index.json` unchanged so no intermediate catalog entry can reference a missing asset. For each public overlay, run the exact CLI `--check` against the separate `sources/historical/{id}.input.geojson`, verify the asset byte/hash, identity collisions, finite paths, source/license records, worldwide fallback, and the exact six coverage record IDs `poland`, `lithuania`, `hungary`, `balkans`, `iberia`, `scandinavia`; Poland and Lithuania must retain separate evidence/reviewer records even when geometry is shared. Do not modify candidate/review evidence during promotion.</action>
  <verify><automated>for id in 1492 1700 1815 1914; do node scripts/prepareHistoricalSnapshot.mjs --snapshot "$id" --sources "sources/historical/$id.sources.json" --input "sources/historical/$id.input.geojson" --output "public/data/snapshots/$id.geojson" --review-output "data/historical-reviewed/$id.review.json" --review-html "data/historical-reviewed/$id.review.html" --check || exit 1; done && npm test -- src/utils/historicalValidation.test.ts src/utils/scene.test.ts</automated></verify>
  <acceptance_criteria>All four public assets exist and match approved candidate/review hashes while the production catalog still does not list them; every asset has all six exact separately evidenced coverage IDs.</acceptance_criteria>
  <done>Approved historical bytes are present and fully verified without any dangling catalog reference.</done>
  <commit>feat(2-history): publish approved historical overlays unlisted</commit>
</task>
<task type="auto">
  <name>Task 2: Atomically promote the verified assets by updating the catalog last</name>
  <files>public/data/snapshots/index.json</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/data.md; all four snapshot SUMMARY files; all four public assets; all four review JSON/source manifests; current public/data/snapshots/index.json; src/utils/historicalValidation.ts</read_first>
  <action>Only after Task 1 proves every public asset exists and matches its approved bytes, update `index.json` as the final promotion step. Add exact UI labels/as-of metadata, asset paths/hashes, source records, fallback copy, `historian-reviewed` status, and six separate coverage/review records for Modern, 1492, 1700, 1815, and 1914. Assert the historical entries use exactly the six required IDs and separate Poland/Lithuania evidence. Do not promote draft/source-reviewed data and do not modify any asset after the catalog write.</action>
  <verify><automated>npm test -- src/utils/historicalValidation.test.ts src/utils/scene.test.ts && node -e "const fs=require('node:fs'); const crypto=require('node:crypto'); const x=require('./public/data/snapshots/index.json'); const ids=['poland','lithuania','hungary','balkans','iberia','scandinavia']; const reviewed=x.snapshots.filter(s=>s.reviewStatus==='historian-reviewed'); if(reviewed.length!==5) process.exit(1); for(const s of reviewed.filter(s=>s.id!=='modern')){const got=s.coverageRecords.map(r=>r.id).sort(); if(JSON.stringify(got)!==JSON.stringify([...ids].sort())) process.exit(1); const p='public'+s.assetPath; if(!fs.existsSync(p)||crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex')!==s.sha256) process.exit(1);}"</automated></verify>
  <acceptance_criteria>The catalog is written only after every referenced file exists; it contains exactly Modern plus four approved entries, exact asset hashes, and all six required separate coverage IDs with no dangling or unapproved reference.</acceptance_criteria>
  <done>The production selector is atomically promoted from fully present and verified assets.</done>
  <commit>feat(2-history): promote the approved snapshot catalog</commit>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| approval evidence → production catalog | Status or bytes could be falsely promoted |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-38 | Spoofing | review status | mitigate | Exact summary approval signals and unchanged review/source hashes. |
| T-02-39 | Tampering | public assets | mitigate | Byte equality, catalog SHA-256, deterministic per-snapshot checks. |
</threat_model>
<verification>All four deterministic checks and historical/scene tests pass; catalog has exactly five historian-reviewed entries.</verification>
<success_criteria>Four evidence-approved periods are available for all six regions with explicit modern fallback.</success_criteria>
<execution_notes>Do not change branches. Stop on any missing approval; never self-approve or edit review evidence.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-17-SUMMARY.md` when done.</output>
