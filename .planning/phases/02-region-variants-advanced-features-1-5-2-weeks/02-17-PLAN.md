---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "17"
type: execute
wave: 9
depends_on: ["02-35"]
files_modified:
  - public/data/snapshots/index.json
  - public/data/snapshots/1492.geojson
  - public/data/snapshots/1700.geojson
  - public/data/snapshots/1815.geojson
  - public/data/snapshots/1914.geojson
autonomous: true
requirements: [F2.1, F2.2, F2.3, F2.4, F2.5, NFR3, NFR8, NFR9]
must_haves:
  truths:
    - "Production exposes Modern plus four snapshots only when every durable source/factual approval validates current bytes (D-20)."
    - "Every snapshot has six separate region decisions and exact five-hash qualified approval; executor self-approval is rejected."
    - "Blocked or stale snapshots remain unlisted and are never counted delivered."
    - "Catalog update occurs last after all public bytes exist and verify."
  artifacts:
    - path: public/data/snapshots/index.json
      provides: "Atomic approved production catalog"
    - path: public/data/snapshots/1492.geojson
      provides: "Approved 1492 bytes"
  key_links:
    - from: data/historical-reviewed/1492.approval.json
      to: public/data/snapshots/1492.geojson
      via: "approval-aware exact-byte promotion"
      pattern: "sha256"
---

<objective>
Promote only exact source-approved, factually approved, unchanged historical bytes into production.

Purpose: Convert durable evidence into a selector without overstating blocked or stale snapshots.
Output: Four public overlays and catalog updated last.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-35-SUMMARY.md
@scripts/prepareHistoricalSnapshot.mjs
@data/historical-reviewed/1492.approval.json
@data/historical-reviewed/1700.approval.json
@data/historical-reviewed/1815.approval.json
@data/historical-reviewed/1914.approval.json
@.planning/coding-rules/general.md
@.planning/coding-rules/data.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Publish and verify approved overlays while unlisted</name>
  <files>public/data/snapshots/1492.geojson, public/data/snapshots/1700.geojson, public/data/snapshots/1815.geojson, public/data/snapshots/1914.geojson</files>
  <read_first>Plan 35 summary; all four approval/source/input/output/review bundles; current catalog; CLI</read_first>
  <action>For each snapshot, run approval-aware offline check and reject missing/stale/self-approved/failed-region evidence. Copy exact candidate bytes to public paths while leaving catalog unchanged. Verify public hash equals approved output hash, all six region IDs, stable identities/selectability, finite paths, and fallback. Do not modify evidence/candidates.</action>
  <verify><automated>for id in 1492 1700 1815 1914; do node scripts/prepareHistoricalSnapshot.mjs --snapshot "$id" --sources "sources/historical/$id.sources.json" --input "sources/historical/$id.input.geojson" --output "public/data/snapshots/$id.geojson" --review-output "data/historical-reviewed/$id.review.json" --review-html "data/historical-reviewed/$id.review.html" --approval "data/historical-reviewed/$id.approval.json" --check || exit 1; done && npm test -- src/utils/historicalValidation.test.ts src/utils/scene.test.ts</automated></verify>
  <acceptance_criteria>All public bytes match durable approvals while catalog still lists only prior approved entries.</acceptance_criteria>
  <done>Approved bytes are present unlisted with no dangling reference.</done>
  <commit>feat(2-history): publish exact approved overlays</commit>
</task>
<task type="auto">
  <name>Task 2: Atomically update the production catalog last</name>
  <files>public/data/snapshots/index.json</files>
  <read_first>All public assets/approval JSONs; current index; historical validator</read_first>
  <action>Only after Task 1 passes, update index with Modern plus exact four entries, labels/as-of/path/hash/source/fallback/review status and six separate coverage decisions. Catalog must reference approval artifact hash and reject any candidate whose current five hashes differ. No draft/source-reviewed/blocked entry.</action>
  <verify><automated>npm test -- src/utils/historicalValidation.test.ts src/utils/scene.test.ts && node -e "const fs=require('node:fs'),c=require('node:crypto'),x=require('./public/data/snapshots/index.json'); const req=['poland','lithuania','hungary','balkans','iberia','scandinavia']; if(x.snapshots.length!==5)process.exit(1); for(const s of x.snapshots.filter(v=>v.id!=='modern')){if(s.reviewStatus!=='historian-reviewed'||JSON.stringify(s.coverageRecords.map(r=>r.id).sort())!==JSON.stringify([...req].sort()))process.exit(1); const p='public'+s.assetPath; if(c.createHash('sha256').update(fs.readFileSync(p)).digest('hex')!==s.sha256)process.exit(1);}"</automated></verify>
  <acceptance_criteria>Catalog contains exactly five current valid entries and was written after files/evidence verified.</acceptance_criteria>
  <done>Only exact approved historical snapshots are production-visible.</done>
  <commit>feat(2-history): promote the approved catalog</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| durable approvals → production catalog | Status/bytes could be falsely promoted |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-38 | Spoofing | review status | mitigate | Named non-executor approvals and six decisions. |
| T-02-39 | Tampering | public assets | mitigate | Approval-aware five-hash checks and catalog-last publication. |
</threat_model>
<verification>All four approval-aware checks and catalog tests pass.</verification>
<success_criteria>Four snapshots are delivered only if exact evidence is complete; otherwise Plan 17 remains blocked and no delivery is claimed.</success_criteria>
<execution_notes>Do not change branches. Stop on any missing/stale approval.</execution_notes>
<output>Create `02-17-SUMMARY.md` listing exact promoted hashes.</output>
