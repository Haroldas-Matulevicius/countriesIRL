---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "17"
type: execute
wave: 9
depends_on: []
rescoped: true
rescoped_on: 2026-07-25
rescope_reason: "Historical snapshots deferred — see 02-DESCOPE-DECISION.md"
supersedes_depends_on: ["02-35"]
files_modified:
  - public/data/snapshots/index.json
autonomous: true
requirements: [F2.1, F2.2, NFR3, NFR8, NFR9]
partially_satisfies: [F2.3, F2.4, F2.5]
must_haves:
  truths:
    - "Production exposes exactly one catalog entry, Modern, because zero historical snapshots hold a complete durable approval chain (D-20)."
    - "The approval-aware promotion path remains implemented and tested, but promotes nothing this phase."
    - "Blocked snapshots remain unlisted and are never counted delivered."
    - "No unapproved historical asset exists anywhere under public/data."
  artifacts:
    - path: public/data/snapshots/index.json
      provides: "Verified Modern-only production catalog"
  key_links:
    - from: sources/historical/1492.sources.json
      to: public/data/snapshots/index.json
      via: "blocked packets must remain unlisted"
      pattern: "BLOCKED"
---

> **RESCOPED 2026-07-25.** This plan originally promoted four approved historical
> overlays into production. Historical snapshots are deferred — the rights-cleared
> source material for 1492/1700/1815/1914 does not exist, so no approval chain can
> be completed. See [`02-DESCOPE-DECISION.md`](02-DESCOPE-DECISION.md).
>
> The plan now **verifies** the Modern-only catalog and proves no unapproved
> historical asset leaked into production. The promotion machinery is retained
> unchanged and still covered by tests, so the deferred snapshots drop in later
> with no rework. The original promotion tasks are preserved verbatim in
> [§Deferred original tasks](#deferred-original-tasks) below.

<objective>
Verify production exposes exactly the Modern catalog entry and that no unapproved historical byte reached public/data.

Purpose: Make the descope truthful and auditable rather than silently absent.
Output: A verified Modern-only catalog and an explicit non-promotion record.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-DESCOPE-DECISION.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-31-CORRECTION-CHECKPOINT.md
@scripts/prepareHistoricalSnapshot.mjs
@.planning/coding-rules/general.md
@.planning/coding-rules/data.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Verify the Modern-only catalog and prove non-promotion</name>
  <files>public/data/snapshots/index.json</files>
  <read_first>02-DESCOPE-DECISION.md; 02-31-CORRECTION-CHECKPOINT.md; current catalog; historical validator</read_first>
  <action>Confirm the catalog contains exactly one entry with id `modern`, that its `assetPath` resolves, and that its recorded `sha256` equals the actual SHA-256 of the referenced asset. Confirm no file matching `public/data/snapshots/{1492,1700,1815,1914}.geojson` exists. Confirm no `sources/historical/*.source-approval.json` and no `data/historical-reviewed/*` approval or candidate artifact exists. Confirm `--validate-sources` still exits BLOCKED for 1492 and 1700, proving the fail-closed path is live rather than bypassed. Do not add, alter, or remove any historical byte.</action>
  <verify><automated>node -e "const fs=require('node:fs'),c=require('node:crypto'),x=require('./public/data/snapshots/index.json'); if(x.snapshots.length!==1||x.snapshots[0].id!=='modern')process.exit(1); const s=x.snapshots[0],p='public'+s.assetPath; if(c.createHash('sha256').update(fs.readFileSync(p)).digest('hex')!==s.sha256)process.exit(1); for(const id of ['1492','1700','1815','1914']){if(fs.existsSync('public/data/snapshots/'+id+'.geojson'))process.exit(1); if(fs.existsSync('sources/historical/'+id+'.source-approval.json'))process.exit(1);} console.log('modern-only catalog verified');" && npm test -- src/utils/historicalValidation.test.ts src/utils/scene.test.ts</automated></verify>
  <acceptance_criteria>Catalog holds exactly one verified Modern entry; zero historical assets, approvals, or candidates exist; blocked packets still fail closed.</acceptance_criteria>
  <done>Modern-only production state is verified and the non-promotion is recorded with exact evidence.</done>
  <commit>test(2-history): verify modern-only catalog and non-promotion</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| blocked evidence → production catalog | A deferred snapshot could be silently listed or partially promoted |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-38 | Spoofing | review status | mitigate | No non-modern entry may exist; blocked packets stay unlisted. |
| T-02-39 | Tampering | public assets | mitigate | Catalog hash verified against actual asset bytes; historical asset absence asserted. |
</threat_model>
<verification>Catalog verification passes and blocked packets still exit nonzero.</verification>
<success_criteria>Production is truthfully Modern-only, and the deferral is explicit rather than implied by absence.</success_criteria>
<execution_notes>Do not change branches. Do not create any historical asset, approval, or catalog entry.</execution_notes>
<output>Create `02-17-SUMMARY.md` recording the verified Modern hash and the explicit zero-promotion result.</output>

---

## Deferred original tasks

Preserved verbatim for the follow-on data-acquisition phase. These execute unchanged
once `02-33` → `02-13`–`02-16` → `02-34` → `02-35` complete.

<deferred_task type="auto">
  <name>Task 1 (deferred): Publish and verify approved overlays while unlisted</name>
  <files>public/data/snapshots/1492.geojson, public/data/snapshots/1700.geojson, public/data/snapshots/1815.geojson, public/data/snapshots/1914.geojson</files>
  <action>For each snapshot, run source-approval-aware and factual-approval-aware offline checks and reject missing/stale/self-approved/failed-region evidence at either layer. Copy exact candidate bytes to public paths while leaving catalog unchanged. Verify public hash equals approved output hash, all six region IDs, stable identities/selectability, finite paths, and fallback. Do not modify evidence/candidates.</action>
  <acceptance_criteria>All public bytes match durable approvals while catalog still lists only prior approved entries.</acceptance_criteria>
</deferred_task>

<deferred_task type="auto">
  <name>Task 2 (deferred): Atomically update the production catalog last</name>
  <files>public/data/snapshots/index.json</files>
  <action>Only after Task 1 passes, update index with Modern plus exact four entries, labels/as-of/path/hash/source/fallback/review status and six separate coverage decisions. Catalog must reference both source-approval and factual-approval artifact paths/hashes and reject any candidate whose current source-approval chain or five candidate/review hashes differ. No draft/source-reviewed/blocked entry.</action>
  <acceptance_criteria>Catalog contains exactly five current valid entries and was written after files/evidence verified.</acceptance_criteria>
</deferred_task>
