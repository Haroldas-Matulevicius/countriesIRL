---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "18"
type: execute
wave: 6
depends_on: ["02-07", "02-11", "02-12", "02-17"]
files_modified:
  - src/components/CompositionBar.tsx
  - src/components/MapWorkspace.tsx
  - src/components/MapCanvas.tsx
  - src/components/Tooltip.tsx
  - src/components/MapWorkspace.test.tsx
autonomous: true
requirements: [F2.1, F2.2, F2.3, F2.4, F2.5, F5.2, NFR3, NFR8, NFR9, NFR11]
must_haves:
  truths:
    - "The selector shows exactly Modern/1492/1700/1815/1914 approved labels and retains the prior scene on load failure (D-20, D-21)."
    - "Historical overlays crossfade as complete states, preserve matching colors, start new entities white, and use modern fallback elsewhere (D-21–D-23)."
    - "Tooltips and persistent status state whether a boundary is Modern, Historical, or Modern fallback (D-21)."
    - "The legend remains outside the camera group and above the selected scene (D-10, D-24)."
  artifacts:
    - path: src/components/CompositionBar.tsx
      provides: "Period selection, status, retry, and Reset View presentation"
    - path: src/components/MapCanvas.tsx
      provides: "Layered outgoing/incoming complete scenes"
    - path: src/components/Tooltip.tsx
      provides: "Period-aware boundary context"
  key_links:
    - from: src/components/CompositionBar.tsx
      to: src/hooks/useSnapshotData.ts
      via: "typed parent state and callbacks"
      pattern: "snapshot"
---

<objective>
Integrate reviewed historical scenes, persistent fallback communication, crossfade, tooltip context, legend overlay, and camera controls in the square workspace.

Purpose: Deliver the complete historical creator experience without hiding missing coverage or capturing transitional artifacts.
Output: Period-aware composition bar and layered world workspace.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/data.md
@src/components/MapWorkspace.tsx
@src/components/MapCanvas.tsx
@src/components/Tooltip.tsx
@src/hooks/useSnapshotData.ts
@src/utils/scene.ts
@src/components/LegendOverlay.tsx
@src/components/MapNavigation.tsx
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the exact composition bar states</name>
  <files>src/components/CompositionBar.tsx, src/components/MapWorkspace.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md section 9 and copywriting; src/hooks/useSnapshotData.ts; src/types/composition.ts; src/components/Controls.test.tsx</read_first>
  <behavior>- Native Map period select contains exactly five reviewed labels.
- Persistent modern/historical/loading/failure/partial status uses exact copy and aria-describedby.
- Failed selection restores prior option and offers Try Period Again.
- Only period-dependent settled actions disable while the current map/camera remain usable.
- Preview label is exactly `1080 × 1080 composition preview` and Reset View is separate.</behavior>
  <action>Write SSR/helper tests, then implement a typed presentational CompositionBar. It must not fetch assets or own camera/snapshot state; it renders exact UI-SPEC copy and callbacks only.</action>
  <verify><automated>npm test -- src/components/MapWorkspace.test.tsx</automated></verify>
  <acceptance_criteria>Exact period options/status/error/retry semantics pass; no unreviewed/disabled teaser option or region selector appears.</acceptance_criteria>
  <done>Historical selection and fallback are clear and accessible.</done>
  <commit>feat(2-history): add the composition period bar</commit>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Compose complete historical scenes with accessible crossfade</name>
  <files>src/components/MapCanvas.tsx, src/components/MapWorkspace.tsx, src/components/MapWorkspace.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; .planning/coding-rules/data.md; 02-UI-SPEC.md sections 9, 14, and 17; src/components/MapCanvas.tsx; src/components/MapWorkspace.tsx; src/utils/scene.ts; src/components/LegendOverlay.tsx; src/components/MapNavigation.tsx</read_first>
  <action>Keep the modern world mounted as fallback, render curated historical coverage above it, and maintain outgoing/incoming complete scene groups for a 160ms opacity crossfade. Use zero duration for reduced motion. At completion remove outgoing; on failure retain previous complete scene. Preserve one logical accessible entity set, stable `d` per scene, camera transform-only movement, LegendOverlay outside camera, and editor-only navigation. Provide an imperative `finalizeSelectedScene` for export that removes outgoing and sets selected opacity 1 synchronously.</action>
  <verify><automated>npm test -- src/components/MapWorkspace.test.tsx src/utils/scene.test.ts src/components/MapCanvas.test.tsx && npm run lint</automated></verify>
  <acceptance_criteria>Tests prove fallback layering, complete-state crossfade, reduced-motion swap, previous-scene retention, finalization, and legend/camera layer order.</acceptance_criteria>
  <done>Reviewed historical periods render truthfully and remain export-ready.</done>
  <commit>feat(2-history): render layered historical scenes</commit>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Add period-aware tooltip and warm-switch gate</name>
  <files>src/components/Tooltip.tsx, src/components/MapWorkspace.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md Tooltip Contract; src/components/Tooltip.tsx; src/components/MapCanvas.tsx; src/hooks/useSnapshotData.ts</read_first>
  <action>Extend tooltip data and exact content with name, uppercase current color, and one Modern/Historical/Modern fallback boundary line. Reposition keyboard anchors during camera/wrap movement using the stable logical node and existing viewport clamp. Add a deterministic warm-cache switch measurement/test path; NFR3 is blocking for warm switch under the browser gate but cold source acquisition is not misreported as switch time.</action>
  <verify><automated>npm test -- src/components/MapWorkspace.test.tsx src/utils/historicalValidation.test.ts && npm run build</automated></verify>
  <acceptance_criteria>Tooltip never exposes raw IDs/provenance, remains 8px inside viewport, follows focused wrapped geometry, and all reviewed warm assets are cacheable for the later <500ms browser assertion.</acceptance_criteria>
  <done>Creators always understand period and fallback context for the visible entity.</done>
  <commit>feat(2-history): add period-aware map feedback</commit>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| snapshot data → SVG/tooltip/status | Stored/bundled labels and geometry reach UI |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-40 | Elevation of Privilege | tooltip/status text | mitigate | React/D3 text APIs only; approved copy; no raw source text. |
| T-02-41 | Denial of Service | scene switching | mitigate | Cached assets, complete-state groups, bounded transition, no geometry morph. |
| T-02-42 | Tampering | export during crossfade | mitigate | Synchronous finalizeSelectedScene removes outgoing state. |
</threat_model>
<verification>Focused component/scene/history tests, lint, strict TypeScript, build, and full unit suite pass.</verification>
<success_criteria>Users can select five reviewed periods, see truthful fallback/tooltip context, and switch complete scenes accessibly.</success_criteria>
<execution_notes>Do not change branches. Preserve exact UI copy and D3/React ownership. Commit each task separately.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-18-SUMMARY.md` when done.</output>
