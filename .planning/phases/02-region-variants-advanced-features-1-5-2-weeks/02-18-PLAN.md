---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "18"
type: execute
wave: 10
depends_on: ["02-07", "02-11", "02-12", "02-17"]
rescoped: true
rescoped_on: 2026-07-25
rescope_reason: "Period selector ships Modern-only — see 02-DESCOPE-DECISION.md"
files_modified:
  - src/components/CompositionBar.tsx
  - src/components/FatalErrorState.tsx
  - src/components/MapWorkspace.tsx
  - src/components/MapCanvas.tsx
  - src/components/Tooltip.tsx
  - src/components/MapWorkspace.test.tsx
  - tests/e2e/history.spec.ts
  - tests/e2e/fixtures/history.html
autonomous: true
requirements: [F1.2, F1.3, F1.5, F2.1, F2.2, F2.3, F2.4, F2.5, F5.2, NFR3, NFR8, NFR9, NFR11]
must_haves:
  truths:
    - "Only exact approved labels appear, driven by the live catalog. Under the 2026-07-25 descope the catalog holds exactly `Modern`, so exactly one period option renders; 1492/1700/1815/1914 are deferred and MUST NOT appear (D-20, D-21)."
    - "The selector is catalog-driven, not hardcoded to one entry: adding an approved snapshot to the catalog must surface it with no component change. Prove this with a multi-entry catalog fixture in the unit test."
    - "Failure retains the prior scene (D-20, D-21)."
    - "Outgoing crossfade scene is inaccessible/nonfocusable; incoming selected scene owns all interaction and export finalization (D-23)."
    - "Approved historical entities are click/keyboard selectable/colorable while active, selection reconciles to incoming identities, and history/legend continuity is truthful (D-22)."
    - "MapWorkspace threads the sole MapCanvasHandle and a typed legend slot into MapCanvas; LegendOverlay is rendered as `<g>` inside the one canonical SVG after the D3 camera group and outside its transform, with no sibling fallback (D-10)."
    - "NFR3 measures multiple warm cached switches from committed selection activation to incoming ready+painted+outgoing inaccessible/removal, excluding cold fetch."
  artifacts:
    - path: src/components/FatalErrorState.tsx
      provides: "Exact world fatal recovery copy"
    - path: src/components/MapCanvas.tsx
      provides: "Accessible complete-state crossfade, sole canvas handle, and canonical in-SVG legend slot"
    - path: tests/e2e/history.spec.ts
      provides: "Focused Chrome history/NFR3 slice"
  key_links:
    - from: src/components/MapWorkspace.tsx
      to: src/components/MapCanvas.tsx
      via: "typed `mapCanvasRef` plus `legendSlot` passed into the one canonical SVG"
      pattern: "MapCanvasHandle|legendSlot"
    - from: src/components/MapCanvas.tsx
      to: src/utils/scene.ts
      via: "effective selectable IDs and selection reconciliation"
      pattern: "reconcileSelection"
---

> **RESCOPED 2026-07-25.** Historical snapshots are deferred (see
> [`02-DESCOPE-DECISION.md`](02-DESCOPE-DECISION.md)), so the period selector renders the
> catalog's single `Modern` entry. Everything else in this plan is unchanged and still
> ships: the composition bar, world loading/fatal/recovery states, the accessible
> crossfade, effective-scene interaction and selection reconciliation, the canonical
> in-SVG legend slot, and the tooltip.
>
> The selector must stay **catalog-driven**. Historical scene handling is retained and
> proven by the existing in-memory Playwright fixture in `phase2-composition.spec.ts`, so
> deferred snapshots activate later with no component change. NFR3 warm-switch timing is
> measured against fixture-driven switches rather than production historical assets.

<objective>
Integrate exact world loading/fatal states, approved historical scenes, effective-scene interaction, accessible crossfade, tooltip context, and warm-switch evidence.

Purpose: Deliver the truthful historical creator experience without duplicate accessibility, stale selection, or vague timing claims.
Output: Composition bar, world recovery states, layered scene workspace, tooltip, and focused Chrome evidence.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/data.md
@src/components/MapWorkspace.tsx
@src/components/FatalErrorState.tsx
@src/components/MapCanvas.tsx
@src/hooks/useSnapshotData.ts
@src/utils/scene.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build exact composition bar and world loading/fatal/recovery states</name>
  <files>src/components/CompositionBar.tsx, src/components/FatalErrorState.tsx, src/components/MapWorkspace.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md sections 9, 17, 20, 22; src/components/FatalErrorState.tsx; src/components/MapWorkspace.tsx; src/hooks/useSnapshotData.ts</read_first>
  <behavior>- Loading copy is exactly `Loading world map…`.
- Fatal heading/body/action are exact world copy and `Reload Map`.
- Map label is `Interactive world map, {period label}`.
- Native period select contains exactly approved labels/status/retry.
- CompositionBar is the sole Reset View owner.</behavior>
  <action>Update FatalErrorState from stale Europe copy, implement exact loading/fatal/recovery/map-label assertions, and build the typed CompositionBar without data fetching or camera ownership. Assert exactly one visible Reset View in the composed workspace.</action>
  <verify><automated>npm test -- src/components/MapWorkspace.test.tsx</automated></verify>
  <acceptance_criteria>All exact copy/label/options/status/Reset View assertions pass.</acceptance_criteria>
  <done>World loading and failure states are owned and testable.</done>
  <commit>feat(2-history): add world period and recovery states</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose accessible historical scenes and reconcile interaction</name>
  <files>src/components/MapCanvas.tsx, src/components/MapWorkspace.tsx, src/components/MapWorkspace.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; .planning/coding-rules/data.md; 02-UI-SPEC.md sections 9, 10, 14; 02-REVIEWS.md crossfade/historical-entity concerns; src/utils/scene.ts; src/components/MapCanvas.tsx</read_first>
  <behavior>- MapWorkspace accepts and forwards exactly one `Ref<MapCanvasHandle>`; it never constructs/imports a camera controller.
- MapWorkspace accepts a typed React legend slot and passes it to MapCanvas; MapCanvas renders exactly one canonical `<svg>` containing the D3 camera group first and the React legend `<g>` second, outside the camera transform.
- There is no sibling/absolutely-positioned legend SVG or HTML fallback; camera transforms never move/scale the legend group.
- Incoming scene is the sole accessible/focusable scene during crossfade; outgoing group is aria-hidden, inert/nonfocusable, pointer-disabled.
- Approved distinct historical IDs can be clicked/keyboard-selected/colored/history-managed while active.
- Period switch drops selected IDs absent from incoming effective scene and keeps continuing IDs.
- New IDs start white; dependencies/neutral remain non-selectable.
- finalizeSelectedScene removes outgoing and sets selected opacity 1 synchronously.</behavior>
  <action>Render complete historical groups inside the D3-owned camera layer with 160ms/zero reduced-motion behavior, preserve modern fallback, enforce singular accessibility, reconcile provider selection at successful scene commit, and expose synchronous finalization through the existing MapCanvas boundary. Add typed `mapCanvasRef` and `legendSlot` boundaries to MapWorkspace/MapCanvas. Keep MapCanvas as the only camera-controller owner; render the React-owned LegendOverlay group after the camera group inside the same canonical SVG and outside its transform. Never render a second/sibling SVG, allow outgoing interaction, or apply heuristic color transfer.</action>
  <verify><automated>npm test -- src/components/MapWorkspace.test.tsx src/utils/scene.test.ts src/components/MapCanvas.test.tsx && npm run lint</automated></verify>
  <acceptance_criteria>Tests prove one SVG, exact camera-then-legend layer order, legend camera independence, no sibling-overlay fallback, one forwarded handle, crossfade accessibility, interaction policy, selection reconciliation, fallback, and finalization.</acceptance_criteria>
  <done>Historical switching is complete-state, singular, and colorable where approved.</done>
  <commit>feat(2-history): integrate interactive historical scenes</commit>
</task>

<task type="auto">
  <name>Task 3: Add period tooltip and multi-sample warm-switch Chrome gate</name>
  <files>src/components/Tooltip.tsx, src/components/MapWorkspace.test.tsx, tests/e2e/history.spec.ts, tests/e2e/fixtures/history.html</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-UI-SPEC.md Tooltip Contract; 02-REVIEWS.md NFR3/browser allocation; src/components/Tooltip.tsx; src/hooks/useSnapshotData.ts</read_first>
  <action>Implement exact Modern/Historical/Modern fallback tooltip lines and transformed anchor updates. Create a Vite-served history fixture and Chrome tests for approved option filtering, prior-scene failure retention, singular crossfade accessibility, new/continuing historical selection/color/history/legend behavior, and warm NFR3. Define each sample boundary as committed select `change`/activation timestamp to the first painted frame where incoming is ready/full opacity and outgoing is inaccessible/removed. Prewarm each asset, discard the first diagnostic sample, record at least five subsequent samples per approved historical snapshot, require every measured warm sample below 500ms, and preserve raw sample evidence; no cold fetch or arbitrary sleep may count.</action>
  <verify><automated>npm test -- src/components/MapWorkspace.test.tsx src/utils/historicalValidation.test.ts && npm run build && npm run test:e2e -- --project=chrome --grep "snapshot switch|historical entity|world loading"</automated></verify>
  <acceptance_criteria>Exact tooltip/copy and all Chrome history samples/interactions pass; raw sample boundaries/counts are recorded and no single observation substitutes for the gate.</acceptance_criteria>
  <done>Historical feedback, interaction, and NFR3 have focused browser evidence.</done>
  <commit>test(2-history): verify warm historical switching</commit>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| snapshot data → SVG/tooltip/status | Bundled labels/geometry reach UI |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-40 | Elevation of Privilege | tooltip/status | mitigate | Text APIs and exact safe copy. |
| T-02-41 | Denial of Service | switching | mitigate | Cached bounded complete states and multi-sample gate. |
| T-02-42 | Spoofing | outgoing scene | mitigate | Outgoing is inaccessible/nonfocusable and export-finalized away. |
</threat_model>
<verification>Focused Node tests, Chrome history/NFR3 slice, lint, TypeScript, build pass.</verification>
<success_criteria>Users see exact world recovery and truthful historical interaction/fallback with accessible switching under the required warm bound.</success_criteria>
<execution_notes>Do not change branches. Preserve exact UI copy and D3/React ownership.</execution_notes>
<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-18-SUMMARY.md` when done.</output>
