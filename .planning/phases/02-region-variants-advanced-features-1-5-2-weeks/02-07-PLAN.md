---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "07"
type: execute
wave: 4
depends_on: ["02-05", "02-06"]
files_modified:
  - src/hooks/useCameraController.ts
  - src/components/MapCanvas.tsx
  - src/components/MapCanvas.test.tsx
autonomous: true
requirements: [F1.1, F1.2, F3.2, F3.4, F3.5, F7.1, F7.2, F7.3, NFR1, NFR2, NFR11]
must_haves:
  truths:
    - "Drag, wheel/trackpad, and pinch update one transform-only world camera with pointer/midpoint anchoring (D-03)."
    - "Three visual world copies provide continuous horizontal wrap while only one stable logical country path is accessible (D-04, D-06)."
    - "The controller can synchronously freeze the last painted frame for export and ordinary movement never touches color history (D-09, D-18)."
  artifacts:
    - path: src/hooks/useCameraController.ts
      provides: "D3 zoom lifecycle, live transform ref, Reset/Locate/restore/freeze controller"
    - path: src/components/MapCanvas.tsx
      provides: "Wrapped D3 scene with one accessible logical copy per entity"
  key_links:
    - from: src/hooks/useCameraController.ts
      to: src/utils/camera.ts
      via: "all gesture and programmatic constraints"
      pattern: "constrain|cameraToTransform|transformToCamera"
---

<objective>
Wire the pure camera model into a stable, accessible, horizontally wrapped D3 world canvas.

Purpose: Deliver the direct-manipulation foundation without reprojecting geometry or duplicating accessible countries.
Output: Narrow camera controller and transformed wrapped MapCanvas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/data.md
@src/components/MapCanvas.tsx
@src/utils/camera.ts
@src/utils/mapProjection.ts
@src/providers/MapStateProvider.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the imperative camera controller boundary</name>
  <files>src/hooks/useCameraController.ts, src/components/MapCanvas.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-PATTERNS.md P3/P5; 02-RESEARCH.md Pattern 3 and Pitfalls 3–5; src/utils/camera.ts; src/types/composition.ts</read_first>
  <behavior>- D3 zoom uses scale limits, clickDistance 4, custom constrain, and filters legend/editor controls.
- Gesture frames update only the camera group transform and live ref; settled end commits semantic camera.
- reset/locate/restore pass through the same constraint.
- freezeAndSnapshot locks input, interrupts transitions, cancels RAF, reads the current transform synchronously, and returns semantic state.
- cleanup removes namespaced handlers and cancels all work.</behavior>
  <action>Write focused controller contract tests with injected selection/RAF/transition boundaries, then implement `useCameraController` with a narrow controller ref. Use D3 zoom rather than custom pointer caches. Direct input has no authored easing; programmatic movement respects reduced motion.</action>
  <verify><automated>npm test -- src/components/MapCanvas.test.tsx src/utils/camera.test.ts</automated></verify>
  <acceptance_criteria>Tests prove freeze ordering and cleanup; controller has no color reducer, localStorage, export capture, or geometry-generation responsibility.</acceptance_criteria>
  <done>High-frequency camera state is isolated behind a deterministic imperative API.</done>
  <commit>feat(2-camera): add the camera controller</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render wrapped worlds with one accessible logical country</name>
  <files>src/components/MapCanvas.tsx, src/components/MapCanvas.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; .planning/coding-rules/data.md; 02-UI-SPEC.md wrapped-country accessibility section; 02-PATTERNS.md P5; src/components/MapCanvas.tsx; src/hooks/useCameraController.ts; src/hooks/useGeoData.ts</read_first>
  <action>Refactor the D3 subtree into a camera group with base offsets `-1080,0,+1080`, stable path `d` generated once, one logical path per selectable entity carrying role/title/aria/tabindex, and two decorative repeats with `aria-hidden`, `focusable=false`, no role/title/ID. Reconcile the logical path to the nearest visible wrap offset on each camera frame by transform only. Map pointer activation from every visual copy to one entity ID, suppress country/background clicks after a completed pan, keep roving alphabetical focus on the same logical DOM node, and expose narrow `focusCountry`/camera adapters. Preserve Phase 1 selection/color no-op semantics and D3-only path ownership.</action>
  <verify><automated>npm test -- src/components/MapCanvas.test.tsx src/hooks/useMapState.test.ts src/utils/mapProjection.test.ts && npm run lint</automated></verify>
  <acceptance_criteria>DOM assertions show exactly 195 logical accessible options, decorative repeats have no accessible identity, path `d` does not change during camera moves, drag release does not select, and focus node identity survives horizontal wrap.</acceptance_criteria>
  <done>The unified world canvas is directly navigable, wrapped, stable, and accessible.</done>
  <commit>feat(2-map): render the accessible wrapped world</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| pointer/touch/wheel events → camera | High-frequency untrusted input |
| validated scene labels → SVG attributes/text | Data reaches DOM |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-14 | Denial of Service | camera events | mitigate | D3 zoom, transform-only frames, bounded scale, cancellable RAF/transitions. |
| T-02-15 | Elevation of Privilege | SVG labels | mitigate | D3 `.text`/attributes only; never `.html` or innerHTML. |
| T-02-16 | Spoofing | wrapped accessibility copies | mitigate | Exactly one logical role/name/focus node per entity; repeats are aria-hidden. |
</threat_model>

<verification>
Focused camera/map tests, full unit suite, lint, strict TypeScript, and build pass; browser behavior remains for later E2E.
</verification>

<success_criteria>
Users can manipulate one full-world canvas continuously while assistive technology sees one stable list of logical countries.
</success_criteria>

<execution_notes>Do not change branches. Keep MapCanvas below 300 lines by extracting controller/math rather than embedding them. Commit each task atomically.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-07-SUMMARY.md` when done.</output>
