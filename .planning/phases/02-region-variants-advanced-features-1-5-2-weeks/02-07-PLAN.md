---
phase: 02-region-variants-advanced-features-1-5-2-weeks
plan: "07"
type: execute
wave: 4
depends_on: ["02-01", "02-05", "02-06"]
files_modified:
  - src/hooks/useCameraController.ts
  - src/components/MapCanvas.tsx
  - src/components/MapCanvas.test.tsx
  - tests/e2e/phase2-composition.spec.ts
  - tests/e2e/fixtures/camera.html
autonomous: true
requirements: [F1.1, F1.2, F3.2, F3.4, F3.5, F7.1, F7.2, F7.3, NFR1, NFR2, NFR11]
must_haves:
  truths:
    - "Drag, wheel/trackpad, and pinch update one transform-only camera with pointer/midpoint anchoring (D-03)."
    - "One stable logical path represents each selectable active-scene entity; wrapped repeats remain visual-only (D-04, D-06, D-12–D-15)."
    - "The existing forwarded MapCanvas boundary exposes exactly one `MapCanvasHandle`; every operation targets the visible SVG/controller and no second controller is constructed (D-06, D-09, D-16–D-19)."
    - "The obsolete runtime 57-path assertion is replaced at cutover by 195 logical selectable core states and 248 visible modern units while Phase 1 evidence remains in git history/Plan 01 summary."
  artifacts:
    - path: src/hooks/useCameraController.ts
      provides: "MapCanvas-internal D3 zoom lifecycle consumed only by the single MapCanvasHandle"
    - path: src/components/MapCanvas.tsx
      provides: "Wrapped effective-scene rendering plus the sole forwarded MapCanvasHandle and connected export source"
    - path: tests/e2e/phase2-composition.spec.ts
      provides: "World baseline and focused Chrome camera slice"
  key_links:
    - from: src/components/MapCanvas.tsx
      to: src/types/composition.ts
      via: "the sole forwarded MapCanvasHandle backed by one internal controller"
      pattern: "MapCanvasHandle|useImperativeHandle"
---

<objective>
Wire the pure camera model into a stable wrapped world canvas and complete the live-camera lease boundary.

Purpose: Deliver direct manipulation, one-logical-copy accessibility, and exact current-frame transaction semantics before save/export orchestration.
Output: Tested controller, world canvas, world baseline, and focused Chrome camera evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-CONTEXT.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-RESEARCH.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-PATTERNS.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-UI-SPEC.md
@.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-REVIEWS.md
@CLAUDE.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/data.md
@src/components/MapCanvas.tsx
@src/utils/camera.ts
@src/utils/mapProjection.ts
@src/types/composition.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the live camera controller and idempotent freeze lease</name>
  <files>src/hooks/useCameraController.ts, src/components/MapCanvas.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; 02-REVIEWS.md camera lifecycle concerns; 02-RESEARCH.md Pattern 3/Pitfalls 3–5; src/utils/camera.ts; src/types/composition.ts</read_first>
  <behavior>- Exactly one controller instance is created for each mounted visible MapCanvas and it remains internal to MapCanvas.
- Gesture frames update only the camera transform/live ref; settled end commits semantic camera.
- `readCurrentCamera()` returns the current visible semantic camera without locking or interrupting.
- `freezeAndSnapshot()` locks input, interrupts transitions/momentum, cancels RAF, reads the last painted transform, synchronously commits/returns that semantic camera, and returns `CameraFreezeLease`.
- `release()` re-enables input and is idempotent; success, thrown callback, unmount, and repeated release cannot leak handlers or leave the camera locked.
- reset/locate/restore use the same constraints and cleanup removes all namespaced handlers/work.
- The one forwarded MapCanvasHandle delegates read/freeze/reset/locate/restore to that same controller; no method constructs, clones, or imports another controller.</behavior>
  <action>Write focused controller tests with an injected controller factory/selection/RAF/transition/commit boundary, then implement D3 zoom as a MapCanvas-internal hook. Track lease ownership so only the outermost active freeze controls input and release is exact-once in effect even if callers invoke it repeatedly. In MapCanvas, use the existing forwarded imperative boundary and `useImperativeHandle` to expose the exact shared `MapCanvasHandle`; delegate every camera method to the one internal controller instance and return the existing connected export-source `HTMLDivElement` from `getExportSource()`. Do not export the hook as a root composition controller, add a second provider/controller, or replace the Phase 1 HTMLElement export-source contract.</action>
  <verify><automated>npm test -- src/components/MapCanvas.test.tsx src/utils/camera.test.ts</automated></verify>
  <acceptance_criteria>Tests cover animated Locate, wheel movement, release after success/failure, repeated release, cleanup/unmount, one controller-factory call per mounted visible canvas, handle delegation against the visible SVG transform, connected export-source access, and renewed camera input after every case.</acceptance_criteria>
  <done>The live camera has a complete read/freeze/settle/release lifecycle.</done>
  <commit>feat(2-camera): add the live camera freeze lease</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render wrapped effective scenes with one logical selectable entity</name>
  <files>src/components/MapCanvas.tsx, src/components/MapCanvas.test.tsx</files>
  <read_first>.planning/coding-rules/general.md; .planning/coding-rules/frontend.md; .planning/coding-rules/data.md; 02-UI-SPEC.md wrapped-country accessibility and selection sections; 02-REVIEWS.md historical-entity policy; src/components/MapCanvas.tsx; src/hooks/useCameraController.ts; src/types/composition.ts</read_first>
  <behavior>- `MapCanvas` is `forwardRef<MapCanvasHandle, MapCanvasProps>` and retains one connected export-source div containing exactly one canonical SVG.
- Modern scene exposes exactly 195 logical selectable core options and 248 visible units.
- Approved historical `isSelectable` entities with distinct stable IDs receive click/keyboard/selection semantics while active.
- Dependency, disputed, indeterminate, and neutral features remain non-selectable and unfocusable.
- Two decorative repeats per visible geometry have no role/title/ID/tab stop but map pointer activation back to the logical entity when selectable.
- Path `d` stays stable during camera movement; completed pan never selects; focused logical node survives wrapping.</behavior>
  <action>Refactor the D3 subtree into one D3-owned camera group with offsets `-1080,0,+1080`, stable geometry, one logical path for each active-scene selectable entity, and decorative visual repeats. Keep the React-owned SVG shell available for Plan 02-18's typed legend slot after this camera group. Drive validity from effective-scene `isSelectable`, not the modern lookup, so approved historical entities can be colored by map click/keyboard while active. Preserve the modern 195-state browser/Locate catalog elsewhere. Reconcile focus/wrap by transforms only and preserve Phase 1 color no-op/history semantics.</action>
  <verify><automated>npm test -- src/components/MapCanvas.test.tsx src/hooks/useMapState.test.ts src/utils/mapProjection.test.ts && npm run lint</automated></verify>
  <acceptance_criteria>Modern counts, historical selectable/non-selectable examples, stable geometry, drag suppression, and logical focus identity all pass.</acceptance_criteria>
  <done>The canvas supports modern and approved historical interaction without duplicate accessibility instances.</done>
  <commit>feat(2-map): render the accessible wrapped world</commit>
</task>

<task type="auto">
  <name>Task 3: Replace the Phase 1 runtime smoke with the world camera baseline</name>
  <files>tests/e2e/phase2-composition.spec.ts, tests/e2e/fixtures/camera.html</files>
  <read_first>.planning/coding-rules/general.md; 02-VALIDATION.md; 02-REVIEWS.md obsolete 57-path and browser-allocation concerns; tests/e2e/phase2-composition.spec.ts; playwright.config.ts; src/components/MapCanvas.tsx</read_first>
  <action>Delete the runtime 57-path assertion introduced by Plan 02-01 and replace it with a world baseline proving 195 unique logical selectable core states and 248 visible modern units; preserve the Phase 1 result only in git history and summaries. Add a Vite-served camera fixture and focused installed-Chrome Playwright cases for drag-not-select, pointer-anchored wheel, wrap continuity, pole clamp, one logical accessibility copy, one controller-backed handle operating on the visible SVG transform, freeze/release followed by renewed input, connected export-source access, and no geometry regeneration. Run Chrome now; Edge remains part of Plan 02-27 aggregate.</action>
  <verify><automated>npm run test:e2e -- --project=chrome --grep "world baseline|camera input|camera freeze"</automated></verify>
  <acceptance_criteria>No active E2E assertion expects 57 paths; world counts and camera lifecycle pass in Chrome with no skipped cases or artifact leakage.</acceptance_criteria>
  <done>The browser suite is green after world cutover and proves the owning camera slice early.</done>
  <commit>test(2-camera): replace baseline with world camera coverage</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| pointer/touch/wheel → camera | High-frequency untrusted input |
| effective scene labels/IDs → SVG | Curated and bundled data reaches DOM |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-14 | Denial of Service | camera events | mitigate | D3 zoom, bounded transforms, cancellable work, idempotent lease release. |
| T-02-15 | Elevation of Privilege | SVG labels | mitigate | Text/attributes only, never HTML. |
| T-02-16 | Spoofing | wrapped/historical accessibility | mitigate | One logical node per selectable effective-scene identity and explicit non-selectability. |
</threat_model>

<verification>
Focused unit tests and Chrome camera/world baseline pass; lint, strict TypeScript, build, and full unit suite remain green.
</verification>

<success_criteria>
Users can manipulate one wrapped world continuously, approved historical entities can participate while active, and save/export callers can safely read or lease the exact visible camera.
</success_criteria>

<execution_notes>Do not change branches. Keep MapCanvas bounded by extracting controller/math. Commit each task atomically.</execution_notes>

<output>Create `.planning/phases/02-region-variants-advanced-features-1-5-2-weeks/02-07-SUMMARY.md` when done.</output>
