---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "04"
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/providers/MapStateProvider.tsx
  - src/hooks/useMapState.ts
  - src/hooks/useMapState.test.ts
autonomous: true
requirements: [F1.2, F1.4, F1.5, F1.6, NFR2]
must_haves:
  truths:
    - "Single and bulk color changes each create exactly one immutable history action."
    - "Undo/redo remains correct after more than 50 edits and after branching from an undone state."
    - "Selection changes never create history, reset is undoable, and loading establishes a fresh baseline."
  artifacts:
    - path: "src/providers/MapStateProvider.tsx"
      provides: "React Context plus pure reducer ownership"
      exports: ["MapStateProvider"]
    - path: "src/hooks/useMapState.ts"
      provides: "Typed context consumer and semantic map actions"
      exports: ["useMapState"]
    - path: "src/hooks/useMapState.test.ts"
      provides: "Reducer/history contract tests"
  key_links:
    - from: "src/hooks/useMapState.ts"
      to: "src/providers/MapStateProvider.tsx"
      via: "context consumer"
      pattern: "useContext"
    - from: "src/providers/MapStateProvider.tsx"
      to: "HISTORY_LIMIT"
      via: "bounded commitColors path"
      pattern: "HISTORY_LIMIT"
---

<objective>
Implement the centralized React Context plus useReducer map-state engine with fully tested 50-action undo/redo semantics.

Purpose: Make every color, selection, reset, and load transition deterministic before UI components dispatch against it.
Output: Provider, `useMapState` hook, pure reducer helpers, and exhaustive state tests.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/types/map.ts
@src/constants/config.ts
@src/constants/colors.ts
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md

<interfaces>
The provider owns one `MapState` and exports semantic operations for replacing/toggling selected IDs, applying one color to one or many IDs, reset, undo, redo, and load. Derived booleans are `canUndo`, `canRedo`, and `canReset`. The hook throws a clear developer error outside `MapStateProvider`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify and implement the bounded pure reducer</name>
  <files>src/providers/MapStateProvider.tsx, src/hooks/useMapState.test.ts</files>
  <read_first>
    src/providers/MapStateProvider.tsx
    src/hooks/useMapState.test.ts
    src/types/map.ts
    src/constants/config.ts
    src/constants/colors.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
  </read_first>
  <behavior>
    - SET_COLOR and SET_COLORS apply ID-keyed colors as one snapshot per action.
    - No-op color and already-white reset return the same state object and do not append history.
    - UNDO/REDO traverse snapshots; a new edit after undo discards the redo branch.
    - At least 51 color changes retain only the last 50 actions plus the current baseline semantics defined by the approved pattern.
    - Selection replace/toggle/clear does not alter history.
    - LOAD_STATE replaces colors and resets history/index to one loaded baseline.
  </behavior>
  <action>Write action-sequence tests first, then implement a pure reducer and centralized `commitColors` helper in the provider module per D-05, D-25, D-27 through D-33, D-55, and D-57. Clone incoming records so external callers cannot mutate snapshots. Retain at most 50 color-changing actions, suppress duplicate snapshots, truncate redo on branch edits, and keep one shared selected-ID collection for map and list workflows. Reset colors to the default white/uncolored representation as one undoable action only when colors differ. Export only the reducer/test helpers needed by the test file; keep provider state immutable under React Strict Mode.</action>
  <verify>
    <automated>npm run test:run -- src/hooks/useMapState.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests explicitly perform 50+ edits and prove the bounded history invariant.
    - Tests prove no-op identity, branch truncation, reset undo, selection exclusion, and load reset.
    - No reducer path mutates `state`, `colors`, `history`, or action payloads.
    - The focused test command exits 0.
  </acceptance_criteria>
  <done>The pure reducer satisfies every locked color/history/selection transition, including the 50+ action edge case.</done>
</task>

<task type="auto">
  <name>Task 2: Expose provider state and semantic actions through useMapState</name>
  <files>src/providers/MapStateProvider.tsx, src/hooks/useMapState.ts</files>
  <read_first>
    src/providers/MapStateProvider.tsx
    src/hooks/useMapState.ts
    src/types/map.ts
    .planning/coding-rules/frontend.md
  </read_first>
  <action>Create the typed context value and `MapStateProvider`, memoizing semantic action callbacks and derived `canUndo`, `canRedo`, and `canReset` values. Implement `useMapState` as the only consumer entry point; it must throw a specific developer-facing error when called outside the provider. Ensure map-click replacement, list checkbox toggling, select-all, clear-selection, preset/custom bulk color, history, reset, and load intents are all representable without duplicate selection state in `App`, per D-05, D-12, D-13, D-25, and D-29.</action>
  <verify>
    <automated>npm run test:run -- src/hooks/useMapState.test.ts &amp;&amp; npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `MapStateProvider` is exported from `src/providers/MapStateProvider.tsx`.
    - `useMapState` exposes state, semantic operations, `canUndo`, `canRedo`, and `canReset` with strict types.
    - No second selected-country state exists in the hook/provider API.
    - Tests, lint, and strict TypeScript all exit 0.
  </acceptance_criteria>
  <done>All UI plans can consume one centralized, tested map-state API with truthful derived control state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI events → reducer | Rapid or malformed action sequences mutate creator work. |
| Loaded colors → reducer baseline | Persisted records enter current editing state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Vitest execution | mitigate | Plan 01-approved package, exact lockfile, focused tests. |
| T-01-06 | Tampering | Reducer payloads | mitigate | Clone records, strict action unions, immutable snapshots, and ID-keyed contracts. |
| T-01-07 | Denial of Service | Unbounded history | mitigate | Enforce D-30 `HISTORY_LIMIT = 50`, no-op suppression, and 50+ action tests. |
| T-01-08 | Repudiation | Ambiguous undo/load behavior | mitigate | One commit path and tests proving branch, reset, and load-baseline semantics. |
</threat_model>

<verification>
Run the reducer test file, lint, and strict app TypeScript check.
</verification>

<success_criteria>
The state engine remains correct across single/bulk edits, 50+ undoable actions, branch edits, reset, selection-only operations, and persisted-map loads.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-04-SUMMARY.md` when done.
</output>
