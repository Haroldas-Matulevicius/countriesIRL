---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "10"
type: execute
wave: 5
depends_on: ["01-09"]
files_modified:
  - src/components/SaveLoad.tsx
autonomous: true
requirements: [F6.1, F6.2, NFR5, NFR11]
must_haves:
  truths:
    - "Creators can save, replace, load, and delete maps from an accessible modal using exact approved labels and feedback."
    - "Loading closes the modal, resets map history through LOAD_STATE integration, and returns focus to the map."
    - "Storage corruption, quota, and unavailable states remain recoverable without losing the entered map name or current map."
  artifacts:
    - path: "src/components/SaveLoad.tsx"
      provides: "Accessible reactive save/load/delete modal"
      exports: ["SaveLoad"]
  key_links:
    - from: "src/components/SaveLoad.tsx"
      to: "src/hooks/useLocalStorage.ts"
      via: "typed persistence operations"
      pattern: "useLocalStorage"
    - from: "src/components/SaveLoad.tsx"
      to: "LOAD_STATE callback"
      via: "validated load result"
      pattern: "onLoad"
---

<objective>
Build the complete accessible save/load modal against the tested persistence facade.

Purpose: Deliver reliable browser-local project management with exact overwrite, error, focus, and delete semantics.
Output: One production-ready `SaveLoad` component covering empty, populated, warning, and error states.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/hooks/useLocalStorage.ts
@src/hooks/useMapState.ts
@src/types/ui.ts
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/storage.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the save, replace, load, delete, focus, and storage-error modal contract</name>
  <files>src/components/SaveLoad.tsx</files>
  <read_first>
    src/components/SaveLoad.tsx
    src/hooks/useLocalStorage.ts
    src/hooks/useMapState.ts
    src/types/ui.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/coding-rules/storage.md
  </read_first>
  <action>Implement `SaveLoad` as `role="dialog"`, `aria-modal="true"`, labeled by `Save or load maps`, with focus trap, Escape close, overlay close only when no save is active, and focus restoration to the opener. Lazily refresh saved maps when opened. The `Map name` field trims input, enforces 100 characters, shows exact empty-name error, detects exact trimmed-name replacement, displays the exact overwrite notice, and changes CTA from `Save Current Map` to `Replace Saved Map`. Render `No saved maps yet` copy when empty; otherwise show name, `DD MMM YYYY`, `Load This Map`, and `Delete Saved Map`. Load validates against current country IDs, calls the provided LOAD_STATE callback, closes, returns focus to map, and announces `Saved map loaded.` Delete is immediate with no confirmation, keeps the working map unchanged, refreshes rows, and moves focus to next/previous/name field. Translate typed storage outcomes to the exact corrupt, unavailable, and quota copy while preserving the map-name draft/modal. This implements D-32 and D-34 through D-39, D-48, and D-51. Do not call localStorage directly or use native alerts.</action>
  <verify>
    <automated>npm run test:run -- src/utils/storage.test.ts &amp;&amp; npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Exact modal headings, labels, empty/error/overwrite copy, and row actions from UI-SPEC are present.
    - Dialog has aria-modal, focus trap, Escape close, and opener focus restoration.
    - Delete has no confirmation and load uses a validated callback rather than direct reducer/storage access.
    - Quota/unavailable/corrupt states preserve current map and entered name.
    - Storage tests, lint, and TypeScript exit 0.
  </acceptance_criteria>
  <done>Creators can persist and recover maps across reload with immediate list updates and accessible, exact failure handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted map names/colors → modal and reducer | Untrusted browser data becomes visible text and active map colors. |
| Modal keyboard focus → application | A trapped dialog temporarily owns interaction focus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Build/test packages | mitigate | Exact lockfile and package gate. |
| T-01-24 | Tampering / XSS | Saved map rows | mitigate | Validated adapter data plus React text interpolation; no HTML sinks. |
| T-01-25 | Denial of Service | Focus trap/overlay | mitigate | Escape and explicit close, focus restoration, no close during active save. |
| T-01-26 | Repudiation | Replace/delete behavior | mitigate | Explicit replace warning/CTA and deterministic success announcements; delete affects saved record only. |
</threat_model>

<verification>
Run storage tests, lint, and strict TypeScript; modal keyboard/focus and reload persistence are verified in UAT.
</verification>

<success_criteria>
The complete local save/load/delete workflow is accessible, reactive, exact-copy compliant, and resilient to malformed or unavailable storage.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-10-SUMMARY.md` when done.
</output>
