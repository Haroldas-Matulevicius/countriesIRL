---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "05"
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - scripts/prepareGeoData.mjs
  - public/data/europe-modern.geojson
  - public/data/README.md
  - src/utils/geojson.ts
  - src/utils/geojson.test.ts
  - src/hooks/useGeoData.ts
autonomous: true
requirements: [F1.1, NFR1]
must_haves:
  truths:
    - "The app loads a committed Europe-focused Natural Earth 1:10m asset without third-party runtime requests."
    - "Malformed, duplicate-ID, unnamed, or unsupported features are skipped with warnings while usable countries remain available."
    - "An unusable or failed collection becomes a recoverable fatal load state rather than a crash."
  artifacts:
    - path: "scripts/prepareGeoData.mjs"
      provides: "Reproducible Natural Earth filtering and normalization"
    - path: "public/data/europe-modern.geojson"
      provides: "Bundled normalized modern Europe boundaries"
      contains: "FeatureCollection"
    - path: "src/utils/geojson.ts"
      provides: "Unknown-input runtime normalization"
      exports: ["normalizeGeoJson"]
    - path: "src/hooks/useGeoData.ts"
      provides: "Abortable one-time data loading and O(1) lookup"
      exports: ["useGeoData"]
  key_links:
    - from: "src/hooks/useGeoData.ts"
      to: "/data/europe-modern.geojson"
      via: "single abortable fetch"
      pattern: "europe-modern\\.geojson"
    - from: "src/hooks/useGeoData.ts"
      to: "src/utils/geojson.ts"
      via: "unknown payload normalization"
      pattern: "normalizeGeoJson"
---

<objective>
Create the reproducible Natural Earth data pipeline and the strict browser loading boundary for the modern Europe map.

Purpose: Resolve the highest-risk data contract before D3 rendering, persistence keys, and selection depend on stable country IDs.
Output: Preparation script, committed asset/provenance, tested normalizer, and abortable `useGeoData` hook.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/types/map.ts
@src/constants/config.ts
@.planning/coding-rules/general.md
@.planning/coding-rules/data.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md

<interfaces>
`normalizeGeoJson(input: unknown)` returns either a usable normalized feature collection with warning metadata or a fatal error result. Every accepted feature has a stable non-sentinel string ID, trimmed non-empty name, and Polygon/MultiPolygon geometry. `useGeoData()` exposes a loading/ready/error discriminated state, normalized features, warning state, and `Map<string, GeoFeature>` lookup.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the unknown-input GeoJSON normalizer</name>
  <files>src/utils/geojson.ts, src/utils/geojson.test.ts</files>
  <read_first>
    src/utils/geojson.ts
    src/utils/geojson.test.ts
    src/types/map.ts
    .planning/coding-rules/data.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
  </read_first>
  <behavior>
    - Valid Polygon and MultiPolygon features with stable IDs and names pass.
    - Missing/sentinel/duplicate IDs, missing names, invalid coordinates, and unsupported geometry are omitted with warning details.
    - A partially valid collection succeeds with warnings; a non-FeatureCollection or collection with no valid features fails explicitly.
    - The lookup map contains exactly one entry per accepted feature ID.
  </behavior>
  <action>Write failing table-driven tests, then implement strict unknown-value guards and normalization per D-18, D-19, D-20, D-28, D-56, and D-57. Reject unresolved IDs rather than inventing index IDs. Accept only Polygon/MultiPolygon geometry and build a fresh normalized object containing only `id`, `properties.name`, and geometry. Return warning metadata separately from fatal errors; never throw because one feature is malformed.</action>
  <verify>
    <automated>npm run test:run -- src/utils/geojson.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover unknown collection, valid geometry types, missing name/ID, sentinel/duplicate ID, invalid geometry, partial warning, and unusable collection.
    - No `any`, index-generated ID, or unvalidated upstream property access appears.
    - The focused test command exits 0.
  </acceptance_criteria>
  <done>The runtime data boundary produces stable ID-keyed features and degrades safely around malformed records.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and document the committed Natural Earth Europe asset</name>
  <files>scripts/prepareGeoData.mjs, public/data/europe-modern.geojson, public/data/README.md</files>
  <read_first>
    scripts/prepareGeoData.mjs
    public/data/europe-modern.geojson
    public/data/README.md
    src/utils/geojson.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
  </read_first>
  <action>Create a deterministic Node preparation script that fetches or reads the official Natural Earth 1:10m Admin 0 Countries GeoJSON, filters a documented Europe/transcontinental inclusion set, maps upstream `ADMIN`/`NAME` and stable administrative-code fields into the runtime contract, rejects sentinel/duplicate IDs, and writes `public/data/europe-modern.geojson`. Use Natural Earth 5.1.1 default geopolitical POV per research recommendation and document that choice for later human acceptance. Record source URL/repository, version, public-domain terms, inclusion policy, stable-ID precedence, and the exact transformation command in `public/data/README.md`. This implements D-17 through D-20; runtime code must never call Natural Earth or another map API.</action>
  <verify>
    <automated>node scripts/prepareGeoData.mjs &amp;&amp; npm run test:run -- src/utils/geojson.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Running the script twice produces the same normalized asset content.
    - The asset is a FeatureCollection with only stable unique string IDs, trimmed names, and Polygon/MultiPolygon geometry.
    - `public/data/README.md` records Natural Earth 5.1.1, default POV, public-domain terms, inclusion policy, and the generation command.
    - No runtime source URL is needed to render the map.
  </acceptance_criteria>
  <done>The repository contains a reproducible, provenance-documented, browser-local modern Europe dataset.</done>
</task>

<task type="auto">
  <name>Task 3: Load normalized map data once with abort and lookup semantics</name>
  <files>src/hooks/useGeoData.ts</files>
  <read_first>
    src/hooks/useGeoData.ts
    src/utils/geojson.ts
    src/types/map.ts
    public/data/europe-modern.geojson
    .planning/coding-rules/frontend.md
    .planning/coding-rules/data.md
  </read_first>
  <action>Implement `useGeoData` with one mount-time fetch of `/data/europe-modern.geojson`, `AbortController` cleanup, response-status handling, unknown JSON normalization, cached feature state, warning propagation, and a memoized O(1) ID lookup per D-19 and D-20. Represent loading, ready-with-optional-warnings, and fatal error as a discriminated state. Ignore abort completion without surfacing an error; translate other technical failures into a stable hook error code/message that the UI can map to approved copy.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit &amp;&amp; npm run test:run -- src/utils/geojson.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `/data/europe-modern.geojson` fetch effect exists.
    - The effect returns `controller.abort()` cleanup.
    - Ready state includes normalized features and a memoized ID lookup.
    - Partial warnings and fatal errors remain distinguishable.
    - Lint, strict TypeScript, and GeoJSON tests exit 0.
  </acceptance_criteria>
  <done>Components can consume a stable, cached, abortable map-data state without raw parsing or linear country lookup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Natural Earth upstream → preparation script | External geospatial data enters the repository. |
| Bundled JSON → browser state | Static JSON is still untrusted input at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm test/build dependencies | mitigate | Exact lockfile and approved package gate. |
| T-01-09 | Tampering | GeoJSON IDs/names/geometries | mitigate | Deterministic preprocessing plus runtime unknown-value validation and duplicate rejection. |
| T-01-10 | Denial of Service | Malformed/oversized feature set | mitigate | Europe filter, accepted geometry types, invalid-feature skipping, unusable-collection fatal state. |
| T-01-11 | Information Disclosure | Runtime third-party requests | mitigate | Commit the normalized asset; browser fetches same-origin only. |
</threat_model>

<verification>
Run the preparation command, GeoJSON tests, lint, and strict TypeScript check.
</verification>

<success_criteria>
The same-origin asset loads into stable features/lookup, malformed entries cannot crash the app, and source provenance/POV is documented for approval.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-05-SUMMARY.md` when done.
</output>
