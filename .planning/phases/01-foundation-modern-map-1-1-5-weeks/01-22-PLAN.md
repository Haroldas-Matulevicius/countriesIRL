---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "22"
type: tdd
wave: 12
depends_on: ["01-21"]
files_modified:
  - src/utils/mapProjection.ts
  - src/utils/mapProjection.test.ts
autonomous: true
gap_closure: true
requirements: [F1.1, F5.1, NFR1, NFR5]
must_haves:
  truths:
    - "Initial map geometry performs one finite per-feature projected-bounds traversal for centering and one final safe path-generation traversal, rather than the five traversals introduced by e2f9190."
    - "The approved Natural Earth 5.1.1 dataset still produces exactly 57 non-empty SVG paths with byte-identical path data and the same projection translation as the pre-fix rendering."
    - "Thrown, non-finite, null, NaN, and Infinity geometry outcomes are ignored or converted to an empty path without poisoning the aggregate center or crashing valid-country rendering."
    - "Focused projection tests and the full unfiltered source test suite pass, followed by lint, deterministic data, strict TypeScript, and build in an isolated clean worktree of the exact implementation commit."
  artifacts:
    - path: "src/utils/mapProjection.ts"
      provides: "Single-pass finite bounds aggregation, centered fixed-Europe projection, and one-call safe final path generation"
      exports: ["hasFiniteProjectedBounds", "createSafeMapPath", "createFixedEuropeProjection"]
    - path: "src/utils/mapProjection.test.ts"
      provides: "Traversal-count, rendering-equivalence, 57-path, and malformed-geometry regression coverage"
      contains: "createFixedEuropeProjection"
  key_links:
    - from: "src/utils/mapProjection.ts"
      to: "d3.geoPath().bounds(feature)"
      via: "finite per-feature bounds aggregation"
      pattern: "bounds\(feature\)"
    - from: "src/components/MapCanvas.tsx"
      to: "src/utils/mapProjection.ts"
      via: "one createSafeMapPath call for each final country d attribute"
      pattern: "attr\('d'.*createSafeMapPath"
    - from: "src/utils/mapProjection.test.ts"
      to: "public/data/europe-modern.geojson"
      via: "legacy-versus-optimized translation and path-byte equivalence on all 57 normalized features"
      pattern: "europe-modern\.geojson"
---

<objective>
Remove the proven map-ready regression by replacing redundant D3 geometry traversals with one finite per-feature bounds aggregation followed by one final safe path generation, while preserving the approved map and every malformed-geometry guard.

Purpose: Restore enough production rendering margin for the locked per-sample <500ms map-ready threshold without weakening D-53, changing the accepted Natural Earth presentation, or altering export geometry.
Output: Tested projection/path utilities that retain exact 57-path rendering and safely exclude malformed geometry with approximately two full coordinate traversals instead of five.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
@C:/Users/matul/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/phase1-map-ready-performance.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-06-SUMMARY.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-21-SUMMARY.md
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/data.md
@src/utils/mapProjection.ts
@src/utils/mapProjection.test.ts
@src/components/MapCanvas.tsx
@public/data/europe-modern.geojson

<interfaces>
Current contracts from src/utils/mapProjection.ts:
- `hasFiniteProjectedBounds(bounds: [[number, number], [number, number]]): boolean`
- `createSafeMapPath(pathGenerator: ReturnType<typeof geoPath>, feature: GeoFeature): string`
- `createFixedEuropeProjection(features: ReadonlyArray<GeoFeature>): ReturnType<typeof geoMercator>`

Current integration from src/components/MapCanvas.tsx:
- `const projection = createFixedEuropeProjection(alphabeticalFeatures)`
- `const pathGenerator = geoPath(projection)`
- Final keyed D3 join assigns each `d` with `createSafeMapPath(pathGenerator, feature)`.

The executor may add a narrowly typed bounds-aggregation helper for direct testing, but must preserve the three public contracts above for existing callers.
</interfaces>
</context>

<feature>
  <name>Single-bounds plus single-path fixed-Europe geometry pipeline</name>
  <files>src/utils/mapProjection.ts, src/utils/mapProjection.test.ts</files>
  <behavior>
    - Traversal behavior: an instrumented D3 geoTransform/geoPath stream proves bounds aggregation traverses each feature once and never generates path data during centering; final safe path generation traverses a feature once and does not call bounds again.
    - Bounds aggregation: finite bounds are merged by minimum x/y and maximum x/y; thrown or non-finite candidates are skipped; no valid candidates leave the original finite fixed-Europe projection unchanged.
    - Safe path generation: one generator call returns valid path text unchanged and returns an empty string for thrown, null, NaN, or Infinity output.
    - Dataset equivalence: a test-only reference matching the e2f9190 rendering algorithm and the optimized implementation produce the same finite translation and byte-identical ordered path strings for all 57 normalized Natural Earth features.
    - Invalid feature handling: adding malformed features does not alter the valid-feature center or paths, each malformed final path is empty, and the valid dataset still yields exactly 57 non-empty paths.
  </behavior>
  <implementation>Keep D3 Mercator/SVG rendering per D-03, D-04, and D-21. In `createFixedEuropeProjection`, begin from the existing fixed viewport fit, call projected bounds exactly once for each candidate feature, accept only finite bounds, merge them into one aggregate, compute the aggregate center, and apply the same finite translation correction before the existing clip extent. Do not filter by calling `createSafeMapPath`, do not build a FeatureCollection for another bounds pass, and do not mutate source features. Refactor `createSafeMapPath` so it invokes the path generator exactly once, preserves valid path bytes, rejects null/NaN/Infinity text, and catches malformed-geometry exceptions; MapCanvas then retains one safe final-path call per keyed country without a component rewrite. Write the RED tests first, including an explicit test-only legacy reference for equivalence, then implement GREEN and refactor without weakening D-18, D-19, D-24, D-40, D-53, D-55, D-56, D-57, or D-58. Preserve the exact accepted asset, 57 rendered paths, projection translation, export geometry, keyboard/event wiring, and stable D3 join. Do not simplify the GeoJSON, change Natural Earth bytes, alter centering scope, add region variants, or lazy-load html2canvas in this gap; those items are outside this targeted correction per D-17, D-62, and the diagnosed resolution.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove and implement the two-traversal geometry pipeline</name>
  <files>src/utils/mapProjection.ts, src/utils/mapProjection.test.ts</files>
  <behavior>
    - RED: traversal-count tests fail against e2f9190 behavior because centering invokes path generation/bounds repeatedly.
    - GREEN: each feature contributes at most one finite bounds result to centering and each final path is generated once.
    - Regression: optimized and test-only legacy outputs have identical translation and 57 ordered path strings for the approved dataset.
    - Safety: thrown/non-finite bounds and thrown/null/NaN/Infinity paths remain contained while valid features render.
  </behavior>
  <action>Execute the feature's RED→GREEN→REFACTOR cycle. Use a D3 stream counter or an equivalently direct test seam that measures geometry traversal rather than elapsed time. Keep the legacy five-traversal behavior inside the test reference only; production must use aggregate finite per-feature bounds and one final safe path call. Avoid timing assertions in Vitest because traversal counts, exact output equivalence, malformed handling, and the production browser rerun are the contracts. Run the focused projection test during RED and GREEN, then commit the completed test and implementation changes before the authoritative gate. Capture the exact final implementation commit with `git rev-parse HEAD`; create a detached temporary worktree/checkout of that commit outside the repository's existing evidence directories; run `npm ci`, `npm run lint`, the full unfiltered `npm run test:run`, deterministic GeoJSON validation, strict application TypeScript, and `npm run build` inside that clean checkout. The clean checkout is authoritative because the main checkout contains nested executable evidence tooling that contaminates repository-wide `eslint .`. Remove the temporary worktree afterward and record the exact verified commit plus each command result in `01-22-SUMMARY.md`. Do not treat a filtered-only test run or a gate from the contaminated main checkout as final evidence. Do not modify `MapCanvas.tsx` unless an existing contract is broken; its current single `createSafeMapPath` call per final `d` becomes the required one-path traversal after the utility correction.</action>
  <verify>
    <automated>npm run test:run -- src/utils/mapProjection.test.ts &amp;&amp; VERIFIED_COMMIT=$(git rev-parse HEAD) &amp;&amp; CLEAN_ROOT=$(mktemp -d) &amp;&amp; git worktree add --detach "$CLEAN_ROOT" "$VERIFIED_COMMIT" &amp;&amp; trap 'git worktree remove --force "$CLEAN_ROOT" &gt;/dev/null 2&gt;&amp;1 || true; rm -rf "$CLEAN_ROOT"' EXIT &amp;&amp; (cd "$CLEAN_ROOT" &amp;&amp; npm ci &amp;&amp; npm run lint &amp;&amp; npm run test:run &amp;&amp; node scripts/prepareGeoData.mjs --check &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit &amp;&amp; npm run build)</automated>
  </verify>
  <done>The focused projection suite passes; the full unfiltered test suite, lint, deterministic data check, strict TypeScript, and build all pass in an isolated clean worktree of the exact recorded implementation commit; tests prove one bounds traversal plus one final path traversal, exact 57-path/translation equivalence, and malformed-geometry safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Normalized GeoJSON → D3 projection | Detailed or malformed coordinate streams can consume main-thread time or produce non-finite projected bounds. |
| D3 projection/path utilities → SVG `d` attributes | Invalid generated strings must not reach the rendered/exported SVG. |
| Working repository → authoritative quality gate | Nested executable evidence tooling can contaminate `eslint .`; only a clean checkout of the exact commit is authoritative. |
| Regression tests → performance acceptance | Unit tests can prove traversal count/equivalence but cannot replace the production-browser threshold rerun in Plan 01-15. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Dependency graph | mitigate | Use the exact lockfile with `npm ci` in the detached clean worktree; no dependency change is permitted. |
| T-01-63 | Denial of Service | `createFixedEuropeProjection` | mitigate | Bound main-thread geometry work to one finite bounds pass per feature plus one final path pass, with traversal-count regression tests. |
| T-01-64 | Tampering | Projected bounds/path output | mitigate | Ignore thrown/non-finite bounds, reject null/NaN/Infinity paths, and preserve valid-feature rendering. |
| T-01-65 | Repudiation | Rendering and quality-gate claims | mitigate | Compare every path byte/translation against a legacy reference and record the exact commit verified by the isolated unfiltered gate. |
</threat_model>

<verification>
Run the focused projection suite during TDD. After the final implementation commit exists, run `npm ci`, lint, full unfiltered `npm run test:run`, deterministic data validation, strict TypeScript, and build in a detached clean worktree of that exact commit. The summary records the commit and command outcomes. Confirm the implementation diff remains limited to `src/utils/mapProjection.ts` and `src/utils/mapProjection.test.ts`; Plan 01-15 stays blocked until this clean gate passes.
</verification>

<success_criteria>
- A failing RED traversal/equivalence/safety test is committed as `test(01-22): add failing map traversal regression tests`.
- The aggregate-bounds and one-call safe-path implementation passes the focused suite and is committed as `feat(01-22): implement bounded map geometry traversal`.
- Any optional refactor preserves green tests and uses a separate `refactor(01-22): ...` commit.
- The exact final implementation commit is recorded and passes `npm ci`, lint, full unfiltered tests, deterministic data validation, strict TypeScript, and build in an isolated clean worktree.
- The production geometry pipeline preserves exact approved rendering/export geometry and malformed-data containment before production-preview browser measurement.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-22-SUMMARY.md` with explicit RED, GREEN, optional REFACTOR, exact verified commit, clean-worktree gate commands/results, and commit sections when done.
</output>
