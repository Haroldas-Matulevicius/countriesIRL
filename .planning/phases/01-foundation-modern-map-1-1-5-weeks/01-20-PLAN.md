---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "20"
type: execute
wave: 10
depends_on: ["01-14"]
files_modified:
  - src/utils/export.ts
  - src/utils/export.test.ts
autonomous: true
gap_closure: true
requirements: [F5.1, F5.3, NFR4, NFR5]
must_haves:
  truths:
    - "Chrome 150 and Edge 150 receive a native PNG download without cancellation caused by immediate anchor removal or object-URL revocation."
    - "Export returns success only after the native download activation has crossed an asynchronous browser handoff boundary."
    - "The PNG remains exactly 1080×1080, opaque white, map-only, correctly named, and fully cleaned up after the safe handoff."
  artifacts:
    - path: "src/utils/export.ts"
      provides: "Deferred native-download handoff and cleanup semantics"
      exports: ["exportMapPng"]
    - path: "src/utils/export.test.ts"
      provides: "Timer/handoff/cleanup and existing exact-output regression coverage"
  key_links:
    - from: "src/utils/export.ts"
      to: "anchor click and object URL lifecycle"
      via: "awaited macrotask handoff before remove/revoke and success return"
      pattern: "setTimeout|revokeObjectURL|downloadAnchor"
    - from: "src/App.tsx"
      to: "ExportResult.ok"
      via: "existing success toast only after exportMapPng resolves"
      pattern: "PNG downloaded at 1080 × 1080"
---

<objective>
Fix native PNG cancellation in Chromium by keeping the anchor and object URL alive through a defined browser download handoff before cleanup and success resolution.

Purpose: Stop Chrome 150 and Edge 150 from canceling a received download while preserving exact output and truthful UI feedback.
Output: Minimal export utility lifecycle correction with unit coverage; App orchestration remains unchanged because it already announces success only for `ExportResult.ok`.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/utils/export.ts
@src/utils/export.test.ts
@src/App.tsx
@src/components/Controls.tsx
@src/types/ui.ts
@src/constants/config.ts
@.planning/coding-rules/export.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Defer native download cleanup until an awaited browser handoff completes</name>
  <files>src/utils/export.ts, src/utils/export.test.ts</files>
  <read_first>
    src/utils/export.ts
    src/utils/export.test.ts
    src/App.tsx
    src/components/Controls.tsx
    src/types/ui.ts
    src/constants/config.ts
    .planning/coding-rules/export.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
  </read_first>
  <behavior>
    - After anchor click, the export promise remains pending and the connected anchor/object URL remain live until an asynchronous handoff timer completes.
    - On handoff completion, the existing `finally` removes the anchor, revokes the object URL, and removes the export frame before the caller receives the successful result.
    - If anchor click throws, export resolves failure and cleanup still occurs without waiting for a success handoff.
    - Existing dimension mismatch, null blob, capture rejection, filename, white background, map-only sanitization, and cleanup tests continue to pass.
  </behavior>
  <action>Use fake timers to write a failing regression test around the existing fake DOM: after `downloadAnchor.click()`, assert the returned promise has not resolved, the anchor remains connected/not removed, and `URL.revokeObjectURL` has not run. Advance a named `DOWNLOAD_HANDOFF_DELAY_MS = 100` macrotask, then assert success resolves and all temporary resources are cleaned. Implement a small awaited `waitForDownloadHandoff` helper using `window.setTimeout`/global `setTimeout`; call it only after the connected anchor click succeeds and before returning `{ ok: true, filename }`. Keep the existing `finally` cleanup, so anchor removal and object-URL revocation occur after the awaited handoff rather than in the same click stack. Preserve the exact 540×540 scale-2 capture, 1080×1080 assertion, opaque white map-only clone, `toBlob`, filename, and typed failure reasons. Do not change App: it already emits `PNG downloaded at 1080 × 1080.` only after `exportMapPng` resolves with `ok: true`; the delayed success result corrects the false announcement timing. This fixes D-40, D-41, D-42, D-43, D-44, D-45, D-46, D-48, D-54, and D-57.</action>
  <verify>
    <automated>npm run test:run -- src/utils/export.test.ts &amp;&amp; npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - A focused fake-timer test proves success cannot resolve and URL/anchor cleanup cannot occur before the 100ms handoff.
    - After the handoff, success resolves and anchor, object URL, and export frame are cleaned exactly once.
    - Click failure remains a typed failure with immediate safe cleanup.
    - Existing tests still prove exact 1080×1080 dimensions, white map-only content, filename, blob, capture, and failure behavior.
    - Export-focused tests, lint, strict TypeScript, and build exit 0.
    - Only `src/utils/export.ts` and `src/utils/export.test.ts` change.
  </acceptance_criteria>
  <done>The native download lifecycle survives Chromium's asynchronous handoff, success feedback is no longer premature, and exact PNG/cleanup contracts remain tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blob/object URL → browser download manager | Native download initiation is asynchronous relative to anchor click and cleanup. |
| Export result → success toast | UI trusts the utility's success timing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Existing html2canvas/Vitest packages | mitigate | Install nothing; preserve exact audited dependencies and lockfile. |
| T-01-56 | Denial of Service | Premature object URL revocation | mitigate | Await a named 100ms macrotask handoff before success and cleanup. |
| T-01-57 | Spoofing | False success announcement | mitigate | ExportResult success resolves only after handoff; App already gates toast on `ok: true`. |
| T-01-58 | Information Disclosure | Object URL retained too long | mitigate | Bounded 100ms lifetime followed by finally cleanup on every path. |
</threat_model>

<verification>
Export-focused unit tests and the full static gate pass; Plan 01-21 then verifies real native downloads in Chrome 150 and Edge 150 before the full browser matrix resumes.
</verification>

<success_criteria>
Chrome/Edge cancellation root cause is addressed by delayed lifecycle cleanup, output remains exact and opaque/map-only, and success is announced only after the native handoff boundary.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-20-SUMMARY.md` when done.
</output>
