---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "13"
type: execute
wave: 6
depends_on: ["01-06", "01-07", "01-08", "01-10", "01-11"]
files_modified:
  - src/styles/theme.css
  - src/styles/App.css
  - src/styles/MapCanvas.css
  - src/styles/Controls.css
autonomous: true
requirements: [NFR5, NFR6, NFR7, NFR11]
must_haves:
  truths:
    - "The map is the dominant square workspace on desktop and the app remains fully usable at tablet and 360px mobile widths."
    - "System dark theme changes chrome only; map preview and export colors remain fixed white/light."
    - "All controls meet focus, touch-target, contrast, reduced-motion, typography, spacing, and non-color-cue contracts."
  artifacts:
    - path: "src/styles/theme.css"
      provides: "Approved light/dark tokens, typography, spacing, focus, and motion variables"
    - path: "src/styles/App.css"
      provides: "Desktop/tablet/mobile source-order-compatible layout"
    - path: "src/styles/MapCanvas.css"
      provides: "Map square and editor-only path interaction states"
    - path: "src/styles/Controls.css"
      provides: "Controls, swatches, modal, toast, banners, and state styling"
  key_links:
    - from: "src/styles/theme.css"
      to: "src/styles/App.css"
      via: "CSS custom properties"
      pattern: "var\\(--"
    - from: "src/styles/MapCanvas.css"
      to: "country-path selected/focused classes"
      via: "editor-only style classes"
      pattern: "country-path"
---

<objective>
Apply the complete approved design system, responsive layouts, accessibility styling, and fixed white map/export presentation.

Purpose: Deliver the locked polished desktop/tablet/secondary-mobile experience without altering functional module ownership.
Output: Four component-scoped CSS files implementing every UI-SPEC visual and responsive acceptance rule.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/MapCanvas.tsx
@src/components/MapWorkspace.tsx
@src/components/SelectionPanel.tsx
@src/components/ColorPicker.tsx
@src/components/CountryList.tsx
@src/components/Controls.tsx
@src/components/SaveLoad.tsx
@src/components/AppHeader.tsx
@src/components/OnboardingBanner.tsx
@src/components/ToastRegion.tsx
@.planning/coding-rules/frontend.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the exact token system and responsive workspace layout</name>
  <files>src/styles/theme.css, src/styles/App.css</files>
  <read_first>
    src/styles/theme.css
    src/styles/App.css
    src/App.tsx
    src/components/AppHeader.tsx
    src/components/OnboardingBanner.tsx
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
  </read_first>
  <action>Define the UI-SPEC light/dark color values, seven spacing tokens (4/8/16/24/32/48/64), exactly four type sizes (14/16/20/28), exactly weights 400/600, 8px and 16px radii, 150ms motion, visible accent focus rings, fixed white map tokens, and reduced-motion overrides per D-09, D-49 through D-52. Build desktop layout at 1200+ with centered max 1440, map-first DOM/grid, flexible map column plus 360px controls and 24px gap. Build tablet 768–1199 and mobile <768 layouts using the same DOM/focus order defined by the components: actions, map, selection/color, country list; no CSS order mismatch. At 360px use 16px gutters, two-column action grid with Export spanning both, square full-width map, five-column wrapping swatches when possible, and no page-level horizontal scroll. Use plain CSS only: no gradients, emoji decoration, CSS-in-JS, Tailwind, or third-party library.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Token values and reserved accent usage match UI-SPEC sections 4–6.
    - Authored spacing uses only the declared scale (plus safe-area env values).
    - Exactly four font sizes and two weights are defined/used.
    - Desktop, tablet, and mobile media rules implement specified column/order behavior.
    - Dark theme leaves map/export surface variables white/light.
    - Lint and build exit 0.
  </acceptance_criteria>
  <done>The app has the approved theme and source-order-compatible responsive workspace from desktop through 360px.</done>
</task>

<task type="auto">
  <name>Task 2: Style map states, controls, modal, feedback, focus, touch, and motion</name>
  <files>src/styles/MapCanvas.css, src/styles/Controls.css</files>
  <read_first>
    src/styles/MapCanvas.css
    src/styles/Controls.css
    src/components/MapCanvas.tsx
    src/components/ColorPicker.tsx
    src/components/CountryList.tsx
    src/components/Controls.tsx
    src/components/SaveLoad.tsx
    src/components/ToastRegion.tsx
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
  </read_first>
  <action>Style the responsive square map, white surface, subtle map-shell shadow, default 1px neutral borders, selected 2px black border, focused 3px dashed accent border, hover feedback, and 150ms fill/border transitions per D-22, D-23, D-50, D-51, and D-52. Ensure export-clone classes can suppress selected/focus/hover styling. Style controls with visible text, minimum 48×48 targets, native disabled clarity, one persistent accent-filled Export button, named swatches with borders/checkmarks, mixed-color preview, skeleton/loading/warning/fatal states, bottom-center/safe-area toast, and modal dimensions/focus visuals. Mobile modal is a full-height sheet with sticky header; desktop/tablet max width 560px and max height 80vh. Add `:focus-visible`, 200% zoom resilience, wrapped/truncated names, contrast-safe semantic colors, and reduced-motion removal of transitions/spinner rotation.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Map selection/focus/hover each has a distinct non-color-only visual state.
    - Controls/swatches are at least 48×48 and all focusable controls have focus-visible rings.
    - Export is the only persistent filled accent action.
    - Modal/toast/loading/warning/error classes cover all UI-SPEC states.
    - Reduced motion removes nonessential transitions and continuous rotation.
    - Lint and build exit 0.
  </acceptance_criteria>
  <done>The polished interaction states, modal, feedback, dark chrome, and responsive touch/accessibility presentation are complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS theme/media preferences → visible application | OS theme, reduced motion, viewport, and zoom alter presentation. |
| Editor classes → export clone | Selection/focus styles must not leak into the downloaded image. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Build dependencies | mitigate | Exact lockfile and package gate. |
| T-01-33 | Spoofing | Color-only selected state | mitigate | Border width, dashed focus, checkbox/checkmark, text count, and ARIA. |
| T-01-34 | Information Disclosure | UI theme → export | mitigate | Fixed export classes/tokens and white map surface independent of dark media query. |
| T-01-35 | Denial of Service | Narrow/zoomed layout | mitigate | 360px/no-horizontal-scroll and 200% zoom stress rules, minimum touch targets. |
</threat_model>

<verification>
Run lint/build; desktop, tablet, 360px, dark theme, reduced motion, and 200% zoom are manually verified in Plan 15.
</verification>

<success_criteria>
The interface matches the approved UI contract across themes, motion preferences, desktop/tablet/mobile widths, keyboard focus, and export isolation.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-13-SUMMARY.md` when done.
</output>
