---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "13"
type: execute
wave: 7
depends_on: ["01-12"]
files_modified:
  - src/main.tsx
  - src/styles/theme.css
  - src/styles/App.css
  - src/styles/MapCanvas.css
  - src/styles/Controls.css
autonomous: true
requirements: [NFR5, NFR6, NFR7, NFR11]
must_haves:
  truths:
    - "The map is the dominant square workspace on desktop and the app remains fully usable at tablet and 360px mobile widths."
    - "Styles are imported only after App/component class contracts exist, and CSS never reorders focus relative to the active React workspace branch."
    - "System dark theme changes chrome only; map preview and export colors remain fixed white/light."
    - "All controls meet focus, touch-target, contrast, reduced-motion, typography, spacing, and non-color-cue contracts."
  artifacts:
    - path: "src/main.tsx"
      provides: "Single ordered import point for all four Phase 1 stylesheets"
    - path: "src/styles/theme.css"
      provides: "Approved light/dark tokens, typography, spacing, focus, and motion variables"
    - path: "src/styles/App.css"
      provides: "Desktop/tablet/mobile layouts for the active React-composed workspace"
    - path: "src/styles/MapCanvas.css"
      provides: "Map square and editor-only path interaction states"
    - path: "src/styles/Controls.css"
      provides: "Controls, swatches, modal, toast, banners, and state styling"
  key_links:
    - from: "src/main.tsx"
      to: "src/styles/*.css"
      via: "ordered stylesheet imports after Plan 01-12"
      pattern: "styles/(theme|App|MapCanvas|Controls)\\.css"
    - from: "src/styles/App.css"
      to: "src/App.tsx"
      via: "class-based layout without CSS order/hiding duplicate workspaces"
      pattern: "workspace"
---

<objective>
Wire and apply the complete approved design system after functional composition exists, preserving viewport-correct React DOM/focus order.

Purpose: Deliver the locked polished desktop/tablet/secondary-mobile experience without premature imports, CSS reordering, hidden duplicate workspaces, or dark-theme export leakage.
Output: Style imports in `main.tsx` and four component-scoped CSS files implementing the UI-SPEC.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/main.tsx
@src/App.tsx
@src/hooks/useResponsiveLayout.ts
@src/components/MapCanvas.tsx
@src/components/MapWorkspace.tsx
@src/components/SelectionPanel.tsx
@src/components/ColorPicker.tsx
@src/components/CountryList.tsx
@src/components/Controls.tsx
@src/components/SaveLoad.tsx
@src/components/AppHeader.tsx
@src/components/OnboardingBanner.tsx
@src/components/ToastRegion.tsx
@.planning/coding-rules/frontend.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire stylesheet imports and define the exact token/layout system</name>
  <files>src/main.tsx, src/styles/theme.css, src/styles/App.css</files>
  <read_first>
    src/main.tsx
    src/App.tsx
    src/hooks/useResponsiveLayout.ts
    src/styles/theme.css
    src/styles/App.css
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
  </read_first>
  <action>Import `theme.css`, `App.css`, `MapCanvas.css`, and `Controls.css` from `src/main.tsx` in that order; no component or App duplicates these imports. Define the UI-SPEC light/dark colors, seven spacing tokens (4/8/16/24/32/48/64), exactly four type sizes (14/16/20/28), exactly weights 400/600, 8px/16px radii, 150ms motion, visible accent focus rings, fixed white map tokens, and reduced-motion overrides per D-09, D-49, D-50, D-51, and D-52. Style the desktop branch at 1200+ with centered max 1440, map-first grid, flexible map column plus 360px controls and 24px gap. Style the compact branch for 768–1199 and below 768 according to its already-correct actions/map/selection/list DOM order. Do not use CSS `order`, render-condition hacks, or selectors that hide a duplicate workspace. At 360px use 16px gutters, two-column actions with Export spanning both, a square full-width map, wrapping swatches, and no page-level horizontal scroll. Use plain CSS only and follow G-10 from PATTERNS.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.tsx` imports all four CSS files once and App/components import none.
    - Plan 01-13 depends on 01-12 and is the first plan that wires style imports.
    - CSS contains no `order` declaration used to change workspace focus sequence and no duplicate-workspace hide selector.
    - Token values, typography, spacing, accent reservation, and dark map/export values match UI-SPEC.
    - Desktop/tablet/mobile rules style the corresponding active React branch and 360px has no page overflow.
    - Lint and build exit 0.
  </acceptance_criteria>
  <done>Styles are sequenced after composition and the active workspace receives the approved theme/layout without focus-order divergence.</done>
</task>

<task type="auto">
  <name>Task 2: Style map states, controls, modal, feedback, focus, touch, and motion</name>
  <files>src/styles/MapCanvas.css, src/styles/Controls.css</files>
  <read_first>
    src/styles/MapCanvas.css
    src/styles/Controls.css
    src/components/MapCanvas.tsx
    src/components/ColorPicker.tsx
    src/components/CountryList.tsx
    src/components/Controls.tsx
    src/components/SaveLoad.tsx
    src/components/ToastRegion.tsx
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
  </read_first>
  <action>Style the responsive square map, white surface, subtle shadow, default 1px neutral borders, selected 2px black border, focused 3px dashed accent border, hover feedback, and 150ms fill/border transitions per D-22, D-23, D-50, D-51, and D-52. Ensure export-clone classes suppress selected/focus/hover styling. Style controls with visible text, minimum 48×48 targets, native disabled clarity, one persistent accent-filled Export button, named swatches with borders/checkmarks, mixed-color preview, skeleton/loading/warning/fatal states, bottom-center/safe-area toast, and modal dimensions/focus visuals. Mobile modal is a full-height sheet with sticky header; desktop/tablet max width 560px and max height 80vh. Add `:focus-visible`, 200% zoom resilience, wrapped/truncated names, contrast-safe semantic colors, and reduced-motion removal of transitions/spinner rotation.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Map selection/focus/hover each has a distinct non-color-only visual state.
    - Controls/swatches are at least 48×48 and all focusable controls have focus-visible rings.
    - Export is the only persistent filled accent action.
    - Modal/toast/loading/warning/error classes cover all UI-SPEC states.
    - Reduced motion removes nonessential transitions and continuous rotation.
    - Lint and build exit 0.
  </acceptance_criteria>
  <done>The polished interaction states, modal, feedback, dark chrome, and responsive touch/accessibility presentation are complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS theme/media preferences → visible application | OS theme, reduced motion, viewport, and zoom alter presentation. |
| React composition → CSS selectors | CSS must style, not reorder or duplicate, the active workspace tree. |
| Editor classes → export clone | Selection/focus styles must not leak into the downloaded image. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Build dependencies | mitigate | Exact lockfile and package gate. |
| T-01-33 | Spoofing | Color-only selected state | mitigate | Border width, dashed focus, checkbox/checkmark, text count, and ARIA. |
| T-01-34 | Information Disclosure | UI theme → export | mitigate | Fixed export classes/tokens and white map surface independent of dark media query. |
| T-01-35 | Denial of Service | Narrow/zoomed layout | mitigate | 360px/no-horizontal-scroll and 200% zoom stress rules, minimum touch targets. |
| T-01-45 | Spoofing | CSS visual order differs from focus order | mitigate | One React-composed branch and explicit prohibition on CSS order/duplicate hiding. |
</threat_model>

<verification>
Run lint/build; desktop, tablet, 360px, branch switching, dark theme, reduced motion, and 200% zoom are blocking checks in Plan 01-15.
</verification>

<success_criteria>
The interface matches the approved UI contract across viewport branches, themes, motion preferences, keyboard focus, and export isolation, with style wiring correctly sequenced after Plan 01-12.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-13-SUMMARY.md` when done.
</output>
