---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/types/map.ts
  - src/types/ui.ts
  - src/constants/colors.ts
  - src/constants/config.ts
  - src/utils/colors.ts
  - src/utils/colors.test.ts
autonomous: true
requirements: [F1.3, NFR10, NFR11]
must_haves:
  truths:
    - "Valid #RGB, #RRGGBB, and rgb(r,g,b) input normalizes to uppercase #RRGGBB."
    - "Invalid syntax and RGB channels outside 0-255 are rejected without changing map state."
    - "All later modules share strict ID-keyed map, selection, persistence, and export contracts."
  artifacts:
    - path: "src/types/map.ts"
      provides: "GeoFeature, MapState, MapAction, selection, and GeoJSON result contracts"
      exports: ["GeoFeature", "MapState", "MapAction"]
    - path: "src/types/ui.ts"
      provides: "Saved-map, toast, storage result, and export UI contracts"
      exports: ["SavedMap"]
    - path: "src/utils/colors.ts"
      provides: "Color parsing, validation, normalization, and equality helpers"
      exports: ["normalizeColor"]
    - path: "src/utils/colors.test.ts"
      provides: "Color boundary tests"
  key_links:
    - from: "src/utils/colors.ts"
      to: "src/constants/colors.ts"
      via: "approved palette/default constants"
      pattern: "COLOR_PRESETS|DEFAULT_COLOR"
    - from: "src/types/map.ts"
      to: "Record<string, string>"
      via: "country-ID keyed colors"
      pattern: "Record<string, string>"
---

<objective>
Define the shared strict contracts and tested color normalization boundary used by state, map rendering, persistence, and export.

Purpose: Prevent name-keyed state, invalid colors, and incompatible cross-plan interfaces before parallel feature work begins.
Output: Shared types/constants plus a fully tested color parser/normalizer.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/data.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md

<interfaces>
Create `GeoFeature` with a stable string `id`, trimmed `properties.name`, and `GeoJSON.Polygon | GeoJSON.MultiPolygon` geometry. Create `MapState` with ID-keyed `colors`, immutable `history`, `historyIndex`, and one shared selected-ID collection. Create `MapAction` intents for SET_COLOR, SET_COLORS, RESET_ALL, UNDO, REDO, SELECT_COUNTRY, selection-set updates, and LOAD_STATE. Color normalization returns a discriminated success/error result and stores only uppercase `#RRGGBB`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define domain contracts and named constants</name>
  <files>src/types/map.ts, src/types/ui.ts, src/constants/colors.ts, src/constants/config.ts</files>
  <read_first>
    src/types/map.ts
    src/types/ui.ts
    src/constants/colors.ts
    src/constants/config.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/coding-rules/general.md
  </read_first>
  <action>Create strict exported contracts for normalized GeoJSON, reducer state/actions, selected-country IDs, saved maps, storage outcomes, toast/status messages, and export outcomes. Keep all colors keyed only by normalized country ID per D-18, D-25, D-28, D-29, D-34, and D-56. Define named constants for `HISTORY_LIMIT = 50`, `MAX_SAVED_MAPS = 10`, `MAX_MAP_NAME_LENGTH = 100`, `STORAGE_KEY = 'countriesirl_maps'`, `MAP_VIEWBOX_SIZE = 1080`, map extent `[[64, 64], [1016, 1016]]`, `EXPORT_FRAME_SIZE = 540`, `EXPORT_SCALE = 2`, `EXPORT_SIZE = 1080`, default fill `#FFFFFF`, default border `#9CA3AF`, selected border `#111827`, and the exact ten UI-SPEC palette entries. Use `@types/geojson`; do not define recursive coordinates with `any`, invent index IDs, or use display names as state keys.</action>
  <verify>
    <automated>npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Shared contracts compile under strict TypeScript with no `any`.
    - `HISTORY_LIMIT` equals 50 and `MAX_SAVED_MAPS` equals 10.
    - Export constants express 540 × scale 2 = 1080.
    - Palette values exactly match UI-SPEC section 6.
    - All color maps and selection values use normalized country IDs.
  </acceptance_criteria>
  <done>Parallel plans can import one unambiguous set of strict domain contracts and constants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement tested color parsing and normalization</name>
  <files>src/utils/colors.ts, src/utils/colors.test.ts</files>
  <read_first>
    src/utils/colors.ts
    src/utils/colors.test.ts
    src/constants/colors.ts
    src/types/ui.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/coding-rules/general.md
  </read_first>
  <behavior>
    - `#abc`, whitespace-padded `#A1b2C3`, and rgb forms normalize to uppercase six-digit hex.
    - RGB boundary channels 0 and 255 pass.
    - Empty, partial, malformed, alpha, and channels below 0 or above 255 fail with no fallback color.
    - Equality helpers compare normalized ID-keyed color records without mutation.
  </behavior>
  <action>Write failing Vitest cases first, then implement pure color parsing/normalization helpers per D-26, D-33, D-56, and D-57. Return an explicit discriminated result for user input; never silently coerce invalid input to white. Normalize three-digit hex by expansion, six-digit hex by uppercasing, and rgb channels by validated decimal conversion. Keep UI copy outside the parser while exposing enough error state for `ColorPicker` to show the exact approved message.</action>
  <verify>
    <automated>npm run test:run -- src/utils/colors.test.ts &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover valid 3/6-digit hex, case, whitespace, rgb formatting, 0/255, malformed input, and out-of-range input.
    - Valid results are uppercase `#RRGGBB`.
    - Invalid results never return `#FFFFFF` as an implicit fallback.
    - Tests and lint exit 0.
  </acceptance_criteria>
  <done>The color boundary accepts every locked form, rejects every invalid form, and is safe for one-intent reducer commits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text → color state | Arbitrary custom-color input becomes SVG fill data. |
| Shared types → parallel modules | Incorrect contracts would propagate across state, storage, and rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm test toolchain | mitigate | Use only Plan 01-approved Vitest with exact lockfile from Plan 02. |
| T-01-04 | Tampering | `src/utils/colors.ts` | mitigate | Strict parser, numeric range checks, uppercase canonical storage, no HTML/CSS passthrough. |
| T-01-05 | Denial of Service | Oversized/odd color input | mitigate | Fixed accepted grammars and bounded parsing; no dynamic evaluation. |
</threat_model>

<verification>
Run the color test file, strict app TypeScript check, and lint.
</verification>

<success_criteria>
All shared contracts compile without `any`, and supported color inputs normalize deterministically while invalid input is rejected visibly by downstream UI.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-03-SUMMARY.md` when done.
</output>
