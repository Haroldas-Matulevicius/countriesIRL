---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "14"
type: execute
wave: 8
depends_on: ["01-13"]
files_modified: []
autonomous: true
requirements: [F1.1, F1.2, F1.3, F1.4, F1.5, F1.6, F5.1, F5.3, F6.1, F6.2, NFR1, NFR2, NFR4, NFR5, NFR6, NFR7, NFR10, NFR11]
must_haves:
  truths:
    - "The complete lint, unit-test, and production-build gates pass together without this plan changing product or documentation files."
    - "Automated tests cover state/history, colors, GeoJSON determinism/validation, storage/onboarding persistence, and export failure boundaries."
    - "A failed gate stops the phase and is routed to a targeted gap plan rather than patched inside the verification plan."
  artifacts: []
  key_links:
    - from: "package.json"
      to: "Phase 1 quality gate"
      via: "lint, test:run, and build scripts"
      pattern: "test:run"
---

<objective>
Run a verification-only automated production-readiness gate after all functional and styling work is complete.

Purpose: Produce an unbiased phase gate before human UAT; this plan must not conceal failures by editing implementation or documentation.
Output: Green lint/test/build results, or exact failure evidence routed to targeted gap planning.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@package.json
@src/hooks/useMapState.test.ts
@src/utils/colors.test.ts
@src/utils/geojson.test.ts
@src/utils/storage.test.ts
@src/utils/export.test.ts
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute the immutable Phase 1 quality gate</name>
  <files>package.json, src/hooks/useMapState.test.ts, src/utils/colors.test.ts, src/utils/geojson.test.ts, src/utils/storage.test.ts, src/utils/export.test.ts</files>
  <read_first>
    package.json
    src/hooks/useMapState.test.ts
    src/utils/colors.test.ts
    src/utils/geojson.test.ts
    src/utils/storage.test.ts
    src/utils/export.test.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
  </read_first>
  <action>Run `npm run lint`, `npm run test:run`, `node scripts/prepareGeoData.mjs --check`, and `npm run build` from the completed Phase 1 tree. Inspect output for warnings/errors and confirm tests include 50+ history actions, onboarding dismissal persistence, malformed GeoJSON/storage, blocked/quota storage results, invalid color ranges, exact export arithmetic, null blob/rejection cleanup, and all persistence operations per D-53, D-54, D-55, D-56, D-57, and D-58. This is verification-only: do not modify product source, tests, configs, README, or planning authorities; do not weaken lint/compiler rules, skip tests, or change assertions. If any command fails, stop the plan, capture the exact command/output in the summary, and route the defect to `/gsd:plan-phase 1 --gaps` so the owning subsystem receives a targeted gap plan before this gate is rerun.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:run &amp;&amp; node scripts/prepareGeoData.mjs --check &amp;&amp; npm run build &amp;&amp; git diff --exit-code</automated>
  </verify>
  <acceptance_criteria>
    - `files_modified` remains empty and `git diff --exit-code` is clean after the verification task.
    - Lint exits 0 with no suppressed project errors.
    - Every Vitest file passes in non-watch mode.
    - GeoJSON `--check` proves the committed asset matches deterministic generation.
    - Vite production build exits 0.
    - Any failure is reported and routed to targeted gap planning without an inline implementation fix.
  </acceptance_criteria>
  <done>The completed Phase 1 tree passes the immutable automated gate and is ready for blocking browser UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test configuration → quality gate | Skipped, weakened, or self-repaired checks could hide defects. |
| Verification agent → implementation tree | The verifier must not become an unreviewed fixer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Dependency/test execution | mitigate | Exact lockfile and package gate; deterministic commands. |
| T-01-36 | Repudiation | Automated acceptance | mitigate | One explicit full command, clean diff assertion, and exact failure capture. |
| T-01-46 | Tampering | Verification-only plan edits | mitigate | Empty files_modified, no fix permission, and mandatory gap-plan routing on failure. |
</threat_model>

<verification>
`npm run lint && npm run test:run && node scripts/prepareGeoData.mjs --check && npm run build` exits 0 and leaves `git diff --exit-code` clean.
</verification>

<success_criteria>
All automated gates pass on the already-completed tree, or execution stops with exact evidence for targeted gap planning; this plan never edits product or documentation files.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-14-SUMMARY.md` when done.
</output>
