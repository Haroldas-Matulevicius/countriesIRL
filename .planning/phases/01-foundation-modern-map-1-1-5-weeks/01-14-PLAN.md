---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "14"
type: execute
wave: 7
depends_on: ["01-12", "01-13"]
files_modified:
  - README.md
autonomous: true
requirements: [F1.1, F1.2, F1.3, F1.4, F1.5, F1.6, F5.1, F5.3, F6.1, F6.2, NFR1, NFR2, NFR4, NFR5, NFR6, NFR7, NFR10, NFR11]
must_haves:
  truths:
    - "The complete lint, unit-test, and production-build gates pass together."
    - "Automated tests cover state/history, colors, GeoJSON, storage, and export failure boundaries."
    - "README documents only implemented Phase 1 behavior, commands, data provenance, browser-local persistence, and deployment workflow."
  artifacts:
    - path: "README.md"
      provides: "Accurate Phase 1 creator/developer usage and provenance documentation"
      contains: "CountriesIRL Map Generator"
  key_links:
    - from: "README.md"
      to: "public/data/README.md"
      via: "Natural Earth provenance reference"
      pattern: "Natural Earth"
    - from: "package.json"
      to: "quality gate"
      via: "lint, test:run, build scripts"
      pattern: "test:run"
---

<objective>
Document the implemented Phase 1 product and run the complete automated production-readiness gate.

Purpose: Catch cross-subsystem failures before human UAT and ensure the repository tells creators/developers exactly what actually ships.
Output: Updated README and green lint/test/build verification.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@package.json
@public/data/README.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/coding-rules/general.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the shipped creator workflow and local development/deployment commands</name>
  <files>README.md</files>
  <read_first>
    README.md
    package.json
    public/data/README.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
  </read_first>
  <action>Update README with the implemented modern Europe creator workflow: select one/many countries, apply presets/custom colors, undo/redo/reset, browser-local save/load/delete, onboarding/help, responsive access, and exact 1080×1080 PNG export. Document `npm install`, `npm run dev`, `npm run lint`, `npm run test:run`, `npm run build`, and `npm run preview`; state Node compatibility from the chosen Vite version. Explain that saves use the current browser/origin, require no login/backend/`.env.local`, and may be unavailable in restricted storage. Link Natural Earth provenance and document that the UI is browser-only. Describe Vercel deployment without adding unimplemented routes or environment variables. Do not advertise historical borders, centering, zoom presets, legends, SVG/batch export, cloud sync, auth, sharing URLs, or true disconnected reload. This implements D-07, D-10, D-17, D-47, D-59, and prepares D-60.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:run &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - README lists every implemented Phase 1 creator action and exact export dimensions.
    - README contains all six actual npm commands and Natural Earth provenance link/reference.
    - README states saves are local and no login/backend/.env.local is required.
    - README contains no deferred Phase 2 feature claims.
    - Full automated gate exits 0.
  </acceptance_criteria>
  <done>Repository documentation accurately describes the shipped Phase 1 product and verified commands.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full Phase 1 automated regression and static-quality gate</name>
  <files>README.md</files>
  <read_first>
    package.json
    src/hooks/useMapState.test.ts
    src/utils/colors.test.ts
    src/utils/geojson.test.ts
    src/utils/storage.test.ts
    src/utils/export.test.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
  </read_first>
  <action>Run `npm run lint`, `npm run test:run`, and `npm run build` from a clean dependency state and inspect output for warnings/errors. Confirm tests include 50+ history actions, malformed GeoJSON/storage, invalid color ranges, exact export arithmetic, null blob/rejection cleanup, and all persistence operations per D-53 through D-58. Do not weaken lint/compiler rules, skip tests, or change assertions merely to make the gate green. If a gate fails, fix the owning implementation and rerun its focused test before repeating the complete command.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:run &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Lint exits 0 with no suppressed project errors.
    - Every Vitest file passes in non-watch mode.
    - Vite production build exits 0.
    - Tests explicitly cover 50+ history actions and exact 1080 output arithmetic.
    - Valid bundled data produces no test warnings treated as failures.
  </acceptance_criteria>
  <done>The complete automated Phase 1 quality gate is green and ready for browser UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → operator actions | Incorrect commands or feature claims can cause unsafe deployment or false acceptance. |
| Test configuration → quality gate | Skipped/weakened checks could hide product defects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Dependency/test execution | mitigate | Exact lockfile and package gate; clean deterministic commands. |
| T-01-36 | Repudiation | Automated acceptance | mitigate | One explicit full command and focused test coverage for every critical boundary. |
| T-01-37 | Tampering | README claims | mitigate | Document only implemented locked Phase 1 scope; exclude deferred controls. |
</threat_model>

<verification>
`npm run lint && npm run test:run && npm run build` must exit 0 twice: after documentation and as the final regression task.
</verification>

<success_criteria>
All automated gates pass without weakened rules, and README is an accurate operational guide for the complete browser-only Phase 1 product.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-14-SUMMARY.md` when done.
</output>
