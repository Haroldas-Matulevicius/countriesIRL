---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "21"
type: execute
wave: 11
depends_on: ["01-19", "01-20"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [F1.3, F1.4, F5.1, F5.3, NFR4, NFR5, NFR11]
user_setup:
  - service: browserstack
    why: "Run focused regression checks in Chrome 150 and Edge 150 if either exact browser version is not locally available"
    account_setup:
      - "BrowserStack account/trial or local Chrome 150 and Edge 150 installations"
    dashboard_config: []
must_haves:
  truths:
    - "The complete automated lint/test/GeoJSON/build gate remains green after both product fixes."
    - "Chrome 150 and Edge 150 show all preset swatches natively disabled with zero selected countries."
    - "Chrome 150 and Edge 150 complete a real 1080×1080 PNG download without cancellation and show success only after download initiation."
    - "Full browser-matrix availability and Natural Earth POV acceptance remain assigned to the later Plan 01-15 checkpoint."
  artifacts: []
  key_links:
    - from: "Plans 01-19 and 01-20"
      to: "Plan 01-15 rerun"
      via: "focused automated and Chrome/Edge regression gate"
      pattern: "depends_on:.*01-19.*01-20"
---

<objective>
Run the complete automated gate and a blocking focused Chrome 150/Edge 150 regression pass for the two confirmed defects before resuming the full Plan 01-15 matrix.

Purpose: Prove the fixes in the exact affected Chromium browsers without conflating browser availability or Natural Earth presentation with product defects.
Output: Human approval or precise remaining defect evidence routed to another gap plan.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/ColorPicker.tsx
@src/components/ColorPicker.test.tsx
@src/utils/export.ts
@src/utils/export.test.ts
@src/App.tsx
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-15-PLAN.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve automated and Chrome 150/Edge 150 focused regressions</name>
  <files>src/components/ColorPicker.tsx, src/components/ColorPicker.test.tsx, src/utils/export.ts, src/utils/export.test.ts</files>
  <read_first>
    src/components/ColorPicker.tsx
    src/components/ColorPicker.test.tsx
    src/utils/export.ts
    src/utils/export.test.ts
    src/App.tsx
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-15-PLAN.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
  </read_first>
  <action>Run the full automated gate, then test only the two confirmed defects in exact Chrome 150 and Edge 150 using local browsers or BrowserStack. Do not treat unavailable Safari/previous-version matrix cells or pending Natural Earth POV acceptance as failures of these fixes; those remain blocking items in Plan 01-15 after this focused checkpoint.</action>
  <what-built>Plan 01-19 adds explicit native preset disabled semantics and focused tests. Plan 01-20 holds the anchor/object URL through an asynchronous download handoff before success and cleanup.</what-built>
  <how-to-verify>
    1. Start the app with `npm run dev -- --host 127.0.0.1` and open it in Chrome 150.
    2. With zero countries selected, inspect every named preset button and confirm it is natively disabled, cannot receive activation, and matches the disabled custom input and Apply Custom Color button. Select one country and confirm presets become enabled and still apply normally.
    3. In Chrome 150, export a colored map. Confirm the browser download completes rather than showing canceled, the success toast appears only after native download initiation, the file is named `CountriesIRL_<date>.png`, and image properties show exactly 1080×1080 with opaque white map-only content.
    4. Repeat steps 2–3 in Edge 150.
    5. Repeat export twice in each browser and confirm every download completes, no object-URL/DOM error appears in Console, and the Export button returns from busy state after each attempt.
  </how-to-verify>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:run &amp;&amp; node scripts/prepareGeoData.mjs --check &amp;&amp; npm run build</automated>
    <human-check>Complete all five focused checks in exact Chrome 150 and Edge 150 and record each browser version/result.</human-check>
  </verify>
  <acceptance_criteria>
    - Full automated gate exits 0.
    - Chrome 150 and Edge 150 each show ten natively disabled presets at zero selection and enabled presets after a valid selection.
    - Two successive native PNG downloads complete in each browser with no canceled download.
    - Success feedback occurs after initiation, busy state recovers, and no console lifecycle error appears.
    - Each downloaded file remains exactly 1080×1080, opaque white, map-only, and correctly named.
    - The summary records Chrome 150 and Edge 150 outcomes only; full matrix availability and Natural Earth POV remain pending Plan 01-15 items.
  </acceptance_criteria>
  <done>Both confirmed defects are fixed in their affected Chromium environments and the complete automated gate is ready for the full Plan 01-15 rerun.</done>
  <resume-signal>Type `approved` after all Chrome 150 and Edge 150 checks pass, or provide the browser, step, and observed defect.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit/static fixes → real Chromium behavior | Native disabled and download-manager behavior require browser confirmation. |
| Focused regression → full UAT matrix | This checkpoint must not incorrectly close unrelated availability or POV acceptance items. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Full existing build/test graph | mitigate | Re-run exact lockfile-based automated gate before browser approval. |
| T-01-59 | Spoofing | Simulated versus native browser success | mitigate | Require exact Chrome 150/Edge 150 native download and disabled-state observation. |
| T-01-60 | Repudiation | Unrecorded browser outcomes | mitigate | Record exact versions and per-browser results in the summary. |
</threat_model>

<verification>
The full automated gate passes and exact Chrome 150/Edge 150 focused checks are human-approved before Plan 01-15 resumes.
</verification>

<success_criteria>
Preset native disabled state and PNG download completion are proven in the affected browsers, while unrelated full-matrix and Natural Earth approval work remains correctly deferred to Plan 01-15.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-21-SUMMARY.md` when done.
</output>
