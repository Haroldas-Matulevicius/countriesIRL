---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "21"
type: execute
wave: 11
depends_on: ["01-19", "01-20"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [F1.3, F1.4, F5.1, F5.3, NFR4, NFR5, NFR11]
user_setup:
  - service: browserstack
    why: "Run focused regression checks in exact Chrome 150 and Edge 150 when either version is not installed locally, and expose the local Vite server to the remote browser session"
    account_setup:
      - "BrowserStack account/trial or exact local Chrome 150 and Edge 150 installations"
    dashboard_config:
      - task: "Enable BrowserStack Local desktop tooling or an explicitly approved equivalent tunnel when using remote browsers"
        location: "Local machine and BrowserStack session settings; tooling only, never a product dependency"
must_haves:
  truths:
    - "The complete automated lint/test/GeoJSON/build gate remains green after both product fixes."
    - "Each accepted browser route is preflighted: exact local browser access or a remote BrowserStack Local/equivalent tunnel that successfully loads the local Vite URL."
    - "Chrome 150 and Edge 150 show all preset swatches natively disabled with zero selected countries."
    - "Chrome 150 and Edge 150 complete a real 1080×1080 PNG download without cancellation and show success only after download initiation."
    - "Full browser-matrix availability and Natural Earth POV acceptance remain assigned to the later Plan 01-15 checkpoint."
  artifacts: []
  key_links:
    - from: "Plans 01-19 and 01-20"
      to: "Plan 01-15 rerun"
      via: "focused automated and Chrome/Edge regression gate"
      pattern: "depends_on:.*01-19.*01-20"
    - from: "local Vite server"
      to: "exact Chrome 150 and Edge 150 sessions"
      via: "local-direct route or BrowserStack Local/equivalent approved tunnel preflight"
      pattern: "127.0.0.1:5173|localhost:5173"
---

<objective>
Run the complete automated gate and a blocking focused Chrome 150/Edge 150 regression pass for the two confirmed defects, with an executable local-or-tunneled browser preflight before acceptance.

Purpose: Prove the fixes in the exact affected Chromium browsers without conflating browser availability or Natural Earth presentation with product defects.
Output: Recorded browser route/version preflight plus human approval, or precise remaining defect evidence routed to another gap plan.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/ColorPicker.tsx
@src/components/ColorPicker.test.tsx
@src/utils/export.ts
@src/utils/export.test.ts
@src/App.tsx
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-15-PLAN.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Preflight and approve Chrome 150/Edge 150 focused regressions</name>
  <files>src/components/ColorPicker.tsx, src/components/ColorPicker.test.tsx, src/utils/export.ts, src/utils/export.test.ts, .planning/coding-rules/export.md</files>
  <read_first>
    src/components/ColorPicker.tsx
    src/components/ColorPicker.test.tsx
    src/utils/export.ts
    src/utils/export.test.ts
    .planning/coding-rules/export.md
    src/App.tsx
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-15-PLAN.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
  </read_first>
  <action>Run the full automated gate, start the local Vite server, and choose an executable route for each exact affected browser before testing: use a locally installed exact version directly, or use BrowserStack Local/an explicitly approved equivalent tunnel for a remote exact version. For a remote route, activate the tunnel outside the repository and prove the remote browser can load the local Vite URL before accepting any regression result. BrowserStack Local or equivalent is test tooling only: do not add it to package.json, source, Vite config, runtime code, or deployed product. Record the route (`local-direct`, `browserstack-local`, or named approved equivalent), exact browser version/build, local URL presented to the browser, and successful preflight result. Then test only the two confirmed defects. Do not treat unavailable Safari/previous-version matrix cells or pending Natural Earth POV acceptance as failures of these fixes; those remain blocking items in Plan 01-15 after this focused checkpoint.</action>
  <what-built>Plan 01-19 adds explicit native preset disabled semantics and focused tests. Plan 01-20 holds a connected anchor/object URL through a bounded asynchronous download handoff before success and finally cleanup, and records the durable export rule.</what-built>
  <how-to-verify>
    1. Run `npm run dev -- --host 127.0.0.1` and keep the Vite server running. For each exact browser, choose either a locally installed Chrome 150/Edge 150 route or a BrowserStack Local/approved equivalent tunnel route. If remote, activate the tunnel, open the local Vite URL from the remote session, and confirm the exact `CountriesIRL Map Generator` title and rendered map before continuing. Record route, local URL, tunnel name when used, and exact browser version/build.
    2. In preflighted Chrome 150, with zero countries selected, inspect every named preset button and confirm it is natively disabled, cannot receive activation, and matches the disabled custom input and Apply Custom Color button. Select one country and confirm presets become enabled and still apply normally.
    3. In preflighted Chrome 150, export a colored map. Confirm the browser download completes rather than showing canceled, the success toast appears only after native download initiation, the file is named `CountriesIRL_<date>.png`, and image properties show exactly 1080×1080 with opaque white map-only content.
    4. Repeat steps 2–3 in the preflighted exact Edge 150 session and record its route and exact version/build separately.
    5. Repeat export twice in each browser and confirm every download completes, no object-URL/DOM error appears in Console, and the Export button returns from busy state after each attempt.
  </how-to-verify>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:run &amp;&amp; node scripts/prepareGeoData.mjs --check &amp;&amp; npm run build</automated>
    <human-check>Preflight exact Chrome 150 and Edge 150 through local-direct or a proven BrowserStack Local/approved tunnel route, then complete all five focused checks and record route plus exact version/build for each.</human-check>
  </verify>
  <acceptance_criteria>
    - Full automated gate exits 0.
    - Each exact browser has a recorded route, exact version/build, and successful load of the local Vite URL before regression acceptance.
    - Any remote route uses BrowserStack Local or a named explicitly approved equivalent tunnel as external test tooling only, with no repository or product dependency change.
    - Chrome 150 and Edge 150 each show ten natively disabled presets at zero selection and enabled presets after a valid selection.
    - Two successive native PNG downloads complete in each browser with no canceled download.
    - Success feedback occurs after initiation, busy state recovers, and no console lifecycle error appears.
    - Each downloaded file remains exactly 1080×1080, opaque white, map-only, and correctly named.
    - The summary records Chrome 150 and Edge 150 route/version outcomes only; full matrix availability and Natural Earth POV remain pending Plan 01-15 items.
  </acceptance_criteria>
  <done>Both confirmed defects are fixed through proven exact-browser routes, and the complete automated gate is ready for the full Plan 01-15 rerun.</done>
  <resume-signal>Type `approved` after both exact-browser preflights and all focused checks pass, or provide the browser, route, version, preflight/check step, and observed defect.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit/static fixes → real Chromium behavior | Native disabled and download-manager behavior require browser confirmation. |
| Local Vite origin → remote browser session | A remote result is valid only after an approved tunnel proves the browser can load the intended local app. |
| Focused regression → full UAT matrix | This checkpoint must not incorrectly close unrelated availability or POV acceptance items. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Full existing build/test graph | mitigate | Re-run exact lockfile-based automated gate before browser approval; install no product dependency. |
| T-01-59 | Spoofing | Simulated versus native browser success | mitigate | Require exact Chrome 150/Edge 150 native download and disabled-state observation. |
| T-01-60 | Repudiation | Unrecorded browser outcomes | mitigate | Record exact versions/builds, route, local URL, and per-browser results in the summary. |
| T-01-61 | Spoofing | Remote browser pointed at wrong content | mitigate | Require BrowserStack Local/approved tunnel preflight showing exact title and rendered local map before acceptance. |
</threat_model>

<verification>
The full automated gate passes, each exact Chrome 150/Edge 150 route loads the local Vite app through a proven direct or tunneled preflight, and focused checks are human-approved before Plan 01-15 resumes.
</verification>

<success_criteria>
Preset native disabled state and PNG download completion are proven in exact affected browsers through recorded executable routes, while unrelated full-matrix and Natural Earth approval work remains correctly assigned to Plan 01-15.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-21-SUMMARY.md` when done.
</output>
