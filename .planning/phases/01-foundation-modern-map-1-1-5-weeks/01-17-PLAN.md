---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "17"
type: execute
wave: 14
depends_on: ["01-16"]
files_modified:
  - README.md
autonomous: false
requirements: [F5.1, F6.1, F6.2, NFR5]
must_haves:
  truths:
    - "Automated production prechecks prove the root contains the CountriesIRL title and a Vite module entry, and GeoJSON parses as a non-empty FeatureCollection."
    - "A human verifies the exact production origin in a browser, confirms core behavior, and confirms network requests remain same-origin with no runtime third-party dependency."
    - "README documents the verified URL, implemented workflow, commands, provenance, local persistence, and resolved offline boundary without deferred-feature claims."
  artifacts:
    - path: "README.md"
      provides: "Verified production URL and accurate Phase 1 creator/developer documentation"
      contains: "https://"
  key_links:
    - from: "01-16-SUMMARY.md"
      to: "production root and /data/europe-modern.geojson"
      via: "exact captured HTTPS URL"
      pattern: "https://"
    - from: "README.md"
      to: "public/data/README.md"
      via: "Natural Earth provenance reference"
      pattern: "Natural Earth"
---

<objective>
Block on explicit human production verification after strong automated root/data prechecks, then publish accurate final Phase 1 documentation.

Purpose: Ensure the deployed origin, Vite entry, bundled data, browser behavior, and network boundary are genuinely verified before README advertises the URL.
Output: Human-approved production site followed by README containing the exact live URL and only shipped Phase 1 behavior.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md
@README.md
@package.json
@public/data/README.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the production root, bundled data, browser flow, and network boundary</name>
  <files>.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md, package.json, public/data/README.md</files>
  <read_first>
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md
    package.json
    public/data/README.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
  </read_first>
  <action>Extract the exact HTTPS production URL from `01-16-SUMMARY.md`. Run the automated root and data prechecks before asking for approval. The root HTML must contain the exact title `CountriesIRL Map Generator` and a Vite script element with `type="module"` and a non-empty `src` attribute. `/data/europe-modern.geojson` must parse as JSON with `type === 'FeatureCollection'` and a non-empty `features` array. Then require explicit browser/network human verification; do not update README until approval.</action>
  <what-built>Plan 01-16 deployed the approved static build exactly once and recorded its URL. This checkpoint validates the served document, generated Vite entry, bundled map data, production browser behavior, and same-origin network boundary.</what-built>
  <how-to-verify>
    1. Open the exact production URL from `01-16-SUMMARY.md` in a browser with DevTools Network and Console open.
    2. Confirm the page title is `CountriesIRL Map Generator`, the modern Europe map renders, and there are no console errors or warnings from valid data.
    3. Select and color one country, bulk-select and color several countries, use undo/redo/reset, open the saved-map modal, save/load a map on the production origin, and activate Export PNG.
    4. Inspect Network after load and during the core flow. Confirm required application/map/font/image requests remain on the production origin and there is no required runtime request to Natural Earth or another third party.
    5. Disable network after the app is loaded and confirm color, local save/load, and export continue. Do not reload while disconnected; fresh disconnected reload is not required.
  </how-to-verify>
  <verify>
    <automated>PRODUCTION_URL=$(python -c "import re,pathlib; s=pathlib.Path('.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md').read_text(); print(re.search(r'https://[^\\s)]+', s).group(0))") &amp;&amp; ROOT_HTML=$(curl -fsS "$PRODUCTION_URL") &amp;&amp; printf '%s' "$ROOT_HTML" | python -c "import re,sys; s=sys.stdin.read(); assert '&lt;title&gt;CountriesIRL Map Generator&lt;/title&gt;' in s; assert re.search(r'&lt;script[^&gt;]*type=[\"\\\']module[\"\\\'][^&gt;]*src=[\"\\\'][^\"\\\']+[\"\\\']', s)" &amp;&amp; curl -fsS "$PRODUCTION_URL/data/europe-modern.geojson" | python -c "import json,sys; d=json.load(sys.stdin); assert d.get('type') == 'FeatureCollection'; assert isinstance(d.get('features'), list) and len(d['features']) &gt; 0"</automated>
    <human-check>Confirm the explicit production browser flow and same-origin Network inspection in all five steps.</human-check>
  </verify>
  <acceptance_criteria>
    - Automated precheck finds the exact title and a non-empty module-script `src` in production root HTML.
    - Production GeoJSON parses as a non-empty FeatureCollection.
    - Human confirms map, single/bulk color, history/reset, persistence, and export controls work on the deployed origin.
    - Human confirms no required runtime third-party requests.
    - Already-loaded offline editing/save/export works; fresh disconnected reload remains excluded.
  </acceptance_criteria>
  <done>The exact production deployment passes both strong automated content checks and explicit browser/network human verification.</done>
  <resume-signal>Type `approved` only after all five production browser/network steps pass, or provide the failed precheck/step and observed response.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Publish the approved production URL and shipped Phase 1 contract in README</name>
  <files>README.md</files>
  <read_first>
    README.md
    package.json
    public/data/README.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
  </read_first>
  <action>Only after Task 1 approval, follow G-12 in `01-PATTERNS.md` and D-59 to update README with the exact approved production URL and implemented workflow: one/many country selection, preset/custom colors, undo/redo/reset, browser-local save/load/delete, persisted onboarding/help, responsive access, and exact 1080×1080 PNG export. Document `npm install`, `npm run dev`, `npm run lint`, `npm run test:run`, `npm run build`, and `npm run preview`; state compatible Node requirements. Link Natural Earth provenance/default POV and explain saves remain local to the browser/origin. State the resolved offline boundary exactly: all runtime assets are bundled/same-origin, no runtime third-party requests, and an already-loaded session continues working offline; fresh disconnected reload is not required and no service worker is included. State that no login/backend/`.env.local` is required. Do not advertise deferred features. Run the final full gate after the README edit.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:run &amp;&amp; node scripts/prepareGeoData.mjs --check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - README contains the exact human-approved HTTPS URL from Plan 01-16.
    - README lists every shipped Phase 1 creator action, exact export dimensions, and all six npm commands.
    - README links Natural Earth provenance and documents local persistence/default POV.
    - Offline text excludes fresh disconnected reload/service-worker support while promising bundled same-origin/no-runtime-third-party behavior.
    - README contains no deferred Phase 2 feature claim.
    - Final lint, tests, deterministic GeoJSON check, and build exit 0.
  </acceptance_criteria>
  <done>Phase 1 is publicly verified and accurately documented according to G-12 and every resolved scope boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 01-16 summary → production verification | A captured URL drives automated and human external checks. |
| Public URL → browser users | The static app and bundled public data are internet-accessible. |
| README → operator/user expectations | Incorrect claims can create false offline, security, or feature expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Final build/test dependencies | mitigate | Exact lockfile and completed package gate. |
| T-01-47 | Spoofing | Production URL/content | mitigate | Extract URL from Plan 01-16 summary, assert title/module entry/GeoJSON, then human browser verification. |
| T-01-48 | Information Disclosure | Production network/configuration | mitigate | Human Network inspection; no secrets/env vars/backend/runtime third-party dependency. |
| T-01-49 | Repudiation | README scope/offline claims | mitigate | README waits for checkpoint approval and follows G-12 plus resolved offline text. |
</threat_model>

<verification>
Automated production root/data checks pass, the human approves the browser/network flow, README records the exact approved URL/scope, and the final local gate is green.
</verification>

<success_criteria>
The live Vercel site is explicitly human-verified after strong content prechecks, and README accurately documents production, provenance, persistence, offline behavior, commands, and shipped scope.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-17-SUMMARY.md` when done.
</output>
