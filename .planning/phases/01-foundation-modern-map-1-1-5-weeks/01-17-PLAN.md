---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "17"
type: execute
wave: 11
depends_on: ["01-16"]
files_modified:
  - README.md
autonomous: true
requirements: [F5.1, F6.1, F6.2, NFR5]
must_haves:
  truths:
    - "The production root and bundled GeoJSON respond successfully from the exact URL captured by Plan 01-16."
    - "Production exposes the approved core map/color/save/export interface without runtime third-party requests or backend/environment requirements."
    - "README documents the verified URL, implemented creator workflow, commands, provenance, local persistence, and resolved offline boundary without deferred-feature claims."
  artifacts:
    - path: "README.md"
      provides: "Verified production URL and accurate Phase 1 creator/developer documentation"
      contains: "https://"
  key_links:
    - from: "01-16-SUMMARY.md"
      to: "production root and /data/europe-modern.geojson"
      via: "exact captured HTTPS URL"
      pattern: "https://"
    - from: "README.md"
      to: "public/data/README.md"
      via: "Natural Earth provenance reference"
      pattern: "Natural Earth"
---

<objective>
Verify the deployed production site and publish accurate final Phase 1 documentation.

Purpose: Separate deployment authorization/execution from production acceptance and README changes, following G-12 documentation/deployment hygiene.
Output: Verified production responses and README containing the exact live URL and only shipped Phase 1 behavior.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md
@README.md
@package.json
@public/data/README.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the exact production URL and same-origin bundled assets</name>
  <files>.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md, package.json, public/data/README.md</files>
  <read_first>
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md
    package.json
    public/data/README.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
  </read_first>
  <action>Extract the exact HTTPS production URL from `01-16-SUMMARY.md`. Request the root document and `${PRODUCTION_URL}/data/europe-modern.geojson`; require successful responses, the CountriesIRL title/root entry, and a usable FeatureCollection asset. Confirm production needs no `.env.local`, API, server function, service worker, or runtime third-party request. Open the URL for a concise smoke of map load, one single selection/color, one bulk selection/color, save modal open/list, and Export PNG activation; full browser compatibility/performance already passed Plan 01-15.</action>
  <verify>
    <automated>PRODUCTION_URL=$(python -c "import re,pathlib; s=pathlib.Path('.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md').read_text(); print(re.search(r'https://[^\\s)]+', s).group(0))") &amp;&amp; curl -fsS "$PRODUCTION_URL" &gt; /dev/null &amp;&amp; curl -fsS "$PRODUCTION_URL/data/europe-modern.geojson" &gt; /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - The summary contains one exact HTTPS production URL.
    - Root and bundled GeoJSON requests return success.
    - Production loads the modern map and exposes core color/save/export controls.
    - Network inspection shows no required runtime third-party map/content request.
    - No service worker or disconnected-reload claim is introduced.
  </acceptance_criteria>
  <done>The deployed origin serves the approved browser-only application and bundled map asset successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the production URL and shipped Phase 1 contract in README</name>
  <files>README.md</files>
  <read_first>
    README.md
    package.json
    public/data/README.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
  </read_first>
  <action>Following G-12 in `01-PATTERNS.md` and D-59, update README with the exact verified production URL and the implemented creator workflow: one/many country selection, preset/custom colors, undo/redo/reset, browser-local save/load/delete, persisted onboarding/help, responsive access, and exact 1080×1080 PNG export. Document `npm install`, `npm run dev`, `npm run lint`, `npm run test:run`, `npm run build`, and `npm run preview`; state compatible Node requirements. Link Natural Earth provenance/default POV and explain saves remain local to the current browser/origin. State the resolved offline boundary exactly: all runtime assets are bundled/same-origin, no runtime third-party requests, and an already-loaded session continues working offline; fresh disconnected reload is not required and no service worker is included. State that no login/backend/`.env.local` is required. Do not advertise historical borders, centering, zoom presets, legends, SVG/batch export, cloud sync, auth, sharing URLs, analytics, or other deferred features. Run the final full gate after the README edit.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:run &amp;&amp; node scripts/prepareGeoData.mjs --check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - README contains the exact reachable HTTPS URL from Plan 01-16.
    - README lists every shipped Phase 1 creator action, exact export dimensions, and all six npm commands.
    - README links Natural Earth provenance and documents local persistence/default POV.
    - Offline text exactly excludes fresh disconnected reload and service-worker support while promising bundled same-origin/no-runtime-third-party behavior.
    - README contains no deferred Phase 2 feature claim.
    - Final lint, tests, deterministic GeoJSON check, and build exit 0.
  </acceptance_criteria>
  <done>Phase 1 is publicly reachable and accurately documented according to G-12 and every resolved scope boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 01-16 summary → production verification | A captured URL drives external requests and documentation. |
| Public URL → browser users | The static app and bundled public data are internet-accessible. |
| README → operator/user expectations | Incorrect claims can create false offline, security, or feature expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Final build/test dependencies | mitigate | Exact lockfile and completed package gate. |
| T-01-47 | Spoofing | Production URL | mitigate | Extract only the URL from the signed-off Plan 01-16 summary and verify root/data responses. |
| T-01-48 | Information Disclosure | Production configuration | mitigate | No secrets/env vars/backend; static public assets only. |
| T-01-49 | Repudiation | README scope/offline claims | mitigate | Follow G-12, cite verified URL/provenance, and state the resolved offline boundary exactly. |
</threat_model>

<verification>
Production root/data requests succeed, the concise production smoke passes, README records the exact URL and resolved scope, and the final full local gate is green.
</verification>

<success_criteria>
The live Vercel site serves the approved static application and README accurately documents production, provenance, local persistence, offline behavior, commands, and shipped scope.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-17-SUMMARY.md` when done.
</output>
