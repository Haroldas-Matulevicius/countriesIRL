---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "15"
type: execute
wave: 13
depends_on: ["01-22"]
files_modified:
  - .planning/ui-reviews/01-15-production-preview-rerun/acceptance-evidence.json
  - .planning/ui-reviews/01-15-production-preview-rerun/clean-gate.log
autonomous: false
gap_closure: true
requirements: [F1.1, F1.2, F1.3, F1.4, F1.5, F1.6, F5.1, F5.3, F6.1, F6.2, NFR1, NFR2, NFR4, NFR5, NFR6, NFR7, NFR10, NFR11]
must_haves:
  truths:
    - "An isolated clean worktree of the exact recorded commit passes npm ci, lint, the full unfiltered test suite, deterministic data validation, strict TypeScript, and one production build before browser evidence is generated."
    - "No executable browser harness is retained inside the repository; only immutable non-executable JSON/log evidence is copied into the planned evidence directory."
    - "Chrome 150 and Edge 150 each contribute five controlled cold-cache and five controlled warm-cache production-preview map-ready samples, and every raw sample is under 500ms."
    - "The immutable evidence contains exactly 20 map-ready and 60 interaction samples plus exact browser versions/builds, commit, cache/transfer/service-worker fields, launch/bootstrap fields, path counts, and per-browser pass/fail cells."
    - "The metric is recorded as map-data-start through painted interactive 57-path SVG; browser launch and pre-data bootstrap remain separately reported."
    - "A human reviews the immutable raw evidence and completes the remaining current-code UAT before approval; no browser cell can pass while any raw automated check fails."
    - "The complete UAT passes in installed Chrome 150 and Edge 150, while Firefox, Safari, and previous-version certification remains explicitly unverified/deferred."
  artifacts:
    - path: ".planning/ui-reviews/01-15-production-preview-rerun/acceptance-evidence.json"
      provides: "Immutable machine-readable clean-gate, browser, cache, timing, path-integrity, and pass/fail evidence tied to one exact commit"
      contains: "verifiedCommit"
    - path: ".planning/ui-reviews/01-15-production-preview-rerun/clean-gate.log"
      provides: "Non-executable authoritative npm ci/lint/test/data/type/build transcript from the isolated clean worktree"
      contains: "npm run test:run"
  key_links:
    - from: "01-22-SUMMARY.md"
      to: "detached clean worktree"
      via: "exact final implementation commit used for gate, build, preview, and browser evidence"
      pattern: "[0-9a-f]{40}"
    - from: "clean detached worktree"
      to: "npm run preview -- --host 127.0.0.1 --port 4173 --strictPort"
      via: "the one dist build produced by the authoritative gate"
      pattern: "127.0.0.1:4173"
    - from: "temporary out-of-repo CDP harness"
      to: ".planning/ui-reviews/01-15-production-preview-rerun/acceptance-evidence.json"
      via: "validated atomic JSON copy after browser sessions finish"
      pattern: "acceptance-evidence\.json"
    - from: "countriesirl-map-load-start"
      to: "countriesirl-map-ready"
      via: "map data fetch/parse/normalize, centered projection, final 57-path D3 join, and double-requestAnimationFrame painted completion"
      pattern: "countriesirl-(map-load-start|map-ready)"
---

<objective>
Generate authoritative, immutable production-preview evidence automatically from an isolated clean checkout of the exact Plan 01-22 commit, then block for human review of that artifact and the remaining current-code UAT.

Purpose: Close the map-ready gap without weakening the threshold, contaminating `eslint .` with nested evidence tooling, rebuilding between gate and benchmark, or asking the human to manufacture machine-verifiable timing evidence.
Output: Two non-executable evidence artifacts, a complete raw Chrome 150/Edge 150 acceptance record, and explicit human approval or a precise failed browser/check record.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-21-SUMMARY.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-22-SUMMARY.md
@.planning/debug/phase1-map-ready-performance.md
@.planning/ui-reviews/01-15-current-head-20260722/normalized-acceptance-record.json
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
@vitest.config.ts
@src/vitestScope.test.ts
@src/utils/mapProjection.ts
@src/utils/mapProjection.test.ts
@src/components/MapCanvas.tsx
@src/components/ColorPicker.tsx
@src/providers/MapStateProvider.tsx
@src/components/SaveLoad.tsx
@src/App.tsx
@src/components/Tooltip.tsx
@public/data/README.md

<interfaces>
The generated `acceptance-evidence.json` must contain:
- `schemaVersion`, `plan`, `verifiedCommit`, `generatedAt`, `previewUrl`, and `evidenceSha256` or a summary-recorded SHA-256 computed after final write.
- `gate`: exact commit, ordered commands, exit statuses, `pass`, and `logPath` for npm ci, lint, full unfiltered tests, deterministic data, strict TypeScript, and the single build.
- `runtime`: external clean-worktree path, preview process identity, preview start time, and cleanup ownership for Task 2; no repository-relative executable path.
- `browsers`: exactly Chrome 150 and Edge 150 records with executable, product/version/build, user agent, route, process-launch/debugger timings, bootstrap timings, ten map-ready samples, thirty interaction samples, integrity checks, and `pass`.
- Each map-ready sample: series/index, raw duration, threshold result, pathCount, uniquePathCount, GeoJSON URL/status, transferSize, encodedBodySize, decodedBodySize, resourceDuration, deliveryType or null, fromDiskCache, fromServiceWorker, serviceWorkerResponseSource or null, serviceWorkerRegistrationCount, controllerPresent, cacheControl or null, contentEncoding or null, encodedDataLength or null.
- `deferredBrowsers`, `dataDecision`, `checks`, failed-check names, and `overallPass` derived from raw fields rather than manually asserted.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate immutable clean-checkout production evidence</name>
  <files>.planning/ui-reviews/01-15-production-preview-rerun/acceptance-evidence.json, .planning/ui-reviews/01-15-production-preview-rerun/clean-gate.log</files>
  <action>Read the exact verified implementation commit from `01-22-SUMMARY.md` and confirm it equals the commit selected for this run. Refuse to reuse or overwrite an existing final evidence JSON/log. Create a detached temporary worktree outside the repository evidence tree at that exact commit. Inside the clean worktree run `npm ci`, `npm run lint`, the full unfiltered `npm run test:run`, `node scripts/prepareGeoData.mjs --check`, `npm exec tsc -- -p tsconfig.app.json --noEmit`, and exactly one `npm run build`, capturing the complete transcript to a temporary log. Do not run the authoritative gate in the main checkout because existing nested executable evidence files contaminate repository-wide `eslint .`. Stop before browser measurement if any clean-gate command fails. Serve the already-built `dist` from that same clean worktree with `npm run preview -- --host 127.0.0.1 --port 4173 --strictPort`; do not rebuild and do not use `npm run dev`.

Create the CDP evidence harness only in an OS temporary directory outside every Git worktree. Use built-in Node/browser protocol facilities or already-approved dependencies without adding packages. Launch fresh-profile installed Chrome 150 and Edge 150 sessions. Record process start, debugger readiness, first navigation/title/57-path readiness, and pre-data bootstrap separately. For each browser collect five cold samples by clearing/disabling cache before each counted reload and five warm samples after one uncounted warm-up with normal cache behavior. Each sample reads the current document's `countriesirl-map-ready`, asserts exactly 57 unique non-empty paths, and captures every ResourceTiming/CDP cache, transfer, and service-worker field defined in the interfaces block. Define map-ready as `countriesirl-map-load-start` through painted interactive SVG and never add browser launch/bootstrap to it. Collect exactly ten effective color, ten undo, and ten redo samples per browser; active-color no-ops create no sample. Every raw map-ready value must be compared to 500ms and every raw interaction value to 100ms without averaging, percentile substitution, outlier removal, or rerun replacement.

Automate every existing objectively scriptable current-code check in the same sessions: exact version/title/path preflight, source-scope result from the clean gate, active White/Red no-op state, 55-edit/100+ interaction integrity, persistence success and failure simulations, native PNG completion and exact image analysis, responsive path/workspace counts, keyboard/accessibility state, offline same-origin behavior, console/network failures, and unchanged Natural Earth/region scope. Derive each browser's pass/fail cell and `overallPass` from raw assertions. Write the complete JSON to a temporary file, validate its schema/counts, then atomically copy only the final non-executable JSON and clean-gate log to `.planning/ui-reviews/01-15-production-preview-rerun/`. Remove the temporary executable harness and browser profiles after the final JSON is validated, but keep the clean worktree and its production-preview process available outside the repository through Task 2 so the human can inspect the identical commit/build without rebuilding. Record the external clean-worktree path and preview process identity in the evidence runtime section. If the preview process stops before Task 2, restart only `npm run preview` from the retained clean worktree; never rebuild. Do not copy `.mjs`, `.js`, profiles, cache files, screenshots, node_modules, dist, or other executable/tooling artifacts into the repository. Record the final JSON SHA-256 for Task 2 and `01-15-SUMMARY.md`; Task 2 owns final preview/worktree cleanup after approval or rejection.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const p='.planning/ui-reviews/01-15-production-preview-rerun/acceptance-evidence.json'; const e=JSON.parse(fs.readFileSync(p,'utf8')); const ok=e.gate?.pass===true &amp;&amp; /^[0-9a-f]{40}$/.test(e.verifiedCommit) &amp;&amp; e.browsers?.length===2 &amp;&amp; e.browsers.every(b=&gt;b.mapReadySamples?.length===10 &amp;&amp; b.interactionTimings?.color?.length===10 &amp;&amp; b.interactionTimings?.undo?.length===10 &amp;&amp; b.interactionTimings?.redo?.length===10 &amp;&amp; b.mapReadySamples.every(s=&gt;s.pathCount===57 &amp;&amp; s.uniquePathCount===57 &amp;&amp; 'fromServiceWorker' in s &amp;&amp; 'transferSize' in s)); if(!ok) process.exit(1);"</automated>
  </verify>
  <done>The exact commit passes the clean authoritative gate and one build; the out-of-repo harness produces immutable non-executable JSON/log evidence with exactly 20 map-ready and 60 interaction samples, required metadata and path counts, derived browser/overall pass-fail fields, and no executable evidence file retained in the repository.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review raw evidence and approve remaining current-code UAT</name>
  <files>.planning/ui-reviews/01-15-production-preview-rerun/acceptance-evidence.json, .planning/ui-reviews/01-15-production-preview-rerun/clean-gate.log, .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md, public/data/README.md</files>
  <read_first>
    .planning/ui-reviews/01-15-production-preview-rerun/acceptance-evidence.json
    .planning/ui-reviews/01-15-production-preview-rerun/clean-gate.log
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-22-SUMMARY.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
    public/data/README.md
  </read_first>
  <action>Treat the Task 1 JSON and log as read-only inputs. Run the automated `overallPass` and browser-cell assertion before beginning manual review. Verify the artifact SHA-256, exact commit, clean-gate transcript, raw sample counts/thresholds, metadata completeness, and derived failed-check/browser cells; do not regenerate, edit, hand-correct, or replace any evidence value. Complete only the remaining human-observation UAT against the identical commit/build. Reject approval if the automated assertion fails, either browser cell is false, any raw threshold/path/metadata requirement fails, the artifact provenance is inconsistent, or any remaining manual check fails. Record deferred browsers and the Natural Earth decision exactly as specified by D-61 and D-62. After the human returns approval or a rejection, terminate the external preview process and remove the retained temporary clean worktree; do not alter the immutable JSON/log while cleaning up.</action>
  <what-built>Task 1 generated a write-once evidence record from the exact clean-gated production commit, using an out-of-repository harness and the same single `dist` build for Chrome 150 and Edge 150 measurement.</what-built>
  <how-to-verify>
    1. Hash `acceptance-evidence.json`, record the SHA-256, and do not edit or regenerate it during review. Confirm `verifiedCommit` matches Plan 01-22's exact clean-gated commit and `gate.pass` is true with the ordered npm ci/lint/unfiltered-test/data/type/single-build transcript in `clean-gate.log`.
    2. Review both browser records. Each must identify the exact installed product/version/build and local production-preview route, include five cold plus five warm raw map-ready samples, and contain all required cache/transfer/service-worker/path fields. Confirm every one of all 20 raw durations is strictly under 500ms, all samples have 57 unique paths, and no sample was replaced or averaged.
    3. Review all 60 raw effective interaction samples. Confirm ten color, ten undo, and ten redo entries per browser are each under 100ms and active no-op attempts produced no entries. Confirm each browser's raw checks, failed-check list, and `pass` agree, and `overallPass` is derived true only when both browser cells and all automated checks pass.
    4. Review automated evidence for active White/Red semantics, stress/history/path integrity, storage success/failures, completed exact PNGs, responsive/workspace integrity, keyboard/accessibility state, console/network cleanliness, same-origin already-loaded offline behavior, unchanged Natural Earth 5.1.1 presentation, and absence of Phase 1 World/North America implementation.
    5. Complete the remaining human-observation UAT against the still-running or reproducibly restarted preview of the same clean build: five-country flow in under two minutes; onboarding persistence and Show Help; visual hover/selection/focus feedback; custom-color error clarity; 360px tooltip containment; both 1200px modal-focus restoration directions; dark-theme white-map behavior; 200% zoom and reduced motion; keyboard flow and live announcements; visually map-only opaque PNG quality. Do not rebuild or change commit.
    6. Record installed Chrome 150 and Edge 150 as passed only if their immutable raw cells are `pass: true` and every remaining manual check passes. Record Firefox, Safari, and all previous-version cells exactly as `unverified — deferred by user choice per D-61`. Record the unchanged Natural Earth presentation as already approved per D-62 without reopening it.
  </how-to-verify>
  <resume-signal>Type `approved` only when the immutable artifact hash/commit/gate are verified, `overallPass` and both raw browser cells are true, all 20 map-ready and 60 interaction samples pass, and every remaining manual UAT item passes; otherwise provide the artifact field, browser/check, or manual step that failed.</resume-signal>
  <verify>
    <automated>node -e "const e=JSON.parse(require('node:fs').readFileSync('.planning/ui-reviews/01-15-production-preview-rerun/acceptance-evidence.json','utf8')); if(e.overallPass!==true || e.browsers?.length!==2 || !e.browsers.every(b=&gt;b.pass===true &amp;&amp; b.failedChecks?.length===0)) process.exit(1);"</automated>
    <human-check>Review the immutable artifact and clean-gate log, confirm both raw browser cells and all thresholds, then complete the remaining visual/usability/accessibility UAT without rebuilding.</human-check>
  </verify>
  <done>The immutable exact-commit evidence, both raw browser pass cells, all 20 map-ready and 60 interaction samples, every automated current-code check, and every remaining manual UAT check pass; deferred browsers remain unverified and deployment is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main checkout → quality claim | Nested executable evidence tooling contaminates repository-wide lint and cannot be used for the authoritative gate. |
| Exact commit → detached clean worktree | Gate, build, preview, and evidence must all use the same immutable source revision. |
| Temporary executable harness → repository evidence | Executable tooling and browser profiles stay outside Git; only validated JSON/log artifacts cross the boundary. |
| Browser/cache/service worker → benchmark record | Cache and service-worker state must be explicit for every sample. |
| Machine-generated evidence → human approval | Human review may approve only raw passing browser cells and cannot override failed machine evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Dependency/test/build graph | mitigate | Exact lockfile via npm ci and authoritative lint/unfiltered-test/data/type/build gate in a clean detached worktree. |
| T-01-38 | Spoofing | Performance/compatibility claim | mitigate | Immutable JSON contains exact commit/builds, 20 raw map samples, 60 raw interaction samples, metadata, and derived pass/fail cells. |
| T-01-39 | Denial of Service | Geometry, stress, export, and storage paths | mitigate | Depend on Plan 01-22 and preserve automated 55-edit, 100+ interaction, export, and persistence-failure evidence. |
| T-01-40 | Repudiation | Evidence provenance | mitigate | Use one commit/build, write-once artifacts, recorded SHA-256, clean-gate log, and no artifact regeneration during human review. |
| T-01-42 | Tampering | Harness/cache classification | mitigate | Harness exists only in OS temp; cold/warm controls and ResourceTiming/CDP/service-worker facts are retained per sample. |
</threat_model>

<verification>
Task 1 owns all machine-verifiable work: exact-commit clean gate, one build, preview, browser automation, raw sample capture, schema validation, and non-executable artifact creation. Task 2 reviews that immutable record and performs only the remaining human-observation UAT. Approval is impossible unless the automated artifact reports `overallPass: true` and both browser cells pass.
</verification>

<success_criteria>
The exact Plan 01-22 implementation commit passes the isolated authoritative gate and one production build; an out-of-repo harness produces immutable JSON/log evidence with complete raw sample and provenance fields; a human verifies both passing browser cells and completes remaining UAT; no executable evidence tooling is retained under the repository.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-15-SUMMARY.md` with the exact verified commit, evidence paths and SHA-256, clean-gate results, browser versions/cells, all raw threshold ranges, manual UAT result, and explicit deferred-browser labels.
</output>
