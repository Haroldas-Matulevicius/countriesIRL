---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "15"
type: execute
wave: 13
depends_on: ["01-22"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [F1.1, F1.2, F1.3, F1.4, F1.5, F1.6, F5.1, F5.3, F6.1, F6.2, NFR1, NFR2, NFR4, NFR5, NFR6, NFR7, NFR10, NFR11]
must_haves:
  truths:
    - "Default Vitest discovery runs only source tests under src and never collects agent-worktree copies under .claude."
    - "A first-time creator can color at least five countries in under two minutes and onboarding dismissal remains gone after reload while Show Help still reopens it."
    - "Effective-white and colored selections expose exactly one active natively disabled preset, and attempting that active color creates no history, success feedback, or color timing mark."
    - "In both installed release browsers, five controlled cold-cache and five controlled warm-cache production-preview map-ready samples are each under 500ms; ten effective color, undo, and redo samples per operation are each under 100ms."
    - "The map-ready evidence defines the metric as map-data-start through painted interactive 57-path SVG, records transfer/cache metadata per sample, and reports browser launch/bootstrap separately rather than folding it into the product measure."
    - "Persistence preserves valid data while partial-corrupt loads produce warning feedback and a blocked startup storage read immediately produces an accessible alert."
    - "At 360px tooltips clamp or flip at top/right/bottom edges, and closing Saved Maps after a 1200px responsive remount restores focus to the currently mounted control."
    - "The downloaded PNG is exactly 1080×1080, opaque white, map-only, and consistent in light/dark UI themes."
    - "The complete current-code UAT passes locally in the currently installed Chrome 150 and Edge 150; Firefox, Safari, and previous-version certification stays explicitly unverified/deferred, and the approved Natural Earth Europe presentation is recorded without reopening it."
  artifacts: []
  key_links:
    - from: "src/utils/mapProjection.ts"
      to: "src/components/MapCanvas.tsx"
      via: "Plan 01-22 aggregate-bounds projection and one safe final path per keyed country"
      pattern: "createFixedEuropeProjection|createSafeMapPath"
    - from: "vitest.config.ts"
      to: "npm run test:run"
      via: "source-only include and agent-worktree exclusion"
      pattern: "src/\*\*/\*.test|\.claude/\*\*"
    - from: "countriesirl-map-load-start"
      to: "countriesirl-map-ready"
      via: "GeoJSON fetch/parse/normalize, centered projection, 57-path D3 join, and double-requestAnimationFrame painted completion"
      pattern: "countriesirl-(map-load-start|map-ready)"
    - from: "npm run build"
      to: "npm run preview -- --host 127.0.0.1 --port 4173 --strictPort"
      via: "release-mode browser acceptance without React development StrictMode double-effect timing"
      pattern: "127.0.0.1:4173"
    - from: "browser editor"
      to: "downloaded PNG"
      via: "Export PNG end-to-end flow"
      pattern: "1080 × 1080"
---

<objective>
Rerun the blocking full browser acceptance pass after Plan 01-22, using a production build and controlled cold/warm cache measurement in the installed Chrome 150 and Edge 150 while retaining every existing current-code UAT check.

Purpose: Close the only failed Plan 01-15 condition without weakening the locked <500ms per-sample threshold, without measuring React development StrictMode twice, and without misreporting browser launch or cache behavior as product map-render work.
Output: Complete production-preview Chrome 150/Edge 150 evidence and explicit human approval, or a concrete remaining defect list; deferred browsers remain unverified and deployment stays blocked until approval.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-21-SUMMARY.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-22-SUMMARY.md
@.planning/debug/phase1-map-ready-performance.md
@.planning/ui-reviews/01-15-current-head-20260722/normalized-acceptance-record.json
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
@vitest.config.ts
@src/vitestScope.test.ts
@src/utils/mapProjection.ts
@src/utils/mapProjection.test.ts
@src/components/MapCanvas.tsx
@src/components/ColorPicker.tsx
@src/providers/MapStateProvider.tsx
@src/components/SaveLoad.tsx
@src/App.tsx
@src/components/Tooltip.tsx
@public/data/README.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve production-preview current-code UAT in installed Chrome 150 and Edge 150</name>
  <files>.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-21-SUMMARY.md, .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-22-SUMMARY.md, .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md, .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md, vitest.config.ts, src/vitestScope.test.ts, src/utils/mapProjection.ts, src/utils/mapProjection.test.ts, src/components/MapCanvas.tsx, src/components/ColorPicker.tsx, src/providers/MapStateProvider.tsx, src/components/SaveLoad.tsx, src/App.tsx, src/components/Tooltip.tsx, public/data/README.md</files>
  <action>Run the source-scoped automated pre-check, build the exact current commit with `npm run build`, and serve only the built `dist` output with `npm run preview -- --host 127.0.0.1 --port 4173 --strictPort`. Do not use `npm run dev` for map-ready acceptance: the diagnosed Vite development environment runs React StrictMode mount effects twice and is not release-performance evidence. Complete every existing current-code UAT check through local-direct production-preview routes in the currently installed Chrome 150 and Edge 150 per D-53, D-58, and D-61. Preserve all locked behavior: D3/SVG rendering per D-03/D-04, exact 57-path Natural Earth presentation per D-17 through D-24 and D-62, effective-white no-ops per D-23/D-31, storage feedback per D-37/D-38, exact PNG per D-40, 100+ interaction stability per D-55, and accessibility per D-51. Keep every map-ready sample below 500ms; do not relax, average, percentile, discard, or reinterpret the threshold. Record Firefox, Safari, Chrome previous, Edge previous, Firefox previous, and Safari previous as `unverified — deferred by user choice per D-61`, never passed. Do not implement GeoJSON simplification, alter the accepted dataset, add region variants, or lazy-load html2canvas during this rerun; Plan 01-22 is the required blocking product correction.</action>
  <read_first>
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-21-SUMMARY.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-22-SUMMARY.md
    .planning/debug/phase1-map-ready-performance.md
    .planning/ui-reviews/01-15-current-head-20260722/normalized-acceptance-record.json
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
    vitest.config.ts
    src/vitestScope.test.ts
    src/utils/mapProjection.ts
    src/utils/mapProjection.test.ts
    src/components/MapCanvas.tsx
    src/components/ColorPicker.tsx
    src/providers/MapStateProvider.tsx
    src/components/SaveLoad.tsx
    src/App.tsx
    src/components/Tooltip.tsx
    public/data/README.md
  </read_first>
  <what-built>The complete browser-only modern Europe editor plus the Plan 01-22 geometry correction that aggregates finite per-feature bounds once and generates each final safe path once while preserving exact approved geometry and malformed-data safety.</what-built>
  <how-to-verify>
    Preflight. Run `npm run build`, then start `npm run preview -- --host 127.0.0.1 --port 4173 --strictPort`. Open `http://127.0.0.1:4173/` directly in the currently installed Chrome 150 and Edge 150. Confirm the exact `CountriesIRL Map Generator` title, exactly 57 unique non-empty country paths, and record each exact version/build, current Git commit, `local-direct-production-preview` route, URL, preview command, and successful preflight. Do not run a Vite development server or create remote routes. Record each browser's process-launch-to-debugger-ready duration and first-navigation/title/map-ready duration separately; neither value is added to `countriesirl-map-ready`.
    0. Automated test scope. Run `npm run test:run -- --reporter=verbose`. Confirm `src/vitestScope.test.ts` passes, every discovered test file is under `src/`, no `.claude/` path is collected, and the command exits non-watch. Then run the complete automated command in this plan's verify block.
    1. Clear `countriesirl_onboarding_dismissed`, load the app through the production-preview route, confirm onboarding remains until explicit dismissal, use `Start Coloring`, verify map focus, and color at least five countries in under two minutes. Reload, confirm dismissal persists, use `Show Help`, reload again, and confirm the durable dismissal remains effective.
    2. With zero countries selected, confirm all ten presets are natively disabled. Select one uncolored country and confirm White is active/disabled while nine alternatives enable. Clear `countriesirl-color-start`, attempt active White, and confirm no success, history, color change, or timing mark. Apply Red once, confirm Red becomes active/disabled and White enables, then attempt active Red and confirm the same no-op guarantees. Undo to effective white, redo to Red, exercise country-list checkboxes, Select All/Clear, another preset, valid `#RGB`, `#RRGGBB`, and `rgb(...)`, plus invalid/out-of-range input with exact inline feedback and no mutation.
    3. Controlled production map-ready benchmark. Define `countriesirl-map-ready` in the evidence as elapsed time from `countriesirl-map-load-start` immediately before the GeoJSON request through fetch/body read, JSON parse, normalization, React update, centered projection, final 57-path D3 join, and the existing double-requestAnimationFrame painted interactive SVG completion. It excludes browser process launch, CDP connection, navigation response, initial HTML/JS bundle transfer/evaluation before the map-data mark, and the first shell/skeleton commit; report those excluded launch/bootstrap values separately. For each browser use a fresh profile. Record five cold-cache samples by clearing browser cache before each counted navigation/reload, disabling cache or using `Page.reload({ignoreCache:true})`, and waiting for one map-ready measure plus exactly 57 unique paths. Then re-enable cache, perform one uncounted warm-up load, and record five warm-cache samples with normal cache behavior and `Page.reload({ignoreCache:false})`. Before each counted sample ensure only the current document's measure is read. For every one of the 20 samples record duration, series/index, exact browser build, commit, route, GeoJSON URL/status, `PerformanceResourceTiming.transferSize`, `encodedBodySize`, `decodedBodySize`, resource duration, `deliveryType` when exposed, and CDP response cache facts including `fromDiskCache`, `fromServiceWorker`, response `cache-control`, `content-encoding`, and encoded transfer length when exposed. Every cold and warm sample in both browsers must be strictly under 500ms; do not substitute averages or omit outliers. Record 57 path count/uniqueness for each sample.
    4. Effective interaction timing and stress. Perform ten effective visible color changes and collect ten `countriesirl-color-visible` durations, ten undo operations and `countriesirl-undo-visible`, and ten redo operations and `countriesirl-redo-visible` in each browser. Active-color attempts produce no entry. Every one of the 60 cross-browser interaction samples is under 100ms. Then perform 55 distinct color actions, repeated undo/redo, branch truncation, reset/undo reset, and 100+ rapid interactions; confirm no crash, stale selection, duplicate path, double event, or valid-data console warning/error.
    5. Persistence success. Save two named maps, replace one by exact trimmed name, reload, load each, delete one without confirmation, and confirm load resets undo/redo.
    6. Persistence recovery/failures. Seed a partial-valid saved map and confirm valid colors apply with exact warning-severity copy. Test malformed JSON recovery. Simulate a startup `Storage.getItem` SecurityError and confirm an immediate assertive alert while editing/export remain available. Simulate blocked `setItem` and QuotaExceededError; confirm exact feedback, current map/name preservation, and saved-list visibility. Restore prototypes after each case.
    7. Export colored and all-white maps. Confirm exact 1080×1080 RGBA, opaque white background, map only, neutral borders, current fills, no editor UI, filename `CountriesIRL_<date>.png`, native download completion, and under 3 seconds. Compare Chrome/Edge output integrity and confirm the 57-path geometry remains export-equivalent.
    8. Responsive composition, tooltip edges, modal remount focus, and theme. Verify desktop ≥1200px map→controls DOM/focus order; tablet and 360px actions→map→selection/color→country list; exactly one workspace/map across 1200px with state preserved. At 360px verify keyboard top-edge and pointer right/bottom tooltips flip or clamp within viewport margins. Open Saved Maps, cross 1200px in both directions, close, and confirm focus restores to the currently mounted control. Verify square map, 48px targets, no horizontal page scroll, mobile full-height sheet, and dark chrome with unchanged white map/export.
    9. Accessibility. Operate the map with Tab, arrows, Home/End, Enter/Space, Escape; confirm visible focus, focus tooltip, country-list alternative, live announcements, modal trap/Escape/restore, 200% zoom usability, and reduced-motion behavior.
    10. Blocking browser matrix. Run the complete core flow in installed Chrome 150 and Edge 150 and record both as pass only if all checks, including both production cache series, pass. Record Firefox, Safari, and all previous-version cells as `unverified — deferred by user choice per D-61`.
    11. Offline boundary. In both installed browsers confirm no runtime third-party requests after load and that the already-loaded app continues to color/save/export while offline. Do not require fresh disconnected reload or a service worker.
    12. Data decision record. Confirm the rendered asset and `public/data/README.md` still identify Natural Earth 5.1.1 and the approved Europe/transcontinental inclusion. Do not reopen geopolitical approval. Confirm World and North America variants were not added to Phase 1.
  </how-to-verify>
  <acceptance_criteria>
    - Verbose default Vitest discovery includes only `src/**/*.test.{ts,tsx}`, excludes `.claude/**`, passes the scope guard, and exits non-watch.
    - The exact current commit is built with `npm run build` and served with `npm run preview -- --host 127.0.0.1 --port 4173 --strictPort`; no development-server timing is used for release acceptance.
    - Installed Chrome 150 and Edge 150 each record exact version/build, production-preview route/URL, successful title/57-path preflight, browser launch duration, and first-navigation/bootstrap duration.
    - The evidence states that map-ready measures map-data-start through painted interactive 57-path SVG and excludes/reports browser launch and pre-data bootstrap separately.
    - Each browser records five controlled cold-cache and five controlled warm-cache samples with transfer/cache metadata, and every one of all 20 samples is strictly under 500ms.
    - Ten effective color, ten undo, and ten redo visible samples per browser are each under 100ms; active-color no-ops create no measure.
    - Zero selection disables all ten presets; White/Red active states are natively disabled and active attempts create no color action, history, success feedback, or start mark.
    - 55+ edits and 100+ rapid interactions preserve bounded history and exactly 57 unique stable paths.
    - Successful persistence plus partial-corrupt warning, malformed JSON recovery, blocked startup read, blocked writes, and quota cases pass without current-map loss.
    - PNGs are exactly 1080×1080, opaque, map-only, geometry-equivalent, and under 3 seconds in both themes/browsers.
    - Responsive DOM/focus order, 360px tooltip containment, 1200px modal focus restoration, keyboard accessibility, 200% zoom, and reduced motion pass in both browsers.
    - Already-loaded offline behavior works with no runtime third-party requests; fresh disconnected reload remains excluded.
    - Firefox, Safari, and all previous-version cells are explicitly unverified/deferred, never passed, and do not block this release.
    - Natural Earth 5.1.1 Europe presentation remains unchanged/user-approved, and no World/North America implementation enters Phase 1.
  </acceptance_criteria>
  <resume-signal>Type `approved` only after the production build/preview preflight, all 20 map-ready samples, all 60 interaction samples, every remaining current-code UAT step, and both installed-browser cells pass with complete metadata; otherwise provide the failed browser, cache series/sample or UAT step, exact version, and observed behavior.</resume-signal>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:run &amp;&amp; node scripts/prepareGeoData.mjs --check &amp;&amp; npm run build</automated>
    <human-check>Serve the built app with Vite preview, collect controlled cold/warm production evidence and separate launch/bootstrap values, complete all current-code UAT in installed Chrome 150 and Edge 150, and preserve explicit deferred-browser labels.</human-check>
  </verify>
  <done>The production-preview automated gate, 20 per-sample map-ready thresholds, transfer/cache and separate launch metadata, 60 interaction thresholds, every current-code/deep-review behavior, exact export, stress, persistence failures, responsive/accessibility/offline checks, and both installed release-browser cells pass with no unresolved defect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built `dist` → Vite preview browser session | Release-performance claims must exercise production code rather than Vite development/StrictMode behavior. |
| Browser/network cache → benchmark record | Cache state and transfer metadata must be controlled and reported per sample. |
| Performance marks → acceptance claim | The map-data-start-to-painted-SVG metric must not be conflated with browser launch or hidden bootstrap work. |
| Browser storage failure simulation → application | Mandatory corrupt, unavailable, and quota cases exercise untrusted persistence. |
| Geopolitical dataset → published map | Default Natural Earth POV affects boundary presentation and must remain the approved artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Executed dependency/test graph | mitigate | Use the exact lockfile, source-only Vitest discovery, immutable full gate, and production build before preview. |
| T-01-38 | Spoofing | Performance/compatibility claim | mitigate | Record 20 raw map samples, 60 raw interaction samples, exact builds/commit/routes, cache/transfer metadata, and separate browser launch/bootstrap values; require every sample below its threshold. |
| T-01-39 | Denial of Service | Geometry, rapid interaction, export, and storage failure paths | mitigate | Depend on Plan 01-22 traversal correction and run 55+ edits, 100+ interactions, exact export, and all storage-failure cases. |
| T-01-40 | Repudiation | Metric semantics and dataset scope | mitigate | State the start/end boundary explicitly, retain raw evidence, cite D-53/D-61/D-62, and keep deferred browsers/regions visibly unverified or out of scope. |
| T-01-42 | Tampering | Cold/warm cache classification | mitigate | Use fresh profiles, explicit cache clear/disable for cold, uncounted warm-up plus normal caching for warm, and capture ResourceTiming/CDP cache facts for every sample. |
</threat_model>

<verification>
Human approval is mandatory. No unit test or counterfactual substitutes for a current production build, controlled cold/warm Chrome 150 and Edge 150 measurements with every sample under 500ms, separately reported launch/bootstrap values, complete transfer/cache metadata, and the unchanged full UAT matrix.
</verification>

<success_criteria>
The exact current production build passes all automated and human Phase 1 acceptance in installed Chrome 150 and Edge 150, including every controlled cold/warm map-ready sample below 500ms with truthful metric/cache/launch reporting; deployment Plans 01-16 and 01-17 are then unblocked.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-15-SUMMARY.md` when done.
</output>
