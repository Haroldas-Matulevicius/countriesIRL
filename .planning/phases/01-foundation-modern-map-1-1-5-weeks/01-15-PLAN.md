---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "15"
type: execute
wave: 12
depends_on: ["01-21"]
files_modified: []
autonomous: false
requirements: [F1.1, F1.2, F1.3, F1.4, F1.5, F1.6, F5.1, F5.3, F6.1, F6.2, NFR1, NFR2, NFR4, NFR5, NFR6, NFR7, NFR10, NFR11]
user_setup:
  - service: browserstack
    why: "Run the blocking current-and-previous compatibility matrix when all required browser versions, especially Safari, are not locally available"
    account_setup:
      - "BrowserStack account/trial or equivalent macOS/browser access for Safari current and previous"
    dashboard_config: []
must_haves:
  truths:
    - "A first-time creator can color at least five countries in under two minutes and onboarding dismissal remains gone after reload while Show Help still reopens it."
    - "Five measured initial map samples are each under 500ms, and ten measured color, undo, and redo samples per operation are each under 100ms."
    - "Undo/redo remains correct after 50+ edits, mandatory malformed/blocked/quota storage cases recover, and 100+ rapid interactions do not duplicate SVG nodes or crash."
    - "The downloaded PNG is exactly 1080×1080, opaque white, map-only, and consistent in light/dark UI themes."
    - "Chrome, Firefox, Edge, and Safari current and previous versions all pass the blocking compatibility matrix."
  artifacts: []
  key_links:
    - from: "Performance API measures"
      to: "NFR1/NFR2 thresholds"
      via: "multi-sample browser timing"
      pattern: "countriesirl-(map-ready|color-visible|undo-visible|redo-visible)"
    - from: "browser editor"
      to: "downloaded PNG"
      via: "Export PNG end-to-end flow"
      pattern: "1080 × 1080"
---

<objective>
Perform the blocking human browser acceptance pass for measured performance, compatibility, interaction quality, accessibility, responsive composition, exact export, persistence failures, stress, onboarding persistence, and Natural Earth presentation.

Purpose: Verify every behavior that unit/static checks cannot prove before Vercel authorization and deployment.
Output: Human approval of the complete matrix or a concrete defect list routed to gap planning.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
@public/data/README.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve complete performance, storage, compatibility, browser, and data UAT</name>
  <files>.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md, .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md, public/data/README.md</files>
  <action>Run the automated pre-check and complete every numbered human verification step. Use BrowserStack or equivalent macOS/browser access where local current and previous versions are unavailable. Do not approve deployment while any timing sample, browser matrix cell, mandatory storage failure, responsive DOM order, exact export, accessibility, stress, onboarding persistence, or geopolitical-presentation check is unresolved; route failures to `/gsd:plan-phase 1 --gaps`.</action>
  <read_first>
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
    public/data/README.md
  </read_first>
  <what-built>The complete browser-only modern Europe editor, including one active matchMedia-composed workspace, persisted onboarding dismissal, performance measures, single/bulk coloring, 50-action history, reset, local save/load/delete, exact PNG export, error states, responsive layout, accessibility, dark UI, and fixed white map/export.</what-built>
  <how-to-verify>
    1. Start with `npm run dev -- --host 127.0.0.1`. Clear `countriesirl_onboarding_dismissed`, load the app, confirm exact onboarding copy remains until explicit dismissal, use `Start Coloring`, and verify map focus. Color at least five countries in under two minutes. Reload and confirm help stays dismissed; use `Show Help` to reopen it; reload again and confirm the persisted dismissal remains effective.
    2. Selection/color: click one country, select several with country-list checkboxes, use Select All/Clear, apply a named preset, enter valid `#RGB`, `#RRGGBB`, and `rgb(...)`, and confirm invalid/out-of-range input shows exact inline feedback without changing colors/history.
    3. Performance: clear prior performance entries before each series. Reload and collect five `countriesirl-map-ready` durations; every sample must be under 500ms. Perform ten visible color changes and collect ten `countriesirl-color-visible` durations; perform ten undo operations and collect ten `countriesirl-undo-visible` durations; perform ten redo operations and collect ten `countriesirl-redo-visible` durations. Every one of the 30 interaction samples must be under 100ms. Record sample values in the plan summary.
    4. History/stress: perform at least 55 distinct color actions, undo/redo repeatedly, undo then branch with a new edit, reset and undo reset, then perform 100+ rapid selection/color/history interactions. Confirm no crash, stale selection, duplicate country paths, double event, or console error/warning for valid data.
    5. Persistence success: save two named maps, replace one by exact trimmed name, reload the page, load each map, delete one without confirmation, and confirm load resets undo/redo.
    6. Mandatory persistence failures: set `countriesirl_maps` to malformed JSON before opening the modal and confirm the corrupt-data warning while valid editing continues. Temporarily override `Storage.prototype.setItem` to throw `DOMException('Blocked', 'SecurityError')`, attempt a save/onboarding dismissal, and confirm the blocked-storage copy with no current-map loss. Repeat with `DOMException('Full', 'QuotaExceededError')` and confirm quota copy, preserved map name/current map, and saved-list visibility. Restore the original method after each case.
    7. Export: export colored and all-white maps. Inspect image properties and confirm exactly 1080×1080 pixels, opaque white background, map only, neutral uniform borders, current fills, no selection/focus/tooltip/header/label/toast/legend, filename `CountriesIRL_<date>.png`, and completion under 3 seconds.
    8. Responsive composition/theme: at desktop ≥1200px inspect DOM/focus order as map then controls. At tablet 768–1199px and 360px inspect DOM/focus order as actions, map, selection/color, country list. Resize across 1200px and confirm exactly one workspace/map exists while colors/history/selection persist. Verify square uncropped map, 48px targets, no horizontal page scroll, mobile full-height saved-map sheet, and dark chrome with unchanged white map/export.
    9. Accessibility: operate map with Tab, arrows, Home/End, Enter/Space, Escape; confirm focus-visible rings, tooltip on focus, country-list bulk alternative, live announcements, modal focus trap/Escape/restore, 200% zoom usability, and reduced-motion behavior.
    10. Blocking browser matrix: run the core flow (load map, single selection, bulk selection, preset/custom color, undo/redo/reset, save/reload/load/delete, export, keyboard map, modal focus, no console errors) in Chrome current/previous, Firefox current/previous, Edge current/previous, and Safari current/previous. Use BrowserStack or macOS as needed and record each of the eight cells as pass with exact browser version.
    11. Offline boundary: confirm there are no runtime third-party requests after load and the already-loaded app continues to color/save/export when the network is disabled. Do not test or require a fresh disconnected reload; no service worker is part of Phase 1.
    12. Data acceptance: review rendered modern borders and `public/data/README.md`; approve Natural Earth 5.1.1 default geopolitical POV and documented Europe/transcontinental inclusion.
  </how-to-verify>
  <acceptance_criteria>
    - Five-country flow is under two minutes and onboarding dismissal persists across reload while Show Help reopens guidance.
    - Five map-ready samples are each under 500ms; ten color, ten undo, and ten redo visible samples are each under 100ms and are recorded.
    - 55+ edits and 100+ rapid interactions preserve bounded history and stable SVG behavior.
    - Successful persistence plus malformed JSON, blocked storage, and quota exceeded all pass mandatory checks without current-map loss.
    - PNG is exactly 1080×1080, opaque, map-only, correct in light/dark UI, and under 3 seconds.
    - Desktop/tablet/360px DOM/focus order uses exactly one active workspace and preserves state across the 1200px boundary.
    - All eight Chrome/Firefox/Edge/Safari current/previous matrix cells pass with recorded versions.
    - Already-loaded offline behavior works with no runtime third-party requests; fresh disconnected reload is explicitly not required and no service worker is present.
    - Natural Earth 5.1.1 default POV/inclusion is explicitly approved.
    - D-17, D-40, D-47, D-49, D-50, D-51, D-52, D-53, D-54, and D-55 are human-verified.
  </acceptance_criteria>
  <resume-signal>Type "approved" only after all twelve steps and eight browser cells pass, or provide failed step/matrix cells with measured values and observed behavior.</resume-signal>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:run &amp;&amp; node scripts/prepareGeoData.mjs --check &amp;&amp; npm run build</automated>
    <human-check>Complete all twelve browser/data steps, record performance samples and browser versions, and approve Natural Earth presentation.</human-check>
  </verify>
  <done>All automated gates, measured thresholds, mandatory storage failures, responsive/accessibility checks, eight browser cells, offline boundary, export, and data presentation are approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser implementation → human acceptance | Visual, timing, keyboard, compatibility, and downloaded-file claims require direct observation. |
| Browser storage failure simulation → application | Mandatory malformed/security/quota cases exercise untrusted and unavailable persistence. |
| Geopolitical dataset → published map | Default Natural Earth POV can affect sensitive boundary presentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Executed dependency graph | mitigate | Automated gate passed; human observes runtime and console across eight browser cells. |
| T-01-38 | Spoofing | False size/performance/compatibility claim | mitigate | Record downloaded dimensions, 35 timing samples, exact browser versions, and matrix outcomes. |
| T-01-39 | Denial of Service | Rapid interaction/history/export/storage failures | mitigate | 55+ edit, 100+ interaction, and mandatory storage-failure passes. |
| T-01-40 | Repudiation | Geopolitical POV | mitigate | Explicit approval of documented Natural Earth 5.1.1 default POV/inclusion before deploy. |
</threat_model>

<verification>
Human approval is mandatory; no automated result substitutes for the twelve-step UAT, recorded timing samples, and eight-cell compatibility matrix.
</verification>

<success_criteria>
Every measured browser, responsive, accessibility, persistence, failure, stress, export, offline-boundary, compatibility, and map-presentation check passes with recorded evidence and no unresolved defect.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-15-SUMMARY.md` when done.
</output>
