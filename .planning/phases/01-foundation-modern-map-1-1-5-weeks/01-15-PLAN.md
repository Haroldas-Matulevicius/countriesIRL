---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "15"
type: execute
wave: 12
depends_on: ["01-21"]
files_modified: []
autonomous: false
requirements: [F1.1, F1.2, F1.3, F1.4, F1.5, F1.6, F5.1, F5.3, F6.1, F6.2, NFR1, NFR2, NFR4, NFR5, NFR6, NFR7, NFR10, NFR11]
user_setup:
  - service: browserstack
    why: "Run the blocking current-and-previous compatibility matrix when required exact browser versions, especially Safari, are not locally available, and expose the local Vite server to remote sessions"
    account_setup:
      - "BrowserStack account/trial or equivalent macOS/browser access for Safari current and previous"
    dashboard_config:
      - task: "Enable BrowserStack Local desktop tooling or an explicitly approved equivalent tunnel for every remote browser route"
        location: "Local machine and BrowserStack session settings; tooling only, never a product dependency"
must_haves:
  truths:
    - "Default Vitest discovery runs only source tests under src and never collects agent-worktree copies under .claude."
    - "A first-time creator can color at least five countries in under two minutes and onboarding dismissal remains gone after reload while Show Help still reopens it."
    - "Effective-white and colored selections expose exactly one active natively disabled preset, and attempting that active color creates no history, success feedback, or color timing mark."
    - "Five measured initial map samples are each under 500ms, ten measured effective color, undo, and redo samples per operation are each under 100ms, and 100+ rapid interactions remain stable."
    - "Persistence preserves valid data while partial-corrupt loads produce warning feedback and a blocked startup storage read immediately produces an accessible alert."
    - "At 360px tooltips clamp or flip at top/right/bottom edges, and closing Saved Maps after a 1200px responsive remount restores focus to the currently mounted control."
    - "The downloaded PNG is exactly 1080×1080, opaque white, map-only, and consistent in light/dark UI themes."
    - "Chrome, Firefox, Edge, and Safari current and previous versions all pass the blocking compatibility matrix through recorded executable routes, and Natural Earth POV acceptance remains explicit."
  artifacts: []
  key_links:
    - from: "vitest.config.ts"
      to: "npm run test:run"
      via: "source-only include and agent-worktree exclusion"
      pattern: "src/\*\*/\*.test|\.claude/\*\*"
    - from: "src/components/ColorPicker.tsx"
      to: "src/providers/MapStateProvider.tsx"
      via: "active native-disabled preset and boolean effective-change contract"
      pattern: "disabled=.*isActive|recordColorInteractionIfChanged"
    - from: "src/components/SaveLoad.tsx"
      to: "src/components/ToastRegion.tsx"
      via: "warning-severity feedback for valid-subset loads"
      pattern: "PARTIAL_LOAD_WARNING|severity.*warning"
    - from: "src/App.tsx"
      to: "ToastRegion"
      via: "initial storage-unavailable state becomes an assertive error"
      pattern: "hasInitialStorageError|storageUnavailable"
    - from: "Performance API measures"
      to: "NFR1/NFR2 thresholds"
      via: "multi-sample browser timing"
      pattern: "countriesirl-(map-ready|color-visible|undo-visible|redo-visible)"
    - from: "browser editor"
      to: "downloaded PNG"
      via: "Export PNG end-to-end flow"
      pattern: "1080 × 1080"
    - from: "local Vite server"
      to: "eight exact browser matrix cells"
      via: "local-direct or BrowserStack Local/equivalent approved tunnel preflight"
      pattern: "127.0.0.1:5173|localhost:5173"
---

<objective>
Perform the blocking full human browser acceptance pass for source-scoped test discovery, measured performance, revised active-color semantics, persistence warnings/startup failures, compatibility, interaction quality, accessibility, responsive tooltip/focus behavior, exact export, stress, onboarding persistence, and Natural Earth presentation.

Purpose: Verify every behavior that unit/static checks cannot prove before Vercel authorization and deployment, including the completed deep-review fixes.
Output: Recorded automated scope evidence, browser-route preflights, and human approval of the complete matrix, or a concrete defect list routed to gap planning.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-21-SUMMARY.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
@vitest.config.ts
@src/vitestScope.test.ts
@src/components/ColorPicker.tsx
@src/providers/MapStateProvider.tsx
@src/components/SaveLoad.tsx
@src/App.tsx
@src/components/Tooltip.tsx
@public/data/README.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Preflight and approve complete deep-review, performance, storage, compatibility, browser, and data UAT</name>
  <files>.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-21-SUMMARY.md, .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md, .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md, vitest.config.ts, src/vitestScope.test.ts, src/components/ColorPicker.tsx, src/providers/MapStateProvider.tsx, src/components/SaveLoad.tsx, src/App.tsx, src/components/Tooltip.tsx, public/data/README.md</files>
  <action>Run the source-scoped automated pre-check, start the local Vite server, and establish an executable route for every exact browser version before accepting its matrix result. Use an exact locally installed browser directly, or activate BrowserStack Local/an explicitly approved equivalent tunnel outside the repository for a remote browser. For every remote route, prove that the browser can load the local Vite URL and renders the exact CountriesIRL title/map before running acceptance. Record route, exact version/build, local URL, and tunnel name when used. BrowserStack Local or equivalent is test tooling only: do not add it to package.json, source, Vite config, runtime code, or deployed product. Verify the completed deep-review outcomes without weakening locked decisions: effective white and active-color no-ops per D-23/D-31/D-53, corrupt and unavailable storage feedback per D-37/D-38, tooltip and modal focus behavior per D-48/D-51, exact export per D-40, and the current/previous matrix per D-10. Do not approve deployment while any source-discovery assertion, preflight, timing sample, browser matrix cell, mandatory storage failure, responsive DOM/focus order, tooltip edge, modal focus restoration, exact export, accessibility, stress, onboarding persistence, or geopolitical-presentation check is unresolved; route product failures to `/gsd:plan-phase 1 --gaps` while reporting unavailable browser access separately as a human checkpoint blocker.</action>
  <read_first>
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-21-SUMMARY.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-VALIDATION.md
    vitest.config.ts
    src/vitestScope.test.ts
    src/components/ColorPicker.tsx
    src/providers/MapStateProvider.tsx
    src/components/SaveLoad.tsx
    src/App.tsx
    src/components/Tooltip.tsx
    public/data/README.md
  </read_first>
  <what-built>The complete browser-only modern Europe editor plus the completed deep-review fixes: source-scoped test discovery, effective-white/active-color no-op semantics, partial-load warning feedback, startup storage alerts, viewport-safe tooltips, responsive-remount modal focus restoration, and the previously proven Chromium download lifecycle.</what-built>
  <how-to-verify>
    Preflight. Start `npm run dev -- --host 127.0.0.1`. For each of the eight exact browser versions, record either `local-direct` or `browserstack-local`/named approved equivalent. For every remote route, activate the tunnel, open the local Vite URL in that remote browser, and confirm the exact `CountriesIRL Map Generator` title and rendered map before accepting any test result. Record the exact browser version/build, route, URL, tunnel name when applicable, and preflight outcome in the summary.
    0. Automated test scope. From the repository root run `npm run test:run -- --reporter=verbose`. Confirm `src/vitestScope.test.ts` passes, every discovered test file is under `src/`, no path under `.claude/` or an agent worktree is collected, and the command exits instead of entering watch mode. Then run the complete automated command in this plan's verify block.
    1. Clear `countriesirl_onboarding_dismissed`, load the app through a preflighted route, confirm exact onboarding copy remains until explicit dismissal, use `Start Coloring`, and verify map focus. Color at least five countries in under two minutes. Reload and confirm help stays dismissed; use `Show Help` to reopen it; reload again and confirm the persisted dismissal remains effective.
    2. Selection/color and revised active semantics: with zero countries selected, confirm all ten presets are natively disabled. Select one uncolored country and confirm White is active plus natively disabled while the other nine presets enable. Dismiss status feedback, clear `countriesirl-color-start`, and attempt active White; confirm no success, history entry, or timing mark. Apply Red once; confirm Red becomes active plus disabled and White enables. Dismiss the one success message, clear the color-start mark, and attempt active Red; confirm no new success or timing mark. Undo once to effective white with no second Undo available, then redo once to Red. Continue with several country-list checkboxes, Select All/Clear, another named preset, valid `#RGB`, `#RRGGBB`, and `rgb(...)`, and invalid/out-of-range input; invalid input must show exact inline feedback without changing colors/history.
    3. Performance: clear prior performance entries before each series. Reload and collect five `countriesirl-map-ready` durations; every sample must be under 500ms. Perform ten effective visible color changes and collect ten `countriesirl-color-visible` durations; active-color no-op attempts must produce no color timing entry. Perform ten undo operations and collect ten `countriesirl-undo-visible` durations; perform ten redo operations and collect ten `countriesirl-redo-visible` durations. Every one of the 30 effective interaction samples must be under 100ms. Record sample values in the plan summary.
    4. History/stress: perform at least 55 distinct color actions, undo/redo repeatedly, undo then branch with a new edit, reset and undo reset, then perform 100+ rapid selection/color/history interactions. Confirm no crash, stale selection, duplicate country paths, double event, or console error/warning for valid data.
    5. Persistence success: save two named maps, replace one by exact trimmed name, reload the page, load each map, delete one without confirmation, and confirm load resets undo/redo.
    6. Mandatory persistence recovery and failures. First seed `countriesirl_maps` with a named record containing at least one valid current-country color and one invalid color, open Saved Maps, load that record, and confirm the valid subset is applied while the global status announces exactly `Saved map loaded, but some invalid saved colors were omitted.` with warning severity rather than success-only feedback. Next set `countriesirl_maps` to malformed JSON before opening the modal and confirm the corrupt-data warning while valid editing continues. For startup failure, set a breakpoint before the application mounts, reload, override `Storage.prototype.getItem` in the paused page realm to throw `DOMException('Blocked', 'SecurityError')`, then resume; confirm an immediate assertive error alert says local saves are blocked while editing/export remain available. Reload normally to restore the prototype. Then temporarily override `Storage.prototype.setItem` to throw the same SecurityError, attempt a save/onboarding dismissal, and confirm blocked-storage copy with no current-map loss. Repeat with `DOMException('Full', 'QuotaExceededError')` and confirm quota copy, preserved map name/current map, and saved-list visibility. Restore the original method after each case.
    7. Export: export colored and all-white maps. Inspect image properties and confirm exactly 1080×1080 pixels, opaque white background, map only, neutral uniform borders, current fills, no selection/focus/tooltip/header/label/toast/legend, filename `CountriesIRL_<date>.png`, and completion under 3 seconds.
    8. Responsive composition, tooltip edges, modal remount focus, and theme. At desktop ≥1200px inspect DOM/focus order as map then controls. At tablet 768–1199px and 360px inspect DOM/focus order as actions, map, selection/color, country list. Resize across 1200px and confirm exactly one workspace/map exists while colors/history/selection persist. At a 360px viewport, use keyboard focus on a country near the top edge and confirm the tooltip flips below/clamps inside the viewport; hover countries near the right and bottom edges and confirm the pointer tooltip flips left/above or clamps so no content crosses the viewport margin. Open Saved Maps from the desktop `Save or Load Maps` control, cross below 1200px while the modal remains open, close it, and confirm focus lands on the newly mounted compact `Save or Load Maps` control rather than disappearing or jumping to the document; repeat compact-to-desktop. Verify square uncropped map, 48px targets, no horizontal page scroll, mobile full-height saved-map sheet, and dark chrome with unchanged white map/export.
    9. Accessibility: operate map with Tab, arrows, Home/End, Enter/Space, Escape; confirm focus-visible rings, tooltip on focus, country-list bulk alternative, live announcements, modal focus trap/Escape/restore including the responsive-remount case, 200% zoom usability, and reduced-motion behavior.
    10. Blocking browser matrix: run the core flow (load map, single uncolored selection with White active/disabled, Red application with Red active/disabled and White enabled, active-preset no-op, bulk selection, custom color, undo/redo/reset, save/reload/load/delete, export, keyboard map, modal focus, no console errors) in Chrome current/previous, Firefox current/previous, Edge current/previous, and Safari current/previous. Use only successfully preflighted exact local or tunneled routes and record each of the eight cells as pass with exact browser version/build and route.
    11. Offline boundary: confirm there are no runtime third-party requests after load and the already-loaded app continues to color/save/export when the network is disabled. Do not test or require a fresh disconnected reload; no service worker is part of Phase 1.
    12. Data acceptance: review rendered modern borders and `public/data/README.md`; approve Natural Earth 5.1.1 default geopolitical POV and documented Europe/transcontinental inclusion.
  </how-to-verify>
  <acceptance_criteria>
    - Verbose default Vitest discovery includes only `src/**/*.test.{ts,tsx}`, excludes `.claude/**` agent-worktree copies, passes `src/vitestScope.test.ts`, and exits non-watch.
    - All eight browser cells record exact version/build, route, local URL, and successful preflight before acceptance; every remote route proves the local Vite app loads through BrowserStack Local or a named approved equivalent tunnel.
    - Browser tunnel tooling remains external to the repository and product dependency graph.
    - Five-country flow is under two minutes and onboarding dismissal persists across reload while Show Help reopens guidance.
    - Zero selection disables all ten presets; an uncolored selection keeps White active/disabled with nine alternatives enabled; Red application switches the active disabled preset to Red and enables White.
    - Active White/Red attempts create no color action, extra history, success feedback, or `countriesirl-color-start` timing residue.
    - Five map-ready samples are each under 500ms; ten effective color, ten undo, and ten redo visible samples are each under 100ms and are recorded.
    - 55+ edits and 100+ rapid interactions preserve bounded history and stable SVG behavior.
    - Successful persistence plus partial-corrupt load warning feedback, malformed JSON recovery, blocked startup read alert, blocked writes, and quota exceeded all pass without current-map loss.
    - PNG is exactly 1080×1080, opaque, map-only, correct in light/dark UI, and under 3 seconds.
    - Desktop/tablet/360px DOM/focus order uses exactly one active workspace and preserves state across the 1200px boundary.
    - At 360px, keyboard and pointer tooltips remain inside the viewport by flipping/clamping at top, right, and bottom edges.
    - Closing Saved Maps after desktop-to-compact and compact-to-desktop 1200px remounts restores focus to the currently mounted `Save or Load Maps` control.
    - All eight Chrome/Firefox/Edge/Safari current/previous matrix cells pass with recorded versions and routes, including revised active-color semantics.
    - Already-loaded offline behavior works with no runtime third-party requests; fresh disconnected reload is explicitly not required and no service worker is present.
    - Natural Earth 5.1.1 default POV/inclusion is explicitly approved.
    - D-10, D-17, D-23, D-31, D-37, D-38, D-40, D-47, D-49, D-50, D-51, D-52, D-53, D-54, and D-55 are human-verified.
  </acceptance_criteria>
  <resume-signal>Type "approved" only after the source-scope precheck, all browser preflights, twelve browser/data steps, and eight browser cells pass, or provide the failed route/preflight/step/matrix cell with exact version and observed behavior.</resume-signal>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:run &amp;&amp; node scripts/prepareGeoData.mjs --check &amp;&amp; npm run build</automated>
    <human-check>Verify source-scoped discovery, preflight and record every exact browser route, complete all twelve browser/data steps, record performance samples and browser versions/builds, and approve Natural Earth presentation.</human-check>
  </verify>
  <done>Source-scoped discovery, all deep-review acceptance items, browser routes, automated gates, measured thresholds, mandatory storage recovery/failures, responsive/accessibility checks, eight browser cells, offline boundary, export, and data presentation are approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository test config → reported automated evidence | Duplicate agent-worktree tests must not inflate or contaminate the product test run. |
| Browser implementation → human acceptance | Visual, timing, keyboard, compatibility, storage-feedback, and downloaded-file claims require direct observation. |
| Local Vite origin → remote browser matrix | Remote results are valid only after an approved tunnel proves the intended local app is loaded. |
| Browser storage failure simulation → application | Mandatory partial-corrupt, malformed, security, and quota cases exercise untrusted and unavailable persistence. |
| Geopolitical dataset → published map | Default Natural Earth POV can affect sensitive boundary presentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Executed dependency and test graph | mitigate | Confirm source-only Vitest discovery, run the automated gate, keep tunnel tooling external, and observe runtime/console across eight browser cells. |
| T-01-38 | Spoofing | False size/performance/compatibility claim | mitigate | Record downloaded dimensions, 35 timing samples, exact browser versions/builds, route, and matrix outcomes. |
| T-01-39 | Denial of Service | Rapid interaction/history/export/storage failures | mitigate | Run 55+ edit, 100+ interaction, partial-corrupt, startup-blocked, blocked-write, and quota passes. |
| T-01-40 | Repudiation | Geopolitical POV | mitigate | Require explicit approval of documented Natural Earth 5.1.1 default POV/inclusion before deploy. |
| T-01-41 | Spoofing | Remote browser pointed at wrong app | mitigate | Require local Vite URL/title/map preflight through BrowserStack Local or approved equivalent before accepting each remote cell. |
| T-01-42 | Repudiation | Claimed active-color or focus behavior without evidence | mitigate | Record active-preset state/no-op evidence, tooltip edge observations, and both 1200px remount focus-restoration directions. |
</threat_model>

<verification>
Human approval is mandatory; no automated result substitutes for source-scope evidence, successful browser-route preflights, the twelve-step UAT, recorded timing samples, revised active-color/storage/tooltip/focus checks, and the eight-cell compatibility matrix.
</verification>

<success_criteria>
Every source-discovery, preflighted browser, deep-review behavior, measured performance, responsive, accessibility, persistence, failure, stress, export, offline-boundary, compatibility, and map-presentation check passes with recorded evidence and no unresolved defect.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-15-SUMMARY.md` when done.
</output>
