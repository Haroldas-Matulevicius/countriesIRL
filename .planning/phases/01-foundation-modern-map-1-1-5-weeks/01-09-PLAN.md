---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "09"
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/utils/storage.ts
  - src/utils/storage.test.ts
  - src/hooks/useLocalStorage.ts
autonomous: true
requirements: [F6.1, F6.2]
must_haves:
  truths:
    - "Saved maps round-trip under countriesirl_maps without trusting malformed browser storage."
    - "Save/replace/list/load/delete obey max-10, newest-first, trimmed-name, and explicit error semantics."
    - "Corrupt records, blocked storage, and quota failures never crash editing or export."
  artifacts:
    - path: "src/utils/storage.ts"
      provides: "Typed raw localStorage adapter and schema validation"
      exports: ["createStorageAdapter"]
    - path: "src/utils/storage.test.ts"
      provides: "Persistence and failure-mode tests"
    - path: "src/hooks/useLocalStorage.ts"
      provides: "Reactive lazy saved-map state and mutation facade"
      exports: ["useLocalStorage"]
  key_links:
    - from: "src/utils/storage.ts"
      to: "countriesirl_maps"
      via: "exact storage key"
      pattern: "countriesirl_maps"
    - from: "src/hooks/useLocalStorage.ts"
      to: "src/utils/storage.ts"
      via: "all list/save/load/delete operations"
      pattern: "storage"
---

<objective>
Implement the fully validated localStorage adapter and reactive persistence hook for save, overwrite, list, load, and delete.

Purpose: Make browser persistence reliable across corruption, quota, unavailable storage, capacity, and reload cases before modal wiring.
Output: Tested storage utility and a lazy reactive `useLocalStorage` facade.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/types/map.ts
@src/types/ui.ts
@src/constants/config.ts
@src/utils/colors.ts
@.planning/coding-rules/general.md
@.planning/coding-rules/storage.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md

<interfaces>
All raw `Storage`, `JSON.parse`, and `JSON.stringify` work stays in `src/utils/storage.ts`. Operations return explicit discriminated results for success, validation warning, quota exceeded, and storage unavailable. The adapter accepts current valid country IDs when loading so stale/corrupt color keys are omitted. The hook owns reactive `savedMaps`, warning/error state, lazy open-time refresh, and save/load/delete methods.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the typed storage adapter and exhaustive failure tests</name>
  <files>src/utils/storage.ts, src/utils/storage.test.ts</files>
  <read_first>
    src/utils/storage.ts
    src/utils/storage.test.ts
    src/types/ui.ts
    src/constants/config.ts
    src/utils/colors.ts
    .planning/coding-rules/storage.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
  </read_first>
  <behavior>
    - Empty store lists no maps successfully.
    - Saving trims names, rejects empty/>100 names, prepends newest, replaces exact trimmed-name matches, and caps at 10 by dropping oldest.
    - Load returns only normalized valid colors restricted to supplied country IDs; delete removes only the selected record.
    - Malformed JSON/records/colors are omitted with warnings while valid records survive.
    - QuotaExceededError and other blocked/unavailable writes produce distinct explicit failures.
  </behavior>
  <action>Write failing tests with an injected fake `Storage`, then implement all raw persistence in `src/utils/storage.ts` per D-07, D-34, D-35, D-36, D-37, D-38, and D-39. Parse to `unknown`, validate each record and every ID/color entry, cap names at 100, preserve timestamps/newest-first order, overwrite by exact trimmed name, cap records at 10, and never render or preserve arbitrary HTML. Distinguish quota from unavailable/security failures. Avoid ambiguous `null` results and never throw browser storage exceptions through callers.</action>
  <verify>
    <automated>npm run test:run -- src/utils/storage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover list/save/replace/load/delete, max 10, trim/length, malformed JSON/records/colors, quota, and unavailable storage.
    - Exact key `countriesirl_maps` is used once through a named constant.
    - Loaded colors are restricted to known IDs and normalized valid values.
    - No direct `window.localStorage` dependency is required by tests.
    - The focused test command exits 0.
  </acceptance_criteria>
  <done>The persistence boundary is deterministic, capacity-bounded, schema-validated, and failure-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Expose lazy reactive persistence state through useLocalStorage</name>
  <files>src/hooks/useLocalStorage.ts</files>
  <read_first>
    src/hooks/useLocalStorage.ts
    src/utils/storage.ts
    src/types/ui.ts
    .planning/coding-rules/storage.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
  </read_first>
  <action>Implement `useLocalStorage` as the reactive facade over the adapter. It must load/list lazily when the save/load modal opens, keep `savedMaps` in state, refresh immediately after save/replace/delete, and expose explicit operation results/warnings for UI translation. Load accepts the current valid country-ID set and returns a validated colors record ready for the reducer's LOAD_STATE baseline reset. Storage unavailable must disable persistence actions while leaving editing/export unaffected, per D-07, D-35, D-38, and D-39.</action>
  <verify>
    <automated>npm run test:run -- src/utils/storage.test.ts &amp;&amp; npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Hook contains reactive saved-map and warning/error state.
    - No raw JSON or localStorage calls appear outside `src/utils/storage.ts`.
    - Save/delete update hook state immediately without modal reopen.
    - Load accepts valid IDs and returns explicit success/error data.
    - Tests, lint, and TypeScript exit 0.
  </acceptance_criteria>
  <done>The UI can lazily list and mutate saved maps with immediate updates and precise user-facing failure reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → application state | Browser-stored JSON can be manually corrupted or maliciously altered. |
| User map name → persisted/rendered text | Arbitrary text survives reload and appears in a modal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Test/build dependencies | mitigate | Exact lockfile and package gate. |
| T-01-21 | Tampering / XSS | `src/utils/storage.ts` records | mitigate | Parse unknown, validate shape/colors/lengths, React text rendering only downstream. |
| T-01-22 | Denial of Service | Unbounded maps/names | mitigate | 10-map cap, 100-character name cap, finite color-key filtering. |
| T-01-23 | Information Disclosure | Browser-local saves | accept | D-39 states no credentials/sensitive data; document same-browser/origin scope. |
</threat_model>

<verification>
Run focused storage tests, lint, and strict TypeScript.
</verification>

<success_criteria>
Valid maps persist across reload, all required mutations update immediately, and corrupt/unavailable/quota cases degrade with explicit safe results.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-09-SUMMARY.md` when done.
</output>
