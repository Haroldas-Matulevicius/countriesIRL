---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "06"
type: execute
wave: 5
depends_on: ["01-04", "01-05"]
files_modified:
  - src/components/MapCanvas.tsx
  - src/components/Tooltip.tsx
  - src/components/MapWorkspace.tsx
  - src/components/FatalErrorState.tsx
autonomous: true
requirements: [F1.1, F1.2, NFR1, NFR2, NFR11]
must_haves:
  truths:
    - "Every valid country renders as one stable SVG path using the fixed Mercator preview/export view."
    - "Color and selection updates do not recompute or duplicate path geometry."
    - "Pointer and keyboard users can identify, focus, and select every country, including through approved loading/warning/error states."
  artifacts:
    - path: "src/components/MapCanvas.tsx"
      provides: "D3-owned interactive SVG countries layer"
      exports: ["MapCanvas"]
    - path: "src/components/Tooltip.tsx"
      provides: "Country name and current-color hover/focus feedback"
      exports: ["Tooltip"]
    - path: "src/components/MapWorkspace.tsx"
      provides: "Persistent map shell for loading, warning, ready, and fatal states"
      exports: ["MapWorkspace"]
    - path: "src/components/FatalErrorState.tsx"
      provides: "Approved map-load recovery UI"
      exports: ["FatalErrorState"]
  key_links:
    - from: "src/components/MapCanvas.tsx"
      to: "d3.geoMercator/geoPath"
      via: "geometry effect"
      pattern: "geoMercator|geoPath"
    - from: "src/components/MapCanvas.tsx"
      to: "selectedIds/colors"
      via: "lightweight attribute and ARIA effect"
      pattern: "classed|attr.*fill"
---

<objective>
Build the stable, accessible D3/SVG map surface and its complete loading, warning, tooltip, and fatal-error shell.

Purpose: Deliver the core modern Europe interaction without React/D3 ownership conflicts, duplicate nodes, or pointer-only access.
Output: `MapCanvas`, map tooltip, workspace state shell, and fatal recovery component.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/types/map.ts
@src/constants/colors.ts
@src/constants/config.ts
@src/hooks/useGeoData.ts
@src/hooks/useMapState.ts
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/coding-rules/data.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md

<interfaces>
`MapCanvas` receives normalized features, ID-keyed colors, one selected-ID set, stable single-select/clear callbacks, and hover/focus reporting callbacks. It renders `viewBox="0 0 1080 1080"`, `preserveAspectRatio="xMidYMid meet"`, and a countries layer owned only by D3. The SVG exposes an exportable ref or wrapper contract consumed later by App/export integration.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement stable D3 geometry, styling updates, and map keyboard navigation</name>
  <files>src/components/MapCanvas.tsx</files>
  <read_first>
    src/components/MapCanvas.tsx
    src/types/map.ts
    src/constants/colors.ts
    src/constants/config.ts
    src/hooks/useMapState.ts
    .planning/coding-rules/frontend.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
  </read_first>
  <action>Implement a React-owned outer SVG and D3-owned `[data-layer="countries"]` subtree per D-03, D-04, D-13, D-14, and D-21 through D-25. Use `geoMercator().fitExtent([[64,64],[1016,1016]], fixedEuropeViewObject)` and `geoPath`, keyed joins by stable feature ID, namespaced events, and symmetric cleanup for Strict Mode. Separate geometry/join work from fill, selected class, title, ARIA, and tabindex updates so color edits do not clear/rebuild paths. Render the SVG as `role="listbox"`, `aria-label="Interactive map of modern Europe"`, `aria-multiselectable="true"`; paths are `role="option"` with current-color accessible text, `<title>`, `aria-selected`, and roving tabindex. Implement Arrow/Home/End focus movement in stable alphabetical order, Enter/Space single selection, Escape clear, click/tap single selection, and explicit empty-background clear. Expose pointer/focus tooltip data and a concrete export-source ref contract. Do not implement pan, zoom, centering, historical periods, or legends.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The component contains one keyed D3 join and no `selectAll('*').remove()` call.
    - Geometry effect dependencies exclude colors/selection; style/ARIA updates include them.
    - SVG and country path roles/labels match the UI-SPEC accessibility contract.
    - Arrow/Home/End/Enter/Space/Escape handling is implemented without `any`.
    - Lint and strict TypeScript exit 0.
  </acceptance_criteria>
  <done>The map can render and select countries accessibly while keeping path geometry stable across edits.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tooltip and complete map workspace states</name>
  <files>src/components/Tooltip.tsx, src/components/MapWorkspace.tsx, src/components/FatalErrorState.tsx</files>
  <read_first>
    src/components/Tooltip.tsx
    src/components/MapWorkspace.tsx
    src/components/FatalErrorState.tsx
    src/components/MapCanvas.tsx
    src/hooks/useGeoData.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/coding-rules/frontend.md
  </read_first>
  <action>Create `Tooltip` for pointer hover and keyboard focus with country name and current normalized color, never as the sole information channel. Create `MapWorkspace` that preserves the complete square shell and `1080 × 1080 PNG preview` label during loading, ready, partial-warning, and fatal states. Loading uses the approved neutral skeleton and `Loading Europe map…`; partial data uses the exact non-blocking warning copy; fatal state delegates to `FatalErrorState` with exact heading/body and `Reload Map` action. Do not expose raw errors/URLs or replace the whole application shell with a bare message. This implements D-19, D-22, D-47, D-48, D-51, D-52, and the UI-SPEC state contract.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Exact loading, partial-warning, fatal heading/body, and reload copy exists.
    - Tooltip content includes country name and current color for hover and focus data.
    - Fatal UI never interpolates a raw exception message.
    - Components use semantic buttons and explicit return/prop types.
    - Lint and strict TypeScript exit 0.
  </acceptance_criteria>
  <done>The map surface communicates all required non-ready and interaction states without crashing or hiding the application shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Normalized features → D3 DOM | Data becomes SVG attributes, text, and event targets. |
| Pointer/keyboard events → selection state | High-frequency input changes creator state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | D3 and build dependencies | mitigate | Exact audited packages and lockfile. |
| T-01-12 | Tampering / XSS | SVG labels and titles | mitigate | Use D3 `.text`/attribute setters with normalized strings; never `innerHTML`. |
| T-01-13 | Denial of Service | Duplicate paths/listeners | mitigate | Stable keyed joins, split effects, namespaced handlers, and Strict Mode cleanup per D-24/D-55. |
| T-01-14 | Spoofing | Color-only selection | mitigate | Border, aria-selected, focus state, and listbox semantics per D-51. |
</threat_model>

<verification>
Run lint and strict TypeScript; final path-count, keyboard, tooltip, and performance behavior is exercised in the Phase 1 UAT checkpoint.
</verification>

<success_criteria>
The modern Europe SVG is stable, keyboard/pointer operable, correctly labeled, and resilient across loading, warning, and fatal data states.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-06-SUMMARY.md` when done.
</output>
