---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "16"
type: execute
wave: 14
depends_on: ["01-15"]
files_modified: []
autonomous: false
requirements: [F5.1, NFR5]
user_setup:
  - service: vercel
    why: "Authorize, link, and deploy the static Vite app to the locked production host"
    account_setup:
      - "A Vercel account with permission to create/link the CountriesIRL project"
    dashboard_config: []
must_haves:
  truths:
    - "Plan 01-15 has explicit functional approval for clean current-HEAD Chrome 150 and Edge 150 cells, with timing observations correctly recorded as non-blocking per D-63 and no prior deployment."
    - "A human explicitly completes Vercel CLI authorization and identifies the intended account/team scope before linking."
    - "The repository is linked non-interactively to the `countriesirl` Vercel project using Vercel CLI 56.4.1 flags."
    - "Production deployment executes exactly once, the HTTPS URL is captured, and all later verification uses read-only inspection."
    - "Ignored `.vercel` metadata is allowed while tracked product, config, planning, and documentation files remain unchanged."
  artifacts: []
  key_links:
    - from: "01-15-SUMMARY.md"
      to: "Vercel authorization"
      via: "explicit functional release approval with both local browser cells clean and no timing-threshold prerequisite"
      pattern: "Chrome 150.*PASS|Edge 150.*PASS"
    - from: "Vercel CLI authorization"
      to: "vercel link --yes --project countriesirl --scope {scope}"
      via: "blocking human account/team confirmation followed by non-interactive linking"
      pattern: "vercel.*link.*--yes.*--project"
    - from: "single production deploy"
      to: ".vercel/phase1-production-url.txt"
      via: "captured CLI output"
      pattern: "deploy.*--prod.*--yes"
---

<objective>
After Plan 01-15 functional approval, complete human-required Vercel authorization, non-interactively link the project, deploy production exactly once, and capture the URL without modifying tracked repository content.

Purpose: Preserve the release dependency boundary while preventing timing diagnostics from re-entering deployment authorization or causing accidental redeploys during verification.
Output: Authenticated/linked Vercel project, one production deployment, ignored URL metadata, and the exact URL in `01-16-SUMMARY.md`.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-15-SUMMARY.md
@package.json
@vite.config.ts
@public/data/europe-modern.geojson
@.gitignore
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Confirm functional release approval, authorize Vercel, and confirm deployment scope</name>
  <files>.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-15-SUMMARY.md, package.json, vite.config.ts, .gitignore</files>
  <read_first>
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-15-SUMMARY.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
    package.json
    vite.config.ts
    .gitignore
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
  </read_first>
  <action>Before any Vercel command, verify `01-15-SUMMARY.md` exists and explicitly records PASS for both installed Chrome 150 and Edge 150 functional cells, exactly 57 unique non-empty paths, no console/runtime/required-product-network errors, accepted final review/test/build/PNG/functionality evidence, immutable failed timing evidence left unchanged, timing observations non-blocking per D-63, and no deployment during Plan 01-15. Do not require timing thresholds, a CDP timing artifact, or a passing `overallPass` timing field. If the functional approval is absent, stop without authenticating or deploying. Once the dependency is satisfied, use only the Plan 01-approved official `vercel@56.4.1` CLI. Start `npx --yes vercel@56.4.1 login`; the human completes the browser/email authentication flow. Then run `npx --yes vercel@56.4.1 whoami` and identify the exact account/team slug that should own the project. Do not provide tokens in files, commit credentials, or add environment variables.</action>
  <what-built>Plan 01-15 approved the complete app on accepted final evidence plus clean current-HEAD Chrome 150 and Edge 150 functional smoke, without threshold-based timing release gates.</what-built>
  <how-to-verify>
    1. Confirm `01-15-SUMMARY.md` records both functional browser cells as PASS, no console/product errors, exact 57-path integrity, exact PNG correctness, D-63's non-blocking timing disposition, and no deployment.
    2. Complete the Vercel login link/email/browser flow.
    3. Run `npx --yes vercel@56.4.1 whoami` and confirm the intended account/team.
    4. Determine the exact account/team scope slug for `--scope`.
    5. Confirm no token or credential was written into repository files.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-15-SUMMARY.md &amp;&amp; python -c "from pathlib import Path; s=Path('.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-15-SUMMARY.md').read_text(encoding='utf-8'); assert 'Chrome 150' in s and 'Edge 150' in s and 'PASS' in s and '57' in s and 'D-63' in s and 'no deployment' in s.lower()" &amp;&amp; npx --yes vercel@56.4.1 whoami</automated>
    <human-check>Confirm the Plan 01-15 functional approval and printed Vercel identity, then provide the exact Vercel scope slug.</human-check>
  </verify>
  <acceptance_criteria>
    - Plan 01-15 summary exists and records clean functional Chrome 150 and Edge 150 PASS cells, accepted final evidence, exact 57-path/export correctness, no console/product errors, D-63 timing disposition, and no deployment.
    - No timing threshold, CDP timing artifact, or historical timing `overallPass` is required for deployment authorization.
    - Human completes authentication and approves the account/team.
    - `vercel whoami` exits 0.
    - The resume response includes the exact scope slug used by Task 2.
    - No credentials, `.env.local`, or token file is added to the repository.
  </acceptance_criteria>
  <done>The functional release dependency is satisfied, the official CLI is authenticated, and the intended Vercel scope is explicitly known.</done>
  <resume-signal>Type `authorized {scope-slug}` after the Plan 01-15 functional approval check passes and `vercel whoami` shows the intended account/team, or report the missing functional evidence/authentication problem.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Link the CountriesIRL Vercel project non-interactively</name>
  <files>package.json, vite.config.ts, .gitignore</files>
  <read_first>
    package.json
    vite.config.ts
    .gitignore
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
  </read_first>
  <action>Using the scope slug supplied by Task 1, run `npx --yes vercel@56.4.1 link --yes --project countriesirl --scope {scope-slug}`. The `--yes`, `--project`, and `--scope` flags must make linking non-interactive. If Vercel still requests project/account selection or creation input, stop and create a `checkpoint:human-action` for that unavoidable prompt rather than guessing. Allow Vercel to create/update ignored `.vercel` metadata only. Do not edit `.gitignore`, package/config files, product source, planning documents, or README.</action>
  <verify>
    <automated>npx --yes vercel@56.4.1 project inspect countriesirl --scope {scope-slug} &amp;&amp; git diff --exit-code -- . ':(exclude).vercel'</automated>
  </verify>
  <acceptance_criteria>
    - Linking uses Vercel CLI 56.4.1 with `--yes --project countriesirl --scope {scope-slug}`.
    - Read-only `project inspect` confirms the linked project under the intended scope.
    - Only ignored `.vercel` metadata may change.
    - Tracked product, config, planning, and documentation files remain unchanged.
  </acceptance_criteria>
  <done>The repository is linked to the intended CountriesIRL Vercel project without an interactive selection or tracked-file change.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy production exactly once and inspect it read-only</name>
  <files>package.json, vite.config.ts, .gitignore</files>
  <read_first>
    package.json
    vite.config.ts
    .gitignore
    public/data/europe-modern.geojson
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
  </read_first>
  <action>Run `npm run build` once, then execute exactly one production deployment command: `npx --yes vercel@56.4.1 deploy --prod --yes`. Capture the command's exact HTTPS production URL, write it to ignored `.vercel/phase1-production-url.txt`, and record it in `01-16-SUMMARY.md`. After deployment, run only read-only `npx --yes vercel@56.4.1 inspect {production-url}` and `project inspect`; do not run another deploy command as verification. The Vite build command remains `npm run build` and output directory `dist`. Do not add functions, routes, `vercel.json`, environment variables, analytics, authentication, service workers, or tracked repository changes. This implements D-08 and D-60.</action>
  <verify>
    <automated>PRODUCTION_URL=$(python -c "from pathlib import Path; print(Path('.vercel/phase1-production-url.txt').read_text().strip())") &amp;&amp; test -n "$PRODUCTION_URL" &amp;&amp; npx --yes vercel@56.4.1 inspect "$PRODUCTION_URL" &amp;&amp; git diff --exit-code -- . ':(exclude).vercel'</automated>
  </verify>
  <acceptance_criteria>
    - Task execution contains exactly one `vercel deploy --prod --yes` invocation.
    - Verification uses only `vercel inspect`/`project inspect` and never redeploys.
    - `.vercel/phase1-production-url.txt` and the summary contain the same HTTPS URL.
    - Ignored `.vercel` metadata is allowed; tracked product, config, planning, and documentation files remain unchanged.
    - No backend, secret, service worker, or runtime Vercel environment dependency is introduced.
  </acceptance_criteria>
  <done>The approved static build is deployed once and its exact URL is captured and read-only inspected for Plan 01-17.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 01-15 functional approval → deployment authorization | Deployment requires clean browser/functionality acceptance, not timing thresholds or historical timing artifact status. |
| Human identity → Vercel CLI | Account authorization and scope confirmation cannot be safely inferred. |
| Repository → Vercel project metadata | Linking may create ignored local metadata but must not edit tracked files. |
| Local build → Vercel CDN | Static artifacts and public map data leave the developer machine once. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `vercel@56.4.1` CLI | mitigate | Plan 01 human legitimacy approval and exact transient version. |
| T-01-41 | Spoofing | Plan 01-15/Vercel account/team/project | mitigate | Assert functional summary fields, then human whoami/scope confirmation and non-interactive project inspect. |
| T-01-42 | Information Disclosure | Deployment configuration | mitigate | No secrets/env vars/backend; static public assets only. |
| T-01-43 | Tampering | Production vs approved build | mitigate | One deployment after functional UAT; read-only inspection and tracked-diff gate afterward. |
| T-01-50 | Denial of Service | Accidental repeated deployment | mitigate | Exactly one deploy command; all verification commands are read-only. |
</threat_model>

<verification>
Plan 01-15 functional approval is confirmed without reintroducing timing gates; authentication and scope are human-approved; linking is non-interactive; production deploy runs once; URL metadata/summary match; read-only inspection succeeds; and tracked diff remains clean.
</verification>

<success_criteria>
After clean functional local acceptance, the correct Vercel account/project receives exactly one approved static deployment, with only ignored `.vercel` metadata and no tracked repository changes.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md` when done and include the Plan 01-15 functional-dependency check plus the exact production URL from `.vercel/phase1-production-url.txt`.
</output>
