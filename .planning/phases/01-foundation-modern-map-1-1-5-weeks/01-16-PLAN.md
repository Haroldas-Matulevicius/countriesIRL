---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "16"
type: execute
wave: 13
depends_on: ["01-15"]
files_modified: []
autonomous: false
requirements: [F5.1, NFR5]
user_setup:
  - service: vercel
    why: "Authorize, link, and deploy the static Vite app to the locked production host"
    account_setup:
      - "A Vercel account with permission to create/link the CountriesIRL project"
    dashboard_config: []
must_haves:
  truths:
    - "A human explicitly completes Vercel CLI authorization and identifies the intended account/team scope before linking."
    - "The repository is linked non-interactively to the `countriesirl` Vercel project using Vercel CLI 56.4.1 flags."
    - "Production deployment executes exactly once, the HTTPS URL is captured, and all later verification uses read-only inspection."
    - "Ignored `.vercel` metadata is allowed while tracked product, config, planning, and documentation files remain unchanged."
  artifacts: []
  key_links:
    - from: "Vercel CLI authorization"
      to: "vercel link --yes --project countriesirl --scope {scope}"
      via: "blocking human account/team confirmation followed by non-interactive linking"
      pattern: "vercel.*link.*--yes.*--project"
    - from: "single production deploy"
      to: ".vercel/phase1-production-url.txt"
      via: "captured CLI output"
      pattern: "deploy.*--prod.*--yes"
---

<objective>
Complete human-required Vercel authorization, non-interactively link the project, deploy production exactly once, and capture the URL without modifying tracked repository content.

Purpose: Make authentication and linking explicit while preventing accidental redeploys during verification.
Output: Authenticated/linked Vercel project, one production deployment, ignored URL metadata, and the exact URL in `01-16-SUMMARY.md`.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@package.json
@vite.config.ts
@public/data/europe-modern.geojson
@.gitignore
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Authorize Vercel and confirm the deployment scope</name>
  <files>package.json, vite.config.ts, .gitignore</files>
  <read_first>
    package.json
    vite.config.ts
    .gitignore
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
  </read_first>
  <action>Use only the Plan 01-approved official `vercel@56.4.1` CLI. Start `npx --yes vercel@56.4.1 login`; the human completes the browser/email authentication flow. Then run `npx --yes vercel@56.4.1 whoami` and identify the exact account/team slug that should own the project. Do not provide tokens in files, commit credentials, or add environment variables.</action>
  <what-built>The complete app has passed browser UAT. This checkpoint authorizes the external account and supplies the explicit scope required for non-interactive project linking.</what-built>
  <how-to-verify>
    1. Complete the Vercel login link/email/browser flow.
    2. Run `npx --yes vercel@56.4.1 whoami` and confirm the intended account/team.
    3. Determine the exact account/team scope slug for `--scope`.
    4. Confirm no token or credential was written into repository files.
  </how-to-verify>
  <verify>
    <automated>npx --yes vercel@56.4.1 whoami</automated>
    <human-check>Confirm the printed identity and provide the exact Vercel scope slug.</human-check>
  </verify>
  <acceptance_criteria>
    - Human completes authentication and approves the account/team.
    - `vercel whoami` exits 0.
    - The resume response includes the exact scope slug used by Task 2.
    - No credentials, `.env.local`, or token file is added to the repository.
  </acceptance_criteria>
  <done>The official CLI is authenticated and the intended Vercel scope is explicitly known.</done>
  <resume-signal>Type `authorized {scope-slug}` after `vercel whoami` shows the intended account/team, or report the authentication problem.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Link the CountriesIRL Vercel project non-interactively</name>
  <files>package.json, vite.config.ts, .gitignore</files>
  <read_first>
    package.json
    vite.config.ts
    .gitignore
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
  </read_first>
  <action>Using the scope slug supplied by Task 1, run `npx --yes vercel@56.4.1 link --yes --project countriesirl --scope {scope-slug}`. The `--yes`, `--project`, and `--scope` flags must make linking non-interactive. If Vercel still requests project/account selection or creation input, stop and create a `checkpoint:human-action` for that unavoidable prompt rather than guessing. Allow Vercel to create/update ignored `.vercel` metadata only. Do not edit `.gitignore`, package/config files, product source, planning documents, or README.</action>
  <verify>
    <automated>npx --yes vercel@56.4.1 project inspect countriesirl --scope {scope-slug} &amp;&amp; git diff --exit-code -- . ':(exclude).vercel'</automated>
  </verify>
  <acceptance_criteria>
    - Linking uses Vercel CLI 56.4.1 with `--yes --project countriesirl --scope {scope-slug}`.
    - Read-only `project inspect` confirms the linked project under the intended scope.
    - Only ignored `.vercel` metadata may change.
    - Tracked product, config, planning, and documentation files remain unchanged.
  </acceptance_criteria>
  <done>The repository is linked to the intended CountriesIRL Vercel project without an interactive selection or tracked-file change.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy production exactly once and inspect it read-only</name>
  <files>package.json, vite.config.ts, .gitignore</files>
  <read_first>
    package.json
    vite.config.ts
    .gitignore
    public/data/europe-modern.geojson
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
  </read_first>
  <action>Run `npm run build` once, then execute exactly one production deployment command: `npx --yes vercel@56.4.1 deploy --prod --yes`. Capture the command's exact HTTPS production URL, write it to ignored `.vercel/phase1-production-url.txt`, and record it in `01-16-SUMMARY.md`. After deployment, run only read-only `npx --yes vercel@56.4.1 inspect {production-url}` and `project inspect`; do not run another deploy command as verification. The Vite build command remains `npm run build` and output directory `dist`. Do not add functions, routes, `vercel.json`, environment variables, analytics, authentication, service workers, or tracked repository changes. This implements D-08 and D-60.</action>
  <verify>
    <automated>PRODUCTION_URL=$(python -c "from pathlib import Path; print(Path('.vercel/phase1-production-url.txt').read_text().strip())") &amp;&amp; test -n "$PRODUCTION_URL" &amp;&amp; npx --yes vercel@56.4.1 inspect "$PRODUCTION_URL" &amp;&amp; git diff --exit-code -- . ':(exclude).vercel'</automated>
  </verify>
  <acceptance_criteria>
    - Task execution contains exactly one `vercel deploy --prod --yes` invocation.
    - Verification uses only `vercel inspect`/`project inspect` and never redeploys.
    - `.vercel/phase1-production-url.txt` and the summary contain the same HTTPS URL.
    - Ignored `.vercel` metadata is allowed; tracked product, config, planning, and documentation files remain unchanged.
    - No backend, secret, service worker, or runtime Vercel environment dependency is introduced.
  </acceptance_criteria>
  <done>The approved static build is deployed once and its exact URL is captured and read-only inspected for Plan 01-17.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human identity → Vercel CLI | Account authorization and scope confirmation cannot be safely inferred. |
| Repository → Vercel project metadata | Linking may create ignored local metadata but must not edit tracked files. |
| Local build → Vercel CDN | Static artifacts and public map data leave the developer machine once. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `vercel@56.4.1` CLI | mitigate | Plan 01 human legitimacy approval and exact transient version. |
| T-01-41 | Spoofing | Vercel account/team/project | mitigate | Human whoami/scope confirmation plus non-interactive project inspect. |
| T-01-42 | Information Disclosure | Deployment configuration | mitigate | No secrets/env vars/backend; static public assets only. |
| T-01-43 | Tampering | Production vs approved build | mitigate | One deployment after UAT; read-only inspection and tracked-diff gate afterward. |
| T-01-50 | Denial of Service | Accidental repeated deployment | mitigate | Exactly one deploy command; all verification commands are read-only. |
</threat_model>

<verification>
Authentication and scope are human-approved, linking is non-interactive, production deploy runs once, URL metadata/summary match, read-only inspection succeeds, and tracked diff remains clean.
</verification>

<success_criteria>
The correct Vercel account/project receives exactly one approved static deployment, with only ignored `.vercel` metadata and no tracked repository changes.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md` when done and include the exact production URL from `.vercel/phase1-production-url.txt`.
</output>
