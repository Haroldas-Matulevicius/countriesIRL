---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "16"
type: execute
wave: 10
depends_on: ["01-15"]
files_modified: []
autonomous: false
requirements: [F5.1, NFR5]
user_setup:
  - service: vercel
    why: "Authorize and deploy the static Vite app to the locked production host"
    account_setup:
      - "A Vercel account with permission to create/link the CountriesIRL project"
    dashboard_config: []
must_haves:
  truths:
    - "A human explicitly completes Vercel CLI authorization before deployment automation continues."
    - "The approved static Vite build is deployed to production with no backend, secrets, service worker, or environment-variable dependency."
    - "The production HTTPS URL is captured in the plan summary for Plan 01-17 verification."
  artifacts: []
  key_links:
    - from: "Vercel CLI authorization"
      to: "production deployment"
      via: "blocking human action before automated deploy"
      pattern: "vercel.*(login|whoami|--prod)"
---

<objective>
Complete the human-required Vercel authorization and deploy the already-approved static Phase 1 build.

Purpose: Make the external-service trust boundary explicit and non-autonomous while keeping production verification/documentation separate.
Output: Authenticated Vercel CLI session, production deployment, and captured HTTPS URL in `01-16-SUMMARY.md`.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@package.json
@vite.config.ts
@public/data/europe-modern.geojson
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Authorize the official Vercel CLI session</name>
  <files>package.json, vite.config.ts</files>
  <read_first>
    package.json
    vite.config.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
  </read_first>
  <action>Use only the Plan 01-approved official `vercel@56.4.1` CLI. Start `npx --yes vercel@56.4.1 login`; the human must complete the browser/email authentication flow that cannot be automated. Do not provide tokens in files, commit credentials, or add environment variables.</action>
  <what-built>The local app has passed automated and browser UAT. This checkpoint authorizes the external Vercel account before any production deployment command runs.</what-built>
  <how-to-verify>
    1. Complete the Vercel login link/email/browser flow opened by `npx --yes vercel@56.4.1 login`.
    2. Run `npx --yes vercel@56.4.1 whoami` and confirm it prints the intended Vercel account/team.
    3. Confirm no token or credential was written into repository files.
  </how-to-verify>
  <verify>
    <automated>npx --yes vercel@56.4.1 whoami</automated>
    <human-check>Confirm the printed account/team is authorized to deploy CountriesIRL.</human-check>
  </verify>
  <acceptance_criteria>
    - Human completes Vercel authentication and approves the resulting account/team.
    - `vercel whoami` exits 0.
    - No credentials, `.env.local`, or Vercel token file is added to the repository.
  </acceptance_criteria>
  <done>The official Vercel CLI is authenticated to the correct account/team without repository secrets.</done>
  <resume-signal>Type "authorized" after `vercel whoami` shows the intended account/team, or report the authentication problem.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Deploy the verified static Vite build and capture its production URL</name>
  <files>package.json, vite.config.ts</files>
  <read_first>
    package.json
    vite.config.ts
    public/data/europe-modern.geojson
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
  </read_first>
  <action>After authorization, run `npm run build` and `npx --yes vercel@56.4.1 --prod`. Link/create the CountriesIRL project as a static Vite deployment with build command `npm run build` and output directory `dist`. Do not add Vercel functions, backend routes, `vercel.json`, required environment variables, analytics, authentication, service workers, or product-code use of Vercel variables. Capture the exact CLI-returned HTTPS production URL in `01-16-SUMMARY.md` for Plan 01-17. This implements D-08 and D-60.</action>
  <verify>
    <automated>npm run build &amp;&amp; npx --yes vercel@56.4.1 --prod</automated>
  </verify>
  <acceptance_criteria>
    - Build and production deployment commands exit 0 after the checkpoint.
    - The CLI returns an HTTPS production URL and the summary records it exactly.
    - No repository file is modified by the deployment plan.
    - No backend, secret, service worker, or runtime Vercel environment dependency is introduced.
  </acceptance_criteria>
  <done>The approved static build is deployed and its exact production URL is available to Plan 01-17.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human identity → Vercel CLI | Account authorization cannot be safely automated. |
| Local build → Vercel CDN | Static build artifacts and public map data leave the developer machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `vercel@56.4.1` CLI | mitigate | Plan 01 human legitimacy approval and exact transient version. |
| T-01-41 | Spoofing | Vercel account/team | mitigate | Blocking human login plus `vercel whoami` confirmation. |
| T-01-42 | Information Disclosure | Deployment configuration | mitigate | No secrets/env vars/backend; publish only static app and public map data. |
| T-01-43 | Tampering | Production vs approved build | mitigate | Deploy only after Plan 01-15 approval and a fresh build. |
</threat_model>

<verification>
Human authorization is confirmed, build/deploy exit 0, repository diff stays clean, and the production URL is recorded in the summary.
</verification>

<success_criteria>
The correct Vercel account deploys the approved static Phase 1 build without repository secrets or backend expansion, and hands an exact HTTPS URL to Plan 01-17.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md` when done and include the exact production URL.
</output>
