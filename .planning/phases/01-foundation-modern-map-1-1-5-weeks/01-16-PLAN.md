---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "16"
type: execute
wave: 9
depends_on: ["01-15"]
files_modified:
  - README.md
autonomous: true
requirements: [F5.1, NFR5]
user_setup:
  - service: vercel
    why: "Deploy the static Vite app to the locked production host and obtain a shareable URL"
    account_setup:
      - "A Vercel account with permission to create/link the CountriesIRL project"
    dashboard_config: []
must_haves:
  truths:
    - "The production Vercel URL serves the same verified static Vite app with its bundled GeoJSON and no backend/environment requirements."
    - "The shareable URL loads successfully and supports the core color/save/export workflow in production."
    - "README records the verified production URL and deployment command without advertising deferred features."
  artifacts:
    - path: "README.md"
      provides: "Verified production URL and deployment instructions"
      contains: "https://"
  key_links:
    - from: "Vercel production deployment"
      to: "dist output"
      via: "Vite static build"
      pattern: "npm run build"
    - from: "README.md"
      to: "production URL"
      via: "verified link"
      pattern: "vercel.app"
---

<objective>
Deploy the approved Phase 1 static build to Vercel, verify the production URL, and record it in project documentation.

Purpose: Complete the locked shareable-delivery outcome with no backend, secrets, or runtime Vercel dependency.
Output: Live production URL and README update.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@package.json
@vite.config.ts
@public/data/europe-modern.geojson
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and deploy the static Vite app to Vercel production</name>
  <files>README.md</files>
  <read_first>
    package.json
    vite.config.ts
    README.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
  </read_first>
  <action>Run the full automated gate and build, then use the Plan 01-approved official CLI with `npx --yes vercel@56.4.1 --prod`. Link/create the CountriesIRL project as a static Vite deployment with build command `npm run build` and output directory `dist`. Do not add Vercel functions, backend routes, `vercel.json`, required environment variables, analytics, authentication, or product-code use of Vercel variables. If CLI authentication is unavailable, stop at the dynamic authentication gate, have the user authorize Vercel, then retry the same command. Capture the resulting HTTPS production URL and verify the root page plus `/data/europe-modern.geojson` return successful responses. This implements D-08 and D-60.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:run &amp;&amp; npm run build &amp;&amp; npx --yes vercel@56.4.1 --prod</automated>
  </verify>
  <acceptance_criteria>
    - Full local gate exits 0 before deployment.
    - Official Vercel CLI 56.4.1 produces an HTTPS production URL.
    - Root app and bundled GeoJSON asset return successful production responses.
    - No `.env.local`, server function, backend, or Vercel environment-variable dependency is introduced.
  </acceptance_criteria>
  <done>The verified static Phase 1 build is live on a shareable Vercel production URL.</done>
</task>

<task type="auto">
  <name>Task 2: Verify production behavior and publish the URL in README</name>
  <files>README.md</files>
  <read_first>
    README.md
    public/data/README.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
  </read_first>
  <action>Open or request the production URL and verify title, modern Europe asset load, one-country selection/color, local save listing, and PNG export activation on the deployed origin. Update README with the exact verified URL, deployment command, and statement that persistence remains local per browser/origin. Retain the accurate Phase 1 feature/deferred-scope boundaries from Plan 14. Run one final full gate after the documentation edit.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:run &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - README contains the exact reachable HTTPS production URL.
    - Production title and bundled map asset load successfully.
    - Core selection/color/save/export controls are present on production.
    - README states saves are local to the production origin/browser.
    - Final lint, tests, and build exit 0.
  </acceptance_criteria>
  <done>Phase 1 is publicly reachable, documented, and still passes every local production gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repository → Vercel CLI/CDN | Build artifacts and metadata leave the developer machine. |
| Public URL → browser users | The static app and bundled data become internet-accessible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `vercel@56.4.1` CLI | mitigate | Plan 01 human legitimacy approval, exact transient version, no lookalike package. |
| T-01-41 | Spoofing | Production URL | mitigate | Use CLI-returned HTTPS URL and verify root/data responses before documenting. |
| T-01-42 | Information Disclosure | Deployment configuration | mitigate | No secrets/env vars/backend; publish only static app and non-sensitive public map data. |
| T-01-43 | Tampering | Production vs local build | mitigate | Deploy only after full gate; verify core behavior and static asset on production. |
</threat_model>

<verification>
The full local gate passes, Vercel returns an HTTPS URL, production root/data respond, and README records that exact URL.
</verification>

<success_criteria>
A shareable Vercel production URL serves the complete approved Phase 1 app with no backend or environment setup, and documentation points to the verified deployment.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-16-SUMMARY.md` when done.
</output>
