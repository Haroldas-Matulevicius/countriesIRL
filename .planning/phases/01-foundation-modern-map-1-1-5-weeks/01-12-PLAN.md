---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "12"
type: execute
wave: 6
depends_on: ["01-06", "01-07", "01-08", "01-10", "01-11"]
files_modified:
  - src/hooks/useResponsiveLayout.ts
  - src/main.tsx
  - src/App.tsx
autonomous: true
requirements: [F1.1, F1.2, F1.3, F1.4, F1.5, F1.6, F5.1, F6.1, F6.2, NFR5, NFR6, NFR7, NFR11]
must_haves:
  truths:
    - "A creator can load the app, select one or many countries, apply colors, undo/redo/reset, save/load/delete, and export through one coherent workspace."
    - "Exactly one viewport-appropriate workspace tree is mounted: desktop DOM order is map then controls, while tablet/mobile DOM order is actions, map, selection/color, then country list."
    - "Onboarding dismissal persists through the storage utility and Show Help can reopen guidance without clearing the persisted dismissal."
    - "Map data, reducer, storage, export, onboarding, responsive layout, and feedback use narrow interfaces with no duplicate selection or raw boundary calls in App."
  artifacts:
    - path: "src/hooks/useResponsiveLayout.ts"
      provides: "matchMedia-driven desktop versus compact composition state"
      exports: ["useResponsiveLayout"]
    - path: "src/main.tsx"
      provides: "React 18 StrictMode bootstrap and MapStateProvider composition"
    - path: "src/App.tsx"
      provides: "Complete one-way Phase 1 application orchestration with one active workspace"
      exports: ["default"]
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useResponsiveLayout.ts"
      via: "conditional rendering of one active workspace tree"
      pattern: "useResponsiveLayout"
    - from: "src/App.tsx"
      to: "src/hooks/useLocalStorage.ts"
      via: "saved-map and onboarding-dismissal persistence"
      pattern: "useLocalStorage"
    - from: "src/App.tsx"
      to: "src/utils/export.ts"
      via: "connected active map export source"
      pattern: "exportMapPng"
---

<objective>
Wire every completed Phase 1 subsystem into one responsive browser application with viewport-correct DOM/focus order and persisted onboarding dismissal.

Purpose: Turn the independently verified contracts/components into the complete creator product without CSS reordering, duplicated workspace trees, duplicated state, or bypassed storage/export boundaries.
Output: A matchMedia layout hook, React bootstrap, and fully composed App. Styling imports intentionally remain absent until Plan 01-13.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/providers/MapStateProvider.tsx
@src/hooks/useMapState.ts
@src/hooks/useGeoData.ts
@src/hooks/useLocalStorage.ts
@src/components/MapWorkspace.tsx
@src/components/MapCanvas.tsx
@src/components/SelectionPanel.tsx
@src/components/ColorPicker.tsx
@src/components/CountryList.tsx
@src/components/Controls.tsx
@src/components/SaveLoad.tsx
@src/components/AppHeader.tsx
@src/components/OnboardingBanner.tsx
@src/components/ToastRegion.tsx
@src/utils/export.ts
@src/utils/storage.ts
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement matchMedia-based responsive composition state</name>
  <files>src/hooks/useResponsiveLayout.ts</files>
  <read_first>
    src/hooks/useResponsiveLayout.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/coding-rules/frontend.md
  </read_first>
  <action>Create `useResponsiveLayout` around `window.matchMedia('(min-width: 1200px)')` and return a strict `'desktop' | 'compact'` union. Read the initial match synchronously, subscribe with `MediaQueryList.addEventListener('change', ...)`, remove the listener on cleanup, and avoid resize polling. `desktop` means the map-first DOM order required at 1200px and wider; `compact` means the tablet/mobile actions-first DOM order below 1200px. The hook controls React composition, not CSS visibility or order.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The hook uses exactly one `(min-width: 1200px)` MediaQueryList.
    - Change listener setup and cleanup are symmetric.
    - Return type is the strict desktop/compact union.
    - No `window.resize` listener, DOM measurement loop, or CSS-order assumption exists.
  </acceptance_criteria>
  <done>React can choose the correct workspace DOM order from one stable matchMedia source.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap React 18 StrictMode without premature style imports</name>
  <files>src/main.tsx</files>
  <read_first>
    src/main.tsx
    index.html
    src/providers/MapStateProvider.tsx
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
  </read_first>
  <action>Create the React 18 `createRoot` entry, verify `#root` exists, and wrap `App` in `React.StrictMode` and `MapStateProvider` per D-01, D-02, D-05, D-13, and D-56. Do not import `theme.css`, `App.css`, `MapCanvas.css`, or `Controls.css` in this plan; Plan 01-13 owns stylesheet wiring after every component class contract exists.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `createRoot` mounts App to `#root` inside StrictMode and MapStateProvider.
    - No stylesheet import appears in `src/main.tsx` or `src/App.tsx`.
    - No duplicate root/provider or default React import exists solely for JSX.
  </acceptance_criteria>
  <done>The application boots through the locked React provider hierarchy while style wiring remains sequenced to Plan 01-13.</done>
</task>

<task type="auto">
  <name>Task 3: Compose one active responsive creator workspace and persisted help state</name>
  <files>src/App.tsx</files>
  <read_first>
    src/App.tsx
    src/hooks/useResponsiveLayout.ts
    src/hooks/useMapState.ts
    src/hooks/useGeoData.ts
    src/hooks/useLocalStorage.ts
    src/components/MapWorkspace.tsx
    src/components/SelectionPanel.tsx
    src/components/ColorPicker.tsx
    src/components/CountryList.tsx
    src/components/Controls.tsx
    src/components/SaveLoad.tsx
    src/components/AppHeader.tsx
    src/components/OnboardingBanner.tsx
    src/components/ToastRegion.tsx
    src/utils/export.ts
    src/utils/storage.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
  </read_first>
  <action>Compose the complete app with stable `useCallback` handlers and one provider-owned selected-ID set. Use `useResponsiveLayout` to conditionally render exactly one workspace branch: desktop renders map workspace before the control column; compact renders action controls, map workspace, selection/color controls, then country list. Do not render both branches and hide one with CSS, and do not use CSS `order` to repair focus order. Keep shared hooks/state above the branch so viewport changes preserve colors/history/selections while remounting only the active presentation. Feed map loading/warning/error states into the shell and disable map-dependent controls during loading/fatal states. Wire map single selection and list bulk selection to the same provider actions; wire preset/custom apply, history/reset, SaveLoad LOAD_STATE, map focus restoration, and one connected active export-source ref. Initialize help visibility from persisted onboarding dismissal exposed by the storage utility/hook; `Dismiss Help` and `Start Coloring` write `countriesirl_onboarding_dismissed`, while `Show Help` reopens guidance without deleting that marker. Manage export busy/success/failure/retry through ToastRegion. Keep storage failures local to persistence while editing/export continue. This fulfills D-11, D-12, D-13, D-14, D-15, D-25, D-26, D-27, D-28, D-29, D-30, D-31, D-32, D-33, D-35, D-38, D-40, D-41, D-42, D-43, D-44, D-45, D-46, D-47, D-48, D-49, and D-51. Do not import styles, expose deferred controls, make runtime third-party requests, add a service worker, or use native alerts.</action>
  <verify>
    <automated>npm run test:run &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - App renders exactly one desktop or compact workspace tree from matchMedia state.
    - Desktop and compact JSX orders exactly match the UI-SPEC DOM/focus requirements.
    - Hooks/state are shared above the branch; viewport changes preserve creator work.
    - Onboarding dismissal reads/writes only through the storage utility/hook and persists independently of saved maps.
    - There is one selected-ID collection and one active export-source ref.
    - No stylesheet import, service worker, runtime third-party request, deferred control, or native alert appears.
    - Tests, lint, and production build exit 0.
  </acceptance_criteria>
  <done>The complete functional app is wired with one viewport-correct DOM tree, persisted onboarding dismissal, and no styling-sequence or state-duplication defect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Media query → React tree | Viewport changes select which focus-order-correct workspace is mounted. |
| Storage → onboarding visibility | Browser-local configuration controls first-use help state. |
| Hooks/utilities → App orchestration | Boundary results become user actions and messages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Full dependency graph | mitigate | Exact package pins/lockfile and prior package checkpoint. |
| T-01-30 | Tampering | Duplicate/bypassed state | mitigate | Shared hooks above one active branch; no raw storage/GeoJSON/D3 mutation or duplicate selection. |
| T-01-31 | Information Disclosure | Raw technical errors | mitigate | Typed outcomes map to approved copy and ToastRegion; exceptions are never rendered. |
| T-01-32 | Denial of Service | Reentrant export/actions | mitigate | Loading/fatal/busy disabled states and stable callbacks. |
| T-01-44 | Spoofing | Hidden duplicate responsive workspace | mitigate | Conditional React composition mounts exactly one branch; CSS does not reorder or hide a duplicate tree. |
</threat_model>

<verification>
Run the full test suite, lint, and production build. Viewport DOM order, preserved state, onboarding persistence, and browser behavior are blocking checks in Plan 01-15.
</verification>

<success_criteria>
The app builds and exposes one coherent creator workflow with viewport-correct DOM/focus order, persisted onboarding dismissal, exact export integration, and no premature style wiring.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-12-SUMMARY.md` when done.
</output>
