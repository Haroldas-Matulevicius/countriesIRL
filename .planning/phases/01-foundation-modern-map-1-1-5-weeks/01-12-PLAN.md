---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "12"
type: execute
wave: 6
depends_on: ["01-06", "01-07", "01-08", "01-10", "01-11"]
files_modified:
  - src/main.tsx
  - src/App.tsx
autonomous: true
requirements: [F1.1, F1.2, F1.3, F1.4, F1.5, F1.6, F5.1, F6.1, F6.2, NFR5, NFR6]
must_haves:
  truths:
    - "A creator can load the app, select one or many countries, apply colors, undo/redo/reset, save/load/delete, and export through one coherent workspace."
    - "Map data, reducer, storage, export, onboarding, and feedback are wired through their narrow interfaces with no duplicate selection or raw boundary calls in App."
    - "Loading and fatal map states disable dependent controls while preserving the approved application shell."
  artifacts:
    - path: "src/main.tsx"
      provides: "React 18 StrictMode bootstrap and MapStateProvider composition"
    - path: "src/App.tsx"
      provides: "Complete one-way Phase 1 application orchestration"
      exports: ["default"]
  key_links:
    - from: "src/main.tsx"
      to: "src/providers/MapStateProvider.tsx"
      via: "root provider wrapper"
      pattern: "MapStateProvider"
    - from: "src/App.tsx"
      to: "src/utils/export.ts"
      via: "connected map export source"
      pattern: "exportMapPng"
    - from: "src/App.tsx"
      to: "src/hooks/useLocalStorage.ts"
      via: "SaveLoad modal orchestration"
      pattern: "SaveLoad"
---

<objective>
Wire every completed Phase 1 subsystem into the browser application and creator workflow.

Purpose: Turn the independently verified contracts/components into one complete vertical product without duplicating state or bypassing boundaries.
Output: React bootstrap and a fully composed `App` supporting all locked Phase 1 interactions and states.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/providers/MapStateProvider.tsx
@src/hooks/useMapState.ts
@src/hooks/useGeoData.ts
@src/hooks/useLocalStorage.ts
@src/components/MapWorkspace.tsx
@src/components/MapCanvas.tsx
@src/components/SelectionPanel.tsx
@src/components/ColorPicker.tsx
@src/components/CountryList.tsx
@src/components/Controls.tsx
@src/components/SaveLoad.tsx
@src/components/AppHeader.tsx
@src/components/OnboardingBanner.tsx
@src/components/ToastRegion.tsx
@src/utils/export.ts
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap React 18 StrictMode with the map-state provider</name>
  <files>src/main.tsx</files>
  <read_first>
    src/main.tsx
    index.html
    src/providers/MapStateProvider.tsx
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
  </read_first>
  <action>Create the React 18 `createRoot` entry, verify `#root` exists, wrap `App` in `React.StrictMode` and `MapStateProvider`, and import the global theme/application styles only from the bootstrap/app boundary. Use the automatic JSX runtime and strict explicit behavior per D-01, D-02, D-05, D-09, D-13, and D-56.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `createRoot` from React DOM 18 mounts `App` to `#root`.
    - `MapStateProvider` wraps `App` inside StrictMode.
    - No duplicate root/provider or default React import exists solely for JSX.
    - Lint and TypeScript exit 0.
  </acceptance_criteria>
  <done>The application boots through the locked React 18 provider hierarchy.</done>
</task>

<task type="auto">
  <name>Task 2: Compose the complete modern-map creator workflow</name>
  <files>src/App.tsx</files>
  <read_first>
    src/App.tsx
    src/hooks/useMapState.ts
    src/hooks/useGeoData.ts
    src/hooks/useLocalStorage.ts
    src/components/MapWorkspace.tsx
    src/components/SelectionPanel.tsx
    src/components/ColorPicker.tsx
    src/components/CountryList.tsx
    src/components/Controls.tsx
    src/components/SaveLoad.tsx
    src/components/AppHeader.tsx
    src/components/OnboardingBanner.tsx
    src/components/ToastRegion.tsx
    src/utils/export.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
  </read_first>
  <action>Compose `AppHeader`, persistent/reopenable onboarding, action controls, map workspace/canvas/tooltip, selection panel, color picker, country list, lazy save/load modal, and toast region using stable `useCallback` handlers. Consume one `useMapState` selected-ID set; do not create duplicate selection state. Feed `useGeoData` ready/warning/error states into the shell and disable all map-dependent controls during loading/fatal states. Wire map single selection and list bulk selection to the same provider actions; preset/custom apply to one bulk reducer action; history/reset use derived availability. Wire SaveLoad load results to LOAD_STATE and map focus restoration. Maintain a connected HTML map-source ref for `exportMapPng`; Controls owns no export logic, App manages `isExporting`, exact success/failure copy, retry only after settlement, and selection-neutral export. Keep storage failures local to persistence while editing/export continue. This fulfills D-11 through D-15, D-25 through D-33, D-35, D-38, D-40 through D-48, and D-51. Do not expose any deferred control or native alert.</action>
  <verify>
    <automated>npm run test:run &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - App imports and renders every required core module from D-12.
    - There is one provider-owned selected-ID collection and one connected export-source ref.
    - Loading/fatal states disable dependent actions; storage failure does not disable export/editing.
    - Export busy/success/failure, reset, save/load/delete, and color operations route through ToastRegion.
    - No historical, centering, zoom, legend, SVG export, cloud, auth, or sharing controls appear.
    - Tests, lint, and production build exit 0.
  </acceptance_criteria>
  <done>The full Phase 1 application workflow is connected end to end with all locked behaviors and no deferred-scope leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hooks/utilities → App orchestration | Boundary results are translated into user actions and messages. |
| App refs → export and focus | Connected DOM references trigger capture and keyboard focus changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Full dependency graph | mitigate | Exact package pins/lockfile and prior package checkpoint. |
| T-01-30 | Tampering | Duplicate/bypassed state | mitigate | App consumes narrow hooks only; no raw storage/GeoJSON/D3 mutation or duplicate selection. |
| T-01-31 | Information Disclosure | Raw technical errors | mitigate | Map typed results to approved user copy and ToastRegion; never render exceptions. |
| T-01-32 | Denial of Service | Reentrant export/actions | mitigate | Loading/fatal/busy disabled states and stable callbacks. |
</threat_model>

<verification>
Run the full automated test suite, lint, and production build. Integrated browser behavior is approved in Plan 15.
</verification>

<success_criteria>
The app builds and exposes one coherent creator workflow for modern map coloring, history, persistence, and exact PNG export with all locked error/help states.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-12-SUMMARY.md` when done.
</output>
