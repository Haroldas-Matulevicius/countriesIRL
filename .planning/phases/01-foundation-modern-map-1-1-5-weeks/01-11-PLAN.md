---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "11"
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified:
  - src/utils/export.ts
  - src/utils/export.test.ts
autonomous: true
requirements: [F5.1, F5.3, NFR4]
must_haves:
  truths:
    - "Every downloaded PNG is exactly 1080×1080, opaque white, map-only, and independent of device pixel ratio or dark theme."
    - "The export pipeline uses a 540×540 HTML frame at scale 2, asserts dimensions, and never passes SVG directly to html2canvas."
    - "Blob, object URL, anchor, and temporary DOM are cleaned on both success and failure."
  artifacts:
    - path: "src/utils/export.ts"
      provides: "Deterministic html2canvas PNG pipeline"
      exports: ["exportMapPng", "createExportFilename"]
    - path: "src/utils/export.test.ts"
      provides: "Dimension, filename, failure, and cleanup tests"
  key_links:
    - from: "src/utils/export.ts"
      to: "html2canvas"
      via: "HTML export frame capture"
      pattern: "html2canvas"
    - from: "src/utils/export.ts"
      to: "URL.revokeObjectURL"
      via: "finally cleanup"
      pattern: "revokeObjectURL"
---

<objective>
Implement and test the deterministic exact-size PNG export utility before application controls invoke it.

Purpose: Resolve the highest-risk export type/dimension/cleanup contract with pure tests and explicit browser boundaries.
Output: `exportMapPng`, filename helper, exact-size assertions, blob download, and failure cleanup tests.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/constants/config.ts
@src/constants/colors.ts
@src/types/ui.ts
@.planning/coding-rules/general.md
@.planning/coding-rules/export.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md

<interfaces>
`exportMapPng(source: HTMLElement, date?: Date): Promise<ExportResult>` clones the current SVG from a connected HTML source into a temporary fixed-light export frame. `createExportFilename(date)` returns exactly `CountriesIRL_<YYYY-MM-DD>.png`. Expected operation failures return/reject in a form App can translate to the approved toast copy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic export framing, encoding, download, and cleanup</name>
  <files>src/utils/export.ts, src/utils/export.test.ts</files>
  <read_first>
    src/utils/export.ts
    src/utils/export.test.ts
    src/constants/config.ts
    src/constants/colors.ts
    src/types/ui.ts
    .planning/coding-rules/export.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
  </read_first>
  <behavior>
    - Capture options are width/height 540, scale 2, white background, and deterministic window dimensions.
    - Canvas dimensions other than exactly 1080×1080 fail before encoding/download.
    - Filename is `CountriesIRL_YYYY-MM-DD.png`.
    - Null blob and html2canvas rejection surface failure while removing temporary DOM and revoking any created URL.
    - Export clone contains map only and removes editor hover/focus/selected classes/attributes while preserving user fills and neutral borders.
  </behavior>
  <action>Write failing tests with mocked html2canvas, canvas blob conversion, URL, anchor, and temporary DOM helpers, then implement D-06, D-40, D-41, D-42, D-43, D-44, D-45, D-46, D-54, and D-57. Accept an `HTMLElement` source, locate/clone only the SVG map, place it in an offscreen 540×540 HTML frame with fixed white/light export values, `viewBox="0 0 1080 1080"`, and no header/label/tooltip/toast/modal/onboarding/selection/focus/hover state. Capture with html2canvas width/height/windowWidth/windowHeight 540, scale 2, and `backgroundColor: '#FFFFFF'`; assert exact 1080×1080 canvas dimensions. Encode with `toBlob('image/png')`, download through an object URL and temporary anchor, and remove/revoke everything in `finally`. Keep all resources same-origin and use the exact date filename. Never call `html2canvas` with `SVGSVGElement`, use `toDataURL`, or rely on devicePixelRatio.</action>
  <verify>
    <automated>npm run test:run -- src/utils/export.test.ts &amp;&amp; npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert 540×540 at scale 2 and exact 1080×1080 output dimensions.
    - Tests cover date filename, mismatch, null blob, html2canvas rejection, and cleanup on every path.
    - Utility accepts `HTMLElement`, not `SVGSVGElement`.
    - Export clone strips editor-only state and applies fixed white/neutral map styling.
    - Tests, lint, and strict TypeScript exit 0.
  </acceptance_criteria>
  <done>The export utility deterministically produces or visibly fails an exact 1080×1080 map-only PNG with complete cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live DOM → canvas | Editor DOM is cloned and rasterized. |
| Canvas → downloaded file | Browser-generated binary is exposed to the filesystem. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | html2canvas package | mitigate | Exact audited package and lockfile; no substitute exporter. |
| T-01-27 | Information Disclosure | Export subtree | mitigate | Clone map SVG only; exclude UI, saved names, toasts, help, and external resources. |
| T-01-28 | Denial of Service | Canvas/Blob failure | mitigate | Exact bounded dimensions, explicit null/rejection handling, busy lock downstream, finally cleanup. |
| T-01-29 | Tampering | Object URL lifecycle | mitigate | Revoke created URL and remove anchor/frame on all paths. |
</threat_model>

<verification>
Run focused export tests, lint, and strict TypeScript; pixel dimensions, opacity, appearance, and under-3-second behavior are manually verified in UAT.
</verification>

<success_criteria>
The tested pipeline can only succeed with a 1080×1080 white map-only PNG and cannot leak temporary DOM/object URLs after errors.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-11-SUMMARY.md` when done.
</output>
