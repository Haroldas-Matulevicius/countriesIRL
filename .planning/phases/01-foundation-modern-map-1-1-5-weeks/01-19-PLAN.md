---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "19"
type: execute
wave: 10
depends_on: ["01-14"]
files_modified:
  - src/components/ColorPicker.tsx
  - src/components/ColorPicker.test.tsx
autonomous: true
gap_closure: true
requirements: [F1.3, F1.4, NFR5, NFR11]
must_haves:
  truths:
    - "Every preset color button has a truthful native disabled state when zero countries are selected."
    - "Preset disabled behavior matches the already-disabled custom input and Apply Custom Color button."
    - "Focused component tests prevent zero-selection preset activation regressions without adding a test dependency."
  artifacts:
    - path: "src/components/ColorPicker.tsx"
      provides: "Explicit native disabled state on each preset swatch"
      contains: "disabled={controlsDisabled}"
    - path: "src/components/ColorPicker.test.tsx"
      provides: "Zero-selection native disabled-state regression coverage"
  key_links:
    - from: "src/components/ColorPicker.tsx"
      to: "selectedIds"
      via: "controlsDisabled applied to every preset/custom control"
      pattern: "selectedCount === 0|disabled={controlsDisabled}"
---

<objective>
Close the zero-selection color-control defect by making every preset swatch explicitly and natively disabled, with focused regression tests.

Purpose: Ensure visible/native button state truthfully matches the no-selection guard and custom color controls.
Output: Minimal ColorPicker change plus one dependency-free component test file.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@src/components/ColorPicker.tsx
@src/hooks/useMapState.ts
@src/providers/MapStateProvider.tsx
@src/constants/colors.ts
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit preset disabled semantics and focused component coverage</name>
  <files>src/components/ColorPicker.tsx, src/components/ColorPicker.test.tsx</files>
  <read_first>
    .planning/coding-rules/general.md
    .planning/coding-rules/frontend.md
    src/components/ColorPicker.tsx
    src/hooks/useMapState.ts
    src/providers/MapStateProvider.tsx
    src/constants/colors.ts
    package.json
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
  </read_first>
  <behavior>
    - With the provider's initial empty selected-ID set, all ten preset buttons render their own native `disabled` attribute/property.
    - The custom text input and Apply Custom Color button remain disabled in the same zero-selection render.
    - No preset status callback or map mutation can be activated from the zero-selection state.
    - Existing selected-country color application behavior remains unchanged.
  </behavior>
  <action>After reading `general.md` and then `frontend.md` in that mandatory order, write `src/components/ColorPicker.test.tsx` first using existing React 18 `react-dom/server` plus Vitest, wrapping ColorPicker in `MapStateProvider`; do not install Testing Library, jsdom, or another package. Render the initial zero-selection state and assert exactly ten named preset buttons plus the custom input/apply button expose native disabled markup. Then add `disabled={controlsDisabled}` directly to every preset button while retaining the fieldset disabled state and existing event guard. Do not alter palette values, selection state, custom validation, success copy, or one-action bulk coloring. This closes D-26, D-27, D-31, D-47, D-48, and D-51 for the confirmed preset-state defect.</action>
  <verify>
    <automated>npm run test:run -- src/components/ColorPicker.test.tsx &amp;&amp; npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Each of the ten preset button elements contains explicit native disabled state when selected count is zero.
    - Existing fieldset, custom input, and custom submit disabled semantics remain intact.
    - Focused test uses only already-installed React/ReactDOM/Vitest packages.
    - Focused test, lint, strict TypeScript, and build exit 0.
    - Only `ColorPicker.tsx` and `ColorPicker.test.tsx` change.
  </acceptance_criteria>
  <done>Preset and custom color controls expose consistent truthful native disabled state with focused zero-selection regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Selection state → interactive controls | Empty selection must not expose an apparently actionable color mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Existing React/Vitest toolchain | mitigate | Install nothing; use the exact lockfile and existing test runner. |
| T-01-54 | Spoofing | Preset enabled appearance/native state | mitigate | Explicit disabled attribute on every preset plus focused source/render assertion. |
| T-01-55 | Tampering | Zero-selection event dispatch | mitigate | Native disabled button and retained handler guard prevent mutation/status dispatch. |
</threat_model>

<verification>
The focused ColorPicker test, lint, strict TypeScript, and build pass; Plan 01-21 then confirms the disabled state in Chrome 150 and Edge 150.
</verification>

<success_criteria>
All preset swatches are truly disabled with no selected countries, focused tests lock the behavior, and selected-country coloring remains unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-19-SUMMARY.md` when done.
</output>
