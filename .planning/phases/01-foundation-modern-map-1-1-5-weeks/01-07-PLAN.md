---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "07"
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - src/components/SelectionPanel.tsx
  - src/components/ColorPicker.tsx
  - src/components/CountryList.tsx
autonomous: true
requirements: [F1.2, F1.3, F1.4, NFR5, NFR11]
must_haves:
  truths:
    - "Map selection and country-list bulk selection represent one visible selected set."
    - "A preset applies immediately to every selected country as one reducer action."
    - "Custom color edits remain local until one valid explicit apply action."
  artifacts:
    - path: "src/components/SelectionPanel.tsx"
      provides: "Selected count/names, mixed-color state, preview, and clear action"
    - path: "src/components/ColorPicker.tsx"
      provides: "Named preset palette and validated custom-color transaction"
    - path: "src/components/CountryList.tsx"
      provides: "Accessible alphabetical bulk-selection workflow"
  key_links:
    - from: "src/components/ColorPicker.tsx"
      to: "src/utils/colors.ts"
      via: "draft validation and normalization"
      pattern: "normalizeColor"
    - from: "src/components/CountryList.tsx"
      to: "selectedIds"
      via: "ID-keyed checkbox updates"
      pattern: "country.id"
---

<objective>
Build the complete single/bulk selection and color-application control surface against the shared state contracts.

Purpose: Make the five-countries-under-two-minutes creator workflow explicit, accessible, and history-safe.
Output: Selection summary, ten-preset/custom color picker, and alphabetical multi-select country list.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/types/map.ts
@src/constants/colors.ts
@src/utils/colors.ts
@src/hooks/useMapState.ts
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the selected-country summary and mixed-color preview</name>
  <files>src/components/SelectionPanel.tsx</files>
  <read_first>
    src/components/SelectionPanel.tsx
    src/types/map.ts
    src/hooks/useMapState.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
  </read_first>
  <action>Implement `SelectionPanel` with exact no-selection heading/body, one-country name and count, multi-country count plus first three names and `and {remaining} more`, clear-selection action, and current-color preview. Show `Mixed colors` with a neutral diagonal split when selected IDs do not share one color. Use IDs for state and lookup names only for display. Selection must be conveyed by text/check state/border rather than color alone per D-25, D-28, D-47, D-48, and D-51.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Exact `Select countries to color` heading and approved empty-state body are present.
    - One/multiple/mixed selection branches are explicit and strictly typed.
    - Clear action uses a semantic button and stable callback.
    - No display name is used as a color or selection key.
  </acceptance_criteria>
  <done>Creators always see an accurate, non-color-only summary of the shared current selection.</done>
</task>

<task type="auto">
  <name>Task 2: Build preset and explicit custom color application</name>
  <files>src/components/ColorPicker.tsx</files>
  <read_first>
    src/components/ColorPicker.tsx
    src/constants/colors.ts
    src/utils/colors.ts
    src/hooks/useMapState.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/coding-rules/frontend.md
  </read_first>
  <action>Implement exactly ten named preset swatches from UI-SPEC, each with visible color name, border, `aria-label="Apply {color name}"`, and active checkmark/outline only when every selected country has that color. Disable all apply controls with no selection. Preset activation calls one bulk-color semantic action and emits `Applied {color name} to {count} countries.` Keep custom input as local draft; validate through `normalizeColor`, preview valid drafts, show the exact inline error and `aria-invalid`/`aria-describedby` for invalid drafts, and dispatch only when `Apply Custom Color` is activated. Store/announce normalized uppercase hex and emit one action regardless of selection count per D-26, D-27, D-30, D-33, D-48, and D-51. Do not use native alert feedback.</action>
  <verify>
    <automated>npm run test:run -- src/utils/colors.test.ts &amp;&amp; npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Ten approved preset names/values render with visible labels and aria-labels.
    - Custom edits do not call a map-state action until explicit valid apply.
    - Error copy exactly matches `Enter #RGB, #RRGGBB, or rgb values from 0 to 255.`
    - Success copy uses color name for presets and uppercase normalized hex for custom color.
    - Color tests, lint, and TypeScript exit 0.
  </acceptance_criteria>
  <done>Single or multiple selected countries receive a preset/custom color as one validated, announced history action.</done>
</task>

<task type="auto">
  <name>Task 3: Build the accessible alphabetical bulk-selection list</name>
  <files>src/components/CountryList.tsx</files>
  <read_first>
    src/components/CountryList.tsx
    src/types/map.ts
    src/hooks/useMapState.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/coding-rules/frontend.md
  </read_first>
  <action>Implement an alphabetically sorted list of valid rendered countries using stable feature IDs. Each row contains a native checkbox, full accessible country name, and current-color swatch. Checking/unchecking updates the shared selected-ID set; `Select All Countries` selects only rendered valid IDs and `Clear Selection` clears the same set. Support wrapped/truncated long names with full accessible text, 48px targets, and no pointer-only interaction. This completes D-25, D-27, D-47, and D-51 and provides the alternative access path for small countries.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Rows are sorted by display name but keyed/emitted by stable ID.
    - Native checkboxes mirror the shared selected-ID set.
    - Select-all includes only provided valid features.
    - Visible color swatches and labels do not replace checkbox/checkmark cues.
    - Lint and strict TypeScript exit 0.
  </acceptance_criteria>
  <done>Creators can reliably select one, several, or all rendered countries even when map shapes are too small to tap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User controls → bulk reducer action | One event may modify many country colors. |
| Country labels → rendered UI | External-derived names appear in list and status text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Build/test packages | mitigate | Exact lockfile and approved package identities. |
| T-01-15 | Tampering | Custom color draft | mitigate | Pure strict normalizer; explicit commit only; no CSS/HTML injection sink. |
| T-01-16 | Elevation of Privilege | Stored/display names | mitigate | React text interpolation only; no `dangerouslySetInnerHTML`. |
| T-01-17 | Denial of Service | Select-all/bulk action | accept | Europe dataset is bounded; one reducer action handles the finite rendered ID set. |
</threat_model>

<verification>
Run color tests, lint, and strict TypeScript; final integrated single/bulk interaction is covered by UAT.
</verification>

<success_criteria>
The complete selection/color workflow supports one or many countries, exact validation/copy, one-action history semantics, and accessible non-color cues.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-07-SUMMARY.md` when done.
</output>
