---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "08"
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - src/components/Controls.tsx
  - src/components/AppHeader.tsx
  - src/components/OnboardingBanner.tsx
  - src/components/ToastRegion.tsx
autonomous: true
requirements: [F1.5, F1.6, NFR5, NFR6, NFR11]
must_haves:
  truths:
    - "Undo, redo, reset, save/load, help, and export controls expose exact labels and truthful disabled/busy states."
    - "First-use guidance persists until explicit dismissal and can be reopened from Show Help."
    - "State-changing operations announce success/error through accessible live regions rather than browser alerts."
  artifacts:
    - path: "src/components/Controls.tsx"
      provides: "History, reset, persistence, and export action surface"
    - path: "src/components/AppHeader.tsx"
      provides: "Approved title/subtitle and Show Help action"
    - path: "src/components/OnboardingBanner.tsx"
      provides: "Persistent three-step first-use guidance"
    - path: "src/components/ToastRegion.tsx"
      provides: "Polite status and assertive error announcements"
  key_links:
    - from: "src/components/Controls.tsx"
      to: "canUndo/canRedo/canReset"
      via: "native disabled semantics"
      pattern: "disabled"
    - from: "src/components/OnboardingBanner.tsx"
      to: "map focus target"
      via: "Start Coloring callback"
      pattern: "Start Coloring"
---

<objective>
Build the global action, onboarding/help, and live-feedback components with exact UI contract copy and accessibility semantics.

Purpose: Make history/reset discoverable, guide first-time creators, and centralize non-blocking feedback before application wiring.
Output: Controls, header, persistent help banner, and toast/live-region components.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/hooks/useMapState.ts
@src/types/ui.ts
@.planning/coding-rules/general.md
@.planning/coding-rules/frontend.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build truthful history, reset, save/load, and export controls</name>
  <files>src/components/Controls.tsx</files>
  <read_first>
    src/components/Controls.tsx
    src/hooks/useMapState.ts
    src/types/ui.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
  </read_first>
  <action>Implement typed callback-driven controls with exact visible labels `Undo Color Change`, `Redo Color Change`, `Reset All Colors`, `Save or Load Maps`, `Export PNG`, and busy `Exporting PNG…`. Use native disabled semantics from `canUndo`, `canRedo`, `canReset`, map readiness, storage availability where applicable, and `isExporting`; prevent repeated export activation. Reset has no confirmation because it is immediately undoable and must announce `All colors reset. Use Undo Color Change to restore them.` Keep actions as text buttons with no emoji/icon-only controls. This implements D-29, D-31, D-45, D-47, D-48, and D-51; the component receives save/export callbacks rather than calling storage/html2canvas directly.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All six exact control labels exist.
    - Disabled/busy props map to native `disabled` attributes.
    - Reset has no confirmation component or callback.
    - Controls do not import localStorage or html2canvas.
    - Lint and strict TypeScript exit 0.
  </acceptance_criteria>
  <done>The action card exposes truthful, accessible controls ready for persistence/export orchestration.</done>
</task>

<task type="auto">
  <name>Task 2: Build approved header and persistent onboarding/help</name>
  <files>src/components/AppHeader.tsx, src/components/OnboardingBanner.tsx</files>
  <read_first>
    src/components/AppHeader.tsx
    src/components/OnboardingBanner.tsx
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/coding-rules/frontend.md
  </read_first>
  <action>Create `AppHeader` with exact title `CountriesIRL Map Generator`, subtitle `Color countries and export an Instagram-ready map.`, and neutral `Show Help` action. Create `OnboardingBanner` as a controlled component with explicit visible/dismiss/start callbacks and exact heading, body, three numbered steps, `Start Coloring`, and `Dismiss Help`. It remains visible until explicit dismissal, never auto-dismisses, and `Start Coloring` calls a provided focus-to-map callback. `Show Help` reopens the same non-modal banner. The component never accesses storage directly; App persists dismissal through the storage utility. Dismissal changes help UI only and never map state, implementing D-47, D-48, D-51, and the approved onboarding contract.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All exact title/subtitle/onboarding/step/action copy from UI-SPEC is present.
    - No timer, auto-dismiss effect, or direct localStorage access exists.
    - Start Coloring accepts a focus callback; dismissal does not accept map-state callbacks.
    - Components use semantic headings and buttons.
  </acceptance_criteria>
  <done>First-time and returning creators have persistent, reopenable guidance that leads focus directly to the map.</done>
</task>

<task type="auto">
  <name>Task 3: Build one-message accessible toast and status region</name>
  <files>src/components/ToastRegion.tsx</files>
  <read_first>
    src/components/ToastRegion.tsx
    src/types/ui.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/coding-rules/general.md
  </read_first>
  <action>Implement a single-visible-message `ToastRegion` driven by typed success/info/warning/error data. Use polite `role="status"` for success/information and assertive `role="alert"` for export/storage operation errors, with visible semantic text and `Dismiss Message`. Later messages replace earlier success messages; errors remain until dismissed/replaced. Expose an optional retry callback for settled export failure only. Do not use `alert()`, inject HTML, or render raw exception content. Include support for selection/color, undo/redo, reset, save/replace/load/delete, and export announcements required by D-48 and D-51.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Status and alert roles are selected by typed message severity.
    - Only one message is rendered at a time.
    - Dismiss action label is exactly `Dismiss Message`.
    - No native `alert()` or HTML injection sink exists.
  </acceptance_criteria>
  <done>All normal operation feedback can be announced accessibly without blocking browser dialogs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operation results → live region | Technical errors become user-visible text. |
| Help/action controls → focus and state callbacks | Controls can redirect focus or trigger destructive state changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Build/test packages | mitigate | Exact lockfile and package gate. |
| T-01-18 | Information Disclosure | Toast error messages | mitigate | Render approved stable copy, never raw exceptions, URLs, or stored data. |
| T-01-19 | Spoofing | Disabled control state | mitigate | Native disabled semantics sourced from reducer/export/map readiness. |
| T-01-20 | Denial of Service | Repeated export clicks | mitigate | `isExporting` lock and disabled button before callback execution. |
</threat_model>

<verification>
Run lint and strict TypeScript; integrated focus, announcements, and controls are exercised in UAT.
</verification>

<success_criteria>
The workspace has exact labeled actions, persistent onboarding/help, truthful disabled states, and accessible non-blocking feedback for every operation.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-08-SUMMARY.md` when done.
</output>
