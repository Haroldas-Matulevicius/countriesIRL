---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "20"
type: execute
wave: 10
depends_on: ["01-14"]
files_modified:
  - src/utils/export.ts
  - src/utils/export.test.ts
  - .planning/coding-rules/export.md
autonomous: true
gap_closure: true
requirements: [F5.1, F5.3, NFR4, NFR5]
must_haves:
  truths:
    - "Chrome 150 and Edge 150 receive a native PNG download without cancellation caused by immediate anchor removal or object-URL revocation."
    - "Export returns success only after a connected-anchor click succeeds and a bounded awaited browser handoff completes."
    - "The PNG remains exactly 1080×1080, opaque white, map-only, correctly named, and fully cleaned up after the safe handoff."
    - "The durable export coding rule records the connected-anchor, bounded-handoff, and finally-cleanup lifecycle."
  artifacts:
    - path: "src/utils/export.ts"
      provides: "Deferred native-download handoff and cleanup semantics"
      exports: ["exportMapPng"]
    - path: "src/utils/export.test.ts"
      provides: "Connected-click, timer/handoff/cleanup, and exact-output regression coverage"
    - path: ".planning/coding-rules/export.md"
      provides: "Durable browser download lifecycle rule and updated two-entry Last updated history"
  key_links:
    - from: "src/utils/export.ts"
      to: "anchor click and object URL lifecycle"
      via: "connected anchor plus awaited bounded handoff before remove/revoke and success return"
      pattern: "appendChild|click|setTimeout|revokeObjectURL|downloadAnchor"
    - from: "src/App.tsx"
      to: "ExportResult.ok"
      via: "existing success toast only after exportMapPng resolves"
      pattern: "PNG downloaded at 1080 × 1080"
    - from: ".planning/coding-rules/export.md"
      to: "src/utils/export.ts"
      via: "durable lifecycle invariant"
      pattern: "connected|handoff|finally|revokeObjectURL"
---

<objective>
Fix native PNG cancellation in Chromium by keeping a connected download anchor and object URL alive through a bounded browser handoff before cleanup and success resolution, then record the durable lifecycle rule.

Purpose: Stop Chrome 150 and Edge 150 from canceling a received download while preserving exact output, truthful UI feedback, and the project export invariant.
Output: Export utility lifecycle correction, unit coverage, and targeted export coding-rule update; App orchestration remains unchanged because it already announces success only for `ExportResult.ok`.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/coding-rules/general.md
@.planning/coding-rules/export.md
@src/utils/export.ts
@src/utils/export.test.ts
@src/App.tsx
@src/components/Controls.tsx
@src/types/ui.ts
@src/constants/config.ts
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Defer native download cleanup through a connected browser handoff and record the rule</name>
  <files>src/utils/export.ts, src/utils/export.test.ts, .planning/coding-rules/export.md</files>
  <read_first>
    .planning/coding-rules/general.md
    .planning/coding-rules/export.md
    src/utils/export.ts
    src/utils/export.test.ts
    src/App.tsx
    src/components/Controls.tsx
    src/types/ui.ts
    src/constants/config.ts
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-UI-SPEC.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
  </read_first>
  <behavior>
    - The anchor is appended and connected before `click()` is invoked.
    - After a successful anchor click, the export promise remains pending and the connected anchor/object URL remain live until a bounded asynchronous handoff timer completes.
    - On handoff completion, the existing `finally` removes the anchor, revokes the object URL, and removes the export frame before the caller receives the successful result.
    - If anchor click throws, export resolves failure and `finally` cleans the anchor, object URL, and frame immediately without waiting for the success handoff.
    - Existing dimension mismatch, null blob, capture rejection, filename, white background, map-only sanitization, and cleanup tests continue to pass.
    - The export coding rule states the same lifecycle and retains only the two most recent Last updated entries.
  </behavior>
  <action>After reading `general.md` and then `export.md` in that mandatory order, use fake timers to write a failing regression test around the existing fake DOM. Assert the anchor is connected when `click()` runs; after a successful click, assert the returned promise has not resolved, the anchor remains connected/not removed, and `URL.revokeObjectURL` has not run. Advance a named bounded `DOWNLOAD_HANDOFF_DELAY_MS = 100` macrotask, then assert success resolves and all temporary resources are cleaned exactly once by `finally`. Add a click-failure case proving no handoff wait occurs and `finally` immediately removes the anchor/frame and revokes the object URL. Implement a small awaited `waitForDownloadHandoff` helper using `window.setTimeout`/global `setTimeout`; call it only after the connected anchor click succeeds and before returning `{ ok: true, filename }`. Preserve the exact 540×540 scale-2 capture, 1080×1080 assertion, opaque white map-only clone, `toBlob`, filename, and typed failure reasons. Do not change App: it already emits `PNG downloaded at 1080 × 1080.` only after `exportMapPng` resolves with `ok: true`. Make a targeted correction to `.planning/coding-rules/export.md` requiring this durable lifecycle: append/connect the anchor before click, await a bounded browser handoff only after successful click, perform anchor removal/object-URL revocation/frame removal in `finally` after that handoff, and clean immediately through the same `finally` when click fails. Update the rule file's Last updated history with the current correction and retain only the two most recent entries. This fixes D-40, D-41, D-42, D-43, D-44, D-45, D-46, D-48, D-54, and D-57.</action>
  <verify>
    <automated>npm run test:run -- src/utils/export.test.ts &amp;&amp; npm run lint &amp;&amp; npm exec tsc -- -p tsconfig.app.json --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - A focused fake-timer test proves the anchor is connected at click and success cannot resolve or cleanup occur before the bounded 100ms handoff.
    - After the handoff, success resolves and anchor, object URL, and export frame are cleaned exactly once in `finally`.
    - Click failure remains a typed failure with immediate `finally` cleanup and no handoff delay.
    - Existing tests still prove exact 1080×1080 dimensions, white map-only content, filename, blob, capture, and failure behavior.
    - `.planning/coding-rules/export.md` contains the durable connected-anchor/bounded-handoff/finally-cleanup rule and only the two most recent Last updated entries.
    - Export-focused tests, lint, strict TypeScript, and build exit 0.
    - Only `src/utils/export.ts`, `src/utils/export.test.ts`, and `.planning/coding-rules/export.md` change.
  </acceptance_criteria>
  <done>The native download lifecycle survives Chromium's asynchronous handoff, click failures clean immediately, success feedback is no longer premature, and the durable export rule matches the tested implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blob/object URL → browser download manager | Native download initiation is asynchronous relative to anchor click and cleanup. |
| Export result → success toast | UI trusts the utility's success timing. |
| Product fix → coding rule | The durable project invariant must match the verified browser lifecycle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Existing html2canvas/Vitest packages | mitigate | Install nothing; preserve exact audited dependencies and lockfile. |
| T-01-56 | Denial of Service | Premature object URL revocation | mitigate | Click a connected anchor, then await a named bounded 100ms handoff before success and finally cleanup. |
| T-01-57 | Spoofing | False success announcement | mitigate | ExportResult success resolves only after successful click and handoff; App already gates toast on `ok: true`. |
| T-01-58 | Information Disclosure | Object URL retained too long | mitigate | Bounded 100ms lifetime followed by finally cleanup; click failure cleans immediately. |
</threat_model>

<verification>
Export-focused unit tests and the full static gate pass, the durable export rule is synchronized, and Plan 01-21 then verifies real native downloads in Chrome 150 and Edge 150 before the full browser matrix resumes.
</verification>

<success_criteria>
Chrome/Edge cancellation root cause is addressed by connected click initiation and bounded delayed cleanup, output remains exact and opaque/map-only, failure cleanup remains immediate, and the durable export rule records the verified lifecycle.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-20-SUMMARY.md` when done.
</output>
