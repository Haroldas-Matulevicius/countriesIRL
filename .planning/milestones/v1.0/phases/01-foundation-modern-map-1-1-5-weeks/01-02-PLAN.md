---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - vitest.config.ts
  - eslint.config.js
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - .gitignore
autonomous: true
requirements: [NFR1, NFR2, NFR4]
must_haves:
  truths:
    - "The repository has a non-destructive React 18, strict TypeScript, Vite, ESLint, and Vitest toolchain."
    - "The locked D3 and html2canvas runtime dependencies are installed at exact compatible versions."
    - "The project exposes deterministic dev, lint, test, build, and preview commands."
  artifacts:
    - path: "package.json"
      provides: "Exact Phase 1 dependencies and scripts"
      contains: "react"
    - path: "vite.config.ts"
      provides: "Vite React build configuration"
      contains: "@vitejs/plugin-react"
    - path: "eslint.config.js"
      provides: "ESLint flat configuration"
      contains: "react-hooks"
    - path: "vitest.config.ts"
      provides: "Node-environment unit-test configuration"
      contains: "environment"
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm install with exact versions"
      pattern: "save-exact"
    - from: "vite.config.ts"
      to: "@vitejs/plugin-react"
      via: "React plugin import"
      pattern: "plugin-react"
---

<objective>
Create the bounded Phase 1 dependency, Vite configuration, strict TypeScript, lint, and unit-test infrastructure without running a generator over the existing planning repository.

Purpose: Establish the locked React 18/Vite/D3/html2canvas toolchain and fast verification commands that every later plan uses.
Output: Exact dependency lockfile, Vite configuration, strict TypeScript projects, ESLint flat config, Vitest config, and ignore rules. Root `index.html` is owned by Plan 01-12 Task 2.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/CODING_RULES.md
@.planning/coding-rules/general.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the exact locked runtime and development toolchain</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    package.json
    package-lock.json
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
    .planning/coding-rules/general.md
  </read_first>
  <action>After the approved Plan 01 package gate, create `package.json` manually rather than running create-vite in the repository. Pin runtime packages exactly to `react@18.3.1`, `react-dom@18.3.1`, `d3@7.9.0`, and `html2canvas@1.4.1`. Pin development packages exactly to `vite@8.1.5`, `@vitejs/plugin-react@6.0.3`, `typescript@6.0.2`, `@types/react@18.3.31`, `@types/react-dom@18.3.7`, `@types/d3@7.4.3`, `@types/geojson@7946.0.16`, `eslint@10.7.0`, `@eslint/js@10.0.1`, `typescript-eslint@8.65.0`, `eslint-plugin-react-hooks@7.1.1`, `globals@17.7.0`, and `vitest@4.1.10`. Add scripts `dev`, `build`, `preview`, `lint`, `test`, and `test:run`; use `tsc -b && vite build` for build and non-watch `vitest run` for `test:run`. Run npm install so `package-lock.json` records exact resolved versions. This implements D-01 through D-03, D-06, D-10, D-15, D-56, D-57, and D-58; do not accept React 19, Oxlint, caret ranges, or generator-created replacements.</action>
  <verify>
    <automated>npm ls --depth=0 &amp;&amp; npm run test:run -- --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` declares React and React DOM at exactly 18.3.1.
    - `package.json` declares D3 7.9.0 and html2canvas 1.4.1.
    - Every dependency and devDependency uses an exact version without `^` or `~`.
    - `package-lock.json` exists and `npm ls --depth=0` exits 0.
    - Scripts include `dev`, `build`, `preview`, `lint`, `test`, and `test:run`.
  </acceptance_criteria>
  <done>The exact locked stack is installed with a reproducible lockfile and no unapproved scaffold defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vite and strict TypeScript projects</name>
  <files>vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json</files>
  <read_first>
    vite.config.ts
    tsconfig.json
    tsconfig.app.json
    tsconfig.node.json
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
    .planning/coding-rules/general.md
  </read_first>
  <action>Configure Vite with the official React plugin and port 5173. Configure project-reference TypeScript with strict browser/tooling projects, automatic JSX runtime, `moduleResolution: bundler`, no emit for app code, JSON modules, and no `any` escape hatches per D-01, D-02, and D-56. This task is limited to four Vite/TypeScript configuration files; do not add application components, test cases, responsive hooks, data, storage, export, or deployment behavior.</action>
  <verify>
    <automated>npm exec tsc -- -p tsconfig.node.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.app.json` contains `strict: true`, `noEmit: true`, and `jsx: react-jsx`.
    - Vite uses only the official React plugin and expected port.
    - Tooling TypeScript check exits 0.
  </acceptance_criteria>
  <done>The bounded Vite/TypeScript configuration shell is ready without owning the later application HTML entry.</done>
</task>

<task type="auto">
  <name>Task 3: Configure bounded lint, unit-test, and ignore infrastructure</name>
  <files>vitest.config.ts, eslint.config.js, .gitignore</files>
  <read_first>
    vitest.config.ts
    eslint.config.js
    .gitignore
    package.json
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-PATTERNS.md
    .planning/coding-rules/general.md
  </read_first>
  <action>Configure Vitest for pure Node tests with no watch-mode default. Configure ESLint 10 flat config for TypeScript browser code, browser globals, and React Hooks rules. Ignore `node_modules`, `dist`, local Vercel metadata, logs, and editor files; do not ignore planning documents or committed GeoJSON. This task is limited to three infrastructure files; feature tests and product source belong to later plans.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:run -- --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.js` uses flat config and applies React Hooks rules.
    - `vitest.config.ts` sets a Node test environment and non-watch execution remains available.
    - `.gitignore` excludes generated/build/local metadata but not plans or map data.
    - Lint and empty-suite test commands exit 0.
  </acceptance_criteria>
  <done>The bounded lint/test infrastructure is ready for focused feature plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | Installed packages execute during development and build. |
| HTML entry → browser runtime | The root document bootstraps all client code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs | mitigate | Plan 01 human gate, exact package pins, committed lockfile, and no postinstall-bearing substitute packages. |
| T-01-02 | Tampering | Scaffold/config files | mitigate | Create files manually; reject React 19, Oxlint, generator overwrites, and any attempt to create `public/index.html` before Plan 01-12 owns the root entry per D-15. |
| T-01-03 | Elevation of Privilege | TypeScript/lint bypass | mitigate | Strict compiler configuration and ESLint Hooks rules; no `any` or unsafe config exclusions. |
</threat_model>

<verification>
Run `npm ls --depth=0`, `npm run lint`, `npm run test:run -- --passWithNoTests`, and the tooling TypeScript check.
</verification>

<success_criteria>
The exact React 18 toolchain is reproducible, all configuration parses, and later plans have fast non-watch validation commands.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-02-SUMMARY.md` when done.
</output>
