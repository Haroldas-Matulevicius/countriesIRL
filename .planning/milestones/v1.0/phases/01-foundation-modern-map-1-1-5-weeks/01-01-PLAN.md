---
phase: 01-foundation-modern-map-1-1-5-weeks
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [NFR1, NFR2, NFR4]
must_haves:
  truths:
    - "The developer has explicitly verified the official Vitest and Vercel CLI package identities before either package is installed or executed."
    - "No [SUS] or [ASSUMED] package crosses the package-manager trust boundary without human approval."
  artifacts: []
  key_links:
    - from: "01-01 package checkpoint"
      to: "01-02 toolchain installation"
      via: "blocking dependency"
      pattern: "depends_on:.*01-01"
---

<objective>
Approve the two package identities that require a blocking supply-chain check before Phase 1 installation or deployment commands run.

Purpose: Satisfy the package-legitimacy gate recorded by research without weakening the locked Vitest test stack or Vercel delivery requirement.
Output: Human approval for `vitest` and `vercel`, or a stop signal before package execution.
</objective>

<execution_context>
@C:/Users/matul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
@.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify Vitest and Vercel CLI package legitimacy</name>
  <files>.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md, .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md</files>
  <action>Perform the blocking package-identity review below before allowing any dependent npm install or npm exec command. Preserve the approved package names and stop execution if either official identity cannot be confirmed.</action>
  <read_first>
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-RESEARCH.md
    .planning/phases/01-foundation-modern-map-1-1-5-weeks/01-CONTEXT.md
  </read_first>
  <what-built>No product code has been changed. Research marked `vitest` as [SUS] because of name similarity to Vite, and the deployment plan uses the official `vercel` CLI package, which was not present in the research audit table and is therefore treated as [ASSUMED].</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vitest and confirm the package links to the official Vitest project at https://github.com/vitest-dev/vitest and https://vitest.dev/.
    2. Open https://www.npmjs.com/package/vercel and confirm the package links to the official Vercel CLI project under the Vercel organization and https://vercel.com/docs/cli.
    3. Confirm the intended package names are exactly `vitest` and `vercel`; do not approve similarly named alternatives.
  </how-to-verify>
  <acceptance_criteria>
    - Human explicitly approves both exact package names.
    - No npm install, npm exec, or npx command for either package has run before approval.
    - D-57 and D-58 testing obligations remain intact; D-08 and D-60 deployment obligations remain intact.
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorize both package identities, or identify the package that must not be used.</resume-signal>
  <verify>
    <automated>npm view vitest name repository.url homepage --json &amp;&amp; npm view vercel name repository.url homepage --json</automated>
    <human-check>Confirm the registry metadata and official pages match the exact package identities.</human-check>
  </verify>
  <done>Human approval authorizes only the exact `vitest` and `vercel` packages for dependent plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local toolchain | Third-party package code would execute with developer-machine privileges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `vitest` and `vercel` package execution | mitigate | Blocking human verification against official npm, documentation, and source-repository identities before install or execution. |
| T-01-01 | Spoofing | Lookalike package names | mitigate | Require exact names `vitest` and `vercel`; reject similarly named packages. |
</threat_model>

<verification>
The checkpoint must be approved before any dependent plan begins.
</verification>

<success_criteria>
Both package identities are explicitly approved, preserving the exact locked test and deployment scope while preventing unreviewed package execution.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-modern-map-1-1-5-weeks/01-01-SUMMARY.md` when done.
</output>
